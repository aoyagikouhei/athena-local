# quick #24 SHOW TABLES の .metadata が本物では protobuf ではない

- 入力: issue #24。モード: standalone。ブランチ `main-24`、着手前 SHA `2fd7b31`
- ゲート: サイズ 本番コード 0 行・0 ファイル（README・CHANGELOGS・ノートのみ。≤50・3）／呼び出し元 0（≤5）／契約=文書のみで値の形は変えない／新規性=`src` に新ファイル無し／事実=#1・#5・#17 の実測の生バイトが `$HOME` に残っており手元で再導出できた／判断=issue の「次にやること」2 番目（特定できなければ README の Caveats に書く）が方針を決めている → **6 観点すべて通過**
- 時系列: 着手 21:17 ／ red（README の Caveat が「presumably encrypted」止まりで issue の 3 つの「分からないこと」に答えていないことを確認）21:20 ／ green（README・CHANGELOGS 更新）21:23 ／ コミット 21:25
- 検証: `cargo fmt --check` ok ／ `cargo clippy --all-targets --locked -- -D warnings` ok ／ `cargo test --locked` ok（ユニット 124 passed、結合 15 本すべて ok、failed 0）／ `.claude/issue-notes/24-verify-metadata-form.sh` 全項目 ok（README の数値を保存済みの実測バイト列から再導出）／ミューテーション: 期待値 233 → 232 に壊すと 15 件 NG・終了コード 1 → 落ちた
- 自己レビュー: 本番コードの分岐 0 件・エラーの出口なし・呼び出し元 0 箇所。代わりに README に書いた主張を 1 つずつスクリプトで固定し、書きすぎていた 2 文（「先頭 9 バイトはラウンド内で共通」「JDBC が 3 モードすべてで metadata を読んだ」）を実測の範囲に戻した
- 打ち切り・escalate: 提案なし（はしご不要）。issue の「実測が要る」は、#1（2026-09-16）・#5（2026-09-17）・#17（2026-09-18）の生データが `$HOME/athena-txt-measurements` ほかに残っていたため、AWS に触れずに解析できた
- 予算の実績: Bash 約 24 本、所要 約 12 分、コミット 1、サブエージェント 0 本（止めない独立レビューは docs だけの変更のため流さない）
- 出荷: ブランチ `main-24`（PR 番号は `gh pr list --head main-24` で引ける）

## 解析でわかったこと（issue の「分からないこと」への答え）

| 問い | 答え |
| --- | --- |
| この形式が何か | **特定できず**。312 文字の base64（`SHOW TBLPROPERTIES` だけ 460）で、戻すと結果の中身によらず 233 バイト（同 345）固定。先頭バイト `0x01` だけが全ラウンドで一定で、それ以降はラウンドをまたぐと変わり、同じラウンドの中でも割れることがある（2026-09-17 の 2 件）。同じ表を返す `SHOW TABLES` を 2 回実行してもバイト列が変わる。S3 のオブジェクトメタデータに手がかりは無い（`ServerSideEncryption: AES256` = SSE-S3 のみで、クライアント側暗号化のヘッダは無い）。暗号化らしいという以上は言えない |
| `SHOW` 系すべてか `SHOW TABLES` だけか | `SHOW TABLES` / `SHOW DATABASES` / `SHOW COLUMNS` / `SHOW PARTITIONS` / `SHOW TBLPROPERTIES` の 5 文（#5 が 2026-09-16 の生データで確認済み）。**`SHOW CREATE TABLE` は例外で素の protobuf**（`DESCRIBE` と同じ）。結果本体の `.txt` は平文 |
| クライアントが読めているか | #5 の実機検証（2026-09-17）で、Athena JDBC 3.8.1 の既定 `ResultFetcher=auto` が athena-local の `SHOW TABLES` の `.txt.metadata` を実際に GET し、素の protobuf を例外なく読んだ |

`.metadata` の Content-Type が `binary/octet-stream` に割れる件は、同じラウンドで protobuf の
`SELECT 1`（`select-nocolname`）も `binary` だったので、#1 で既知の揺れであって `SHOW` 固有ではない。

## 未実測のまま残るもの

- 不透明な形式そのものの中身（復号できないので、AWS 側の仕様が公開されない限り埋まらない）
- `SHOW` 5 文それぞれを JDBC で読ませた場合の挙動（実機検証は `SHOW TABLES` のみ）
