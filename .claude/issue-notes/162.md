# issue #162 400 行を超えた operation/ の 3 ファイルのテストを子モジュールに出し、EngineDdl を改名する

## 基本情報

- issue: #162。ベースブランチ: main。ブランチ: main-162。着手前 SHA: e1b85f0（quick ノートのコミット 059d14d も src は同じ）
- 経路: implement-issue の「挙動を変えない移動・分割」（`move-refactor.md`）＋改名のフェーズ。**3 フェーズなので小規模 issue の経路ではない**（最終パス＋独立レビュー、Step 6b は必ず止まる）
- quick-implement から escalate（ゲートで変更サイズ・新規性・事実・判断が外れた。quick ノートは末尾の「quick の記録」）

## 事前に確認した事実

移動後には取れなくなる基準値（03:50 に e1b85f0 相当のツリーで実測。検証足場の期待値の正）:

| 項目 | 値 |
|---|---|
| `tools/dev.sh cargo test --locked` の総数 | 396（lib 228 ＋ 結合テスト等） |
| `operation::classification::tests` | 8 |
| `operation::target_table::tests` | 21 |
| `operation::table_format::tests` | 20 |
| `src/` 全体の `///` 行数 | 725 |
| `src/` 全体の「実測」「採取」を含む行 | 300 |
| 行数と `#[cfg(test)]` の位置 | classification.rs 532（169）／target_table.rs 631（126）／table_format.rs 457（171） |

移動単位と依存（Explore 1 から転記。落とした項目: 単独 `//` の行番号一覧＝check.sh の正規化 diff が全行を見る）:

- 3 ファイルとも `#[cfg(test)]` は 1 件だけで、`mod tests` に doc・属性は無く、直前は空行、ファイル末尾まで続く。字下げが中身に効く複数行文字列は無い（#75 の raw JSON の問題は起きない）。`<name>::tests` を外から参照する箇所は 0 件
- classification.rs: 移動 169–532、use は `use super::*;` のみ。テストは `statement_type`・`substatement_type`（pub(super)）だけを呼ぶ。`#[test]` 8、ヘルパ無し、`///` 0、「実測」18 行。本体の分岐 32
- target_table.rs: 移動 126–631、use は `use super::*;` のみ。private の `parse_qualified_name`（:91）を :470 で直接呼ぶ。親の private `use super::table_format::TargetStatement;`（:7）を super::* 越しに 33 箇所。`crate::catalog::skip_qualified_name`（:473）を直接参照。`#[test]` 21、ヘルパ `既定付き`（:501 の `///` 付き、:502–508、テストの間）。`///` 3 行、「実測」1 行。本体の分岐 16
- table_format.rs: 移動 171–457、use は `use serde_json::Value;`（テスト内 2 箇所）と `use super::*;`。private の `probe_sql`・`parse_probe_result` を直接呼ぶ。親の private `use crate::trino::{Cancel, Outcome, Trino};`（:9）を super::* 越しに使う。`#[test]` 19＋`#[tokio::test]` 1、ヘルパ `outcome_with_probe_result`（:373–379）。「実測」7 行。本体の分岐 8

文書参照と改名（Explore 2 から転記。落とした項目: design-checklist の全文＝リポジトリにある、型名の軸の例＝計画の名前の判断に使った）:

- 3 ファイルへのパス参照（CLAUDE.md:32,47、catalog.rs ほか、tools/ の実測スクリプト、tests/ の doc）は**すべて残る本体を指す**。`tools/measure/alter-substatement.sh:12` の `classification.rs:64-70` もテストより前なのでずれない。`tests/table_format.rs`（結合テスト）への参照は無関係
- 移動後に誤りになる記述は `CLAUDE.md:62`（「例外は `src/results.rs` だけ」）の 1 文だけ
- 改名の棚卸し（src のみ。tests/・README・docs/*.md・CLAUDE.md・tools は 0 件）: table_format.rs 11/15、result_output.rs 11/6、completion.rs 8/2、execution.rs 3/5、format_probe.rs 2/3（`EngineDdl`/`engine_ddl`）。本番の `engine_ddl()` 呼び出しは format_probe.rs:66 の 1 箇所。束縛名 `engine_ddl` が format_probe・execution・completion・result_output にある。doc の言及は table_format.rs:35,43-44、result_output.rs:23,167、execution.rs:244
- decisions.md:119（#160 の文中の型名）・:121（#151「改名しない」）。`decisions.md:6` の規約（覆った判断は新しいほうに置き換えて「#N で変更」）で両方書き換える。measurements/ には 0 件
- `FormatOverride`／`format_override` は衝突 0 件

## 設計判断

| # | 論点 | 決定 | 理由 |
|---|---|---|---|
| 1 | 範囲 | classification.rs・target_table.rs に加えて table_format.rs（457 行）のテストも出す（ユーザー選択 03:48） | `docs/dev/decisions.md:221` は全体（本体＋テスト）で 400 行を測る。同じ形・同じ足場で検証できる |
| 2 | EngineDdl の改名 | この issue で改名する（ユーザー選択 03:48）。名前は計画で候補を出して Step 6b で聞く | issue が検討を求めている。#151 の「改名しない」（decisions.md:121）を覆すので、その行も直す |
| 3 | テストだけのファイルの行数 | 400 行の基準を当てない | decisions.md:221（#75） |
| 5 | 名前 | 型 `FormatOverride`・関数と束縛名 `format_override`・定数 `DDL_OVERRIDE_CONTENT_TYPE`（ユーザー選択 05:03 までに） | 定数は DDL の腕だけの application なので型名に揃えない（R2-1） |
| 4 | 置き場所の形 | `<name>.rs` + `<name>/tests.rs`（`mod.rs` 無し。末尾に `#[cfg(test)]\nmod tests;`） | #75 の `src/results.rs:291` と同じ形。issue 本文どおり |

## 計画

Plan の下書き（番号 1〜13）から転記。落とした項目: 9-4（呼び出しを `crate::` のフルパスに書き換えるミューテーション。移動の検証にならない）、12 の「tests.rs の下書きを先に fmt に通す」（足場の受け入れ判定 (c) で target_table を実際に移して fmt に通し ok を確認済み）。統括役が直した点: 7 の doc 文案（経緯はコードに残さず decisions.md に置く）、9 のミューテーションを 3 ファイルそれぞれ「移動先のテストが本体を見ている」に揃えた。

**フェーズ 1: 移動（1 コミット）**

| 元ファイル | 残す | `#[cfg(test)]` の次行を `mod tests;` に | 移す（4 桁字下げ解除） | 捨てる | 見込み |
|---|---|---|---|---|---|
| classification.rs | 1–169 | 170 | 171–531 | 532 `}` | 本体 170／tests.rs 約 363 |
| target_table.rs | 1–126 | 127 | 128–630（`既定付き` 501–508 込み） | 631 `}` | 本体 127／tests.rs 約 505 |
| table_format.rs | 1–171 | 172 | 173–456（`use serde_json::Value;` と `outcome_with_probe_result` 込み） | 457 `}` | 本体 172／tests.rs 約 286 |

- 移す単位は `mod tests { ... }` の中身全体（行範囲の中に `///` の取りこぼしは起きない。中の `///` はすべてブロック内）
- `use`: 移した `use` 行をそのまま（classification・target_table は `use super::*;` のみ、table_format は `Value` と `super::*`）。**可視性の変更 0 件・`use` の追加 0 件**
- `//!`（「実測」「採取」を入れない。decisions.md:226）: `//! classification.rs のユニットテスト。`／`//! target_table.rs のユニットテスト。`／`//! table_format.rs のユニットテスト（結合テストは tests/table_format.rs）。`（結合テストに同名がある table_format だけ #75 の `results/tests.rs` と同じ注記）
- 手順: 元から削りながら切り出す（`sed -n` で取り出し → `sed -i` で削る）→ 宣言 → 字下げ解除と `//!` → build・clippy・fmt → check.sh phase1 全 ok（項目 5 の内訳が 3 ファイルとも `< }` の 1 行だけ）
- 前提表:

| 前提 | 破らないか | 保証元 |
|---|---|---|
| `use super::*` が親の private `use` を子に持ち込む（target_table.rs:7 の `TargetStatement`、table_format.rs:9 の `Cancel`/`Outcome`/`Trino`） | モジュールの位置が同じなので破らない | Rust の名前解決。#75 の `results.rs:13` の `Outcome` で実績（945b5fc）。最初のビルドで確かめる（U1） |
| private 関数は子孫モジュールから見える（`parse_qualified_name` target_table.rs:91、`probe_sql` :119・`parse_probe_result` :131 table_format.rs） | 破らない | 同上。足場の受け入れ判定 (c) で target_table を実際に移してビルド・テストが通った |
| `<name>.rs` + `<name>/tests.rs`（`mod.rs` 無し）が解決される | 先例あり | `src/results.rs:291`（#75） |
| check.sh 項目 5 の許容は `< }` の 1 行だけ | 可視性・`use` の変更 0 件なら満たす | check.sh。受け入れ判定 (c) で fmt 後の形で ok を確認 |

- ミューテーション（フェーズ 1）: (1) `classification.rs` の `mod tests;` を消す → check.sh 項目 1（388）・2 が NG／(2) `target_table/tests.rs` の `#[test]` を 1 つ消す → 項目 1・2 が NG／(3) 本体の腕を壊して移動先のテストが落ちる（3 ファイルそれぞれ）: classification の `statement_type` の DDL の腕、target_table の `parse_target_table` の既定カタログの当て方、table_format の `engine_ddl` の `(Describe, View)` を `None` に → それぞれ `<name>::tests` の該当テストが落ちる

**フェーズ 2: 移動に伴う文書の追随（1 コミット）**

- `CLAUDE.md:62` を「ユニットテストはインラインの `#[cfg(test)] mod tests` に置く。例外は本体が 400 行を超えたファイルで、テストを子モジュール `<name>/tests.rs`（`mod.rs` 無し）に出している: `src/results.rs`（#75）と `src/operation/classification.rs`・`target_table.rs`・`table_format.rs`（#162）。」に。ほかの参照はすべて残る本体を指すので直さない。README・CHANGELOG・docs/*.md は変えない（decisions.md:24）

**フェーズ 3: 改名（1 コミット）**

- 名前（Step 6b で聞く）: 推奨 `FormatOverride`／`format_override`／`FORMAT_OVERRIDE_CONTENT_TYPE`。別案 `WriteOverride`、`EngineOverride`。3 案とも衝突 0 件
- 対象: 型（table_format.rs:46）、関数（:146）、束縛名 `engine_ddl`（format_probe・execution・completion・result_output）、**定数 `ENGINE_DDL_CONTENT_TYPE`（result_output.rs:15,54）**、テスト名の接頭辞 `engine_ddl_`（table_format のテスト 4 件）、doc の言及（table_format.rs:35,43-44、result_output.rs:23,167、execution.rs:244）。腕の名前は変えない（Ddl を含まない）
- `table_format.rs:44` の「型名は DDL だけを対象にしていた頃のまま（#151 で改名はしない）。」は消す（改名で誤りになる。経緯は decisions.md に置く）。check.sh phase3 の `RENAME_DOC_ALLOW` でこの 1 行だけを名指しする
- decisions.md:119 は型名を置き換えるだけ。:121 は decisions.md:6 に従い、「`EngineDdl` の改名はしない（…）」を「#162 で `FormatOverride` に改名した」旨に置き換え、「（#151、2026-09-24。#162 で変更）」を添える
- 定数 `ENGINE_DDL_CONTENT_TYPE` は上書きのうち DDL の腕（DropTableIceberg・AlterColumnsHive）だけの Content-Type（application。SHOW CREATE TABLE・DESCRIBE の上書きは BINARY を直書き）なので、`FORMAT_OVERRIDE_CONTENT_TYPE` にすると意味がずれる（計画レビュー R2-1）。名前は Step 6b で聞く（推奨 `DDL_OVERRIDE_CONTENT_TYPE`）
- 検証（R1-1 を反映）: `tools/dev.sh RENAME_EXTRA=<定数の新名>=ENGINE_DDL_CONTENT_TYPE 'RENAME_DOC_ALLOW=型名は DDL だけを対象にしていた頃のまま|^[+-]/// （2026-09-20〜24 実測）。$' .claude/issue-notes/162-verify/check.sh phase3 <フェーズ 2 の SHA> <型名> <関数名>`（旧名に戻して rustfmt に通すと改名前と一致／旧名が大文字小文字を問わず 0 件／396 件／fmt・clippy）。04:18 に `FormatOverride`・`DDL_OVERRIDE_CONTENT_TYPE`・44 行目の削除で試し、許容なしで NG・許容ありで ok を確認
- ミューテーション（フェーズ 3）: 定数だけ改名し忘れる → phase3 項目 2 が NG（04:09 に確認済み）／改名に腕の差し替えを混ぜる → 項目 1 が NG（04:07 に確認済み）。実装後にもう一度流す

**全体**

- 直列化境界・正規化境界・境界の名前・意味論マトリクス・人間検証リスト・追加するテスト・一致台帳は該当なし
- 実機検証（Step 9）: 該当なし。フェーズ 1 は `#[cfg(test)]` の中身だけ、フェーズ 3 は識別子の綴りだけで、バイナリの挙動は変わらない（#75 の設計判断 8 と同じ）。本体の経路は check.sh 項目 1 の 396 件（結合テスト込み）が覆う
- 未検証の前提: U1（table_format の 3 つの import が super::* 越しに届く）は最初のビルドで確かめる。U2・U3 は足場の受け入れ判定 (c)（target_table を実際に移して fmt に通し phase1 ok）で target_table について解消、残り 2 ファイルは実装時のビルドと check.sh で確かめる

## フェーズの記録

### フェーズ 1: 移動

- `cargo fmt --check` に差分（classification.rs のみ）。dedent で 1 関数のシグネチャの表示幅が 100 を割り、`fn ...()\n{` が `fn ...() {` の 1 行に結合された。`cargo fmt` を適用済み
- `check.sh phase1`: 項目 1〜4・6〜8 は ok。項目 5 のみ classification が NG（上の結合で `< }` 以外の差分が残る）。target_table・table_format は想定どおり `< }` のみで ok
- ミューテーション: (1) classification.rs 末尾の `mod tests;` 削除 → 項目 1・2 NG／(2) target_table/tests.rs の `#[test]` 1 件削除 → 項目 1・2 NG／(3) 本体を壊す: classification `statement_type` の DDL 腕を UTILITY に→ `statement_type_は先頭のキーワードで決まる` ほか 1 件が落ちる、target_table `parse_target_table` の既定カタログ当て（63 行目）を `default_schema` に差し替え→ 12 件落ちる、table_format `engine_ddl` の `(Describe, View)` を `None` に→ `engine_ddl_は_describe_とビューの組み合わせを_describe_view_にし_ほかの文は上書きしない` が落ちる。3 件とも確認後に復元し build/fmt/check.sh で復旧を確認
- 計画から外れた点: classification.rs の 1 関数だけ fmt が「字下げ解除以外の変更」（2 行→1 行結合）をした。挙動は変わらないが check.sh 項目 5 の許容 `< }` に収まらない
- 統括役の判断: 案 (A)。許容リストは広げず、項目 5 の正規化で「`{` だけの行を前の行につなげる」を足した（影響はこの折り返しだけ）。3 ファイルとも ok。加えて新しい tests.rs と「e1b85f0 のブロックの字下げ解除」を直接 diff し、classification の結合 1 か所以外は完全一致を確認
- 観察: 足場の修正直後の 1 回だけ項目 1 が「合計 265」で NG になった。直後の `cargo test --locked` 単独と phase1 の再実行 2 回はすべて 396・失敗 0 で、原因は特定できていない（偽の NG 側なので安全側）
- 7-0: 消すガード・増える分岐は無し。呼び出し元は `<name>::tests` を参照する箇所 0 件。7-5b: 分岐の突き合わせは 0 件（本体の分岐数 32/16/8 は不変）、`use` の未使用は clippy で 0

### フェーズ 2: 移動に伴う文書の追随

- `CLAUDE.md:62` の「例外は `src/results.rs` だけ」を、400 行を超えたファイルの規則と 4 ファイルの一覧に直した。ほかの参照はすべて残る本体を指すので直さない（Explore 2）。README・CHANGELOG・docs/*.md は変えない（decisions.md:24）。自分で実装（1 行・1 ファイル）

### フェーズ 3: 改名

- `check.sh phase3`（5870690 起点、`FormatOverride`/`format_override`、`RENAME_EXTRA` で定数対応）: 1 回目は項目 3 が「合計 265」で NG（フェーズ 1 と同じ既知の偽 NG）、再実行ですべて ok
- 許容された差分は table_format.rs の enum 直上 doc 2 行目の削除（「型名は DDL だけを対象にしていた頃のまま（#151 で改名はしない）。」）だけ
- 旧名 grep（`git grep -niE 'engine[ _-]?ddl'`）の残りは `.claude/issue-notes/` 配下と `docs/dev/decisions.md` の新しい項目（意図して残した `EngineDdl` 1 語）のみ
- ミューテーション: (M4) result_output.rs の定数だけ旧名に戻す → 項目 2 が NG（残存箇所を正しく指摘）。(M5) `format_override` の `(Describe, View)` を `Some(FormatOverride::DescribeIceberg)` に → 項目 1 が該当行の diff で NG（項目 3・4 も連鎖で NG）。両方確認後に復元し phase3 再実行で ok を確認
- 計画から外れた点: 無し。decisions.md:119・121 は指示どおり書き換え、121 は指示の 2 項目に置き換えた

Explore 2 本（R1: 契約と足場、R2: 構造と削減）。

| # | sev | 指摘 | 判定 |
|---|---|---|---|
| R1-1 | high | phase3 の検証コマンドに `RENAME_DOC_ALLOW` の値が無く、44 行目の doc を消す正しい改名が NG になる | **採用**。値を計画に書き、実際に流して確認（04:18）。design-checklist 22 行目に #162 を足した |
| R2-1 | medium | 定数を `FORMAT_OVERRIDE_CONTENT_TYPE` にすると、BINARY 側の上書きにも使える値に読める | **採用**。名前を Step 6b で聞く。足場に `RENAME_EXTRA`（型名・関数名から決まらない別名の対）を足した |
| R2-2 | low | decisions.md:119 の型名の置き換えは外せる | **棄却**（単純さゲートでは残す側）: decisions.md は今のコードを説明する文書で、コードに無い型名が残ると新名の grep で #160 の決定にたどり着けない。直すのは 1 語 |
| — | — | 改名の棚卸し・移動単位の行番号・途中状態の CI | 指摘なし（R1 が e1b85f0 で行番号の一致を実測、R2 が基準値の一致を実測） |

## 覆した事前判断

| 元の判断 | 新しい判断 | 理由 | 影響 | 聞いたか |
|---|---|---|---|---|
| EngineDdl を改名しない（#151、decisions.md:121） | 改名する | issue #162 が検討を求め、ユーザーが選んだ | 内部の型名のみ。外部契約・挙動・運用への影響なし | 聞いた（03:48） |
| issue 本文の対象は 2 ファイル | table_format.rs を足して 3 ファイル | 全体基準で 400 行超え | なし | 聞いた（03:48） |

## 時系列

- 03:37 quick-implement 着手
- 03:42 ゲートで外れて implement-issue へ escalate（059d14d）
- 03:48 範囲（3 ファイル）と改名（この issue で）をユーザーが選択
- 03:50 移動前の基準値を実測
- 03:51 Explore 2 本（移動単位と依存／文書参照と改名の棚卸し）と、先行作業として足場 check.sh の作成（general-purpose）を起動
- 04:00 Explore 2 本が返り、Plan を起動（足場の作成は継続中）
- 04:07 足場 `162-verify/check.sh` が返る。受け入れ判定: 移動前のツリーで phase1 全 ok を自分で再確認。phase3 に 2 点を足した（旧名に戻した木を rustfmt に通してから比べる＝改名で fmt が 11 行の折り返しを変えるため。テスト総数・fmt・clippy）。実際の改名＋fmt で ok、改名に腕の差し替えを混ぜると NG を確認して src を戻した
- 04:09 Plan が返る。定数 `ENGINE_DDL_CONTENT_TYPE` が phase3 の旧名検出から漏れる指摘（U4）を採用し、足場に定数の綴りと大文字小文字を区別しない検出を足した（定数だけ改名し忘れると NG、全部改名で ok を確認）。計画を転記
- 04:10 計画レビュー（Explore 2 本: 契約と足場／構造と削減）を起動。論点に定数 `ENGINE_DDL_CONTENT_TYPE` の名前（DDL の腕だけの Content-Type で、FORMAT_OVERRIDE にすると意味がずれる）を入れた。先行作業: 無し（足場は完成、実機は該当なし）
- 04:18 計画レビューの反映を終える（R1-1 採用・R2-1 採用・R2-2 棄却）。足場に RENAME_EXTRA を足して確認
- 04:18 計画確定コミット 17c9e06。Step 6b で提示
- 05:03 名前（FormatOverride／DDL_OVERRIDE_CONTENT_TYPE）を選択、設計の承認（「進めて」）。フェーズ 1 を委譲
- 05:16 フェーズ 1 の委譲先が返る（fmt の 1 行結合で項目 5 が NG と報告）。足場の正規化を直して全 ok、差分を目視と直接 diff で確かめる
- 05:16 フェーズ 1 コミット 6c860a6。フェーズ 2 に着手
- 05:16 フェーズ 2 コミット 5870690。フェーズ 3 を委譲
- 05:22 フェーズ 3 の委譲先が返る（phase3 全 ok、M4・M5 とも NG を確認）。差分を目視

## 委譲の記録

| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| 基準値の実測 | 自分 | コマンド 1 本 |
| 調査 | 委譲（Explore 2） | |
| 検証の足場 | 委譲（general-purpose） | |
| 計画の下書き | 委譲（Plan） | |
| 足場 phase3 の修正（rustfmt で正規化、定数の綴り、総数・fmt・clippy） | 自分 | 1 ファイルの数行。受け入れ判定の中で見つけた |
| フェーズ 1 の実装 | 委譲（general-purpose） | |
| 足場 phase1 項目 5 の正規化の修正 | 自分 | 1 行。委譲先が止まって返した判断の実装 |
| フェーズ 2 | 自分 | CLAUDE.md の 1 行 |
| フェーズ 3 の実装 | 委譲（general-purpose） | |

## quick の記録

- 入力: issue #162。モード: standalone。ブランチ main-162、着手前 SHA e1b85f0
- ゲート: サイズ 移動約 860 行（classification.rs 約 360 行・target_table.rs 約 500 行）＋改名するなら EngineDdl 35 か所・本番 5 ファイル（≤50・3）／呼び出し元 35（≤5・同一モジュール。operation/ 内には収まる）／契約 通過（公開 API に出ない）・新規性 外れ（src に tests.rs を 2 つ新設。先行例は #75 の src/results/tests.rs のみ）・事実 外れ（挙動を変えない移動で red が成立しない）・判断 外れ（EngineDdl を改名するか・名前は未決）→ 外れ
- 時系列: 着手 03:37 ／ ゲート判定・質問 03:40 ／ escalate 決定・ノート 03:42
- 検証: コードに触っていないので無し
- 自己レビュー: 無し（実装前に escalate）
- 打ち切り・escalate: 提案あり（変更サイズ・新規性・事実・判断。移動・分割のため）→ 選択: implement-issue（移動・分割の経路 move-refactor.md）
- 予算の実績: Bash 約 8 本、所要 5 分、サブエージェント 0 本
- 出荷: main-162 に本ノートを 1 コミットで残し、implement-issue に引き継ぐ

## 引き継ぎ

- 対象と移動前のテスト件数（`#[test]` を grep で数えた。2026-09-25）
  - `src/operation/classification.rs` 532 行。`#[cfg(test)]` 169 行目、`mod tests {` 170 行目。テスト 8 件
  - `src/operation/target_table.rs` 631 行。`#[cfg(test)]` 126 行目、`mod tests {` 127 行目。テスト 21 件
  - issue に無い発見: `src/operation/table_format.rs` も 457 行（`#[cfg(test)]` 171 行目、テスト 20 件）で 400 行を超えている。範囲に入れるかは要判断
- 先行例: `src/results.rs:291` の `mod tests;`（`#[cfg(test)]` 付き）と `src/results/tests.rs`（`mod.rs` 無し。#75）
- EngineDdl: 定義は `src/operation/table_format.rs:46`（doc は 43〜44 行目。「型名は DDL だけを対象にしていた頃のまま（#151 で改名はしない）」）。参照はファイルごとに completion.rs 8・execution.rs 3・format_probe.rs 2・result_output.rs 11・table_format.rs 11。関数名 `engine_ddl` も同じ語を持つ（`table_format.rs:35` の doc が言及）。改名するなら関数名・doc の言及も揃うかを確かめる
- 分割の注意は `.claude/design-checklist.md` の 11・12・14・19〜22 行目（doc ブロックを単位に運ぶ、テスト件数の突き合わせ、use の配り方、検証スクリプトの自己検証、属する側の固定、非純粋変更の許容リスト、fmt 後の形での確認）
- 未確認: テストモジュール内の `use super::*` 以外の import、テストから見ている非公開項目（子モジュールに出しても `super::` 経由で見えるはずだが、#75 と同じ形かの裏取りは未実施）
- 再現手順: 無し（挙動を変えない移動）。stash 無し
