# quick #18 JDBC 3.x をつなぐには TLS 終端が要ることを README に書く
- 入力: issue #18。モード: standalone。ブランチ `main-18`、着手前 SHA `bce4bd9`
- ゲート: サイズ 0 行・0 ファイル（本番コードは触らず README / CHANGELOGS のみ。≤50・3）／呼び出し元 0（≤5）／契約: docs のみ・新規性: src に新ファイル無し・事実: #5 の実機検証（2026-09-17）の足場が scratchpad に残っており nginx.conf / keytool / JDBC プロパティを逐語で引けた・判断: 新しい既定値なし → 通過
- 時系列: 着手 10:04 ／ red 10:05（README に手順が無いことが再現。openssl の生成コマンドだけ #5 の記録に無く、自分で走らせて SAN を確認）／ green 10:07 ／ コミット 10:09
- 検証: `cargo fmt --check`・`cargo clippy --all-targets --locked -- -D warnings`・`cargo test --locked` ok（218 passed / failed 0、`test result: ok` 17 本）。README に載せた nginx 設定を `nginx:1.27.0` の `nginx -t` に通して ok。生成した証明書と鍵でそのまま起動できることも同時に確認。ミューテーション: nginx.conf から `ssl_certificate_key` の行を消す → `nginx -t` が `[emerg] no "ssl_certificate_key" is defined` で落ちた
- 自己レビュー: 分岐 0 件（文書）／エラーの出口なし／呼び出し元 0 箇所。compose 例が Usage 節の `minio` サービスを前提にしている点だけ本文で明示した。JDBC プロパティは #5 の `Jdbc.java` から、nginx の `proxy_set_header Host $http_host`（SigV4 が Host を署名する）と `client_max_body_size 0` も同じ足場から引いた
- 打ち切り・escalate: 提案なし
- 予算の実績: Bash 約 15 本、所要 5 分、コミット 1
- 出荷: standalone。ブランチ `main-18`（PR 番号は `gh pr list --head main-18` で引ける）
