# quick #27 本物は /* comment */ SHOW CREATE TABLE を実行時に弾くが athena-local は通る

- 入力: issue #27。モード: standalone。ブランチ `main-27`、着手前 SHA dd93665
- ゲート: サイズ 本番コード 0 行・0 ファイル（≤50・3。差分は README・CHANGELOG とテスト 1 本）／呼び出し元 0（≤5）／契約: 触らない・新規性: 新ファイルなし・事実: #17 の 2026-09-18 実測が出典・判断: 新しい既定値なし → 全観点通過
- 時系列: 着手 22:08 ／ red 22:10（README に実測のエラー文言と未実測の範囲が無い、CHANGELOG に Caveat の記載が無い）／ green 22:12 ／ テスト追加 22:14 ／ コミット 22:16
- 検証: `cargo fmt --check` ok、`cargo clippy --all-targets --locked -- -D warnings` ok、`cargo test --locked` ok（221 passed。追加前 220）／ミューテーション: `execution.rs` の Trino に送る SQL から `/* c */ ` を削ると、追加したテストが `["SHOW CREATE TABLE t"]` と `["/* c */ SHOW CREATE TABLE t"]` の不一致で落ちた
- 自己レビュー: 分岐 0 件（本番コードは変えていない）／エラーの出口なし／呼び出し元 0 箇所。README の Caveat が主張する athena-local 側の挙動（SQL を書き換えず Trino に投げるので通る）を tests/syntax.rs のテストで固定した
- 打ち切り・escalate: 提案なし。issue の対応案 1（README の Caveats に書く）は #17 のコミット 5087668 で既に入っていたので、残っていた CHANGELOG の記載・実測したエラー文言・未実測の範囲の明示・裏付けのテストを足した
- 予算の実績: Bash 約 14 本、所要 8 分、コミット 1、サブエージェント 0 本（本番コードを変えていないので止めない独立レビューは流さない）
- 出荷: ブランチ `main-27`（PR 番号は `gh pr list --head main-27` で引ける）
