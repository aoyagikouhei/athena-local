# quick #242 DESCRIBE <db>.<t>; の GetQueryExecution の Query から DB 名が落ちる件を測る
- 入力: issue #242。モード: standalone。ブランチ main-242、着手前 SHA 796f76d
- ゲート: サイズ 見込み 50 行超・3 ファイル超（Query と実行する文を分ける構造、表／ビューの判定の後で Query を決める配線）／呼び出し元: Store の query を読む箇所が複数（query_execution.rs・execution.rs の run・completion・result_output）／契約: GetQueryExecution の Query（本物に合わせる修正なので契約には当たらない）・新規性: 外れた（Query を実行する文から分ける新しい構造、実行中に決まる表／ビューの区別＝共有状態の Execution に触れる）・事実: 外れた（下の未実測）・判断: 無し → 外れた観点 2 つ（新規性・事実）
- 時系列: 着手 14:53 ／ 過去の生データの読み直しと提案 14:53〜14:55 ／ escalate の記録 14:56
- 検証: 無し（コードに触っていない）。過去の生データの読み直しを 1 回（下の引き継ぎ）
- 自己レビュー: 該当なし
- 打ち切り・escalate: 提案あり（新規性・事実）→ 選択: implement-issue で進める（ユーザー）
- 予算の実績: Bash 約 9 本、所要 約 3 分、サブエージェント 0 本
- 出荷: implement-issue に引き継ぐ（ブランチ main-242）

## 引き継ぎ
### issue の前提の訂正
issue 本文は「`;` を付けると DB 名が落ちる」と読めるが、過去の生データ（~/athena-*-measurements の execution.json 全件）を送った文と突き合わせると、`;` とは関係が無い:
- **表**への `DESCRIBE <db>.<t>`（11 件、#204 ほか）・`DESC <db>.<t>`（2 件）・`DESCRIBE awsdatacatalog.<db>.<t>`（3 件）・`DESCRIBE /* c */ <db>.<t>` → `DESCRIBE /* c */ <t>`・``DESCRIBE `<db>`.`<t>` `` → ``DESCRIBE `<t>` ``・`DESCRIBE <DB の大文字>.<t>`（Context の Database も大文字）・#240 の l18 `DESCRIBE <db>.<t>;` → すべて Query は修飾部分（カタログ・DB と `.`）を落とした `DESCRIBE <t>`。コメント・バッククォート・表の名前の綴りは残る
- 修飾が残ったもの: **ビュー**への `DESCRIBE <db>.<v>`（run-20260924-115811 の d5、UTILITY/DESC_VIEW）、Context の Catalog が実在しない `nocatalog_212`（run-20260925-122837 の y3-desc-2）・`nocatalog_214`（run-20260925-131314 の z-desc-nodbctx）
- 手元の代役（Catalog が hive / iceberg）に向けた実行は Query を受け取ったまま返している（athena-local の今の挙動）

### 判定箇所（file:line）
- Query は Store の `Execution.query`（execution.rs の Submission の `query`、#240 で正規化済みの文）をそのまま返す: src/operation/query_execution.rs:165
- 表／ビューの区別は実行中に決まる: src/operation/format_probe.rs:67-76（`DESC_VIEW`）→ execution.rs:317・341 → store.rs:69・248 の `substatement_type`
- DESCRIBE の対象の名前の範囲: src/operation/completion.rs:36 の `describe_target_name`（`athena_sql::Cursor::qualified_name`）。修飾を落とす範囲はここから取れる見込み
- 実行する文は修飾つきのまま Trino に送る必要がある（Context と違う DB を書いた場合など）。Query の表示だけを変える

### 未実測・未確認
- Context の Database と違う DB を修飾に書いた `DESCRIBE <other_db>.<t>`（修飾が落ちるか。落ちると Query だけでは別 DB だと分からなくなる）
- `SHOW COLUMNS FROM <db>.<t>`・`SHOW COLUMNS IN <t> FROM <db>`、`DESCRIBE EXTENDED`・`DESCRIBE FORMATTED <db>.<t>`、`DESCRIBE <db>.<t> <列>`、Iceberg の表、S3 Tables の Context、実在しない表（開始時に Entity Not Found で弾かれるので Query は無い見込み）
- 実在しない Context の Catalog で修飾が残る理由（#214 の既定のカタログへの差し替えと関係するか）

### 再現手順
`tools/dev.sh cargo run` で athena-local を立て、表に `DESCRIBE <db>.<t>` を投げて GetQueryExecution の Query を見る（今は `DESCRIBE <db>.<t>` のまま。本物は `DESCRIBE <t>`）

### stash
無し（コードに触っていない）

---
# #242 implement-issue（quick からの escalate）

## 基本情報
- issue: #242 ／ ベース: main ／ ブランチ: main-242 ／ 着手前 SHA: 796f76d（quick ノートのコミット f6185ec は docs 予算の別枠）

## 事前に確認した事実
### コードの経路（調査 1）
1. 開始時に表かビューかは分かる: entity_check.rs:72-91 の `check` は `system.jdbc.tables.table_type` を引き、VIEW なら `Check::Run`、表や不明なら `Check::Continue`（entity_check.rs:15-22）。Context の Catalog が実在しなければ `context_catalog::resolve`（context_catalog.rs:39-59）が既定のカタログに差し替えるが、SQL の文は変えない
2. 修飾の範囲は `Cursor::qualified_name`（name.rs:52-74）の `QualifiedName.start` と `parts.last().start` で切り出せる（部品の間の空白・コメントも修飾の側に入る）。`completion::describe_target_name`（completion.rs:36-42）が DESCRIBE・DESC の名前を読む。バッククォートは crate が読まない（Trino の構文チェックが先に弾く）
3. Store の query を読む側（format_probe の parse_target_table、completion の iceberg_partition_specs、utility_rows、result_output、classification）は、修飾を落としても Context（X-Trino-Catalog / X-Trino-Schema）が同じ表を指せば結果が変わらない。entity_check の `names_catalog`（3 部の判定）は修飾を落とす前に済ませる必要がある
4. DESCRIBE 系の Query・修飾つきの trino_sqls を完全一致で見る既存テストは無い
- 落とした: 調査 1 の 5（書き換えの約束の位置。#240 と同じ箇所）
### 過去の実測（調査 2。全 1187 件の execution.json と送った文の突き合わせ）
1. 表への `DESCRIBE <db>.<t>`・`DESC <db>.<t>`・`DESCRIBE awsdatacatalog.<db>.<t>`（Hive 外部・Hive 管理・Iceberg）→ Query は修飾（カタログ・DB）を落とした `DESCRIBE <t>`。コメントは残る
2. 修飾が残る: ビュー（d5、DESC_VIEW）、Context の Catalog が実在しない（z-desc-nodbctx・y3-desc-2）
3. DB の綴りが Context と違う（大文字 w1-desc-upper2、バッククォート d-n11）→ Query は `DESCRIBE <t>` で、GetQueryExecution の `QueryExecutionContext.Database` が文中の綴りに書き換わる（client-request-token.md の「Database は送ったまま」の例外）
4. ほかの文は **カタログ部分だけ** 落ちる: `SHOW COLUMNS FROM awsdatacatalog.<db>.<t>` → `SHOW COLUMNS FROM <db>.<t>`（w1-showcol-n7）、`SHOW CREATE TABLE awsdatacatalog.<db>.<t>`（c-n7）、`SHOW TABLES IN awsdatacatalog.<db>`（v3-showtables-u）、S3 Tables の Context の `CREATE TABLE AwsDataCatalog.<db>.<t>`（i4・j1〜j4・j9）。`SHOW COLUMNS FROM <db>.<t>` は DB が残る
5. 無い: 実在する別の DB を修飾に書いた形、Context に Database が無い形、`DESCRIBE EXTENDED <db>.<t>`、`DESCRIBE <t> <列>`、S3 Tables の Context の DESCRIBE（2 catalogs で弾かれる）
- 落とした: 無し
### 手元の athena-local での到達確認（15:39、統括役。.toolbox/probe242/run.sh。変更前のコード）
- 届いて今は修飾が残る: DESCRIBE・DESC の 2 部（m1〜m4・m9・m10・m12・m13）、ビュー（m11）、実在しない Catalog（nc）
- **開始時に弾かれて届かない**: `awsdatacatalog.` の 3 部の DESCRIBE・SHOW COLUMNS（m5・m6・m20〜m23・m37・m39）→ 400 `Catalog 'awsdatacatalog' does not exist`（本物は成功）。entity_check の names_catalog の経路で、無引用の `awsdatacatalog` を別名のキー `AwsDataCatalog` と大文字小文字を区別して照合している（design-checklist の #227 の項と同じ形）
- 届くが FAILED: SHOW CREATE TABLE・SHOW TABLES IN（m24〜m27・m38）、SELECT・INSERT・EXPLAIN（m30・m32・m41）→ Trino に `awsdatacatalog` カタログが無い（無引用の別名は置換しない既知の caveat）。DROP TABLE IF EXISTS（m34）は SUCCEEDED
- Trino の構文に無く届かない: DESCRIBE EXTENDED（m7）・列指定（m8）・SHOW TBLPROPERTIES（m28）・ALTER ... SET TBLPROPERTIES（m33）・SHOW VIEWS（m42）
### 手元の Trino での確認（15:18、統括役。.toolbox/probe242/schema.py）
- Trino 482 の hive カタログで、X-Trino-Schema を大文字（`S242`）にしても小文字のスキーマ `s242` に解決する（`DESCRIBE t242`・`SELECT count(*) FROM t242` とも同じ結果）。Store の database を文中の綴り（大文字）に変えて実行しても同じ表を引ける

## 実測の結果
ROUND=8（run-20260926-061322、M 群 36 項目）と過去の生データ。表は docs/dev/measurements/statements.md の「DESCRIBE の修飾と、ほかの文のカタログ部分が Query から落ちる範囲（#242）」。
- 決め手 1: 表への DESCRIBE / DESC（EXTENDED・列指定を含む）は Query から修飾（カタログ・DB と `.`、`.` の前後の空白）が落ち、返る Context の Database は修飾に書いた DB（文中の綴り）になる。Context と違う実在の DB・Context に Database 無しでも同じ。ビューは残る
- 決め手 2: SHOW COLUMNS（ビューは残る）・SHOW CREATE TABLE・SHOW TABLES IN・SHOW TBLPROPERTIES・ALTER TABLE・DROP TABLE は `awsdatacatalog.`（大文字小文字によらず、`.` の前後の空白ごと）が落ち、DB は残る。返る Context の Database は修飾に書いた DB
- 決め手 3: SELECT・INSERT・CTAS・CREATE VIEW・EXPLAIN・SHOW VIEWS IN は落ちない
- `DESCRIBE <db>./* c */<t>` は Query が `DESCRIBE /* c */<t>` になり、Hive の ParseException で FAILED（m10）

## 実測の経緯
- 2026-09-26 15:13（UTC 06:13）ROUND=8。36 項目と準備・後始末 10 本すべて測れた。後始末の残り無し

## 設計判断
- D2（ユーザーの判断、15:59 までに）: ビューへの `SHOW COLUMNS FROM awsdatacatalog.<db>.<v>`（m37）は、実行はカタログを落とした文で行い、表示（GetQueryExecution の Query と Context の Database）は本物どおり受け取った文と Database にする（Store に表示用の値を別に持つ）
- D3（ユーザーの判断、15:59 までに）: DESCRIBE の直後がブロックコメントになる文（`DESCRIBE /* c */ t`（statements.md:87 の実測）と、DB を落とした後の m10 `DESCRIBE <db>./* c */<t>`）は、Trino に送らず本物と同じ `FAILED: ParseException line 1:0 cannot recognize input near 'DESCRIBE' '/' '*' in describe statement`（ErrorCategory 1 / ErrorType 1003、`.txt` に `FAILED: <文言>`、`.metadata` 無し）にする。SHOW CREATE TABLE・ALTER・MSCK の同じ形は #244
- D1（ユーザーの判断、15:11）: 範囲は「表への DESCRIBE/DESC の修飾落ちと Context.Database の書き換え」と「ほかの文のカタログ部分の落ち」の両方（本物に最も近い）。2 フェーズ。未実測の形は ROUND=8 で測ってから決める

## 計画
小規模 issue の経路（2 フェーズ）。Plan（sonnet）の下書きを転記し、計画レビューと手元の到達確認で組み直した（組み直しの理由は「計画レビューの結果」）。落とした下書きの番号: 13（実機の足場は統括役が先に作った）、14-1（手元で確かめ済み）。

### 共通
- **Store の query と database を書き換え、実行もそれで行う**（下書き 2 の案 (a)）。`execution.database` を読むのは GetQueryExecution の表示（query_execution.rs:178）と run の X-Trino-Schema（execution.rs:359-362）。大文字の DB を X-Trino-Schema に渡しても Trino は同じスキーマに解決する（手元で確認）。冪等の指紋は書き換える前の受け取った文（#240）
- **書き換える条件（両フェーズ共通）**: Context の Catalog が省略か `awsdatacatalog`（大文字小文字によらない）。実測はすべてこの Context。実在しない Catalog では修飾が残る実測と合う
- **範囲の書き換え**: `athena_sql::Cursor::qualified_name` の部品の位置で、落とす範囲 `[落とす部品の先頭, 残す部品の先頭)` からコメントだけを残し、空白・`.`・無引用の名前を捨てる（m9・m10・m38・`DESCRIBE /* c */ <db>.<t>`）。落とす部品は無引用に限る（引用符付きの 1 部目は quoted_names が弾く。#204）
- **新規ファイル** `src/operation/reported_query.rs`（責務: 本物が Query と Context の Database を組み直す文を、同じ形にする。本番 約 120 行＋ユニットテスト）。mod.rs に宣言だけ足す

### フェーズ 1: カタログ部分を開始時の判定より前に落とす（本番 約 70 行）
- 呼ぶ位置: start_query_execution で `single_statement` の直後、`result_location`・構文チェック・entity_check より前（開始時の判定と実行のすべてが、カタログを落とした文を見る。今は `awsdatacatalog.` の 3 部の DESCRIBE・SHOW COLUMNS が entity_check で `Catalog 'awsdatacatalog' does not exist` になり、SHOW CREATE TABLE・SHOW TABLES IN は Trino で CATALOG_NOT_FOUND になっている。到達確認）
- 対象: DESCRIBE・DESC・SHOW COLUMNS FROM/IN・SHOW CREATE TABLE・SHOW TABLES IN（2 部 `awsdatacatalog.<db>`）・ALTER TABLE・DROP TABLE [IF EXISTS] で、名前が**ちょうど 3 部**（SHOW TABLES IN は 2 部）かつ 1 部目が無引用の `awsdatacatalog`（`eq_ignore_ascii_case`）。4 部以上は quoted_names の `Invalid table name`（quoted_names.rs:53-69、本物の実測 statements.md の V2・W2・Y4）に任せて触らない（再レビュー 1）。SELECT・INSERT・CTAS・CREATE VIEW・EXPLAIN・SHOW VIEWS IN は対象外（決め手 3）。SHOW TBLPROPERTIES・ALTER ... SET TBLPROPERTIES・DESCRIBE EXTENDED・列指定は Trino の構文に無く届かないので対象に入れない
- 書き換え: 1 部目と直後の `.`・空白を落とす。Store の database を DB の部品の綴り（`.text`）にする（m6・m22・m27）
- ビューの例外（m37）: 設計の確認で決める（下の「6b の論点」）
- テスト（新規 tests/reported_query.rs と reported_query.rs のユニット）: m5・m6（DESCRIBE の 3 部がフェーズ 2 と合わせて `DESCRIBE <t>`）、m20〜m23・m39（SHOW COLUMNS）、m24・m25・m38（SHOW CREATE TABLE）、m26・m27（SHOW TABLES IN）、m34（DROP）、ALTER TABLE の 3 部（ADD COLUMNS）、落ちない文（SELECT・INSERT・CTAS・CREATE VIEW・EXPLAIN）は 1 文字も変えない、Context の Catalog が別名でないときは変えない、4 部の `DESCRIBE awsdatacatalog.db.t.n` は今までどおり `Invalid table name awsdatacatalog.db.t.n`、trino_sqls と X-Trino-Schema が書き換え後
- 既存テストの予測: `awsdatacatalog.` の無引用の 3 部を送るテストがあれば期待値が変わる（tests/entity_check.rs の names_catalog の経路。実装前に grep）
- 壊す候補: 部品数の条件を外す（4 部の Invalid table name）／`eq_ignore_ascii_case` を `==` に（m21・m25）／対象から SHOW TABLES IN を外す（m26）／database の上書きを外す（m22）／Context の Catalog の条件を外す

### フェーズ 2: 表への DESCRIBE・DESC の DB 部分を落とす（本番 約 50 行）
- `entity_check::Check` に `Table` を足し、`(false, Some("TABLE"))` で返す（今は `_ => Continue`。entity_check.rs:90）。execution.rs は `if let Check::Reject` と `matches!(check, Check::Run)` しか見ないので既存は壊れない
- 条件: 文が DESCRIBE / DESC、修飾名が 2 部（フェーズ 1 で 3 部は 2 部になっている）、`Check::Table`、Context の Catalog の条件
- 書き換え: Query から DB 部分と `.`・空白を落とし、コメントは残す（m10 → `DESCRIBE /* c */<t>`）。Store の database を DB の部品の綴り（`.text`。小文字化しない。m12）にする
- テスト: m1〜m4・m9・m10・m12・m13、ビューは残る（m11）、Context の Catalog が実在しないときは残る、`DESCRIBE t`（1 部）は変えない、フェーズ 1 と合わせた m5・m6
- 壊す候補: `Check::Table` の腕を外す／database に `.value()`（小文字化）を使う（m12）／コメントを残す処理を外す（m10）／Context の Catalog の条件を外す

### 6b の決定（D2・D3）の配線
- D2: `Submission`・`Execution` に表示用の `reported: Option<Reported { query, database }>` を足し、GetQueryExecution（query_execution.rs:165・178）はあればそれを返す。フェーズ 1 でカタログを落とした文のうち、entity_check が `Check::Run`（ビュー）の SHOW COLUMNS だけ、受け取った文（#240 の正規化の後）と送った Database を入れる。実行・分類・結果ファイルは Store の query（カタログを落とした文）のまま（フェーズ 2。約 +25 行）
- D3: フェーズ 2 で、DB を落とした後（または元から）の文が `DESCRIBE` の直後にブロックコメント `/*` を持ち、`Check::Table` のとき、開始時の失敗（immediate_failure）にする。今の immediate_failure は `.txt` を書かない（#227 の実測。execution.rs:307-311）ので、開始時の失敗に「`.txt` を置くか」を持たせ、DESCRIBE のときは `result_output::write_failure` で `FAILED: <文言>` を置く（`.metadata` は置かない）。文言の `'DESCRIBE'` は書いた綴り（未実測）。DESC は未実測なので対象外（約 +30 行）
- テスト: m37（表示は受け取った文、trino_sqls はカタログを落とした文）、`DESCRIBE /* c */ t`・m10（FAILED・文言・ErrorCategory/Type・`.txt` の中身・`.metadata` 無し・Trino に送らない）、`DESCRIBE -- c\nt` は FAILED にしない（行コメントは本物で成功したか未確認なので今までどおり）
- 壊す候補: 表示用の値を返さない（m37）／`.txt` を置くかの判定を外す（`.txt` の有無）／ブロックコメントの判定を外す

### docs（フェーズ 2 のコミット）
CLAUDE.md の不変条件（書き換えの条件）と開発上の約束（Store の Query の例外）、docs/configuration.md の "not rewritten"、docs/api.md（Query と Context の Database）、docs/caveats.md（m10、Catalog aliases の項の「無引用の別名はそのまま送る」に DESCRIBE などの例外）、docs/dev/decisions.md、architecture.md、unmeasured.md、CHANGELOGS.md

### 未実測のまま残るもの（unmeasured.md へ）
`.` の前のコメント、2 部の別の DB の SHOW COLUMNS・ALTER・DROP（Database が変わるか）、ビューへの 3 部の DESCRIBE、SHOW TABLES FROM、S3 Tables・連携カタログの Context、Context の Catalog 省略時（本物の既定の AwsDataCatalog として扱う）

### 先行できる作業
- 実機（Step 9）: .toolbox/probe242/run.sh（手元の Trino に表・ビュー・別スキーマを作り、M 群と同じ文を athena-local に投げる。変更前の結果は「事前に確認した事実」）。済み

## 計画レビューの結果
Explore 1 本（sonnet。15:37 起動、15:50 までに返った）。
- 採用 1（high）: `DESCRIBE AwsDataCatalog.<db>.<t>` は entity_check（entity_check.rs:49-60、config.rs:95-119 のキーの完全一致）で `Catalog 'awsdatacatalog' does not exist` に落ち、`Check::Table` に届かない。統括役の到達確認（15:39）でも同じで、SHOW COLUMNS の 3 部も同じ・SHOW CREATE TABLE・SHOW TABLES IN は Trino で CATALOG_NOT_FOUND と分かった → **構造を変えた**: カタログ部分を開始時の判定より前に落とすフェーズ 1 と、表と分かってから DB 部分を落とすフェーズ 2 に組み直した
- 棄却 2（削減）: 実機の足場を後回しに → 既に作ってあり、この指摘の裏取りに使えた（先行作業の効果）
- 再レビュー（構造の観点、Explore 1 本）: 採用 1（high）: フェーズ 1 の条件に部品数が無く、4 部の `DESCRIBE awsdatacatalog.db.t.n` が 3 部に書き換わって quoted_names の `Invalid table name`（本物の実測）を素通りする → ちょうど 3 部（SHOW TABLES IN は 2 部）に限り、4 部のテストを足した
- design-checklist: 既存の #227 の行（無引用の名前で TRINO_CATALOG_MAP のキーを引くときの大文字小文字）と同じ形なので、出典に #242 を足す

## フェーズの記録
### フェーズ 1（自分。小規模経路・本番 約 90 行（reported_query.rs の本体 約 75・execution.rs +12））
- 7-0: 増える分岐 = drop_catalog の Context の条件／キーワードの並びに当たらない／部品数が違う／1 部目が awsdatacatalog でない／引用符付きの部品／コメントを残す、execution.rs の Some/None。呼び出し元は start_query_execution の 1 箇所。構文チェック・quoted_names・unquoted_ddl・create_table_catalog には受け取った文を渡し（文言の位置と input は受け取った文で実測）、context_catalog・entity_check・Submission には落とした文と修飾の DB を渡す
- red（16:01）: tests/reported_query.rs の 2 本（書き換える文・3 部の DESCRIBE）が落ち、書き換えない形と 4 部の 2 本は通った（予測どおり）。偽 Trino は無いカタログでも応答するので、3 部の DESCRIBE が今は開始時に弾かれる本物の Trino での挙動は Step 9 の実機で固定する
- green 16:02。標準コマンド ok（516 passed、clippy 警告なし）
- ミューテーション: (a) 部品数の条件を外す → ユニットの「対象外の文と形は落とさない」が落ちた（結合テストの 4 部は、開始時の文言の判定に受け取った文を渡すので書き換えの有無によらず弾かれ、落ちない。この条件を守るのはユニット）(b) 大文字小文字を区別 (c) SHOW TABLES IN を外す (d) database の上書きを外す (e) Context の条件を外す (f) コメントを残さない → それぞれ対応するテストが落ちた
- 7-5b: 分岐の突き合わせ 0 抜け。エラーの出口は増えない。7-6: 省略（本番コード約 90 行・新しいパターン無し・共有状態に触れない）

## レビュー実施記録

## dismissed

## deferred
| 指摘 | severity | 再トリアージ | 起票 |
|---|---|---|---|

## 覆した事前判断
- ブロックコメントの ParseException を再現しない既存の方針（docs/caveats.md の SQL dialect。手元では成功させて caveat に書く）→ DESCRIBE の直後のブロックコメントだけ本物どおり FAILED にする（D3。ユーザーの判断。外部契約: その形の StartQueryExecution は開始できるが FAILED になる。ほかの文は #244）
- issue 本文の前提（`;` を付けると DB 名が落ちる）→ `;` とは関係なく、表への DESCRIBE 全般で落ちる（過去の生データ 17 件の読み直し。issue にコメント済み）

## 実機検証

## 範囲外の発見
| 内容 | 出どころ | issue |
|---|---|---|
| SHOW CREATE TABLE・ALTER TABLE・MSCK REPAIR のブロックコメントの ParseException | 6b（D3） | #244 |

## 委譲の記録
| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| 生データの読み直し（quick のゲート） | 自分 | quick の手順（Explore を投げない） |
| 調査 | Explore 2 本（sonnet） | |
| 実測スクリプト（ROUND=8） | 自分 | ROUND=6・7 と同じ形の追記で、指示を書く方が長い |
| 計画の下書き | Plan（sonnet） | |
| 計画レビュー | Explore 2 本（sonnet。初回と構造の再レビュー） | |
| フェーズ 1 の実装 | 自分（小規模経路・本番 約 90 行） | 委譲の固定費が実装より長い |

## 出荷

## 時系列
- 14:53 quick-implement で着手。14:56 ゲートで escalate（新規性・事実）
- 14:57 implement-issue に切り替え、Explore 2 本（DESCRIBE の経路・過去の実測）を起動
- 15:09 調査 2 本の結果を転記（DESCRIBE 以外にカタログ部分だけ落ちる文があると分かる）
- 15:11 範囲を「DESCRIBE とカタログ落ちの両方」に決定（ユーザー）。ROUND=8（M 群 36 項目）を書き、15:12 ドライラン
- 15:13 d3e496c ROUND=8 をコミット。ユーザーが実行（run-20260926-061322）
- 15:17 実測の結果を記録。Plan（sonnet）を起動。待つ間に手元の Trino で大文字の X-Trino-Schema を確かめた
- 15:59 までに 6b: ビューの表示は本物どおり（D2）。m10 の前提（既知の DESCRIBE のブロックコメントの失敗と同じ形）を訂正して聞き直し、DESCRIBE の 2 形とも FAILED（D3）。#244 を起票
- 16:00 設計の承認。フェーズ 1 に入る。16:01 red、16:02 green
- 15:50 計画を組み直し（カタログを開始時の判定より前に落とす）、構造の再レビューを起動。返った指摘 1 件（部品数）を採用
- 範囲の質問で工数の小さい方（DESCRIBE だけ）を推奨にしていたとユーザーに指摘され、CLAUDE.md の開発上の約束に「選択肢は実測に近い方を推奨にする」を足した（ユーザーの指示でこのブランチに同梱）
