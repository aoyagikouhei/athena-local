# quick #44 結果ファイルまわりのファイルを分割する（execution.rs 448 行 / table_format.rs 704 行）

- 入力: issue #44。モード: standalone（ブランチ未作成）。ブランチ `main`、着手前 SHA `6c2379c`
- ゲート: サイズ 約 400 行・5 ファイル（≤50・3）→ **外れた** ／ 呼び出し元 3（≤5・同一モジュール）→ 通過 ／ 契約 通過（挙動を変えない移動）・新規性 **外れた**（`src/operation/` に新ファイル 2 つ）・事実 **外れた**（リファクタで再現テストが無く red が成立しない）・判断 通過（分け方は issue が指定済み）
- 時系列: 着手 19:26 ／ red なし（ゲートで停止） ／ green なし ／ コミット 19:30
- 検証: 標準コマンド `cargo test --locked` ok（275 passed）。移動前の基準値として取得。ミューテーション: 実施せず（本番コード未変更）／足した層: なし
- 自己レビュー: 本番コードを変更していないため対象なし
- 打ち切り・escalate: 提案あり（変更サイズ 約 400 行・5 ファイル／新規性 新ファイル 2 つ／事実 再現テスト無し）→ 選択: **implement-issue で進める**。stash なし（コードに触れていない）
- 予算の実績: Bash 約 7 本、所要 4 分（19:26→19:30）、サブエージェント 0 本
- 出荷: implement-issue へ引き継ぐ（このノートのコミットのみ）

## 引き継ぎ

### 判定箇所（file:line）

`execution.rs`（448 行。issue 本文の 442 行から増えている）の移動候補は結果ファイル書き込みの 4 関数:

- `write_result` … `src/operation/execution.rs:241-309`
- `write_failure` … `src/operation/execution.rs:316-332`
- `write_metadata` … `src/operation/execution.rs:337-354`
- `metadata_query_id` … `src/operation/execution.rs:359-371`

合計 約 131 行（`///` ブロック込み）。残る責務は受付（`start_query_execution:44`・`submit_response:102`・`client_request_token:122`・`context_defaults:148`・`result_location:172`）とバックグラウンド実行（`spawn_query:205`・`run:378`）。

`table_format.rs`（704 行。issue 本文の 616 行から増えている。本体 1-239、テスト 240-704）の移動候補は修飾名の解析:

- `parse_target_table` … `src/operation/table_format.rs:81-108`（解析の入口。`pub(super)`）
- `table_name_start` … `:112-122`
- `skip_keyword` … `:126-138`
- `parse_qualified_name` … `:141-154`
- `read_name_part` … `:158-174`

残るのは `target_statement:56`・`verb:66`・`probe_format:180`・`probe_sql:199`・`parse_probe_result:210`・`engine_ddl:225`。

### 呼び出し元（実測）

- `write_result` / `write_failure` … `execution.rs:217` / `:222`（`spawn_query` 内）のみ。ファイル外の呼び出し元は 0。
- `write_metadata` … `execution.rs:298` のみ。`metadata_query_id` … `execution.rs:293` のみ。
- `parse_target_table` … `execution.rs:403` の 1 箇所のみ。それ以外の解析系 4 関数は `table_format.rs` 内からのみ。
- `mod.rs` の再エクスポートは `start_query_execution` など 6 関数だけで、移動対象は 1 つも含まない。外から見える形は変わらない。

### 共有できる既存機構

- 字句処理は `catalog.rs` の `skip_quoted` / `comment_end` / `unquote` / `skip_leading_trivia` を既に使っている。解析系を分けるとこの依存がその新ファイルに集まる。
- `mod.rs` は `mod` 宣言と 6 関数の再エクスポートだけ（`src/operation/mod.rs` 全文で 11 行）。新ファイルの追加は `mod` 宣言 1 行で済むが、`operation/execution/result_writer.rs` にするなら `execution.rs` を `execution/mod.rs` に移すか Rust 2018 スタイルの `execution.rs` + `execution/` ディレクトリにするかの選択が要る。**issue はこの構造を決めていない**。

### 未実測・未確認

- **`skip_keyword` が 2 箇所に同名で別定義されている**: `table_format.rs:126` と `classification.rs:138`。中身が同一かは未確認。issue の制約は「挙動を変えない移動と抽出だけ」なので統合は範囲外だが、解析系を新ファイルに出すと同名 3 箇所になる。どう扱うかは要判断。
- **`catalog.rs:148` の doc コメントが `operation/table_format.rs::parse_qualified_name` をパスで参照している**。解析系を移すとこの参照が古くなる。CLAUDE.md の「実測コメントを落とさない」方針と同じ理由で、移動時に追随が要る。
- `classification.rs:250` の doc コメントが `metadata_query_id` を名前で参照している（パスは書いていないので移動しても壊れない）。
- `execution.rs` に `#[cfg(test)] mod tests` は無い（テストは `tests/` の結合テストのみ）。`table_format.rs` は 29 件のテストを持ち、内訳は `parse_target_table_*` 11 件・`target_statement_*` 5 件・`engine_ddl_*` 2 件ほか。解析系を分けるとテストも分割対象になる。
- 分割後のファイルの名前・置き場所（`operation/execution/result_writer.rs` か `operation/result_files.rs` か）は issue が案として 2 つ挙げたまま決めていない。

### 再現手順

リファクタなので再現テストは無い。担保は移動前後のテスト件数の突き合わせ（#19 の教訓）。**移動前の基準値（2026-09-21 19:28 実測）**:

```
cargo test --locked          → 合計 275 passed（lib 162 + 結合 113）
cargo test --locked --lib operation::table_format → 29 passed
```

移動後にこの 2 つが同じ件数になることを確認する。`git diff -M` で「挙動を変えない移動」であることも見える。
