# issue #44 結果ファイルまわりのファイルを分割する（execution.rs 448 行 / table_format.rs 704 行）

## 基本情報

| | |
|---|---|
| issue | #44 |
| ベースブランチ | `main` |
| ブランチ | `main-44` |
| 着手前 SHA | `6c2379c`（quick ノートのコミット前） |
| レビューの `base` | `6ed63dc`（quick ノートのコミット。実装差分だけをレビュー対象にする） |
| 経緯 | quick-implement のゲートで変更サイズ・新規性・事実の 3 観点が外れ、implement-issue へ escalate（ユーザーが選択） |

## 事前に確認した事実

### quick-implement のゲート判定（2026-09-21 19:26-19:30、コードに触れずに実施）

| 観点 | 判定 | 実測値（閾値） |
|---|---|---|
| 変更サイズ | 外れた | 本番コード約 400 行・5 ファイル（50 行・3 ファイル） |
| 新規性 | 外れた | `src/operation/` に新ファイル 2 つ（0） |
| 事実 | 外れた | リファクタで再現テストが無く red が成立しない |
| 影響範囲 | 通過 | ファイルをまたぐ呼び出しは `parse_target_table` の 1 箇所のみ |
| 契約 | 通過 | 挙動を変えない移動。`mod.rs` の再エクスポート 6 関数に移動対象は含まれない |
| 判断 | 通過 | 分け方は issue が指定済み |

### ファイルの現状（issue 起票時より育っている）

- `src/operation/execution.rs`: issue 本文 442 行 → **448 行**
- `src/operation/table_format.rs`: issue 本文 616 行 → **704 行**（本体 1-239 / テスト 240-704）

### 移動候補（quick の grep 実測。doc コメントの範囲は Explore で確定させる）

| 対象 | 関数 | 行 |
|---|---|---|
| execution.rs の結果書き込み（約 131 行） | `write_result` | 241-309 |
| | `write_failure` | 316-332 |
| | `write_metadata` | 337-354 |
| | `metadata_query_id` | 359-371 |
| table_format.rs の修飾名解析（約 94 行） | `parse_target_table` | 81-108 |
| | `table_name_start` | 112-122 |
| | `skip_keyword` | 126-138 |
| | `parse_qualified_name` | 141-154 |
| | `read_name_part` | 158-174 |

### 呼び出し元（quick の grep 実測）

- `write_result` / `write_failure` … `execution.rs:217` / `:222`（`spawn_query` 内）のみ
- `write_metadata` … `execution.rs:298` のみ／`metadata_query_id` … `execution.rs:293` のみ
- `parse_target_table` … `execution.rs:403` の 1 箇所のみ（**ファイルをまたぐ唯一の呼び出し**）
- `mod.rs` の再エクスポートは 6 関数で、移動対象は 1 つも含まない

### 移動前のテスト件数（2026-09-21 19:28 実測。突き合わせの基準値）

```
cargo test --locked                                → 275 passed（lib 162 + 結合 113）
cargo test --locked --lib operation::table_format  → 29 passed
```

`execution.rs` に `#[cfg(test)] mod tests` は無い（結合テストのみ）。

### 移動後には取れない基準値（2026-09-21 19:36 実測）

| ファイル | 総行 | `///` | `//!` | `#[` | fn | test |
|---|---|---|---|---|---|---|
| `src/operation/execution.rs` | 448 | 43 | 1 | 0 | 11 | 0 |
| `src/operation/table_format.rs` | 704 | 42 | 7 | 34 | 41 | 29 |
| `src/operation/classification.rs` | 346 | 22 | 1 | 5 | 10 | 4 |
| `src/catalog.rs` | 467 | 39 | 15 | 25 | 36 | 24 |

`src/` 全体の合計: `///` **435 行**、`#[test]`/`#[tokio::test]` **162 件**。
後者は `cargo test --lib` の 162 passed と一致するので、grep の数え方が正しいことも確認できている。
**分割後もこの 2 つが変わらないことが、doc コメント取りこぼし（#19）とテスト宣言漏れ（#19）の機械的な検査になる。**

### Step 3 の調査結果（Explore 3 本。計画に転記済みのものは「計画」節が正）

**doc コメントの取りこぼし（#19 の罠を実際に踏みかけた）**: quick の見込みは `fn` の行から数えていて、
9 関数すべてで直前の `///` ブロックを落としていた（合計 34 行）。とくに `write_result` は 241 ではなく **231** から。
`parse_target_table` の doc は `:79` が **`///` だけの行**で、「空行だから終わり」と誤検出すると `:80` が静かに落ちる。
→ 正しい行範囲は「計画」節の移動単位の表。

**`skip_keyword` の 2 版は挙動が違う**: `table_format.rs:126` はトリビアを扱わず呼び出し元が `catalog::skip_leading_trivia` を都度呼ぶ（`:115`・`:118`）。
`classification.rs:138` は**内部で呼ぶ**。コピー由来だが関数単体の挙動が違うので統合は挙動の変更になる（→ D3）。
字句処理の重複はこの 1 組だけ。`catalog.rs` に `skip_keyword` 相当は無い。

**越境依存は 2 ファイルで逆向き**（見落としやすい）:
`execution.rs` は残留 → 移動（`spawn_query:217,222`）で `pub(super)` が要る。
`table_format.rs` は移動 → 残留（`parse_target_table:87` → `verb:66`）で、`verb` を一緒に移せば変更 0 件（→ D2）。

**`missing_docs` は無効**: `Cargo.toml` に `[lints]` 無し、`src/lib.rs` にクレート属性無し
（リポジトリ全体で `#![...]` は `tests/common/mod.rs:5` の `#![allow(dead_code)]` のみ）。
**doc の取りこぼしは `clippy -D warnings` でも検出されない** → `check.sh` のチェック 3・4 が唯一の網。

**`//!` の配分材料**: `execution.rs:1` は 1 行に 3 つの役割が同居（受付／Trino での実行／結果ファイルと `.metadata` の書き込み）。
`table_format.rs:1-7` は 2 段落目（「Phase 2 では修飾名…」）が丸ごと移動対象 5 関数の説明。

**先行分割 #19・#30 の実績**: どちらも 2 フェーズ・5 コミット（quick 引き継ぎ・計画確定・分割・文書・出荷記録）、
所要 1 時間 2 分 / 1 時間 49 分、計画レビュー 1 回、独立レビュー 1 回で指摘 0 件。
`git diff -M` は使わない方針（#19 で 41% 類似度。`30.md:279`）→ D4。
50 行超の関数の内部分割はしない・起票もしない（`19.md:62`）→ D5。README・CHANGELOGS は変えない → D7。
`foo/mod.rs` スタイルで一貫（`foo.rs` + `foo/` の併存は 0 件）。`src/lib.rs` は #19/#30 で 1 文字も変わっていない。

**パス参照の追随（統括役が 8 件すべての本文を読んで確定）**: 要追随は `catalog.rs` の **4 件のみ**
（`:59` `skip_quoted` ／ `:78` `comment_end` ／ `:148` `parse_qualified_name` を名指し・issue #44 に言及 ／ `:181` `unquote`）。
いずれも「`operation/table_format.rs` が …に再利用する」の形で、使う側が移動する。
残り 4 件（`store.rs:53`・`store.rs:125`・`athena.rs:22`・`tests/table_format.rs:21`）が指す
`context_defaults` / `client_request_token` / `probe_sql` はすべて残留側なので**書き換えない**。
`README.md` / `CHANGELOGS.md` へのパス参照は 0 件。

**テストの越境 1 件**: `table_format.rs:583-605` のテストが `parse_qualified_name` を直接呼ぶ
（`crate::catalog::skip_qualified_name` との突き合わせが目的）。移動先へ一緒に運ぶ。

**落とした項目**: 3 ファイルの字句処理関数の全体像（22 行の表。結論だけ採用）／`use` の残留側の行番号詳細／
#19・#30 の分割後のファイルごとの行数（元ファイルの規模が違い参考にならない）／`mod.rs`・`lib.rs` の全文（変更しないことが確定）／
Explore A の「ネスト構成にした場合の再エクスポート設計」（直下構成を採るので不要）。


### 未確認（Step 3 の Explore で確かめる。すべて解決）

1. ~~`skip_keyword` の重複~~ → **解決**（Explore C。挙動が違い統合不可）
2. ~~`catalog.rs:148` のパス参照~~ → **解決**（Explore C。追随が必須と確定）
3. 分割後の置き場所 → **Explore C で決め手が出た**（`operation/` 直下なら `super` の書き換え不要）。名前は Step 4/5 で確定
4. ~~`CLAUDE.md` の書き換え範囲~~ → **解決**（Explore B。`:29-33`・`:46`）。ただし `table_format.rs` の記述が元から無い問題が新たに判明 → 範囲の判断が要る

### design-checklist の該当項目（#19・#30 の先行分割から）

- 移す単位を行番号の範囲で指定せず、直前の `///` ブロックと属性を 1 単位として運ぶ（#19）
- 移動前後のテスト件数を突き合わせる手順を計画に入れる（#19。`mod tests` の宣言漏れは件数が減るだけで green のまま通る）
- 突き合わせの期待値は内訳の列挙から数え直して総数と一致させる（#30）
- `use` を配るときは移した先で実際に使うかを本文の grep で確かめる（#30。`unused_imports` は警告止まりで `clippy -D warnings` まで気づけない）

## 設計判断

### D1. 新ファイルは `src/operation/` 直下に置く（ネストしない）

issue 本文は `operation/execution/result_writer.rs`（ネスト）も案に挙げているが、**直下を採る**。根拠は 2 つとも実測:

- `metadata_query_id`（`execution.rs:364`）が `super::classification::words` を呼ぶ。ネストすると `super` が
  `operation::execution` を指して壊れ、`crate::operation::classification::words` への書き換えが要る
- `TargetTable` は `execution.rs:410,414,415` がフィールドアクセスで使う。ネストすると `pub(super)` が届かず再エクスポートが要る

どちらも「挙動を変えない移動」に余計な書き換えを持ち込む。直下なら**両方とも 1 行も変えずに済む**。
リポジトリは `foo/mod.rs` スタイルで一貫していて（`find src -name mod.rs` は 2 件、`foo.rs` + `foo/` の併存は 0 件）、
`operation/` の下にファイルを足すのは既存の形そのまま。

### D2. `verb` も修飾名解析側へ移す（issue 本文の 5 関数リストを覆す）

issue 本文の移動対象リストに `verb`（`table_format.rs:66`）は無い。しかし **`verb` の呼び出し元は `parse_target_table:87` ただ 1 つ**
（Explore A が定義 `:66` と呼び出し `:87` のみと実測）。残留側に置くと `verb` を `pub(super)` に上げる必要があり、
一緒に移せば**可視性の変更が 0 件**になる。公開範囲を最小にする原則にも合う。

### D3. `skip_keyword` の重複は残す

`table_format.rs:126` と `classification.rs:138` は**挙動が違う**（後者だけ内部で `skip_leading_trivia` を呼ぶ）。
統合は挙動を変える操作なので issue の制約の外。`catalog.rs:148-151` が
「用途が違うので無理に共通化しない」と既に明記しているのと同じ判断。

### D4. `git diff -M` ではなく正規化 diff で「純粋な移動」を確かめる

issue 本文は「`git diff -M` で一目で確認できる」と書いているが、**先行 2 件がこれを使わない方針に落ち着いている**
（#19 で 41% 類似度、`--find-renames=30%` の明示が要った）。#30 の
「`use`・`mod`・空行を除いて行をソートして `diff`」を採る。

### D5. `write_result`（69 行）の内部分割はしない。起票もしない

#19 の設計判断 6 をそのまま踏襲する（`19.md:62`）。理由は「分岐の形を書き換える操作で、
『分岐・エラー経路を 1 行も変えない』という issue の制約の保証が弱くなる」。

### D6. CLAUDE.md を最新化する（2026-09-21 19:46 ユーザー確認）

`CLAUDE.md` に `table_format.rs` の記述が一度も無い（#39 の追随漏れ）。**#44 で書き起こす**（ユーザーの選択）。
分割の追随（`:29-33`・`:46`）に加えて、`table_format.rs` と新ファイルの説明を新規に書く。
**「実測していない振る舞いは推測で埋めない」の約束に従い、`//!` と doc コメントに実測日が付いている事実だけを写す。**

### D7. README・CHANGELOGS は変えない

挙動が変わらないため。`CLAUDE.md:63` の約束は「挙動を変えたら」が条件。#19（`19.md:116`）・#30（`30.md:291`）の先例どおり。

## 先行できる作業（実装を待たずに作った）

### 検証スクリプト `.claude/issue-notes/44-verify/check.sh`（19:50 作成・受け入れ判定済み）

`check.sh [<着手前 SHA>]`（既定 `6ed63dc`）。6 項目を機械的に見る:

1. テスト総数が **275** のまま（全テストバイナリの `test result` を合算）
2. `--lib operation` のユニットテストの内訳（現状 `classification` 4 / `query_execution` 4 / `table_format` 29）
3. `src/` 全体の `///` が **435 行**のまま
4. 実測記録（`実測`・`採取` を含む行）**128 行**の全件突き合わせ（`missing_docs` が無いのでこれでしか気づけない）
5. 正規化 diff（`use` / `mod` / `//!` / 空行を除いて行をソート）で「純粋な移動」か
6. `cargo fmt --check` と `cargo clippy --all-targets --locked -- -D warnings`

**受け入れ判定（2 段階とも実施）**:

- 変更前のツリーで流して **1・2・3・4・6 がすべて ok、5 は skip**（新ファイルがまだ無い）
- `table_format.rs:18` の doc 行（`/// Iceberg では本物が実行時に失敗する（2026-09-21 実測）。`）を 1 行消して流すと
  **3 が「434 行（期待 435）」で NG、4 が消えた行を名指しで NG**。壊したら気づくことを確認した（本体はコピーから復元済み）

直した欠陥: 初版はチェック 4 の比較範囲が非対称だった（`before` は 2 ファイル、`after` は `operation/` 全体）。
`src` 配下の `.rs` 全部で揃えた。

## 計画

### フェーズ分割（2 フェーズ。#19・#30 と同型）

| | 内容 | コミット |
|---|---|---|
| フェーズ 1 | 両ファイルの分割（挙動を変えない移動） | 1 本 |
| フェーズ 2 | 文書の追随（CLAUDE.md・`catalog.rs` のパス参照） | 1 本 |

`execution.rs` と `table_format.rs` は完全に独立（共有コードも呼び出し関係も無い）なので 1 コミットにまとめても
正規化 diff は 2 組に分けて別々に走らせられ、検証の粒度は落ちない。移動は約 513 行で #30 の単一コミット（419 行）と同程度。

### ファイル構成

| ファイル | 責務 | 行数（現 → 見込み） | `///`（現 → 見込み） |
|---|---|---|---|
| `operation/execution.rs`（残留） | `StartQueryExecution` の受付とバックグラウンド実行の起動 | 448 → 約 300 | 43 → **20** |
| `operation/result_output.rs`（新規） | 結果ファイル本体と `.metadata` の書き込み | – → 約 160-170 | – → **23** |
| `operation/table_format.rs`（残留） | テーブル形式の問い合わせと DDL 組み合わせの決定 | 704 → 約 330 | 42 → **26** |
| `operation/target_table.rs`（新規） | `DROP TABLE` / `ALTER TABLE` の対象テーブルの修飾名解析 | – → 約 390-400 | – → **16** |

`///` の合計 20+23+26+16 = **85 = 43+42**（移動元の合計と一致。検算済み）。

**名前の根拠**: `result_writer.rs` は `crate::results::ResultWriter` と衝突して紛らわしい。`result_files.rs` は `src/results.rs` と字面が近い。
`qualified_name.rs` は機構寄りで `work_group.rs` 型の「対象の概念で名付ける」慣習からずれる。
`operation/mod.rs` の `mod` 宣言はアルファベット順に挿入（`rustfmt` の `reorder_modules = true` が既定で効く。`rustfmt.toml` は無い）:
`classification, execution, query_execution, result_output, table_format, target_table, work_group`。再エクスポート 3 行は変更不要。

### 移動する単位（doc コメント・属性込み。Plan が Read で実測し直してノートと一致を確認）

| 対象 | 種別 | 移動元 | 移動先 |
|---|---|---|---|
| **`ENGINE_DDL_CONTENT_TYPE`** | 定数 | `execution.rs:25-27` | `result_output.rs` |
| `write_result` | fn | `execution.rs:231-309` | `result_output.rs` |
| `write_failure` | fn | `execution.rs:311-332` | `result_output.rs` |
| `write_metadata` | fn | `execution.rs:334-354` | `result_output.rs` |
| `metadata_query_id` | fn | `execution.rs:356-371` | `result_output.rs` |
| `TargetTable` | 型 | `table_format.rs:44-51` | `target_table.rs` |
| `verb` | fn | `table_format.rs:65-73` | `target_table.rs` |
| `parse_target_table` | fn | `table_format.rs:75-108` | `target_table.rs` |
| `table_name_start` | fn | `table_format.rs:110-122` | `target_table.rs` |
| `skip_keyword` | fn | `table_format.rs:124-138` | `target_table.rs` |
| `parse_qualified_name` | fn | `table_format.rs:140-154` | `target_table.rs` |
| `read_name_part` | fn | `table_format.rs:156-174` | `target_table.rs` |

**Plan が新たに見つけた移動漏れ**: `ENGINE_DDL_CONTENT_TYPE`(`execution.rs:25-27`)。
`grep -rn` の唯一の使用箇所が `write_result:269` なので一緒に移す。**見落とすと `result_output.rs` で未定義になりビルドが壊れる。**

### `use` の配り方（移した先で実際に使うシンボルを確かめた。#30 の教訓）

**`result_output.rs`（新規）** — 8 行:
`Failure`（戻り値・引数・`Failure::result_write`）／`App`（引数）／`convert`（`write_metadata` の `column_infos`）／
`metadata`（`to_metadata`）／`results::{self, ResultFile, ResultLocation}`（`to_csv`・`to_text`・分岐・`write_metadata` の引数型）／
`store::Execution`（引数）／`trino::Outcome`（引数・戻り値）／`super::table_format::EngineDdl`（引数と比較）。
`crate::catalog::` 系は不要（`super::classification::words` はフルパス呼び）。

**`execution.rs`（残留）**: `crate::convert` と `crate::metadata` の 2 行を**削除**（移動側だけが使う）。
`crate::results::{self, ResultFile, ResultLocation}` から `self` を落とす（`to_csv`/`to_text` は移動側のみ。残留は `ResultFile::of`・`ResultLocation::new`）。
`store`・`trino` の行は変更なし。`use super::result_output;` と `use super::target_table;` を足す。

**`target_table.rs`（新規）**: `use super::table_format::TargetStatement;` の **1 行だけ**。
`crate::catalog::skip_leading_trivia`/`skip_quoted`/`unquote` はフルパス呼びなので `use` 不要（現行もそう）。

**`table_format.rs`（残留）**: `use crate::statement::quote_literal;` と `use crate::trino::{Cancel, Outcome, Trino};` は**そのまま**。
移動対象 5 関数はどれも使っていないので **`target_table.rs` に配ってはいけない**（配ると `clippy -D warnings` で初めて気づく）。

### 可視性の変更（2 件のみ）

| 対象 | 変更 | 理由 |
|---|---|---|
| `write_result` | `async fn` → **`pub(super) async fn`** | 呼び出し元 `execution.rs:217` が別ファイルになる |
| `write_failure` | `async fn` → **`pub(super) async fn`** | 同 `:222` |

他はすべて変更なし。`parse_target_table`・`TargetTable`・`TargetStatement` は既に `pub(super)` で、
`operation/` 直下なら到達範囲が変わらない（D1）。`verb` を一緒に移したことで `table_format.rs` 側の変更は 0 件（D2）。
**execution.rs 側の 2 件は避けられない**（呼び出し元が残るため）。

### テストの分け方（14 / 15。検算済み）

- **`target_table.rs` へ 14 件**（`table_format.rs:368-626`、246 行）… `parse_target_table_*` 13 件 + `parse_qualified_name_と_catalog_skip_qualified_name_は同じ書き方を受け付ける` 1 件
- **`table_format.rs` に残す 15 件** … `target_statement_*` 6 件・`engine_ddl_*` 2 件・`形式の問い合わせが失敗すれば今までどおりに倒す` 1 件・`probe_sql_*` 2 件・`parse_probe_result_*` 4 件
- **14 + 15 = 29 = 全件数と一致**

ヘルパー `outcome_with_probe_result`(`:659-667`) は残留側の 4 テストだけが使う → `table_format.rs` に残す。
`use serde_json::Value;` は同ヘルパーだけが使う → **`target_table.rs` の `mod tests` には配らない**。`use super::*;` は両方に必要。

**`--lib operation` の内訳の期待値**: 分割前 `classification` 4 / `query_execution` 4 / `table_format` 29 = 37
→ 分割後 `classification` 4 / `query_execution` 4 / `table_format` **15** / `target_table` **14** = **37**（不変）。

### 依拠する既存機構の前提表

| 暗黙の前提 | 破らないか | 誰が保証しているか |
|---|---|---|
| `operation/*.rs` の直接の子では `super` が `operation` を指す | 破らない。新 2 ファイルもフラットに宣言する（D1） | `operation/mod.rs:3-7`。現に `execution.rs:364` の `super::classification::words` が動いている（`classification.rs:4` の `pub(super) fn words`） |
| `pub(super)` は `operation` 全体に届く | 破らない。`TargetTable`・`TargetStatement`・`parse_target_table` の可視性を変えない | `table_format.rs:14`・`:47-50`・`:81`。現に `execution.rs:410,414,415` がフィールドアクセスできている |
| `mod` 宣言はアルファベット順でないと `fmt --check` が落ちる | 破らない。順序を守って挿入する | `rustfmt --print-config default` で `reorder_modules = true` を実測（`rustfmt.toml` は無い） |
| `crate::catalog::` のフルパス呼びは `use` 無しで解決する | 破らない。5 関数は 1 文字も変えずに運べる | `table_format.rs:113,115,116,118,121,147,149,160,162`（すべてフルパスで `use` 文が無い） |

### ミューテーション確認で壊す候補

1. **`target_table.rs`（ユニットテスト）**: `read_name_part` の `input[..end].to_lowercase()` を `.to_string()` に変える
   → `cargo test --lib operation::target_table` で `parse_target_table_は引用符の無い名前を小文字にする` が落ちること
2. **`result_output.rs`（ユニットテストが無いので結合テスト）**: `write_result` の `should_write` を `false` に固定
   → `cargo test --test select` で CSV 本体の PUT を見ているテストが落ちること

### 人間検証リスト

**無し**（#19・#30 と同じ理由。挙動を変えない機械的な分割で外部依存が増えない）。

### CLAUDE.md の更新（D6 = (b) 案を採用）

統括役が `grep -n` で全件を取り直して確定（19:59）。**Plan の判定に 1 件誤りがあった**:

| 行 | 内容 | 判定 |
|---|---|---|
| `:29` | `operation/` の構成 | **要書き換え**（`result_output.rs`・`table_format.rs`・`target_table.rs` を加える） |
| `:30` | `start_query_execution` | 変更不要（残留） |
| `:31` | `spawn_query` → `run` | 変更不要（残留） |
| `:32` | `execution.rs` の `write_result` | **要書き換え** → `result_output.rs` の |
| `:33` | `execution.rs` の `write_failure` | **要書き換え** → `result_output.rs` の |
| `:44` | 文の種類の判定 | 変更不要（`query_execution.rs` にマッチしただけ。`execution.rs` 単体の言及ではない） |
| `:46` | `metadata.rs` の説明 | **要書き換え**。末尾の「クエリ ID の出どころ（Trino の ID か `QueryExecutionId` か）は `operation/execution.rs` 側で決める」は `metadata_query_id` の話で、**移動対象**。→ `operation/result_output.rs` |

**Plan は `:46` を「変更不要」と判定していたが誤り**（`:46` を「文の種類の判定」の行と取り違えていた。実際はそれが `:44`）。
要書き換えは `:29`・`:32`・`:33`・`:46` の **4 箇所**。`table_format` / `target_table` / `result_output` の言及は現状 0 件。

#### フェーズ 2 の文案（20:12 に用意。実装を待たずに作れる）

**`CLAUDE.md:29`** — 冒頭の一覧に `result_output.rs` を加え、`table_format.rs` / `target_table.rs` を 1 文で足す
（#39 の追随漏れをここで埋める。D6）。**実測日付きの doc に書かれている事実だけを写す**:

> `operation/` — 6 つのオペレーションの本体。`execution.rs`（`StartQueryExecution` の受付とバックグラウンド実行）、
> `result_output.rs`（結果ファイルと `.metadata` の書き込み）、`query_execution.rs`（`GetQueryExecution`／`GetQueryResults`／`StopQueryExecution`）、
> `work_group.rs`（`GetWorkGroup`／`ListWorkGroups`）に分かれ、文の種類の判定だけ `classification.rs` に置いて実行系と参照系の両方から呼ぶ。
> DROP TABLE と ALTER TABLE ... ADD COLUMNS / REPLACE COLUMNS は、対象テーブルの形式によって本物が列なしでも本体と `.metadata` を置くので、
> その判定を `table_format.rs`（Trino への形式の問い合わせと組み合わせの決定）と `target_table.rs`（対象の修飾名の解析）に置く。
> `mod.rs` は `mod` 宣言と 6 関数の再エクスポートだけで、`handler` からの見え方は分割前と変わらない。

**`CLAUDE.md:32`・`:33`** — 見出しだけ差し替える（本文はそのまま）:
`execution.rs` の `write_result` → **`result_output.rs` の `write_result`** ／ 同 `write_failure` → **`result_output.rs` の `write_failure`**

**`CLAUDE.md:46`** — `metadata.rs` の説明の末尾。
「クエリ ID の出どころ（Trino の ID か `QueryExecutionId` か）は `operation/execution.rs` 側で決める。」
→ **`operation/result_output.rs` 側で決める。**（`metadata_query_id` が移動するため）

**`src/catalog.rs` の 4 箇所** — いずれも `operation/table_format.rs` → `operation/target_table.rs` に差し替える
（移動後は `table_format.rs` がこれらを使わなくなり、記述が事実として誤りになる）:

| 行 | 現行 | 変更 |
|---|---|---|
| `:59`（`skip_quoted`） | `operation/table_format.rs` が DROP TABLE の修飾名の引用符付き識別子を読むのにも再利用する（issue #39 Phase 2）。 | パスのみ差し替え |
| `:78`（`comment_end`） | `operation/table_format.rs` が DROP TABLE の修飾名を読むのにも再利用する（issue #39 Phase 2）。 | 同 |
| `:148`（`skip_qualified_name`） | `operation/table_format.rs::parse_qualified_name` は名前の中身を取り出す関数で、… | 同（`issue #44` の言及はそのまま残す） |
| `:181`（`unquote`） | `operation/table_format.rs` が DROP TABLE の修飾名の引用符付き識別子を読むのにも再利用する（issue #39 Phase 2）。 | 同 |

**注意**: `issue #39 Phase 2` の出典は**書き換えない**（その事実を作ったのは #39 で、#44 は置き場所を変えただけ）。

### 残っている前提（未検証）

1. 新ファイルの総行数（約 160-170 / 約 390-400）は `///` と本体の実測合計に `use`・空行の見込みを足した**算出**。実装時に `wc -l` で確定する
（2 番目にあった「`fmt` の整形は未検証」は計画レビュー R4 で削除。`fmt --check`・`clippy` が守る事実の二重化だった）

## 計画レビューの結果

### Plan の削減提案（項目 13）→ **棄却**

Plan は「`catalog.rs:59`・`:78`・`:181` のパス参照の書き換えを計画から外してよい。
`pub(crate)` なのでコンパイルもテストも壊れず、`:148` だけ直せば足りる」と提案した。**採らない。**

理由: 3 行はいずれも「`operation/table_format.rs` が …に再利用する」という記述で、移動後は
**`table_format.rs` はこれらを使わなくなる**（使うのは `target_table.rs`）。つまり記述が**事実として誤りになる**。
#19 が同じ状況で「分割後は `operation.rs` というファイルが存在しなくなり、コメントが事実として誤りになる」として
完了条件の外でもフェーズ 2 に含めた先例（`19.md:152` の棄却理由）があり、そちらに揃える。
CLAUDE.md の「本物に合わせることが目的」＝事実と記述を一致させる方針とも整合する。
書き換えは 3 行の定型文なのでコストも小さい。

### R1（medium・**採用**）: `check.sh` の項目 5 が、計画どおり実装しても必ず NG になる

出どころ: 計画レビュー 1 本目（契約・構造）。

正規化 diff の `strip()`（`44-verify/check.sh:39-54`）は `use` / `mod` 宣言だけを除外するので、
**関数シグネチャの `pub(super)` は残る**。レビュアーが実測:

```
$ printf 'async fn write_result(\n' | strip          → async fn write_result(
$ printf 'pub(super) async fn write_result(\n' | strip → pub(super) async fn write_result(
```

before 側の `async fn write_result(` と after 側の `pub(super) async fn write_result(` はソート後に一致せず、
「1 行減って 1 行増えた」と判定される。`write_failure` も同じ。**計画が要求する唯一の非純粋変更（可視性 2 件）そのもの**なので、
実装が正しいほど確実に踏む。受け入れ判定では項目 5 が skip されていた（新ファイルがまだ無いため）ので、この偽陽性は実装時に初めて出る。

**4 ゲートの判定**: 単純さ → 起こらない失敗ではなく計画どおり実装すれば必ず起きる／前提検証 → レビュアーが実測済み／
決定照合 → D4（正規化 diff を正とする）を弱めるのではなく強める方向／振動ガード → 初出。**採用。**

**対応**: レビュアーの提案（「目視で判定する」と明文化）は採らない。D4 の「機械的に確認できる」が弱まるため。
代わりに **`check.sh` に期待差分の許容リストを実装する**。許容するのは次の 3 種類だけで、**それ以外の差分が 1 行でも残れば NG**:

1. `write_result` / `write_failure` の宣言行に `pub(super)` が付く（可視性変更 2 件）
2. 呼び出しの修飾が変わる（`write_result(` → `result_output::write_result(`、`table_format::parse_target_table(` → `target_table::parse_target_table(`）
3. テストモジュールの枠（`#[cfg(test)]` / `mod tests {` / 閉じ `}`）が 1 組から 2 組に増える

これなら「意図しない可視性変更」は許容リストに無いので検出できる。

### R2: 計画から外せるもの → 1 本目は「特に無し」

`check.sh` 項目 5 の枠組みは残り約 509 行の純粋性を検証する価値があるので削らない、との判定。

### R3（**high**・採用）: `check.sh` のチェック 2 に合否判定が無く、テストの移動漏れを 6 項目すべてが見逃す

出どころ: 計画レビュー 2 本目（一貫性・削減）。

チェック 2 は `grep | sort | uniq -c` の結果を**印字するだけ**で、期待値と比較していなかった。
末尾の合否は `[ "$fail" = "0" ]` だけで決まるので、この項目が何を出しても「すべて ok」が返る。

**レビュアーが示した失敗シナリオ（実測付き）**: 移動対象 14 件は `table_format.rs:368-626` の連続ブロックで、
`:607-626` の次の `:628` からすぐ残留側のテストが始まる。境界を 1 つ間違えて
`parse_target_table_は_alter_table_でも_if_exists_を読み飛ばす`(`:607-626`) を移し忘れると:

| チェック | 結果 | 理由 |
|---|---|---|
| 1（総数 275） | **ok のまま** | テストは消えていない |
| 3（`///` 435） | **ok のまま** | このテストに doc コメントが無い |
| 4（実測記録） | **ok のまま** | 移動対象 14 件の本文に「実測」「採取」が 1 件も無い（レビュアーが `sed -n '368,626p' \| grep` で実測） |
| 5（正規化 diff） | **ok のまま** | **和集合しか見ないので、どちらのファイルに入っているかを区別しない** |
| 2 | 唯一検出できる | だが判定ロジックが無い |

design-checklist の #19「移動前後のテスト件数を突き合わせる手順があるか」・#30「期待値を内訳から数え直して総数と一致させたか」は、
**ノートの文章としては満たしていたが、機械的検証としては満たしていなかった。**

**4 ゲートの判定**: 単純さ → 境界のすぐ隣に残留テストがあり現実に踏む／前提検証 → レビュアーが 4 項目とも実測で確認／
決定照合 → checklist の趣旨そのもの／振動ガード → 初出。**採用（high）。**

**対応（実装済み・20:06）**: チェック 2 に期待値判定を入れた。`target_table.rs` の有無で分割前後を切り替える
（前: `classification:4 query_execution:4 table_format:29` ／ 後: `... table_format:15 target_table:14`）。
**空の `target_table.rs` を置いて分割後モードに入らせ、内訳が合わないことを NG として検出することを実測で確認した。**

### R4（削減・**一部採用**）: チェック 3 はチェック 5 と重複している

レビュアーの主張: チェック 5 は 2 組の内容完全一致を要求し `///` も strip 対象外なので、
チェック 5 が ok ならこの 2 ファイル由来の `///` は保たれる。チェック 3 のグローバルカウントは追加情報を与えない。

**チェック 3 は残す（棄却）**。理由: **チェック 5 はフェーズ 1 の 2 組しか見ない**。
フェーズ 2 で `catalog.rs` の doc コメント 4 箇所を書き換えるが、そこを見るのはチェック 3 だけ。
文言の書き換えなら行数は変わらないので、変われば事故。コストは `grep` 1 回で、
「doc 1 行を消すと NG」を実測済み。スクリプトにその旨のコメントを入れた。

**「残っている前提」の 2 番目は削除（採用）**。「`fmt` が `pub(super)` や `use` の折り返しをどう整形するかは未検証
→ 検証手順の `fmt --check`・`clippy` で見る」は、文自体が「別の既存チェックで見る」と認めており、
**既に固定されている事実の二重化**。#30 の計画レビューが前提表から「コンパイラ・既存コマンドが守る事実」を落とした判断（`30.md:180`）と同型。

### 一貫性の観点 → 指摘 0 件

レビュアーが実コードと #19/#30 を突き合わせて裏取りした結果、design-checklist・CLAUDE.md の約束に反する箇所も、
#19/#30 から意図なく外れた箇所も見つからなかった。確認された事項:

- ファイル名の根拠（`src/results.rs`・`ResultWriter` との衝突回避）は妥当。既存の命名慣習のどちらのグループとも矛盾しない
- D1・D2・D4・D5 は #19/#30 のノートの該当箇所と実際に一致
- `rustfmt --print-config default` で `reorder_modules = true` を再実測。計画の前提と一致
- **移動対象 9 関数・1 型の行範囲（doc 込み）を `Read` で数え直し、計画の表と 1 行のズレもなく一致**
- `///` の検算（20+23+26+16 = 85 = 43+42）とテスト内訳（14 / 15 = 29）も実ファイルを数え直して一致

## フェーズの記録

### フェーズ 1（分割本体）の 7-0（着手前に答えた）

- **消す・変えるガードや条件**: **無し**。挙動を変えない移動だけで、分岐・エラー経路・共有状態の扱いを 1 行も変えない
- **変える契約の呼び出し元**（grep 実測）:
  - `write_result` … `execution.rs:217`（`spawn_query` の Ok 分岐）のみ
  - `write_failure` … `execution.rs:222`（同 Err 分岐）のみ
  - `write_metadata` … `write_result:298` のみ／`metadata_query_id` … `write_result:293` のみ（どちらも移動先内に収まる）
  - `parse_target_table` … `execution.rs:403` のみ（ファイルをまたぐ唯一の呼び出し）
  - `verb` … `parse_target_table:87` のみ（一緒に移すので移動先内に収まる）
  - `ENGINE_DDL_CONTENT_TYPE` … `write_result:269` のみ（同上）
- **このフェーズで増える分岐**: **0 件**。`match` の腕・`if`・早期 return・エラー経路を 1 つも足さない。
  したがって「固定するテスト名の無い分岐」も 0 件。移動したテストが移動後も効いていることは 7-4 のミューテーションで見る
- **通る境界入力**: 該当なし（入力を処理する新しい経路を作らない）
- **design-checklist の該当項目**: 分割・移動の 7 項目（#19 の 2 件 = doc ブロックを 1 単位で運ぶ・テスト件数の突き合わせ、
  #30 の 2 件 = 期待値の検算・`use` を配る先の確認、#44 の 3 件 = 合否判定の無い項目・和集合の限界・許容リスト）

### フェーズ 1 の結果（20:45 green）

| | 結果 |
|---|---|
| red | 成立しない（移動リファクタ）。代わりに着手前に `check.sh` が全 ok であることを確認してから始めた |
| green | `check.sh` **6 項目すべて ok**。`cargo test --locked` 275 passed（lib 162 + 結合 113） |
| 内訳 | `classification:4 query_execution:4 table_format:15 target_table:14`（期待どおり） |
| 行数 | `execution.rs` 448 → **302** ／ `result_output.rs` **157**（見込み 160-170）／ `table_format.rs` 704 → **319** ／ `target_table.rs` **393**（見込み 390-400） |

**ミューテーション 2 件とも検出**:

1. `target_table.rs` の `read_name_part` を `.to_lowercase()` → `.to_string()`
   → `parse_target_table_は引用符の無い名前を小文字にする` が FAILED（left `"CAT"` / right `"cat"`）
2. `result_output.rs` の `write_result` の `should_write` を `false` に固定
   → **`tests/results.rs` で 7 件 FAILED**（`select_の結果を_csv_で書き_フルパスを返す` ほか）

**計画から外れた点 3 件**:

1. **統括役の指示ミス**: ミューテーション 2 件目に `cargo test --test select` を指定したが、
   `tests/select.rs` は S3 出力を検証していない（`GetQueryResults` の応答しか見ない）。
   正しくは `tests/results.rs`。委譲先が気づいて正しい方で確認した
2. **`check.sh` の許容リストに偽陽性**（委譲先が修正）: `^[<>] (table_format|target_table)::parse_target_table\(` が
   実際の行 `Some(statement) => match table_format::parse_target_table(` に前方一致せず NG になっていた。
   `^[<>] .*(table_format|target_table)::parse_target_table\($` に広げた。行末が `(` で終わることを要求しているので許容範囲は広がりすぎていない（統括役が確認）
3. **統括役が 7-5b で `//!` の配分を 1 行直した**: 「Phase 3b は ALTER TABLE ... ADD COLUMNS × Hive を対象に足す（2026-09-21 実測）」が
   `target_table.rs` に入っていたが、これは `EngineDdl::AlterColumnsHive` と `engine_ddl`（**残留側**）の話で修飾名解析の説明ではない。
   `table_format.rs` へ戻した。**`check.sh` のチェック 4 は `src` 全体で突き合わせるのでこの誤配置を検出しない**
   （R3 で指摘された「和集合の限界」と同じ形。目視で拾った）

**7-5b の自己レビュー**: 分岐の突き合わせ → 差分に入った分岐は **0 件**（正規化 diff が示すとおり、追加されたのは
`pub(super)` 付きの宣言 2 行と呼び出しの修飾 3 箇所だけ）。エラーの出口 → 新しいエラー戻り値なし。
呼び出し元 → 7-0 で数えた 6 箇所すべてが期待どおり（`use` の過不足なし、テスト 15/14、`use serde_json::Value;` は残留側のみ）。

（7-0 以降で埋める）

### フェーズ 2 の結果（20:58 green）

文書の追随のみ（本番コードの挙動は 1 行も変えていない）。

- **`src/catalog.rs` 4 箇所**（`:59` `skip_quoted` ／ `:78` `comment_end` ／ `:148` `skip_qualified_name` ／ `:181` `unquote`）
  … `operation/table_format.rs` → `operation/target_table.rs`。**`issue #39 Phase 2` の出典はそのまま**（その事実を作ったのは #39）
- **`CLAUDE.md` 4 箇所** … `:29`（構成の一覧。#39 の追随漏れもここで埋めた）／`:32`・`:33`（`result_output.rs` の に差し替え）／`:46`（クエリ ID の出どころ）
- `tests/table_format.rs:21` は**書き換えない**。指す `probe_sql` は残留側なので今も正しい

**検証**: `check.sh` 6 項目すべて ok（テスト 275・内訳・`///` 435・実測記録 128・正規化 diff・fmt/clippy）。

**design-checklist の「文書」節 4 項目を当てた結果、1 件直した**:
呼び名の揺れ（`CLAUDE.md:29` が「結果ファイルと `.metadata`」、`result_output.rs` の `//!` が「結果ファイル**本体**と `.metadata`」）。
`CLAUDE.md:32` が既に「本体（CSV / TXT）を置き」と書いているので「本体」に揃えた。
「6 つのオペレーション」の数字は API の数（`mod.rs` の再エクスポート 6 関数）でファイル数ではないので**変えない**。

### 7-7: 分割リファクタの検討 → **不要**

この issue 自体が分割なので改めて割る必要は無い。分割後の最大は `target_table.rs` 392 行だが、
うち 246 行がテストで**本体は約 146 行**。`table_format.rs` 319 行・`execution.rs` 302 行・`result_output.rs` 157 行も
すべて 400 行以下。#30 の `render.rs`（339 行・本体 88 行）と同じ形。

## レビュー実施記録

| 工程 | 回数 | 本番影響のある妥当指摘 |
|---|---|---|
| Step 6 計画レビュー（Explore 2 本） | 1 | **2 件**（R1 medium・R3 high。どちらも検証の足場の欠陥で、実装前に潰した） |
| 7-6 フェーズ別レビュー | 0（小規模経路のため省略） | – |
| Step 8 最終パス | 0（小規模経路のため省略） | – |
| Step 8 独立レビュー（general-purpose 1 本） | 1 | **0 件**（medium 以上 0 件。9 issue 連続で 0 件） |

独立レビューは検証コマンドと `check.sh` を自分で走らせ、6 項目すべて ok を確認したうえで
「差分は純粋なファイル分割でロジックの変更は一切ない」と判定。medium 未満として 1 点だけ挙げた
`execution.rs:251` のコメント（`write_result` を修飾なしで参照）は統括役の判断で採用して直した
（呼び出し行 2 箇所は `result_output::` と書いており不統一だった。`execution.rs` 内にもうその関数は無い）。

## 実機検証（Step 9）

バイナリを実際に起動して HTTP で叩いた（結合テストは `athena_local::router` を直接使うので、`main` の配線は別に見る必要がある）。

| # | ケース | 通る経路 | 結果 |
|---|---|---|---|
| 1 | `ATHENA_LOCAL_BIND=127.0.0.1:18844` で起動 | `main` の設定読み取り・ポートのバインド・起動ログ | **ok**（3 行のログが出て待ち受け） |
| 2 | `StartQueryExecution`（`SELECT 1`） | 受付 → `result_location` → `spawn_query`（すべて残留側） | **ok**（`QueryExecutionId` を返す） |
| 3 | Trino が到達不能なときの `GetQueryExecution` | `run` が `Err` → **`result_output::write_failure`** | **ok**（`FAILED` ＋ `StateChangeReason` ＋ `AthenaError`。`ErrorCategory` 1 / `ErrorType` 100） |
| 4 | 偽 Trino を立てて `SELECT 1 AS n` | `run` が `Ok` → **`result_output::write_result`** | **ok**（`SUCCEEDED`。`OutputLocation` が `s3://bucket/path/<id>.csv` に解決） |
| 5 | `GetQueryResults` | `convert` 経由の `ResultSet` | **ok**（列名行 + データ行、`ColumnInfo` の `Precision` 10 まで正しい） |

**未実施**: `target_table.rs` の経路（`DROP TABLE` の修飾名解析 → 形式の問い合わせ）は、
`ResultsMode::None` では問い合わせ自体が走らない設計（`execution.rs:251`。#39 の最終パスの指摘で入った）のため、
実機で通すには S3 互換ストレージが要る。**ユニットテスト 14 件（`operation::target_table::tests`）と
結合テスト 13 件（`tests/table_format.rs`）が担保している**ので足場は立てなかった。

偽 Trino（`fake_trino.py`）はスクラッチパッドに置き、検証後に停止した。作業ツリーは clean。

（Step 8 で埋める）

## dismissed

（なし）

## deferred

（なし）

## 覆した事前判断

（なし）

## 範囲外の発見

| 内容 | 出どころ | 起票 |
|---|---|---|
| `skip_keyword` が `classification.rs` と `target_table.rs` に同名で別定義（トリビアの読み飛ばしを内側でするかだけが違う） | Step 3 の調査（Explore C が両方の本文を突き合わせ） | **#49** |
| `write_result` が 69 行で 50 行を超える | Step 3 の調査 | **起票しない**。#19 の設計判断 6 が「分岐の形を書き換えると『1 行も変えない』制約の保証が弱くなる」として起票もしないと決めた先例に従う |
| `tests/table_format.rs:21` が `src/operation/table_format.rs` をパスで参照 | Step 3 の調査 | **対応不要**。指す `probe_sql` は残留側なので今も正しい |
| `CLAUDE.md:44` が `operation/query_execution.rs` を名指し | Step 3 の調査 | **対応不要**。残留側で変わらない |

## 委譲の記録

| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| quick のゲート判定 | 自分 | quick-implement の規定（コードに触れる前の判定は統括役） |
| Step 3 調査 | 委譲（Explore 3 本） | 規則どおり |
| パス参照 8 件の本文確認 | 自分 | 8 行を読むだけで、指示を書く方が長い |
| Step 3 計画 | 委譲（Plan 1 本） | 規則どおり |
| 検証スクリプト | 自分 | 60 行のシェル 1 本。指示を書く方が長い（Plan の待ち時間に作った） |
| Step 6 計画レビュー | 委譲（Explore 2 本） | 規則どおり |
| フェーズ 1 実装 | 委譲（general-purpose 1 本） | 移動 513 行で小規模経路の目安 150 行を超える |
| フェーズ 2 実装 | 自分 | 文書 8 箇所の置換。委譲の固定費の方が長い（小規模経路の緩和） |
| Step 8 独立レビュー | 委譲（general-purpose 1 本） | 規則どおり |
| Step 9 実機検証 | 自分 | 起動とcurl 数本。指示を書く方が長い |

## 出荷

| | |
|---|---|
| PR | **#50** https://github.com/aoyagikouhei/athena-local/pull/50 |
| 起票した issue | **#49**（`skip_keyword` の重複） |
| issue コメント | #44 に出荷報告を投稿 |
| 出荷判定 | 検証の green（`check.sh` 6 項目 + `cargo test --locked` 275 passed）・7-5b の分岐突き合わせ 0 抜け・実機検証 5 ケース ok の 3 つで判定。独立レビューは medium 以上 0 件 |

**ノートに `未確認|未実施|未検証|未実測` が残る箇所の始末**:

- 実機検証の「`target_table.rs` の経路は未実施」… `ResultsMode::S3` が要るため。**ユニット 14 件 + 結合 13 件が担保**しているので起票しない
- 計画の「残っている前提 1（新ファイルの総行数は算出）」… 実測で確定（`result_output.rs` 157・`target_table.rs` 393。見込み 160-170 / 390-400 の範囲内）

**コミット本数と差分の規模は PR 本文の「予算の実績」に置く**（ノートに書くと後から積まれた分で古くなる。
`git log --oneline 6ed63dc..main-44` で引ける）。

## 時系列

| 時刻 | 出来事 |
|---|---|
| 19:26 | quick-implement 着手。ゲート判定を開始 |
| 19:28 | 移動前のテスト件数を実測（275 passed / table_format 29） |
| 19:30 | ゲート 3 観点が外れ、ユーザーが implement-issue を選択。quick ノートを `6ed63dc` でコミット、issue #44 にコメント投稿 |
| 19:33 | ブランチ `main-44` を作成（ベース `main`） |
| 19:34 | Step 3 の Explore 3 本を並列起動（移動対象の全文と依存 / 先行分割 #19・#30 の手順 / `skip_keyword` の重複と検証の足場） |
| 19:36 | 移動後には取れない基準値を実測（`///` 435 行・テスト 162 件） |
| 19:38 | Explore C が返る。置き場所の決め手（`super` の指す先）と `skip_keyword` の非統合を確定 |
| 19:40 | Explore B が返る。#19/#30 の 2 フェーズ構成・正規化 diff・50 行超関数を触らない判断を継承 |
| 19:42 | パス参照 8 件の本文を統括役が読み、要追随は `catalog.rs` の 4 件だけと確定 |
| 19:44 | Explore A が返る。quick の行範囲が doc を 34 行取りこぼしていたと判明（#19 の罠）。`verb` の越境依存も判明 |
| 19:45 | Plan を 1 本起動。同時に Step 4 の論点（CLAUDE.md の範囲）をユーザーに確認 |
| 19:46 | ユーザーが「CLAUDE.md を最新化する」を選択（D6） |
| 19:50 | 検証スクリプト `44-verify/check.sh` を作成。変更前 ok ／ doc 1 行を消すと NG を確認（2 段階の受け入れ判定） |
| 19:56 | Plan が返る。`ENGINE_DDL_CONTENT_TYPE` の移動漏れを発見。ファイル名を `result_output.rs` / `target_table.rs` に確定 |
| 19:58 | 計画レビュー（Step 6）の Explore 2 本を起動 |
| 19:59 | 統括役が CLAUDE.md の言及を grep で取り直し、Plan の `:46` 判定の誤りを訂正 |
| 20:03 | 計画レビュー 1 本目が返る。R1（medium・採用）: `check.sh` 項目 5 の偽陽性 |
| 20:05 | 計画レビュー 2 本目が返る。R3（**high**・採用）: チェック 2 に合否判定が無く移動漏れを 6 項目すべてが見逃す |
| 20:06 | `check.sh` に R1 の許容リストと R3 の期待値判定を実装。空ファイルで NG が出ることを実測 |
| 20:07 | ノートを 682 → 494 行に圧縮し、計画確定を `440fca8` でコミット |
| 20:09 | Step 6b でユーザーが設計を承認（issue 本文を覆す 3 点を含む） |
| 20:11 | フェーズ 1（分割本体）を general-purpose に委譲。7-0 は分岐 0 件 |
| 20:12 | 先行作業としてフェーズ 2 の文案（CLAUDE.md 4 箇所・catalog.rs 4 箇所）を用意 |
| 20:45 | フェーズ 1 の実装が返る。`check.sh` 6 項目 ok、ミューテーション 2 件とも検出 |
| 20:50 | 7-5b で `//!` の配分を 1 行訂正（Phase 3b の記述は残留側の話だった） |
| 20:55 | フェーズ 1 を `6644b02` でコミット |
| 20:58 | フェーズ 2（文書の追随）を自分で実装。呼び名の揺れを 1 件直し `aa9cc96` でコミット |
| 20:59 | 独立レビューを起動し、並行して実機検証を開始 |
| 21:00 | 実機検証 5 ケースすべて ok（起動・受付・`write_failure`・`write_result`・`GetQueryResults`） |
| 21:01 | 独立レビューが返る。medium 以上 **0 件**。medium 未満の 1 件を採用して `00ee42e` でコミット |
| 21:02 | #49 を起票（`skip_keyword` の重複） |
| 21:03 | **PR #50 を作成**し、#44 に出荷報告をコメント |
