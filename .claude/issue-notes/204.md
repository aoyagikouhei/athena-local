# #204 引用符付きの名前を取る DESCRIBE・SHOW CREATE TABLE・ALTER TABLE・DROP TABLE を本物は開始時に弾く

## 基本情報
- issue #204。ベース main、ブランチ main-204。着手前 SHA 34f5e68（#200 のマージ後）
- 出どころ: #200 の実測（run-20260925-055931、`tools/measure/keyword-boundary.sh`）。結果は `docs/dev/measurements/statements.md` の「引用符付きの名前は空白の有無によらず開始時に弾かれる」
- issue のコメント: OPTIMIZE は Trino に無く athena-local も弾くので、差は DESCRIBE・DESC・SHOW CREATE TABLE・ALTER TABLE ... ADD COLUMNS・DROP TABLE の 5 種

## 事前に確認した事実
### 開始時の 400 の経路（Explore A）
- A1 StartQueryExecution の検証順は トークン → OutputLocation → 構文（`src/operation/execution.rs:50-77`。2026-09-24 実測）。構文チェックは `app.trino.syntax_error`（`src/trino.rs:166-181`、PREPARE、`errorName == SYNTAX_ERROR` のときだけ Some）。400 の本文は `invalid_request_with_code(message, "MALFORMED_QUERY")`（`src/response.rs:71-79`、4 キー `__type`・`AthenaErrorCode`・`ErrorCode`・`Message`）。文言は Trino のまま、行番号だけ `unshift_line`（trino.rs:243-254）
- A2 SQL の中身を athena-local 独自に見て 400 にする箇所は今は 0（request.rs は JSON だけ、validation.rs はページングだけ）
- A3 本物が開始時に弾くと分かっている文: #200 の 5 種、`SELECT FROM`（`Queries of this type are not supported`。result-files.md:334）、不完全な文（caveats.md:33-34 に既知の差として記載）、`CREATE OR REPLACE TABLE`（Trino の文言と同じ形）
- A4 結合テストの型: `tests/syntax.rs`（`構文エラーは開始時に_400_で弾き_本体は_trino_に送らない` など 6 本。偽 Trino の `syntax_check_response`、tests/common/mod.rs:117-119・494-518）
- A5 決定: decisions.md:73「SQL の本文は書き換えない」理由 (3)「SQL の解釈を持たない。正しさは Trino と実測した Athena が決める」、:65「crate は SQL が正しいかどうかを判定しない」。弾く判定を足すのはこの 2 つに触れる（判定の規則は athena-local 側に置き、crate は読むだけなら :65 と両立）
### 引用符付きの名前を前提にした既存（Explore B）
- B1 `TRINO_CATALOG_MAP` の別名は引用符付きのカタログ名だけを置換（`src/catalog.rs`）。これを使う結合テスト `tests/table_format.rs:161-198`（`DROP TABLE "s3tablescatalog/my-bucket".ns.t` が SUCCEEDED）と `tests/describe.rs:311-342`（`DESCRIBE "s3tablescatalog/b".ns.t`）。**引用符付きの先頭部を一律に弾くと S3 Tables の DESCRIBE・DROP TABLE が使えなくなる。本物が `"s3tablescatalog/…".ns.t` を受けるかは未実測**
- B2 対象テーブルの解析 `target_table.rs:21-31,88-92`（DROP TABLE・ALTER ADD|REPLACE COLUMNS・SHOW CREATE TABLE・DESCRIBE・SHOW COLUMNS）、`completion.rs:36-42` describe_target_name、`format_probe.rs:40-63`
- B3 引用符付きの名前を使うテスト: classification/tests.rs（:149-197 #39 の `ALTER TABLE "my table"`・`DROP TABLE "my table"`、TIGHT_AND_SPACED の後半、#195 固定表 :567-608）、target_table/tests.rs（:89-104・192-220・274-298・314-321・331-355・461-483・486-507・509-683）、completion.rs:253-286。分類・解析の単体テストは「弾くかどうか」と独立なので、弾く判定を別に足すなら書き直しは要らない見込み
- B4 docs: `docs/ddl.md:86-95`（「quoted or not」）、caveats.md:65-82（#200 の記述。ALTER は誤り）、api.md:142-149
- B5 JDBC・PyAthena・tools/e2e は引用符付きの名前を投げていない
- B6 バッククォートは Trino が構文エラーにする（caveats に記述なし。本物は Hive 系の文で受ける可能性）
- B7 未実測: 修飾名の一部だけ引用符（`db."t"`・`"db".t`）、`"AwsDataCatalog".db.t`、`IF EXISTS`、`SHOW COLUMNS FROM "t"`、ALTER の DROP COLUMN・RENAME TO・ADD COLUMN（Trino の綴り）、`DESCRIBE "t" PARTITION`、MSCK
### 実測の足場（Explore C）
- C1 雛形 `tools/measure/keyword-boundary.sh`。開始時に弾かれた文言の全文を実名マスクで summary に出す節（:797-804）がある。後始末は `run z-drop-*`（無引用 + IF EXISTS）に揃えれば結果を確かめられる
- C2 文言の手がかり: Trino 系は `mismatched input 'X'. Expecting: ...`、Hive 系は `no viable alternative at input`・`mismatched input '...' expecting {...}`（小文字・波括弧）・`ParseException`。`Queries of this type are not supported` は `SELECT FROM` でも出る汎用の拒否
- 落とした番号: A-7（api.md の構文エラーの段落。docs 更新時に見る）、C-6（development.md の流し方。既知）
- 16:24 手元の Trino 482（memory カタログ）: 受ける `DESCRIBE "t"`・`DESC "t"`・`SHOW CREATE TABLE "t"`・`SHOW COLUMNS FROM "t"`・`ALTER TABLE "t" ADD COLUMN m int`・`DROP TABLE "t"`・`DROP TABLE IF EXISTS "nope"`・`DESCRIBE "default"."t"`・`DESCRIBE default."t"`。弾く `ALTER TABLE "t" ADD COLUMNS (m int)` と無引用の `ALTER TABLE t ADD COLUMNS` はどちらも `line 1:21: mismatched input 'COLUMNS'. Expecting: '.', 'ADD'`（本物が引用符付きに返した文言と同じ形。本物は無引用なら成功）、`ALTER TABLE "t" SET TBLPROPERTIES`・`SHOW TBLPROPERTIES "t"` も構文エラー
  - → ALTER TABLE "t" ADD COLUMNS は athena-local も今すでに（Trino の構文チェックで）弾いている。差は DESCRIBE・DESC・SHOW CREATE TABLE・DROP TABLE の 4 種。#200 の docs/caveats.md が「5 種は実行する」と書いたのは ALTER について誤り（この issue で直す）
- 16:21 src に「Queries of this type are not supported」を返す経路は無い（grep 0 件）。過去の実測の生データ（~/athena-*-measurements の Query と .sql）に、引用符付きの名前でこれらの文を投げた記録は #200 以外 0 件

## 実測の手順
- `tools/measure/quoted-names.sh`（雛形 keyword-boundary.sh。S3 の読み取りは外し、開始時に弾かれた文言は start.err から Message と AthenaErrorCode を抜く）
- 実行: `tools/dev.sh OUTPUT=s3://<バケット>/<プレフィックス>/ DB=<DB> S3TABLES_CATALOG=s3tablescatalog/<bucket> S3TABLES_NS=<ns> S3TABLES_TABLE=<table> bash tools/measure/quoted-names.sh`（PROBE_DDL の既定は 1）
- 出力: `~/athena-quoted-names-measurements/run-<日時>/summary.txt`（実名マスク済み）
- StartQueryExecution 68 本（S3 Tables ありで 72）。DDL は準備の Hive テーブル `athena_local_probe_204` を作って無引用 IF EXISTS で消すだけ（結果を summary に残す、trap は保険）。DROP・ALTER は実在しない名前 `athena_local_probe_204_nope`（S3 Tables は `nope_204`）にだけ投げる
- ラウンド 2: `ROUND=2` で U1〜U7 だけを流す（ROUND=1 の項目はラベル 72 件が実データと一致することを偽の aws で確認済み）。StartQueryExecution 45 本（S3 Tables ありで 55、CREATE TABLE が万一成功したら後始末 +2）。start.err は加工せず保存、summary では実際のタブ・CR・改行を `<TAB>` などに変換
- 受け入れ判定（統括役 16:48）: `--debug` の投げ直しを外させた（start.err に AthenaErrorCode が出る。生ログの署名を残さない）／破壊的な文の宛先を grep で確認／bash -n ok／ドライランは未実施

## 実測の結果
2026-09-25 07:54 UTC（run-20260925-075408、`tools/measure/quoted-names.sh`、PROBE_DDL=1、S3 Tables あり、StartQueryExecution 72 本。後始末 z-drop-hive SUCCEEDED）
### 決め手 1: 引用符付きの部分を 1 つでも含む名前は開始時に弾かれる（MALFORMED_QUERY）。無引用・バッククォートは通る
| 文 | 通った（SUCCEEDED／対象が無く FAILED） | 開始時に弾かれた |
|---|---|---|
| DESCRIBE（n0〜n11） | `t`・`db.t`・`awsdatacatalog.db.t`・`` `t` ``・`` `db`.`t` `` | `"t"`・`"T"`・`"db"."t"`・`db."t"`・`"db".t`・`"AwsDataCatalog".db.t`・`"awsdatacatalog"."db"."t"` |
| DESC | `t`・`` `t` `` | `"t"`・`"db"."t"`・`db."t"`・`"db".t` |
| SHOW CREATE TABLE（n0〜n11） | DESCRIBE と同じ 5 形 | DESCRIBE と同じ 7 形 |
| SHOW COLUMNS FROM／IN | `t`・`` `t` `` | `"t"`・`"db"."t"`・`db."t"`・`"db".t`、`IN "t"` |
| DROP TABLE（実在しない名前） | `nope`・`` `nope` ``（FAILED、対象なし）、`IF EXISTS nope`（SUCCEEDED） | `"nope"`・`"db"."nope"`・`db."nope"`・`"db".nope`・`IF EXISTS "nope"` |
| ALTER TABLE "nope" … | — | `ADD COLUMNS`・`SET TBLPROPERTIES`（どちらも Trino の文言。Trino も同じく弾く）、`ADD COLUMN`・`DROP COLUMN`・`RENAME TO`（Hive 系の文言） |
| ALTER TABLE nope …（無引用） | `ADD COLUMNS`・`DROP COLUMN`・`RENAME TO`・`SET TBLPROPERTIES`（FAILED、対象なし） | `ADD COLUMN`（単数）: `line 1:45: no viable alternative at input 'ALTER TABLE <nope> ADD COLUMN'` |
| MSCK REPAIR TABLE | `t`（SUCCEEDED、MSCK_REPAIR） | `"t"`（Trino の文言 `mismatched input 'MSCK'`。Trino に MSCK は無い） |
| S3 Tables `"s3tablescatalog/<b>".<ns>.<t>` | — | DESCRIBE: `Unsupported DDL with 2 catalogs`／SHOW CREATE TABLE: `Queries of this type are not supported`／`DROP TABLE IF EXISTS "<cat>".<ns>.nope_204`: `line 1:22: mismatched input '"<cat>"' expecting {…}` |
- S3 Tables の対照 `SELECT * FROM "<cat>".<ns>.<t> LIMIT 1` は `SCHEMA_NOT_FOUND`（名前空間の指定の綴り違いとみられる）。上の 3 つは名前空間を解決する前の開始時の拒否なので結論は変わらないと判断（前提として残す）
### 決め手 2: 文言の規則（Trino が構文として受ける文だけ。Trino が弾く文は Trino の文言がそのまま出る＝判定の順は「構文チェック → この拒否」）
| 文 | 名前の最初の部分が引用符付き | 最初の部分が無引用で後ろが引用符付き（`db."t"`） |
|---|---|---|
| DESCRIBE・DESC・ALTER TABLE（Trino が受ける操作） | `line L:C: no viable alternative at input '<文の最初の語 … 最初の引用符付きの部分の終わり>'`（例 `'DESCRIBE "t"'`・`'ALTER TABLE "nope"'`） | `line L:C: no viable alternative at input '<名前の始まり … 引用符付きの部分の終わり>'`（例 `'db."t"'`。ALTER は未実測） |
| SHOW COLUMNS FROM／IN・DROP TABLE［IF EXISTS］ | `line L:C: mismatched input '<最初の部分>' expecting {…}`（一覧は 5 件とも同一、#200 の DROP とも同一） | 同上の no viable alternative |
| SHOW CREATE TABLE | `Queries of this type are not supported`（位置なし。末尾の空白があっても同じ） | 同じ |
| DESCRIBE の S3 Tables（最初の部分が別カタログ） | `Unsupported DDL with 2 catalogs`（位置なし） | — |
- C は引用符付きの部分の開始位置 + 1（1 文字目が 1）。先頭の空白は数えない（`  DESCRIBE "t"` → `1:10`）、先頭のコメントは数える（`/* c */ DESCRIBE "t"` → `1:18`。input にはコメントを含めない）、改行の後は `line 2:1`（`DESCRIBE\n"t"`）。空白 2 つはそのまま数え input にも残る。小文字のキーワードも同じ規則で input は元の綴り
- input の中の改行は文字どおりの `\n`（バックスラッシュと n）で出る
### ラウンド 2（2026-09-25 08:35 UTC、run-20260925-083528、ROUND=2、S3 Tables あり、55 本。後始末 SUCCEEDED、CREATE TABLE は 2 本とも開始時に弾かれ何も作られていない）
- 名前の形ごとの文言（q = 最初の引用符付きの部分の番号、k = 部品数。p_i は i 番目の部分）:
  | 文 | q=1（k=1〜3） | q=2（k=2・3） | q=3（k=3） |
  |---|---|---|---|
  | DESCRIBE・DESC | NV(文の最初の語 … p1 の終わり) | NV(名前の始まり … p2 の終わり)（例 `'awsdatacatalog."db"'`） | MM(p3)（例 `mismatched input '"t"' expecting {…}`） |
  | SHOW COLUMNS FROM／IN・DROP TABLE［IF EXISTS］ | MM(p1) | NV(名前の始まり … p2 の終わり) | MM(p3) |
  | ALTER TABLE（Trino が受ける操作） | NV(文の最初の語 … p1 の終わり) | NV(文の最初の語 … p2 の終わり)（k=2 だけ実測。k=3 の q=2 は未実測） | NV(文の最初の語 … p3 の終わり) |
  | SHOW CREATE TABLE | `Queries of this type are not supported` | 同じ | 同じ |
  | DESCRIBE・DESC・SHOW COLUMNS の最初の部分が S3 Tables のカタログ | `Unsupported DDL with 2 catalogs`（全部引用符付き・存在しない表でも同じ） | | |
  | DROP・ALTER・SHOW CREATE の最初の部分が S3 Tables のカタログ | 上の一般の規則どおり（MM／NV／定数） | | |
  NV = `line L:C: no viable alternative at input '…'`、MM = `line L:C: mismatched input '…' expecting {…}`（一覧は全部同一、md5 一致）。C は対象の部分の開始位置 + 1
- 位置: 先頭のタブ・LF・CRLF・連続 LF も数えない（すべて `1:10`）。行コメント `-- c\n` の後は `line 2:10`、改行入りのブロックコメントの後は `line 2:15`。列は **UTF-16 の単位**（`/* あ */` の後は 18、`/* 😀 */` の後は 19）。区切りのタブは `1:10` で input に `\t`、区切りの CRLF は `line 2:1` で input に `\r\n`（どちらもバックスラッシュ表記）→ R3 で外した `\r`・`\t` のエスケープは実測で要ると分かった
- ALTER TABLE IF EXISTS は引用符の有無によらず `line 1:16: no viable alternative at input 'ALTER TABLE IF EXISTS'`（無引用も本物は弾く。athena-local は Trino が受けるので実行する。範囲外）
- DESCRIBE EXTENDED／FORMATTED "t" は Trino と同じ文言（`mismatched input '"t"'. Expecting: '.', <EOF>`）で、athena-local も今すでに同じ
- 引用符付きの名前を取るほかの文: `SHOW TABLES IN "db"` は MM(p1)（Trino は受ける＝差）、`CREATE TABLE "x" (n int)` は NV(文の最初の語 … p1)（Trino は受ける＝差）、`DROP DATABASE IF EXISTS "x"` は Trino と同じ文言（差なし）
- `DESCRIBE "日本"`（存在しない非 ASCII の名前）は `Entity Not Found (Service: AmazonDataCatalog; … Request ID: <毎回違う>)`。構文の文言にならない（再現できない ID を含む）
- 範囲外（本物だけ受ける・本物だけ弾く、無引用）: 無引用の `DROP DATABASE`（本物は受ける、athena-local は Trino が弾く）、無引用の `CREATE TABLE x (n int)`（本物は `No location was specified for table. An S3 location must be specified` で開始時に弾く、athena-local は作る）
- S3 Tables の対照 SELECT は 2 回とも SCHEMA_NOT_FOUND（名前はユーザーがコンソールから写したもの。Lake Formation の権限の可能性）。DESCRIBE などの拒否はそれより前の段階なので結論は変わらないと判断
### ほかに分かったこと（範囲外）
- 本物は無引用の `ALTER TABLE t ADD COLUMN m int`（Trino の綴り）を開始時に弾く。docs/ddl.md:48・caveats.md:94-105 は athena-local で ADD COLUMNS の行に届く書き方として `ADD COLUMN` を勧めている（本物では弾かれることが書かれていない）
- 本物はバッククォートの名前（`` DESCRIBE `t` ``・`` DROP TABLE `t` `` など）を受ける。athena-local は Trino が構文エラーにする
- 本物は無引用の `MSCK REPAIR TABLE t` を受ける（MSCK_REPAIR）。athena-local は Trino に MSCK が無いので弾く

## 実測の経緯
- 2026-09-25 17:35 JST ラウンド 2（run-20260925-083528）: 55 本すべて測れた。S3 Tables の対照 SELECT は再び SCHEMA_NOT_FOUND
- 2026-09-25 16:54 JST ラウンド 1（run-20260925-075408）: 72 本すべて測れた。S3 Tables の対照 SELECT だけ SCHEMA_NOT_FOUND（ユーザーの指定の綴り違いとみられる）

## 設計判断
- D1 判定は構文チェックの直後、新モジュール quoted_names.rs、名前の前のキーワード列は target_table の table_name_start を再利用（計画・R1）
- D2 未実測の形の扱い（6b、17:18）: ユーザーの判断で「先に追加の実測をする」。ラウンド 2（ROUND=2）で U1〜U7 を測ってから一致条件を確定
- D3 範囲外の差 3 つ（ADD COLUMN 単数を本物は弾く・バッククォートを本物は受ける・無引用の MSCK を本物は受ける）は、この PR の docs（caveats・ddl.md・measurements）に書く（6b、ユーザーの判断）。コードは変えない

## 計画
（Plan の下書き 16:43 を土台に、実測の結果で統括役が確定。Plan から覆した点は「覆した事前判断」）

### 方式
- 新しいモジュール `src/operation/quoted_names.rs`（純関数。I/O 無し）: `rejection(query: &str, is_alias: impl Fn(&str) -> bool) -> Option<String>` が、本物が開始時に弾く形なら文言を返す
- 呼び出し: `execution.rs` の構文チェック（:73-77）の**直後**。Some なら `invalid_request_with_code(message, "MALFORMED_QUERY")`。Trino が構文エラーにする文（`ALTER TABLE "t" ADD COLUMNS`・`SET TBLPROPERTIES`・`MSCK`）は本物も Trino の文言を返したので、構文チェックを先にする（実測の決め手 2）。`is_alias` は `app.config.catalog_map.contains_key`
- 一致条件と文言（2 ラウンドの実測で確定。**実測した形だけ弾き、それ以外は今までどおり実行する**＝D2。q・k・NV・MM は「実測の結果」の記号）:
  | 文（名前の前のキーワード） | 弾く形 | 文言 |
  |---|---|---|
  | DESCRIBE・DESC | 最初の部分が S3 Tables の別名（`catalog_map` のキー） | `Unsupported DDL with 2 catalogs` |
  | 同上 | k≤3 で q=1／q=2／q=3（引用符付きの部分に非 ASCII があれば弾かない＝u5-jp） | q=1: NV(文の最初の語…p1)、q=2: NV(名前の始まり…p2)、q=3: MM(p3) |
  | SHOW COLUMNS FROM／IN | 最初の部分が別名 | `Unsupported DDL with 2 catalogs` |
  | 同上・DROP TABLE［IF EXISTS］ | k≤3 で q=1／2／3 | q=1: MM(p1)、q=2: NV(名前の始まり…p2)、q=3: MM(p3) |
  | SHOW CREATE TABLE | k≤3 でどこかが引用符付き | `Queries of this type are not supported` |
  | ALTER TABLE（IF EXISTS なし。Trino が受ける操作＝構文チェックの後なので自然に絞られる） | (k,q) が (1,1)(2,1)(3,1)(2,2)(3,3) | NV(文の最初の語…p_q の終わり)。(3,2) は未実測なので弾かない |
  | SHOW TABLES IN | k=1 で引用符付き | MM(p1) |
  | CREATE TABLE（CTAS でない＝`substatement_type == CREATE_TABLE`） | k=1 で引用符付き | NV(文の最初の語…p1) |
- 位置: 先頭の ` `・`\t`・`\r`・`\n` を除いた SQL で、L = 対象の部分より前の `\n` の数 + 1、C = 直前の `\n` からの UTF-16 の単位数 + 1。input の中の `\n`・`\r`・`\t` はバックスラッシュ表記（R3 は実測で覆った）
- キーワード列は `target_table::table_name_start` を `pub(super)` にして使う（DESCRIBE・DESC・SHOW COLUMNS FROM／IN・DROP TABLE・SHOW CREATE TABLE・ALTER TABLE・SHOW TABLES IN・CREATE TABLE）。ALTER は `IF` で始まれば弾かない
- EXPECTING は実測の生データ（x-n1.start.err）から機械的に取り出した定数。転記後に生データと diff で突き合わせる
- 読む道具は既存の `athena_sql::Cursor::keyword`・`qualified_name`・`QualifiedName`/`NamePart`（`text` が `"` で始まるか）。crate には足さない（decisions.md:65。規則は athena-local 側）
- classification・target_table・completion は変えない（分類と「開始時に弾く」は別）

### フェーズ（小規模経路: 2 フェーズ）
- P1: `src/operation/quoted_names.rs`（新規。本体 90〜120 行 + EXPECTING 定数 1 行 + 単体テスト）、`src/operation/mod.rs`（+1）、`src/operation/execution.rs`（+4）。結合テスト `tests/syntax.rs` に追加。既存の `tests/describe.rs:311-342`・`tests/table_format.rs:161-198`（S3 Tables の引用符付きカタログ名で SUCCEEDED を前提）を 400 の期待に書き換える
- P2: docs（caveats.md の #200 の項を実測に合わせて書き直し＝ athena-local も弾くように、ALTER の誤りも直る／api.md の構文エラーの段落／ddl.md:86-95 の quoted or not／decisions.md に「開始時に弾く判定」の決定と理由／architecture.md／measurements／unmeasured／CHANGELOG）。範囲外の差 3 つ（ADD COLUMN 単数・バッククォート・MSCK）を caveats に書くか起票するかは 6b で聞く

### テスト（何を固定するか）
- 単体（quoted_names.rs）: 実測した形ごとに文言の完全一致（D・E・C・L・X・A 群と位置の規則の P 群、S3 Tables 3 種）と、弾かない形（無引用・`db.t`・`awsdatacatalog.db.t`・`SHOW CREATE VIEW "v"`・`DROP VIEW "v"`・`CREATE TABLE "t" AS SELECT`・`SELECT * FROM "t"`・`DESCRIBE EXTENDED t`）が None
- 結合（tests/syntax.rs）: `DESCRIBE "t"` が 400・`MALFORMED_QUERY`・本物の文言で、構文チェックは送るが実行は作らない。構文チェックが SYNTAX_ERROR を返す形はそちらの文言が勝つ。`ExecutionParameters` 付きでも元の SQL で判定
- 既存 2 本（S3 Tables）: 400 と `Unsupported DDL with 2 catalogs`／DROP は mismatched の文言に書き換え

### 前提表
| 前提 | 破らないか | 保証元 |
|---|---|---|
| 検証順 トークン → OutputLocation → 構文 | 構文の直後に置く（本物も Trino の構文エラーが先。実測） | execution.rs:50-77 |
| 構文チェックと同じく置換前・パラメータ適用前の SQL を見る | `request.query_string` を渡す | execution.rs:75・283 |
| 400 の本文は 4 キー | `invalid_request_with_code` をそのまま | response.rs:71-79 |
| 弾いた要求は Store に乗らない | submit の前で return | execution.rs:83 |
| SQL を書き換えない | 読むだけ | decisions.md:73 |
| `qualified_name` は引用符付きの部分を `"` 付きの text で返し、読めなければ None で位置を進めない | 読めなければ弾かない（今までどおり実行） | name.rs:41-74 |

### 壊す候補
1. 構文チェックの前に置く（Trino の文言が負ける）→ ADD COLUMNS の結合テスト 2. 「後ろの部分だけ引用符付き」を見ない → `db."t"` のテスト 3. 位置の +1 を外す 4. 先頭の空白を数える → `  DESCRIBE "t"` 5. 改行のエスケープを外す 6. 別名の判定を外す → S3 Tables の DESCRIBE 7. SHOW CREATE TABLE のキーワードを SHOW CREATE に縮める → `SHOW CREATE VIEW "v"` が弾かれる

### 先行できる作業
- 実機検証の足場（`.claude/issue-notes/204-verify/e2e.sh`。#200 の足場 git 履歴 b3f59d9 が雛形）: compose の athena-local に実測の形を投げ、400 と文言・弾かない形の SUCCEEDED を確かめる。起動: 計画レビューと同時

### 人間検証リスト
- なし（本物の実測は済み。S3 Tables の対照 SELECT が SCHEMA_NOT_FOUND だった点は前提として残す）

## 計画レビューの結果
Explore 2 本（契約・呼び出し元と実測の突き合わせ／構造・削減）
| # | 指摘 | sev | 判定 |
|---|---|---|---|
| R1 | `quoted_names.rs` が target_table.rs の `keywords()`・`table_name_start()`（:21-31・:71-86）と同じキーワード列を作り直す（#49 で統合した形の再発） | medium〜high | 採用。`table_name_start` を `pub(super)` にして再利用する。ただし ALTER の `IF EXISTS` は読み飛ばさない（本物は ALTER TABLE IF EXISTS を別の文言で弾く。#43 実測、result-files.md:246）ので、ALTER だけ残りの先頭が `IF` なら判定しない |
| R2 | ALTER の「後ろの部分だけ引用符付き」は未実測の一般化 | 削減 | 6b で聞く（未実測の形の扱い、Q1） |
| R3 | `\r`・`\t` のエスケープは未実測 | 削減 | 一度採用したが、ラウンド 2 で `\t`・`\r\n` もバックスラッシュ表記と実測されたので 3 つともエスケープする |
| R4 | DESC の S3 Tables の別名は未実測なのに DESCRIBE と同じ文言を当てている | medium | 6b で聞く（Q1 と同じ論点） |
契約の観点が確かめて問題なしとしたもの: 文言・位置・EXPECTING が生データと 1 文字単位で一致（no viable 22 件、mismatched 11 件は md5 一致）／既存の結合テストへの影響は 2 本で漏れなし／弾かない形（SHOW CREATE VIEW・DROP VIEW・CTAS・CREATE VIEW・INSERT・SELECT・DESCRIBE EXTENDED・SHOW COLUMNS LIKE・無引用 DROP IF EXISTS）／冪等と GetQueryExecution への影響なし

## フェーズの記録
### P1 7-0
- 変えるもの: `target_table::table_name_start` を `fn` → `pub(super) fn`（呼び出し元 target_table.rs:44 の 1 か所。挙動は変えない）。execution.rs の構文チェックの後に 1 分岐足す（依存: 構文チェックで Trino の文言が返る形はそちらが先）
- 増える分岐（quoted_names.rs）: 文 9 種の一致（DESCRIBE・DESC・SHOW COLUMNS FROM・IN・DROP TABLE・SHOW CREATE TABLE・ALTER TABLE・SHOW TABLES IN・CREATE TABLE）／名前が読めない・k>3・引用符付きの部分が無い → None／別名（DESCRIBE・DESC・SHOW COLUMNS）／非 ASCII（DESCRIBE・DESC）→ None／q=0・1・2 の文言の出し分け（文ごと）／ALTER の IF → None・(3,2) → None／SHOW TABLES IN・CREATE TABLE の k≠1 → None／CREATE TABLE の CTAS → None／位置（先頭の空白・改行・コメント・UTF-16）／エスケープ（\n・\r・\t）。固定するテストはそれぞれ単体テストに 1 件ずつ（名前は実装時に表へ）
- 境界入力: `"a""b"`（引用符の重ね）、非 ASCII（あ・😀・日本）、改行・CRLF・タブ、空白 2 つ、小文字のキーワード
- checklist: 「近似判定を出力を左右する分岐に転用していないか」（判定は実測した形に絞った）、「無いことの証拠」系は該当なし
### P1 結果
- red: 単体 7 本（弾く側。弾かない側 2 本は空の関数でも通る）、結合 3 本（配線を外して `--no-fail-fast`）。Trino の文言が勝つ結合テストは今の構文チェックで既に通る（順番を守る回帰テスト）
- green: fmt ok／clippy -D warnings ok／cargo test 430 passed 0 failed。EXPECTING は生データ 3 件（x-n1・u7-showtables-q・#200 の d3）と diff で一致
- mutation（単独フィルタ）13 件すべて落ちた: 名前の始まり→文の最初の語 2／位置の +1 6／先頭の空白 1／エスケープ 1／別名 1／SHOW CREATE に縮める 1／非 ASCII 1／ALTER の (3,1) 1／UTF-16→文字数 1／ALTER の IF 1／CTAS 1／3 番目を no viable に 3／構文チェックの前に置く 1
- 7-5b: 分岐は 7-0 の一覧すべてにテスト有り／エラーの出口は既存の 400 と同じ形だけ／table_name_start は可視性だけ変更（呼び出し元 target_table.rs:44 の挙動は同じ）
- 既存テストの書き換え: `tests/describe.rs` の `describe_は_iceberg_の引用符付きの修飾名にも別名を当てて_show_create_table_を問い合わせる` → `describe_は_s3_tables_の引用符付きのカタログ名を本物と同じく開始時に弾く`、`tests/table_format.rs` の `引用符付きのカタログ名にも別名を当てて問い合わせる` → `drop_table_は_s3_tables_の引用符付きのカタログ名を本物と同じく開始時に弾く`（本物がこの形を弾くため。別名を当てて形式を問い合わせる経路は、実行の既定のカタログが別名のときに残る）
- 7-6: 小規模経路のため省略。7-7: 分割不要（quoted_names.rs 134 行、テストは子モジュール 256 行）
### P2 結果（docs。委譲して統括役が差分を確認）
- caveats.md（#200 の項を「athena-local も同じ文言で弾く」に書き直し、未実測で実行する形の列挙、S3 Tables、範囲外の差 5 件＝ADD COLUMN 単数・バッククォート・MSCK・DROP DATABASE・素の CREATE TABLE、ALTER TABLE IF EXISTS の文言を実測の値に）・api.md・ddl.md（quoted or not、ADD COLUMN 単数の注意）・configuration.md（TRINO_CATALOG_MAP）・decisions.md（新しい決定 3 件、#200 の記述を更新）・architecture.md（quoted_names.rs と検証順）・measurements/statements.md（2 ラウンドの節を追加）・unmeasured.md（未実測 7 項目）・CHANGELOG（1 項目）
- 受け入れで直したもの: docs/*.md の時間軸の言葉 2 か所（api.md の「and now athena-local too」、ddl.md の「now rejects」。design-checklist の文書の節）
- 実名の grep（DB・バケット・アカウント・S3 Tables の名前空間）0 件

## レビュー実施記録
- 計画レビュー: Explore 2 本（指摘 4 件。採用 2、6b で聞いた 2）
- 7-6・最終パス: 小規模経路のため無し
- 独立レビュー: 1 本（PR #209 作成後、出荷を止めない）。medium 相当 2 件、どちらも採用して修正コミット 1 本:
  1. 非 ASCII を含む引用符付きの名前を弾かないのが DESCRIBE だけで、ほかの文には推測の文言を当てていた（実測は DESCRIBE だけ）→ どの文でも弾かないように直し、テストに DROP TABLE・SHOW COLUMNS・ALTER の非 ASCII を足した（除外を外すとテストが落ちることを確認）。本番影響: 本物と違う文言を返す形が減る（妥当指摘 1 件）
  2. `CREATE TABLE IF NOT EXISTS "t"` は弾かない（未実測）のに一覧に無かった → unmeasured.md・caveats・テストのコメントに足した（docs 級）
  実機検証のケースに非 ASCII の名前は無いので再実行はしない

## dismissed

## deferred
| 指摘 | severity | 再トリアージ |
|---|---|---|

## 覆した事前判断
- 判定の置き場: Plan は構文チェックの「直前」→ 構文チェックの「直後」。本物は Trino が構文エラーにする形（`ALTER TABLE "t" ADD COLUMNS` など）で Trino の文言を返したため（実測）。外部契約: 返す文言が変わる形の優先順位。ユーザーには 6b で提示
- 対象の範囲: issue 本文の 5 種（DESCRIBE・DESC・SHOW CREATE TABLE・ALTER ADD COLUMNS・DROP TABLE）→ DESCRIBE・DESC・SHOW CREATE TABLE・SHOW COLUMNS・DROP TABLE・ALTER TABLE（Trino が受ける操作）。ALTER ADD COLUMNS は Trino が既に同じ文言で弾くので対象外、SHOW COLUMNS と ALTER の他の操作は実測で弾かれると分かったので足した

## 実機検証
- 足場: `.claude/issue-notes/204-verify/e2e.sh`（447 行）。`COMPOSE_PROJECT_NAME=athena204 tools/dev.sh env EXPECT=after .claude/issue-notes/204-verify/e2e.sh`（終了コード = FAIL 数）。q1〜q12（弾く形）と c1〜c9（弾かない対照）
- 受け入れ判定（着手前のコード b7b2ed9、2 回流して一致）: PASS 10（q12・c1〜c9）／FAIL 11（q1〜q11 は 200 SUCCEEDED）。q8（RENAME）は着手前だと本当に改名するので、直後に Trino へ直接 `DROP TABLE IF EXISTS u`・`CREATE TABLE IF NOT EXISTS t` で戻す。q6・q7 は expecting の前方一致だけ
- 変更後（7a73d7e、release、memory カタログ、17:47 完了）: EXPECT=after で PASS 21／FAIL 0（終了コード 0）。q1〜q11 は本物の文言で 400、q12 は Trino の文言、c1〜c9 は SUCCEEDED

## 範囲外の発見
| 内容 | 出どころ | 起票 |
|---|---|---|
| 未実測の引用符付きの名前の形（3 部の ALTER の途中・4 部以上・SHOW TABLES IN と CREATE TABLE の 2 部以上・存在しない名前の DESCRIBE・S3 Tables の対照 SELECT が通る状態） | 実測 2 ラウンド・D2 | #207 |
| 本物は開始時に弾くが athena-local は実行する無引用の DDL（ALTER TABLE IF EXISTS・ADD COLUMN 単数・場所の無い CREATE TABLE） | 実測 u2-ifu・alt-addcol-u・u7-createtable-u | #208 |
| 本物は受けるが athena-local は構文エラーにする形（バッククォート・MSCK REPAIR TABLE・DROP DATABASE） | 実測 d-n10・msck-u・u7-dropdb-u | 起票しない: SQL を書き換えないと揃えられない（CLAUDE.md の約束）。caveats に記載 |

## 委譲の記録
| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| Step 3 調査 | 委譲（Explore 3 本、sonnet） | |
| 手元の Trino 確認 | 自分 | コマンド 1 本 |
| 実測スクリプト作成 | 委譲（general-purpose、sonnet） | 受け入れ判定は自分 |
| 計画の下書き | 委譲（Plan、sonnet） | |
| 計画の確定（実測の反映） | 自分 | 判断そのもの |
| 計画レビュー | 委譲（Explore 2 本、sonnet） | |
| 実機検証の足場 | 委譲（general-purpose、sonnet） | |
| P1 実装（7-1〜7-4） | 自分（小規模経路・本番約 150 行） | 委譲の固定費が実装より長い。規則の表を自分で持っている |

## 出荷
- PR #209（main-204 → main、Closes #204）。起票: #207（未実測の引用符付きの名前の形）、#208（本物は弾くが athena-local は実行する無引用の DDL 3 種）
- deferred 0 件なので #204 へのコメントは無し
- 出荷判定: 検証 green（430 passed）・7-5b の分岐の突き合わせ 0 抜け・実機 PASS 21／FAIL 0 で PR を出した。独立レビューの妥当指摘 2 件は 1226ca6 で直し、PR にコメント・本文の予算を更新

## 時系列
- 16:20 着手（main-204、34f5e68）。過去のノート（200）を git rm
- 16:21 前例と過去の生データを grep（どちらも 0 件）。Explore 3 本を起動（拒否の経路／引用符付きの名前の前提／実測の足場）。先行作業: 手元の Trino を起動
- 16:24 手元の Trino で 13 形を確認（ALTER ADD COLUMNS は Trino も同じ文言で弾く）
- 16:32 Explore 3 本の転記。S3 Tables の別名（引用符付きのカタログ名）とぶつかることが分かり、判定の規則は実測で決める
- 16:34 実測スクリプト（tools/measure/quoted-names.sh）の作成と Plan を並列に起動。ユーザーに S3 Tables の有無を確認 →「ある」（実測に S3TABLES_* を渡してもらう）
- 16:45 Plan の下書きを転記。実測スクリプトの受け入れで `--debug` の投げ直しを外すよう差し戻し（start.err に AthenaErrorCode が出るので不要、生ログに署名が残る）
- 16:48 実測スクリプトを受け入れ、コミット（b7b2ed9）して実測を依頼
- 16:54 ユーザーが実測を実行（run-20260925-075408）、Monitor で監視
- 16:56 実測完了（16:58 整理）。引用符付きの部分を含む名前は全部弾かれ、S3 Tables も弾かれる。文言の規則を整理
- 16:59 計画を確定（構文チェックの直後に置く＝Plan の「直前」を実測で覆した）
- 17:00 計画レビュー Explore 2 本（契約・呼び出し元と実測の突き合わせ／構造・削減）と実機検証の足場（general-purpose）を起動
- 17:09 構造・削減のレビューと足場が返った（足場は受け入れ）
- 17:11 契約のレビューが返り、4 件を裁いた（採用 2・6b で聞く 2）
- 17:12 計画確定のコミット（6386910）。17:18 6b で提示、ユーザーの判断: 未実測の形は先に追加の実測（ラウンド 2）、範囲外の差 3 つはこの PR の docs に書く。ラウンド 2 の項目をスクリプトに足すよう委譲
- 17:32 ラウンド 2 の項目を受け入れ（DROP・ALTER・DROP DATABASE は実在しない名前だけ、CREATE TABLE は成功時に直後に消す）、コミットして実測を依頼
- 17:35 ユーザーがラウンド 2 を実行、Monitor で監視。17:37 完了を確かめ規則を確定
- 17:39 6b 承認（この規則で進める）。P1 に着手（自分）
- 17:45 P1 green・mutation 13 件。コミット
- 17:45 実機検証（EXPECT=after）をバックグラウンドで起動、P2 の docs を general-purpose に委譲
- 17:47 実機検証 PASS 21／FAIL 0
- 17:59 P2 の docs を受け入れ（時間軸の言葉 2 か所を直した）
- 18:01 push、PR #209 を作成
- 18:07 独立レビューが 2 件を返し、両方を直した（1226ca6）
- 18:08 出荷の記録をコミット
