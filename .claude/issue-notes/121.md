# quick #121 保持期限によるメモリの頭打ちを数時間の実運用相当で確かめる（人間検証）
- 入力: issue #121（#111 の人間検証）。モード: standalone。ブランチ main-121、着手前 SHA 8f61806（過去ノートの削除コミット 0f6f24e の後から作業）
- ゲート: 本番コード 0 行（docs 3 ファイル）／契約・新規性なし／事実: 手元の足場 1 コマンドで再現（時間だけかかる）／判断: `DURATION=3600 ROWS=100` は issue（3600 以上）と足場の README の推奨どおり。判定の閾値は既定のまま → 通過
- 時系列: 着手 06:58 ／ 足場の起動 06:58 ／ 対照側の終了 07:59 前後 ／ 足場の終了 08:58 ／ コミット 08:59（待ちの間に #114 のクローズ、#190 の CLAUDE.md の整理と PR #191、#192〜#195 の起票を別に進めた）
- 検証: `tools/dev.sh DURATION=3600 ROWS=100 tools/e2e/retention/verify.sh` FAIL 0（(a)〜(e) すべて PASS）。保持 1 秒の RSS は 1 時間を通して 11.3〜11.7MiB、保持 3600 秒は 3,127MiB まで伸び続けた。保持 1 秒の側で 34,610 件中 1 件だけポーリングが 400 QUERY_EXECUTION_NOT_FOUND（保持 1 秒で想定どおり）／`cargo fmt --check`・`cargo clippy --all-targets --locked -- -D warnings`・`cargo test --locked` ok（398 passed）
- 自己レビュー: docs だけ。caveats の数値（11〜12MiB、3.1GiB、約 35,000 件、0.1MiB）は load-{high,low}.csv と summary.txt から写した
- 打ち切り・escalate: 提案なし。数時間（2 時間以上）は流していないことを記録に書いた
- 予算の実績: Bash 約 12 本、所要 約 5 分（足場の 2 時間の待ちを除く）、サブエージェント 0 本（docs だけなので止めない独立レビューは流さない）
- 出荷: main-121
