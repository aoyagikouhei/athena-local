# quick #63 EXPLAIN の結果ファイル <id>.txt に本物は列名の見出し行（Query Plan）を入れるが athena-local は入れない
- 入力: issue #63。モード: standalone。ブランチ `main-63`、着手前 SHA 2dbac3e
- ゲート: サイズ 16 行・2 ファイル（≤50・3）／呼び出し元 1（≤5。`results::to_text` の本番の呼び出しは `operation/result_output.rs` の 1 箇所）／契約: 結果ファイルの中身を本物に合わせる修正で、README・CHANGELOG を同じコミットで直す条件で通す／新規性: 新ファイル・新パターン無し／事実: 過去 2 ラウンドの生データ（`~/athena-txt-measurements/run-01-20260915`・`run-20260916-094100` の `explain.bytes`）を「列名行込みの全行を `\n` で連結・末尾改行なし」で再構成すると 393 バイトが一致／判断: #60 の分け方（StatementType が DML のときだけ見出し）をそのまま使い、新しい既定値は無し → 通過
- 時系列: 着手 20:32 ／ red 20:35 ／ green 20:35 ／ コミット 20:38
- 検証: `cargo fmt --check` ok／`cargo clippy --all-targets --locked -- -D warnings` ok／`cargo test --locked` ok（lib 174 passed、results 13 passed、全バイナリ 0 failed）／ミューテーション: `to_text` の `skip(if with_header { 0 } else { 1 })` を `skip(1)` に戻す → 結合テスト `explain_の結果は_txt_の先頭に列名行を入れて書く` とユニットテスト `to_text_は見出しありなら列名の行を先頭に入れる` の両方が落ちた／足した層: 無し
- 自己レビュー: 分岐 2 件（DML → 見出しあり: EXPLAIN の結合テストとユニットテスト、UTILITY／DDL → 見出し無し: 既存の `show_の結果を_txt_で書く` と DDL の 0 バイトのテスト）／エラーの出口: 新規なし／呼び出し元 1 箇所（`result_output.rs`。DROP TABLE × Iceberg の改行 1 つの腕は `to_text` を通らず不変）
- 打ち切り・escalate: 提案なし。範囲外の発見: EXPLAIN の `Query Plan` 列の Precision（本物 371、athena-local は無制限 varchar の 2147483647）を #68 に起票
- 予算の実績: Bash 約 14 本、所要 6 分、サブエージェント 1 本（止めない独立レビュー）
- 出荷: standalone、ブランチ `main-63`
