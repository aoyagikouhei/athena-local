# quick #169 EXPLAIN の GetQueryResults の UpdateCount を本物と同じく省く
- 入力: issue #169。モード: standalone。ブランチ main-169、着手前 SHA 2ad190a
- ゲート: サイズ 8 行・2 ファイル（≤50・3。content_type.rs に述語 `plain_text_statement` を切り出し、execution.rs の `update_count` がそれを使う）／呼び出し元 2（`text_content_type`・`update_count`。どちらも既存の呼び出し）／契約・新規性: 通過（本物に合わせる修正。docs・CHANGELOG 同コミット）／事実: 通過（新しい実測はせず、#92 の生データ e1〜e8 と対照 SHOW TABLES を読み直して 8 変種とも null を確認。#1・#17・#70 の EXPLAIN も null）／判断: 通過（issue 本文どおり。#160 で決めた「UpdateCount 無し ＝ application の群」の述語を EXPLAIN まで広げただけ）
- 時系列: 着手 19:54 ／ 生データ読み直し 19:55 ／ red 19:56 ／ green 19:56 ／ コミット 19:57
- 検証: `tools/dev.sh cargo fmt --check`・`cargo clippy --all-targets --locked -- -D warnings`・`cargo test --locked` ok（345 passed）／ミューテーション: (1) `update_count` の述語を `carries_execution_id` に戻す → select.rs の EXPLAIN 2 本が落ちた、(2) `plain_text_statement` から EXPLAIN を外す → select.rs の 2 本と content_type の単体が落ちた／実測: 0 ラウンド（読み直しで決めた。measurements/statements.md に記録）
- 自己レビュー: 分岐 0 件（述語の切り出しと呼び替え）／エラーの出口 該当なし／呼び出し元 2 箇所: `text_content_type` は挙動不変（content_type の単体 7 本 green）、`update_count` は EXPLAIN 3 変種の単体と結合 2 本
- 打ち切り・escalate: 提案なし
- 予算の実績: Bash 約 7 本、所要 約 233 分、サブエージェント 1 本（止めない独立レビュー）
- 出荷: ブランチ main-169（PR 番号は `gh pr list --head main-169`）
