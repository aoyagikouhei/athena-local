# #245 execution.rs の start_query_execution の開始時の判定を別モジュールに分ける

経路: quick-implement のゲート（変更サイズ・新規性・事実）で外れ、ユーザーの選択で move-refactor（2026-09-27 02:2x）。
着手前 SHA: 2762fc8。ブランチ: main-245。

## 事前に確認した事実
- execution.rs 281 行（#244 のフェーズ 0 で 501 → 分割済み。start_request.rs・background_execution.rs が先行例）
- 種類: 本文が変わるリファクタ（長い関数の内部分割。`return X` が `return Err(..)` になる）。純粋な移動ではない
- 基準値は下の「検証」の足場で着手前に取る

## 計画
- 新ファイル `src/operation/start_checks.rs`（名前は start_request.rs と揃える: 受付値の検証 → 開始時の判定）
- 移す範囲: execution.rs L67（`// S3 Tables のカタログは ...`）〜 L235（ブロックコメントの判定の `}` まで）。コメントも文言のまま移す
- 関数: `pub(super) async fn decide(app: &App, query: String, catalog: Option<&str>, database: Option<String>,
  result_location: Option<&ResultLocation>) -> Result<Decision, Box<Response>>`
- 戻り値: `pub(super) struct Decision { statement: String, database: Option<String>, immediate_failure: Option<ImmediateFailure>, reported: Option<Reported> }`
  （フィールドは pub(super)）
- 本文の変更（許容リスト）: `return invalid_request_with_code(..)` → `return Err(Box::new(invalid_request_with_code(..)))`、
  `return *response` → `return Err(response)`、`catalog.as_deref()` → `catalog`、`result_location.as_ref()` → `result_location`、末尾の `Ok(Decision { .. })`
- 呼び出し側: `let Decision { statement, database, immediate_failure, reported } = match decide(app, query, catalog.as_deref(), database, result_location.as_ref()).await { Ok(d) => d, Err(response) => return *response };`
- どの判定にどちらの文を渡すか（受け取った文 `query`／落とした文 `statement`）は 1 箇所も入れ替えない（decisions.md の #242 の項）
- `IDEMPOTENT_MISMATCH`・`submit_response` は残す。execution.rs の `use` は使わなくなるものを落とす
- mod.rs に `mod start_checks;`（可視性の付与は decide・Decision とそのフィールドだけ）
- 文書: architecture.md の 13 行目（モジュール一覧）と 14 行目（execution.rs の説明を start_checks.rs に振り分け）、decisions.md:116 の「(開始時の判定の分割は #245)」、entity_check.rs:3・24 の `execution.rs` → `start_checks.rs`、CLAUDE.md の operation/ の行は列挙が「など」なので触らない
- README・CHANGELOG は変えない（挙動を変えない）

## 検証の足場
`.claude/issue-notes/245-verify/check.sh`（`tools/dev.sh .claude/issue-notes/245-verify/check.sh [--no-test]`）。
1 テスト総数（着手前 569）／2 テストバイナリごとの内訳（`tests-before.txt`、28 バイナリ）／3 着手前の `///` 行が全部残る（包含。足した行は印字）／
4 「実測」の行の多重集合（着手前 447）／5 空白を潰した文字列の順序つき突き合わせ（5a 移した範囲を許容の書き換えだけで再現、5b 残った範囲が同一）／6 fmt・clippy。
許容: `return` → `Err(..)`、`catalog.as_deref()` → `catalog`、`result_location.as_ref()` → `result_location`、fmt が外した match の腕の `{ }` 1 箇所、
entity_check.rs の参照の `execution.rs` → `start_checks.rs`。
受け入れ: 着手前のツリーで全 ok、doc 1 行削除と文言 1 文字の変更で 3・4・5 が NG（02:23）。

## 計画攻撃（Explore sonnet 1 本、02:22〜02:31、約 9 分・ツール 16 回（予算 15））
- [high] 足場が順序を見ず、隣接行の入れ替え（`sent_database` と `drop_catalog`）を見逃す → **dismissed（反証）**: 計画の文面は「ソートした多重集合」だったが、
  実装した norm.py の 5a は空白を潰した文字列の順序つき比較。実際に 2 文を入れ替えて `NG 5 移した範囲が違う（1210 文字目）` を確かめた（02:32）
- [low] 項目 4（実測の行）は項目 5 に包摂 → **dismissed**: 項目 5 は execution.rs と start_checks.rs だけ、項目 4 は src 全体（文書の追随で entity_check.rs の「実測」の行を書き換えている）
- 観点 1〜3（Send 性・start_request.rs との一貫性・query/statement の取り違え）は「再発なし・不一致なし」と報告

## フェーズの記録
### フェーズ 1（切り出し。16f2221、02:27）
- `start_checks.rs`（222 行）に `decide`・`Decision`。execution.rs 281 → 118 行。可視性の付与は `decide`・`Decision` とフィールド 4 つ（`pub(super)`）
- 検証: 足場 ALL OK（テスト 569・28 バイナリの内訳一致、doc 行 1105 が全部残り 4 行追加、実測 447、5a・5b ok、fmt・clippy）
- ミューテーション: `Decision` の `reported` を `None` → S3 Tables の CTAS／CREATE TABLE の結合テスト 2 件が落ちた。
  `s3_tables_rejection` の判定を `if false &&` で殺す → `s3_tables_の_context_では_hive_の_location_と_external_を構文チェックの前に弾く` が落ちた
- 実機: `tools/e2e/block-comment/verify.sh` 45/45 PASS、`tools/e2e/ctas-catalog/verify.sh` 8/8 PASS（02:28〜02:31）

### フェーズ 2（文書の追随）
- architecture.md 13 行目（モジュール一覧に start_checks.rs、S3 Tables の判定の呼び出し元 5 箇所）・14 行目（開始時の流れに decide の 1 文）、
  decisions.md 106・109・111・116、entity_check.rs:3・24 を `start_checks.rs` に
- 直さない（残留側・過去の記録）: store.rs:54（ワークグループの既定は execution.rs）、tools/measure/unmeasured-batch の 2 件のコメント
- README・CHANGELOG は挙動を変えないので触らない
