# issue #1 DDL と SHOW の結果ファイル `<id>.txt` を書く

## 基本情報

- issue: #1
- ベースブランチ: main
- 作業ブランチ: main-1
- 着手前 SHA: e09c30d
- 着手日: 2026-09-16

## 設計判断

（`humanDecisions` の正。Step 4 と Step 6b の結果をここに積む）

- 2026-09-16 ユーザーの判断: **実測してから実装する**。この環境から本物の Athena は叩けないので、ユーザーが実測し、その結果を渡すまで実装に入らない。推測で中身を埋めない（CLAUDE.md 59 行目の約束どおり）。
- 実測に使うスクリプトを用意した。文ごとに GetQueryExecution、GetQueryResults、`.txt` のバイト列、`.metadata` の有無を保存する。
- 実測待ちのため、調査（Step 3）まで進めて停止している。計画（Step 5）と計画レビュー（Step 6）は実測結果を見てから作る。中身の形が決まらないとテストで固定する値が決まらないため。

## 調査結果の要点（Step 3）

ファイル名の決定は実装済みで、足りないのは中身を書く処理だけ。

- `src/results.rs` の `ResultFile::of` は、CTAS でない `CREATE`、`DROP`、`SHOW` などを `Text` に落とす。`path` は `<id>.txt` を返す。つまりパスの生成はできている。
- `src/operation.rs` の `write_result` にある `location.file != ResultFile::Csv` の分岐が、`.txt` を書かせない唯一のガード。実装はここを広げることになる。
- `ResultWriter::put` は `Content-Type: text/csv` を固定で送る。`.txt` に何を送るかは実測して決める。署名付き URL を作って素の PUT を投げる形。
- `Outcome` の中身は文の種類で変わる。`SHOW` は `SELECT` と同じく列と行が入る。DDL は Trino が `updateType` を返すので行は積まれず、列も行も空になりやすい。つまり DDL の `.txt` に何を書くかは Trino の応答からは決まらない。
- 既存テスト `tests/results.rs` の `dml_と_ddl_は何も書かず_ファイル名だけ本物に合わせて返す` が、今の「DDL では PUT が起きない」を固定している。実装時はこのテストの DDL の部分を書き換える。
- CSV のバイト列は `src/results.rs` のユニットテストと `tests/results.rs` の両方で、実測した文字列リテラルと突き合わせて固定している。`.txt` も同じ形で固定する。
- `src/config.rs` の `ResultsMode` は `none` と `s3` だけ。`.txt` を書くのは `s3` のときに限る。
- 行数は `src/results.rs` が 372 行、`src/operation.rs` が 556 行。CLAUDE.md の「実測していない振る舞いは推測で埋めない」と、README と CHANGELOG の更新の約束が今回に効く。
- 中身の組み立ては `convert::all_rows` が担う。列名を先頭行にし、値は文字列にする。列が空か更新件数があるときは行を返さない。`to_csv` はそれをカンマ区切りで囲むだけ。`.txt` 用の組み立ては、この行の並びから作れる。
- `SHOW` と `DESCRIBE` と `EXPLAIN` を偽 Trino に投げるテストは無い。実測結果に合わせて、偽 Trino の応答も新しく作ることになる。
- **判定の食い違いが 1 つある。** `EXPLAIN` は `statement_type` では DML だが、`ResultFile::of` では `Text`、つまり `<id>.txt` になる。`VACUUM` は `Csv` 側に入っているのに `EXPLAIN` は入っていない。意図なのか抜けなのかはコードから読み取れず、テストも無い。実測項目に追加する。

## 実測の結果（2026-09-16、1 回目）

実データの名前は書かない。形だけを残す。生の出力はリポジトリの外（`$HOME/athena-txt-measurements`）に置いてある。

分かったこと。

- **`SHOW` が成功したとき**: `.txt` には結果の行だけが入る。行は `\n` で区切られ、**末尾に改行は付かない**。列名の行は入らない。GetQueryResults が返す行と一致した（1 列の例で確認）。
- **`EXPLAIN`**: `.txt` には計画の本文が入る。`OutputLocation` の拡張子は `.txt` で、`.csv` ではなかった。GetQueryResults の列名は `Query Plan` の 1 列で、`UpdateCount` は返らない。本文の末尾は改行 2 つだった。これは本文そのものの形。
- **件数の無い DDL**（`CREATE DATABASE`、`DROP DATABASE`）: **0 バイトのファイルが置かれる**。GetQueryResults は列も行も返さず、`UpdateCount` も返らない。
- **失敗したクエリ**: `SUCCEEDED` でなくても `.txt` は置かれ、中身は `FAILED: ` に続けて `StateChangeReason` と同じ文言が入る。末尾に改行は付かない。
- **`.txt.metadata`**: 成功した `SHOW` と `EXPLAIN` には置かれた。0 バイトの DDL と、失敗したクエリには置かれなかった。中身は未解析。issue #1 の範囲外。

まだ測れていないこと。1 回目は指定されたデータベースが実在せず、複数列になる文がすべて失敗した。

- 複数列のときの**区切り文字**。PyAthena はタブ区切りとして読むが、未確認。
- NULL の書き方と、値に区切り文字や改行が入るときのエスケープ。
- `SHOW COLUMNS`、`DESCRIBE`、`SHOW CREATE TABLE`、`SHOW PARTITIONS`、`SHOW TBLPROPERTIES`。
- `CREATE TABLE`、`ALTER TABLE`、`DROP TABLE`。
- `.txt` を書くのは成功時だけか、athena-local でも失敗時に書くべきか。これは範囲の判断。

## 実測でつまずいた点（2026-09-16）

同じ失敗を繰り返さないための記録。

- **`aws` が Docker のラッパだった。** マウントされるのは実行時のカレントディレクトリだけ。`aws s3 cp <src> <絶対パス>` はコンテナの中に書いて消え、しかも終了コードは 0 になる。1 回目だけ取得できたのは、出力先が当時のカレントディレクトリの下だったから。対処として、取得は `aws s3 cp <src> -` で標準出力に流し、リダイレクトはシェルが行う。`aws athena ...` の JSON が毎回取れていたのは、同じくシェルがリダイレクトしていたため。
- **データベース名が実在しなかった。** すべての `SHOW` が Entity Not Found で落ちた。対処として、最初に `SHOW TABLES` で疎通を確かめ、失敗したら `SHOW DATABASES` の一覧をファイルに残す。
- **テーブル名が実在しなかった。** 対処として、`SHOW TABLES` の結果からテーブル名を取り、指定が無いか一覧に無ければ 1 件目を使う。
- 名前は端末に出さず、ファイルに置くだけにしている。

3 回目で再現できたこと。`SHOW TABLES`、`SHOW DATABASES`、`EXPLAIN` は `.txt` と `.txt.metadata` の両方が置かれ、`CREATE DATABASE` と `DROP DATABASE` は 0 バイトの `.txt` だけで `.metadata` は無い。中身は未取得。

## 実測の手順

- スクリプト: `.claude/issue-notes/1-measure-txt.sh`
- 文ごとに、GetQueryExecution の応答、GetQueryResults の応答、`.txt` のバイト列、`.metadata` の有無を保存する。
- 対象: `SHOW TABLES`、`SHOW DATABASES`、`SHOW COLUMNS`、`DESCRIBE`、`SHOW CREATE TABLE`、`SHOW PARTITIONS`、`SHOW TBLPROPERTIES`、`EXPLAIN`、`CREATE DATABASE`、`DROP DATABASE`、失敗するクエリ。
- `CREATE TABLE`、`ALTER TABLE`、`DROP TABLE` は、テーブルの形式に合わせて呼び出しを足す。

## dismissed

（範囲外・判断済み・反証済みの指摘を理由付きで）

## 一致台帳

（同じ判断を複数箇所で持つものと、その守り方）

## 最新の nextArgs

（review-fix-loop を使った場合のみ）

## レビュー実施記録

| 工程 | 回数 | 本番影響のある妥当指摘の件数 |
| --- | --- | --- |

## deferred

| 指摘 | severity | file | 再トリアージ |
| --- | --- | --- | --- |

## 覆した事前判断

（元の判断 / 新しい判断 / 理由 / 外部契約・プロダクト挙動・運用への影響 / ユーザーに聞いたか）

## 委譲の記録

| 工程 | 委譲先 | 自分でやった場合の理由 |
| --- | --- | --- |
| Step 3 調査 | Explore 3 本 | |
| issue ノートの作成 | 自分 | 1 ファイルの雛形で、指示を書く方が長い |

## 時系列

- 2026-09-16 `main-1` を作成し、着手。着手前 SHA は e09c30d。
