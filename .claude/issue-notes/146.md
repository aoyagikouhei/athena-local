# issue #146: unmeasured-batch の SQL / API の 19 項目を本物の Athena で段階的に測り、docs/dev に転記する

## 基本情報

- ベース: main（de67e1b、#148 のマージ）。ブランチ: main-146。着手前 SHA（レビューの base）: 25320c4（過去ノートの削除だけ）
- 親: #114。前: #113（バッチ本体。PR #148）。兄弟: #147（生 HTTP と保持期限）
- 経路: 小規模 issue（フェーズ 2 以下）。フェーズ 1 = run.sh の raw_preflight の切り離し（コード）、実測（ユーザー、Step 3b）、フェーズ 2 = 転記（docs）

## 事前に確認した事実

Explore 1 本（opus、10 項目）から転記。落とした番号: 8（measurements/README.md の「書き方」は README.md:23-42 をそのまま読む）、10（行数。分割の判断に使わない）。

1. `raw_preflight` の呼び出しは run.sh:139-140（local）と :154-155（real）の 2 か所。real では OUTPUT/DB/WORKGROUP2 の必須チェック → RAW_* の設定（:151-152）→ raw_preflight → GetWorkGroup(WORKGROUP2) → SELECT 1 → `trap cleanup_real`（:192）の順。raw_preflight は失敗すると `exit 1`（lib-raw.sh:45-49）で、何も作っていない段階なので trap 未設定でも問題ない
2. `want_item`（run.sh:70-77）は TARGET 分岐より前にあり、`RETENTION_SELECTED`（run.sh:78-79 `if want_item t6 || want_item t7`）が同じ形の手本。ONLY が空なら全項目対象
3. `run_raw_item` を呼ぶのは item_t2/t3/t4（items-token.sh:107-109）と item_e2（items-workgroup-errors.sh:111）だけ。他の 19 項目の関数に `raw`／`RAW_` は出ない。t4c は aws CLI（items-token.sh:84-90）。`run_raw_item` は raw_preflight を通したかに依存しない（lib-raw.sh:10-31）
4. items-retention.sh は raw.py にも preflight にも依存しない（grep 0 件）
5. report.py は preflight を知らない。real では raw_preflight は summary.tsv に行を書かない（echo だけ）ので、省いても summary.txt は変わらない。local では lib-raw.sh:55 の `declare_expectation preflight raw_endpoint ...` が item_id=preflight の expect 行を 1 行書き、report.py:146-170 が `### preflight` の節にする。この行は `reset_item_rows` の対象外なので、local で RUN_DIR を再利用すると重複する（未検証）
6. `local_harden_credentials`（lib-local.sh:164-173）が資格情報をダミーにする。local の raw_preflight は #113 の受け入れ判定 2（送り先の突き合わせ）の証拠なので、local では常に通してよい
7. 転記先の対応表（19 項目）: (a) unmeasured.md の行 / (b) caveats.md / (c) src のコメント / (d) 転記先
   - r1: :9 / 無し / result_output.rs:107-110（コメント無し） / result-files.md（:252「失敗・取り消しのときの結果ファイル」の隣）
   - r2: :10 / 無し / results.rs:203-207 / result-files.md（:7「`<id>.txt` の中身」）
   - r3: :11 / 無し / result_output.rs:111「未測定で…置かない（#80）」 / result-files.md（:252）＋ statements.md:113 の SHOW FUNCTIONS 節
   - r4: :12 / docs/result-files.md:102「was not measured」 / content_type.rs:40-41「未測定…SHOW CREATE TABLE に揃えている」 / result-files.md（:323 Content-Type、:361）
   - x1: 無し / caveats.md:53-55「is not measured」 / 無し / statements.md（:66 キーワード間のコメント、:79）
   - x2: 無し / caveats.md:154-156「was not measured」 / render.rs:195（テスト） / 置き場が無い（値の表記の measurements ファイルが無い → result-files.md か新設は Step 6 で決める）
   - m1: :16 / caveats.md:212-214 / metadata.rs:65,67「未実測」、テスト :268-269 / metadata.md（:54 型ごとの表、:64）
   - m2: :17 / caveats.md:210-212 / metadata.rs:28「未実測」、:301 / metadata.md（:36 UPDATE/DELETE/MERGE の節）
   - m3: :19 / 無し / metadata.rs:26-28 / metadata.md（:24 top-level）
   - m5: :20 / roadmap.md:74 / metadata.rs:9-11「未観測なので写像は作らない」 / metadata.md（:22 備考、:71）
   - s1: :24 / caveats.md:86-91 / classification.rs:116-117「単数形は測っていないので分類しない」 / result-files.md:206「ALTER TABLE の結果ファイル」（statements.md:120 はそこへ誘導するだけ）
   - s2: :25 / caveats.md:113-116（「Iceberg 側は存在しえない」と既に書いてある）、:71 の表 / table_format.rs:170-179 / result-files.md:230「ALTER TABLE の亜種 × テーブル形式」
   - p1: :54 / 無し / classification.rs:5、テスト :175-177、content_type.rs:60-62 / statements.md:106（既存節に追記）
   - t1: :29 / 無し / store.rs:89「Catalog は未実測」 / client-request-token.md（:7 冪等性、:26 の比較表）
   - t4c・t5: :32-33 / 無し / execution.rs:59-79（検証順。コメント無し） / client-request-token.md
   - t8: :36 / caveats.md:293-297「has not been measured」 / store.rs:90 / client-request-token.md
   - w1: :40 / 無し / work_group.rs:69-71 / work-groups.md（:7 GetWorkGroup、:30「OutputLocation は無い」）
   - e1: :45 / docs/api.md:75「(`RUNNING` measured; `QUEUED` while still queued)」 / query_execution.rs:149-152 / query-results.md（:18 検証の節。errors.md に該当節は無い）
   - unmeasured.md:3 の規則: 測ったら「済み」の節（:60〜）へ移し、caveats の「not measured」も直す。「測れないもの」は :98〜
8. コメントの不整合: items-token.sh:4,106・items-workgroup-errors.sh:110・raw.py:10 が「lib-aws.sh の run_raw_item」と書いているが定義は lib-raw.sh:10（#113 の 7-7 で移した名残）

## 実測の手順

## 実測の結果

2026-09-24、run-20260924-004554（2 回目 17 項目 + 3 回目 r2・m5）。転記はフェーズ 2（委譲）。「差」は athena-local との違い（起票待ち）。

| 項目 | 本物の答え | athena-local |
|---|---|---|
| r1 | Hive の `CREATE EXTERNAL TABLE` 重複は FAILED・理由を `<id>.txt`（166B、application）・`.metadata` 無し。Iceberg の CTAS 重複は FAILED・何も置かない | 同じ（Hive 綴りは Trino に無い既知の差） |
| r2 | `.txt` はエスケープ無し（タブ・改行そのまま）。`GetQueryResults` は値の改行で行が分かれる。コメント無しの列は DESCRIBE で空白 20 個。列コメントの改行は Glue が拒否 | `.txt` の書き方は同じ。空白の詰め物は既知の差 |
| r3 | 失敗した `SHOW FUNCTIONS` は `<id>.csv` も `.metadata` も置かない | 同じ |
| r4 | `SHOW CREATE VIEW` は本体・`.metadata` とも binary、`.metadata` は不透明 312B。対照の Iceberg の `SHOW CREATE TABLE` も binary・不透明 332B で #1・#70 の記録と食い違う | **差**（application・素の protobuf） |
| x1 | `DESCRIBE /* c */`・`MSCK REPAIR /* c */ TABLE` は FAILED（ParseException、1/1003）。SHOW 4 文・`CREATE EXTERNAL /* c */ TABLE` は成功 | 既知の差の範囲（caveat を実測で更新） |
| x2 | 複合型の中の varbinary は `[B@<16 進>`（Java の `byte[]` の toString） | **差**（`01 02`） |
| m1 | tstz・ttz は 7/8/10 あり、iv_ym は 10 だけ、uuid・ipaddress は 3 つとも無し | 同じ |
| m2 | 0 件の UPDATE / DELETE / MERGE も field 3 `18 00` | 同じ |
| m3 | 0 行の CTAS も field 3 `18 00` | 同じ |
| m5 | NOT NULL 列の Iceberg テーブルは Hive 風・`WITH` 風とも MALFORMED_QUERY で作れない → Nullable 1/2 は「測れないもの」。NULL 可の列は 9 = 3 | 同じ（常に 3） |
| s1 | `REPLACE COLUMN`（単数形）は StartQueryExecution で `mismatched input 'REPLACE'`（MALFORMED_QUERY） | 同じ（分類しない） |
| s2 | `DROP PARTITION` × Iceberg は FAILED（`Query type not supported by Athena Iceberg at this time`、1200）、SubstatementType あり、何も置かず `RESULT_NOT_FOUND` | 既知の差（綴りが Trino に無い）。caveat の「Iceberg に構文が無い」を実測で直した |
| p1 | `( SELECT 1 )`・改行入りとも DML / SELECT、application | 同じ |
| t1 | Catalog の違い（大文字小文字だけでも、実在しない名前でも）は `IDEMPOTENT_PARAMETER_MISMATCH` | **差**（Catalog を比べず同じ ID） |
| t4c | 不正な OutputLocation と構文エラーが同時なら OutputLocation のエラーが先 | 同じ |
| t5 | 同じトークンの再送でも OutputLocation の不正・構文エラーの検証が照合より先 | 同じ |
| t8 | Database の省略と `default` の明示は衝突。OutputLocation の省略と明示は同じ ID（出力先を強制する WG でだけ測った） | **差**（OutputLocation も衝突。強制しない WG は未実測で残した） |
| w1 | 出力先ありの WG の `ResultConfiguration` は `{"OutputLocation": "s3://.../"}` だけ | 同じ形 |
| e1 | 5 本とも直後に SUCCEEDED（キュー待ち 47〜86ms）。QUEUED は捉えられず | 未実測のまま残した |

## 実測の経緯

- 2026-09-24 09:40 1 回目（run-20260924-004011）: preflight の `SELECT 1` が `No output location provided` で止まり、本物の項目は 0 本。`run_stmt` が 8 番目の引数を渡したときしか `OutputLocation` を付けておらず、19 項目のほぼ全部と `best_effort_drop`・e1 の直接呼び出しが付けていなかった（本物の primary には既定の出力先が無い。local は athena-local が既定で補うので気づけなかった）。WORKGROUP2 の preflight は通り、w1 の応答（`ResultConfiguration.OutputLocation` あり、`EnforceWorkGroupConfiguration=true`）は `.preflight/preflight-workgroup2.json` に取れている
- 2026-09-24 09:46〜09:55 2 回目（run-20260924-004554、19 項目）: 完走。StartQueryExecution 111 回（後始末の DROP 17 本を含む）、後始末 17/17 SUCCEEDED、created.tsv は空。スクリプト起因で測れなかったのは 2 つ: r2（列コメントの改行を Glue が `ValidationException` で拒否。制約 `[\u0020-\uD7FF\uE000-\uFFFD\uD800\uDC00-\uDBFF\uDFFF\t]*` に改行が無い）と m5（Hive 風 DDL の `NOT NULL` は Trino のパーサに落ちて `WITH` を求められ START_FAILED）。e1 は 5 本とも直後の状態が SUCCEEDED で QUEUED を捉えられず（記録する）。それ以外は本物の答えが取れた
- 3 回目（同じ run に `ONLY=r2,m5` で追記）: 完走、後始末 20/20。r2 は CREATE・DESCRIBE・SHOW TBLPROPERTIES とも SUCCEEDED（列コメントにタブ、TBLPROPERTIES にタブ＋改行）。m5 の NOT NULL は Hive 風（`mismatched input 'LOCATION'. Expecting: 'COMMENT', 'WITH'`）も `WITH (...)` 風（`no viable alternative at input '... (n int NOT'`）も START_FAILED → 本物では NOT NULL 列の Iceberg テーブルを DDL で作れず、Nullable の 1/2 は「測れないもの」へ

## 設計判断

- 本物での実行は 19 項目を 1 回で流す（項目は独立で、失敗しても次へ進む run.sh:236）。落ちた項目だけ `RUN_DIR` + `ONLY` で再実行。「群ごとに 4 回」は issue 本文の例だが、群に分ける利点は raw_preflight の道連れ回避だけで、それはフェーズ 1 で消える（統括役。#146 本文の「例」を狭めた）
- local では raw_preflight を常に通す（受け入れ判定 2 の証拠。事実 6）。real では ONLY に t2/t3/t4/e2 のどれかがあるときだけ通す

## 計画

小規模経路（フェーズ 2）。Plan は省略し統括役が書いた。

### フェーズ 1: run.sh の raw_preflight を生 HTTP の項目に閉じる（コード。本番コード約 15 行）
- 変更ファイル: `tools/measure/unmeasured-batch/run.sh`（258 → 約 270 行）。責務は変えない
  - `RAW_SELECTED`: `RETENTION_SELECTED`（run.sh:78-79）と同じ形で `want_item t2 || t3 || t4 || e2`
  - real 分岐（:154-155）: `[ "$RAW_SELECTED" = 1 ]` のときだけ raw_preflight。省いたときは「== preflight: 生 HTTP の項目が無いので raw.py の preflight は通さない」と 1 行出す
  - local 分岐（:139-140）: 変えない（`reset_item_rows preflight` は計画レビュー 4 で削った。重複しても match=yes の行が 2 行になるだけで判定は誤らない）
  - ヘッダコメント: #146（A の 19 項目の ONLY の値）と #147（生 HTTP・保持期限）の流し方を書く
- 同梱: 事実 8 のコメント 4 か所を「lib-raw.sh の run_raw_item」に直す。items-statements.sh:22 の s1 の対照（複数形）を `(n int, s varchar)` → `(n int, s string)` に直す（計画レビュー 1。#43 で本物が成功した a1 と同じ形にし、対照が落ちて単数形の切り分けができなくなるのを防ぐ）
- 依拠する前提表: `want_item` は ONLY だけで決まり TARGET 分岐の前にある（run.sh:68-77 が保証）／`run_raw_item` は preflight に依存しない（lib-raw.sh:10-31）／report.py は preflight 行の有無で落ちない（事実 5。real では元々行が無い）
- テスト（この領域にテストランナーは無い。代替は実行で確かめる）:
  - local: `ONLY=r3,s1` のドライランで `### preflight` の expect 行が出て match=yes（local は常に通す）、s1 が local の期待どおり
  - real の分岐（本物には投げない）: ダミーの資格情報で `TARGET=real ONLY=r3` → raw.py の preflight を通さず、GetWorkGroup の preflight で認証エラーになって止まる。`ONLY=t2` → raw.py の preflight を通して認証エラーで止まる（出力に「raw.py preflight」の行が出る）
- ミューテーション: 無し（計画レビュー 3 で削った。判定を壊しても `run_raw_item` は preflight に依存せず測定結果は変わらない。#147 の 1 回目の出力で preflight が通ることは必ず見える）

### 実測（Step 3b、ユーザー）
- 1 回目: `ONLY=r1,r2,r3,r4,x1,x2,m1,m2,m3,m5,s1,s2,p1,t1,t4c,t5,t8,w1,e1`（約 10 分。StartQueryExecution 約 75 本、CREATE 約 19・DROP 同数）
- 落ちた項目: スクリプトを直して `RUN_DIR=<同じ run> ONLY=<id,...>` で再実行（予算: 3 ラウンドまで）

### フェーズ 2: 転記（docs。委譲: general-purpose opus）
- 生データ（summary.txt と各項目の execution.json・results・.metadata のバイト列）を読み、事実 7 の対応表どおりに `docs/dev/measurements/*.md` へ README.md:23-42 の形式で追記（上書きしない）。x2 の置き場は result-files.md の Content-Type の近くではなく、`docs/dev/measurements/statements.md`… → Step 6 で決める（候補: result-files.md に「値の表記」の `##` を新設）
- `docs/dev/unmeasured.md`: 測れた項目を「済み」へ、測れなかった項目は理由を添えて残す
- `docs/caveats.md`／`docs/result-files.md:102`／`docs/api.md:75` の「not measured」: 本物が athena-local と同じなら文言を「measured」に直し、違うなら差分を書いて起票（athena-local の挙動はこの issue で変えない）
- `src/` の「未実測」コメント（metadata.rs:9-11,28,65,67・classification.rs:116-117・content_type.rs:40-41・store.rs:89・result_output.rs:111）: 同じなら「2026-09-24 実測」に書き換え、違うなら起票番号を添える。コードは変えない
- CHANGELOG: docs のみの変更（caveats の文言）は Unreleased に 1 行（過去の同種 PR に倣う）

### 先行できる作業
- 無し（フェーズ 2 は実測の結果に全面依存。ユーザーの実行待ちの間に統括役が summary の読み方を用意するだけ）

### 人間検証リスト
- 本物での 1 回目の実行（WORKGROUP2 の出力先が設定済みであること）

## 計画レビューの結果

Explore 1 本（opus、契約・削減・実測の設計）。採用 3、棄却 0。
1. 採用（実測の設計、high）: s1 の対照 `REPLACE COLUMNS (n int, s varchar)` は Athena の Hive DDL で長さ無しの `varchar` が通らない見込み（未確認）。対照が落ちると単数形の失敗をフィクスチャの問題と切り分けられない → #43 の a1 と同じ `(n int, s string)` に直してフェーズ 1 に同梱。design-checklist に 1 行
2. 記録のみ: raw_preflight を外しても壊れる呼び出し元は無い（`.preflight` は get_work_group/run_stmt が mkdir -p する）
3. 採用（削減）: `RAW_SELECTED` のミューテーション確認を削る
4. 採用（削減）: local の `reset_item_rows preflight` と 2 回目の local 実行を削る
5. 記録のみ: `declare_expectation` はすべて `TARGET=local` の中（19 か所を列挙）。probe_prefix_exists は本物の `.txt`（改行区切り・見出し無し）で正しく動く。WORKGROUP2 は run.sh:163-170 で機械確認
6. 記録のみ: 後始末の VIEW/TABLE の対応は揃っている
7. 記録のみ（フェーズ 2 の委譲に添える）: RUN_DIR 再実行で drops.tsv と項目ディレクトリの古いファイルが残るので、転記は summary.tsv の `id=` と照合して読む

## フェーズの記録

### フェーズ 1（2026-09-24、自分）
- 7-0: 変えるのは run.sh の real 分岐の raw_preflight の呼び出しだけ。依存する呼び出し元は無し（run_raw_item は preflight に依存しない。lib-raw.sh:10-31）。増える分岐は `RAW_SELECTED` の if の 2 腕（real のみ）
- 検証（テストランナー無し。実行で確認）: ダミー資格情報の `TARGET=real ONLY=r3` → 「生 HTTP の項目が無いので通さない」を出して GetWorkGroup の preflight で停止。`ONLY=t2` → raw.py の preflight を通り `400 UnrecognizedClientException` で停止（本物には認証で弾かれた 2 リクエストだけ）。local `ONLY=r3,s1` → preflight の raw_endpoint match=yes、s1 の local_ddl_fails match=yes
- 計画から外れた点: 無し
- 7-6: 省略（小規模経路）。7-7: 分割不要（run.sh 270 行、責務は変えていない）

### フェーズ 2（2026-09-24、general-purpose opus。転記）
- 19 項目を docs/dev/measurements 6 ファイルに README の形式で追記（+300 行）。unmeasured.md は 14 行を「済み」へ、1 行を「測れないもの」へ、e1・t8（強制しないワークグループ）・SHOW CREATE TABLE（テーブル形式で割れるか）を未実測として残す／新設。caveats.md・docs/result-files.md・roadmap.md の「not measured」を本物と同じ項目だけ直し、src のコメント 3 ファイル（metadata.rs・classification.rs・result_output.rs）を「2026-09-24 実測」に。CHANGELOG に 1 行
- 統括役の受け入れ: 意外な結論 3 つ（r2 のタブ後の `b` が消える、x2 の `[B@...`、r4 の Iceberg の SHOW CREATE TABLE が binary・不透明）を生データ（od -c、s3 の一覧、.metadata の od）で確認し一致。直したのは Glue の正規表現の文字化け 1 か所。`cargo fmt --check` 通過（src はコメントだけ）。実名の grep 0 件は委譲先の報告どおり
- 本物との差分は起票（下の「範囲外の発見」）。挙動は変えない
- 7-7: 分割不要。Step 9（実機検証）: 本 issue の成果物は docs と実測スクリプトで、実機に当たるのは本物での実測そのもの（3 回）

### フェーズ 1b（2026-09-24、自分。1 回目の実測で崩れた前提の修正）
- `build_stmt_args` の OutputLocation を "-" で `$OUTPUT` に、省略そのものを測る t8 の output-omit だけ "omit" に。`best_effort_drop`（local/real の 2 か所）と e1 の直接呼び出しにも `--result-configuration` を足す
- 検証: `build_stmt_args` を単体で 4 通り（既定・omit・上書き・OUTPUT 無し）。local `ONLY=t8,e1,r3,m2` で t8 の照合 2 件 match=yes、e1・r3・m2 が完走
- 実測で崩れた前提 = 最終レビュー後の無レビュー差分。7-5b: 増えた分岐は `output_override = "-"` の 1 つ。`start-query-execution` の直接呼び出し 3 か所すべてに result-configuration が付いていることを grep で確認

## レビュー実施記録

- 計画レビュー: Explore 1 本（採用 3、棄却 0）
- 独立レビュー（Step 8、フェーズ 1 の差分 25320c4...24ffe4d）: 1 回。本番影響のある妥当指摘 0 件。low の参考 1 件（`want_item` が `ONLY="t2, t3"` の空白に対応しない。変更前からの挙動）→ 対応しない（ヘッダの例は空白無しで示している）

## dismissed

## deferred

| 指摘 | severity | 再トリアージ |
|---|---|---|

## 覆した事前判断

## 実機検証

## 範囲外の発見

| 内容 | 出どころ | issue |
|---|---|---|
| 複合型の中の varbinary を本物は `[B@...`（Java の byte[] の toString）で返す。athena-local は 16 進 | 実測 x2 | #149 |
| 冪等性の照合に本物は Catalog を入れる（大文字小文字違いも衝突）。OutputLocation の省略と既定値の明示は本物では同じ ID（強制するワークグループでのみ実測） | 実測 t1・t8 | #150 |
| SHOW CREATE VIEW と Iceberg への SHOW CREATE TABLE は本物では binary/octet-stream・不透明な .metadata・UpdateCount 0。#1・#70 の SHOW CREATE TABLE（application・素の protobuf）と食い違う | 実測 r4・x1 | #151 |
| e1: QUEUED を捉えられなかった（キュー待ち 47〜86ms） | 実測 e1 | 起票しない。unmeasured.md に「別の手が要る」と残した |
| t4 の短いトークンの組・生 HTTP・保持期限 | 計画 | #147（既存） |

## 委譲の記録

| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| 調査 | Explore 1 本（opus） | 小規模経路。#113 の run.sh・lib-raw.sh を継承 |
| 計画 | 自分 | 小規模経路（Plan 省略） |
| 計画レビュー | Explore 1 本（opus） | 規則どおり |
| フェーズ 1 の実装 | 自分（小規模経路・本番 約 15 行） | 委譲の固定費の方が長い |
| フェーズ 1b（OutputLocation）・r2/m5 の直し | 自分 | 実測で崩れた前提の数行の修正 |
| 独立レビュー | general-purpose 1 本（opus） | 規則どおり |
| フェーズ 2 の転記 | general-purpose 1 本（opus） | 規則どおり |
| 差分の起票・PR 本文 | 自分 | 小規模経路 |

## 出荷

- PR #152（2026-09-24 10:17）。issue #146 に申し送りのコメントを投稿（起票 #149・#150・#151、未実測の残り、#147）。deferred 0 件
- 出荷判定: 検証（bash -n・fmt --check・local ドライラン）green、独立レビュー 0 件、実機 = 本物での実測 3 回。小規模経路のため PR を先に出した

## 時系列

- 2026-09-24 09:23 25320c4 過去のノートを消して着手。Explore 1 本を起動
- 2026-09-24 09:29 Explore が返った。事実と計画をノートに書き、計画レビュー（Explore 1 本）を起動
- 2026-09-24 09:34 302342a 計画レビューが返った（採用 3、棄却 0）。6b: 論点 0 件のため提示のみ
- 2026-09-24 09:36 24ffe4d フェーズ 1 を実装し、real（ダミー資格情報）と local で確認。独立レビュー（opus）を起動し、ユーザーに 1 回目の実測を依頼
- 2026-09-24 09:37 独立レビューが返った（本番影響 0 件）
- 2026-09-24 09:40 1 回目の実測が preflight で停止（OutputLocation 無し）。フェーズ 1b で直して再実行を依頼
- 2026-09-24 09:44 4dbf251 フェーズ 1b をコミット
- 2026-09-24 09:55 2 回目が完走。09:56 c18deb8 r2・m5 を直して 3 回目（2 項目）を依頼。フェーズ 2（転記）を 17 項目ぶん先行して委譲
- 2026-09-24 09:59 3 回目が完走（r2 取れた、m5 の NOT NULL は作れず）。委譲先に r2・m5 を追加
- 2026-09-24 10:15 フェーズ 2 が返った。生データで 3 点を確認、差分 3 件を起票（#149・#150・#151）
- 2026-09-24 10:16 e2fb837 フェーズ 2 をコミット
- 2026-09-24 10:17 PR #152 を作成、#146 に申し送りをコメント
