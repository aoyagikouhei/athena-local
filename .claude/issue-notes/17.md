# issue #17 先頭にコメントが付いた SQL の文の種類を正しく判定する

## 基本情報

- issue: #17（`gh issue view 17`）。ベースブランチ `main`、着手前 SHA `f31c26c`
- ブランチ: `main-17`
- 経緯: `quick-implement` で着手し、ゲートの「事実」（本物の Athena の実測が未了）で外れたため `implement-issue` へ引き継いだ。
  quick ノートのコミットは `e09d40f`（docs 予算の別枠）。issue にも同じ内容をコメント済み
  （<https://github.com/aoyagikouhei/athena-local/issues/17#issuecomment-5722534423>）
- 検証コマンド: `cargo fmt --check` ／ `cargo clippy --all-targets --locked -- -D warnings` ／ `cargo test`

## 事前に確認した事実

Explore 3 本の番号付き返答から転記。落とした番号と理由は末尾に書く。

### 判定ロジックの現状

- `src/operation.rs:638-644` `words()`: `split_whitespace()` した**全トークン**に `trim_start_matches('(')` を掛けて大文字化する。
  doc コメントに「先頭のコメントは考慮しない」と明記（638 行）。
- `words()` の呼び出し元は **3 箇所**（すべて `src/operation.rs` 内、private）:
  - `:353` `metadata_query_id` — `.metadata` の先頭に入れるクエリ ID を Trino の ID にするか `QueryExecutionId` にするかを決める
  - `:648` `statement_type`
  - `:660` `substatement_type`（`is_create_table_as(&words)` にもこの配列を渡す。`:685`）
- `src/results.rs:38-59` `ResultFile::of()`: 括弧を残した配列を作り、`word(0)` を使う瞬間にだけ `trim_start_matches('(')` を掛ける（`:47`）。
  唯一の呼び出し元は `src/operation.rs:219`（`result_location`）。
- **重要な非対称**: `is_create_table_as`（`src/results.rs:86-102`）には 2 種類の形の配列が渡る
  （`operation.rs:685` は括弧除去済み、`results.rs:55-56` は括弧付き）。`:96-101` の
  `starts_with("SELECT") || starts_with("WITH") || starts_with('(')` の 3 通り OR がこの差異を吸収している。**崩さないこと。**
  - 例 `CREATE TABLE t AS (SELECT 1)`: `operation.rs` 側 `[...,"AS","SELECT","1)"]`／`results.rs` 側 `[...,"AS","(SELECT","1)"]`
- レスポンスへの流れ: `statement_type`/`substatement_type` → `to_query_execution`（`:575-576`）→ `get_query_execution`（`:405-407`）→
  `GetQueryExecutionResponse.QueryExecution.StatementType`/`.SubstatementType`。`update_count`（`:629-636`）→ `get_query_results`（`:526`）→
  `GetQueryResultsResponse.UpdateCount`。`ResultFile::of` → `ResultLocation::new`（`results.rs:120-130`）→ `uri()`（`:152-155`）→
  `ResultConfiguration.OutputLocation`。加えて `write_result`（`operation.rs:276-277`）の「本体を書くか」の判断にも効く。
- **先頭のキーワードを見ている箇所は上記 6 関数に閉じている**（grep 済み）。`src/metadata.rs`・`src/convert.rs`・`src/statement.rs` には無い。

### 共有できる字句処理（`src/catalog.rs`）

- `pub` は `alias_qualified_names` だけ。`skip_quoted`(:55) / `comment_end`(:72) / `next_is_dot`(:89) / `unquote`(:103) / `replacement`(:113) は private。
- `comment_end`(:72-86) の挙動: `--` は `\n` の**直前**まで（`\n` 自体は含まない）。`/**/`→4、`/* */`→5、`/*`（未閉じ）→2、`/*/` 単独→3（未閉じ扱い）。
  `rest[2..]` から `*/` を探すので `/*` の `*` を閉じの一部として誤読しない（`catalog.rs:203-204` がテスト済み）。
  ネストしたブロックコメントは内側の最初の `*/` で閉じる（SQL 標準どおりで変える必要なし）。
- `next_is_dot`(:89-100) の「空白とコメントを交互に読み飛ばす」ループがそのまま転用できる。戻り値を `bool` → 位置に変えて切り出せる。
- `src/lib.rs:5` は `mod catalog;`（非 pub）のままでよく、`pub(crate) fn` にすれば `operation.rs`/`results.rs` から `crate::catalog::...` で呼べる。
- 先頭に文字列リテラルが来る有効な SQL 文は無いので、先頭コメント飛ばしで `skip_quoted` は要らない。

### 既存テストと docs

- `StatementType` の結合テスト: `tests/dml.rs:23`、`:65-66`、`tests/parameters.rs:79`、`:114` の 4 箇所だけ。
  **`SubstatementType` の結合テストは 1 つも無い**（ユニットテスト `src/operation.rs:795-852` のみ）。
- `OutputLocation` のファイル名: `tests/results.rs:63-66`/`124-127`/`204-220`（4 形をまとめて固定）/`264-267`/`417-423`、
  `tests/metadata.rs:107`/`149`/`330-337`/`432`、`tests/failed_results.rs:49`/`118-119`。
- 雛形: `tests/results.rs:50-81`（`s3_puts()` の典型）と `tests/dml.rs:44-92`（`StatementType` の典型）。
- `tests/common/mod.rs:32-33` の `SYNTAX_CHECK_PREFIX` を偽 Trino が `:477-489` で見て構文チェックを識別する。
- README: Caveats は `345-536`。**先頭コメントの扱いに触れた条項は無い。** 関連は `381-385`（Syntax differs）と
  `445-450`（Unmeasured file names。CTAS の Iceberg 判定が文字列リテラルとコメントの中の `table_type='ICEBERG'` も数える既知の粗さ）。
  Supported API は `166-171`（`StatementType`/`SubstatementType` の説明）と `329-331`（`StatementType` は元の SQL から導く）。
- CHANGELOGS: `## [Unreleased]`(:10) の直下が `### Added`(:12)。過去版は `## [0.4.0] - 2026-09-15`(:139) の形。
- `.claude/design-checklist.md` は #5 由来の 1 項目のみ（早期 return の下流に新しい出力を足すときの全経路確認）。

### 実測の状況（着手時の訂正。実測は完了済み）

着手時、`.claude/issue-notes/5.md:407` の記述は athena-local 相手の観測と推測であって本物の実測ではないと分かった
（「本物なら `.csv` になるはず」と書かれていた）。issue 本文の「#5 の実機検証で判明」も実測値ではなかった。
そのため実測から始めた（結果は「実測の結果」節）。統括役の環境に AWS 認証情報は無い。

落とした番号と理由: Explore 1 の (6) 既存ユニットテストの内容の逐条引用（テスト名と場所だけ残し、中身は実装時に読めばよい）、
Explore 3 の (1) `tests/` 各ファイルの doc コメント全文（ファイル名と主題だけ残した）、(8) 実測スクリプト 2 本の全文
（実測スクリプト作成の委譲先に直接読ませた）。

## 実測の手順

- スクリプト: `.claude/issue-notes/17-measure-leading-comment.sh`（33 項目）
- 実行: `OUTPUT=s3://... DB=... PROBE_DDL=1 bash .claude/issue-notes/17-measure-leading-comment.sh`
- 出力は `$HOME/athena-comment-measurements/run-<日時>/`（リポジトリの外）。貼るのは実名を伏せた `summary.txt`
- `PROBE_DDL=0` でも `CREATE/DROP DATABASE athena_local_probe_17` が走る。`=1` は Iceberg テーブル 2 件を作って必ず消す
- `metadata_query_id` 列は `.metadata` の先頭 field 1 の**形**（trino / uuid）だけを出す

## 実測の結果

2026-09-18 09:06 実測（`run-20260918-090622`、33 項目）。**分岐 A で確定: 本物は先頭のコメントを飛ばして文の種類を判定する。**

### 決め手（C 群）

| SQL | StatementType | Substatement | ファイル |
|---|---|---|---|
| `-- SELECT\nSHOW TABLES` | **UTILITY** | SHOW_TABLES | `.txt` |
| `/* SELECT */ SHOW TABLES` | **UTILITY** | SHOW_TABLES | `.txt` |
| `/* SHOW TABLES */ SELECT 1` | **DML** | SELECT | `.csv` |

コメントの中のキーワードは読まれない。行コメントもブロックコメントも同じ。

### 文ごと（A・B・F 群）

| SQL | StatementType | Substatement | ファイル | UpdateCount | `.metadata` の ID |
|---|---|---|---|---|---|
| `-- c\nSELECT 1` / `/* c */ SELECT 1` | DML | SELECT | `.csv` | 0 | uuid |
| `-- c\nSHOW TABLES` / `/* c */ SHOW TABLES` | UTILITY | SHOW_TABLES | `.txt` | 0 | （protobuf でない。下記） |
| `-- c\nDESCRIBE t` | UTILITY | DESCRIBE_TABLE | `.txt` | 省く | **uuid** |
| `-- c\nSHOW CREATE TABLE t` | UTILITY | SHOW_CREATE_TABLE | `.txt` | 省く | **uuid** |
| `-- c\nEXPLAIN SELECT 1` | DML | EXPLAIN | `.txt` | 省く | trino |
| `-- c\nCREATE DATABASE ...` / `/* c */ ...` | DDL | CREATE_DATABASE | `.txt` | **省く** | （`.metadata` 無し） |
| `-- c\nDROP DATABASE ...` / `/* c */ ...` | DDL | DROP_DATABASE | `.txt` | **省く** | （`.metadata` 無し） |
| `-- c\n<Iceberg CTAS>` / `/* c */ <CTAS>` | DDL | CREATE_TABLE_AS_SELECT | `<id>`（拡張子なし） | 1 | trino |
| `-- c\nINSERT INTO ...` | DML | INSERT | `<id>` | 1 | trino |
| `DROP TABLE ...`（コメント無し） | DDL | DROP_TABLE | `.txt` | 省く | trino |

**D6 の波及が裏付けられた**: コメント付き `DESCRIBE` と `SHOW CREATE TABLE` の `.metadata` の先頭は `uuid`（= `QueryExecutionId`）。
athena-local は現状 `word(0) == "--"` になるので `engine_id`（trino）を入れており、**本物とずれている**。読み飛ばしを入れると一致する。

### 空白・連続・境界（D 群）

`-- c\n\n   SELECT 1`／`-- a\n-- b\nSELECT 1`／`-- a\n/* b */ SELECT 1`／`--c\nSELECT 1`／`/* c */SELECT 1`／
`/* a\nb */ SELECT 1`／`   SELECT 1`／`SELECT 1 -- c` は**すべて DML / SELECT / `.csv`**。
空行・連続・混在・区切りの空白の有無・コメント内の改行のどれも飛ばす。末尾のコメントは判定に影響しない。

### 失敗系（E 群）

| SQL | 結果 |
|---|---|
| `-- only` | `InvalidRequestException` / `line 1:8: mismatched input '<EOF>'`（`-- only` は 7 文字。**コメントを飛ばした先**の位置） |
| `/* only */` | `InvalidRequestException` / `line 1:11: mismatched input '<EOF>'`（同上） |
| `/* c SELECT 1`（未閉じ） | `InvalidRequestException` / `line 1:1: mismatched input '/'`。**本物は未閉じをコメントとして扱わない** |
| `''`（空文字列） | CLI が送信前に拒否（`ParamValidation`。ワイヤに乗らない） |

athena-local はこれらを `Trino::syntax_error` の `PREPARE` で弾く（既存の経路）。**文言は Trino のものになるが、
構文エラーの文言が Trino のものになることは README:381-385 に既記**。未閉じの扱いの差（`comment_end` は末尾まで飛ばす）は、
どちらも構文エラーになって実行が作られないので**観測できる差にならない**。

### 実装に効かない発見（範囲外）

- `/* c */ SHOW CREATE TABLE t` は本物では **FAILED**（`ParseException line 1:0 cannot recognize input near '/' '*' 'c'`、
  ErrorCategory 1 / ErrorType 1003）。ただし `StatementType`／`SubstatementType`／`OutputLocation` は
  `UTILITY`／`SHOW_CREATE_TABLE`／`.txt` と**正しく返る**。分類はコメントを飛ばすが、`SHOW CREATE TABLE` の実行パーサ
  （Hive 系）がブロックコメントを受け付けない。行コメント版は成功する。athena-local は Trino に投げるので成否が違いうる
- `SHOW TABLES` の `.metadata`（312 バイト）は **protobuf ではない**（先頭が `41 57 71 43 2f`＝`AWqC/`…）。
  `DESCRIBE` は `0a 24` + UUID で正しく protobuf。コメントの有無で変わらないので本 issue とは無関係
- `SELECT` の `.csv` の Content-Type が `binary/octet-stream` だった（athena-local は `application/octet-stream`）。
  `#1` のノートにある既知の揺れ（実測のたびに binary と application に割れる）

## 実測の経緯

- 2026-09-18 08:30 ラウンド 0: 統括役の環境に認証情報が無いことを確認し、スクリプト作成を委譲した。
- 2026-09-18 09:06 ラウンド 1: ユーザーが `PROBE_DDL=1` で実行。33 項目すべて測れた（未測定 0、再試行 0）。
  分岐 A で確定。追加のラウンドは要らない。

## 設計判断

| # | 判断 | 理由 |
|---|---|---|
| D1 | **1 フェーズにする**（Plan の 2 フェーズ案を覆す） | Step 6b の承認前にフェーズへ入らない規則（4 原則 2）があるので、実測待ちの間にフェーズ 1 を進めることはできない。分ける実利が無い。加えて実測が「本物も飛ばさない」と出れば読み飛ばし関数自体が不要になり、未配線の新モジュールを先に入れると捨てることになる |
| D2 | **読み飛ばし関数は `src/catalog.rs` に足す**（Plan の `src/lexer.rs` 新設案を覆す） | `catalog.rs` は既に `skip_quoted`／`comment_end`／`next_is_dot` という SQL 字句走査の道具箱を持っている（`:55`/`:72`/`:89`）。`lexer.rs` を新設して `comment_end` を `catalog.rs` に残すと**字句処理が 2 ファイルに散る**。`comment_end` ごと移すのは挙動を変えない移動とはいえ差分が大きく、issue の規模（判定の前に前処理を 1 つ足す）に対して過剰。モジュール doc を 1 行広げて実態に追随させる。**割れる判断なので Step 6b でユーザーに確認する** |
| D3 | シグネチャは `&str -> &str`（読み飛ばし後のスライスを返す） | 呼び出し側は直後に `split_whitespace()` するので、位置より文字列のほうが 1 行の差し替えで済む。区切りは全部 ASCII なので UTF-8 境界は壊れない（`catalog.rs:20` のコメント） |
| D4 | `trim_start_matches('(')` の非対称は**変えない** | `is_create_table_as`（`results.rs:96-101`）の 3 通り OR がこの非対称を吸収している。崩すとどちらかの呼び出し元で CTAS 判定が壊れる |
| D5 | `ResultFile::of` の中で、**`words` は読み飛ばし後の文字列から作り、`is_iceberg_table` に渡す引数だけを元の `query` にする** | 計画レビュー（契約）の high。`is_create_table_as` は `words[1..]` の先頭が `"TABLE"` であることを要求する（`results.rs:88-94`）ので、元の `query` から `words` を作ると `-- c\nCREATE TABLE t AS SELECT 1` が `["--","C","CREATE","TABLE",...]` になり `words[1]=="C"` で `false` に落ち、**コメント付き CTAS だけが `.txt` のまま残る**。`is_iceberg_table` は全文検索（`results.rs:107-110`）でコメント内の `table_type='ICEBERG'` も数える既知の粗さ（README:445-450）なので、そちらは本 issue で変えない。変数名を `trimmed`（読み飛ばし後）と `query`（元）に分けて境界を明示する |
| D6 | `metadata_query_id`（`operation.rs:353`）も `words()` 経由で自動的に影響を受けるのを**そのまま受け入れる** | 特別扱いすると判定が 3 箇所に増え、issue の意図に反する。実測に `-- c\nDESCRIBE t` を入れて未実測のまま残さないようにする |
| D7 | `is_iceberg_table` の粗さの是正は**範囲外**（起票する） | 先頭コメントを飛ばしても直らない別の問題。README に既知として書いてある |
| D8 | **`next_is_dot`（`catalog.rs:89-100`）は書き直さない**（計画レビューの削減指摘を採用） | `next_is_dot` は `alias_qualified_names`（カタログ別名）専用で本 issue と無関係。Trino 実機で検証済みの機能（`catalog.rs` の 12 テスト）に、戻り値の表現（`bool` か位置か）を合わせる過程で挙動が変わるリスクを持ち込む必要はない。`skip_leading_trivia` を独立に書き、ループ数行の重複は許容する |
| D9 | **新規テストファイルは作らず、既存の担当ファイルに足す**（計画レビュー 2 本が独立に指摘） | このリポジトリのテストは機能ごとに割られている（`tests/dml.rs` が `StatementType`、`tests/results.rs` が `OutputLocation`、`tests/metadata.rs` が `.metadata`）。「先頭にコメントが付いた SQL」は機能ではなく入力の一形態なので、同じ `StatementType` の検証が 2 ファイルに分かれると次に判定を変える人がどちらを直すか分からなくなる |
| D10 | **`SubstatementType` の結合テストは足さない**（計画レビューの削減指摘を採用） | `StatementType` と `SubstatementType` は同じ 2 行（`operation.rs:575-576`）で作られ、HTTP 経由の配線は `StatementType` 側で 4 箇所実証済み。コメント処理固有の分岐は `words()` という純粋関数に閉じているので、ユニットテストの表（`operation.rs:795-852`）に 1 行足せば同じ確信度が得られる |

## 計画

**1 フェーズ**（D1）。実測結果を受けてから着手する。

### 分岐

- **分岐 A（本物はコメントを飛ばす）**: 下記のとおり実装する
- **分岐 B（本物もコメントを飛ばさない）**: コードは変えず、実測日つきで「本物と一致することを確認した」と
  CLAUDE.md・README に書き、現状の挙動を固定する結合テストだけを足す。issue の完了条件（実測どおりであることを
  結合テストで固定する）はこの分岐でも満たせる

### ファイル構成（実績。`git diff f31c26c...HEAD --stat` より）

`src/catalog.rs` +73／`src/operation.rs` +11／`src/results.rs` +22／`tests/dml.rs` +38／`tests/results.rs` +39／
`tests/metadata.rs` +53／`CLAUDE.md` +2／`README.md` +12／`CHANGELOGS.md` +12。新規ファイルなし。
差分後の行数は `catalog.rs` 369／`operation.rs` 860／`results.rs` 610。

### 依拠する既存機構の前提表

| 暗黙の前提 | 自分の変更が破らないか | 誰が保証しているか |
|---|---|---|
| 区切り文字は ASCII なのでバイト単位の走査で UTF-8 の途中を切らない | 破らない（同じ走査を使う） | `src/catalog.rs:20`（`alias_qualified_names` 冒頭のコメント） |
| `comment_end` はネストしたブロックコメントを内側の最初の `*/` で閉じ、未閉じなら末尾を返す | 破らない（呼ぶだけで実装を変えない） | `src/catalog.rs:72-86` と `catalog.rs:285`（`閉じていない引用符やコメントでも止まらない`） |
| `is_create_table_as` の 3 通り OR は 2 つの呼び出し元の括弧の扱いの差を吸収している | 破らない（D4） | `src/results.rs:96-101` |
| `GetQueryExecution` の `Query` は受け取った SQL のまま返す | 破らない（判定用のローカルなスライスにしか触らない） | `src/operation.rs:574`（`query: execution.query.clone()`） |
| 偽 Trino の `route(sql, response)` は SQL の完全一致 | 破らない（新規テストはコメント込みの SQL をキーにする） | `tests/common/mod.rs:181-182` |

### 直列化・エスケープ境界／表現の正規化境界

- 直列化・エスケープ境界: **該当なし**。SQL 本文を書き換えず、判定用のローカルなスライスしか作らない。
- 表現の正規化境界: `ResultFile::of` の中で「読み飛ばし後」と「元の `query`」の 2 つの表現が同居する（D5）。
  変数名で境界を明示する（`query` は元の文字列だけを指す）。

### 一致台帳

| 対 | 守り方 |
|---|---|
| `operation.rs::words()` と `results.rs::ResultFile::of` が同じ前処理を使うこと | **共通化**（同一関数 `crate::catalog::skip_leading_trivia` を両方が呼ぶ）。`is_create_table_as` が `results.rs` で定義され `operation.rs:685` から呼ばれているのと同じパターン |

### 追加・変更するテスト（実績）

- `src/catalog.rs` ユニット 8 件（新規）: 7-0 の 5 分岐 + 非 ASCII + 未閉じ + 括弧始まり
- `src/operation.rs` ユニット: `statement_type_は...` に 2 行、`substatement_type_は...` に 1 行（既存の表に追加）
- `src/results.rs` ユニット: `文の先頭で置くファイルの種類を決める` に 4 行（コメント付き SELECT／SHOW／CTAS／Iceberg CTAS）、
  `iceberg_の_ctas_だけ_tables_を付けない` に 1 件（D5 を守る: 先頭コメント内の `table_type='ICEBERG'` も数えたまま）
- 結合 3 件（既存ファイルに 1 関数ずつ。D9）: `tests/dml.rs`（`StatementType` と DDL の `UpdateCount` 省略）／
  `tests/results.rs`（`.csv` と `tables/<id>`）／`tests/metadata.rs`（`.metadata` の先頭が `QueryExecutionId`）
- 配線の予測は的中: 既存テストに先頭コメント付きの SQL は 0 件で、全件無変化だった

### ミューテーション確認で壊す候補

1. `skip_leading_trivia` のループを 1 回だけの読み飛ばしに壊す → 連続コメント（`-- a\n-- b\nSELECT 1`）のテストが落ちる
2. `words()` への差し込み行を削る → `tests/dml.rs` の `StatementType` が落ちる
3. `ResultFile::of` への差し込み行を削る → `tests/results.rs` の `OutputLocation` が落ちる
4. `is_iceberg_table` に渡す引数を読み飛ばし後の変数に差し替える → D5 を守るユニットテストが落ちる
5. **`ResultFile::of` の `words` を元の `query` から作る**（D5 の裏）→ コメント付き CTAS のユニットテストが落ちる

### 人間検証リスト

1. issue 本文の 3 点（`-- comment\nSELECT 1`／`/* comment */ SELECT 1`／コメント付き CTAS の `StatementType` と `OutputLocation`）
2. コメントだけの文（`-- only`）を `StartQueryExecution` に渡したときの本物のエラー
3. コメントの後に空行・空白が続く形
4. `-- c\nDESCRIBE t` と `-- c\nSHOW CREATE TABLE t` の `.metadata` の先頭のクエリ ID（D6 の波及。
   `metadata_query_id` は `word(1)` まで見る唯一の分岐を持つ）

→ すべて実測スクリプトに入れてある（4 は `metadata_query_id` 列として形（trino / uuid）を出す）。

## 計画レビューの結果

Explore 2 本（契約・構造／再利用・削減）。4 ゲートを通した結果:

| # | severity | 指摘 | 判定 |
|---|---|---|---|
| 1 | high | `ResultFile::of` の `words` をどちらの文字列から作るか計画が明記していない。元の `query` から作ると `is_create_table_as` が語のずれで `false` を返し、**コメント付き CTAS だけが `.txt` のまま残る** | **採用**（D5 を書き換え）。前提検証: `results.rs:88-94` を読み、`words[1]` が `"TABLE"` でないと `false` を返すことを確認した |
| 2 | high | `metadata_query_id`（`operation.rs:356-358`）は `(word(0), word(1))` で `("SHOW","CREATE")` を見る唯一の分岐。先頭コメント付き `SHOW CREATE TABLE` が実測にもテストにも入っていない | **採用**。実測スクリプトに `a-line-show-create` / `b-block-show-create` と `metadata_query_id` の列（先頭 field 1 の形を trino / uuid で出す）を足した。テストは `tests/metadata.rs` に足す |
| 3 | medium | コメント付き DDL で `UpdateCount` の有無が変わるのにテスト計画に無い | **採用**。`tests/dml.rs` の 1 関数に含める（追加コストはほぼゼロ） |
| 4 | low（削減） | `tests/comments.rs`（新規）は要らない。既存の担当ファイルに 1 関数ずつ足すほうが小さい | **採用**（D9） |
| 5 | medium（削減） | `SubstatementType` の結合テストは実利がない。ユニットテストの表に 1 行足せば同じ確信度 | **採用**（D10） |
| 6 | low（削減） | `next_is_dot` を書き直す計画は本 issue のスコープ外。無関係な既存機能に挙動変化のリスクを持ち込む | **採用**（D8） |
| 7 | low（削減） | 前提表の「未保証: 先頭に文字列リテラルが来る文は無い」行と人間検証リストの項目 5 は、計画自身が「実測の対象外」と書いており確認しようがない | **採用**（両方を削り、理由は `skip_leading_trivia` の doc コメント 1 行に残す） |
| 8-10 | low（確認） | D2（`catalog.rs` に足す）は妥当／`-- only` は読み飛ばし後が空文字列になり分類は変わらず直後の構文エラーで実行が作られない／`write_result`・`write_failure` の波及（`Text`→`Csv`）は既存の実測済みルールに合流するだけ | いずれも計画の修正なし。`skip_leading_trivia` の doc に空文字列の経路を 1 行書いた |

**再レビューはしない**（構造の観点で計画を書き直してはいないため。採用したのは境界の明示・実測項目の追加・テストの置き場所と削減）。

## フェーズの記録

### 7-0（着手前に答えておくこと。2026-09-18 09:05）

- **消す・変えるガードや条件**: 無い。既存の分岐は 1 つも消さず、判定に渡す文字列を前処理するだけ。
- **変える関数の呼び出し元**（grep 済み）:
  - `words()`（`src/operation.rs:639`）→ `:353` `metadata_query_id`／`:648` `statement_type`／`:660` `substatement_type` の 3 箇所。すべて同ファイル内の private。
  - `ResultFile::of`（`src/results.rs:40`）→ 本番は `src/operation.rs:219`（`result_location`）の 1 箇所のみ。ほかは `src/results.rs` のユニットテスト 4 箇所（`:565`/`:577`/`:584`/`:588`）。
  - 公開シグネチャは変えない。`skip_leading_trivia` を `pub(crate)` で新設するだけ。
- **このフェーズで増える分岐の全列挙**: `skip_leading_trivia` の 5 つ（空白／`--`／`/* */`／トリビアでない文字で止まる／
  全部トリビア）。それぞれに固定するテスト名を先に決め、7-5b で 1 対 1 で照合した（下記）。ほかの 2 ファイルは
  1 行の差し込みだけで分岐は増えない。
- **通る境界入力**: `--c\nSELECT 1`（`--` の直後に空白なし）／`/* c SELECT 1`（未閉じ）／`/*/ x */ SELECT 1`（`/*` の `*` を閉じと誤読しないか）／`""`（空文字列）／`"   "`（空白だけ）／`-- あ\nSELECT 1`（**非 ASCII のコメント**。`comment_end` はバイト単位で `\n` を探すが `\n` は ASCII なので多バイト文字の途中には現れない。テストで固定する）／`(SELECT 1)`（括弧始まり。トリビアではない）
- **design-checklist の該当項目**: #17 で足した 2 項目がまさに該当し、D5（前処理後・前処理前のどちらを渡すか 1 つずつ決める）と D6・実測の `SHOW CREATE TABLE` 追加で扱い済み。#5 由来の項目（早期 return の下流に新しい出力を足すとき）は非該当（新しい出力を足さない）。
- **配線の予測の確認**: `grep -rn '"-- |"/\* |\\n-- ' tests/ src/` の結果は 0 件。既存テストで先頭にコメントを置いている SQL は無く、**既存テストは全件無変化**の見込み。

### 実装（2026-09-18）

1. red: 先にテストを足して確認。`operation::tests::statement_type_は先頭のキーワードで決まる`／
   `substatement_type_は実測した文の種類を返す`／`results::tests::文の先頭で置くファイルの種類を決める`／
   `iceberg_の_ctas_だけ_tables_を付けない` の 4 件が落ちた（予測どおり）。`catalog.rs` の新規ユニットテストは
   `skip_leading_trivia` を実装済みで足したので最初から green。
2. green 後: `cargo fmt --check` OK／`cargo clippy --all-targets --locked -- -D warnings` OK（警告 0）／
   `cargo test` 全 218 件 green（lib 122、結合テスト 96）。
3. ミューテーション 5 件、1 件ずつスクラッチパッドに退避して壊し単独フィルタで確認、すべて予測どおり落ちて復元した:
   (1) ループを 1 回にする→`空白とコメントを交互に読み飛ばす` 落ちる (2) `words()` の差し込み削除→
   `statement_type_は先頭のキーワードで決まる` 落ちる (3) `ResultFile::of` の差し込み削除→
   `文の先頭で置くファイルの種類を決める` 落ちる (4) `is_iceberg_table` の引数を `trimmed` に差し替え→
   `iceberg_の_ctas_だけ_tables_を付けない` 落ちる (5) `words` を元の `query` から作る（D5 の裏）→
   `文の先頭で置くファイルの種類を決める`（コメント付き SELECT の行で）落ちる。
4. 計画からの逸脱: 無し。D1〜D10 はすべてそのまま踏襲した。
5. **TDD の手順の緩み（記録）**: `catalog.rs` の新規ユニットテスト 8 件は `skip_leading_trivia` の実装と同時に
   足したので red を見ていない。効いていることはミューテーション (1) で確認した（ループを 1 回にすると落ちる）。
   ほかの 4 件は予測どおり red → green を見ている。

### 7-5b 自己レビュー（統括役が差分を読んだ。2026-09-18）

- **分岐の突き合わせ**: 差分に入った分岐は `skip_leading_trivia` の 4 つ（空白／`--`／`/* */`／トリビアでない文字で break）と
  ループ終了（全部トリビア）。7-0 の一覧と 1 対 1 で照合し、**固定するテストの無い分岐は 0**。加えて非 ASCII と未閉じも固定されている。
  `words()` と `ResultFile::of` は 1 行の差し込みだけで分岐は増えていない。
- **エラーの出口**: 新しいエラー戻り値・例外は無い。コメントだけの文は空文字列になり、既存の
  `Trino::syntax_error` が弾く経路に合流する（`ResultFile::of` は `_ => Text` に落ちるが、実行が作られないので観測されない）。
- **呼び出し元 4 箇所の確認**: `statement_type`（ユニットテストあり）／`substatement_type`（同）／
  `metadata_query_id`（`tests/metadata.rs` の新規テストが `execution_id_field` を期待値にして固定）／
  `result_location`（`tests/results.rs` の新規テストが `.csv` と `tables/<id>` を固定）。**全箇所にテストがある。**
- **統括役が直した点（2 件）**: `catalog.rs` の doc の行番号参照（`:20`）を理由の文言に置き換えた（参照はズレる）。
  `results.rs` の doc の「本 issue では変えない」を「是正は #26」に変えた（issue の文脈はコードに残らないため）。

### 7-7 分割リファクタ

分割不要。`catalog.rs` 369 行・`results.rs` 610 行・`operation.rs` 860 行で、差分はどれも 1 桁行か新規関数 1 つ（15 行）。
新規ファイルは作っていない。`operation.rs` の分割は範囲外として **#25** に起票済み。

## レビュー実施記録

| レビュー | 回数 | 本番影響のある妥当指摘 |
|---|---|---|
| 計画の敵対的レビュー（Step 6、Explore 2 本） | 1 | **high 2 件**（どちらも採用。実測スクリプトの修正に間に合った）＋ 削減 4 件 |
| フェーズのレビュー（7-6） | 0（小規模 issue の経路で省略） | — |
| 最終パス（Step 8） | 0（小規模 issue の経路で省略） | — |
| 独立レビュー（Step 8、general-purpose 1 本） | 1 | **0 件** |

レビュー起動は計 3 回（上限 4）。**計画レビューが high を 2 件とも先に捕まえ、独立レビューは 0 件**だった。
high の 1 件（`metadata_query_id` が 2 語目を見る唯一の分岐）は、実測スクリプトを**ユーザーが実行する前に**直せたので
追加ラウンドにならなかった。

## Step 8 の結果

独立レビュー（差分と検証コマンドだけを渡し、`scope`・`humanDecisions`・ノートは渡していない）:

- **medium 以上の指摘: 0 件。**
- レビュアーが自分で落とした 1 点: コメント付き `SHOW CREATE TABLE` を通しで見る結合テストが無い（`DESCRIBE` のみ）。
  「実害のある失敗シナリオは組み立てられなかった」として指摘の対象から外している。
- 統括役の判定: **記録として 1 行だけ足した**（`substatement_type` のテスト表に `("-- c\nSHOW CREATE TABLE t", "SHOW_CREATE_TABLE")`）。
  **ただしこれは独立した欠陥を捕まえるテストではない**。`words()` を共有するので `-- c\nSELECT 1` と同じ経路を通り、
  ミューテーション（`words()` の差し込み削除）でも表の最初のケースが先に落ちる。2 語目以降も読み飛ばし後の並びから取ることを、
  次に `metadata_query_id` を触る人に示すための記録として残した。結合テストは足していない（固定費に見合わない）。
- そのほか確認された事項: `skip_leading_trivia` が無限ループしないこと（`comment_end` ごとに必ず前進する）、
  `result_location` が構文チェックより前に呼ばれても観測できる差にならないこと、D5 の非対称が新規のバグでないこと。

## dismissed

（なし。計画レビューの指摘は 10 件すべて採用または「確認のみ・修正不要」で、反証して落としたものは無い）

## deferred

| 指摘 | severity | file | 再トリアージ | 起票 |
|---|---|---|---|---|
| （なし） | | | | |

## 覆した事前判断

| 元の判断 | 新しい判断 | 理由 | 外部契約・プロダクト挙動・運用への影響 | ユーザーに聞いたか |
|---|---|---|---|---|
| Plan の下書き: 2 フェーズ（フェーズ 1 で読み飛ばし関数だけ作り、フェーズ 2 で配線） | **1 フェーズ**（D1） | Step 6b の承認前にフェーズへ入れない規則がある以上、実測待ちの間にフェーズ 1 を進められず分ける実利が無い。実測が分岐 B なら未配線の新モジュールを捨てることになる | 無し（工程の分け方だけ） | Step 6b で提示して承認を得た |
| Plan の下書き: `src/lexer.rs` を新設し `comment_end` を `pub(crate)` にして跨いで呼ぶ | **`src/catalog.rs` に足す**（D2） | `catalog.rs` は既に `skip_quoted`／`comment_end`／`next_is_dot` という字句走査の道具箱。新モジュールを作ると字句処理が 2 ファイルに散る。計画レビュー 2 本が独立に「新モジュールの実利は無い」と結論 | 無し（内部構造だけ。`skip_leading_trivia` は `pub(crate)`） | Step 6b の選択肢に入れて提示し、現案が選ばれた |
| 計画（初版）: `tests/comments.rs` を新設し `SubstatementType` の結合テストも足す | **既存の 3 ファイルに 1 関数ずつ。`SubstatementType` の結合テストは足さない**（D9・D10） | 計画レビュー 2 本が独立に指摘。`StatementType` と `SubstatementType` は同じ 2 行で作られ結合テストの実利が無く、新規ファイルは同じ検証が 2 箇所に分かれる | 無し（テストの置き場所だけ） | Step 6b の選択肢に入れて提示し、現案が選ばれた |
| 計画（初版）: `next_is_dot` を読み飛ばし関数の上に書き直す | **`next_is_dot` は触らない**（D8） | 計画レビューの削減指摘。`alias_qualified_names` 専用で本 issue と無関係。Trino 実機で検証済みの機能に挙動変化のリスクを持ち込まない | 無し | 聞いていない（工学判断。リポジトリ内の前例から導出できる） |

## 実機検証

2026-09-18。**バイナリ経由**（`main.rs` → `axum::serve`）で検証した。結合テストはライブラリの `router()` を使うので、
バイナリ全体を通した HTTP 経路はここで初めて通した。本物の Trino はローカルに無いので（8080/8081 とも閉）、
python3 の偽 Trino をスクラッチパッドに立て、`curl` で Athena API を叩いた。ポートは 19080（athena-local）/ 19081（偽 Trino）。

| # | QueryString | StatementType | SubstatementType | 結果 |
|---|---|---|---|---|
| 1 | `-- c\nSELECT 1` | DML | SELECT | ○ |
| 2 | `/* c */ SELECT 1` | DML | SELECT | ○ |
| 3 | `-- SELECT\nSHOW TABLES` | UTILITY | SHOW_TABLES | ○ |
| 4 | `/* SHOW TABLES */ SELECT 1` | DML | SELECT | ○ |
| 5 | `-- a\n-- b\nSELECT 1` | DML | SELECT | ○ |
| 6 | `SELECT 1`（対照） | DML | SELECT | ○ |
| 7 | `SHOW TABLES`（対照） | UTILITY | SHOW_TABLES | ○ |

**7 件すべて期待どおり。落ちたケースは無い。**

- `GetQueryExecution` の `Query` は 7 件すべて受け取った `QueryString` とバイト単位で一致（コメント・改行込み）。
  **SQL 本文を書き換えていない**ことをバイナリ経由で確かめた。
- 偽 Trino に届いた SQL は、構文チェックの `PREPARE athena_local_syntax_check FROM\n<SQL>` も本体も、
  7 件すべてコメントと改行がそのまま。削られていない。
- `OutputLocation` を付けたケース（`-- c\nSELECT 1` + `s3://bucket/prefix/`）は `...<id>.csv` で終わった（`.txt` ではない）。
  `ATHENA_LOCAL_RESULTS` 未設定なので S3 への書き込みは試みず `SUCCEEDED`。
- **最小構成の検証**: 絞り込み無しで `cargo fmt --check`／`cargo clippy --all-targets --locked -- -D warnings`／
  `cargo test` を 1 回ずつ走らせ、すべて green（218 件）。
- 起動したプロセスは両方停止し、ポートの解放を確認済み。既存の docker コンテナには触れていない。

**本物の Trino に対する検証は未実施**（ローカルに無い）。ただしこの変更は SQL を書き換えず、Trino から見た入力は
本 issue の前後で 1 バイトも変わらないので、本物の Trino でしか出ない欠陥の余地は無い（上記のとおり届く SQL が同一であることを確認済み）。

## 範囲外の発見

| 内容 | 出どころ | 起票 |
|---|---|---|
| `is_iceberg_table`（`src/results.rs:107-110`）は全文検索なので、コメントや文字列リテラルの中の `table_type='ICEBERG'` も数える。先頭コメントを飛ばしても直らない | Explore 1・README:445-450（既知の粗さとして記載済み） | D7 で範囲外と決めた。**#26** |
| `src/operation.rs` が 853 行。本 issue の差分は 10 行未満なので分割しない | 計画のファイル構成 | **#25** |
| 本物は `/* c */ SHOW CREATE TABLE t` を FAILED にする（分類は正しく返る）。athena-local は Trino に投げるので成功しうる | 実測 `b-block-show-create` | **#27**（README の Caveats にも書く） |
| 本物の `SHOW TABLES` の `.metadata` は protobuf ではない別形式。athena-local は protobuf を書いている | 実測（`probe-show-tables` / `a-line-show` の先頭バイト） | **#24** |
| `SELECT` の `.csv` の Content-Type が今回は `binary/octet-stream` だった（athena-local は `application/octet-stream`） | 実測 `a-line-select` | 起票しない。`#1` のノートにある既知の揺れ（実測のたびに binary と application に割れ、多数派を採ると決めてある） |

## 委譲の記録

| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| Step 3 調査 | 委譲（Explore 3 本） | |
| Step 3 の結果の転記 | 自分 | 読み取り専用の委譲先の結果は統括役が転記する規則 |
| Step 3b 実測スクリプト | 委譲（general-purpose 1 本） | |
| Step 5 計画の下書き | 委譲（Plan 1 本） | |
| Step 3b スクリプトの受け入れ判定 | 自分 | 本番の認証情報で実行するものなので規則どおり委譲しない |
| Step 5 計画の転記と判断 | 自分 | 読み取り専用の委譲先の結果は統括役が転記する規則。D1・D2 は Plan の案を覆した |
| Step 6 計画レビュー | 委譲（Explore 2 本） | |
| Step 6 のトリアージと実測スクリプトの修正 | 自分 | トリアージは判断そのもの。スクリプトの修正は 1 ファイル・数分で、指示を書くほうが長い |

## 出荷

- PR: **#28**（<https://github.com/aoyagikouhei/athena-local/pull/28>）。`Closes #17`
- issue #17 のコメント: <https://github.com/aoyagikouhei/athena-local/issues/17#issuecomment-5723273133>
  （実測の結果・本文の前提の訂正・起票した番号・人間にお願いする検証）
- 起票した issue: **#24**（`SHOW TABLES` の `.metadata` が protobuf でない）／**#25**（`operation.rs` 853 行）／
  **#26**（`is_iceberg_table` の全文検索）／**#27**（本物は `/* c */ SHOW CREATE TABLE` を弾く）
- deferred: **0 件**（low として先送りした指摘は無いので、その旨の issue コメントは投稿していない）
- **出荷判定**: 独立レビューが「本番影響のある妥当指摘 0 件」を返し、停止規則を満たした。実機検証も 7 ケース全パス。
- 予算の実績: 実装コミット 1（上限 3）／レビュー対応コミット 1（上限 5）／docs 単独 2（上限 2。ほかに quick ノート 1 と
  実測スクリプト 1 が別枠）／レビュー起動 3（上限 4）／実測ラウンド 1

## 最終の予算と委譲

| 種類 | 実績 |
|---|---|
| コミット | 7 本（実装 1・レビュー対応 1・docs 単独 2・実測スクリプト 1・quick ノート 1・出荷記録 1） |
| レビュー起動 | 3（計画レビュー 2 + 独立レビュー 1。フェーズのレビューと最終パスは小規模 issue の経路で省略） |
| サブエージェント | 9 本（Explore 3・実測スクリプト 1・Plan 1・計画レビュー 2・実装 1・実機検証 1） |
| 実測ラウンド | 1（33 項目すべて測れ、未測定 0・再試行 0） |

## 時系列

| 時刻 | 出来事 | SHA |
|---|---|---|
| 2026-09-18 08:19 | quick-implement で着手、ゲートの「事実」で外れる | |
| 2026-09-18 08:21 | `main-17` を作成し quick ノートをコミット、implement-issue へ引き継ぎ | `e09d40f` |
| 2026-09-18 08:25 | Explore 3 本を並列起動 | |
| 2026-09-18 08:30 | 実測スクリプト作成と Plan を並列起動 | |
| 2026-09-18 08:40 | 実測スクリプトを受け入れ、ユーザーに実測を依頼 | |
| 2026-09-18 08:45 | 計画レビュー 2 本を起動 | |
| 2026-09-18 08:58 | レビューを 4 ゲートでトリアージし、計画と実測スクリプトを修正 | `9ba8f26` |
| 2026-09-18 09:03 | Step 6b: ユーザーが設計を承認（「この計画で進める」） | |
| 2026-09-18 09:05 | 7-0 を記録。実測の結果を待つ | `d5cbefc` |
| 2026-09-18 09:06 | ユーザーが実測を実行（33 項目、分岐 A で確定） | |
| 2026-09-18 09:30 | 実装を委譲 → 差分を読んで受け入れ、コミット | `5087668` |
| 2026-09-18 09:35 | 範囲外の発見 4 件を起票（#24〜#27） | |
| 2026-09-18 09:45 | 実機検証（偽 Trino + 実バイナリ）7 ケース全パス | |
| 2026-09-18 09:50 | 独立レビュー: medium 以上 0 件 | |
| 2026-09-18 09:55 | 記録として 1 行足す | `156b201` |
| 2026-09-18 10:00 | PR #28 を作成、issue にコメント | |
