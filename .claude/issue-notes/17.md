# issue #17 先頭にコメントが付いた SQL の文の種類を正しく判定する

## 基本情報

- issue: #17（`gh issue view 17`）。ベースブランチ `main`、着手前 SHA `f31c26c`
- ブランチ: `main-17`
- 経緯: `quick-implement` で着手し、ゲートの「事実」（本物の Athena の実測が未了）で外れたため `implement-issue` へ引き継いだ。
  quick ノートのコミットは `e09d40f`（docs 予算の別枠）。issue にも同じ内容をコメント済み
  （<https://github.com/aoyagikouhei/athena-local/issues/17#issuecomment-5722534423>）
- 検証コマンド: `cargo fmt --check` ／ `cargo clippy --all-targets --locked -- -D warnings` ／ `cargo test`

## 事前に確認した事実

Explore 3 本の番号付き返答から転記。落とした番号と理由は末尾に書く。

### 判定ロジックの現状

- `src/operation.rs:638-644` `words()`: `split_whitespace()` した**全トークン**に `trim_start_matches('(')` を掛けて大文字化する。
  doc コメントに「先頭のコメントは考慮しない」と明記（638 行）。
- `words()` の呼び出し元は **3 箇所**（すべて `src/operation.rs` 内、private）:
  - `:353` `metadata_query_id` — `.metadata` の先頭に入れるクエリ ID を Trino の ID にするか `QueryExecutionId` にするかを決める
  - `:648` `statement_type`
  - `:660` `substatement_type`（`is_create_table_as(&words)` にもこの配列を渡す。`:685`）
- `src/results.rs:38-59` `ResultFile::of()`: 括弧を残した配列を作り、`word(0)` を使う瞬間にだけ `trim_start_matches('(')` を掛ける（`:47`）。
  唯一の呼び出し元は `src/operation.rs:219`（`result_location`）。
- **重要な非対称**: `is_create_table_as`（`src/results.rs:86-102`）には 2 種類の形の配列が渡る
  （`operation.rs:685` は括弧除去済み、`results.rs:55-56` は括弧付き）。`:96-101` の
  `starts_with("SELECT") || starts_with("WITH") || starts_with('(')` の 3 通り OR がこの差異を吸収している。**崩さないこと。**
  - 例 `CREATE TABLE t AS (SELECT 1)`: `operation.rs` 側 `[...,"AS","SELECT","1)"]`／`results.rs` 側 `[...,"AS","(SELECT","1)"]`
- レスポンスへの流れ: `statement_type`/`substatement_type` → `to_query_execution`（`:575-576`）→ `get_query_execution`（`:405-407`）→
  `GetQueryExecutionResponse.QueryExecution.StatementType`/`.SubstatementType`。`update_count`（`:629-636`）→ `get_query_results`（`:526`）→
  `GetQueryResultsResponse.UpdateCount`。`ResultFile::of` → `ResultLocation::new`（`results.rs:120-130`）→ `uri()`（`:152-155`）→
  `ResultConfiguration.OutputLocation`。加えて `write_result`（`operation.rs:276-277`）の「本体を書くか」の判断にも効く。
- **先頭のキーワードを見ている箇所は上記 6 関数に閉じている**（grep 済み）。`src/metadata.rs`・`src/convert.rs`・`src/statement.rs` には無い。

### 共有できる字句処理（`src/catalog.rs`）

- `pub` は `alias_qualified_names` だけ。`skip_quoted`(:55) / `comment_end`(:72) / `next_is_dot`(:89) / `unquote`(:103) / `replacement`(:113) は private。
- `comment_end`(:72-86) の挙動: `--` は `\n` の**直前**まで（`\n` 自体は含まない）。`/**/`→4、`/* */`→5、`/*`（未閉じ）→2、`/*/` 単独→3（未閉じ扱い）。
  `rest[2..]` から `*/` を探すので `/*` の `*` を閉じの一部として誤読しない（`catalog.rs:203-204` がテスト済み）。
  ネストしたブロックコメントは内側の最初の `*/` で閉じる（SQL 標準どおりで変える必要なし）。
- `next_is_dot`(:89-100) の「空白とコメントを交互に読み飛ばす」ループがそのまま転用できる。戻り値を `bool` → 位置に変えて切り出せる。
- `src/lib.rs:5` は `mod catalog;`（非 pub）のままでよく、`pub(crate) fn` にすれば `operation.rs`/`results.rs` から `crate::catalog::...` で呼べる。
- 先頭に文字列リテラルが来る有効な SQL 文は無いので、先頭コメント飛ばしで `skip_quoted` は要らない。

### 既存テストと docs

- `StatementType` の結合テスト: `tests/dml.rs:23`、`:65-66`、`tests/parameters.rs:79`、`:114` の 4 箇所だけ。
  **`SubstatementType` の結合テストは 1 つも無い**（ユニットテスト `src/operation.rs:795-852` のみ）。
- `OutputLocation` のファイル名: `tests/results.rs:63-66`/`124-127`/`204-220`（4 形をまとめて固定）/`264-267`/`417-423`、
  `tests/metadata.rs:107`/`149`/`330-337`/`432`、`tests/failed_results.rs:49`/`118-119`。
- 雛形: `tests/results.rs:50-81`（`s3_puts()` の典型）と `tests/dml.rs:44-92`（`StatementType` の典型）。
- `tests/common/mod.rs:32-33` の `SYNTAX_CHECK_PREFIX` を偽 Trino が `:477-489` で見て構文チェックを識別する。
- README: Caveats は `345-536`。**先頭コメントの扱いに触れた条項は無い。** 関連は `381-385`（Syntax differs）と
  `445-450`（Unmeasured file names。CTAS の Iceberg 判定が文字列リテラルとコメントの中の `table_type='ICEBERG'` も数える既知の粗さ）。
  Supported API は `166-171`（`StatementType`/`SubstatementType` の説明）と `329-331`（`StatementType` は元の SQL から導く）。
- CHANGELOGS: `## [Unreleased]`(:10) の直下が `### Added`(:12)。過去版は `## [0.4.0] - 2026-09-15`(:139) の形。
- `.claude/design-checklist.md` は #5 由来の 1 項目のみ（早期 return の下流に新しい出力を足すときの全経路確認）。

### 実測の状況（**重要な訂正**）

`.claude/issue-notes/5.md:407` と `:419` の記述は **athena-local 相手の実機検証での観測と推測**であって、本物 Athena の実測値ではない
（「本物なら `.csv` になるはず」と書かれている）。issue 本文の「#5 の実機検証で判明」は実測値の意味ではない。
したがって `-- comment\nSELECT 1` を含め、**コメント付き SQL に対する本物の `StatementType`／`OutputLocation` は 1 件も実測されていない。**
統括役の環境には AWS 認証情報が無い（`aws sts get-caller-identity` が `NoCredentials`）。

落とした番号と理由: Explore 1 の (6) 既存ユニットテストの内容の逐条引用（テスト名と場所だけ残し、中身は実装時に読めばよい）、
Explore 3 の (1) `tests/` 各ファイルの doc コメント全文（ファイル名と主題だけ残した）、(8) 実測スクリプト 2 本の全文
（実測スクリプト作成の委譲先に直接読ませた）。

## 実測の手順

- スクリプト: `.claude/issue-notes/17-measure-leading-comment.sh`（535 行）
- 実行: `OUTPUT=s3://your-bucket/prefix/ DB=your_db PROBE_DDL=1 bash .claude/issue-notes/17-measure-leading-comment.sh`
- 必須の環境変数: `OUTPUT`（`s3://bucket/prefix/`、末尾の `/` を付ける）・`DB`（実在するデータベース名）。
  任意: `TABLE`・`CATALOG`（既定 `AwsDataCatalog`）・`REGION`（既定 `ap-northeast-1`）・`PROBE_DDL`
- 出力先: `$HOME/athena-comment-measurements/run-<日時>/`（リポジトリの外）。貼るのは `summary.txt`
- 本物への呼び出し: `StartQueryExecution` を `PROBE_DDL=0` で 26 回、`=1` で 31 回
- DDL: `PROBE_DDL=0` でも `CREATE/DROP DATABASE athena_local_probe_17` を 4 回（A 群と B 群で作って消す）。
  `=1` なら Iceberg テーブル `athena_local_probe_17` / `athena_local_probe_17b` を作って必ず消す（`trap` でも保険をかける）
- 課金: スキャンしない計算だけのクエリ（`SELECT 1` 等）と `SHOW`／`DESCRIBE`／`EXPLAIN` のみ。`SELECT *` は使わない
- 秘密: `summary.tsv`／`summary.txt` は DB 名・テーブル名を伏せてある。`<label>.reason.txt` と
  `<label>.results.json` は実名を含みうるので貼る前に確認する
- 統括役の受け入れ判定（2026-09-18 08:40）: 課金・DDL・後始末・秘密の扱いを全文読んで確認した。
  `PROBE_DDL=0` でも DATABASE の DDL が走る点だけ依頼文に明記する

## 実測の結果

（未実施）

## 実測の経緯

- 2026-09-18 08:30 ラウンド 0: 統括役の環境に認証情報が無いことを確認し、スクリプト作成を委譲した。

## 設計判断

| # | 判断 | 理由 |
|---|---|---|
| D1 | **1 フェーズにする**（Plan の 2 フェーズ案を覆す） | Step 6b の承認前にフェーズへ入らない規則（4 原則 2）があるので、実測待ちの間にフェーズ 1 を進めることはできない。分ける実利が無い。加えて実測が「本物も飛ばさない」と出れば読み飛ばし関数自体が不要になり、未配線の新モジュールを先に入れると捨てることになる |
| D2 | **読み飛ばし関数は `src/catalog.rs` に足す**（Plan の `src/lexer.rs` 新設案を覆す） | `catalog.rs` は既に `skip_quoted`／`comment_end`／`next_is_dot` という SQL 字句走査の道具箱を持っている（`:55`/`:72`/`:89`）。`lexer.rs` を新設して `comment_end` を `catalog.rs` に残すと**字句処理が 2 ファイルに散る**。`comment_end` ごと移すのは挙動を変えない移動とはいえ差分が大きく、issue の規模（判定の前に前処理を 1 つ足す）に対して過剰。モジュール doc を 1 行広げて実態に追随させる。**割れる判断なので Step 6b でユーザーに確認する** |
| D3 | シグネチャは `&str -> &str`（読み飛ばし後のスライスを返す） | 呼び出し側は直後に `split_whitespace()` するので、位置より文字列のほうが 1 行の差し替えで済む。区切りは全部 ASCII なので UTF-8 境界は壊れない（`catalog.rs:20` のコメント） |
| D4 | `trim_start_matches('(')` の非対称は**変えない** | `is_create_table_as`（`results.rs:96-101`）の 3 通り OR がこの非対称を吸収している。崩すとどちらかの呼び出し元で CTAS 判定が壊れる |
| D5 | `ResultFile::of` の中で、**`words` は読み飛ばし後の文字列から作り、`is_iceberg_table` に渡す引数だけを元の `query` にする** | 計画レビュー（契約）の high。`is_create_table_as` は `words[1..]` の先頭が `"TABLE"` であることを要求する（`results.rs:88-94`）ので、元の `query` から `words` を作ると `-- c\nCREATE TABLE t AS SELECT 1` が `["--","C","CREATE","TABLE",...]` になり `words[1]=="C"` で `false` に落ち、**コメント付き CTAS だけが `.txt` のまま残る**。`is_iceberg_table` は全文検索（`results.rs:107-110`）でコメント内の `table_type='ICEBERG'` も数える既知の粗さ（README:445-450）なので、そちらは本 issue で変えない。変数名を `trimmed`（読み飛ばし後）と `query`（元）に分けて境界を明示する |
| D6 | `metadata_query_id`（`operation.rs:353`）も `words()` 経由で自動的に影響を受けるのを**そのまま受け入れる** | 特別扱いすると判定が 3 箇所に増え、issue の意図に反する。実測に `-- c\nDESCRIBE t` を入れて未実測のまま残さないようにする |
| D7 | `is_iceberg_table` の粗さの是正は**範囲外**（起票する） | 先頭コメントを飛ばしても直らない別の問題。README に既知として書いてある |
| D8 | **`next_is_dot`（`catalog.rs:89-100`）は書き直さない**（計画レビューの削減指摘を採用） | `next_is_dot` は `alias_qualified_names`（カタログ別名）専用で本 issue と無関係。Trino 実機で検証済みの機能（`catalog.rs` の 12 テスト）に、戻り値の表現（`bool` か位置か）を合わせる過程で挙動が変わるリスクを持ち込む必要はない。`skip_leading_trivia` を独立に書き、ループ数行の重複は許容する |
| D9 | **新規テストファイルは作らず、既存の担当ファイルに足す**（計画レビュー 2 本が独立に指摘） | このリポジトリのテストは機能ごとに割られている（`tests/dml.rs` が `StatementType`、`tests/results.rs` が `OutputLocation`、`tests/metadata.rs` が `.metadata`）。「先頭にコメントが付いた SQL」は機能ではなく入力の一形態なので、同じ `StatementType` の検証が 2 ファイルに分かれると次に判定を変える人がどちらを直すか分からなくなる |
| D10 | **`SubstatementType` の結合テストは足さない**（計画レビューの削減指摘を採用） | `StatementType` と `SubstatementType` は同じ 2 行（`operation.rs:575-576`）で作られ、HTTP 経由の配線は `StatementType` 側で 4 箇所実証済み。コメント処理固有の分岐は `words()` という純粋関数に閉じているので、ユニットテストの表（`operation.rs:795-852`）に 1 行足せば同じ確信度が得られる |

## 計画

**1 フェーズ**（D1）。実測結果を受けてから着手する。

### 分岐

- **分岐 A（本物はコメントを飛ばす）**: 下記のとおり実装する
- **分岐 B（本物もコメントを飛ばさない）**: コードは変えず、実測日つきで「本物と一致することを確認した」と
  CLAUDE.md・README に書き、現状の挙動を固定する結合テストだけを足す。issue の完了条件（実測どおりであることを
  結合テストで固定する）はこの分岐でも満たせる

### ファイル構成（分岐 A）

| ファイル | 責務 | 差分の見込み |
|---|---|---|
| `src/catalog.rs`（296 行） | SQL の字句走査と別名の適用。`pub(crate) fn skip_leading_trivia(sql: &str) -> &str` を足す（`comment_end` を呼ぶ）。**`next_is_dot` は触らない**（D8）。モジュール doc を 1 行広げる | +15〜20 行（テスト込みで +55 行程度） |
| `src/operation.rs`（853 行） | `words()`（`:639`）の 1 行目に差し込む。`statement_type`／`substatement_type`／`words` の doc コメントの「先頭のコメントは考慮しない」を実測日つきで書き換える | +5〜10 行 |
| `src/results.rs`（592 行） | `ResultFile::of`（`:41`）で `words` を読み飛ばし後の `trimmed` から作り、`is_iceberg_table(query)` だけ元のまま（D5）。doc コメントを書き換える | +5〜10 行 |
| `tests/dml.rs` | コメント付きの `StatementType` と `UpdateCount` を固定する 1 関数 | +25〜35 行 |
| `tests/results.rs` | コメント付きの `OutputLocation`（`.csv` と CTAS の形）を固定する 1 関数 | +25〜35 行 |
| `tests/metadata.rs` | コメント付き `DESCRIBE`／`SHOW CREATE TABLE` の `.metadata` の先頭のクエリ ID を固定する 1 関数（D6 の波及） | +25〜35 行 |
| `CLAUDE.md` | 「どちらも先頭のコメントは考慮しない」の記述を書き換える | 1〜3 行 |
| `README.md` | Supported API（`166-171`）に挙動と実測日。Caveats に未実測のまま残る項目 | 5〜15 行 |
| `CHANGELOGS.md` | `[Unreleased]` に `### Fixed` を新設して 1 項目 | 5〜10 行 |

`operation.rs` は既に 853 行だが、本 issue では分割しない（差分は 10 行未満で、分割は別 issue に起票する → 範囲外の発見）。

### 依拠する既存機構の前提表

| 暗黙の前提 | 自分の変更が破らないか | 誰が保証しているか |
|---|---|---|
| 区切り文字は ASCII なのでバイト単位の走査で UTF-8 の途中を切らない | 破らない（同じ走査を使う） | `src/catalog.rs:20`（`alias_qualified_names` 冒頭のコメント） |
| `comment_end` はネストしたブロックコメントを内側の最初の `*/` で閉じ、未閉じなら末尾を返す | 破らない（呼ぶだけで実装を変えない） | `src/catalog.rs:72-86` と `catalog.rs:285`（`閉じていない引用符やコメントでも止まらない`） |
| `is_create_table_as` の 3 通り OR は 2 つの呼び出し元の括弧の扱いの差を吸収している | 破らない（D4） | `src/results.rs:96-101` |
| `GetQueryExecution` の `Query` は受け取った SQL のまま返す | 破らない（判定用のローカルなスライスにしか触らない） | `src/operation.rs:574`（`query: execution.query.clone()`） |
| 偽 Trino の `route(sql, response)` は SQL の完全一致 | 破らない（新規テストはコメント込みの SQL をキーにする） | `tests/common/mod.rs:181-182` |

### 直列化・エスケープ境界／表現の正規化境界

- 直列化・エスケープ境界: **該当なし**。SQL 本文を書き換えず、判定用のローカルなスライスしか作らない。
- 表現の正規化境界: `ResultFile::of` の中で「読み飛ばし後」と「元の `query`」の 2 つの表現が同居する（D5）。
  変数名で境界を明示する（`query` は元の文字列だけを指す）。

### 一致台帳

| 対 | 守り方 |
|---|---|
| `operation.rs::words()` と `results.rs::ResultFile::of` が同じ前処理を使うこと | **共通化**（同一関数 `crate::catalog::skip_leading_trivia` を両方が呼ぶ）。`is_create_table_as` が `results.rs` で定義され `operation.rs:685` から呼ばれているのと同じパターン |

### 追加・変更するテスト

- `src/catalog.rs` のユニットテスト: トリビア無し／行コメント（末尾に改行が無い形も）／ブロックコメント（閉・未閉・`/*/ ... */`）／
  空白とコメントの交互／コメントだけ／空白だけ／括弧始まり（`(SELECT 1)` が変化しないこと）
- `src/operation.rs` のユニットテスト: `statement_type_は先頭のキーワードで決まる`（`:776-793`）と
  `substatement_type_は実測した文の種類を返す`（`:795-852`）の表に、コメント付きの行を 1〜2 行足す（D10）
- `src/results.rs` のユニットテスト: `文の先頭で置くファイルの種類を決める`（`:538-567`）にコメント付きの行を足す。
  加えて**先頭コメントの中に `table_type='ICEBERG'` がある CTAS がこれまでどおり Iceberg 扱いのまま**であること（D5 を守るテスト）と、
  **コメント付き CTAS が `Table`／`Manifest` になる**こと（`words` を元の `query` から作る誤りを殺すテスト）
- 結合テスト（D9 のとおり既存ファイルに 1 関数ずつ）:
  - `tests/dml.rs`: コメント付き `SELECT` の `StatementType` と、コメント付き DDL の `UpdateCount` が省かれること
  - `tests/results.rs`: コメント付き `SELECT` の `OutputLocation` が `<id>.csv` に、コメント付き CTAS が `tables/<id>` になること
  - `tests/metadata.rs`: コメント付き `DESCRIBE`／`SHOW CREATE TABLE` の `.metadata` の先頭のクエリ ID（D6 の波及）
- **配線の予測**: 既存テストで先頭にコメントを置いているものは無い見込み（着手時に `grep -rn -- '-- \|/\*' tests/ src/` で確認する）。
  無ければ既存テストは全件無変化。読み飛ばす対象が 0 文字のとき `skip_leading_trivia` は恒等関数として振る舞う

### ミューテーション確認で壊す候補

1. `skip_leading_trivia` のループを 1 回だけの読み飛ばしに壊す → 連続コメント（`-- a\n-- b\nSELECT 1`）のテストが落ちる
2. `words()` への差し込み行を削る → `tests/dml.rs` の `StatementType` が落ちる
3. `ResultFile::of` への差し込み行を削る → `tests/results.rs` の `OutputLocation` が落ちる
4. `is_iceberg_table` に渡す引数を読み飛ばし後の変数に差し替える → D5 を守るユニットテストが落ちる
5. **`ResultFile::of` の `words` を元の `query` から作る**（D5 の裏）→ コメント付き CTAS のユニットテストが落ちる

### 人間検証リスト

1. issue 本文の 3 点（`-- comment\nSELECT 1`／`/* comment */ SELECT 1`／コメント付き CTAS の `StatementType` と `OutputLocation`）
2. コメントだけの文（`-- only`）を `StartQueryExecution` に渡したときの本物のエラー
3. コメントの後に空行・空白が続く形
4. `-- c\nDESCRIBE t` と `-- c\nSHOW CREATE TABLE t` の `.metadata` の先頭のクエリ ID（D6 の波及。
   `metadata_query_id` は `word(1)` まで見る唯一の分岐を持つ）

→ すべて実測スクリプトに入れてある（4 は `metadata_query_id` 列として形（trino / uuid）を出す）。

## 計画レビューの結果

Explore 2 本（契約・構造／再利用・削減）。4 ゲートを通した結果:

| # | severity | 指摘 | 判定 |
|---|---|---|---|
| 1 | high | `ResultFile::of` の `words` をどちらの文字列から作るか計画が明記していない。元の `query` から作ると `is_create_table_as` が語のずれで `false` を返し、**コメント付き CTAS だけが `.txt` のまま残る** | **採用**（D5 を書き換え）。前提検証: `results.rs:88-94` を読み、`words[1]` が `"TABLE"` でないと `false` を返すことを確認した |
| 2 | high | `metadata_query_id`（`operation.rs:356-358`）は `(word(0), word(1))` で `("SHOW","CREATE")` を見る唯一の分岐。先頭コメント付き `SHOW CREATE TABLE` が実測にもテストにも入っていない | **採用**。実測スクリプトに `a-line-show-create` / `b-block-show-create` と `metadata_query_id` の列（先頭 field 1 の形を trino / uuid で出す）を足した。テストは `tests/metadata.rs` に足す |
| 3 | medium | コメント付き DDL で `UpdateCount` の有無が変わるのにテスト計画に無い | **採用**。`tests/dml.rs` の 1 関数に含める（追加コストはほぼゼロ） |
| 4 | low（削減） | `tests/comments.rs`（新規）は要らない。既存の担当ファイルに 1 関数ずつ足すほうが小さい | **採用**（D9） |
| 5 | medium（削減） | `SubstatementType` の結合テストは実利がない。ユニットテストの表に 1 行足せば同じ確信度 | **採用**（D10） |
| 6 | low（削減） | `next_is_dot` を書き直す計画は本 issue のスコープ外。無関係な既存機能に挙動変化のリスクを持ち込む | **採用**（D8） |
| 7 | low（削減） | 前提表の「未保証: 先頭に文字列リテラルが来る文は無い」行と人間検証リストの項目 5 は、計画自身が「実測の対象外」と書いており確認しようがない | **採用**（両方を削り、理由は `skip_leading_trivia` の doc コメント 1 行に残す） |
| 8 | low（確認） | D2（`catalog.rs` に足す。新モジュールを作らない）は妥当。2 本が独立に同じ結論 | D2 を維持 |
| 9 | low（確認） | `-- only` は読み飛ばし後が空文字列になり、修正前後どちらも `Text` に落ちる。分類は変わらず、直後の構文エラーで実行が作られない | 計画の修正なし。`skip_leading_trivia` の doc に「空文字列になる経路がある」と 1 行書く |
| 10 | low（確認） | `write_result` / `write_failure` への波及（`Text` → `Csv`）は既存の実測済みルールに合流するだけで、新しい失敗経路は生まれない | 計画の修正なし |

**再レビューはしない**（構造の観点で計画を書き直してはいないため。採用したのは境界の明示・実測項目の追加・テストの置き場所と削減）。

## フェーズの記録

（7-0 以降で埋める）

## レビュー実施記録

（Step 8 で埋める）

## dismissed

（なし）

## deferred

| 指摘 | severity | file | 再トリアージ | 起票 |
|---|---|---|---|---|
| （なし） | | | | |

## 覆した事前判断

（なし）

## 実機検証

（Step 9 で埋める）

## 範囲外の発見

| 内容 | 出どころ | 起票 |
|---|---|---|
| `is_iceberg_table`（`src/results.rs:107-110`）は全文検索なので、コメントや文字列リテラルの中の `table_type='ICEBERG'` も数える。先頭コメントを飛ばしても直らない | Explore 1・README:445-450（既知の粗さとして記載済み） | D7 で範囲外と決めた。出荷前に起票する |
| `src/operation.rs` が 853 行。本 issue の差分は 10 行未満なので分割しない | 計画のファイル構成 | 出荷前に起票する |

## 委譲の記録

| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| Step 3 調査 | 委譲（Explore 3 本） | |
| Step 3 の結果の転記 | 自分 | 読み取り専用の委譲先の結果は統括役が転記する規則 |
| Step 3b 実測スクリプト | 委譲（general-purpose 1 本） | |
| Step 5 計画の下書き | 委譲（Plan 1 本） | |
| Step 3b スクリプトの受け入れ判定 | 自分 | 本番の認証情報で実行するものなので規則どおり委譲しない |
| Step 5 計画の転記と判断 | 自分 | 読み取り専用の委譲先の結果は統括役が転記する規則。D1・D2 は Plan の案を覆した |
| Step 6 計画レビュー | 委譲（Explore 2 本） | |
| Step 6 のトリアージと実測スクリプトの修正 | 自分 | トリアージは判断そのもの。スクリプトの修正は 1 ファイル・数分で、指示を書くほうが長い |

## 出荷

（Step 10 で埋める）

## 時系列

| 時刻 | 出来事 | SHA |
|---|---|---|
| 2026-09-18 08:19 | quick-implement で着手、ゲートの「事実」で外れる | |
| 2026-09-18 08:21 | `main-17` を作成し quick ノートをコミット、implement-issue へ引き継ぎ | `e09d40f` |
| 2026-09-18 08:25 | Explore 3 本を並列起動 | |
| 2026-09-18 08:30 | 実測スクリプト作成と Plan を並列起動 | |
| 2026-09-18 08:40 | 実測スクリプトを受け入れ、ユーザーに実測を依頼 | |
| 2026-09-18 08:45 | 計画レビュー 2 本を起動 | |
| 2026-09-18 08:58 | レビューを 4 ゲートでトリアージし、計画と実測スクリプトを修正 | |
