# quick #56 Trino が MERGE の updateType に "MERGE" を返すかを実機で確かめる
- 入力: issue #56。モード: standalone。ブランチ main-56、着手前 SHA 5897e75
- ゲート: サイズ 0 行・0 ファイル（本番コード。≤50・3。触ったのは `39-e2e/verify.sh` と README・CHANGELOG・TODO）／呼び出し元 0（≤5・同一モジュール）／契約: 無し・新規性: 無し・事実: 本物の Trino の挙動を測る issue だが足場（`39-e2e/`、Docker の Trino 482 + MinIO）が手元にあり `verify.sh` の 1 コマンドで再現できるので通過・判断: issue が結果ごとの進め方を決めている → 通過
- 時系列: 着手 19:00 ／ red 19:02（ケース 9 を足す前は verify.sh に MERGE の項目が無い）／ green 19:04（ケース 9 PASS。updateType=MERGE、count=2、field 3 より後ろが本物の Athena の `rows bigint` 列と同じバイト列。`.metadata` は 74 バイト）／ コミット 19:07
- 検証: `cargo fmt --check`・`cargo clippy --all-targets --locked -- -D warnings`・`cargo test --locked` ok（lib 173 passed、全体 287 passed）／ verify.sh 全 19 項目 PASS（Trino 482）／ミューテーション: ケース 9 の期待値を `MERGE INTO` に変えて `SKIP_BUILD=1` で再実行 → ケース 9 だけ FAIL（`updateType=MERGE(期待 MERGE INTO)`）
- 自己レビュー: 分岐: `read_metadata_head` の形が違う 3 分岐と `run_merge_case` の本体あり・`.metadata` 無し・不一致は、実機では踏めない（偽のバイト列で関数単体を確かめたのはタグと長さの正常系のみ。ミューテーションで不一致の経路は踏んだ）／エラーの出口: 無し（本番コード変更なし）／呼び出し元: 0
- 打ち切り・escalate: 提案なし
- 予算の実績: Bash 約 14 本、所要 7 分（verify.sh の 2 回の実行待ちを含む）、サブエージェント 0 本（本番コードを変えていないので止めない独立レビューは流さない）
- 出荷: standalone、ブランチ main-56
