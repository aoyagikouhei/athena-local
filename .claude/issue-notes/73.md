# quick #73 EXPLAIN の結果を本物はプランの行ごとに Rows に分けるが athena-local は Trino の 1 行を改行入りのまま返す
- 入力: issue #73。モード: standalone。ブランチ `main-73`、着手前 SHA 7aa96a3
- ゲート: サイズ 29 行・1 ファイル（≤50・3。`src/operation/execution.rs` に `split_explain_rows` を足して `run` の末尾で呼ぶ。ユニットテスト 2 本は別枠）／呼び出し元 1（≤5・同一モジュール。`run` だけ）／契約: `EXPLAIN` の `Rows` の行数と `.txt` の末尾の改行が変わるが、README が「`.txt` は `GetQueryResults` の行を `\n` で連結したもの」と実測済みで書いている挙動に本物を合わせる修正で、README・CHANGELOG を同じコミットで直す／新規性: 新ファイル・新パターン無し／事実: 本物の 4 ラウンド（`~/athena-txt-measurements/*/explain.results.json`・`explain.od.txt`）で `Rows` 15 行と `.txt` 393 バイトの両方が「全文 + `\n` を `\n` で分ける」で一致。issue が未確定としていた 2 つの規則（split の後に 1 行足す／末尾に `\n` を足してから split）は常に同じ結果になるので 1 種類の EXPLAIN で決まる。偽 Trino で再現／判断: 新しい既定値無し → 通過
- 時系列: 着手 06:00 ／ red 06:02 ／ green 06:03 ／ コミット 06:05
- 検証: `cargo fmt --check` ok／`cargo clippy --all-targets --locked -- -D warnings` ok／`cargo test --locked` ok（lib 181 passed、select 9 passed、results 15 passed、全バイナリ 0 failed）／ミューテーション: `split_explain_rows` の `.chain(std::iter::once(""))`（末尾の空行 1 つ）を消す → `explain_の結果には_select_と同じく列名行が入る`（Rows の行数）と `explain_の結果は_txt_の先頭に列名行を入れて書く`（`.txt` の末尾の `\n`）の両方が落ちた
- 自己レビュー: 分岐 3 件（EXPLAIN でない文 → ユニットテスト、EXPLAIN の文字列 → 結合 2 本 + ユニット、先頭の値が文字列でない → ユニットテスト）すべてテスト有り／エラーの出口: 新規なし／呼び出し元 1 箇所（`run` の末尾。`write_result` と `Store::finish` の両方に分けた行が渡るので GetQueryResults と `.txt` が揃う）
- 打ち切り・escalate: 提案なし。未実測（末尾が改行で終わらない `EXPLAIN (FORMAT JSON)` / `EXPLAIN (TYPE IO)` の行数、`EXPLAIN ANALYZE`）は README の Caveats に書いた
- 予算の実績: Bash 約 12 本、所要 約 5 分（待ち時間を除く）、サブエージェント 1 本（止めない独立レビュー）
- 出荷: standalone、ブランチ `main-73`
