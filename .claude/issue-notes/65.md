# quick #65 TABLE t の分類が ResultFile::of（.csv）と statement_type（UTILITY）で食い違い、GetQueryResults の列名行と結果ファイルの行数がずれる
- 入力: issue #65。モード: standalone。ブランチ `main-65`、着手前 SHA fe044d4
- ゲート: サイズ 4 行・2 ファイル（≤50・3。`statement_type` と `substatement_type` の腕に `TABLE` を足しただけ）／呼び出し元 5（≤5・同一モジュール `operation/`: `query_execution.rs` 3 箇所、`result_output.rs`、`table_format.rs`）／契約: 本物に合わせる修正で README・CHANGELOG を同じコミットで直す条件で通す／新規性: 新ファイル・新パターン無し／事実: **外れ**（本物が `TABLE t` を受け付けるか未実測。過去 15 ラウンドの生データにも記録なし）／判断: 実測で決まるので追加の判断なし → はしごで実測工程を足した
- 時系列: 着手 20:46 ／ 実測スクリプト依頼 20:49・受け入れ 21:00（コミット 7a10a19）／ 実測 21:02〜21:07（ラウンド 1 回）／ red 21:08 ／ green 21:08 ／ コミット 21:10
- 検証: `cargo fmt --check` ok／`cargo clippy --all-targets --locked -- -D warnings` ok／`cargo test --locked` ok（lib 174 passed、全バイナリ 0 failed）／ミューテーション: `statement_type` と `substatement_type` の腕から `"TABLE"` を外す → `statement_type_は先頭のキーワードで決まる` と `substatement_type_は実測した文の種類を返す` の両方が落ちた／実測: ラウンド 1 回・7 項目すべて測れた。本物は `TABLE t`・小文字・`(TABLE t)`・先頭コメント付き・`LIMIT 1` 付きのすべてを受け付け、StatementType `DML`／SubstatementType `SELECT`／`<id>.csv`／`.metadata` あり／`GetQueryResults` の 1 行目は列名行（本体の先頭行と一致）。存在しないテーブルは FAILED でも `DML`／`SELECT`／`.csv`。生データ: `~/athena-table-statement-measurements/run-20260922-210254`
- 自己レビュー: 分岐 3 件（`statement_type` の DML の腕、`substatement_type` の SELECT の腕、`ResultFile::of` の Csv の腕）すべてユニットテストで固定／エラーの出口: 新規なし／呼び出し元 5 箇所: `query_execution.rs:51`（列名行を残す。これが本題）、`:116`（StatementType が DML に）、`:177`（UpdateCount は非 DDL で 0 のまま不変）、`result_output.rs:57`（`to_text` は `.txt` の文だけで `TABLE` は `.csv` なので通らない）、`table_format.rs:44`（DROP／ALTER 以外は None で不変）
- 打ち切り・escalate: 提案あり（事実）→ 実測工程を足して続行。範囲外の発見: `SELECT 1` の `.csv`／`.metadata` と `SHOW TABLES` の `.metadata` の Content-Type が `binary/octet-stream` で、README と `ResultFile::content_type`（2026-09-17 実測の `application/octet-stream`）と食い違う → #70 に起票（原因未特定）
- 予算の実績: Bash 約 22 本（うち実測 6 本）、所要 約 12 分（着手からコミットまで 35 分のうち、実測スクリプトの作成待ち 11 分と実測待ち 6 分などの待ちを除く）、サブエージェント 2 本（実測スクリプト 1、止めない独立レビュー 1）
- 出荷: standalone、ブランチ `main-65`
