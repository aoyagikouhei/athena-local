# quick #75 src/results.rs（696 行）の mod tests を別ファイルに分けて 400 行以内にする
- 入力: issue #75。モード: standalone。ブランチ main-75、着手前 SHA 9add5d3
- ゲート: サイズ 約 425 行・1 ファイル移動 + 新ファイル 1（≤50・3）→ 外れ／呼び出し元 0（テストの移動なので本体の呼び出し元は変わらない）／契約 通過／新規性 `src/results/tests.rs` の新規作成で外れ／事実 挙動を変えない移動で red が成立せず外れ／判断 通過（issue が置き場所を `src/results/tests.rs など` と決めている）
- 時系列: 着手 06:15 ／ red なし ／ green なし ／ コミット 06:21（ノートのみ）
- 検証: コードに触っていない。移動前のテスト件数を実測: `cargo test --lib results::tests` → 19 passed（lib 全体 181 件）
- 自己レビュー: 該当なし（差分なし）
- 打ち切り・escalate: 提案あり（変更サイズ 約 425 行・新規性 新ファイル 1・事実 red なし）→ 選択: implement-issue で進める（移動・分割の経路）。stash なし
- 予算の実績: Bash 約 6 本、所要 6 分、サブエージェント 0 本
- 出荷: main-75（このノートのコミットのみ。実装は implement-issue が積む）

## 引き継ぎ

- 判定箇所: `src/results.rs:286-710` が `#[cfg(test)] mod tests`（425 行。本体は `:1-285`）。移動後の本体は 285 行程度で issue の 400 行以内に収まる
- テストモジュールの構成: `use serde_json::Value;` / `use super::*;` / `use crate::trino::Column;` の 3 本の import。テスト 19 件。モジュール直下のヘルパは `column`(`:293`)・`scalar`(`:301`)・`csv`(`:305`)・`text`(`:309`)・`全型を含む_outcome`(`:317`)・`location`(`:495`) の 6 つで、いずれもテスト専用（本体からは呼ばれない）
- 共有できる既存機構: 先行の分割は #30（`convert/`）と #44（`operation/`）で、どちらも `mod.rs` 形式のディレクトリに本体を分けた。テストは「対象関数が移った先に追従」（#30 の設計判断）。今回は本体を分けずテストだけ出すので、`src/results.rs` + `src/results/tests.rs`（`mod.rs` 無しの子モジュール）の形はリポジトリに先行例が無い。`#[path]` の利用も 0 件
- 消えても気づけないもの: `results.rs` の `///` は 55 行、「実測」「採取」を含む行は 28 行（うち `mod tests` 内 14 行）。移動前後で件数を突き合わせる
- パス参照: `src/catalog.rs:14` のモジュール doc が `results.rs` を名指しする。指すのは本体（残留側）なので、`results.rs` を残す構成なら書き換え不要
- 原因の仮説: 該当なし（欠陥ではなく分割）
- 未実測・未確認: 子モジュールからの `use super::*;` で本体の private 項目（`ResultFile::of` など）がそのまま見えるか（Rust の規則上は見えるはず。実装で確かめる）。移動先の名前（`tests.rs`）と `mod tests;` の宣言に `#[cfg(test)]` を残す形
- 再現手順: `wc -l src/results.rs`（710）、`cargo test --lib results::tests`（19 passed）
- stash 名: なし
