# issue #75 src/results.rs（696 行）の mod tests を別ファイルに分けて 400 行以内にする

## 基本情報

- issue: #75。ベースブランチ: main。ブランチ: main-75。着手前 SHA: 9add5d3（quick ノートのコミット a1b6919 も src は同じ）
- 経路: implement-issue の「挙動を変えない移動・分割」（`move-refactor.md`）。小規模 issue の経路（2 フェーズ）
- quick-implement から escalate（ゲートで変更サイズ・新規性・事実が外れた。quick ノートは末尾の「quick の記録」）

## 事前に確認した事実

移動後には取れなくなる基準値（着手前に実測。検証足場 `75-verify/check.sh` の期待値の正）:

| 項目 | 値 |
|---|---|
| `cargo test --locked` の総数 | 303（lib 181・結合テスト 122・main 0・doc 0） |
| `results::tests` の件数 | 19（`cargo test --locked --lib results::tests`） |
| `src/` 全体の `///` 行数 | 502 |
| `src/` 全体の「実測」「採取」を含む行 | 156（`results.rs` は 28。うち mod tests 内 14・本体 14。「採取」は 0 件） |
| `results.rs` 本体（`mod tests {` より前）の分岐（if / match / return / `?`） | 17（if 5・match 5・return 3・? 4） |
| `results.rs` の行数 | 710（本体 1–285、`#[cfg(test)]` 286、`mod tests {` 287、閉じ `}` 710） |

移動単位と依存（Explore 1 の返答から転記。落とした項目: 本体側の実測記録 14 行の行番号一覧＝check.sh の項目 4 が全件突き合わせる）:

- 移動単位は `src/results.rs:286-710`（`#[cfg(test)]` から閉じ括弧まで）。直前は `impl ResultWriter` の閉じ `}`（284）と空行（285）だけで、テストモジュールに付く doc・コメントは無い。`mod tests` 内に `//!` は無い。`results.rs` の `#[cfg(test)]` はこの 1 件だけ
- テストモジュール直下の `use` は 3 行: `use serde_json::Value;`（288。約 11 箇所で使う）、`use super::*;`（290）、`use crate::trino::Column;`（291。ヘルパ `column` が使う）。`serde_json::from_str` はフルパスで 2 箇所
- `super::*` 経由で使う本体の項目はすべて `pub`: `ResultFile`（20）と `of`（46）、`ResultLocation`（102）と `new` / `metadata` / `failed` / `uri`、`is_valid_output_location`（157）、`to_csv`（181）、`to_text`（205）。private の `ResultFile::path`・`split_output_location`、`pub(crate)` の `is_create_table_as`、`SIGNATURE_EXPIRY`、`ResultWriter` は直接参照しない
- 親の `use` 由来で `super::*` 越しに使う名前は `Outcome`（`results.rs:13` の private な `use crate::trino::Outcome;`。18 箇所）だけ。edition 2018 以降、`use super::*` は親の private `use` で入った名前も子に持ち込むので、子モジュールを別ファイルにしても位置（`crate::results::tests`）は同じで届く。**可視性の変更は 0 件、`use` の追加も 0 件で済む**
- テスト 19 件と、`#[test]` の無いヘルパ 6 つ（`column` 293・`scalar` 301・`csv` 305・`text` 309・`全型を含む_outcome` 317（`///` 313–316）・`location` 495）。`location` はテスト 9 と 10 の間にあり、順序を保って運ぶ
- mod tests 内の `///` は 9 行（313–316・518–520・555–556）、単独行の `//` は 29 行。本体側の `///` は 46 行
- `src/lib.rs:14` の `mod results;`（private）。`lib.rs:26` に `use crate::results::ResultWriter;`
- 本体先頭の `//!` は 2 行（「結果 CSV を S3 互換ストレージ（MinIO など）の OutputLocation に書く。」「置き場所・ファイル名・CSV の書式は 2026-09-14 に本番 Athena で実測したもの。」）

慣習・先行例・文書の参照（Explore 2 の返答から転記。落とした項目: `///` と実測記録のファイル別内訳＝check.sh の項目 3・4 が総数で見る、結合テストの内訳＝項目 1 が総数で見る）:

- ディレクトリモジュールは `mod.rs` 形式だけ（`src/convert/mod.rs`、`src/operation/mod.rs`。中身は `//!`・`mod` 宣言・`pub(crate) use` の再エクスポートだけ）。`foo.rs` + `foo/` の併存は 0 件、`#[path]` は 0 件、`#[cfg(test)] mod tests;`（別ファイルのテスト）も 0 件で `mod tests` 17 件はすべてインライン。**今回の形はリポジトリで初めて**。ただし `4.md:123` が「`store.rs` は 600 行を超えたら `mod tests;` + `src/store/tests.rs` に出す」と同じ形を予定として書いている
- 結合テストに `tests/results.rs`（634 行）があり、`src/results/tests.rs` と字面が近い（呼び方に注意。Rust の慣習名なので変えない）
- #30 の決定: 400 行の基準は「全体基準（本体＋テスト）」をユーザーが選択。テストは対象関数が移った先に追従。可視性は最小。`use` は移した先で実際に使うものだけ。新ファイルの `//!` に「実測」「採取」の語を入れない。分割本体と文書の追随を 2 フェーズ・2 コミットに分ける
- #44 の決定: ネストしない。`git diff -M` に頼らず正規化 diff。CLAUDE.md は追随させ、README・CHANGELOGS は変えない。移動後に誤りになるパス参照は完了条件の外でも直す
- 文書・コメントのパス参照: `issue-notes` を除くと `results.rs|src/results` のヒットは `src/catalog.rs:14`（モジュール doc）の 1 行だけで、本体（残留側）の `ResultFile::of` を指す → 書き換え不要。CLAUDE.md の `results::to_csv`（:43）・`results::ResultFile::of`（:44）・`ResultLocation::failed`（:33）も本体。README・TODO・CHANGELOGS にソースファイル名の言及は無い
- lint: `missing_docs` 相当は無効（`[lints]` 無し、`#![...]` は `tests/common/mod.rs:5` だけ、`clippy.toml`・`rustfmt.toml` 無し）。CI は `fmt --check`／`clippy --all-targets --locked -D warnings`／`test --locked`
- 呼び出し元: `grep -rn 'results::' src tests` は 13 件、すべて `crate::results::<本体の項目>`。`results::tests` の参照は無い → テストだけを子モジュールに出す形なら呼び出し元は 1 行も変わらない

## 設計判断

| # | 論点 | 決定 | 理由 |
|---|---|---|---|
| 1 | 置き場所の形 | `src/results.rs`（本体そのまま）+ `src/results/tests.rs`（`mod.rs` 無しの子モジュール）。`results.rs` 末尾に `#[cfg(test)]\nmod tests;` | `results/mod.rs` にすると本体 284 行が `mod.rs` に入り「`mod.rs` は `mod` 宣言と再エクスポートだけ」（CLAUDE.md:29,:43）を破る。`catalog.rs:14` の `results.rs` 参照も誤りになる。issue 本文と `4.md:123` の予定どおり。リポジトリで初めての形（先行例 0 件）なので PR 本文に書く |
| 2 | 名前 | `tests.rs`（Rust の慣習名。`mod tests;` とモジュールパス `results::tests` に固定） | 結合テスト `tests/results.rs` と字面が近いが、`//!` で打ち消す |
| 3 | 可視性・`use` | 変更 0 件。`tests.rs` 先頭の `use` は元の 3 行そのまま | テストが使う本体の項目はすべて `pub`。`Outcome` は親の private `use`（`results.rs:13`）が `use super::*` で届く（edition 2018 以降。モジュールの位置が変わらない） |
| 4 | 字下げ | 一律に 4 桁外す。raw 文字列 2 つ（`row` の JSON 330–332、`rows` の JSON 356–358）の継続行も外す | 文字列の中身は JSON のトークン間の空白で、`serde_json::from_str` の結果は変わらない。継続行だけ元の字下げで残すと見た目が崩れる。check.sh の項目 5 は先頭空白を除いて比べるので影響なし。**純粋な移動からの唯一の逸脱として PR 本文に書く** |
| 5 | `tests.rs` の `//!` | `//! results.rs のユニットテスト（結合テストは tests/results.rs）。` の 1 行 | 「実測」「採取」の語を入れない（#30） |
| 6 | 400 行の基準と `tests.rs`（424 行） | **ユーザーが Step 6b で (a) を選択（06:33）**: テストだけのファイルには当てない。`tests.rs` は 1 ファイルのまま | #30 は「全体基準（本体＋テスト）で 400 行以内」を選んだが、それは本体を含むファイルの話。(b) `tests.rs` を 2 つに割ると `results/` 直下に 2 ファイル＋`mod` 2 つの新しい構造が増え、足場の作り直しが要る |
| 7 | フェーズ 2（文書の追随） | CLAUDE.md の「テストの足場」節に 1 行足す（`src/results.rs` のユニットテストだけ `src/results/tests.rs` に出している）。README・CHANGELOG は変えない | 参照の書き換えは 0 件（Plan の結論）。ただし唯一の構造なので、次の agent が CLAUDE.md で知れるようにする（#44 D6「CLAUDE.md は追随させる」）。Plan は「足さない」を勧めたが、docs の判断なので覆す |
| 8 | 実機検証 | **該当なし**（テストの移動のみで、release バイナリは移動の前後で変わらない） | 動くのは `#[cfg(test)]` の中身だけで、バイナリの起動はこの変更について何も確かめない（計画レビュー 2 を採用）。本体の経路は check.sh の項目 1（結合テスト `tests/results.rs` 15 件を含む 303 件）と項目 6 が覆う |

## 計画

**フェーズ 1: 移動そのもの（1 コミット）**

- ファイル構成: `src/results.rs` 287 行（元の 1–285 + `#[cfg(test)]` + `mod tests;`）／`src/results/tests.rs` 約 424 行（`//!` + 空行 + 元の 288–709 を 4 桁字下げ解除）
- 移動単位: 285 空行は残す／286–287（`#[cfg(test)]`・`mod tests {`）は `results.rs` 末尾の宣言に置き換える／288–291 `use` 3 行は `tests.rs` 先頭／292–709 ヘルパ 6 個とテスト 19 件を同じ順序で（`location` は 495、テスト 9 と 10 の間）／710 `}` は捨てる（check.sh 項目 5 の許容 `^< \}$`）
- 手順: check.sh で移動前の全 ok を再確認 → `sed -n 288,709p` で取り出しつつ `sed -i 286,710d` で元から削る（コピーして後で消さない）→ `results.rs` 末尾に宣言 → `tests.rs` の字下げを外し `//!` を足す → `cargo build --all-targets --locked`・clippy・`fmt --check` → check.sh 全 ok・項目 5 の内訳が `< }` の 1 行だけであることを目視 → ミューテーション 3 件 → コミット
- 前提表:

| 依拠する前提 | 破らないか | 保証元 |
|---|---|---|
| `use super::*` が親の private `use`（`Outcome`）を持ち込む | モジュールの位置が同じなので破らない | Rust の名前解決。`results.rs:13`、移動前に 19 件 pass |
| `results.rs` の `mod tests;` が `results/tests.rs` を読む（`mod.rs` 無し） | 新たに使う | edition 2024（`Cargo.toml`）。先行例 0 件なので最初のビルドで確かめる（U1） |
| テストは `pub` の API だけを通る | 変えない | `results.rs:20,46,102,115–151,157,181,205` |
| 呼び出し元は `results::tests` を参照しない | 変えない | `grep 'results::' src tests` 13 件すべて本体 |
| check.sh 項目 6 の awk は `^mod tests \{` で止まる | 移動後はこの行が無くファイル全体を数えるが、テストが無いので 17 のまま | check.sh |
| テスト件数は `cfg(test)` の読み込みに依存し、宣言漏れは green のまま件数が減る | check.sh 項目 2 が拾う | check.sh |

- ミューテーション候補: (1) `results.rs` の `#[cfg(test)] mod tests;` を消す → 総数 284、項目 1・2 が NG／(2) `tests.rs` の `#[test]` を 1 つ消す（`ファイル名は文の種類で変わる`）→ 18 件で項目 1・2 が NG（`dead_code` で clippy も NG）／(3) 本体 `ResultFile::of` の `"INSERT" => Self::Manifest` を `Self::Csv` に → `tests.rs` の `文の先頭で置くファイルの種類を決める`・`insert_は…` と結合テストが落ちる（移動先のテストが本体を見ている証拠）
- 未検証の前提: U1 `mod.rs` 無しの子モジュールがこのツールチェーンで解決される（最初のビルドで確認）／U2・U3 **解消**: 字下げを 4 桁外して `//!` を足した下書き（424 行）を `rustfmt --check --edition 2024` に通し、差分 0 を確認（06:31。折り返しも `use` の並びも変わらない）／U4 raw 文字列の JSON 空白を減らしてもパース結果は同じ（テストが pass することで確かめる）
- 該当なしとして一括で省く: 直列化境界・正規化境界・境界の名前・意味論マトリクス・人間検証リスト・追加するテスト・一致台帳（ヘルパの複製なし）

**フェーズ 2: 文書の追随（1 コミット）**

- CLAUDE.md「テストの足場」節に 1 行: `src/results.rs` のユニットテストだけ `src/results/tests.rs` に出している（本体が 400 行を超えたため。#75）。他のモジュールはインラインの `mod tests`
- 書き換え後に `results.rs` を grep して残骸 0 件を確認（`catalog.rs:14` は残留側を指すので書き換えない）

**先行できる作業**: 検証の足場（済み）。実機検証の最小ケース（設計判断 8）はフェーズ 1 の後に統括役が流す。

**レビューに渡す材料**: base 9add5d3、検証コマンドは CLAUDE.md の 3 本と `.claude/issue-notes/75-verify/check.sh`

落とした番号と理由: Plan の 11「先行できる作業」のうち「基準を控える」は check.sh 項目 5 が BASE から取るので不要。
## 計画レビューの結果

Explore 1 本（opus、06:29–06:33）。現実に起こる失敗は 0 件。high 0 件なので design-checklist への追記は無し。

| # | severity | 指摘 | 採否 |
|---|---|---|---|
| 1 | low | U2・U3 は解消済み（レビュアーも独立に rustfmt で差分 0 を確認。288–709 に字下げ 4 桁未満の非空行・行末空白は無い）。fmt が差分を出したときの分岐を手順から外す | 採用（手順を修正） |
| 2 | low | 実機検証（release ビルドとバイナリ起動）は動かない `#[cfg(test)]` の中身しか変えないので何も確かめない。「該当なし」にする | 採用（設計判断 8 を修正） |
| 3 | low | `tests.rs` 424 行の 400 行基準は聞かずに (a) で決め打ちにできる（issue の対象は `results.rs`。check.sh も 1 ファイル前提） | **不採用**: #30 の「全体基準」はユーザーの決定で、覆すかどうかはユーザーの判断（Step 7「事前確定の判断を覆すとき」）。聞くのは 1 問だけなので Step 6b で聞く。推奨は (a) |

観点ごとの確認（指摘なし）: 境界 286–291／709–710 に doc・属性の取りこぼし無し。項目 5 の `#[cfg(test)]` は前後 1 回ずつで相殺、`mod tests {`／`mod tests;` は両方 strip、許容は `< }` だけで過不足なし。和集合の穴は項目 2（`#[test]` の所属）と項目 8（cfg(test) 外に残ったヘルパは `dead_code` で clippy が落ちる）が塞ぐ。項目 6 は移動後もファイル全体で 17。ミューテーション (2) は `[lints]` が無いので `cargo test` は警告で落ちず 18 件を検出できる。(3) の対象 `src/results.rs:60` は実在。

## フェーズの記録

**フェーズ 1: 移動（06:33–06:37、自分で実装）**

- 7-0: 消す・変えるガードや条件: 無し／変える関数・契約の呼び出し元: 無し（`results::tests` の参照 0 件）／増える分岐: 0 件／境界入力: 該当なし／design-checklist: 移す単位は `#[cfg(test)]` 属性から閉じ括弧まで（行番号ではなく塊。doc の取りこぼしは check.sh 項目 3・4 が見張る）、`use` は元の 3 行をそのまま（すべて使用あり）、期待値は内訳から数え直し済み（19 = 0 + 19）
- red: 成立しない（移動）。代わりに着手前に check.sh 全 ok を再確認
- 手順: `sed -n 288,709p | sed 's/^    //'` で `tests.rs` へ書きつつ `sed -i 286,710d` で元から削り、末尾に `#[cfg(test)]\nmod tests;` を追記。`cargo build --all-targets --locked`・clippy・`fmt --check` すべて通過（U1 解消: `mod.rs` 無しの子モジュールは解決された）
- check.sh: 全 8 項目 ok。項目 5 の差分は `< }` の 1 行だけ。`results.rs` 287 行・`tests.rs` 424 行
- ミューテーション（本体はスクラッチパッドにコピーして退避し、コピーで戻した。`git status` で差分が移動分だけであることを確認）:

| 壊し方 | 結果 |
|---|---|
| (1) `results.rs` の `#[cfg(test)] mod tests;` を消す | 項目 1 合計 284・項目 2 `results::tests` 0 件と宣言無しで NG（項目 5・fmt も NG） |
| (2) `tests.rs` の `ファイル名は文の種類で変わる` の `#[test]` を消す | 項目 1 合計 302・項目 2 18 件で NG（clippy も `dead_code` で NG） |
| (3) 本体 `results.rs:60` の `"INSERT" => Self::Manifest` を `Self::Csv` に | `results::tests` 5 件 FAILED（`文の先頭で置くファイルの種類を決める`・`insert_は…` ほか）。移動先のテストが本体を見ている。計画が「落ちる」と予測した結合テスト `tests/dml.rs` の 9 件は落ちなかった（予測の取り違え。ユニット 5 件で目的は足りる） |

- 計画から外れた点: 無し

**フェーズ 2: 文書の追随（06:38、自分）**

- CLAUDE.md「テストの足場」節に 1 行追加。`results.rs` の参照を grep し直し、残骸 0 件（`catalog.rs:14` は残留側を指すので書き換えない）
- 7-7: 分割不要（新規ファイルは `tests.rs` 424 行のみ。ユーザーの判断で対象外）

## レビュー実施記録

## dismissed

## deferred

## 覆した事前判断

なし（今のところ）

## 実機検証

## 範囲外の発見

## 委譲の記録

| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| Step 3 調査 | Explore 2 本（opus） | |
| Step 3 Plan | Plan 1 本（opus） | |
| Step 6 計画レビュー | Explore 1 本（opus。小規模 issue の経路） | |
| フェーズ 1 実装（7-1〜7-4） | 自分（小規模経路・本番 0 行、テストの移動のみ） | 移動は sed 3 本で済み、委譲の固定費の方が長い |
| フェーズ 2 文書 | 自分 | 1 行の追記 |
| 検証の足場 `75-verify/check.sh` | 自分 | #44 の check.sh を 1 ファイル差し替えるだけで、指示を書く方が長い（期待値は着手前に実測） |

## 出荷

## 時系列

- 06:15 quick-implement で着手。ゲート判定で外れ、implement-issue を選択
- 06:20 quick ノートをコミット（a1b6919）、issue にコメント
- 06:21 Explore 2 本を起動。先行作業: 検証の足場 `75-verify/check.sh` を作成し、移動前で全 ok・わざと壊して NG を確認（06:23）
- 06:25 Explore 2 本が返り、Plan を起動
- 06:29 Plan が返り、設計判断と計画をノートに転記。計画レビュー（Explore 1 本）を起動
- 06:31 先行作業: 字下げを外した `tests.rs` の下書きを rustfmt に通して U2・U3 を解消
- 06:33 計画レビューが返る（失敗 0 件・削減 3 件、2 件採用）。計画確定をコミット（193a2d8）し Step 6b へ
- 06:33 Step 6b: ユーザーが 400 行の基準を `tests.rs` に当てないと回答。フェーズ 1 開始
- 06:37 フェーズ 1: 移動・check.sh 全 ok・ミューテーション 3 件 OK。コミット 945b5fc
- 06:38 フェーズ 2: CLAUDE.md に 1 行

## quick の記録

- 入力: issue #75。モード: standalone。ブランチ main-75、着手前 SHA 9add5d3
- ゲート: サイズ 約 425 行・1 ファイル移動 + 新ファイル 1（≤50・3）→ 外れ／呼び出し元 0／契約 通過／新規性 `src/results/tests.rs` の新規作成で外れ／事実 挙動を変えない移動で red が成立せず外れ／判断 通過
- 時系列: 着手 06:15 ／ red なし ／ green なし ／ コミット 06:20（ノートのみ）
- 検証: コードに触っていない。移動前のテスト件数を実測: `cargo test --lib results::tests` → 19 passed
- 自己レビュー: 該当なし（差分なし）
- 打ち切り・escalate: 提案あり（変更サイズ・新規性・事実）→ 選択: implement-issue で進める。stash なし
- 予算の実績: Bash 約 6 本、所要 6 分、サブエージェント 0 本
- 出荷: main-75（ノートのコミットのみ。実装は implement-issue が積む）
