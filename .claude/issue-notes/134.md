# quick #134 jdbc-show-metadata.sh の SHOW 系の突き合わせが古い期待（列名行 +1）のまま FAIL する
- 入力: issue #134。モード: standalone。ブランチ main-134、着手前 SHA 9c28a8b（main 3a641e6 から）
- ゲート: サイズ 5 行・1 ファイル（実測スクリプト。本番コード 0）／呼び出し元 1（compare_row_counts）／契約・新規性・判断（期待値は #60（7244a30）で本物に合わせた athena-local の挙動と、docs/dev/measurements/clients.md の #57 の備考「+1 行は覆った」で裏取り）・事実（#128 の基準値で再現済み）→ 通過
- 時系列: 着手 03:39 ／ red（#128 の基準値と #129 の S6、FAIL 3）／ green 03:39〜03:41 ／ コミット 03:41
- 検証: `tools/dev.sh SKIP_BUILD=1 bash tools/measure/jdbc-show-metadata.sh` → PASS 17 SKIP 1 INFO 2 FAIL 0 rc 0（「行数の突き合わせ」7 ケースすべて一致）。標準コマンド（`tools/dev.sh` の cargo fmt --check / clippy --all-targets --locked -D warnings / test --locked）ok、329 passed／ミューテーション: SHOW だけ +1 の期待（元の case 文）→ 突き合わせ SHOW_TABLES(対照)・SHOW_SCHEMAS・SHOW_COLUMNS の FAIL 3（#128 の基準値と #129 の S6 の 2 回）
- 自己レビュー: 分岐 1 件を消した（SHOW_* の case。消した後は全ケース同じ期待）／エラーの出口: 無し／呼び出し元 1 箇所
- 打ち切り・escalate: 提案なし
- 予算の実績: Bash 約 5 本、所要約 3 分（待ち時間を除く）、サブエージェント 0 本（実測スクリプトだけなので止めない独立レビューは流さない）
- 出荷: main-134
