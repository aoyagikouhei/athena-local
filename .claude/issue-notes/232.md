# #232 S3 Tables の Context で別カタログを指す名前の CTAS を本物に合わせる

## 基本情報
- issue #232 ／ ベース main ／ ブランチ main-232 ／ 着手前 SHA 97cf8cb（レビューの base）
- quick-implement から escalate（外れた観点: サイズ 約 65 行・4 ファイル、判断: i12 の扱い、新規性: 無引用の名前への別名置換）
- 経路: 小規模 issue の経路（1 フェーズ）。Step 3 は Explore 1 本、Plan は省略（#237 と同じファイル・同じ経路で事実を継承）

## quick の記録
- 入力: issue 232。モード: standalone。ブランチ main-232、着手前 SHA 97cf8cb
- ゲート: サイズ 約 65 行・4 ファイル（≤50・3。見込み）／呼び出し元 create_table_catalog::check 1・three_part_name 1（同一モジュール）／契約: Query は受け取ったまま → 通過・事実: i12・j13 は実測済み・判断: i12 の扱いを issue が決めていない／新規性: 無引用の名前への別名置換は decisions.md の判断を覆す → サイズ・判断・新規性で外れた
- 時系列: 着手 18:23 ／ 質問 18:24 ／ escalate 18:25（red・green なし）
- 打ち切り・escalate: 提案あり → implement-issue を選択（ユーザー）。i12 はユーザーの判断で「1 部目を差し替える」
- 予算の実績: Bash 約 12 本、所要 2 分、サブエージェント 0 本

## 事前に確認した事実（Explore 1 本の返答から転記。落とした番号: 6 の後半・9 の一部は計画の docs 表に統合、10 の unquoted_ddl.rs の順序コメントは CTAS に無関係なので落とした）
1. CTAS は開始時の判定をどれも通らない: `quoted_names::rejection` は無引用 3 部で None、`unquoted_ddl::create_table::rejection` は `substatement_type != CREATE_TABLE` で None（`create_table.rs:35`）、`two_part_failure` も内部で `rejection` を呼ぶので None。`drop_catalog`（`reported_query.rs:16-22` に CREATE 無し）・`entity_check`（`Check::Continue`）も素通り。`statement == query`・`reported == None` で Store に入る（`execution.rs:146-197`）
2. `table_name`（`create_table.rs:110-116`）は名前の後ろを見ないので CTAS の名前も読める。`three_part_name` はそれを使う。`table_name_start` は `IF` の後に `EXISTS` が無ければ None（`target_table.rs:78-85`）。CTAS の判定は `classification::substatement_type` → `results::is_create_table_as`（`results.rs:97-110`。WITH 句・列の別名つきも CTAS）
3. `catalog::replacement(identifier, trino)`（`catalog.rs:65-73`、private）は `"<trino>"` に空白を足して桁を揃える。Trino 名が長ければ揃わない（テスト `別名の方が長ければ空白で埋めずに置き換える`）。`config.trino_catalog` は大文字小文字を区別、`create_table_catalog::trino_catalog`（private）は区別しない
4. 書き換えた `statement` は `Execution.query` に入り、`StatementType`／`SubstatementType`・結果ファイル・content_type はそれを読む（`query_execution.rs:163-172`）。`Query` の表示だけ `reported` を使う。`format_probe` は CTAS を対象にしない（`target_table.rs:19-30`）。`run()` の `alias_qualified_names` は引用符付きの別名だけなので、無引用の Trino 名に差し替えた後は素通り
5. `ImmediateFailure { writes_result_file: false }` なら本体も `.metadata` も置かない（`execution.rs:376-382`）。CTAS の `ResultLocation::failed()` は None なので、`true` にしても何も書かれない。`result_location` は開始の早い段階で `Option<ResultLocation>`、`uri()` で `s3://<bucket>/<prefix>tables/<id>`（CTAS）。結果を書かないモードで OutputLocation も無ければ None
6. 「manually clean the data」を組み立てるコードは無い。本物は j13 で `.metadata` だけ 81 バイト置いた（成功した CTAS の `.metadata` と同じ大きさ。中身は未実測）
7. テストは `tests/create_table_catalog.rs` の `s3_tables_request`・`schema_probe_sql`・`trino_error`・`harness.trino_sqls()`・`harness.s3_puts()`・`.catalog_map(...)`・`.results_s3()` を使う
8. 既定の Context の CTAS は `tests/reported_query.rs:181-208`（`CREATE TABLE awsdatacatalog.db.t3 AS SELECT 1 AS n`、別名 `AwsDataCatalog=hive`）で受け取ったまま送ることを固定済み。変えない
9. `execution.rs` は 479 行（400 行超）。開始時の判定の分割は #245 で起票済み（割り方に設計判断が要る）ので、本 issue では呼び出しの 1 ブロックだけ足す

## 設計判断
- D1（ユーザー、18:24）: i12 は 1 部目を差し替える。S3 Tables の Context の CTAS で、無引用の 1 部目が大文字小文字によらず `awsdatacatalog` なら、`TRINO_CATALOG_MAP` の `AwsDataCatalog` の Trino 名（キーは大文字小文字によらず当てる）に `catalog::replacement` で差し替える。decisions.md の「無引用の名前は置換しない」を S3 Tables の CTAS に限って覆す
- D2: j13 は、2 部目の DB が AwsDataCatalog 側の Trino カタログに無い（`SCHEMA_NOT_FOUND`）と確かめられたら Trino に送らず FAILED（2/1301、`Database <書いたとおり> not found. Please check your query. You may need to manually clean the data at location '<uri>' before retrying. Athena will not delete data in your account.`）。確かめられなければ差し替えて送る（#237 と同じ向き）
- D3: 本物が j13 で置いた `.metadata`（81 バイト）は中身が未実測なので置かない（推測で埋めない）。caveats と unmeasured に書く
- D4: 結果の場所が無い（結果を書かないモードで OutputLocation も無い）ときは文言の `<uri>` を作れないので FAILED にせず、差し替えて送る（Trino の SCHEMA_NOT_FOUND で FAILED になる）。場所を推測で埋めない
- D5: `IF NOT EXISTS` の CTAS は未実測なので対象にせず、今までどおり受け取ったまま送る（decisions.md の「未実測の形は今までどおり」）。未実測の出口は #251 に追記
- D6: AwsDataCatalog の別名が無い（Trino 名が `awsdatacatalog` のまま）なら差し替えない（Trino は無引用の名前を小文字で引くので同じ）。DB の確認は `awsdatacatalog` で行う
- D7: 既定の Context は測っていないので変えない（`tests/reported_query.rs` の固定のまま）

## 計画（フェーズ 1 のみ。本番 約 80 行）
| ファイル | 責務 | 見込み |
|---|---|---|
| `src/operation/create_table_catalog.rs` | `pub(super) async fn ctas(trino, config, query, location: Option<&str>) -> Outcome` を足す（S3 Tables の判定は呼び出し側）。CTAS（`substatement_type == CREATE_TABLE_AS_SELECT`）・`IF NOT EXISTS` でない（`target_table::if_follows(query, "CREATE")` が偽）・`three_part_name` が Some・1 部目が `awsdatacatalog`（大小無視）のときだけ判定。Trino 名 = `trino_catalog(config, "AwsDataCatalog")`。`schema_missing(schema_probe_sql(trino 小文字, db 小文字))` が真かつ location が Some → `FailAtRuntime(Failure::database_not_found(db, location))`。それ以外で Trino 名が `awsdatacatalog`（大小無視）でなければ `Rewrite(1 部目の範囲を replacement で差し替え)`、同じなら `Continue` | 130 → 約 185 |
| `src/failure.rs` | `pub fn database_not_found(database: &str, location: &str) -> Self`（USER・1301・false） | +15 |
| `src/catalog.rs` | `replacement` を `pub(crate)` に | 1 |
| `src/operation/execution.rs` | 既存の if / else if の後に `if s3_tables` で `ctas` を呼び、`FailAtRuntime` → `immediate_failure`（writes_result_file: false）、`Rewrite` → `reported`（受け取ったまま）と `statement`、それ以外は何もしない | 479 → 約 497（#245） |

- 1 部目の範囲: `three_part_name` が返す範囲の先頭から 1 部目の長さ（`catalog.len()`）まで。`.`・空白・コメントは残す
- 前提表: (a) CTAS は `message` が常に None で既存の分岐を通らない → 保証元 `create_table.rs:35`・`quoted_names`（無引用 3 部）。(b) `execution.query` は差し替え後も `CREATE`・`TABLE`・`AS SELECT` の語を保つので分類は変わらない → `replacement` は識別子 1 つを `"x"` と空白に置くだけ。(c) `alias_qualified_names` は差し替えた `"hive"` に当たらない（別名のキーでなければ）→ 別名のキーと Trino 名が同じ文字列なら当たるが同じ値への置換で無害。(d) 構文チェックは受け取った文で済んでいる（`execution.rs:98`）
- テスト（`tests/create_table_catalog.rs` に足す）:
  - T1 DB があれば 1 部目を Trino 名に差し替えて送る: 別名 `AwsDataCatalog=hive`（キーの大小違い `awsdatacatalog=hive` も 1 ケース）、`awsdatacatalog.db.t AS SELECT 1 AS n`・`AwsDataCatalog.Db.t AS SELECT 1 AS n`・`AWSDATACATALOG /* c */ . db.t WITH (format = 'PARQUET') AS SELECT 1 AS n`。送った文・Query は受け取ったまま・SUCCEEDED・`CREATE_TABLE_AS_SELECT`・probe は `hive`.`db`
  - T2 DB が無ければ Trino に送らず FAILED: 文言（`Database Missing not found. ... 's3://results-bucket/athena/tables/<id>' ...`）・AthenaError 2/1301/false・本体を送らない・S3 に何も置かない
  - T3 確かめられない（GENERIC_INTERNAL_ERROR）→ 差し替えて送る。結果の場所が無い（OutputLocation 無し・結果を書かないモード）で DB が無い → 差し替えて送る
  - T4 送る文が受け取ったままのもの: 別名なし（probe は `awsdatacatalog`）、1 部目が他のカタログ（`iceberg.ns.t AS SELECT`。probe しない）、`IF NOT EXISTS`（probe しない）、2 部の CTAS（probe しない）
  - 既存: 既定の Context の CTAS（`tests/reported_query.rs`）、#237 の CREATE TABLE のテストは変わらない
- 壊す候補: (1) `ctas` の CTAS 判定を外す（CREATE TABLE にも当たり #237 のテストが落ちる想定）(2) `database_not_found` の location を別の値に (3) Rewrite の腕で reported を立てない（Query が変わる）
- docs: CLAUDE.md の書き換え条件の一覧、`docs/configuration.md` の not rewritten、`docs/caveats.md`（別名の節の例外・Failed queries の `.metadata`・CTAS の説明）、`docs/dev/decisions.md`（74 行の判断の変更と #232 の決定）、`docs/dev/architecture.md`（create_table_catalog・catalog の節）、`docs/dev/unmeasured.md`、`docs/dev/measurements/statements.md` の #227 の節の採用した判断の行（CTAS は #232 → 実装した）、`CHANGELOGS.md`
- 先行できる作業: docs の下書き（計画攻撃の待ちの間）、未実測の出口（#251 への追記）
- 実機検証（Step 9）: compose の trino（iceberg・hive のカタログ）で S3 Tables の Context の CTAS を流せるか確認し、流せれば T1・T2 相当を 1 回ずつ。人間検証リスト: 無し見込み

## 計画レビューの結果
- 計画攻撃 Explore 1 本（sonnet、約 15 分・Bash 21 本）。指摘 2 件・採用 0 件
  - [medium] execution.rs の `location` の作り方が計画に無い → dismissed（置き方を誤ってもコンパイルで止まる。実装で `result_location.as_ref().map(ResultLocation::uri)` を Submission の前に置いた）
  - [low] T1 の別名キーの大小ループは既存テストと重複 → dismissed（CTAS の経路が `config.trino_catalog`（大小を区別）を使ったら落とせるのはこのループだけ。design-checklist の「無引用の名前で設定のキーを引く照合」の項）
- 6b: 論点 0 件のため提示のみ（D1 はユーザーが 18:24 に決めた）

## 覆した事前判断
- decisions.md 74 行「引用符の無い名前は置換しない（回避策で受ける）」→ S3 Tables の Context の CTAS の 1 部目 `awsdatacatalog` に限って AwsDataCatalog の Trino 名に差し替える。理由: 本物が i12 で Glue の DB に作ると実測（#235 で実測した書き換えは認めた）。影響: Trino に送る SQL が変わる（Query は受け取ったまま）。ユーザーに聞いた（18:24）

## フェーズの記録
- 7-0: 消す・変えるガード無し。呼び出し元 `ctas` 1（execution.rs）、`replacement` 2（catalog.rs・ctas）、`database_not_found` 1。増える分岐: CTAS でない／IF NOT EXISTS／3 部でない／1 部目が別カタログ／場所ありで DB 無し（FAILED）／場所なし（問い合わせない）／Trino 名が awsdatacatalog（差し替えない）／差し替え、と execution の FailAtRuntime・Rewrite・その他
- red 18:39（新 4 本が落ちた）／ green 18:55（create_table_catalog 15 passed）
- 計画から外れた点: D4 を「場所が無ければ DB を問い合わせない」にした（問い合わせても使い道が無い）。T3 の期待値を合わせた
- 検証: `tools/dev.sh cargo fmt --check`・`tools/dev.sh cargo clippy --all-targets --locked -- -D warnings`・`tools/dev.sh cargo test` ok（532 passed）
- ミューテーション: IF NOT EXISTS の除外を外す → 落ちた／理由の場所を別の値に → 落ちた／Rewrite で reported を立てない → 落ちた／CTAS の判定を外す → 落ちない（CTAS でない 3 部の文は `(n int COMMENT 'c')` なら #237 の No location の書き換え、`(n int WITH (p = 1))` なら NV で先に決まり、Trino が受ける形でこの関数に届くものが無い。判定は関数の定義として残す）
- 7-5b: 分岐の突き合わせ 0 抜け（CTAS の判定は上の理由で踏めない）。エラーの出口は `Failure::database_not_found` で FAILED、ファイルは置かない（`writes_result_file: false`）
- 7-7: 分割不要（最大ファイル create_table_catalog.rs 173 行。execution.rs は 501 行で #245）

## レビュー実施記録
- 計画攻撃 1 回（指摘 2・採用 0）
- 独立レビュー 1 回（general-purpose sonnet、約 12 分・Bash 10 本）: medium 以上 0 件。本番影響のある妥当指摘 0 件（外れた観点: サイズ・判断・新規性）
- 最終パス・フルループ: 小規模経路のため無し

## 実機検証
- 足場 `tools/e2e/ctas-catalog/verify.sh`（compose の trino・minio。`TRINO_CATALOG_MAP=s3tablescatalog/e2e232=iceberg,AwsDataCatalog=hive`）。general-purpose（sonnet）に委譲
- (a) HEAD（b429e82）: C1（hive に DB あり → 差し替えて hive に表、iceberg には無い）・C2（DB 無し → FAILED、理由・2/1301・ファイル無し）・C3（回帰 #237）すべて PASS（証跡 /tmp/athena-local-issue232-e2e.MFLzYh）
- (b) 変更前（97cf8cb）: C1・C2 が FAIL（CATALOG_NOT_FOUND `awsdatacatalog`）、C3 PASS（証跡 /tmp/athena-local-issue232-e2e.hG1XHe）。足場が新挙動を検出できる

## 範囲外の発見
| 内容 | 出どころ | 出口 |
|---|---|---|
| j13 の `.metadata`（81 バイト）の中身、`Database <ns>` の大文字小文字、既定の Context の同じ形、`IF NOT EXISTS` の CTAS、大文字混じりで DB がある形・小文字で DB が無い形 | issue 本文・計画 | #251 に追記（同じ S3 Tables の Context の実測ラウンドで測れる） |
| execution.rs が 400 行を超えている（501 行） | Explore 11 | #245（起票済み） |

## 委譲の記録
- 調査: Explore 1 本（sonnet、約 10 分）
- 計画: 自分（小規模経路で Plan 省略。#237 と同じ経路）
- 計画攻撃: Explore 1 本（sonnet、約 15 分）
- 実装: 自分（小規模経路・本番 80 行・Bash 約 12/30 本・16/20 分（18:39〜18:55））
- 独立レビュー: general-purpose 1 本（sonnet、約 8 分）
- 実機の足場と実行: general-purpose 1 本（sonnet、約 22 分・Bash 約 20 本。作成 14 分・実行 2 回 6 分）
- PR 本文: 自分（小規模経路）

## 出荷
- PR #253（main-232）。独立レビューの結果は PR にコメント済み。deferred 0 件なので issue へのコメントは無し
- 未実測の出口: #251 にコメントで追記（https://github.com/aoyagikouhei/athena-local/issues/251#issuecomment-5845286781）。execution.rs の 400 行超えは #245
- 出荷判定: 検証 green（532 passed）・7-5b 分岐 0 抜け（CTAS の判定は届く入力が無い理由つき）・実機 (a) 全 PASS／(b) 新挙動 FAIL。本番影響のある妥当指摘 0 件

## 時系列
- 18:23 quick 着手 ／ 18:25 escalate（63e94e8）
- 18:26 Explore 起動。先行作業: toolbox で既存の結合テストを流しビルドを温めた、caveats・design-checklist を読んだ
- 18:37 Explore 返着、計画を書いた
- 18:39 計画攻撃を起動。先行作業: red のテスト 4 本（18:39 に落ちるのを確認）、docs の下書き 8 ファイル
- 18:4x ユーザーの指示で CLAUDE.md の issue-notes を消す規則を外した（この PR に含める、#237 のノートは復元しない）
- 18:54 計画攻撃返着（採用 0 件）。6b は論点 0 件で提示のみ
- 18:55 green、標準コマンド ok（532 passed）
- 18:57 実装コミット 0170694、ノート規則のコミット b429e82
- 18:58 独立レビューと実機の足場を起動。先行作業: #251 への未実測の追記、PR 本文の下書き
- 19:06 独立レビュー返着（medium 以上 0 件）
- 19:06 実機の足場返着（(a) 全 PASS、(b) C1・C2 FAIL）
- 19:07 足場のコミット c31f254、push、PR #253 を作成、独立レビューの結果を PR にコメント
