# quick #232 S3 Tables の Context で別カタログを指す名前の CTAS を本物に合わせる
- 入力: issue 232。モード: standalone。ブランチ main-232、着手前 SHA 97cf8cb
- ゲート: サイズ 約 65 行・4 ファイル（≤50・3。見込み）／呼び出し元 create_table_catalog::check 1・three_part_name 1（同一モジュール）／契約: Query は受け取ったまま → 通過・事実: i12・j13 は実測済み（`<ns>` の大文字小文字・`.metadata` の中身は未実測）・判断: i12 の扱いを issue が決めていない／新規性: 無引用の名前への別名置換は decisions.md の判断を覆す → サイズ・判断・新規性で外れた
- 時系列: 着手 18:23 ／ 質問 18:24 ／ escalate 18:25（red・green なし）
- 検証: なし（コードに触れていない）
- 自己レビュー: なし
- 打ち切り・escalate: 提案あり（サイズ 約 65 行・4 ファイル、判断: i12 の扱い）→ 選択: implement-issue で進める。i12 はユーザーの判断で「1 部目を差し替える」（S3 Tables の Context の CTAS で無引用の 1 部目を TRINO_CATALOG_MAP の AwsDataCatalog の Trino 名に、桁を空白で揃えて差し替える。decisions.md の「無引用の名前は置換しない」を S3 Tables の CTAS に限って覆し、変更として記録する）
- 予算の実績: Bash 約 12 本、所要 2 分、サブエージェント 0 本
- 出荷: main-232（implement-issue に引き継ぐ）。未測定・範囲外の出口: 未定（implement-issue が決める）

## 引き継ぎ
- 判定箇所: `src/operation/execution.rs:146-195`（`quoted_names::rejection`／`unquoted_ddl::rejection` → No location なら `create_table_catalog::check`、S3 Tables の 2 部は `two_part_failure`）。CTAS は `unquoted_ddl/create_table.rs:34` の `rejection` が `substatement_type != CREATE_TABLE` で None を返すので、今は開始時の判定をどれも通らず Trino にそのまま送られる
- 共有できる既存機構: `create_table_catalog.rs` の `namespace_missing`／`schema_missing`（`schema_probe_sql` で SCHEMA_NOT_FOUND を見る）、`blank_out`（範囲の空白埋め）、`Outcome::{FailAtRuntime, Rewrite}` と execution.rs の `Reported`（Query は受け取ったまま）。名前の読み取りは `create_table.rs` の `table_name`／`three_part_name`（CTAS の名前も `CREATE TABLE` 直後なので読めるはず。`three_part_name` 自体は substatement_type を見ない）。別名の Trino 名は `create_table_catalog::trino_catalog`（大文字小文字によらずキーを当てる）。引用符付き別名の置換で桁を空白で揃える先例は `src/catalog.rs`
- 原因の仮説: 本物は S3 Tables の Context の CTAS で、1 部目が awsdatacatalog（i12 は小文字、j13 は `AwsDataCatalog`）なら 2 部目を Glue（AwsDataCatalog）の DB として引く。CTAS でない CREATE TABLE（2 部目を S3 Tables の名前空間として引く #227・#237）と逆
- 実装の方針（ユーザーの判断込み）:
  - i12: DB がある → 1 部目を AwsDataCatalog の Trino 名に空白で桁を揃えて差し替えて送る（Query は受け取ったまま）
  - j13: DB が無い（Trino の AwsDataCatalog 側カタログで SCHEMA_NOT_FOUND）→ Trino に送らず FAILED。ErrorCategory 2・ErrorType 1301・`Database <ns> not found. Please check your query. You may need to manually clean the data at location '<OUTPUT>tables/<id>' before retrying. Athena will not delete data in your account.`
- 未実測・未確認:
  - j13 の `<ns>` が書いたとおりか小文字か（実測の名前空間は小文字だった見込み）
  - j13 で本物が置いた `.metadata`（81 バイト）の中身。CTAS で書かれたデータの有無
  - 既定の Context の同じ形（i12・j13）
  - i12 の 1 部目が `AwsDataCatalog`（大文字混じり）で DB がある形（j13 から Glue で引くと推測できるが成功の形は未測定）
  - OutputLocation が無いとき（athena-local の結果無しの設定）の `<OUTPUT>` の埋め方
  - 4 部以上・引用符付きの部分・IF NOT EXISTS の CTAS
- 再現手順: `tools/dev.sh cargo test --test create_table_catalog` の既存テストの形で、Context を `s3tablescatalog/<bucket>` にして `CREATE TABLE awsdatacatalog.db.t AS SELECT 1 AS n` を投げ、偽 Trino に届く SQL が受け取ったまま（`awsdatacatalog.` のまま）であることを見る
- stash: なし
