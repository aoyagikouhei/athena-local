# quick #118 dbt-athena が EXPLAIN と SHOW FUNCTIONS の GetQueryResults の先頭行（列名行）をデータとして読む可能性の調査
- 入力: issue #118（調査）。モード: standalone。ブランチ main-118、着手前 SHA 6627fc8（過去ノートの削除コミット 81f736c の後から作業）
- ゲート: 本番コード 0 行（docs 2 ファイル）／契約・新規性なし／事実: 本物の Athena の先頭行は実測済み（EXPLAIN は #60・#63、SHOW FUNCTIONS は #80）、dbt の判定は手元の venv の dbt-athena 1.11.1 のソースで確定／判断: athena-local は本物と同じ返し方なので変えない → 通過
- 時系列: 着手 06:30 ／ コミット 06:31（red・green は無し。docs だけ）
- 検証: `cargo fmt --check`・`cargo clippy --all-targets --locked -- -D warnings`・`cargo test --locked` ok（398 passed）／athena-local の SHOW FUNCTIONS の列名行は docs/api.md の記述（2026-09-23 実測）で裏取り／dbt は実際には流していない（ソース読み）
- 自己レビュー: 分岐無し（docs だけ）。dbt の判定（`connections.py:362-369`、`464-466`）と本物の返し方の組み合わせを 4 通り（EXPLAIN、SHOW FUNCTIONS、ほかの UTILITY、DDL）で突き合わせた
- 打ち切り・escalate: 提案なし
- 予算の実績: Bash 約 10 本、所要 約 1 分（コミットまで。調査の読み取りは着手前の時刻に含まれない）、サブエージェント 0 本（docs だけなので止めない独立レビューは流さない）
- 出荷: main-118
