# issue #111 本物の Athena が要らない未実測 10 件を手元の e2e で潰す

## 基本情報

- issue: #111（親 #114）。ベースブランチ: main。作業ブランチ: main-111
- 着手前 SHA: f972b67（レビューの `base` の正）。quick ノートのコミット 3091033（docs 予算の別枠）
- 経路: quick-implement のゲートで「事実」「新規性」「変更サイズ」が外れて implement-issue へ escalate（2026-09-23 15:53〜15:55）
- 性質: 本番コードの変更は想定しない。成果物は `tools/e2e/` の足場と `docs/dev/measurements/` の記録、`docs/dev/unmeasured.md` の整理。本物と食い違う項目は別 issue を切る（#114 の方針）

## 事前に確認した事実

quick ノートから引き継いだ出発点（Explore の裏取り前）:

- 対象 10 件の出典: `docs/dev/unmeasured.md` の「実クライアントでの疎通」9 件と「Trino」1 件。書き先は `clients.md`（クライアント相手）と `trino.md`（Trino 相手）
- 既存の足場: `tools/e2e/minio/`（Trino 482 Hive/Iceberg + MinIO + TLS 終端 nginx + JDBC 3.8.1 クライアント）、`tools/measure/jdbc-metadata.sh`・`jdbc-show-metadata.sh`（`DRIVER_VERSION="3.8.1"` 埋め込み）、`tools/e2e/trino-probe/`（`TRINO_TAG` で版を切り替え）、`tools/e2e/sdk-retry/`（boto3 の venv の先行例）、`tools/measure/retention-stress.sh`（CPU 飽和下で tests/retention を回す雛形）
- 環境（2026-09-23 統括役が確認）: Docker 29.8.1 / compose v5.5.1。手元のイメージに `trinodb/trino:482`・`483`、`quay.io/minio/minio`・`mc`、`eclipse-temurin:21-jdk`、`maven:3.9-eclipse-temurin-21`、`python:3.12-slim`。ホストの Python は 3.14.6（venv 可。`uv` は `~/git/utilities.uv_command` の独自コマンドで astral の uv ではない）。Java / mvn はホストに無い（コンテナで使う）。稼働中のコンテナはポート 8080 を 1 つ使う（無関係）
- 「暗号化系の SHOW」= 本物が不透明な base64 の `.txt.metadata` を返す `SHOW TABLES` / `DATABASES` / `COLUMNS` / `PARTITIONS` / `TBLPROPERTIES`（`docs/dev/measurements/metadata.md:21`）。athena-local は素の protobuf を置く

Explore 3 本の返答（原文は scratchpad の explore-111.md。落とした番号と理由: Explore 1 の 9（.gitignore の全文）と 10（CHANGELOG の空）は出荷時に見直せば足りる／Explore 2 の 10（uv の言及箇所）は既知／Explore 3 の 2（tests/retention.rs の一覧）と 9（README の書式）は計画に写した分だけ）から、設計判断・計画・7-0 が参照する事実:

- 足場の衝突: minio 系 3 本（`verify.sh`・`jdbc-metadata.sh`・`jdbc-show-metadata.sh`）は同じプロジェクト名・コンテナ名・ポート（8092/9002/9003/8443/9443）・athena-local 8087 を共有し同時に走らせられない。他の e2e は 8093〜8096・8088〜8091。trino-probe は 8090（request-errors の athena-local と重複）
- JDBC: `DRIVER_VERSION` は固定値。ドライバのキャッシュは版ごとに別名。`Main.java` は引数 1 が fetcher、2 がシナリオ（46/57）。`runProbe` は SQLException を REJECTED として失敗に数えない。nginx ログで `.metadata` の GET を数える仕組みあり。`compare_row_counts` は SHOW の GetQueryResults で +1 を期待しており #60 後は古い。3.4.0 未満に `ResultFetcher=auto` は無く、3.0.0 は `jdbc:awsathena://` を受けない恐れ
- 失敗した DDL の `<id>.txt`: `write_failure` は `ResultFile::Text` だけ、Content-Type `application/octet-stream`、`.metadata` 無し、FAILED にする前に置く。JDBC が FAILED の結果ファイルを読みに行くかの記録は無い
- 保持期限: sweep は `lock()` ごと（定期タスク無し）。S3 を消すコードは `src/` に無い。捨てた後は `QUERY_EXECUTION_NOT_FOUND`。`tests/retention.rs` は S3 と組み合わせていない。バイナリの最小は 1 秒。結果の全行を `Outcome.rows` としてメモリに持ち上限無し。保持件数を外から見る API は無い
- UPDATE/DELETE: 分類は DML、`ResultFile::Csv`、本体は書かず `.metadata` だけ（`tests/metadata.rs:269,305`）。Iceberg カタログは `format_version` 未指定（既定 v2 の知識）。Hive は UPDATE 不可の見込み
- Python: awswrangler 3.17.1 は `read_sql_query(ctas_approach=False)` で GetWorkGroup → Start → GetQueryExecution → `GetQueryResults(MaxResults=1)` → S3 の CSV。`_fetch_api_result`（`wr.athena.get_query_results`、`output_location` 無しの DML）は 1 ページ目の先頭行を無条件で落とす。PyAthena 3.36.0 は `connect(endpoint_url=)` を S3 client にも流用、`Catalog` 既定 `awsdatacatalog`、FAILED は結果ファイルを読まずに `OperationalError`。dbt-athena 1.11.1 は impl の client に `endpoint_url` を渡さず、`GetWorkGroup` は CTAS の `is_work_group_output_location_enforced()` だけ、`dbt run` は Glue/STS 必須。3 クライアントとも FAILED の結果ファイルは読まない。botocore の `AWS_ENDPOINT_URL_<SERVICE>` は 1.31.0 から。`AWS_S3_ADDRESSING_STYLE` は無い
- athena-local の GetWorkGroup: s3 モードだけ `OutputLocation`、`EnforceWorkGroupConfiguration=false`、`ManagedQueryResultsConfiguration` 無し。`StartQueryExecution` は `WorkGroup` を検証しない。`TRINO_CATALOG_MAP` は大文字小文字を区別
- Trino 旧版: `connector_name` 列は 364 前後で追加（推測）。`catalog-legacy`（`fs.native-local.enabled`）と `catalog-nofsflag` は起動ログで選ぶ。probe.sh に後始末は無い
- measurements の README: athena-local 自身の観測は書かない → (D)(I) の書き先は Step 4 で決めた

## 実測の手順

（該当なし。本物の Athena は使わない。手元の e2e の手順は「計画」の各フェーズに書く）

## 実測の結果

（未着手）

## 実測の経緯

（未着手）

## 設計判断

- **dbt-athena の到達範囲**（Step 4、ユーザー決定 2026-09-23）: `dbt debug` と、`GetWorkGroup` を呼ぶ `is_work_group_output_location_enforced()` を `dbt run-operation` のマクロで呼ぶ（work_group 付き）まで通す。`dbt run` は Glue で止まる箇所を観測して記録し、Glue の代役（moto 等）は範囲外として起票する。理由: `dbt run` には Glue・STS が要り、dbt-athena の CTAS（`table_type`/`is_external`）は Trino で通らない見込み（explore-111.md Explore 2 の 5）
- **(D) 保持期限×S3 と (I) メモリの書き先**（Step 4、ユーザー決定 2026-09-23）: measurements には書かない（README の「athena-local 自身の観測は書かない」）。`docs/caveats.md` の該当の記述（"memory is still bounded…"、捨てた後の S3 の結果）を実測済みの記述に直し、`docs/dev/unmeasured.md` の「済み」に結果の一言と足場のパスを書く。issue 本文の「clients.md / trino.md に書く」をこの 2 件だけ覆す（→ 覆した事前判断）

## 計画

Plan の下書き（scratchpad の Plan 返答）を統括役が転記した。落とした番号と理由: §14-4 の補足案（440 の代わりに 420）は 440 を pull 済みなので不要／§14-10 の別案（毎回 0.4.0 でも流す）は固定費に見合わない／§11-8（0.4.0 の取得）は (7) で FAIL が出たときだけ／§3 フェーズ 1 の「`'z''q'` を含む値」は任意なので入れない（`jq --arg` で既に固定）。

### フェーズ分割（5 フェーズ。実行が排他なのは 1 と 2 だけ）

| フェーズ | 目的 | 項目 | 変更・新規 | 見込み | 並列委譲 | 共有リソース |
|---|---|---|---|---|---|---|
| 1 | minio の verify に UPDATE / DELETE と s3×保持期限を足す | (8)=C、(4)=D | 変更 `tools/e2e/minio/verify.sh`（+25）、`README.md`（+10）。新規 `tools/e2e/minio/cases-dml-retention.sh`（140） | 約 175 行 | 実装可。**実行は 2 と排他** | compose `athena-local-issue39-e2e`、8092/9002/9003、athena-local 8087 |
| 2 | JDBC: 失敗した DDL と旧版ドライバのループ | (3)=A の JDBC 側、(6)=B | 変更 `Main.java`（+25）。新規 `FailedDdlScenario.java`（90）、`tools/e2e/jdbc-drivers/{verify.sh,lib.sh,judge.sh}`（180/170/130） | 約 595 行 | 実装可。**実行は 1 と排他** | 1 と同じ compose・ポート、8443/9443、`~/.cache/athena-local-jdbc` |
| 3 | Python クライアント 3 本 | (1)=E、(2)=F、(7)=G、(9)=H、(3)=A の PyAthena 側 | 新規 `tools/e2e/python-clients/` 一式 | 約 750 行 | **可**（別 compose） | `athena-local-issue111-py`、Trino 8097、MinIO 9006、athena-local 8098（s3）/8099（none）、中継 8102 |
| 4a | 保持期限でメモリが頭打ちになるか | (5)=I | 新規 `tools/e2e/retention/{verify.sh,load.py}`（compose は `../sdk-retry/docker-compose.yml` を `-f` で流用） | 約 310 行 | 可。**実行は重い負荷と並べない**（sdk-retry とも排他） | `athena-local-issue94-e2e`（流用）、Trino 8095、athena-local 8101 |
| 4b | 旧版 Trino の値 | (10)=J | 変更 `trino-probe/docker-compose.yml`（1 行）。新規 `trino-probe/versions.sh`（170） | 約 175 行 | 可 | コンテナ名 `athena-local-issue39-trino`、`TRINO_PORT=8104` |

- 記録（measurements・unmeasured・caveats・docs/clients.md）は各フェーズのコミットに同梱する（足場＋結果で 1 コミット）。
- `cargo build --release --locked` は最初に 1 回。各足場は `SKIP_BUILD=1` で呼ぶ（並列だと `target/` のロックで待つ）。
- 同時に立てる Trino は 2 つまで（JVM 1 つ約 2GB、空き約 13GB）。
- 7-6: `tools/e2e/` はテスト足場なので行数条件は非該当。先行例 0 件（dbt プロジェクト・RSS の判定）と「本物の AWS に漏れうる」危険から、**フェーズ 3 と 4a だけ軽量レビュー**（条件付き 2 = 予算内）。1・2・4b は既存パターンの拡張として省略（ノートに記す）。

### ファイル構成（責務 1 つ・見込み行数）

- フェーズ 1: `verify.sh`（660→685。cases を `source`、`main` 末尾で 2 関数を呼ぶ、`start_athena_local` に保持期限とログ名の引数）／`cases-dml-retention.sh`（140。`run_update_delete_cases` 45、`run_failed_update_case` 30、`restart_athena_local_with_retention` 20、`run_retention_s3_case` 45）／README（ケース表に 10〜14）。verify.sh は 400 行超なので足すものは cases ファイルへ。ヘルパの lib 抽出は起票（範囲外）。
- フェーズ 2: `Main.java`（246→275。シナリオ `111` の分岐、位置引数 3・4 で `OutputLocation`・JDBC URL を上書き（既定は今の値）、接続直後に `PREFLIGHT`（`SELECT 1`）行、実際に使った fetcher・シナリオ・URL を `CONFIG fetcher=<x> scenario=<y> url=<z>` の 1 行で出す（judge.sh が要求と照合し、引数の受け渡しの誤りを検出する。表示用 `:59` の固定文字列も実際の URL に）、`failures`/`runSelectCase` を package-private）／`FailedDdlScenario.java`（90）／`jdbc-drivers/lib.sh`（170: preflight、証明書、版を引数に取るドライバ取得、compose 起動（サービス指定）、athena-local 起動 `0.0.0.0:8087`、mvn ビルド、`mc_run`、Trino 待ち）／`verify.sh`（180: 版×fetcher×シナリオのループ、JVM 1 回を `timeout 300` で包む（rc 124 は SKIP「ハング」）、後始末、summary。終了コードは FAIL の件数）／`judge.sh`（130: 1 回分の PASS/FAIL/SKIP/INFO、nginx ログからその回の接頭辞の下の `.txt*`（`.txt.metadata` を含む）への GET を数える、`CONFIG` 行の照合、行列の表）。`tools/e2e/minio/docker-compose.yml` の tls-proxy に `depends_on: minio: condition: service_healthy` を足す（1 行）。
- フェーズ 3（`tools/e2e/python-clients/`）: `docker-compose.yml`（45。Trino 482 は `../minio/catalog` を ro でマウント、MinIO、minio-init）／`verify.sh`（190: preflight、compose、athena-local 2 本、check は**逐次**に流す、中継 `../sdk-retry/drop_proxy.py` を `LISTEN=127.0.0.1:8102 UPSTREAM=127.0.0.1:8098 DROP_COUNT=0` で、MinIO の trace、漏れの canary、3 本の check、summary）／`env.sh`（40: AWS 系の環境変数を 1 か所で export）／`common.py`（70: `PASS/FAIL/SKIP/INFO <名前>: <詳細>`、中継ログと trace の**区間読み**（check の開始時に行数を控え、それより後の行だけを数える。canary も自分の区間だけ）、boto3 直接呼び出し）／`check_awswrangler.py`（120）／`check_pyathena.py`（120）／`check_dbt.sh`（90: `dbt debug`/`run-operation`/`run`、`--log-format json`）／`dbt/{dbt_project.yml,macros/check_work_group.sql,models/m111.sql}`／`requirements-wr.txt`、`requirements-dbt.txt`、`setup-venvs.sh`（40）。`profiles.yml` は実行時に出力ディレクトリへ生成（コミットしない）。venv は `$HOME/.cache/athena-local-111/venv-{wr,dbt}`。
- フェーズ 4a（`tools/e2e/retention/`）: compose は `../sdk-retry/docker-compose.yml`（Trino 482 + memory、8095）を `-f` で流用し新設しない／`verify.sh`（140: build → compose → 保持期限 3600 と 1 で 1 回ずつ athena-local（8101、`RESULTS=none`）の起動・負荷・停止 → 判定 → summary。終了コードは FAIL の件数）／`load.py`（170、標準ライブラリだけ: 負荷と 5 秒ごとの VmRSS/VmHWM の標本を CSV に、GetQueryExecution のポーリングは 0.1 秒、`--judge` で 2 つの CSV を判定）。
- フェーズ 4b: `trino-probe/docker-compose.yml` の `"8090:8080"` → `"${TRINO_PORT:-8090}:8080"`／`versions.sh`（170: タグのループ、catalog を `catalog`→`catalog-legacy`→`catalog-nofsflag` の順に試し、`SHOW SCHEMAS FROM hive` と `SHOW SCHEMAS FROM iceberg` が `error` 無しで返る構成を採用、`/v1/info` の `nodeVersion` をタグと照合、`probe.sh` を `OUT_DIR`/`BASE` 付きで呼ぶ、生 JSON から値を抜いて表、版ごとに `down -v`）。

### 変更内容と根拠

- **フェーズ 1 (C)**: `run_merge_case`（`verify.sh:417-418`）は updateType と件数を引数に取り、field 3 以降の固定値 `rows_bigint_column`（`:419`）は athena-local の UPDATE の `.metadata` と同じ（`tests/metadata.rs:298`）。Iceberg に `(1,'a'),(2,'b')` を CTAS（MERGE と同構成で PASS 済み `trino.md:32-37`）。ケース 10 `UPDATE ... SET s='z' WHERE n=1` → `UPDATE`/1。11 `DELETE ... WHERE n=2` → `DELETE`/1。12 Hive への UPDATE は FAILED で `<id>.csv` も `.csv.metadata` も無い（UPDATE は `ResultFile::Csv` `results.rs:58-59`、`write_failure` は Text のみ `result_output.rs:107-135`、本物も置かない `caveats.md:249-250`）。（ケース 13 の「該当なしの DELETE」は削減で外した。件数が届くかはケース 11 で分かる。）
- **フェーズ 1 (D)**: 全ケース後に `ATHENA_LOCAL_RETENTION_SECONDS=1` で再起動。`SELECT 1 AS n` が SUCCEEDED → 2.5 秒待って `GetQueryExecution` → 400 `QUERY_EXECUTION_NOT_FOUND`（破棄の対照。`lock()` の sweep `store.rs:31-41,250-255`。`athena_wait` は 0.3 秒間隔なので SUCCEEDED の観測は完了後 0.4 秒以内で、1 秒の期限より前）→ `mc_stat` で `<id>.csv` と `.csv.metadata` が両方あれば PASS（S3 を消す・書き直すコードは無いので 1 回の確認で足りる。本体は SUCCEEDED の前に置く）。`start_athena_local` の env に `ATHENA_LOCAL_RETENTION_SECONDS="${1:-3600}"`（既定と同値なので既存ケースは不変）、ログ名は引数（今は `>` で上書き `verify.sh:178`）。`main` の末尾は `RESULT_STATUS` の FAIL の件数を返す（今は常に 0。INFO と SKIP は数えない）。
- **フェーズ 2 (A)**: `FailedDdlScenario` の 4 文 `DROP TABLE iceberg.default.nope_<run>`、`SHOW COLUMNS FROM hive.default.nope_<run>`、`DESCRIBE hive.default.nope_<run>`、`ALTER TABLE hive.default.nope_<run> ADD COLUMN m int`（構文チェックは通り FAILED になる `caveats.md:266-273`）。1 文ごとに `FAILED_DDL <label> status=EXPECTED_SQLEXCEPTION|NO_EXCEPTION|OTHER_EXCEPTION msg=<1 行>`。PASS の条件は EXPECTED_SQLEXCEPTION かつ `msg` に Trino の失敗理由（`does not exist` など）が含まれること。SQLException は失敗に数えない（`runProbe` の前例）。最後に `SELECT 1`。シェル側は出力先を `s3://athena-results/e2e-jdbc111/<ver>/<fetcher>/<scenario>/` に分ける。シナリオ 111 の接頭辞の下の `.txt` はすべて失敗 DDL のもの（PREFLIGHT と `SELECT 1` は `.csv`）なので、mc で `.txt` が 4 件あることを対照にし、nginx ログでその接頭辞の下の **`.txt*`（`.txt.metadata` を含む）への GET** を `/<prefix>/` の前方一致で数える（期待 0。auto/S3 のドライバは本体より先に `.txt.metadata` を GET するので、`.metadata` を外すと読みに行く旧版が PASS になる）。`ResultFetcher=GetQueryResults` × 111 は S3 を読まないので INFO（対照）にする。
- **フェーズ 2 (B)**: 版 `3.8.1`（対照。fetcher 未指定・`S3`・`GetQueryResults` × 46/57/111 の 3×3）+ 旧版 `3.5.0 / 3.4.0 / 3.3.0 / 3.2.2 / 3.1.0 / 3.0.0`（各修正の直前）は **`{auto（3.4.0 以上のみ）, S3} × {57, 111}`** に絞る（GetQueryResults は S3 を読まないので (A)(6) のどちらにも答えず、46 は 3.5.0 以下で既知の NoSuchKey（`docs/result-files.md:155-158`）をなぞるだけ。JVM は 63 回 → 9 + 22 = 31 回）。**(6) の合否は `failures=` ではなく `RESULT SHOW_* status=` の行だけで決める**（57 の準備の `CREATE TABLE` は `.metadata` を置かないので、3.5.0 以下では既知の NoSuchKey で `runSetup` が失敗して `failures>0` になる。それは「既知（result-files.md:155）」として別に数え、実測した事実は `docs/result-files.md:155-158` に書く。新しい注意書きや起票はしない）。`PREFLIGHT` が失敗した回は、その回の接頭辞への GET が 1 件以上なら **FAIL**（ドライバは S3 に届いたが athena-local の `.csv.metadata` などを処理できなかった。例外の 1 行目とスタックに metadata/protobuf の解析があるかを記録）、0 件なら SKIP（接続の非互換 U1）。同じ版で fetcher を変えた結果の差が切り分けの材料。URL は位置引数 4 で `jdbc:athena://` に全版そろえる（3.1.0 で `awsathena` は非推奨）。fetcher・シナリオ・出力先・URL は既存 2 本と同じ位置引数 `'$fetcher' '$scenario' '$output' '$url'` で渡し（`-e` は使わない）、`CONFIG` 行を judge.sh が要求と照合する。既存 `jdbc-show-metadata.sh` を改造しない理由: `compare_row_counts` の +1 が #60 後に古い（N1）、版ごとに up/down、シナリオ 57 のみ。
- **フェーズ 3**: どのクライアントにも `endpoint_url` を渡さず環境変数だけで向ける（PyAthena は S3 client にも流用 `filesystem/s3.py:150-155`、dbt の impl client は受け取らない）。`AWS_ENDPOINT_URL`→中継、`AWS_ENDPOINT_URL_ATHENA`→中継、`AWS_ENDPOINT_URL_S3`→MinIO。path-style は生成した `AWS_CONFIG_FILE` の `s3 = addressing_style = path`。`AWS_SHARED_CREDENTIALS_FILE=/dev/null`、`AWS_EC2_METADATA_DISABLED=true`、`DBT_SEND_ANONYMOUS_USAGE_STATS=false`。athena-local に `TRINO_CATALOG_MAP=awsdatacatalog=iceberg,AwsDataCatalog=iceberg`（PyAthena の既定 `Catalog`、dbt の `database`）。(F) は出力先未指定で GetWorkGroup の `OutputLocation` を使う経路。(H) は none の 8099 に boto3 で `OutputLocation` 無しの SELECT → `wr.athena.get_query_results(id)` で `_fetch_api_result` に入る。(E) は debug + run-operation、run は INFO。check（awswrangler → PyAthena → dbt）は逐次に流し、`common.py` は check の開始時に中継ログと trace の行数を控えて区間だけを数える（canary・F3・E3 の STS/Glue の行が F1 の「STS 0 件」に混ざらない。F1/F2 の GetWorkGroup が E2 の証拠に混ざらない）。E2 の証拠は区間内の `AmazonAthena.GetWorkGroup` ≥1（athena-local は `Enforce…=false` を返すので `ENFORCED=False` だけでは呼んだ証拠にならない）。PyAthena の既定 `Cursor` は S3 を読まないので (3) の Cursor 行は INFO（対照）、判定は PandasCursor で行う。
- **フェーズ 4a**: 行は全部メモリ、上限無し（`store.rs:62`、`trino.rs:88-99`）。保持件数は外から見えないので RSS と「古い ID が 400 か」で見る。1 件のメモリの見積もり: `Value` 32B、1 行 ≒ 24B + 内側 Vec 約 80B + String の malloc（`lpad` の長さ + 24）。**`lpad(..., 1000, 'x')` × 2000 行 ≒ 2.2MiB/件**（100 文字だと約 0.45MiB/件で、1 件/秒未満だと 3600 側が 220 秒で 100MiB に届かず常に未測定になる）。ポーリング 0.1 秒で約 1 件/秒 → 3600 側は約 480MiB/220 秒。
- **フェーズ 4b**: `probe.sh` の D 節は `probe_sql` と同形で `OUT_DIR`/`BASE` を受ける。C 節は `IF NOT EXISTS` 無しなので版ごとに `down -v`。catalog の採用基準は `SHOW SCHEMAS FROM hive` と `SHOW SCHEMAS FROM iceberg` の両方が `error` 無しで返ること（`SHOW CATALOGS` に出るだけでは 477 以降の「読めなかったカタログも一覧に出る」状態を採用してしまう）。応答 JSON の `error` を見る（Trino の SQL エラーは HTTP 200）。

### 依拠する既存機構の前提表

| 機構 | 暗黙の前提 | 破らないか | 保証元 |
|---|---|---|---|
| `run_merge_case`/`read_metadata_head` | 長さ・件数が 1 バイト。field 3 以降は `rows bigint` 固定 | 件数 1。列が `rows bigint` かは今回の実測で確かめる | `verify.sh:393-411,419`、`tests/metadata.rs:80-83,298` |
| `mc_stat` | 前方一致。`name` で完全一致に絞る | `.csv` と `.csv.metadata` を区別できる | `verify.sh:254-272` |
| `start_athena_local`/`cleanup` | `ATHENA_PID` は 1 つ。ログは `>` 上書き | 再起動は kill→wait→起動→PID 更新、ログ別名 | `verify.sh:59-65,162-201` |
| `Store::lock` の sweep | API 呼び出しごとに期限切れを捨てる。定期タスク無し | ケース 14 はその API 呼び出しで破棄を起こす。2.5 秒待つ | `store.rs:31-41,250-255` |
| `write_result` | 本体を置いてから SUCCEEDED | ケース 14 の前提 | CLAUDE.md |
| `write_failure` | Text だけ `FAILED: `。EXPLAIN・取り消しは除く。FAILED の前に置く | (A) の対照は DDL・SHOW・DESCRIBE・ALTER でしか作れない | `result_output.rs:107-135`、`execution.rs:216-220`、`results.rs:145-152` |
| `Main` の `failures`/`runProbe` | SQLException だけ REJECTED。`failures>0` で exit 1 | 新シナリオでは期待どおりの SQLException を数えない。46/57 の出力は不変 | `Main.java:68-78,160-185` |
| `tls/nginx.conf` | 8443→`host.docker.internal:8087` 固定 | フェーズ 2 の athena-local は `0.0.0.0:8087` | `nginx.conf:12-20` |
| `MINIO_DOMAIN` と `/etc/hosts` | virtual-hosted style。SDK が Docker のリゾルバを引けない | lib.sh に同じ手順を複製。旧版でも足りるかは U1 | `docker-compose.yml:30-35`、`jdbc-show-metadata.sh:314-330` |
| `drop_proxy.py` | 1 接続 1 リクエスト。Content-Length 前提。ログ 1 行固定形式 | `DROP_COUNT=0` で落とさない。S3 は中継を通さない | `drop_proxy.py:11-12,42-58,85` |
| GetWorkGroup | s3 モードだけ `OutputLocation`。`Enforce...=false` | (F) は s3 の 8098 へ。none の 8099 は対照 | `work_group.rs:47-58` |
| GetQueryResults の先頭行 | DML と SHOW FUNCTIONS だけ列名行 | (H) は SELECT | `query_execution.rs:79-89` |
| `TRINO_CATALOG_MAP` | 大文字小文字を区別して完全一致 | 2 綴りを登録 | `config.rs:94-98` |
| `probe.sh` | `BASE` 既定 8090、テーブル名固定、`set -e` | `BASE` を渡し版ごとに `down -v` | `probe.sh:20-23,55-58,91-99` |
| Trino の `sequence` | 1 万要素まで | 負荷は 2000 行 | `trino.md:97-101` |

裏取り Explore（2026-09-23）で真と確かめた前提: dbt の `is_work_group_output_location_enforced` は `@available`（`impl.py:355-356`）で、`work_group` 設定かつ `skip_workgroup_check` 偽のときだけ GetWorkGroup を呼ぶ（`:360`）。その client は `endpoint_url` を渡さない（`:347-351`）。`dbt debug` は `select 1 as id` だけ（dbt-core `base/impl.py:476-477`）、`run-operation` はキャッシュ（Glue）も STS も呼ばない。PyAthena は FAILED でも `cursor.query_id` が取れる（`cursor.py:146,155,166`）、`Catalog` 既定 `awsdatacatalog`（`connection.py:171`）。awswrangler の `get_query_results` は GetQueryExecution ×2 と `GetQueryResults(MaxResults=1)` だけで GetWorkGroup/STS を呼ばない（`_read.py:901-910`）、先頭行は無条件で落とす（`:356-358,382-384`）。`AWS_ENDPOINT_URL_ATHENA` が `wr.config` 空のとき効くことを実行で確認（S3 は影響なし）。`drop_proxy.py` の環境変数は `LISTEN`/`UPSTREAM`/`MARKER`/`DROP_COUNT`（`TARGET` ではない）、`DROP_COUNT=0` で全部中継、ログは `proxy: relay target=<X-Amz-Target|-> token=<..|->`（STS/S3 は `target=-`）、Content-Length 無しは扱えない（S3 は中継を通さない）。Trino 482 の Iceberg は `format_version` 既定 2、Hive の UPDATE はトランザクションテーブルのみ（ドキュメント）。`ResultFetcher=auto` は 3.4.0 から。3.1.0 で `awsathena` が非推奨（`athena` を使う）。`probe.sh` は `set -e` だが Trino の SQL エラーは HTTP 200 なので止まらない → `versions.sh` は応答 JSON の `error` を見る。新しい clone では minio の `verify.sh` の前に `make-cert.sh` が要る（起票候補）。

未保証の前提: **U1** 旧版ドライバで `AthenaEndpoint`/`S3Endpoint`/`User`/`Password` が使えるか（PREFLIGHT 失敗は未測定に分類。「壊れた」とは判定しない）。**U2** botocore が中継経由のリクエストに Content-Length を付けるか（#94 で boto3 では成立。同じ botocore なので現実には成立と見る。検知コードは足さない）。**U3** DELETE/UPDATE の件数が Trino から届くか（ケース 10・11 で実測）。**U4（解消 2026-09-23 16:40 統括役が実機で確認）** `mc admin trace --json local` の 1 行は `{"api":"s3.GetObject","path":"/athena-results/probe111/abc.txt","statusCode":200,...}` の形で、`api`・`path`（バケット込みの path-style）・`statusCode` が載る。preflight の「既知のオブジェクトを 1 回 GET して trace に出る」確認はそのまま残す。 **U5（未解消）** nginx のアクセスログの形式（`log_format` 未指定 → 既定の combined で、virtual-hosted なのでパスにバケットは含まれないと推測）。フェーズ 2 の preflight で既知の GET が `GET /<prefix>/<key>` としてログに出ることを確かめてから「GET 0 件」を判定する。 統括役が 16:40 に `up -d minio minio-init tls-proxy` を 2 回試したところ、2 回とも nginx が `host not found in upstream "minio"` で起動に失敗した（tls-proxy に `depends_on` が無く、minio の名前解決より先に起動する競合）。**フェーズ 2 で `docker-compose.yml` の tls-proxy に `depends_on: minio: condition: service_healthy` を足す**（minio には healthcheck があり `healthy` になる。既存の jdbc 2 本にも効く 1 行の修正）。

### 直列化・エスケープ境界と正規化境界

- SQL→JSON は既存の `jq -n --arg`（`verify.sh:217-218`）。手書きエスケープ無し。セットアップの SQL は `curl --data-binary`。mc のキーは固定接頭辞+UUID+版だけ。
- シェル→JVM は既存 2 本と同じ位置引数 `'$fetcher' '$scenario' '$output' '$url'`（値は固定リストと接頭辞+版だけで、利用者の入力は入らない）。受け渡しの誤り（空に潰れる等）は `CONFIG` 行の照合で検出する。
- nginx/trace の照合は `grep -F` の固定文字列で `/<prefix>/` の前方一致、`.txt*` は `.txt.metadata` も含めて「読みに行った」に数える（JDBC の judge.sh と PyAthena の check で規則をそろえる）。nginx は virtual-hosted（`GET /e2e-jdbc111/...`）、trace は path-style（`/athena-results/...`）。
- Python: SQL はリテラル、JSON は `json.dumps`、dbt の値は `profiles.yml` の `env_var()`。dbt の出力は `--log-format json`、マクロの結果は `ENFORCED=(True|False)`。`load.py` は `/proc/<pid>/status` の数値だけ。
- クエリ ID は出どころが 1 つで加工しないので完全一致。`connector_name`/`updateType` は生の値で完全一致（小文字化しない。ずれ自体を観測する）。RSS は KiB で判定、summary は MiB。

### red→green の読み替え

- フェーズ 1: 変更前は 19 項目 PASS。変更後はケース 10・11・12・14 が PASS、13 は INFO。red = 再起動に保持期限を渡さないと 14 が未測定になること（統括役が先に 1 回流す）。10・11 の red はミューテーションで兼ねる。
- フェーズ 2: 変更前の `jdbc-show-metadata.sh` は 3.8.1 で「行数の突き合わせ」が FAIL の見込み（N1。新足場の根拠）。最初の red: 版一覧を `3.8.1` と `0.0.0` にして流し、3.8.1 は 46/57/111×3 モードが PASS（GetQueryResults×111 は INFO。111 は EXPECTED_SQLEXCEPTION 4 件、接頭辞の `.txt*` への GET 0、`.txt` が 4 件）、`0.0.0` は SKIP。`CONFIG` 行が要求と一致。全 PASS・全 FAIL なら足場を疑う。green は旧版 6 本の行列（PASS/FAIL/未測定それぞれに理由が付くことが受け入れ条件）。
- フェーズ 3: 最初の red は canary（STS と Glue が中継に届く）。(F) の対照は none の 8099 で STS が中継に現れること。(H) は 1500 行のまま先頭 1。(G) で FAIL が出たときだけ 0.4.0 のイメージで切り分け。
- フェーズ 4a: red = 両方 3600 にすると FAIL、1 クエリ 1 行にすると対照が伸びず未測定。green = 1 秒側が頭打ちで古い ID が 400、3600 側は伸び続け 200。
- フェーズ 4b: red = 版一覧 `482` だけで `trino.md:17-30` の表の値を再現。存在しないタグ `1` が未測定。

### ミューテーション確認で壊す候補

| フェーズ | 壊し方 | 期待 |
|---|---|---|
| 1 | ケース 10 の期待を `MERGE`/2 に | FAIL |
| 1 | `run_failed_update_case` の「無い」を「ある」に反転 | FAIL |
| 1 | ケース 14 の期待を 200 に | FAIL |
| 1 | 再起動に 3600 を渡す | 未測定 |
| 1 | 残存確認を `<id>.csvx` に | FAIL（名前で絞る処理が効いている証拠） |
| 2 | `FailedDdlScenario` で SQLException を失敗に数える | 3.8.1 が FAIL |
| 2 | GET を数えるパターンに `.csv` も含める | PREFLIGHT の `.csv` の GET を拾って FAIL（GET を数える判定が効いている証拠） |
| 2 | judge.sh の `CONFIG` 照合を外して fetcher を空で渡す | 照合が無いと全回 auto で PASS してしまうことの逆証明（照合ありで FAIL） |
| 2 | 版に `0.0.0` | 未測定 |
| 3 | (H) の期待件数を 1499 に | FAIL |
| 3 | (F) を none に向ける | 「STS 0 件」が FAIL |
| 3 | `common.py` の区間の起点を 0 行目に固定 | canary の STS 行を拾って F1 が FAIL（区間読みが効いている証拠） |
| 3 | 失敗 DDL の直後に check 自身が `<id>.txt` を GET | 「GET 0 件」が FAIL |
| 3 | dbt の期待を `ENFORCED=True` に | FAIL |
| 3 | `TRINO_CATALOG_MAP` を外す | PyAthena の preflight で止まり未測定 |
| 4a | 1 秒側と 3600 側を入れ替え | FAIL |
| 4a | 行数を 1 に | 未測定 |
| 4a | 閾値の比較を反転 | FAIL |
| 4b | 482 の対照の期待を `Hive` に | FAIL |
| 4b | catalog の自動選択を `catalog` 固定に | 旧版で SKIP |
| 4b | `nodeVersion` の照合を外して `TRINO_TAG` を渡さない | 全版が 482 の値で PASS（照合ありで FAIL） |

状態は既存どおり `PASS/FAIL/SKIP/INFO` の 4 語に絞り、「未測定」は SKIP の詳細欄に理由を書く。INFO（(E) の `dbt run`、(F) の none 側、JDBC GetQueryResults×111、PyAthena Cursor の (3)）は合否に数えず、summary で PASS と混ぜない。verify.sh の終了コードは FAIL の件数（sdk-retry と同じ）。

### ケース表（10 項目）

| 項目 | 投げるもの | 期待 | 判定 | 未測定になる条件 |
|---|---|---|---|---|
| (1) dbt-athena | profile `work_group: wg111`、`s3_staging_dir: s3://athena-results/dbt/`、`database: awsdatacatalog`、`schema: default`、`region_name: us-east-1`。E1 `dbt debug`。E2 `dbt run-operation check_work_group`（`is_work_group_output_location_enforced()` の値をログに）。E3（INFO）`dbt run --select m111` | E1 rc 0・中継に Start/GetQueryExecution。E2 `ENFORCED=False`・中継に `GetWorkGroup`。E3 Glue（`AWSGlue.*`）か STS が athena-local に届いて止まる | rc、dbt JSON ログ、中継ログ | venv 不可。マクロから呼べない→E2 だけ未測定 |
| (2) awswrangler `read_sql_query(ctas_approach=False)` | F1 出力先未指定・workgroup 既定 `SELECT 1 AS n, 'a' AS s`。F2 `workgroup="wg111"`。F3（INFO）F1 を none の 8099 へ | F1・F2 例外なし、df `[[1,'a']]`、中継に GetWorkGroup ≥1、STS 0。F3 は STS が中継に届いて例外 | df、中継ログ | venv 無し。preflight 失敗 |
| (3) 失敗 DDL の `<id>.txt` | JDBC: 4 文×版×{auto,S3}（GetQueryResults は INFO）。PyAthena: `DROP TABLE iceberg.default.nope_<run>` と `SHOW COLUMNS FROM hive.default.nope_<run>` を PandasCursor で（Cursor は INFO） | JDBC は SQLException（msg に理由）、PyAthena は `OperationalError`（理由を含む）。接頭辞の `.txt*`（`.txt.metadata` 込み）への GET 0。後続 `SELECT 1` が通る。`.txt` が置かれている（対照） | 例外の型、nginx/trace の区間内の GET 件数、mc の一覧 | `.txt` が無い。trace/nginx の preflight 失敗 |
| (4) s3×保持期限 | 保持 1 秒で `SELECT 1 AS n` → 2.5 秒 → GetQueryExecution | 400 `QUERY_EXECUTION_NOT_FOUND`。`.csv` と `.csv.metadata` が残る | 400 の後に mc_stat 1 回 | 400 にならない |
| (5) メモリ | 負荷 `SELECT x, lpad(cast(x AS varchar), 1000, 'x') AS pad FROM UNNEST(sequence(1, 2000)) AS t(x)`（約 2.2MiB/件）を逐次 1 本で Start→完了待ち（0.1 秒ポーリング）→GetQueryResults 1 ページ。保持 3600 と 1 で各 240 秒、5 秒ごと標本、最初の 20 秒は暖機 | 全部満たせば PASS: (a) 3600 側の伸び ≥100MiB かつ後半の伸びが前半の 0.5 倍以上（対照の成立。満たさなければ未測定）(b) 1 秒側の伸びが 3600 側の 1/4 以下 (c) 1 秒側の後半の伸びが前半より小さいか 16MiB 以下 (d) 最初の ID の GetQueryExecution が 1 秒側 400・3600 側 200 (e) 完了数の比 0.7〜1.3（外れたら未測定） | CSV と `load.py --judge` | 負荷不足、`/proc` 不可 |
| (6) 旧版 JDBC | 3.8.1 は 3×3、旧版は `{auto(≥3.4.0),S3}×{57,111}` | 3.8.1 は PASS。旧版は予測不能。3.5.0 以下の 57 の準備の NoSuchKey は既知として別枠 | `RESULT SHOW_* status=` の行だけで合否、`CONFIG` 照合、PREFLIGHT 失敗は GET ≥1 なら FAIL（例外 1 行目とスタックの metadata/protobuf の有無）・0 なら SKIP、JVM の `timeout 300` の rc 124 は SKIP | 取得失敗、接続の非互換 |
| (7) 退行 | 型の行（int, bigint, double, decimal(10,2), varchar（`,` `"` 改行 タブ 非 ASCII）, boolean, date, timestamp, array, map, NULL）、`SHOW TABLES`、`DESCRIBE`、`CREATE TABLE`/`DROP TABLE`、`INSERT`、CTAS。PyAthena は Cursor と PandasCursor、awswrangler は `read_sql_query` | 例外なし。Cursor と PandasCursor で行数と代表列（int・varchar）の値が一致。INSERT・CTAS を PandasCursor で読む反応は未知（INFO） | 値の突き合わせ | 未知は INFO。FAIL は 0.4.0 で切り分け |
| (8) UPDATE/DELETE | ケース 10〜12 | 75 バイト、`UPDATE`/1、`DELETE`/1、Hive は FAILED で何も置かない | `run_merge_case` | Trino が拒否 |
| (9) GetQueryResults の先頭行 | none の 8099 に boto3 で `SELECT x FROM UNNEST(sequence(1,1500)) AS t(x)` と `SELECT 'c' AS c` → `wr.athena.get_query_results(id)` | 1500 行・先頭 1・末尾 1500。後者は 1 行 `c`。S3 の GET 0 | df、trace | STS/GetWorkGroup を要求して止まる |
| (10) 旧版 Trino | 480（対照）/475/470/440/400。`probe.sh` の A2・D1〜D4・C1・C2 | 480 は 482 と同じ見込み。古い版は列が無いか値が違う可能性 | 482 の表と比較、`nodeVersion` 一致 | 取得失敗、3 catalog とも `SHOW SCHEMAS` が通らない |

### 人間検証リスト（出荷時に起票）

- `dbt run` を本物の Glue・STS 相手に通す（本物の AWS が要る）。
- 数時間の RSS の推移（4a は 240 秒×2 の代理指標。`DURATION=3600` で流せる形にしておく）。

### 一致台帳

| 値 | 置き場所 | 守り方（1 つ） |
|---|---|---|
| ポート・プロジェクト名 | 各 compose の `ports:`/`name:` と各 verify.sh の `*_BASE` | 既存どおり、起動待ち（`/v1/info`・ListWorkGroups）への実際の接続が失敗して止まることで守る（照合は足さない） |
| Trino のタグ | 4 つの compose は 482、trino-probe は `TRINO_TAG` | `/v1/info` の `nodeVersion` を summary に出し、4b は期待と照合 |
| ドライバの URL | `jdbc-metadata.sh:40-43`、`jdbc-show-metadata.sh:45-51`、新 `lib.sh` | 新規は lib.sh の 1 か所。既存 2 本との共通化は起票 |
| `probe_sql` | `table_format.rs:75-83` と `probe.sh:112-120` | コピーのままコメントで対を明記。ずれれば 4b の 482 の対照が落ちる |
| catalog の設定 | `minio/catalog` と `trino-probe/catalog` | python-clients は `../minio/catalog` をマウント（複製しない） |
| バケット・認証情報・`TRINO_CATALOG_MAP` | python-clients の `env.sh` と compose | `env.sh` の 1 か所 |
| Python の版 | `requirements-*.txt` と clients.md | 記録は `pip freeze` の出力から写す |
| `drop_proxy.py` のログ形式 | `drop_proxy.py:11-12` と `common.py` | `common.py` のコメントで出典を指す |

### 記録の書き先

| 項目 | 書き先 |
|---|---|
| (1) | clients.md 新 `## dbt-athena` 節。docs/clients.md に `## dbt-athena`（debug は通る、run は Glue/STS、`AWS_ENDPOINT_URL*` で向ける） |
| (2)(9)(7) | clients.md 新 `## awswrangler` 節、(7) は `### PyAthena 3.36.0・awswrangler 3.17.1 の回帰確認`。(9) の備考で `clients.md:58-62` との関係を 1 行 |
| (3)(6) | clients.md の JDBC の節に 2 項目。3.5.0 以下の DDL の NoSuchKey は既知なので `docs/result-files.md:155-158` の記述を実測で裏付けた形に直す（新しい注意書き・起票はしない）。SHOW の `.txt.metadata` を読めない版が見つかったときだけ docs/clients.md に注意書き＋別 issue |
| (8) | trino.md `### UPDATE / DELETE の updateType と .metadata`（#56 の MERGE と同じ形） |
| (10) | trino.md に新しい `### Trino の旧版（400〜480）` 項目（482 を対照にした独立の表。既存の #39 の表には手を入れない。どの catalog で起動したかも） |
| (4)(5) | docs/caveats.md の Query lifecycle（`:303-321`）を確認済みの記述に（数値は 1 つだけ）。unmeasured.md の「済み」に要点と足場のパス |
| 全 | unmeasured.md の `:59-67` と `:75` を「済み」へ |
| CHANGELOG | 挙動を変えないので書かない。食い違いは別 issue の PR が書く |

証跡は e2e の既存どおり `mktemp -d /tmp/athena-local-issue111-<足場>.XXXXXX`（フェーズ 1 は既存の `/tmp/athena-local-issue39-e2e.*`）。記録には #94 の前例どおり「verify.sh が `/tmp/athena-local-issue111-*` に残す」と書く。

### 先行できる作業（状態）

1. 旧版 Trino の pull（480/475/470/440/400）: **済み**（16:1x）
2. 旧版 JDBC jar 6 本のキャッシュ: **済み**
3. venv-wr / venv-dbt: **済み**（scratchpad。dbt-athena は 3.14 で入った → N2 は解消。足場は `$HOME/.cache/athena-local-111/` に作り直す）
4. venv 直後の確認（`@available`、`cursor.query_id`）: **済み**（両方とも真）
5. `cargo build --release --locked` 1 回: **済み**（16:40）
6. MinIO の trace の形式: **済み**（U4）
7. 範囲外の起票: 出荷の手順で
8. （任意）0.4.0 のイメージ: (7) で FAIL が出たときだけ

### 判断が割れる点の決着（統括役。Step 6b の「確認だけ」）

1. dbt は debug + run-operation まで（ユーザー決定）。2. (D)(I) は caveats + unmeasured（ユーザー決定）。3. JDBC は 7 版。4. Trino は 480/475/470/440/400。5. メモリの判定は上の (a)〜(e)（閾値は対照 1 回目で調整可、調整は記録）。6. フェーズ 3 は別 compose。7. フェーズ 2 は新 `jdbc-drivers/`。8. Main.java は新クラス追加。9. ArrowCursor は範囲外（pyarrow のエンドポイントが保証されず本物の S3 に出る恐れ）。10. (7) は今のビルドだけ。11. 漏れ防止は環境変数 + canary。12. dbt はホストの 3.14 venv。13. 4a/4b を分ける。14.（計画レビュー後）旧版 JDBC は `{auto,S3}×{57,111}` に絞る。15. 4a の compose は sdk-retry を流用。16. ケース 13 は外す。

### 範囲外・起票候補

- `jdbc-metadata.sh`・`jdbc-show-metadata.sh`・新 `lib.sh` の環境関数の 3 重複製 → 共通 lib
- `jdbc-show-metadata.sh:423-456` `compare_row_counts` の +1 が #60 後に古い
- `tools/e2e/minio/verify.sh` 660 行のヘルパ抽出
- `verify.sh:497` がサービス指定なしで `up -d`（鍵が無いと tls-proxy の bind mount が壊れる恐れ）
- `sdk-retry/check.py:6` の古いコメント
- trino-probe と request-errors のポート 8090 重複
- `probe.sh` に後始末が無い
- dbt-athena の `_is_plain_text_result` が EXPLAIN / SHOW FUNCTIONS の列名行を落とさない可能性（調査）
- ケース 13 で件数が返らないとき athena-local が `<id>.csv` 本体を書く可能性（観測したら）
- ArrowCursor（pyarrow のエンドポイント）

### 所要時間の見込み

1: 約 5 分／2: 約 25〜30 分（JVM 63 回）／3: 約 6〜8 分／4a: 約 10 分（重い並列作業と同時に流さない）／4b: 約 10〜20 分。

## 計画レビューの結果

Explore 3 本（契約・呼び出し元／再利用・一貫性／構造・判定。各 + 削減）。4 ゲート（単純さ→前提検証→決定照合→振動ガード）で裁いた。

採用（計画に反映済み）:
- **high** 再利用 1: 3.5.0 以下は `.metadata` の無い DDL で既知の NoSuchKey（`docs/result-files.md:155-158`）になり、46 のケース 3 と 57 の準備で `failures>0` → (6) が SHOW と無関係に FAIL。→ (6) は `RESULT SHOW_*` の行だけで判定、既知の NoSuchKey は別枠で数え result-files.md を実測で裏付ける
- **high** 構造 1: `.txt.metadata` への GET を照合から外すと、本体より先に `.metadata` を GET して 404 で止まる旧版（3.5.0 の NoSuchKey）が「GET 0 件」で PASS。→ `/<prefix>/` 前方一致で `.txt*` を数える。`msg` に Trino の理由が含まれることも条件
- **high** 契約 1: 中継ログと trace を足場全体で数えると canary/F3/E3 の STS 行で F1 が FAIL、先行 check の GetWorkGroup で E2 が PASS。→ check は逐次、`common.py` は開始時の行数から後だけを数える
- medium 契約 2: PREFLIGHT 失敗を一律 SKIP にすると「`.csv.metadata` を解けない」非互換が消える。→ その回の接頭辞への GET ≥1 なら FAIL、0 なら SKIP
- medium 契約 3: 4b の採用基準を `SHOW CATALOGS` から `SHOW SCHEMAS FROM hive/iceberg` が `error` 無しに変更
- medium 契約 4: `-e` での受け渡しは fetcher が空に潰れても 3.8.1 の red で検出できない。→ 既存どおり位置引数、`CONFIG` 行を judge が照合、URL 表示は実際の値
- medium 再利用 2: 証跡の置き場を e2e の既存（`mktemp -d /tmp/athena-local-issue111-*`）にそろえる
- medium 再利用 3・構造 5: verify.sh の終了コードを FAIL の件数に（minio の `main` も）
- medium 構造 2: 落ちないミューテーション 2 件を削除し、落ちる候補（`.csv` も数える／`CONFIG` 照合を外す）に差し替え
- medium 構造 3: 負荷が小さく 3600 側が 100MiB に届かない。→ `lpad` 1000 文字（≒2.2MiB/件）、ポーリング 0.1 秒、見積もりを計画に明記
- low 構造 4: JVM を `timeout 300` で包む（rc 124 は SKIP）
- low 再利用 4: 状態は PASS/FAIL/SKIP/INFO の 4 語
- low 再利用 5: trino.md の既存の表を拡張せず新しい項目に

削減（採用）: `docker compose port` の照合（契約・再利用）／ケース 14 の 2 回の stat を 1 回に（契約）／「iceberg だけ」の分岐（契約・構造 R2）／GetQueryResults×111 と PyAthena Cursor の「GET 0」は INFO（契約）／`-e` 渡し（契約）／旧版は `{auto,S3}×{57,111}`（再利用 2）／4a の compose は sdk-retry を流用（再利用 3）／ケース 13（再利用 4）／`FAILED: ` の内容で選ぶ抽出（構造 R1。接頭辞で分けたので `.txt` は全部失敗 DDL）／前提表の `down -v`・`parse_retention` の行（構造 R3）

棄却: 無し（指摘の無い観点: 契約 5・6、構造の (4)(8)(10)、判定の散らばり）。

design-checklist に足した教訓（high 3 件のうち「形」のもの 2 行）: 無いことの証拠（GET 0 件・呼び出し 0 件）を数える範囲を対照と同じ区間・接頭辞・パスの前方一致で閉じたか／既知の不具合が測りたい項目の判定に混ざらないよう合否の材料をその項目の出力行だけに絞ったか。

再レビュー: 構造の観点で計画を書き直してはいない（判定の規則と規模の修正）ので 2 回目は回さない。

## フェーズの記録

### フェーズ 1（(8) UPDATE/DELETE、(4) s3×保持期限）— 完了 2026-09-23 17:17
- 7-0: 触るのは `verify.sh`（source・`start_athena_local` の引数・`main` の末尾）と新規 `cases-dml-retention.sh`（144 行、最長 43 行）・README。`start_athena_local` の呼び出し元は `main` だけ（jdbc 2 本は自前の複製）。分岐: 10/11 の updateType・件数（`run_merge_case`）、12 の FAILED かつファイル無し（SUCCEEDED → FAIL、ファイルあり → FAIL）、14 の 400 → 残存確認（200 → SKIP、400 以外 → FAIL、無い → FAIL）、再起動失敗 → SKIP。すべて red・ミューテーションで踏んだ
- 変更前: 19 項目 PASS。red: 保持期限を渡さないと 14 が SKIP。green: 24 項目 PASS、rc 0
- 結果: UPDATE=`UPDATE`/1、DELETE=`DELETE`/1、`.metadata` 75 バイトで field 3 以降は MERGE と同じ `rows bigint`。Hive への UPDATE は `NOT_SUPPORTED: Modifying Hive table rows is only supported for transactional tables` で FAILED、何も置かない。保持 1 秒で捨てた後も `.csv` と `.csv.metadata` は残る
- ミューテーション 5 件とも落ちた（M1 `MERGE`/2 → FAIL、M2 反転 → FAIL、M3 200 → FAIL、M4 3600 → SKIP、M5 `.csvx` → FAIL）。終了コードが FAIL 件数になることも確認
- 計画から外れた点: 1 回目の green は snap の jq が `/tmp` のファイルを開けず FAIL（足場の不具合。標準入力で渡す形に直した）。`append_metadata_size` を足した（記録用）
- 7-6: 省略（テスト足場・既存パターンの拡張）

### フェーズ 2（(3) の JDBC 側、(6) 旧版 JDBC）— 完了 2026-09-23 17:36
- 7-0: `Main.java`（304 行。位置引数 3・4、CONFIG・PREFLIGHT 行、シナリオ 111）、新規 `FailedDdlScenario.java`（65）、`jdbc-drivers/{verify.sh,lib.sh,judge.sh}`（123/202/174）、minio compose の tls-proxy に `depends_on`。既存 46/57 の出力行は不変（接続失敗の rc が 1→3 になるが既存 2 本は rc=0 かつ failures=0 で PASS なので結論は不変）。分岐: CONFIG 不一致 → FAIL／PREFLIGHT 失敗 → GET ≥1 で FAIL・0 で SKIP／57 は SHOW 3 行だけで合否・準備の NoSuchKey は INFO／111 は 4 文の例外+理由・後続 SELECT・`.txt` 4 件・`.txt*` GET 0／GQR×111 は INFO／rc 124 → SKIP。すべて red・ミューテーションで踏んだ
- red: `3.8.1 0.0.0` で 3.8.1 が 8 PASS + 1 INFO、0.0.0 は SKIP、CONFIG 9 行一致、nginx のログの形式を確認（U5 解消: `GET /e2e-jdbc111/preflight/<id>.csv`）
- green（最終、7 版）: FAIL 0。3.8.1 3×3、3.5.0/3.4.0 は auto と S3、3.3.0〜3.0.0 は S3 で、SHOW 3 文は全版例外なく読めた（auto では `.txt.metadata` の読み込みログ 3 件）。失敗 DDL の 4 文は全版 `TABLE_NOT_FOUND ... does not exist` の SQLException で `.txt*` への GET 0、後続 SELECT 1 は通る。3.5.0・3.4.0 の auto は準備の `CREATE TABLE` で既知の NoSuchKey（INFO で別枠）。PREFLIGHT 失敗・metadata/protobuf 起因の例外は無し
- ミューテーション 4 件とも期待どおり（M1 SQLException を数える → 111 の 3 セル FAIL、M2 `.csv` も数える → 2 セル FAIL、M3a fetcher 空 → 6 セル FAIL rc=6、M3b 照合も外す → 全 PASS（逆証明））
- 計画から外れた点: preflight の Start に `ClientRequestToken` が無く SKIP で止まった（直した）、verify.sh の終了コードがサブシェルで 0 固定だった（直した）、3.0.0・3.1.0 は ServiceLoader 登録が無く本物判定を `AthenaDriver.class` でも通し `Class.forName` を足した（統括役の指示）、JVM に `-T` と `</dev/null`、統括役が REASON_RE を実測の文言 `NOT_FOUND|does not exist` に絞った（全 44 行が一致することを証跡で確認）
- 7-6: 省略（テスト足場・既存パターンの拡張）

### フェーズ 3（(1)(2)(7)(9)、(3) の PyAthena 側）— 完了 2026-09-23 17:40
- 7-0: すべて新規（`tools/e2e/python-clients/` 14 ファイル、約 880 行）。分岐: canary（STS・Glue が中継に届き 4xx）→ FAIL／trace の preflight 失敗 → (3)(9)(F3) を SKIP／PyAthena の preflight（`TRINO_CATALOG_MAP`）失敗 → SKIP／E2 でマクロから呼べない → SKIP／各判定。すべて red・ミューテーションで踏んだ
- red: 中継なし（8098 直接）で canary が FAIL、(9) は 1499 で FAIL
- green（再実行後）: PASS 32・INFO 12・SKIP 0・FAIL 0、「漏れ: 無し（canary で確認）」。E1 rc 0、E2 `ENFORCED=False` と GetWorkGroup 1、E3 は Glue `GetDatabases` で `UnknownOperationException`（SQL を投げる前）。F1/F2 `[[1,'a']]`・STS 0、F3 は STS が中継に届いて `ResponseParserError`。(9) 1500 行・1 行とも欠けず S3 GET 0。(3) PandasCursor は `OperationalError` で `.txt*` の GET/HEAD 0
- 判断（統括役）: PandasCursor × Iceberg DROP TABLE の `EmptyDataError`（1 バイトの `.txt`、本物と同じ形）は athena-local のずれではないので INFO にし、Hive の DROP TABLE（0 バイト）で PASS を取る（案 c）
- ミューテーション 7 件とも落ちた／未測定になった（`TRINO_CATALOG_MAP` を外す件は preflight を `information_schema.schemata` に変えて効くようにした）
- 計画から外れた点: canary の 4xx は botocore の `before-parse` で控える、(3) は HEAD も数える、jq は標準入力
- 7-6: 軽量レビュー 1 本（条件付き。コミット d121554 の差分に対して 17:29 起動）
- 7-6 の対応（統括役、17:32〜17:35）: 3 件を直して流し直し PASS 32・FAIL 0（証跡 `/tmp/athena-local-issue111-py.ose2ev`）。ミューテーション: none の check の冒頭で例外を投げる → `FAIL awswrangler-none 異常終了` で rc 1（`.4LgOeZ`）。落ちた

### フェーズ 4a（(5) メモリの頭打ち）— 完了 2026-09-23 17:58
- 7-0: 新規 `tools/e2e/retention/{verify.sh,load.py,README.md}`（134/205/40。compose は sdk-retry を流用）。分岐: (a)〜(e) の各判定、対照不成立 → SKIP、`/proc` 不可 → SKIP、暖機後の標本 3 未満 → SKIP、athena-local 起動失敗 → FAIL。docker 無しの `--judge` の単体確認 8 通りと red・ミューテーションで踏んだ
- red（短縮 90 秒）: R1 両方 3600 → FAIL（(d)(b)。(c) の修正後は 3 件）、R2 `ROWS=1` → SKIP（対照 3.6MiB）
- green（既定 240 秒×2、488 秒、証跡 `/tmp/athena-local-issue111-retention.KkZAgG`）: PASS。保持 3600: 完了 1577 件、VmRSS 300.7→3568.6MiB（伸び 3267.9、前半 1666/後半 1602）、最初の ID 200。保持 1: 完了 1854 件、68.4→86.3MiB（伸び 17.9、前半 14.8/後半 3.1）、最初の ID 400 `QUERY_EXECUTION_NOT_FOUND`。完了比 1.18。閾値の調整なし
- ミューテーション: M1 入れ替え → FAIL (d)、M3 (b) の向き反転（判定のみ）→ FAIL。落ちた
- 判断（統括役）: (c)「後半 < 前半」は対照側でも処理のペースが落ちて通ってしまう（red で観測）ので「後半 < 前半の 0.5 倍、または 16MiB 以下」に直し、green と R1 の証跡に `--judge` で流し直して green PASS・R1 FAIL 3 件を確認。負荷は見込みの約 7 倍（約 7 件/秒）で、`DURATION=3600` は対照側が数十 GiB になるので README に注意を足した（#121 に効く）
- 7-6: 軽量レビュー 1 本（cbb6bb1、18:02）。high 1 件を統括役が修正: 届かない athena-local（8199）に対して `load.py` が 20 秒以内に rc 1 で終わることを確認（修正前は標本スレッドで止まり続ける）。負荷の正常経路は変えていないので 240 秒の green は流し直していない

### フェーズ 4b（(10) 旧版 Trino）— 完了 2026-09-23 17:15
- 7-0: 触るのは `trino-probe/docker-compose.yml`（ポートの 1 行）と新規 `versions.sh`・`README.md`。呼び出し元 `probe.sh` と README の手順は既定 8090 のまま動く。分岐: 採用基準（3 catalog の順の試行）→ SKIP／nodeVersion 不一致 → FAIL／482 の期待不一致 → FAIL／旧版は INFO（差あり・同じ）／イメージ取得不可 → SKIP。すべて red・ミューテーションで踏んだ
- red: `TRINO_TAGS=482` PASS（trino.md:17-30 を再現）、`TRINO_TAGS=1` SKIP
- green: FAIL 0（2 分 51 秒）。480・475 = 482 と同じ（`catalog-legacy`）。440 = `connector_name` と D3/D4 は同じ、`CREATE SCHEMA` が file メタストアで失敗し D1/D2/C1/C2 は準備の失敗。470 = 3 構成とも起動せず（`fs.*` の設定名が無く、無しだと `No factory for location: file://`）。400 = JVM が cgroup v2 で NPE
- ミューテーション: 期待 `Hive` → FAIL／`catalog` 固定 → 480・440 が SKIP／`TRINO_TAG` を渡さない → 照合なしでは 470 が 482 の値で素通り、照合ありで FAIL。落ちた
- 計画から外れた点: D2/D3 の期待を足した（同じ probe_sql の対の値）。起動失敗の理由の抽出を WARN より ERROR 優先に。`versions.sh` は 238 行（関数は 50 行以内）。採用基準は計画のまま（案 a: 440 は測れた列だけ記録）
- 7-6: 省略（テスト足場・既存パターンの拡張）

## review-fix-loop の最新 nextArgs

（未使用）

## レビュー実施記録

| レビュー | 対象 | 件数 | 本番影響のある妥当指摘 |
|---|---|---|---|
| 軽量レビュー（7-6、フェーズ 4a） | cbb6bb1 | high 1（採用: 対照側が OOM で落ちると `call()` が `URLError` を捕まえず、標本スレッドが非 daemon なので `load.py` が止まり続ける。→ daemon + try/finally、`URLError`/`OSError`/JSON 不正を失敗として返す、README の長時間の例に `ROWS=100`） | 0 |
| 軽量レビュー（7-6、フェーズ 3） | d121554 | medium 3（採用 3: `run_check` が check の異常終了を FAIL に数えない／E2 の SKIP 条件に GetWorkGroup 0 件を足す／docs/clients.md の「STS も要る」は未観測の推測） | 0（本番コードは無い。足場と docs の修正） |

## Step 8 の結果

（未着手）

## dismissed

（無し）

## deferred

| 指摘 | severity | file | 再トリアージ |
|---|---|---|---|

## 覆した事前判断

| 元の判断 | 新しい判断 | 理由 | 影響 | ユーザーに聞いたか |
|---|---|---|---|---|
| issue 本文: 結果は clients.md（クライアント相手）と trino.md（Trino 相手）に書く | (D) 保持期限×S3 と (I) メモリの 2 件は caveats.md と unmeasured.md の済み節に書く | measurements の README が athena-local 自身の観測を除外している | docs の置き場だけ。外部契約・挙動・運用に影響なし | 聞いた（2026-09-23） |
| issue 本文: dbt-athena で `work_group` を設定して 1 回通す | `dbt debug` + `run-operation`（GetWorkGroup 呼び出し）まで。`dbt run` は Glue で止まる箇所の観測まで | `dbt run` は Glue・STS 必須、CTAS の方言も Trino で通らない見込み | 実測の到達範囲だけ | 聞いた（2026-09-23） |

## 実機検証

（この issue は実測そのものが成果物。Step 9 は最終の再実行に当てる）

## マージ後の周知事項

（未着手）

## 範囲外の発見

| 内容 | 出どころ | 起票 |
|---|---|---|
| 手元の `jq` は snap 版で `/tmp` のファイルを開けず、正規表現で日本語を壊す。足場は標準入力で渡す形にした | フェーズ 1・3・4b の実装 | CLAUDE.md に注意を書く（出荷の docs コミット）。既存の足場の見直しは #117 |
| PyAthena 3.36.0 の PandasCursor は Iceberg の `DROP TABLE` の 1 バイトの `.txt`（本物と同じ形）で pandas の `EmptyDataError` → `OperationalError`。本物 + PyAthena では未実測 | フェーズ 3 の実行 | #119 |
| `dbt run` は Glue の代役を入れても `GetTables` や CTAS の方言（`table_type`/`is_external`）が続く見込み | フェーズ 3 の実行 | #120（本物の Glue/STS で通す。人間検証） |
| Athena JDBC 3.0.0・3.1.0 の uber jar には `META-INF/services/java.sql.Driver` が無く、`Class.forName` が要る（3.2.2 から登録あり） | フェーズ 2 の実行 | 対応しない（measurements の備考に記録済み） |
| `Main.java` に PREFLIGHT を足したので `jdbc-show-metadata.sh` の `collect_metadata` の「`.csv.metadata` は 6」の INFO は 9 になる。`check_s3_access` は PREFLIGHT の GET だけでも満たされうる。環境関数の 3 重複製と `compare_row_counts` の古い +1 も同じ issue | フェーズ 2 の実装・計画レビュー | #115 |
| `catalog-nofsflag` は 470・440 では役に立たず（470 は `file://` の factory が無い）。Trino 400 は WSL2 の cgroup v2 で JVM が落ちる | フェーズ 4b の実行 | 対応しない（unmeasured.md に理由つきで残した） |
| `tools/e2e/minio/verify.sh` の 660 行超と、サービス指定なしの `up -d`（tls-proxy が証明書なしで上がる） | 計画・計画レビュー | #116 |
| trino-probe の既定 8090 が request-errors と重複、`probe.sh` に後始末無し、`sdk-retry/check.py:6` の古いコメント | 調査・計画レビュー | #117 |
| dbt-athena の `_is_plain_text_result` が EXPLAIN / SHOW FUNCTIONS の列名行を落とさない可能性 | 調査 | #118（調査） |
| 人間検証: 数時間の RSS の推移（`DURATION=3600`） | 計画の人間検証リスト | #121 |

## 委譲の記録

| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| Step 1〜2（issue・ブランチ・quick ノート） | 自分 | 規則どおり |
| Step 3 調査 | 委譲（Explore 3 本、opus） | 規則どおり |

## 出荷

（未着手）

## 時系列

- 2026-09-23 15:53 着手（quick-implement）。15:55 ゲート外れで escalate、quick ノートをコミット（3091033）、issue にコメント
- 2026-09-23 15:58 implement-issue 開始。Step 3 の Explore 3 本を起動（足場と JDBC 系／Python クライアント系／保持期限と旧版 Trino）。先行作業: 環境の確認（Docker・イメージ・Python）を自分で実施
- 2026-09-23 16:13 Explore 3 本が返った（返答の原文は scratchpad の explore-111.md）。Plan 1 本を起動。先行作業: 旧版 JDBC の URL（3.0.0〜3.8.1 が 200）と旧版 Trino のイメージ（380〜480 に arm64 あり）、Python 3.14 の wheel（awswrangler・pandas・pyarrow が取れる）を自分で確認。Step 4 の質問（dbt の到達範囲、(D)(I) の書き先）を Plan と並行して出す
- 2026-09-23 16:17 Step 4 の 2 点にユーザーが回答（dbt は debug + run-operation まで／(D)(I) は caveats.md と unmeasured.md）。先行作業: 旧版 Trino の pull、旧版 JDBC jar の取得、Python 依存の取得をバックグラウンドで開始
- 2026-09-23 16:31 Plan が返り、計画をノートに転記。未検証の前提の裏取り Explore 1 本を起動。計画レビューに渡す既存関数の本文を scratchpad に抽出
- 2026-09-23 16:37 裏取り Explore が返る（前提はほぼ真。U5 を追加、中継の変数名と probe.sh の SQL エラーの扱いを計画に反映）。Step 6 の計画レビュー Explore 3 本を起動
- 2026-09-23 16:40 レビュー待ちの先行作業: release ビルド完了、MinIO の trace の形式を確認（U4 解消）、tls-proxy の起動競合を発見（フェーズ 2 で depends_on を足す）
- 2026-09-23 16:49 計画レビュー 3 本が返る（high 3・medium 8・low 3、削減 10）。全件採用して計画を修正、design-checklist に 2 行追加
- 2026-09-23 16:49 計画確定をコミット（9ca90aa。docs 単独 1 本目）。Step 6b で設計を提示して承認待ち。先行作業: 残りは無し（イメージ・jar・venv・ビルド・trace の形式は済み）
- 2026-09-23 16:51 設計承認。フェーズ 1・3（実装＋実行）と 2・4a・4b（実装のみ、実行は後）を general-purpose 5 本に並行委譲。委譲の記録: 7-2 は全フェーズ委譲（規則どおり）。docs（measurements・unmeasured・caveats・docs/clients.md）は同時編集の衝突を避けるため委譲先には触らせず、記録ファイルの文面を統括役がコミット時に写す
- 2026-09-23 17:02 フェーズ 4b（versions.sh 217 行）と 4a（verify.sh 134・load.py 203）の実装が返る。構文チェックと docker 無しの判定の模擬は済み。実行は Trino の同時数（2 まで）と負荷の都合で後回し（4b はフェーズ 1 か 3 の後、4a は最後に単独）
- 2026-09-23 17:05 フェーズ 2 の実装が返る（Main.java 304 行、FailedDdlScenario 65、jdbc-drivers の 3 本 123/199/174。mvn のコンパイル OK、偽ログでの判定確認済み）。実行はフェーズ 1 の compose が空いてから
- 2026-09-23 17:14 フェーズ 4b 完了（FAIL 0）。trino.md に項目を足し unmeasured.md を更新してコミット
- 2026-09-23 17:16 フェーズ 1 完了（24 項目 PASS）。trino.md・caveats.md・unmeasured.md に写してコミット。フェーズ 2 の実行を開始させた
- 2026-09-23 17:23 フェーズ 3 が返る（PASS 28・INFO 10・FAIL 2）。FAIL は PyAthena PandasCursor × Iceberg DROP TABLE の 1 バイト .txt（本物と同じ形）で pandas が落ちるもの。判断: INFO に落とし Hive の DROP TABLE で PASS を判定（案 c）、直して再実行させた。フェーズ 2 は実行中
- 2026-09-23 17:27 状況確認（ユーザーの問い）: フェーズ 2 は green 完了（3.8.1 全 PASS、3.5.0〜3.2.2 PASS）でミューテーション中。3.1.0・3.0.0 の SKIP は足場の本物判定（services ファイル）の誤りと統括役が jar で確認し、Class.forName で直して再実行を指示。フェーズ 3 は再実行中
- 2026-09-23 17:28 フェーズ 3 完了（PASS 32・FAIL 0）。clients.md（measurements と docs）・unmeasured.md に写してコミット。軽量レビューを起動
- 2026-09-23 17:33 フェーズ 3 の軽量レビューが返る（medium 3、全採用）。統括役が直して python-clients を流し直し中（green + run_check のミューテーション）。委譲の記録: この修正は自分（3 ファイルの数行、指示を書く方が長い）
- 2026-09-23 17:36 フェーズ 3 のレビュー対応をコミット（レビュー対応 1 本目）
- 2026-09-23 17:39 フェーズ 2 完了（7 版 FAIL 0）。clients.md・result-files.md・unmeasured.md に写してコミット。フェーズ 4a の実行を開始させた（単独）
- 2026-09-23 17:41 範囲外の発見と人間検証リストを起票（#115〜#121）。フェーズ 4a 実行中
- 2026-09-23 17:59 フェーズ 4a 完了（green PASS）。(c) を直して判定を流し直し、caveats.md・unmeasured.md に写してコミット。軽量レビューを起動
- 2026-09-23 18:00 7-7: 分割不要（新規ファイルの最大は trino-probe/versions.sh 238 行と Main.java 304 行（既存の拡張）。関数はいずれも 50 行以内で責務は 1 つずつ。分割は #116（verify.sh のヘルパ抽出）・#115（jdbc の環境関数の共通化）として起票済み）。フェーズ 4a の軽量レビューを起動、標準コマンド（fmt・clippy・test）を最小構成で 1 回流す（Step 9 の最小構成の検証）
- 2026-09-23 18:00 Step 9 最小構成の検証: fmt ok、clippy ok（警告 0）、cargo test --locked 全 green（src は触っていない）
- 2026-09-23 18:02 フェーズ 4a のレビュー対応をコミット（レビュー対応 2 本目）。Step 8: 最終パス 3 本と独立レビュー 1 本を起動
