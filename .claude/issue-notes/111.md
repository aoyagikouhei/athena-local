# quick #111 本物の Athena が要らない未実測 10 件を手元の e2e で潰す
- 入力: issue #111。モード: standalone。ブランチ main-111、着手前 SHA f972b67
- ゲート: サイズ 0 行・0 ファイル（src は触らない見込み。docs/dev と tools/e2e だけ）／呼び出し元 0／契約: 通過／新規性: 外れ（tools/e2e にクライアントごとの足場を 4〜5 系統新設、旧版 Trino・旧版 JDBC のイメージ）／事実: 外れ（実測 10 ラウンド。quick の予算は 1〜2 ラウンド相当）／判断: 外れ気味（メモリ頭打ちの基準、保持期限と S3 の組み合わせの期待値）→ 外れた観点 3 つ
- 時系列: 着手 15:53 ／ red — ／ green — ／ コミット 15:55（ノートのみ）
- 検証: 標準コマンド未実行（コードを触っていない）／ミューテーション: 無し
- 自己レビュー: 無し（コードを触っていない）
- 打ち切り・escalate: 提案あり（事実: 実測 10 ラウンド、新規性: 新規足場 4〜5 系統、変更サイズ: 長時間の負荷試験を含む）→ 選択: implement-issue で進める。stash 無し
- 予算の実績: Bash 約 5 本、所要 2 分、サブエージェント 0 本
- 出荷: ブランチ main-111（このノートのコミットだけ。実装は implement-issue が積む）

## 引き継ぎ

- 判定箇所: コードの変更は想定していない。実測で本物と食い違う項目が出たら、その都度別 issue を切る（親 #114 の方針）
- 対象 10 件の出典: `docs/dev/unmeasured.md` の「実クライアントでの疎通」9 件と「Trino」1 件。結果の書き先は `docs/dev/measurements/clients.md`（クライアント相手）と `docs/dev/measurements/trino.md`（Trino 相手）。書いたら unmeasured.md の「済み」節へ移す
- 共有できる既存機構:
  - `tools/e2e/minio/`（Trino 482 Hive/Iceberg + MinIO + TLS 終端 nginx + JDBC 3.8.1 クライアント `jdbc-client/`。README に aws CLI が Docker ラッパで compose ネットワークに届かない注意あり）
  - `tools/measure/jdbc-metadata.sh`・`jdbc-show-metadata.sh`（公式 JDBC ドライバの取得 URL はバージョン埋め込み。`DRIVER_VERSION="3.8.1"` を変えれば 3.5.1 未満も取れる見込み。未確認）
  - `tools/e2e/trino-probe/`（`TRINO_TAG` で Trino の版を切り替えられる。旧版の `system.metadata.catalogs` はここで測れる）
  - `tools/e2e/sdk-retry/`（boto3 の venv の作り方の先行例。Python クライアントは venv + pip。この環境の `uv` は astral の uv ではない）
  - `tools/measure/retention-stress.sh`（CPU 飽和下で tests/retention を回す雛形。メモリ頭打ちの測定はこれを土台に長時間版を作る）
  - 保持期限の実装: `src/store.rs` の sweep（`completed_at` から `retention` 秒で捨てる）。`src/config.rs` の `ATHENA_LOCAL_RETENTION_SECONDS`（既定 3600）と `ATHENA_LOCAL_RESULTS`（`none`/`s3`）
  - 失敗した DDL の `<id>.txt`（`FAILED: ` + 理由）は `src/operation/result_output.rs` の `write_failure` が置く
- 原因の仮説（各項目の期待）:
  - dbt-athena・awswrangler・PyAthena は `ListWorkGroups` を呼ばない（#9 のソース読み）。`GetWorkGroup` の応答で読む項目は clients.md「クライアントが GetWorkGroup の応答から読む項目」に列挙済み
  - 失敗 DDL の `.txt` はどのクライアントも FAILED を先に見て読まない想定（#6）
  - `ATHENA_LOCAL_RESULTS=s3` で捨てた後も結果 CSV は S3 に残る想定（athena-local は S3 を消さない）
  - `UPDATE`/`DELETE` は `MERGE`（#56、Trino 482 + MinIO Iceberg）と同じ経路で通る想定
- 未実測・未確認の一覧（issue 本文の 10 件そのまま）:
  1. dbt-athena で `work_group` を設定して 1 回通す（#2）
  2. awswrangler `read_sql_query(ctas_approach=False)` が `GetWorkGroup` で例外にならない（#2）
  3. 失敗 DDL の `<id>.txt` を PyAthena・JDBC 3.x が読んでも壊れない（#6）
  4. `ATHENA_LOCAL_RESULTS=s3` と保持期限の組み合わせ
  5. 長時間運用でメモリが頭打ちになるか（保持期限による破棄の実効性）
  6. 暗号化系 `SHOW` の素の protobuf `.txt.metadata` を JDBC 3.5.1 未満が読めるか（#5）
  7. PyAthena・awswrangler で退行が無いこと（#5）
  8. `UPDATE`/`DELETE` の実機確認（#5）
  9. awswrangler が `GetQueryResults` を読む経路での先頭行の扱い（#60）
  10. Trino 482 より古い版の `system.metadata.catalogs` の `connector_name` などの値（#39）
- 再現手順: 無し（欠陥ではなく実測）。足場は `cd tools/e2e/minio && ./verify.sh` が起点
- stash 名: 無し
