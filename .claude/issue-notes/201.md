# quick #201 tests/idempotency.rs の 50 本同時送信のテストがまれに trino_sqls().len() == 1 で落ちる
- 入力: issue #201。モード: standalone。ブランチ main-201、着手前 SHA 4b77cbd
- ゲート: サイズ 本番 0 行・0 ファイル（≤50・3。テスト 1 ファイル +12/-6）／呼び出し元 0（本番に触れない）／契約・新規性・事実・判断 → 通過（事実は spawn_query の先頭に一時的に 200ms の sleep を入れて決定的に再現）
- 時系列: 着手 12:10 ／ red 12:10 ／ green 12:11 ／ コミット 12:13
- 検証: `tools/dev.sh cargo fmt --check` ok、`tools/dev.sh cargo clippy --all-targets --locked -- -D warnings` ok、`tools/dev.sh cargo test --locked` ok（502 passed）／遅延ありの全件（--no-fail-fast）で落ちるのは idempotency.rs の 4 本だけ（:31・:58・:93・:121）／ミューテーション: 遅延ありで 50 本のテストから完了待ちを外す → 落ちた
- 自己レビュー: 分岐は増えない（テストに待ちを足しただけ）／エラーの出口なし／呼び出し元なし。原因は StartQueryExecution が tokio::spawn で実行を起こして先に返るため、応答直後に Trino の記録を数えると POST 前で 0 本になりうること。Store::submit の判定（1 ロック）は無関係
- 打ち切り・escalate: 提案なし
- 予算の実績: Bash 約 14 本、所要 3 分、サブエージェント 0 本
- 出荷: main-201
