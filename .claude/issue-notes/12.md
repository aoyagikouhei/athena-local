# quick #12 Iceberg の CTAS の OutputLocation は tables/ が付かない

- 入力: issue #12。モード: standalone。ブランチ `main-12`、着手前 SHA cb13d22
- ゲート: サイズ 本番 14 行・1 ファイル（テスト 24 行は別枠。≤50・3）／呼び出し元 1（`ResultFile::of` は `src/operation.rs:219` だけ。≤5・同一モジュール）／新規性・事実は通過、**契約と判断は外れたまま続行をユーザーが選択**（契約: `OutputLocation` は README 215 行の表に書いた挙動／判断: issue が「判定を入れる」か「README に差分として書く」かを決めていなかった → 「判定を入れる」をユーザーが選択）
- 時系列: 着手 07:22 ／ red 07:29 ／ green 07:31 ／ コミット 07:34
- 検証: `cargo fmt --check` ok、`cargo clippy --all-targets --locked -- -D warnings` ok、`cargo test --locked` ok（208 passed / 0 failed）／ミューテーション: `is_iceberg_table(query)` の判定を潰して常に `Table` にする → `iceberg_の_ctas_だけ_tables_を付けない` が落ちた（left: Table, right: Manifest）
- 自己レビュー: 分岐 2 件（Iceberg → `Manifest` / それ以外 → `Table`）はどちらもテストあり、CTAS でない `CREATE TABLE ... (i int) WITH (table_type = 'ICEBERG')` も `Text` で固定。エラーの出口は無し（戻り値は enum だけ）。呼び出し元 1 箇所では `Manifest` になるとキーが `<id>` になるだけで、本体を書かない（`write_result` の `should_write` は Text と Csv のみ）・`failed()` は None・Content-Type は未使用という点は `Table` と同じ
- 打ち切り・escalate: 無し（ゲートの 2 観点は記録して続行）
- 予算の実績: Bash 約 13 本、所要 12 分、コミット 1
- 出荷: standalone。ブランチ `main-12`（PR 番号は `gh pr list --head main-12` で引ける）
