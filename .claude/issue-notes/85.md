# quick #85 GetQueryResults / ListWorkGroups のページング検証で未実測のまま残した 3 点
- 入力: issue #85。モード: standalone。ブランチ `main-85`、着手前 SHA 64c9670
- ゲート: サイズ 本番 23 行・2 ファイル（≤50・3。`query_execution.rs` +19/-6、`validation.rs` の doc 4 行）／呼び出し元 1（`get_query_results` ← `handler.rs`。≤5・シグネチャ不変）／契約: 通過（本物に合わせる修正。README・CHANGELOG 同梱）・新規性: 新ファイル無し・判断: issue が「測って揃える」と決めている → **事実が外れた**（3 点とも未実測が中身）
- 時系列: 着手 11:21 ／ 実測スクリプト作成・コミット 11:24 ／ 実測 1 ラウンド目 11:25（重いクエリが `sequence` の 5 万件制限で FAILED。他は測れた）／ red 11:26 ／ green 11:27（枠組みの上限 100000・0 行の UTILITY のトークン無視）／ スクリプト修正・コミット 11:28 ／ 実測 2 ラウンド目 11:29（RUNNING／CANCELLED とトークン発行の規則）／ red 11:30 ／ green 11:31（満杯のページにトークン）／ コミット 11:33
- 検証: `cargo fmt --check` ok、`cargo clippy --all-targets --locked -- -D warnings` ok、`cargo test --locked` ok（324 passed）／ミューテーション: 満杯の条件を「残りがあるとき」に戻す → 落ちた、0 行の無視を消す → 落ちた、枠組みの上限を外す → 落ちた／実測: 2 ラウンド（1 ラウンド目 13 項目 + 未測定 10、2 ラウンド目 33 項目すべて）
- 自己レビュー: 分岐 3 件（`Some(_) if rows.is_empty()`／`filter(<= len)`／`end - offset == limit`）すべて `tests/select.rs` の新テスト 3 本が踏む。既存 2 本はトークン `"3"`・`"1"` が満杯ルールで正当な値になったので `"4"`・`"abc"` に変えた／エラーの出口: 新しいエラー無し（枠組みの上限は既存の `paging_violation`）／呼び出し元 1 箇所: 不変
- 打ち切り・escalate: はしご（事実 → 実測工程 2 ラウンド）。選択: 層を足して続行（ユーザー）。予算: 実測 2 ラウンドで Bash 60 本・40 分が上限。実績は内
- 予算の実績: Bash 約 16 本（うち実測 6 本）、所要約 10 分（着手 11:21 からコミット 11:33 の 12 分から実測待ち約 2 分を除く）、サブエージェント 1 本（止めない独立レビュー）
- 出荷: ブランチ `main-85`（PR 番号は `gh pr list --head main-85` で引ける）
