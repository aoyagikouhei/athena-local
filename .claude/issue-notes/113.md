# issue #113: 本物の Athena に投げる未実測 28 件を 1 本のバッチで測る

## 基本情報

- ベース: main（96b3a99）。ブランチ: main-113。着手前 SHA（レビューの base）: e25aa74（過去ノートの削除だけ）
- 親: #114。進め方: (1) バッチを作って TARGET=local でドライラン → (2) ユーザーが本物で 1 回実行 → (3) 生データを docs/dev へ転記

## 事前に確認した事実

Explore 3 本（sonnet）の返答の全文は計画の材料として Plan に渡した。ここには判断に効く事実だけを置く。

1. 28 件のうち既に答えがあるもの: #2 失敗時の GetQueryResults（measurements/result-files.md:270-321、#6。caveats.md:274-279 にも反映済みで、unmeasured.md:10 だけが古い）、#21 EnableMinimumEncryptionConfiguration（work-groups.md:34-48、#129・#137）、#26 x-amzn-errortype（errors.md:25,44 ほか 4 ラウンドで「付かない」。src/response.rs:22-28,86-89 は付けている）、#27 ErrorCode キー（~/athena-parse-errors-measurements/raw-20260923-114710/summary.txt:571-573。MALFORMED_QUERY でも付く。errors.md への転記漏れ）、#28 `(SELECT 1)`（~/athena-content-type-measurements/run-20260923-065415/g10-parenthesized.execution.json。DML/SELECT。空白入りは未実測）
2. 追跡漏れの "not measured": docs/caveats.md:55（ブロックコメントを弾くほかの文）、docs/caveats.md:156（複合値の中の varbinary）。unmeasured.md にも #113 にも無い
3. tools/measure に TARGET=local の前例は無い。raw-*.py は `https://athena.{region}.amazonaws.com/` 固定（raw-parse-errors.py:89）。MinIO に aws s3 を向けた前例も無い（mc だけ）
4. 共通の lib は作らない（docs/dev/decisions.md:47、#128）。例外は jdbc 系 3 本（decisions.md:48-50、#115）。raw-paging-leftovers.py は raw-get-query-results.py を importlib で読み込む（:44-58）
5. ローカルの Trino のテーブルはコンテナのローカルファイル（tools/compose/catalog/）。MinIO は結果だけ。Athena 固有の DDL（LOCATION 's3://'、table_type='ICEBERG'）は Trino で通らない
6. athena-local は CreateWorkGroup/DeleteWorkGroup を持たない（src/handler.rs:36-42）。GetWorkGroup は名前を無視して既定の出力先を返す（src/operation/work_group.rs:47-53）
7. 保持期限の実測は 60 秒まで（client-request-token.md:12）。src/config.rs:9 の 1 時間は athena-local の都合
8. 過去の生データは ~/athena-*-measurements に 23 ディレクトリ・約 55 ラウンド。上記 1 以外の対象項目の答えは無い（WORKGROUP2 は 2 回とも未設定）

落とした番号: Explore 1 の 3（src の未実測コメント一覧）と Explore 2 の 9（行数）は計画のフェーズ 3 と構成で使うので計画側に回した。Explore 3 の A2・A3 の細部（環境変数の一覧）は計画の TARGET=local の節に回した

## 実測の手順

（未着手）

## 実測の結果

（未着手）

## 実測の経緯

## 設計判断

- Step 4（2026-09-24、ユーザー）: 既に答えがある 5 件（2・21・26・27・28）はバッチに入れず文書フェーズで転記。27 と 28 は答えの形が少し違うので 1 本ずつバッチにも入れる
- Step 4: ListWorkGroups の既定ページサイズ（51 個のワークグループが要る）は測らない →「測れないもの」へ
- Step 4: caveats.md:55・156 の 2 件もバッチに入れ、unmeasured.md にも載せる
- 6b（2026-09-24、ユーザー）: ListWorkGroups の順序の確認のためのワークグループは作らない。w2 はバッチから外し、unmeasured.md には「3 個の根拠のまま」と残す
- 統括役: m4（列の field 2/3）は metadata.md:33 で観測済み（実テーブルの SELECT でも無い）→ 測らずに「測れないもの」へ

## 計画

Plan（sonnet）の下書き 13 項目を転記。統括役が直した点: (a) m4（列の field 2/3）は metadata.md:33「実テーブルの SELECT でも無い」で観測済み → バッチから外し文書フェーズで「測れないもの」へ (b) r2 は SELECT(.csv) ではなく `.txt` の文（DESCRIBE・SHOW TBLPROPERTIES）で測る (c) t3 の境界値を「文字数とバイト数で判定が割れる値」に (d) t4 の短いトークンは CLI が送信前に弾くので生 HTTP (e) 本物の Iceberg の作り方は CTAS の `WITH (table_type='ICEBERG', location=…, is_external=false)`（手本 ctas-iceberg-detection.sh:422）。NOT NULL は Athena の Iceberg DDL で作れるか分からないので試して記録 (f) ONLY に t6/t7 が無ければ保持期限の段階 A を走らせない（ONLY の再実行で 65 分待たない） (g) local の Iceberg は TRINO_CATALOG_MAP を使わず、フィクスチャの作成だけ TARGET で文面を分け、測る文は同じにする (h) ファイルを 20 → 11 に寄せる。落とした番号: 13 の副次案（t7 の StopQueryExecution を落とす）は Plan 自身が不採用

### 進め方とフェーズ
- フェーズ 1: バッチ本体（ドライバ・lib・A 項目）＋ TARGET=local のドライラン
- フェーズ 2: 生 HTTP（B 項目）と保持期限の 2 段構え ＋ 全項目の local ドライランと受け入れ判定 → ユーザーに依頼（本物で 1 回、70〜80 分）
- フェーズ 3（ユーザーの実行の後）: 生データを docs/dev/measurements へ転記、unmeasured.md・caveats.md・src の「未実測」コメントを直す。既知 6 件（2・9(m4)・21・26・27・28）の転記。本物との差分は起票
- 既知 6 件の転記はユーザーの実行を待たずにできるので、実行待ちの間に進める（先行できる作業）

### ファイル構成（tools/measure/unmeasured-batch/、見込み）
| ファイル | 責務 | 行 |
|---|---|---|
| run.sh | 環境変数・ONLY・RUN_DIR・WAIT_RETENTION の解釈、preflight（aws CLI で SELECT 1、生 HTTP で ListWorkGroups の 2 本を独立に。real では GetWorkGroup(WORKGROUP2) の OutputLocation が空なら止める）、項目の実行順、trap（保険） | 180 |
| lib-aws.sh | 文を投げて待って保存（execution.json・全ページの results・本体と .metadata のバイト列）、S3 の取得、再試行、redact、summary.tsv の 1 行 | 200 |
| lib-local.sh | TARGET=local の環境（compose の trino/minio/minio-init を専用プロジェクト名で上げ下げ、athena-local のビルドと起動。AWS_* をダミー（minioadmin）に差し替え、誤って本物に飛んでも認証で落ちるようにする） | 120 |
| raw.py | SigV4 の生 HTTP（エンドポイントは必須の引数で既定値を持たない）と B 項目（t2・t3・t4・e2） | 200 |
| report.py | summary.tsv → summary.txt（冒頭に本物への呼び出し回数・DDL の内訳、実名のマスク） | 120 |
| items-result-files.sh | r1〜r4、x1、x2 | 200 |
| items-metadata.sh | m1〜m3、m5 | 180 |
| items-statements.sh | s1、s2、p1 | 120 |
| items-token.sh | t1、t5、t8（t4 の CLI で送れる組） | 160 |
| items-retention.sh | t6・t7（段階 A / B） | 120 |
| items-workgroup-errors.sh | w1、e1 | 100 |
- 共通 lib の判断: decisions.md:47 は「別々の足場の間で共有しない」決定。今回はバッチ 1 本の中のファイル分割（行数の目安を守るため）で、外から source しない → 当たらない。tools/e2e/minio/lib.sh は source せず、起動の部分を lib-local.sh に複製する
- 生データ: `${DEV_HOST_HOME:-$HOME}/athena-unmeasured-batch-measurements/run-<日時>/<項目 id>/`。RUN_DIR を渡せば同じ run に追記

### 項目（投げるもの / 対照 / 前提 / local で期待すること）
- r1 CREATE TABLE の重複: Hive（CREATE EXTERNAL TABLE … LOCATION）と Iceberg（CTAS）でそれぞれ同名を 2 回 / 1 回目 / DDL / 2 回目が FAILED で、.txt・.csv の有無を保存できる
- r2 .txt の値のタブ・改行・NULL: Hive テーブルの列コメントと TBLPROPERTIES の値にタブ・改行を入れ、DESCRIBE と SHOW TBLPROPERTIES / 素の値のコメント・プロパティ、コメント無しの列（NULL） / DDL / local は Hive の DDL が通らないので失敗を記録
- r3 失敗した SHOW FUNCTIONS: `SHOW FUNCTIONS LIKE 'x' ESCAPE 'ab'`（2 文字の ESCAPE）/ 成功する SHOW FUNCTIONS / 無し / local も失敗させられる
- r4 SHOW CREATE VIEW の Content-Type: CREATE VIEW → SHOW CREATE VIEW → DROP VIEW / SHOW CREATE TABLE / DDL
- m1 5 型の field 7/8/10: timestamp with time zone・time with time zone・interval year to month・uuid・ipaddress / 同じ SELECT に timestamp・time・interval day to second / 無し
- m2 0 件の UPDATE・DELETE・MERGE の field 3: Iceberg テーブルに WHERE false / WHERE で 1 行 / DDL
- m3 0 行の CTAS の field 3: Iceberg の CTAS（is_external=false。DROP で S3 のデータも消える）で `… AS SELECT 1 AS n WHERE false` / 1 行の Iceberg CTAS / DDL
- m5 Nullable（field 9）: Iceberg で NOT NULL 列を持つテーブルを作って SELECT（作れなければ記録して「測れない」へ）/ NULL 可の列 / DDL
- s1 REPLACE COLUMN（単数形）: Hive テーブルに / 複数形 REPLACE COLUMNS / DDL
- s2 DROP PARTITION × Iceberg: パーティション付き Iceberg テーブルに / 同じ文を Hive に / DDL
- t1 Catalog の差: 同じトークンで Catalog だけ変える（大文字小文字違いと、実在しない名前）/ 全部同じ再送 / 無し / local は Catalog を比べないので同じ ID
- t2 トークンの正規化（生 HTTP）: 前後空白・`"`・`\`・大文字化の 4 変種を同じ文で / 変えない再送 / 無し / local は全部別トークン扱い
- t3 長さはバイトか文字か（生 HTTP）: 20 文字（60 バイト）と 50 文字（150 バイト）のマルチバイト / ASCII 32・128（既知）/ 無し / local は文字数
- t4 検証の優先順位（生 HTTP）: 短いトークン × 不正な OutputLocation、短いトークン × 構文エラー、不正な OutputLocation × 構文エラー / 1 つずつ / 無し / local は token → OutputLocation → 構文（execution.rs:41-73）
- t5 #102: 1 回目成功 → 同じトークンで OutputLocation だけ不正 / QueryString だけ構文エラー / 1 回目 / 無し / local は検証エラーが先
- t6・t7 保持期限: 段階 A（最初）でトークン付きの実行を 1 本 → 他の項目 → 65 分待ち → 段階 B で同じトークンの再送・GetQueryExecution・StopQueryExecution / 段階 A 直後の再送 / 待ち（WAIT_RETENTION=0 で省略。local は ATHENA_LOCAL_RETENTION_SECONDS を短くして待ちも短く）
- t8 省略と既定値の明示: Database 省略 vs `default`、OutputLocation 省略 vs 明示（WORKGROUP2 の出力先と同じ値。無ければ skip）を同じトークンで / 完全一致の再送 / WORKGROUP2 / local は別物（Conflict）
- w1 WORKGROUP2 の GetWorkGroup: / primary / WORKGROUP2（ユーザーがコンソールで出力先を設定）/ local は primary と同じ
- w2 ListWorkGroups の順序: 外す（6b。作成・削除をしない）
- e1 QUEUED の GetQueryResults: 軽い文を 5 本続けて投げ、それぞれの直後に GetQueryExecution と GetQueryResults（重い計算は実行時間を延ばすだけで QUEUED を延ばさない）/ RUNNING（既知）/ 捉えられなければ記録
- e2 構文エラーの ErrorCode キー（生 HTTP）: `SELEC 1` を 1 本
- p1 `( SELECT 1 )` と改行入り / `(SELECT 1)`（既知）
- x1 ブロックコメントを弾くほかの文: DESCRIBE・SHOW PARTITIONS・SHOW TBLPROPERTIES・SHOW COLUMNS・SHOW CREATE VIEW・MSCK REPAIR TABLE・CREATE EXTERNAL TABLE の動詞の後に `/* c */` / コメント無しの同じ文 / DDL（既存のテーブル・ビューを使う）
- x2 複合値の中の varbinary: array・map・row に X'0102' / トップレベルの varbinary（既知）

### 依拠する既存機構（前提 / 破らないか / 保証）
- tools/dev.sh と compose の AWS_* の引き継ぎ、DEV_HOST_HOME の出力先 / 変えない / tools/dev.sh・compose.yml の dev
- athena-local は SigV4 を検証しない（local の生 HTTP はダミーの資格情報で署名してよい）/ 変えない / CLAUDE.md「SigV4 は検証しない」、src/handler.rs
- local の比較の相手（Fingerprint に Catalog が無い store.rs:87-96、検証順 execution.rs:41-91、chars().count() execution.rs:117-119、保持 config.rs:9・store.rs:25-40、GetWorkGroup は名前を無視 work_group.rs:40-64）/ バッチは athena-local を変えない
- local の Iceberg はローカルファイル（tools/compose/catalog/iceberg.properties）/ フィクスチャの文面だけ分ける
- 同時実行は COMPOSE_PROJECT_NAME で分ける（docs/dev/development.md:70）/ 専用の名前を既定にする

### 後始末（計画レビュー 3-1 を受けて）
- 作ったテーブル・ビューは、その項目の最後にその場で消す（最後にまとめない。資格情報が 1 時間で切れても、それまでの項目の後始末は済んでいる）。trap は保険として同じ DROP IF EXISTS を流す
- 保持期限の段階 B は最後。段階 B の失敗は t6/t7 だけに閉じる
- Hive の外部テーブル（r1・r2・s1・x1 の Hive、LOCATION 付き）は DROP で S3 のデータが消えないので、LOCATION を cleanup-hints.txt に書き出す（手本 drop-table-format.sh）。LOCATION は OUTPUT の下の専用プレフィックスにし、中身は作らない（空のテーブル）
- 依頼文に「資格情報は 90 分以上有効なもの」と書き、summary の冒頭に開始時刻と「開始 + 90 分」を出す

### 受け入れ判定（ドライラン）
1. TARGET=local で全項目が行を出し、summary.tsv の行数 = 宣言した項目数（抜けが 0）。local の Trino で本物の文が通らない項目（r1・r2・s1・s2・m5・x1 の Hive/Iceberg の DDL）は「local では FAILED が期待どおり」と宣言し、その通りかを見る
2. local の生 HTTP の送り先が athena-local だけであること。summary の送り先の URL を期待値（127.0.0.1:8087）と突き合わせ、違えば FAIL。わざと real のエンドポイントを渡して FAIL（とダミー資格情報による認証エラー）になることも確かめる
3. ONLY=r3 で r3 以外が増えない。RUN_DIR を渡すと同じ run に追記
4. WAIT_RETENTION=0 で待たずに t6/t7 が skip と出る。local の短い保持期限で t6 の「期限内は同じ ID、期限後は別」が再現する
5. local で決まった答えが出る項目（t1 同じ ID、t2 全部別、t3 文字数、t4 token 先、t5 検証先、t8 Conflict、w1 primary と同じ、p1 DML/SELECT）がその通りに summary に出る（足場が測れている証拠）。わざと 1 つ期待と違う値を与えて summary に「期待と違う」と出ることも確かめる
6. 後始末: 走行後にテーブル・ビュー・コンテナが残らない
7. 統括役が自分の目で: 本物への呼び出し回数と DDL の内訳が summary の冒頭に出る、生ログに Authorization・署名・キーが残らない、実名がマスクされる

### 人間検証リスト
- 本物での 1 回の実行（WORKGROUP2 を用意してもらう）。フェーズ 3 はその結果に依存

### 先行できる作業
- 既知 6 件の文書転記（いつでも）
- ユーザーへの依頼文（フェーズ 2 の終わり）

## 計画レビューの結果

Explore 3 本（sonnet、判別力 / 構成と削減 / 実測の要件と安全）。採用 7 件、棄却 0 件。
- 構成 1（high）local の生 HTTP が本物に飛ぶ経路と、受け入れ判定 2 が表示だけ → 採用: raw.py の送り先を必須引数に、local は資格情報をダミーに、判定 2 を突き合わせにしてわざと壊す確認を足す
- 構成 2（medium）r1・r2・s1 は local で本物の文が通らない → 一部採用: local でも走らせるが「FAILED が期待どおり」と宣言する（外すと保存の配線が local で一度も通らない）
- 構成 3（low）local の設計は前例と過不足なし → 記録のみ
- 判別力 1（high）WORKGROUP2 の出力先が未設定で w1 が 3 度目の skip → 採用: real の preflight で GetWorkGroup(WORKGROUP2) の OutputLocation を確かめ、空なら止める。依頼文に明記
- 判別力 2（medium）e1 の重い計算は QUEUED を延ばさない → 採用: 軽い文を 5 本続けて投げる
- 安全 1（high）1 時間の一時資格情報で 65 分待ちの後の段階 B と、最後にまとめた後始末が失敗し本番に残る → 採用: 後始末は項目ごと、段階 B は最後、依頼文に 90 分
- 安全 2（medium-high）m3 を Hive の CTAS で作ると S3 に残る → 採用: m3 は Iceberg CTAS。Hive の LOCATION は cleanup-hints.txt
- 安全 3（medium）preflight が 2 つのクライアントを別々に通すと明記されていない → 採用
- 備考: 安全のレビュアーは読み取り専用の指示だったが、docker compose run が SIGHUP で落ちないかを実機で試していた（結論: 落ちない）

## フェーズの記録

### フェーズ 1（2026-09-24、general-purpose sonnet）
- 作成: run.sh(178行)・lib-aws.sh(378行)・lib-local.sh(171行)・items-result-files.sh(168行、r1-r4,x1,x2)・items-metadata.sh(123行、m1-m3,m5)・items-statements.sh(86行、s1,s2,p1)・items-token.sh(106行、t1,t5,t8,t4c)・items-workgroup-errors.sh(107行、w1,e1)・report.py(145行)。lib-aws.sh は 200 行の目安を超過（run_stmt を build_stmt_args/fetch_result_files に分割して 52 行まで縮めたが全体は 378 行）
- TARGET=local のドライラン（19 項目）を完走。summary.tsv 93 行、宣言した local 期待値 16 件すべて一致、後始末で trino/minio/dev コンテナが残らないことを確認（timeout で強制終了させても trap が効くことも確認）。ONLY=r3 で他項目が増えないこと、RUN_DIR 追記、わざと w1 の期待値を壊して「期待と違う」検出→復元も確認
- local で FAILED が期待どおり: r1(CREATE EXTERNAL TABLE)・r2(同)・s1(同)・s2(DROP PARTITION 自体が無い綴り)・m5(TBLPROPERTIES('table_type'='ICEBERG') が Iceberg カタログに無いプロパティ)・x1(CREATE EXTERNAL TABLE 部分。SHOW CREATE VIEW 部分は逆に成功した)。理由は全部 Trino の構文/プロパティに無いという実行時エラーで、caveats.md の「6 綴りの表」と同種
- ドライラン中に見つけて直した SQL のバグ 3 件: m1 の `TIME '... UTC'`→`+00:00`（Trino は TIME に zone id を取らない）、m3 の `WHERE false`（FROM 無しで構文エラーになり得るため VALUES 経由に直したが、実測では両方成功した）、s2 の Iceberg フィクスチャで列定義と AS SELECT を同時指定していた構文ミス
- 計画から外れた点: 専用 COMPOSE_PROJECT_NAME は採用せず（dev と別プロジェクトにすると trino/minio に届かなくなるため、tools/e2e/minio/verify.sh と同じ「ambient project 共有」に倣った。隔離したければホストで `COMPOSE_PROJECT_NAME=... tools/dev.sh ...` と包む）。t4 の一部（OutputLocation不正×構文エラー）は t4c として phase1 に前倒し。items-retention.sh/raw.py（フェーズ2、別エージェント作成）とは関数名（new_token 等 vs gen_token 等）・summary 列数が不一致で要調整

## レビュー実施記録

## dismissed

## deferred

| 指摘 | severity | 再トリアージ |
|---|---|---|

## 覆した事前判断

## 実機検証

## 範囲外の発見

| 内容 | 出どころ | issue |
|---|---|---|
| 本物は x-amzn-errortype を付けないが athena-local は付けている（src/response.rs:22-28,86-89）。caveats にも無い | 調査（既存の実測 4 ラウンドの読み直し） | #145 |

## 委譲の記録

| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| 調査 | Explore 3 本（sonnet） | 規則どおり |
| 計画の下書き | Plan 1 本（sonnet） | 規則どおり |
| 計画レビュー | Explore 3 本（sonnet） | 規則どおり |
| フェーズ 1 の実装 | general-purpose（sonnet） | 規則どおり |
| 既知 6 件の転記（先行作業） | general-purpose（sonnet） | 規則どおり |
| フェーズ 2 の raw.py・items-retention.sh（先行、docker 無し） | general-purpose（sonnet） | 規則どおり |

## 出荷

## 時系列

- 2026-09-24 05:59 e25aa74 過去のノートを消して着手
- 2026-09-24 06:00 Explore 3 本（項目の対応・既存スクリプトの流儀・local 向け先と過去の生データ）を起動
- 2026-09-24 06:14 Explore 3 本が返った。Step 4 で 3 点をユーザーに確認。Plan を起動（先行作業: 無し。ケース表が計画に無いため）
- 2026-09-24 06:30 Plan が返った。統括役が 8 点直して計画をノートに転記
- 2026-09-24 06:41 計画レビュー 3 本が返った。採用 7 件を計画に反映、design-checklist に 3 行
- 2026-09-24 06:44 6b の承認（「進めて」）。フェーズ 1 の実装、既知 6 件の転記、フェーズ 2 の raw.py と保持期限（docker 無し）を並行で委譲
- 2026-09-24 06:47 範囲外の発見を起票（#145 x-amzn-errortype）
- 2026-09-24 07:56 フェーズ 1 が返った（ドライラン完走、local の期待 16 件一致）。遅さの原因を特定（s3 の呼び出しに --region が無く IMDS を待つ。1 回 2.6 秒→0.55 秒）。自己レビューで 3 点（r4 の対照が本物で Hive CTAS、real に trap 無し、DROP を投げっぱなし）をフェーズ 2 に回す。前のセッションの残りコンテナ trusting_taussig を消した
