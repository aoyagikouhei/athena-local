# quick #244 SHOW CREATE TABLE・ALTER TABLE・MSCK REPAIR のブロックコメントの ParseException を本物どおり FAILED にする
- 入力: issue #244。モード: standalone。ブランチ main-244、着手前 SHA 6707c75
- ゲート: サイズ見込み 60〜120 行・3 ファイル（reported_query.rs・failure.rs・execution.rs）→ 外れ（≤50）／呼び出し元: execution.rs の 1 箇所（#242 の describe_parse_error の隣）→ 通過／契約: 本物に合わせる修正で通過・新規性: #242 の ImmediateFailure（writes_result_file: true）を再利用で通過・事実: ALTER の実在 Hive 表と、位置・綴り・コメントの中身の一般化が未実測 → 外れ・判断: 一般化の範囲が事実の不足と表裏
- 時系列: 着手 19:33 ／ ゲート判定 19:38（生データの読み直しを含む）／ red・green なし
- 検証: なし（コードに触っていない）
- 自己レビュー: なし
- 打ち切り・escalate: 提案あり（変更サイズ 51〜150 と事実が同時に外れ）→ 選択: implement-issue
- 予算の実績: Bash 約 9 本、所要 約 5 分、サブエージェント 0 本
- 出荷: implement-issue に引き継ぎ。未測定・範囲外の出口: 本 issue で扱う

## 引き継ぎ

### 事前に確認した事実（過去の生データの読み直し。5 形とも全文が確定）
どれも ErrorCategory 1 / ErrorType 1003 / Retryable false、StateChangeReason と AthenaError.ErrorMessage は同じ文言、
StatementType・SubstatementType・OutputLocation（`<id>.txt`）はコメント無しと同じ、`<id>.txt` の中身は文言そのもの（末尾改行無し。
バイト数 = 文言の長さ）、`.metadata` 無し、本体の Content-Type は application/octet-stream。

| 投げた文 | StateChangeReason | 出典 |
|---|---|---|
| `/* c */ SHOW CREATE TABLE <t>` | `FAILED: ParseException line 1:0 cannot recognize input near '/' '*' 'c'`（71B） | `~/athena-comment-measurements/run-20260918-090622/b-block-show-create.*`（#17） |
| `SHOW /* c */ CREATE TABLE <t>` | `FAILED: ParseException line 1:5 cannot recognize input near 'SHOW' '/' '*' in ddl statement`（91B） | `~/athena-keyword-comment-measurements/run-20260922-180816/d-show-create-block.*`（#52） |
| `SHOW CREATE /* c */ TABLE <t>` | `FAILED: ParseException line 1:12 mismatched input '/' expecting TABLE near 'CREATE' in show statement`（101B） | 同 `d-show-create-block2.*` |
| `ALTER /* c */ TABLE <DB>.athena_local_probe_52_missing ADD COLUMNS (c int)`（無い表） | `FAILED: ParseException line 1:0 cannot recognize input near 'ALTER' '/' '*' in alter statement`（94B） | 同 `b-alter-block.*`。コメント無し・行コメント版は `SemanticException [Error 10001]: Table not found`（cat 2 / 1006）。実在する Iceberg 表では `f-alter-block` が SUCCEEDED |
| `MSCK REPAIR /* c */ TABLE t` | `FAILED: ParseException line 1:12 missing EOF at '/' near 'REPAIR'`（65B） | `~/athena-unmeasured-batch-measurements/run-20260924-004554/x1/x1-msck-comment.*`（#146） |

- 位置の読み: SHOW 2 形と MSCK は `/` の 0 始まりの列、ALTER と先頭コメントは 1:0。DESCRIBE（#242）も 1:0 で near の 1 語目は書いたキーワード
- 先頭コメントの 3 語目 `'c'` はコメントの中身の最初の字句と読めるが、中身を変えた実測は無い

### 未実測（implement-issue の Step 3b の候補）
- ALTER の実在する Hive 表へのブロックコメント（Iceberg は成功・無い表は失敗で、Hive の実在表がどちらかで判定が変わる）。ALTER の ADD COLUMNS 以外（RENAME・DROP など）
- 空白 2 つ・改行（位置の数え方と `line 2:`）、小文字のキーワード（near の綴り）、コメントの中身の違い（`/**/`・`/* a b */` の 3 語目）
- 先頭のブロックコメントの前に行コメントがあるとき、先頭コメント付きの ALTER・MSCK
- `SHOW CREATE TABLE` のビューへの実行（`SHOW CREATE /* c */ VIEW` は成功と実測済み。SHOW CREATE TABLE <ビュー> は別）

### 判定箇所と共有できる既存機構
- `src/operation/reported_query.rs:61` `describe_parse_error`（DESCRIBE の直後の `/*` を見る）と `src/failure.rs:79` `Failure::describe_parse_error`
- `src/operation/execution.rs:219` 付近: `Check::Table` のときだけ `ImmediateFailure { writes_result_file: true }` にする。SHOW CREATE TABLE・ALTER は `table_format::target_statement`（`src/operation/table_format.rs:73`）の対象で、`entity_check::check` の結果（Check）も使える
- ALTER の「表の有無・形式」は `table_format::probe_format` で引ける（Iceberg なら送る、無ければ ParseException、Hive は未実測）
- 再現: 偽 Trino の結合テスト（`tests/common/mod.rs` の Harness）で StartQueryExecution → GetQueryExecution の State・StateChangeReason と、偽 S3 の `<id>.txt` の中身
- stash: 無し（コードに触っていない）
