# #244 SHOW CREATE TABLE・ALTER TABLE・MSCK REPAIR のブロックコメントの ParseException を本物どおり FAILED にする

## 基本情報
- issue #244。ベース main、ブランチ main-244、着手前 SHA 6707c75（レビューの `base`）
- quick-implement から escalate（変更サイズ 51〜150 と事実が同時に外れ）。quick の記録は末尾の「quick の記録」

## 事前に確認した事実

### 過去の生データの読み直し（5 形とも全文が確定）
どれも ErrorCategory 1 / ErrorType 1003 / Retryable false、StateChangeReason と AthenaError.ErrorMessage は同じ文言、
StatementType・SubstatementType・OutputLocation（`<id>.txt`）はコメント無しと同じ、`<id>.txt` の中身は文言そのもの（末尾改行無し。
バイト数 = 文言の長さ）、`.metadata` 無し、本体の Content-Type は application/octet-stream。

| 投げた文 | StateChangeReason | 出典 |
|---|---|---|
| `/* c */ SHOW CREATE TABLE <t>` | `FAILED: ParseException line 1:0 cannot recognize input near '/' '*' 'c'`（71B） | `~/athena-comment-measurements/run-20260918-090622/b-block-show-create.*`（#17） |
| `SHOW /* c */ CREATE TABLE <t>` | `FAILED: ParseException line 1:5 cannot recognize input near 'SHOW' '/' '*' in ddl statement`（91B） | `~/athena-keyword-comment-measurements/run-20260922-180816/d-show-create-block.*`（#52） |
| `SHOW CREATE /* c */ TABLE <t>` | `FAILED: ParseException line 1:12 mismatched input '/' expecting TABLE near 'CREATE' in show statement`（101B） | 同 `d-show-create-block2.*` |
| `ALTER /* c */ TABLE <DB>.athena_local_probe_52_missing ADD COLUMNS (c int)`（無い表） | `FAILED: ParseException line 1:0 cannot recognize input near 'ALTER' '/' '*' in alter statement`（94B） | 同 `b-alter-block.*`。コメント無し・行コメント版は `SemanticException [Error 10001]: Table not found`（cat 2 / 1006）。実在する Iceberg 表では `f-alter-block` が SUCCEEDED |
| `MSCK REPAIR /* c */ TABLE t` | `FAILED: ParseException line 1:12 missing EOF at '/' near 'REPAIR'`（65B） | `~/athena-unmeasured-batch-measurements/run-20260924-004554/x1/x1-msck-comment.*`（#146） |

### 調査（Explore 2 本、19:40〜19:50）
1. 開始の順序（`src/operation/execution.rs:51-251`）: 単文化 → OutputLocation（`<id>.txt` はここで確定）→ S3 Tables の拒否 → **構文チェック（96-100、Trino の PREPARE）** → `drop_catalog` → `context_catalog::resolve` → `entity_check::check` → `drop_database` → `quoted_names`／`unquoted_ddl` → S3 Tables の CTAS → `describe_parse_error`（219-228、`Check::Table` のときだけ）→ submit
2. `ImmediateFailure`（`src/store.rs:86-90`）は `spawn_query`（`execution.rs:396-404`）で Trino に何も送らず、`writes_result_file` なら `result_output::write_failure`（`result_output.rs:150-172`）で `.txt` だけ書いて FAILED。`.metadata` は書かない
3. **構文チェックが先**なので、Trino の文法に無い形は今は開始時に MALFORMED_QUERY: `MSCK`（Trino に文が無い。`docs/caveats.md:199`）、`ALTER TABLE ... ADD COLUMNS`・`ADD/DROP PARTITION`・`SET TBLPROPERTIES`（`docs/caveats.md` の ALTER の表）。SHOW CREATE TABLE の各形・`DROP COLUMN`・`RENAME TO` は構文チェックを通る
4. `entity_check::check`（`entity_check.rs:37-41`）は DESCRIBE・SHOW COLUMNS だけ。SHOW CREATE TABLE・ALTER・MSCK には `Check::Continue`（存在確認なし）。表の有無・形式は `table_format::probe_format`（`table_format.rs:99-113`）で引ける
5. **分類に MSCK が無い**（`classification.rs:17-83`）: 今は `UTILITY`／`None`。本物は `DDL`／`MSCK_REPAIR`（`docs/dev/measurements/statements.md:91`）。FAILED にするなら GetQueryExecution の種類も合わせる必要がある。SHOW CREATE TABLE・ALTER はコメントを飛ばして正しく分類される
6. 位置の計算: `athena_sql::Cursor::rest()` から `query.len() - rest.len()`（`reported_query.rs:84`・`quoted_names.rs:31` の型）、行:列は `quoted_names::position`（`quoted_names.rs:184-190`、UTF-16）。`describe_parse_error` はカタログを見ない
7. テスト: #242 は `tests/reported_query.rs:277-333`（`s3_puts` で `.txt` 1 件と本文、`trino_sqls` で送らないこと）。GetQueryResults は見ていない。表の有無・形式は `probe_response(connector)`／`probe_response_missing()`（`tests/table_format.rs:29-48`）。SHOW CREATE TABLE は `tests/show_create.rs`、ALTER は `tests/table_format.rs:389,436`、MSCK の結合テストは無い
8. docs: `docs/caveats.md:42-70`（#244 へのリンクの段落）、`docs/api.md:153-158`（DESCRIBE の記述）、`CHANGELOGS.md` [Unreleased] 24-25、`docs/dev/architecture.md:14`、`docs/dev/measurements/statements.md:79-98,624-650`、`docs/dev/unmeasured.md:66`
9. 行数: `reported_query.rs` 253・`failure.rs` 208・`execution.rs` 501（400 超。触るなら分割をフェーズ 0 に検討）・`classification.rs` 171
- 落とした番号: Explore 2 の「MSCK はそのまま Trino に送られる」は誤り（構文チェックで弾かれる。Explore 1 と caveats で確認）。CLAUDE.md の書き換えの不変条件は、送らない失敗なので触れない（転記不要）

## 実測の手順

未実測を測るスクリプトを `tools/measure/block-comment-parse-error.sh` に用意した（request-measurement 経由）。

- スクリプトのパス: `tools/measure/block-comment-parse-error.sh`
- 実行例（`tools/dev.sh` 経由。DB を省略するか実在しなければ SHOW DATABASES の候補の 1 件目を自動で使う）:
  ```
  tools/dev.sh OUTPUT=s3://your-bucket/prefix/ DB=your_db bash tools/measure/block-comment-parse-error.sh
  ```
  資格情報はホストのシェルで `AWS_ACCESS_KEY_ID` などを export してから（または `~/.aws/credentials`）。
- ラウンド 2（RENAME・DROP COLUMN・ADD COLUMNS の先頭寄りの位置、MSCK REPAIR TABLE の I・V・無い表、DESCRIBE の続き、空白 2 つ・タブ・複数改行・コメントの中身の違い）: 同じコマンドに `ROUND=2` を足す（`tools/dev.sh OUTPUT=s3://your-bucket/prefix/ DB=your_db ROUND=2 bash tools/measure/block-comment-parse-error.sh`）。
- 必須の環境変数: `OUTPUT`（s3://bucket/prefix/ の形、末尾に `/`）。任意: `DB`・`CATALOG`（既定 AwsDataCatalog）・`REGION`（既定 ap-northeast-1）・`OUT_DIR`・`POLL_TIMEOUT`・`RETRY_MAX`・`RETRY_DELAY`（スクリプト先頭のコメントに詳細）。
- 出力先: `${DEV_HOST_HOME:-$HOME}/athena-block-comment-parse-error-measurements/run-<日時>/`（リポジトリの外）。`summary.txt` が実名をマスクした、そのまま貼れる一覧。
- 課金と DDL: あり。`athena_local_probe_244_h`（Hive の EXTERNAL TABLE）・`_i`（Iceberg）・`_v`（ビュー）を作り、A 群で ALTER TABLE を最大 17 回投げてから、最後に 3 つとも DROP する。スキャンする SELECT は投げない。StartQueryExecution 69 回の見込み。

## 実測の結果
ラウンド 2（`~/athena-block-comment-parse-error-measurements/run-20260926-114123`、2026-09-26、69 回、フィクスチャ H=hive・I=iceberg を
SHOW CREATE TABLE で裏取り、後始末の DROP 3 つとも SUCCEEDED）。**失敗は特記の無い限り ErrorCategory 1 / ErrorType 1003・Retryable false・
ErrorMessage = StateChangeReason・`<id>.txt` の中身 = 文言（application/octet-stream）・`.metadata` 無し**（44 項目で中身まで突き合わせ）。
種類（StatementType／SubstatementType）と `<id>.txt` の OutputLocation はコメント無しと同じ。

### 決め手（どの表で失敗するか）
| 文 | Hive 表 | 無い表 | ビュー | Iceberg 表 |
|---|---|---|---|---|
| SHOW CREATE TABLE（先頭・SHOW の後・CREATE の後・TABLE の後の 4 位置） | 失敗 | 失敗（s10〜s12） | 失敗（s13） | **成功**（s6〜s9。4 位置とも） |
| DESCRIBE・DESC・`/* c */ DESCRIBE` | 失敗（d2〜d4） | （#242 で開始時に弾く） | 成功（d5） | **成功**（d1） |
| MSCK REPAIR TABLE（先頭・MSCK の後・REPAIR の後・TABLE の後） | 失敗（r2〜r8） | 失敗（r9） | — | 別の失敗: `Query type not supported by Athena Iceberg at this time`、2/1200、本体なし（r10） |
| `ALTER /* c */ TABLE ... ADD COLUMNS` | 失敗（a2） | 失敗（a4〜a7） | 失敗（a14） | **成功**（a3） |
| `/* c */ ALTER TABLE ... ADD COLUMNS`・`ALTER TABLE /* c */ ...` | — | 失敗（a12・a13） | — | — |
| `ALTER /* c */ TABLE ... ADD PARTITION`・`DROP PARTITION`・`SET TBLPROPERTIES` | **成功**（a8〜a10） | — | — | — |
| `ALTER /* c */ TABLE ... RENAME TO` | — | 失敗だが **2/1006**、ErrorMessage は `Query type not supported by DDL engine.`（a11） | — | — |
| `ALTER /* c */ TABLE ... DROP COLUMN` | 失敗だが **2/1006**、ErrorMessage は `line 1:64: mismatched input 'COLUMN' expecting 'PARTITION'`（a16） | 同じ形 `line 1:70: ...`（a15。`COLUMN` の 0 始まりの位置 + 1、コメント込み） | — | **成功**（a17） |
| 行コメント（`DESCRIBE -- c\n<t>`） | 成功（d6） | — | — | — |

### 文言の形（`<k>` は書いた綴りのキーワード。小文字なら小文字: s14・s15・r6・a5・d2）
| 位置 | StateChangeReason |
|---|---|
| 先頭のコメント（SHOW CREATE・MSCK・ALTER・DESCRIBE 共通） | `FAILED: ParseException line L:C cannot recognize input near '/' '*' '<x>'`。`<x>` はコメントの中身の最初の字句: `c`・`abc`・`a`（`/* a b */`）・`*`（`/**/`）・`,`（`/* , */`）（s20〜s24） |
| SHOW の後 | `... line L:C cannot recognize input near '<SHOW>' '/' '*' in ddl statement` |
| SHOW CREATE の後 | `... line L:C mismatched input '/' expecting TABLE near '<CREATE>' in show statement` |
| MSCK の後／MSCK REPAIR の後 | `... line L:C missing EOF at '/' near '<MSCK>'`／`near '<REPAIR>'` |
| 名前の直前（SHOW CREATE TABLE・MSCK REPAIR TABLE・ALTER TABLE の後） | `... line L:C cannot recognize input near '/' '*' '<x>' in table name`（s5・r5・a13） |
| ALTER の後 | `... line 1:0 cannot recognize input near '<ALTER>' '/' '*' in alter statement`（位置は ALTER の位置。空白 2 つ・改行でも 1:0: a6・a7） |
| DESCRIBE・DESC の後 | `... line 1:0 cannot recognize input near '<DESCRIBE>' '/' '*' in describe statement`（d3 は `'DESC'`） |

- 位置 `L:C`（ALTER・DESCRIBE 以外）: `/` の行（1 始まり）と列（0 始まり）。列は **同じ行の空白の連続を 1 つに畳んで**数えた値と全項目が合う（`SHOW  /*` → 1:5・`SHOW CREATE  /*` → 1:12・`MSCK REPAIR  /*` → 1:12。改行の後は `line 2:0`: s18・s19・r8・s25）。「直前のキーワードの末尾 + 1」でも同じ値になり、区別する項目は無い（未実測: 手前の語の間の空白 2 つ・タブ）
- `/* c */\nSHOW ...` は 1:0（s26）、`-- x\n/* c */ SHOW ...` は 2:0（s25）、コメント 2 つ `SHOW /* a */ /* b */` は 1 つと同じ（s27）

## 実測の経緯
- 2026-09-26 ラウンド 1: 過去の生データ（#17・#52・#146）の読み直しで 5 形の全文が確定。変形と Hive 実在表は無かった
- 2026-09-26 ラウンド 2: `run-20260926-114123`（UTC）をユーザーが実行。67 項目すべて測れた（skip 0）。Iceberg で成功する文・ALTER の動作ごとの割れ・位置の畳み方が分かった

## 設計判断
- D1 範囲（ユーザーの判断）: SHOW CREATE TABLE・MSCK REPAIR TABLE・ALTER TABLE（ADD COLUMNS・DROP COLUMN・RENAME TO の 3 動作とも）・
  DESCRIBE／DESC（#242 の欠陥の取り込み: Iceberg 表は成功させる、DESC と先頭コメントも失敗させる）。コメントの位置は「先頭」「キーワードの間」
  「名前の直前（TABLE の後）」。名前の中・名前の後ろ・句の中は測っていないので今までどおり
- D2 MSCK の Iceberg 表（ユーザーの判断）: 2/1200 `Query type not supported by Athena Iceberg at this time`（本体なし）も扱う。
  コメント無し・ビューはラウンド 3 で測って決める
- D3 失敗させる表: SHOW CREATE TABLE・ALTER は Hive 表・無い表・ビュー（Iceberg 表は今までどおり実行）。DESCRIBE は表で Iceberg 以外
  （ビューは成功、無い表は #207 で開始時に弾く）。MSCK は Hive 表・無い表が ParseException、Iceberg 表が 2/1200
- D4 失敗の種類: ParseException は 1/1003・`<id>.txt` に文言・`.metadata` 無し。ALTER の RENAME TO・DROP COLUMN だけ 2/1006 で、
  ErrorMessage が StateChangeReason と別（RENAME は `Query type not supported by DDL engine.`、DROP COLUMN は
  `line 1:<COLUMN の位置 + 1>: mismatched input 'COLUMN' expecting 'PARTITION'`。コメント無しの Hive 表・無い表の過去の実測
  （#39・#204・#208・#43・#217 の生データ）と同じ ErrorMessage で、StateChangeReason だけ ALTER の ParseException に替わる）
- D5 near の語は書いた綴り。位置は `/` の行と列（ALTER・DESCRIBE は 1:0）。列の数え方（空白の畳み方・タブ）はラウンド 3 で決める
- D6 MSCK の分類を本物どおり `DDL`／`MSCK_REPAIR` にする（FAILED にした実行の GetQueryExecution に出るため）
- D7 CLAUDE.md の不変条件: 送る SQL は書き換えない（送らずに失敗させるだけ）

## 計画
（未着手）

## 計画レビューの結果
（未着手）

## フェーズの記録
（未着手）

## レビュー実施記録
（未着手）

## dismissed
（未着手）

## deferred
（未着手）

## 覆した事前判断
（未着手）

## 実機検証
（未着手）

## 範囲外の発見
| 内容 | 出どころ | 出口 |
|---|---|---|
| コメント無しの ALTER TABLE の DROP COLUMN・RENAME TO（Hive 表・無い表）は本物で FAILED（2/1006）だが athena-local は Trino に送る | 過去の生データ（#39・#43・#204・#208・#217）と本 issue のラウンド 2 | #256 に起票 |

## 委譲の記録
- 調査: Explore 2 本（sonnet）
- 実測スクリプト: general-purpose 1 本（sonnet。981 行、ドライラン 3 通り）。受け入れ判定は自分（出力先の明示・後始末・同名の衝突で止まる・マスク・SELECT 無し）

## 時系列
- 19:33 quick-implement 着手（ブランチ main-244、6707c75）
- 19:38 ゲートで変更サイズと事実が外れ、implement-issue を選択
- 19:40 5338f70 quick ノート／issue コメント。implement-issue 開始、Explore 2 本・実測スクリプトの委譲を起動（先行作業: 実測スクリプト）
- 20:04 d3e1bfa 実測スクリプトをコミット、実測を依頼
- 20:41 ユーザーが実測を実行（run-20260926-114123）。先行作業: ノートの節構成
- 20:5x ラウンド 2 の結果をノートに転記、範囲の判断 2 件（ALTER 3 動作・MSCK の Iceberg）をユーザーに聞いて推奨どおり。ラウンド 3 のスクリプトを委譲（先行作業: Plan）
- 20:58 Plan を起動（実測待ちの値は枠だけ）
- 21:01 先行作業（ユーザーの指摘で着手）: 実機検証の足場 tools/e2e/block-comment を委譲、範囲外の差を #256 に起票、docs/dev/measurements/statements.md に #244 の節（ラウンド 1・2）、unmeasured.md から測れた 4 項目を削除

## quick の記録
- 入力: issue #244。モード: standalone。ブランチ main-244、着手前 SHA 6707c75
- ゲート: サイズ見込み 60〜120 行・3 ファイル（reported_query.rs・failure.rs・execution.rs）→ 外れ（≤50）／呼び出し元: execution.rs の 1 箇所（#242 の describe_parse_error の隣）→ 通過／契約: 本物に合わせる修正で通過・新規性: #242 の ImmediateFailure（writes_result_file: true）を再利用で通過・事実: ALTER の実在 Hive 表と、位置・綴り・コメントの中身の一般化が未実測 → 外れ・判断: 一般化の範囲が事実の不足と表裏
- 時系列: 着手 19:33 ／ ゲート判定 19:38（生データの読み直しを含む）／ red・green なし
- 検証: なし（コードに触っていない）
- 自己レビュー: なし
- 打ち切り・escalate: 提案あり（変更サイズ 51〜150 と事実が同時に外れ）→ 選択: implement-issue
- 予算の実績: Bash 約 9 本、所要 約 5 分、サブエージェント 0 本
- 出荷: implement-issue に引き継ぎ。未測定・範囲外の出口: 本 issue で扱う
