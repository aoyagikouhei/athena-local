# quick #76 結果ファイルの Content-Type で未測定の形（DESC、SHOW CREATE VIEW、SELECT の変種、マルチパートの境界）を本物で測る

- 入力: issue #76。モード: standalone。ブランチ `main-76`、着手前 SHA 8a16b13
- ゲート: サイズ 47 行・1 ファイル（≤50・3。`src/content_type.rs` の `mod tests` より前の追加行。doc コメント込み）／呼び出し元 1（≤5・`results.rs` の `ResultLocation::new` だけ）／契約: 文書化済みの値を本物に合わせる修正で README・CHANGELOG を同じコミットで直す条件で通す／新規性: 通過（新ファイル無し。数・識別子の読み飛ばしは既存関数の拡張）／事実: **外れ**（本物の Athena でしか決まらず、過去 17 ディレクトリの生データにも無い → 実測工程を足した）／判断: 通過（issue が「結果が判定と違えば直す」と決めている。`SELECT 1;` は本物 binary だが Trino が弾くので判定を変えない、`SHOW FUNCTIONS` は Content-Type だけ合わせてファイル名の差は #80 に切り出す、の 2 点はこのノートと README に書いた）
- 時系列: 着手 06:41 ／ ゲートの質問 06:46・回答（実測を足す）／ 実測スクリプトの追記とドライラン 06:47〜06:50・コミット 06:51（e20dcff）／ 待ち時間の手元確認（Trino 482 で 18 形の PREPARE）06:51〜06:52 ／ 実測ラウンド 1 06:54〜07:08（57 項目、未測定 3）／ red 07:10 ／ green 07:11 ／ コミット 07:14
- 検証: 標準コマンド ok（`cargo fmt --check`、`cargo clippy --all-targets --locked -- -D warnings`、`cargo test --locked`。lib 181 passed、結合テストも全部 ok）／ミューテーション: 符号の分岐を消す → `SELECT -1` で落ちた、指数を受けない → `SELECT 1.5E0` で落ちた、裸の別名を受けない → `SELECT 1 i` で落ちた、`SHOW FUNCTIONS` を binary に戻す → 落ちた／実測: ラウンド 1 回（StartQueryExecution 57 本、SUCCEEDED 54・START_FAILED 2（`SHOW SESSION`・`SHOW STATS` は Athena が拒否）・SKIPPED 1（`SHOW CREATE VIEW`。DB にビューが無い）。A〜E の 36 項目は #70 と完全一致）。生データ `~/athena-content-type-measurements/run-20260923-065415`、summary は issue のコメント https://github.com/aoyagikouhei/athena-local/issues/76#issuecomment-5785068087
- 自己レビュー: 分岐 6 件（符号・指数（符号あり／なし・数字無し）・`AS` 有り無し×識別子有り無し・引用符付き識別子・`SHOW FUNCTIONS`）すべてユニットに有り／エラーの出口: 新しいエラー無し（判定は bool と定数）／呼び出し元 1 箇所（`ResultLocation::new`）: 値が変わるのは実測で binary だった 4 形と `SHOW FUNCTIONS` の `.txt` だけで、`.metadata` は同じ値を引き継ぐ（既存テスト `付随ファイルの_content_type_は本体と同じ` が通る）
- 打ち切り・escalate: はしご（事実 → 実測工程）。提案なし。範囲外の発見 1 件を起票（#80: `SHOW FUNCTIONS` は本物が `<id>.csv`・見出し行つき・SubstatementType `SHOW_FUNCTIONS`）。stash 無し
- 予算の実績: Bash 約 25 本（うち実測 9 本）、所要 約 25 分（06:41〜06:52 と 07:08〜07:14。実測待ち 16 分と質問の待ちを除く。上限 20 分＋実測 1 ラウンド 10 分の 30 分以内）、サブエージェント 1 本（止めない独立レビュー）
- 出荷: standalone、ブランチ `main-76`
