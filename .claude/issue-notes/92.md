# quick #92 EXPLAIN の変種（FORMAT JSON / TYPE IO / ANALYZE）の Rows の分け方と失敗時の結果ファイルを本物で測る
- 入力: issue #92。モード: standalone。ブランチ `main-92`、着手前 SHA 9058f6f
- ゲート: サイズ 約 25 行・2 ファイル（≤50・3。`split_explain_rows` の Bool の腕と `write_failure` の early return）／呼び出し元 各 1（≤5・同一モジュール `operation/`）／契約: `TYPE VALIDATE` の Rows と失敗した EXPLAIN のファイルが本物に合う方向に変わり、README・CHANGELOG を同じコミットで直す／新規性: 無し／**事実が外れた**（変種と失敗時は本物を測らないと決まらず、過去 21 ディレクトリに `EXPLAIN SELECT 1` しか無く、手元に認証情報が無い）→ 実測工程を足して続行。本番 2 ファイルにまたがるので計画攻撃も足した／判断: 新しい既定値なし
- 時系列: 着手 13:22 ／ 実測スクリプトのコミット 13:25 ／ 実測 13:27〜13:30（ユーザーが実行） ／ red 13:32 ／ green 13:32 ／ 計画攻撃 13:31〜13:33 ／ コミット 13:35
- 検証: `cargo fmt --check`・`cargo clippy --all-targets --locked -- -D warnings`・`cargo test --locked` ok（328 passed）／ミューテーション: Bool の腕を消す → `explain_type_validate_の真偽値の結果も文字列にして末尾に空行を足す` が落ちた、EXPLAIN の early return を消す → `失敗した_select_と_insert_と_explain_は何も置かない` が落ちた（復元後は `touch` して再ビルド）／実測: 1 ラウンド（`92-measure-explain-variants.sh`、`run-20260923-132753`、StartQueryExecution 11 回）。文字列のプラン 7 形はすべて現状の規則と一致（JSON / IO は末尾の空行 1、GRAPHVIZ は 2、text / DISTRIBUTED / ANALYZE / ANALYZE VERBOSE は 3。`.txt` は Rows を `\n` で連結したものと全件バイト一致）。`TYPE VALIDATE` は boolean の `true` にも空行が付く（Rows 3 行・`.txt` 11 バイト）。失敗した `EXPLAIN` と `EXPLAIN ANALYZE` は本体も `.metadata` も無し（404、ErrorType 1301）。`EXPLAIN ANALYZE` の SubstatementType は `EXPLAIN`、列は varchar(2147483647)。待ち時間に手元の Trino 482 で各変種の末尾の改行数を採った（JSON / IO は 0、GRAPHVIZ は 1、ほかは 2、VALIDATE は boolean）／計画攻撃: 指摘 0 件（medium 以上）、low 1 件（`write_failure` の doc コメント）を採用
- 自己レビュー: 分岐 3 件（Bool の腕 → ユニット + 結合、文字列でない他の値 → 既存ユニット、EXPLAIN の early return → 結合）すべてテスト有り／エラーの出口: 新規なし（early return は PUT を省くだけで FAILED と理由は変わらない）／呼び出し元 2 箇所（`run` の末尾、`spawn_query` の失敗経路）を確認
- 打ち切り・escalate: はしご（事実 → 実測工程 1 ラウンド + 計画攻撃）。提案なし
- 予算の実績: Bash 約 17 本（うち実測 6 本）、所要 約 13 分（実測待ち 3 分と計画攻撃の待ち 2 分を除く）、サブエージェント 2 本（計画攻撃 1、止めない独立レビュー 1）
- 出荷: `main-92`
