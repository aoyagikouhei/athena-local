# quick #131 toolbox（tools/dev.sh）の人間検証: amd64 のビルド、Docker Desktop / rootless、Ctrl-C の後始末、jdbc-drivers・retention・trino-probe の走行
- 入力: issue #131（人間検証の一覧。未チェック 6 項目）。モード: standalone。ブランチ main-131、着手前 SHA 69f4881（過去ノートの削除コミット f39c7a3 の後から作業）
- ゲート: 本番コード 0 行（足場 1 ファイル versions.sh +5/-3、docs 2 ファイル）／契約・新規性なし／事実: 6 項目のうち 3 項目（amd64、Docker Desktop／rootless、~/.aws/credentials）はこのマシン（WSL2・arm64・ネイティブ Docker）で実行できない → 判断が外れた → 1 問聞いた: 「4 項目を実行、残りは別 issue」を選択（#184 に移した）
- 時系列: 着手 05:59 ／ 質問の回答 06:00 ／ jdbc-drivers＋同時実行 06:01〜06:04 ／ versions.sh 5 版 06:04〜06:06 ／ red（TRINO_TAGS=482 で FAIL 1）06:07 ／ green 06:07 ／ Ctrl-C 06:08 ／ コミット 06:10
- 検証: jdbc-drivers 全 7 版 FAIL 0／同時実行: dev が 172.22.0.2・172.22.0.7 に解決、cargo test --locked 398 passed、tls-proxy のログに接続失敗・upstream・5xx 0 件／versions.sh 5 版 FAIL 0、482 は期待値の修正前 FAIL 1 → 修正後 PASS（修正前の実行がミューテーション確認を兼ねる）／Ctrl-C: 疑似端末に \x03、1 秒で exit 130、down -v が走り athena-local プロジェクトのコンテナ 0 件／標準コマンド `cargo fmt --check`・`cargo clippy --all-targets --locked -- -D warnings`・`cargo test --locked`（同時実行の回）ok
- 自己レビュー: versions.sh の変更は期待値の配列だけで分岐の追加無し／エラーの出口: 無し／呼び出し元: EXPECTED の参照は versions.sh の比較 1 箇所。発見: #173 で probe.sh だけ直し versions.sh の 482 の期待値を直し忘れていた（既定の 5 版に 482 が入らず INFO の「差あり」に隠れていた）→ 同じコミットで直した
- 打ち切り・escalate: はしご（判断 → 1 問聞く）。実行できない 3 項目は #184 に移し #131 は閉じる
- 予算の実績: Bash 約 30 本（上限 30 に到達。足場 4 本の走行待ちを除いて所要 約 11 分）、サブエージェント 0 本（本番コードを変えていないので止めない独立レビューは流さない）
- 出荷: main-131
