# #227 S3 Tables の Context で 1 部目が小文字の awsdatacatalog でない 3 部の CREATE TABLE を本物に合わせる

## 基本情報
- issue: #227 ／ ベース: main ／ ブランチ: main-227 ／ 着手前 SHA: a053cb1（レビューの base）
- quick-implement から escalate（下の「quick の記録」）。quick ノートのコミットは docs 予算の別枠

## quick の記録
- 入力: issue #227。モード: standalone。ブランチ main-227、着手前 SHA a053cb1
- ゲート: サイズ見込み 60〜100 行・2〜3 ファイル（≤50・3）→ 外れ／呼び出し元 1（unquoted_ddl::rejection → execution.rs）／契約は通過（本物の文言に合わせる）／新規性: Trino に送らずに実行時の FAILED を作る先行例 0 件 → 外れ／事実: AWSDATACATALOG・実在する他のカタログが未実測 → 外れ／判断: 実行時の失敗の作り方 → 外れ
- 時系列: 着手 09:59 ／ ゲート判定と提案 10:03 ／ コミット 10:05（red・green なし）
- 検証: なし（コードに触る前に escalate）
- 自己レビュー: なし
- 打ち切り・escalate: 提案あり（変更サイズ・新規性・事実・判断）→ 選択: implement-issue で進める
- 予算の実績: Bash 約 4 本、所要 約 6 分、サブエージェント 0 本
- 出荷: main-227（implement-issue に引き継ぎ）

## 事前に確認した事実
（Explore 3 本の番号付き返答から転記。落とした番号: 起動・パース・トークン検証・OutputLocation の手順（判定に無関係）、design-checklist の全項目の要旨（原文を Step 6 に渡す）、unmeasured の #216・#157 の行（本 issue に効かない））
1. 開始時の判定の順番: 構文チェック（execution.rs:79）→ context_catalog::resolve（:84）→ entity_check::check（:91。DESCRIBE・SHOW COLUMNS だけ）→ quoted_names::rejection → unquoted_ddl::rejection（:107-120）→ store.submit（:126）。`s3_tables` は Context の生の Catalog から作る（:113-116）
2. create_table.rs:51-57: S3 Tables の Context で No location になった 3 部の名前は、1 部目の `.text == "awsdatacatalog"`（大文字小文字を区別）なら 2 catalogs、それ以外は No location。テストは create_table/tests.rs:313-314 で固定
3. カタログの有無の問い合わせは `table_format::catalog_exists_sql`（table_format.rs:130。`system.metadata.catalogs` を名前で引く）。`context_catalog::resolve`（context_catalog.rs:39-68）は RESOLVED_STATEMENTS（CREATE_TABLE を含む）の Context の Catalog だけを見て、別名のキーなら問い合わせない。失敗・想定外の形は「そのまま」に倒す。名前は小文字にして引く（:53）
4. `entity_check` の DATACATALOG_NOT_FOUND（entity_check.rs:72-80）は DESCRIBE・SHOW COLUMNS だけ。`TargetStatement`（table_format.rs:13-28）と `target_table::keywords`（target_table.rs:21-31）に CREATE TABLE は無い。`table_name_start` の任意の `IF EXISTS` は `IF NOT EXISTS` を読まない（target_table.rs:82-84）
5. 別名のキーの照合は大文字小文字を区別する（`catalog_map.contains_key`・`config.trino_catalog`）。区別しないのは context_catalog::fallback（:78）だけ。`catalog.rs` の別名の置換は引用符付きの名前だけ（catalog.rs:32-45、テスト :184-194）で、無引用の `AwsDataCatalog.db.t` は Trino にそのまま届く。Trino のセッションのカタログは Context の Catalog から決まる（execution.rs:300-312）
6. Trino に送らずに FAILED で終える経路は無い。実行時の失敗は spawn_query（execution.rs:260-283）で Trino のエラーを `Failure::from_query_error` に通す 1 本だけ。`Failure` はフィールドが全部 pub（failure.rs:10-17）、専用コンストラクタの前例は `Failure::result_write`（:56-63）。FAILED の DDL は `write_failure`（result_output.rs:139-160）が `.txt` に `FAILED: <理由>` だけを書き、`.metadata` は書かない（tests/failed_results.rs）
7. GetQueryExecution の StatementType・SubstatementType は SQL から決まる（query_execution.rs:166-171）。AthenaError は `execution.failure` から（:191-196）
8. テストの足場: FAILED まで待つのは `Harness::wait_until_done`／`run_query`（tests/common/mod.rs:396-421）、Trino に送った本体は `trino_requests()`、偽 Trino の問い合わせへの応答は `route(sql, response)`。カタログの有無の応答は tests/context_catalog.rs:26-37 の `catalog_exists_sql`/`catalog_exists_response` の形。「開始できて、Trino に本体を送らずに FAILED」を確かめるテストの前例は無い
9. 実測（docs/dev/measurements/statements.md）: 本物は Context の Catalog の大文字小文字を区別しない（client-request-token.md:125-149）が、SQL に書いた 1 部目の `AwsDataCatalog` は `awsdatacatalog` と同じ扱いにならなかった（#224 i4）。DATACATALOG_NOT_FOUND は DESCRIBE の 3 部の名前でも同じ文言（statements.md:374）。既定の Context で 1 部目が実在しないカタログの CREATE TABLE は未実測
10. i4 と i2 の GetQueryExecution は StateChangeReason・AthenaError（2/1100、Retryable false）・StatementType DDL・SubstatementType CREATE_TABLE まで同じ（生データ run-20260926-004013）
11. decisions.md:97: DATACATALOG_NOT_FOUND は名前に 3 部書いてカタログまで指定した形だけで返す（#207）。decisions.md に #227 の決定はまだ無い

## 実測の手順
- スクリプト: `tools/measure/unquoted-ddl.sh` の `ROUND=5`（J 群 17 項目）。実行例:
  `tools/dev.sh OUTPUT=s3://<bucket>/<prefix>/ DB=<db> ROUND=5 S3TABLES_CATALOG=s3tablescatalog/<bucket> S3TABLES_NS=<ns> bash tools/measure/unquoted-ddl.sh`
- 出力: `~/athena-unquoted-ddl-measurements/run-<日時>/`。FAILED の項目は結果ファイル本体と `.metadata` も取得する（`<label>.output.*`）
- 本物への呼び出し: StartQueryExecution 19 本（S3TABLES_* 無しなら 5 本）＋受理された CREATE の後始末 最大 16 本、FAILED ごとに `aws s3 cp` 最大 2 本。DDL は CREATE TABLE と後始末の DROP だけ
- 受け入れ判定（統括役）: 投げる文・後始末の Context（j12 は S3 Tables 側で `$DB.<名前>` を消すよう直した）・`output_location_of` の列・伏せ字（ドライランで実名 0 件）を確認

## 実測の結果
ROUND=5（J 群 17 項目、生データ run-20260926-012516）と ROUND=4（run-20260926-004013）の統合。開始時に弾かれたものは InvalidRequestException。
- **決め手**: S3 Tables の Context で、1 部目が小文字の `awsdatacatalog` 以外（`AwsDataCatalog`・`AWSDATACATALOG`）の実在するカタログの 3 部の名前は、1 部目を無視して 2 部目を S3 Tables の名前空間として作る。`<ns>` なら SUCCEEDED（j1・j4、2 部の j11 と同じ）、`<db>`（S3 Tables に無い名前空間）なら FAILED（j2・j3・j9、2 部の j12・i2 と同じ）
- FAILED は StateChangeReason・ErrorMessage `Cannot find or access the specified table`、ErrorCategory 2・ErrorType 1100・Retryable false、DDL/CREATE_TABLE。結果ファイル本体も `.metadata` も置かない（j2・j3・j9・j12 とも 404）
- 実在しないカタログは、S3 Tables の Context（j5・j6・j10・i3）でも既定の Context（j14）でも開始時に `DATACATALOG_NOT_FOUND`、`Catalog '<書いたとおりの名前>' does not exist`（j6 の `NoSuchCatalog227` は大文字小文字を保つ）。2 部目が名前空間でも DB でも同じ。`IF NOT EXISTS` でも同じ
- NV は DATACATALOG_NOT_FOUND より先（j7）、`AwsDataCatalog` の形でも先（j8）
- 既定の Context の `AwsDataCatalog.<db>.<t>`・`AWSDATACATALOG.<db>.<t>` は No location（j15・j16。今の athena-local と同じ）
- CTAS `AwsDataCatalog.<ns>.<t> AS SELECT 1 AS n`（S3 Tables の Context、j13）は FAILED（2/1301、`Database <ns> not found. Please check your query. You may need to manually clean the data at location '<OUTPUT>tables/<id>' before retrying. ...`）。CTAS は Glue 側で 2 部目を DB として引く。`.metadata` だけ 81 バイト置かれた

## 実測の経緯
- 2026-09-26 10:24 依頼 → 10:25 頃実行（UTC 01:25）。ROUND=5 の 17 項目すべて測れた。StartQueryExecution 19 本＋後始末 3 本、残った表なし

## 設計判断
- D1（ユーザーの判断、2026-09-26）: S3 Tables の Context で、1 部目が小文字の `awsdatacatalog` 以外の実在するカタログの無引用の 3 部の場所の無い CREATE TABLE は、2 部目の名前空間が S3 Tables のカタログ（Context の Catalog の Trino 側）に無ければ、Trino に本体を送らずに本物と同じ FAILED（`Cannot find or access the specified table`、2/1100、Retryable false、結果ファイルも `.metadata` も置かない）にする。名前空間があれば No location のまま（本物は 1 部目を無視して作るが、再現には SQL の書き換えが要り CLAUDE.md の約束に反する）。選んだ理由: 3 案のうち実測に一番近い（名前空間が無い場合は完全に一致し、ある場合も今より悪くならない）
- D2: 実在しないカタログの無引用の 3 部の場所の無い CREATE TABLE は、S3 Tables の Context でも既定の Context でも、開始時に `DATACATALOG_NOT_FOUND`、`Catalog '<書いたとおりの名前>' does not exist`（j5・j6・j10・j14・i3）。NV は先（j7）

## 計画
（Plan の番号付き下書きから転記。落とした・覆した番号: 1（Trino に問い合わせず別名の表だけで実在を決める案）→ D3 で覆した。3 の「同じ純関数を 2 回呼び is_none() を条件に足す」差し込み → D4 で覆した。5 の execution.rs が 400 行を超えたときの分割先は採用（超えたときだけ）。8 は範囲外として起票）

### 追加の設計判断
- D3: 1 部目の実在（D2）: 大文字小文字によらず `awsdatacatalog` なら実在（本物に必ずある）。それ以外は、`TRINO_CATALOG_MAP` のキーを大文字小文字によらず探して（計画レビューの指摘。context_catalog.rs:78 の fallback が前例）見つかればその Trino 側の名前、無ければ書いたとおりの名前を、小文字にして `table_format::catalog_exists_sql` で Trino に問い合わせ、無い（`_col0` が null の 1 行）ときだけ DATACATALOG_NOT_FOUND。ある・問い合わせが失敗した・形が違うときは今どおり No location。根拠: entity_check.rs:55-80 の DESCRIBE と同じ「実在＝Trino にある」の定義に揃え、Trino にだけあるカタログ（未実測）を推測で弾かない（CLAUDE.md「実測していない振る舞いは推測で埋めない」）
- D4: 差し込み位置: execution.rs:107-120 の `quoted_names::rejection(..).or_else(unquoted_ddl::rejection(..))` が返した文言が `NO_LOCATION` のときだけ、新モジュールの非同期の確認を呼ぶ（純関数は 1 回だけ）。確認の結果が Reject なら DATACATALOG_NOT_FOUND を返し、FailAtRuntime なら submit して Trino に送らず FAILED、Continue なら今どおり No location を返す。NV・2 catalogs・引用符付き・4 部以上は文言が NO_LOCATION でないので確認が走らない（Trino への問い合わせは増えない）
- D5: D1 の対象は、S3 Tables の Context で 1 部目が大文字小文字によらず `awsdatacatalog`（ちょうど小文字は 2 catalogs が先に返るので、実際には `AwsDataCatalog`・`AWSDATACATALOG` など）のときだけ。Trino にある他のカタログは未実測なので No location のまま
- D6: 2 部の `<db>.<t>`（S3 Tables の Context）は範囲外（Plan の推奨 8。今は Trino に送り、名前空間が無ければ Trino の文言で FAILED、あれば作る。本物との差は文言だけ）。起票する

### フェーズ 1（D2・D3・D4 の骨組み）: 実在しないカタログを DATACATALOG_NOT_FOUND に
| ファイル | 責務 | 見込み |
|---|---|---|
| `src/operation/create_table_catalog.rs`（新規） | No location になった無引用の 3 部の名前について、1 部目の実在を確かめる（`check` → `Outcome::{Continue, Reject}`） | 本体 約 60 行＋テスト |
| `src/operation/unquoted_ddl/create_table.rs` | `NO_LOCATION` を `pub(in crate::operation)`。`rejection` の冒頭の名前の読み取り（先頭の空白 → `CREATE TABLE`／`IF NOT EXISTS` → `qualified_name`）を private 関数に抽出し、`rejection` と新しい `three_part_name(query)`（無引用の 3 部の名前の 1 部目・2 部目の書いたとおりの文字列）の両方がそれを使う（名前を読む判定を 1 箇所に保つ。計画レビューの指摘） | +20 行 |
| `src/operation/unquoted_ddl.rs` | 必要なら再エクスポート | +2 行 |
| `src/operation/execution.rs` | D4 の差し込み | +15 行 |
| `src/operation/mod.rs` | `mod create_table_catalog;` | +1 行 |
- 前提表: `create_table::rejection` は純関数で変えない（create_table.rs:27）／別名のキーの照合は大文字小文字を区別（config.rs:95-99）／`catalog_exists_sql` の応答の形は `_col0` の 1 行（context_catalog.rs:55-66 が同じ判定）／問い合わせにセッションのカタログを付けない（context_catalog.rs:54）
- 増える分岐とテスト: (a) 1 部目が awsdatacatalog（大文字小文字によらず）→ 問い合わせずに実在 (b) Trino に無い → DATACATALOG_NOT_FOUND（既定の Context・S3 Tables の Context の両方。文言は書いたとおりの大文字小文字）(c) Trino にある → No location (d) 問い合わせ失敗・形違い → No location (e) 文言が NO_LOCATION 以外・1〜2 部 → 確認を走らせない（Trino への要求が増えない）。`three_part_name` と `rejection` の一致テスト（No location になる 3 部の行ですべて Some）
- 既存テストへの影響の予測: create_table/tests.rs:313-314 は不変（rejection を変えない）。tests/syntax.rs の S3 Tables の 2 catalogs のテストの `AwsDataCatalog.db.t` の行は、フェーズ 1 では awsdatacatalog を問い合わせずに実在とするので不変
- 増える分岐（追加）: (l) 別名のキーが大文字小文字違いで一致する（`SalesFederation=...` に `salesfederation.db.t`）→ 別名の Trino 側の名前で引く
- 壊す候補: `eq_ignore_ascii_case` を `==` に（awsdatacatalog と別名の照合の 2 箇所）／null 判定を反転／文言のカタログ名を小文字に

### フェーズ 2（D1・D5）: 名前空間が無ければ開始して Trino に送らず FAILED
| ファイル | 責務 | 見込み |
|---|---|---|
| `src/operation/table_format.rs` | `schema_exists_sql(catalog, schema)`（`system.jdbc.schemas` を引く。列名は先行作業で確認） | +10 行 |
| `src/failure.rs` | `Failure::cannot_find_table()`（2/1100/Retryable false、`Cannot find or access the specified table`） | +12 行 |
| `src/operation/create_table_catalog.rs` | `Outcome::FailAtRuntime(Failure)`、名前空間の問い合わせ | +35 行 |
| `src/store.rs` | `Submission`・`Execution` に `immediate_failure: Option<Failure>`（既存の Submission リテラル 4 箇所） | +10 行 |
| `src/operation/execution.rs` | FailAtRuntime を submit に渡す、`spawn_query` で `mark_running` の後に `finish(Err)` して return（`write_failure` を呼ばない） | +15 行 |
- 前提表: `mark_running` は QUEUED からだけ進む（store.rs:211-220）／`finish` は終端状態では何もしない（store.rs:223-234、取り消しとの競合はこれに任せる）／`write_failure` は DDL の `.txt` を書く（result_output.rs:139-160）ので呼ばない／StatementType・SubstatementType は SQL から決まる（query_execution.rs:166-171）
- 増える分岐とテスト: (f) S3 Tables の Context・名前空間なし → 200 と Id、FAILED（理由・AthenaError 2/1100/false・DDL/CREATE_TABLE）、本体を Trino に送らない、S3 に何も置かない (g) 名前空間あり → No location (h) 問い合わせ失敗・形違い → No location (i) 既定の Context の `AwsDataCatalog.<db>.<t>` → No location（問い合わせない）(j) Trino にある他のカタログ（S3 Tables の Context）→ No location (k) 名前空間の名前を小文字にして引く
- 既存テストへの影響の予測: tests/syntax.rs の `s3_tables_の_context_で_awsdatacatalog_の_3_部の_create_table_は_2_catalogs_で弾く` の `AwsDataCatalog.db.t` の行は名前空間の問い合わせが 1 本増え、共有の Harness の `trino_requests().is_empty()` がその行と後続の引用符付き 2 行で落ちる見込み（計画レビューの指摘）→ この行をループから外して (g) のテストに移す。残る 3 行の is_empty は保つ
- 壊す候補: `immediate_failure` を `mark_running` の前で見る／`write_failure` を呼ぶ／名前空間を小文字にしない／`Failure` の値を 1 つ変える／Some(false) と None の分岐を反転

### 人間検証リスト
- 実在する連携カタログ・Trino にだけあるカタログを 1 部目に書いた形の本物の挙動（未実測。unmeasured.md に載せる）

### 先行できる作業
- 実機検証の足場 `tools/e2e/create-table-catalog/verify.sh`（`tools/e2e/context-catalog/verify.sh` を雛形。compose の trino の iceberg を S3 Tables の別名にする）。ケース: M1 既定の Context の実在しないカタログ → DATACATALOG_NOT_FOUND／M2 S3 Tables の Context で同じ／M3 名前空間ありの `AwsDataCatalog.<ns>.t` → No location／M4 名前空間なし → 200 の後 FAILED、Trino に本体が届かず S3 に何も置かない／M5 既定の Context の `AwsDataCatalog.db.t` → No location／M6 Trino にあるカタログ（`iceberg.<ns>.t`）→ No location。受け入れ判定: 着手前の a053cb1 で M1・M2・M4 が落ち、実装後に全件通る。起動できるのは計画確定の直後（6b の承認待ちの間）。`system.jdbc.schemas` の列名もこの委譲先に compose の trino で確かめさせる

### docs
docs/caveats.md（plain CREATE TABLE の節）・docs/dev/architecture.md・docs/dev/decisions.md（D1〜D6）・docs/dev/measurements/statements.md（ROUND=5 の節）・docs/dev/unmeasured.md（連携カタログ・Trino だけのカタログ）・CHANGELOGS.md

## 計画レビューの結果
- 契約・呼び出し元（Explore 1 本）: 指摘 1 件 → 採用。tests/syntax.rs:298-341 は Harness を 4 入力で共有し `trino_requests().is_empty()` を毎回見るので、`AwsDataCatalog.db.t` の行で増える名前空間の問い合わせが後続の引用符付き 2 行まで落とす（tests/common/mod.rs:301-303 は累積）。→ フェーズ 2 でこの行をループから外し、新しい「名前空間ありなら No location」のテストに移す。残る 3 行は is_empty の検査を保つ。問題なしと確認されたもの: 冪等トークンの fingerprint は判定より前に確定、Existing の経路は Submission を見ない、取り消しとの競合は mark_running/finish のガードで足りる
- 構造・削減（Explore 1 本）: 指摘 1 → 採用。D3 が SQL の 1 部目で `config.trino_catalog`（大文字小文字を区別する HashMap::get、config.rs:95-99）を引くと、`TRINO_CATALOG_MAP=SalesFederation=pg` に `CREATE TABLE salesfederation.public.t (n int)` で実在するのに DATACATALOG_NOT_FOUND になる → キーを大文字小文字によらず探す（D3 を修正）。削減の提案（新モジュールをやめ entity_check・TargetStatement・target_table::keywords を CREATE TABLE に広げる）→ 棄却: TargetStatement は format_probe（実行時の形式の問い合わせ）と結果ファイルの判定が共有しており（table_format.rs:73-83）、CREATE TABLE を足すと実行時の問い合わせが増える。ただし「名前の読み取りが 2 箇所」の懸念は採用し、create_table.rs 内で読み取りを 1 関数に抽出して共有する
- 再レビュー: 省略。構造の書き直しは create_table.rs 内の関数の抽出 1 つと照合の大文字小文字だけで、モジュール構成・判定の順番は変えていないため

## フェーズの記録
（未着手）

## レビュー実施記録
（未着手）

## dismissed
（なし）

## deferred
| 指摘 | severity | 再トリアージ | 起票 |
|---|---|---|---|

## 覆した事前判断
| 元の判断 | 新しい判断 | 理由 | 影響 | ユーザーに聞いたか |
|---|---|---|---|---|
| #224: S3 Tables の Context の大文字混じり・実在しないカタログの 3 部は No location のまま据え置く | 実在しないカタログは DATACATALOG_NOT_FOUND（既定の Context も）、`AwsDataCatalog` の類は名前空間が無ければ開始して FAILED | ROUND=5 の実測で本物の規則が決まった | プロダクト挙動（開始時の弾き方と、開始できる文が増える） | 聞いた（D1） |
| issue 本文: 実在しないカタログの判定は entity_check と同じ仕組み | 同じ問い合わせ（catalog_exists_sql）を使うが、entity_check は広げず新モジュールに置く | TargetStatement は format_probe と共有で、広げると実行時の問い合わせが増える | なし | 6b で確認 |

## 実機検証
（未着手）

## 範囲外の発見
| 内容 | 出どころ | 起票 |
|---|---|---|
| S3 Tables の Context の 2 部の `<db>.<t>`（名前空間なし）は本物は同じ FAILED、athena-local は Trino の文言（D6） | i2・j12 | #231 |
| S3 Tables の Context で別カタログを指す名前の CTAS（i12 SUCCEEDED、j13 は Glue 側で 1301） | i12・j13 | #232 |

## 委譲の記録
| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| 調査 | Explore 3 本（sonnet） | |
| 実測スクリプト（ROUND=5） | general-purpose（sonnet） | j12 の後始末の向きだけ自分で直した（1 行） |

## 引き継ぎ（quick-implement から）
- 判定箇所: `src/operation/unquoted_ddl/create_table.rs:52-56`（S3 Tables の Context で No location になった 3 部の名前。小文字の `awsdatacatalog` は 2 catalogs、それ以外の 3 部は `[_, _, _] => Some(message)` で No location のまま）。`s3_tables` は `src/operation/execution.rs:113-116` で Context の Catalog から決めて `unquoted_ddl::rejection` に渡している
- 開始時の判定の順番（`execution.rs`）: 構文チェック → `context_catalog::resolve`（:84）→ `entity_check::check`（:91。DESCRIBE・SHOW COLUMNS だけ）→ `quoted_names::rejection` → `unquoted_ddl::rejection`
- 共有できる既存機構:
  - カタログの有無の問い合わせは `table_format::catalog_exists_sql`（`src/operation/table_format.rs:130`）。`context_catalog::resolve`（`src/operation/context_catalog.rs:39`）が開始時と `run`（`execution.rs:300`）の両方で使っている
  - `DATACATALOG_NOT_FOUND` の応答の形は `entity_check.rs:76-77`（`Catalog '<name>' does not exist`。名前は書いたとおり）
  - 実行時の失敗は `Store::finish`（`src/store.rs:223`）に `Err(Failure)` を渡す形だけ。Trino に送らずに FAILED にする経路は無い。ErrorType 1100 の対応は `src/failure.rs:76`
- 原因の仮説（本物）: 2 catalogs の判定は 1 部目を小文字の `awsdatacatalog` と大文字小文字を区別して比べている。それ以外はカタログの一覧を引き、無ければ `DATACATALOG_NOT_FOUND`。`AwsDataCatalog` は一覧で見つかり（カタログ名の照合は大文字小文字を区別しない？）、開始してから FAILED（`Cannot find or access the specified table`。2 部の i2 と同じ文言）
- 未実測・未確認:
  - `AWSDATACATALOG`（全部大文字）、実在する連携カタログ、`TRINO_CATALOG_MAP` の別名・Trino にだけあるカタログを 1 部目にした形（athena-local が「実在する」とみなすカタログ）
  - `AwsDataCatalog` の FAILED の StateChangeReason・ErrorCategory 以外（`.metadata`・結果ファイルの有無、StatementType は DDL/CREATE_TABLE）
  - 既定の Context（AwsDataCatalog）で 1 部目が実在しないカタログの場所の無い CREATE TABLE（No location か DATACATALOG_NOT_FOUND か）
  - 2 部の `<db>.<t>`（i2）も本物は FAILED。athena-local は Trino に送る（S3 Tables のカタログ側に作られうる）
- 再現: S3 Tables の Context（例 `Catalog=s3tablescatalog/b`）で `CREATE TABLE AwsDataCatalog.db.t (n int)` と `CREATE TABLE nosuchcatalog.db.t (n int)` を StartQueryExecution。今はどちらも `No location ...`
- 実測の足場: `tools/measure/unquoted-ddl.sh`（ROUND=4 が雛形。生データ `~/athena-unquoted-ddl-measurements/run-20260926-004013` の i2〜i4）
- stash: なし（コードに触っていない）

## 時系列
- 09:59 quick-implement で着手（a053cb1）
- 10:05 quick ノートをコミットして implement-issue に escalate（main-227）
- 10:05〜10:12 Explore 3 本を起動し、返りを待つ間に ROUND=5 のスクリプト作成を委譲（個々の時刻は記録漏れ）
- 10:12 Explore 3 本が揃い、ノートを implement-issue の節構成に広げる
- 10:24 ROUND=5 のスクリプトを受け入れ、コミットして実測を依頼
- 10:27 ROUND=5 の結果を読み、ノートの実測の結果に書く（仮説は当たった）
- 10:30 設計判断 D1（無いときだけ FAILED。ユーザーの判断）を決め、Plan を起動
- 10:52 Plan の下書きが返り、D3・D4 で 2 点覆して計画を転記
- 10:53 計画レビュー 2 本（契約・呼び出し元／構造・削減）を起動。範囲外を #231・#232 に起票
- 11:06 計画レビュー（契約・呼び出し元）が返る。指摘 1 件を採用
- 11:20 計画レビュー（構造・削減）が返る。D3 を修正、削減案は一部採用。design-checklist に 1 行。計画を確定してコミット
