# quick #91 Iceberg のテーブルへの 0 行の INSERT の結果ファイルを本物で測る
- 入力: issue #91。モード: standalone。ブランチ `main-91`、着手前 SHA 3d43c3d
- ゲート: サイズ 1 行（コメントのみ）・1 ファイル（≤50・3）／呼び出し元 0（≤5・同一モジュール）／契約・新規性・判断は通過。**事実が外れた**（Iceberg × 0 行は本物を測らないと決まらず、手元に認証情報が無く、2026-09-20 のラウンドにも無い）→ 実測工程を足して続行
- 時系列: 着手 12:03 ／ 実測スクリプトのコミット 12:10 ／ 実測 12:14〜12:17（ユーザーが実行） ／ テスト・文書 12:18 ／ コミット 12:21
- 検証: `cargo fmt --check`・`cargo clippy --all-targets --locked -- -D warnings`・`cargo test --locked` ok（326 passed）／ミューテーション: `to_metadata` で更新件数 0 のとき field 3 を書かない → `行の無い_insert_も_metadata_だけを置き_更新件数_0_を書く` が落ちた（復元後は `touch` して再ビルドすること。同じ秒に戻すと cargo が変異ビルドのまま走る）／実測: 1 ラウンド（`91-measure-iceberg-zero-insert.sh`、`run-20260923-121426`、StartQueryExecution 11 回）。争点 a（Iceberg × 0 行）は `<id>`・本体無し・`.metadata` 75 バイト（`INSERT` と `18 00`）・マニフェスト無しで、対照 c（Hive × 0 行）と先頭のクエリ ID 以外一致。b（Iceberg × 1 行）と d（Hive × 1 行）は `18 01` で、d だけ `<id>-manifest.csv` が付く（#35 の再現）。2026-09-20 の c の生データを読み直して Hive × 0 行の `18 00` も確認（当時はバイト数しか見ていなかった）。待ち時間に手元の Trino 482 で同じ INSERT が `updateCount=0`・列 `rows` で返ることを確かめた
- 自己レビュー: 本番コードの分岐の変更なし（コメント 1 行）。テストは既存経路（`<id>`・`.metadata` のみ・field 3）を 0 行で固定する 1 本。エラーの出口なし。呼び出し元の変更なし
- 打ち切り・escalate: はしご（事実 → 実測工程 1 ラウンド）。結果は推定どおりで、コード変更なし。README の「更新件数 0 の DML は未実測」は 0 行の INSERT について実測済みに書き換え、UPDATE / DELETE / MERGE の 0 件だけ未実測として残した（TODO.md も同様）
- 予算の実績: Bash 約 22 本（うち実測 6 本）、所要 約 14 分（実測待ち 3 分を除く）、サブエージェント 1 本（止めない独立レビュー）
- 出荷: `main-91`
