# quick #163 SHOW CREATE VIEW と Iceberg の SHOW CREATE TABLE の binary な .metadata を JDBC ドライバが読めるか確かめる
- 入力: issue #163。モード: standalone。ブランチ main-163、着手前 SHA 88ed0d1（過去ノートの削除コミット 19393cf の後から作業）
- ゲート: 本番コード 0 行・0 ファイル（≤50・3。足場 2 ファイル: Main.java +11、jdbc-show-metadata.sh）／呼び出し元: シナリオ 57 は jdbc-show-metadata.sh の 1 箇所／契約なし・新規性なし（既存の足場に文を足すだけ）・事実はローカルの compose で 1 コマンド再現・判断は issue が決めている（読めなければ caveats）→ 通過
- 時系列: 着手 05:34 ／ red（足場に 2 文を足した初回の実行。JDBC は読めたが GetQueryResults の行数が食い違い FAIL 2 件）05:37 ／ green（既知の差分 #181 を INFO に落とした再実行）05:39 ／ コミット 05:43
- 検証: `tools/dev.sh bash tools/measure/jdbc-show-metadata.sh` FAIL 0（auto で 2 文とも loaded query result metadata、Content-Type binary、先頭 0a1b3230）。標準コマンド `cargo fmt --check`・`cargo clippy --all-targets --locked -- -D warnings`・`cargo test --locked` ok（396 passed）／ミューテーション: `content_type::carries_execution_id` に `VIEW` を足して SHOW CREATE VIEW を application に戻す → `SHOW_CREATE_VIEW の .txt.metadata(auto)` が FAIL（戻した後の .toolbox/target の release バイナリは変異版のまま。次に足場を回すときは SKIP_BUILD を付けない）
- 自己レビュー: 分岐は check_show_create_loaded の 3 つ（ログ無し FAIL／binary PASS／binary 以外 FAIL）と突き合わせの既知の差分の case。PASS と binary 以外は実行とミューテーションで踏んだ、ログ無しは未踏（足場の判定なので PR に記載）／エラーの出口: 本番コード無し／呼び出し元 1 箇所
- 打ち切り・escalate: 提案なし。範囲外の発見: SHOW CREATE TABLE／VIEW の GetQueryResults が改行入りの 1 行（本物は行ごと）→ #181 に起票
- 予算の実績: Bash 約 25 本、所要 約 9 分（足場の実行待ちを含む）、サブエージェント 0 本（本番コードを変えていないので止めない独立レビューは流さない）
- 出荷: main-163
