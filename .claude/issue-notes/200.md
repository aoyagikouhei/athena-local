# #200 語の境界の規則を本物に揃える

## 基本情報
- issue #200。ベース main、ブランチ main-200。着手前 SHA e9d1f15（quick ノートのコミット add349f の前。レビューの base は add349f）
- quick-implement から escalate（下の quick ノートと引き継ぎ）

## 事前に確認した事実
### 判定の 2 系統（Explore A・C）
- F1 words 系（空白とコメントだけで切る。`crates/athena-sql/src/words.rs:15`・`words_iter` :36。記号は直前の語にくっつく。`(SELECT 1)`→`["(SELECT","1)"]` は意図的で words.rs:88・173-181 のテストが固定）。入口:
  - `src/operation/classification.rs:9` `words()` = `athena_sql::words` + `results::strip_open_parens`（`src/results.rs:84`）。`statement_type`（:18、0 語目）・`substatement_type`（:31、0〜4 語目。ALTER は `word(1)=="TABLE"` の guard :80 の後 `alter_table_action`）・`fixed_column`（:93）
  - `src/results.rs:48` `ResultFile::of`（先頭語。CREATE は `is_create_table_as` :91-116 で `AS(` を strip_prefix で割る。SHOW は 2 語目 FUNCTIONS）
  - `src/content_type.rs:61-63` `plain_text_statement`（1 語目 == EXPLAIN。`completion.rs:124` の UpdateCount と共有）、:74-80 `carries_execution_id`（先頭 3 語 DESCRIBE/DESC・SHOW CREATE TABLE。`result_output.rs:196` の `.metadata` の ID も共有）
- F2 Cursor 系（`cursor.rs:32` `keyword`。続きが ASCII 英数字・`_` なら不一致、それ以外は空白なしでも一致）: `classification.rs:113-167` alter_table_action、`target_table.rs:79,82`、`content_type.rs:94,101` is_literal_only_select、`completion.rs:38` describe_target_name
- F3 SQL の先頭で分岐する手段はこの 2 系統だけ（starts_with 等の別経路は無い）
- F4 decisions.md:71-88「キーワードの照合は `Cursor::keyword` 1 つだけ」「文の種類の判定箇所を増やさない。`ResultFile::of` と statement_type／substatement_type は同じ前処理」。architecture.md「Athena の見え方への変換」に #200 の断層が明記済み
- F5 name.rs:46-49 のコメントは「`words()` はドット前後に空白のある修飾名を複数語に数える」前提を述べる（words を変えるなら書き直し）
- F6 checklist:47 字句処理は引用符 → コメント → その他の順（words の改修でも守る）

### 今の値（Explore A がプローブで確定。空白ありとの差）
| SQL | Statement | Substatement | ファイル | Content-Type | 他 |
|---|---|---|---|---|---|
| `SELECT(1)`・`SELECT'a'`・`SELECT*FROM t` | UTILITY（空白あり DML） | None（SELECT） | `.txt`（`.csv`） | binary | |
| `EXPLAIN(TYPE IO) SELECT 1` | UTILITY（DML） | None（EXPLAIN） | `.txt` | binary（application） | 行分割しない（completion.rs:52-55） |
| `ALTER TABLE"t" ADD COLUMNS (m int)` | DDL | None（ALTER_TABLE_ADD_COLUMN） | `.txt` | binary | 対象テーブルの形式を問い合わせない |
| `SHOW CREATE TABLE"t"` | UTILITY | None（SHOW_CREATE_TABLE） | `.txt` | binary（application） | carries_execution_id false（true）、fixed_column 無し |
| `WITH"x" AS (...)`・`VALUES(1)` | UTILITY（DML） | None（SELECT） | `.txt`（`.csv`） | | |
| `DESCRIBE"t"`・`DESC"t"` | UTILITY | None（DESCRIBE_TABLE） | `.txt` | | carries false（true） |
| `DROP TABLE"t"` | DDL | None（DROP_TABLE） | `.txt` | | 形式の問い合わせが飛ぶ |
| `CREATE TABLE"t" AS SELECT` | DDL | None（CTAS） | `.txt`（`tables/<id>`） | | |
| `CREATE VIEW"v" AS`・`SHOW CREATE VIEW"v"` | | None（CREATE_VIEW／SHOW_CREATE_VIEW） | | | fixed_column 無し |
| `OPTIMIZE"t" ...` | UTILITY（DDL） | None（CTAS） | `.txt`（`tables/<id>`） | | |
割れない: `INSERT INTO"t"`・`SHOW TABLES IN"db"`・`SHOW COLUMNS FROM"t"`（見る語がワイルドカード）

### 固定テスト（#195 の「寄せる前と同じ」）
`src/operation/classification/tests.rs:367`（w5〜w8 :485-488、q4 :427、q5 :428）、`src/results/tests.rs:435`（q4 :461、q5 :462、w5〜w8 :485-488 付近）、`src/content_type.rs:250`（:270、:283-285）。completion.rs:253・target_table/tests.rs:512 は空白なしの形を含まない

### 実測の記録と足場（Explore B）
- 雛形は `tools/measure/ctas-parenthesized-query.sh`（#199。start_query_retry・fetch・head_object・emit_row・record_table_info・preflight・trap・PROBE_DDL）
- 既存の記録: `(SELECT 1)` は DML/SELECT/`.csv`（statements.md:127-140）、EXPLAIN の変種（空白あり）は #92 で行分割・application（statements.md:150-192）、SHOW CREATE TABLE の Content-Type は形式で割れる（result-files.md:457-490）、ALTER の 8 種（result-files.md:228-274）、キーワード間コメントは空白扱い（statements.md:66-76）、`/* c */ SHOW CREATE TABLE` は分類は正しいが Hive パーサが FAILED（statements.md:104-107。分類とパース可否が別にずれる前例）
- 過去の生データ（~/athena-*-measurements の全 execution.json の Query）に空白なしの形は 0 件（14:22 grep）
- 直す候補の docs: caveats.md:5-38・66-124、result-files.md:103-125、api.md:121-136、unmeasured.md:11
- 落とした番号: B-7（CHANGELOG の現状。出荷時に見る）、C-1 の API 一覧の大半（計画に使う words/Cursor 以外）

### 手元の Trino 482 が受け付ける形（2026-09-25 14:23、memory カタログ、trino CLI）
受け付けた: `SELECT(1)`・`SELECT'a'`・`SELECT*FROM t`・`SELECT"x" FROM t`・`EXPLAIN(TYPE IO) SELECT 1`・`EXPLAIN(SELECT 1)`・`EXPLAIN ANALYZE(SELECT 1)`・`WITH"w" AS (...)`・`WITH w AS(SELECT 1 x)SELECT*FROM w`・`VALUES(1)`・`DESCRIBE"t"`・`DESC"t"`・`SHOW COLUMNS FROM"t"`・`SHOW CREATE TABLE"t"`・`SHOW TABLES IN"default"`／`FROM"default"`・`ALTER TABLE"t" ADD COLUMN m int`・`INSERT INTO"t"(x) VALUES(2)`・`CREATE TABLE"t2" AS SELECT 1 x`・`DROP TABLE"t2"`・`CREATE VIEW"v" AS SELECT 1 x`・`SHOW CREATE VIEW"v"`・`DROP VIEW"v"`
構文で弾いた: `EXPLAIN"t"`（line 1:8 mismatched input）

## 実測の手順
- `tools/measure/keyword-boundary.sh`（雛形 ctas-parenthesized-query.sh。A 群 16・B 群 8・D 群 15 と準備・後始末）
- 実行: `tools/dev.sh OUTPUT=s3://<バケット>/<プレフィックス>/ DB=<DB> PROBE_DDL=1 bash tools/measure/keyword-boundary.sh`
- 出力: `~/athena-keyword-boundary-measurements/run-<日時>/summary.txt`（実名マスク済み）。`<label>.sql`・`reason.txt`・`gqr-detail.txt` は実名を含みうる
- 本物への StartQueryExecution は PROBE_DDL=1 で約 44 本。DDL は `athena_local_probe_200*`（Hive・CTAS 2・VIEW 2・Iceberg）を作って消す。同名があれば preflight で止まる
- 受け入れ判定（統括役 14:40）: 全呼び出しに OutputLocation と Database を明示／run は SUCCEEDED のときだけ 0／後始末は空白ありの形 + IF EXISTS、失敗時は trap で再送／ドライランは未実施（bash -n のみ）

## 実測の結果
（未着手）

## 実測の経緯

## 設計判断

## 計画

## 計画レビューの結果

## フェーズの記録

## レビュー実施記録

## dismissed

## deferred
| 指摘 | severity | 再トリアージ |
|---|---|---|

## 覆した事前判断
なし

## 実機検証

## 範囲外の発見
| 内容 | 出どころ | 起票 |
|---|---|---|

## 委譲の記録
| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| Step 3 調査 | 委譲（Explore 3 本、sonnet） | |
| 手元の Trino 確認 | 自分 | コマンド 1 本で済む |
| 実測スクリプト作成 | 委譲（general-purpose、sonnet） | 受け入れ判定は自分（本番の認証情報で流すため） |
| 計画の下書き | 委譲（Plan、sonnet） | Explore の生の返答ではなくノートの転記（F1〜F6・表）と実コードを読ませた。生の返答は長く、ノートの転記で計画に要る file:line は残している |

## 出荷

## 時系列
- 14:17 quick-implement で着手、ゲートで事実・影響範囲が外れて implement-issue に escalate（add349f）
- 14:21 Step 3 の Explore 3 本を起動（判定箇所／実測の記録とスクリプト／athena-sql crate）。先行作業: 手元の確認用に Trino を起動
- 14:23 手元の Trino で空白なしの 25 形を確かめた（24 形を受け付け、`EXPLAIN"t"` だけ構文エラー）
- 14:29 Explore 3 本の転記を終えた。14:31 実測スクリプト作成（tools/measure/keyword-boundary.sh）と Plan を並列に起動
- 14:40 実測スクリプトを受け入れ、コミットして実測を依頼

---

## quick ノート（escalate 元）
### quick #200 語の境界の規則が 2 系統で、SELECT(1)・EXPLAIN(TYPE IO)・ALTER TABLE"t"・SHOW CREATE TABLE"t" の分類が本物と違う可能性がある
- 入力: issue #200。モード: standalone。ブランチ main-200、着手前 SHA e9d1f15
- ゲート: サイズ 見込み不明（実測次第）／呼び出し元 9（athena_sql::words を src/operation/classification.rs・src/results.rs・src/operation/target_table.rs が使う。crate と src にまたがり同一モジュールでない → 外れ）／契約 通過（本物に合わせる修正）・新規性 通過・事実 外れ（本物の Athena の値が未実測）・判断 通過
- 時系列: 着手 14:17 ／ red なし ／ green なし ／ コミット 14:19
- 検証: 未実施（コードに触らず escalate）
- 自己レビュー: 該当なし
- 打ち切り・escalate: 提案あり（事実: 本物の未実測、影響範囲: 呼び出し元 9・3 ファイル・crate と src）→ 選択: implement-issue
- 予算の実績: Bash 約 8 本、所要 2 分、サブエージェント 0 本
- 出荷: main-200（implement-issue に引き継ぎ）

### 引き継ぎ
- 判定箇所:
  - `crates/athena-sql/src/words.rs:15` `words()` — 空白とコメントだけで語を切る（`(SELECT 1)` → `["(SELECT", "1)"]`）。`src/operation/classification.rs:8` の `words()` が `results::strip_open_parens` で先頭の `(` を剥がして使い、`src/results.rs:48` の `ResultFile::of` も同じ語を使う
  - `crates/athena-sql/src/cursor.rs:32` `Cursor::keyword` — 識別子の文字以外を境界にする。使うのは `src/operation/classification.rs:113` の `alter_table_action`、`src/operation/target_table.rs:79`、`src/content_type.rs:94`
  - `ALTER TABLE"t"` は `alter_table_action` に入る前の呼び出し元の guard `word(1) == "TABLE"`（words 系）で弾かれる（issue 本文）
- 共有できる既存機構: `athena_sql::Cursor::keyword`（語の境界を識別子の文字で切る規則をすでに持つ）、`athena_sql::words_iter`（範囲つきの語）
- 原因の仮説: 先頭語の判定（StatementType・結果ファイル名・EXPLAIN の行分割）は words 系、キーワード列の読み取りは Cursor 系で、`SELECT(1)`・`SELECT'a'`・`SELECT*FROM t`・`EXPLAIN(TYPE IO)`・`ALTER TABLE"t"`・`SHOW CREATE TABLE"t"` のように語の直後に空白なしで記号や引用符が続く形で両者が割れる
- 固定テスト: #195 で今の値を焼き込んだ「寄せる前と同じ」の表の w5〜w8・q4・q5（`src/operation/classification/tests.rs`・`src/results/tests.rs`・`src/content_type.rs`・`src/operation/completion.rs`・`src/operation/target_table/tests.rs` のどれか。grep「寄せる前と同じ」）。揃えるならこれらの期待値を実測値で焼き直す
- 未実測: 上の 6 形それぞれの本物の StatementType／SubstatementType／結果ファイル名／Content-Type（`EXPLAIN(TYPE IO)` は EXPLAIN の行分割も）。docs/dev/unmeasured.md:11 の `SHOW CREATE TABLE"t"` の項も同じ形
- 再現: Trino 482 は 6 形をすべて SUCCEEDED にした（#195 の `.claude/issue-notes/195-verify/e2e.sh` ケース 4・8。コミット 58780f9 の時点の git 履歴にある）。athena-local 側の今の値はユニットテストで固定済み
- 既存の実測スクリプトで流用できそうなもの: `tools/measure/explain-variants.sh`・`content-type-rules.sh`・`alter-variants.sh`・`txt-result-content-type.sh`
- stash: なし
