# quick #133 jdbc-drivers の preflight の ClientRequestToken が 32 文字未満で athena-local の検証に落ちる
- 入力: issue #133。モード: standalone。ブランチ main-133、着手前 SHA 6827371（main 3a641e6 から）
- ゲート: サイズ 1 行・1 ファイル（足場のシェル。本番コード 0）／呼び出し元 1（judge.sh の check_nginx_log_format）／契約・新規性（ほかの足場は既に `$(uuidgen)`）・事実（#128 の基準値で再現済み）・判断 → 通過
- 時系列: 着手 03:34 ／ red（#128 の基準値、FAIL 1）／ green 03:34〜03:35 ／ コミット 03:36
- 検証: `tools/dev.sh SKIP_BUILD=1 DRIVER_VERSIONS=3.8.1 tools/e2e/jdbc-drivers/verify.sh` → PASS 9 INFO 3 FAIL 0 rc 0（直す前は PASS 2 FAIL 1 で残りは SKIP）。標準コマンド（`tools/dev.sh` の cargo fmt --check / clippy --all-targets --locked -D warnings / test --locked）ok、329 passed／ミューテーション: 元の `issue111-preflight-$$-$(date +%s)`（約 30 文字）→ preflight の FAIL（#128 の基準値・#128 の V10・#129 の S6 の 3 回、同じ INVALID_INPUT の文言）
- 自己レビュー: 分岐 0 件（値を変えただけ）／エラーの出口: 無し／呼び出し元 1 箇所（preflight の StartQueryExecution）
- 打ち切り・escalate: 提案なし
- 予算の実績: Bash 約 5 本、所要 2 分（待ち時間を除く）、サブエージェント 0 本（足場のシェルだけなので止めない独立レビューは流さない）
- 出荷: main-133
