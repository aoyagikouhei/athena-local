# issue #30 src/convert.rs を分割する

## 基本情報

- issue: #30（出どころは #19 の範囲外の発見）
- ベースブランチ: `main` ／ ブランチ: `main-30`
- **着手前 SHA: `c51cfce`**（quick ノートのコミット。レビューの `base` の正。quick 着手時点は `15accc2`）
- 経路: quick-implement のゲートで**変更サイズ**（本体 419 行・本番ファイル 1→4〜5。閾値 50 行・3 ファイル）と**新規性**（`src/convert/` の新ファイル）が外れ、ユーザーの選択で implement-issue に引き継いだ（issue コメント: <https://github.com/aoyagikouhei/athena-local/issues/30#issuecomment-5748340478>）
- 完了条件（issue 原文）: (1) 1 ファイルの本体が 400 行を超えない (2) `cargo test --locked` が無変更で green (3) README と CHANGELOGS は変えない・CLAUDE.md の「Athena の見え方への変換」の節を新構成に合わせる

## 事前に確認した事実

### 分割前の基準値（統括役が実測。2026-09-20）

| 指標 | 分割前 |
|---|---|
| `cargo test --locked` | **221 passed**（`--lib` 124、`--lib convert` フィルタで **27**） |
| `cargo fmt --check` | ok |
| `cargo clippy --all-targets --locked -- -D warnings` | 警告 0 |
| `src/convert.rs` | 869 行（本体 419・テスト 449）、非空行 792 |
| 分岐の計数（本体 1-419 と全体 1-869 で同数 = **テストに分岐が無い**） | `if` 17 / `match` 12 / `=>` 55 / `return` 6 |
| 本体の `fn` | 20 |
| 行ソートのハッシュ（`use`・`mod`・空行を除きインデントを潰して sort → sha256） | `6c1dbd4dbcb5980497abd36c36e0a972f2acd6ec7a991326f55192f9bb1fc4c9` |

### 依存グラフ（quick の実測。全 24 宣言の参照元を本体 1-419 で grep）

塊をまたぐ辺は **4 本だけ**で一方向。**塊 2（レンダリング）と塊 3（型マッピング）の間に直接の辺は無い**。

| 辺 | 呼び出し位置 |
|---|---|
| 塊 1 → 塊 2 | `all_rows` が `to_var_char_value` を呼ぶ（`:33`） |
| 塊 1 → 塊 3 | `column_infos` が `to_column_info` を呼ぶ（`:55`） |
| 塊 2 → 共有 | `to_var_char_value` が `raw_type` を呼ぶ（`:63`） |
| 塊 3 → 共有 | `athena_type` が `raw_type` を呼ぶ（`:267`） |

塊の内部で閉じている辺: 塊 1 は `result_set` → `to_row`(`:45`) / `column_infos`(`:47`)。塊 2 は `to_var_char_value` → `number_text`(`:66`) / `varbinary`(`:63`) / `ValueType::parse`(`:67`) / `render`(`:68`)、`ValueType::parse` → `type_argument`(`:104,:107`) / `key_order`(`:106`) / `named_argument`(`:112`)、`render` → `varbinary`(`:158`) / `number_text`(`:160`) / `join`(`:162,:173,:182`) / `compare_numbers`(`:169`) / 再帰、`number_text` → `java_double`(`:202`)、`varbinary` → `decode_base64`(`:351`)。塊 3 は `athena_type` → `base_type_name`(`:269`) / `UNBOUNDED_VARCHAR`(`:295`) / `AthenaType`(`:308`)、`to_column_info` → `athena_type`(`:405`)。

### 塊の割り当てと物理的な分断（Explore A）

本体の宣言は 24 個。`///` は本体に 32 行、属性（`#[derive]` 等）は本体に **0 件**、宣言直前の通常コメント（`//`）も **0 件**（すべて `///` かコメント無し）。

- 塊 1（入口）: `all_rows`(`:12`) / `result_set`(`:41`) / `column_infos`(`:54`) / `to_row`(`:393`)
- 塊 2（レンダリング）: `to_var_char_value`(`:60`) / `ValueType`(`:80`) / `KeyOrder`(`:92`) / `impl ValueType`(`:97`、`parse:99`) / `type_argument`(`:122`) / `key_order`(`:127`) / `named_argument`(`:143`) / `render`(`:155`) / `number_text`(`:200`) / `java_double`(`:209`) / `compare_numbers`(`:337`) / `varbinary`(`:350`) / `decode_base64`(`:362`) / `join`(`:389`)
- 塊 3（型マッピング）: `AthenaType`(`:254`) / `UNBOUNDED_VARCHAR`(`:262`) / `athena_type`(`:265`) / `base_type_name`(`:318`) / `to_column_info`(`:404`)
- 共有ヘルパ: `raw_type`(`:75-77`、3 行)

**本体の中で塊が 5 つの非連続な範囲に分断されている**: 塊2前半(58-251) → 塊3前半(253-333) → 塊2後半(335-391) → 塊1の `to_row`(393-402) → 塊3の `to_column_info`(404-418)。**行番号の範囲では切れない**（design-checklist の #19 由来の 1 行がそのまま効く）。

### 失ってはいけない実測の記録（Explore A-7）

本体の「実測」コメントは 4 箇所。うち **2 件は宣言直前の `///`** で、行番号で単位を切ると静かに消える種類:

- `:198` `/// Athena の実測: ...`（`number_text` の doc）← 危険
- `:253` `/// ColumnInfo の Type / Precision / Scale / CaseSensitive（2026-09-14 に本番 Athena で実測）。`（`AthenaType` の doc）← 危険
- `:43`（`result_set` 本体内の通常コメント。2026-09-14 実測）／`:165`（`render` 本体内の通常コメント）は関数ごと運べば付いて移る

テスト側にも 3 箇所（`:515`・`:538`・`:787`）と、`mod tests` のモジュール doc `:422-424`（「入力は Trino 482 の応答、期待値は本番 Athena の VarCharValue を、どちらも 2026-09-14 に採取」）がある。

### テスト 27 件の塊分類と行数（Explore A-3・A-6）

| 塊 | テスト | 行数 |
|---|---|---|
| 塊 1 | #23 `列の無い結果は列名行も作らない`(`:833`) のみ | 5 |
| 塊 2 | 20 件（#1-13・17-21・24-27） | 209 |
| 塊 3 | #14 `列情報の型は_athena_の実測と同じになる`(`:592`、94 行)・#15(`:687`、22 行)・#16(`:710`、23 行) | 139 |
| **またぐ** | **#22 `列ごとに自分の型で書く`(`:797-831`)** | 35 |
| 合計 | 27 件 | 388（検算一致） |

- **#22 は塊 1 と塊 2 の両方を検証している**: `Outcome` を組み立てて `all_rows` を直接呼び、列に `array_of(INTEGER)` を持たせて `[1, 2]` の配列表記まで確かめる（`all_rows`→`to_var_char_value`→`render` の経路）。どちらの `mod tests` に置くか一意に決まらない。
- **`result_set` / `column_infos` / `to_row` を直接呼ぶテストは 0 件**。塊 1 の直接テストは #22・#23 の 2 件だけで、いずれも `all_rows` 経由。
- テストヘルパ: `format`(`:428-432`、5 行、塊 2 の 20 件が使う)・`INTEGER`(`:434`)・`array_of`(`:436-438`)（この 2 つは #22 も使う）・`column_with`(`:577-590`、14 行、**#14 専用**。#15・#16 は `Column { .. }` を直接組み立てている）。`mod tests` 直下の import は `use super::*;`(`:426`) の 1 本のみ。

### 塊 2 が 400 行を超える見込み（Explore A-6）

塊 2 の本体は 194+57=**251 行**。テスト 209 + ヘルパ 9 + `mod tests` の枠 6〜9 + 空行 18〜20 を足すと **≒497 行**（#22 を含めれば 532 行）。本体+テストの最小値 460 行で既に 400 行超。**issue 本文の 3 分割案は、全体行数で測ると成立しない**（#19 も issue 案の 2 分割が 516 行で不成立になり 4 ファイルに変えた）。

### `use` の配分（Explore A-2）

| `use` | 使う塊 |
|---|---|
| `:3` `std::cmp::Ordering` | 塊 2 専用（`compare_numbers` の戻り値型と `:343-344`） |
| `:5` `serde_json::Value` | 塊 2 ＋ 共有ヘルパ `raw_type` 専用（塊 1・塊 3 は型名として書かない） |
| `:7` `crate::athena::{ColumnInfo, Datum, ResultSet, ResultSetMetadata, Row}` | **`ColumnInfo` だけ塊 1（`column_infos` の戻り値型）と塊 3（`to_column_info` が構築）の両方**。`Datum`・`ResultSet`・`ResultSetMetadata`・`Row` は塊 1 専用 |
| `:8` `crate::trino::Outcome` | 塊 1 専用 |
| （`crate::trino::Column`） | `use` されておらず全箇所フルパス。分割後も追加 import 不要 |

### #19（`src/operation.rs` → `src/operation/`）の前例（Explore B）

| 論点 | #19 の実物 |
|---|---|
| コミットの割り方 | 分割本体 `18500c9`（4 ファイル + `mod.rs`）と文書の名指し修正 `fe0cefc`（CLAUDE.md・TODO.md・`src/athena.rs`・`src/catalog.rs`・`src/store.rs` のコメント）を**別コミット**にした |
| 分割後の行数 | `execution.rs` 362 / `query_execution.rs` 241 / `classification.rs` 164 / `work_group.rs` 127 / `mod.rs` 10（旧 863 → 計 904、+41） |
| 差分の性質 | **純粋な移動**。関数本文への手入れ 0。唯一の書き換えは越境呼び出しの修飾（`super::classification::words(query)` など 4 箇所） |
| 可視性 | **`pub(super)`**（`classification.rs:4,13,26`）。「公開範囲は最小が原則」。`pub(crate)` の慣習 5 件はすべて**別モジュール間**の共有で、同一モジュール内のサブモジュール間は #19 が初のため慣習は決め手にならなかった |
| 越境の呼び方 | `use super::classification;` を**書かず**フルパスの `super::classification::xxx(...)` |
| 共有ヘルパの置き場所 | `mod.rs` にもどちらの実装ファイルにも置かず、**専用の第 4 ファイル**を新設。ただし `classification.rs` は 164 行の実質的な塊で、3 行のヘルパの前例ではない |
| `mod.rs` | `mod` 宣言 + `pub(crate) use` の再エクスポートだけの 10 行。旧ファイルの `//!` doc を引き継いだ |
| テストの配置 | **対象関数が移った先のファイルに `mod tests` を追従**。集約せず、`tests/` にも出さない。件数 6 → 2+4 = 6 で一致 |
| `lib.rs`・呼び出し元 | **1 文字も変わらなかった**（`mod operation;` のまま） |
| clippy | `[lints]`・`clippy.toml`・`#![...]`・`#[allow]` はすべて無し。分割で新しい警告は出なかった |
| 検証手法 | 「`use`・`mod`・空行を除いて行をソートして diff」＋「分岐の計数（`if`/`match`/`=>`/`return`）の一致」 |
| 却下された提案 | 50 行超の関数を内部分割すること（設計判断 6:「関数 50 行はこのリポジトリの規則ではない」「分岐の形を書き換えるリスクが完了条件に寄与しない利得を上回る」） |

### 書き換えが必要な文書（Explore C）

- **`CLAUDE.md:43` の 1 行だけが「現状記述」**。`README.md`・`CHANGELOGS.md`・`TODO.md`・`Dockerfile`・`tests/`・`.github/`（ci.yml・docker.yml）・`Cargo.toml` の言及は **0 件**。`src/` のコード内コメントに `convert.rs` を名指しする箇所も **0 件**（#19 は 3 ファイルのコメント修正が要ったが今回は不要）。
- `.claude/issue-notes/*.md` の 8 件（1・2・4・5・9・17・19・30）は**日付付きの過去ログなので書き換え対象外**（#19 も同じ扱い）。
- 手本は **`CLAUDE.md:29` の `operation/` の記述**: ディレクトリ名 → 中のファイルを列挙 → 横断的要素の役割 → 「`mod.rs` は `mod` 宣言と再エクスポートだけで、外からの見え方は分割前と変わらない」。
- `CLAUDE.md:41-49`「Athena の見え方への変換」節の体裁は「`ファイル名` — 説明」の**1 ファイル 1 行**（`:43` の `convert.rs` の行だけが長文、`:44` はファイル名で始まらない例外）。
- 呼び出し元 3 ファイルの `use crate::convert;` は**モジュール単位の import** なので、`mod.rs` が再エクスポートすれば 1 行も変わらない。`ci.yml`・`Cargo.toml` はパス非依存で変更不要。`src/lib.rs:7` は `pub mod` ではなく `mod convert;`。

### 落とした番号と理由

- Explore A-6 のテスト 27 件の個別行数の全表 → 塊ごとの合計だけ残した（個別の値は分割後の配分の検算に使うが、ノートには合計で足りる）
- Explore B-8 のコミット・PR 文体の全文引用 2 例 → 文体の要点（日本語・「〜する」で終わる一行・本文に「なぜ」・節見出し・断定形）だけ残した
- Explore C-1 の過去ログ 8 件の全行引用 → 「過去ログは書き換え対象外」の判断だけ残した
- Explore A-8 の未確認事項（呼び出し元側に `result_set`/`column_infos`/`to_row` を間接検証するテストがあるか）→ **未検証の前提として下に残す**

### 未検証の前提

- `src/results.rs`・`src/operation/*.rs` 側のテストが `result_set` / `column_infos` / `to_row` を間接的に検証しているか（convert.rs 内には直接テストが 0 件。分割後にこの 3 つを守るテストがどこにあるかは 7-0 で確かめる）
- 分割後に `clippy -D warnings` が新しい警告を出さないか（`module_inception` は `src/convert/convert.rs` を避ければ回避できる。#19 では新しい警告は出なかったが、`pub(super)` の追加が今回も無警告かは未確認）

## 時系列

| 時刻 | 出来事 | SHA |
|---|---|---|
| 16:08 | quick-implement 着手。ゲートで変更サイズ・新規性が外れた | — |
| 16:15 | ユーザーが「implement-issue で進める」を選択。quick ノートをコミット | `c51cfce` |
| 16:16 | issue #30 に引き継ぎをコメント | — |
| 16:18 | 分割前の基準値を実測（テスト 221・fmt ok・clippy 警告 0・分岐計数・行ソートのハッシュ） | — |
| 16:29 | Explore 3 本の結果を転記。Plan 1 本を起動 | — |
| 16:47 | Plan が返る。塊 2 が全体 497 行で issue の 3 分割案が不成立と判明 | — |
| 16:50 | Step 4: ユーザーが「全体基準・5 ファイル」を選択 | — |
| 17:11 | 計画を転記（Plan の構造の判断 2 点を覆した）。Step 6 の計画レビュー Explore 2 本を起動 | — |
| 17:25 | 計画レビュー 2 本が返る（high 0・medium 2・low 4、採用 5・棄却 1）。計画を確定してコミット | `f848a8b` |
| 17:27 | 7-0 を記録し、フェーズ 1 の実装を general-purpose 1 本に委譲 | — |
| 17:46 | フェーズ 1 が返る。7-5b で差分を確認して受け入れ、コミット | `52b01de` |
| 17:50 | フェーズ 2（`CLAUDE.md:43`）を自分で書き換え。7-7 は分割不要と判定 | — |

## 委譲の記録

| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| Step 3 の調査 | 委譲（Explore 3 本、sonnet） | 規定どおり |
| 分割前の基準値の実測 | 自分 | コマンド 3 本で終わり、指示を書く方が長い |
| Explore の結果の転記 | 自分 | 読み取り専用の委譲先なので規定どおり |
| Step 3 の計画の下書き | 委譲（Plan 1 本、sonnet） | 規定どおり |
| Step 5 の計画の転記と判断の書き換え | 自分 | 規定どおり（転記は理解の検査。Plan の構造の判断 2 点を覆した） |
| Step 6 の計画レビュー | 委譲（Explore 2 本、sonnet） | 小規模経路の規定は 1 本だが、Plan の構造の判断を 2 点覆したため契約・再利用と構造・削減に分けた |
| フェーズ 1 の実装（7-1〜7-4） | 委譲（general-purpose 1 本、sonnet） | 規定どおり（移動 419 行で小規模経路の 150 行を超える） |
| フェーズ 1 の受け入れ判定（7-5b）とコミット | 自分 | 規定どおり |
| フェーズ 2（`CLAUDE.md:43`） | 自分 | 1 行の書き換えで、指示を書く方が作業より長い |

## 設計判断

| # | 論点 | 決定 | 理由・根拠 |
|---|---|---|---|
| 1 | 完了条件の「400 行」の基準 | **全体基準（本体＋テスト）で 400 行以内。`src/convert/` に `mod.rs` + 5 ファイル** | **Step 4 でユーザーが選択**。issue 本文の文言は「本体が 400 行」だが、#19 の分割軸の判定（`19.md:57`・`:235`）は全体行数（参照系 516 行＝関数 322 + テスト 147 + 定数 26）で不成立と判断している。原則「人間が 1 画面でレビューできるか」に沿うのは全体基準。なお `19.md:242` は `store.rs`(614/281)・`results.rs`(610/281)・`config.rs`(441/223) を本体基準で「分割不要」としており、リポジトリ内に両方の基準の実例がある。ユーザーが全体基準を選んだので、issue 本文の 3 分割案は覆る |
| 2 | 塊 2（レンダリング、本体 251 行）の分け方 | **`value_type.rs`（typeSignature の解析）/ `render.rs`（値の入口と複合型の再帰レンダリング）/ `scalar.rs`（スカラ値を Athena の表記にする）の 3 つ。`number_text`・`java_double` は `scalar.rs`、`compare_numbers`・`join` は `render.rs`** | **Plan の案を覆した**。Plan は `varbinary`/`decode_base64` を `scalar.rs`、`number_text`/`java_double` を `render.rs` に置いたが、`number_text`(`:66`,`:160`) と `varbinary`(`:63`,`:158`) は**どちらも `to_var_char_value` と `render` の両方から呼ばれる**スカラ値の表記で対称なのに置き場所が分かれる。対称に揃えると `scalar.rs` の責務が「スカラ値を Athena の表記にする」の 1 つになり、`render.rs` には `render` からのみ呼ばれる補助（`compare_numbers`(`:169`)・`join`(`:162,:173,:182`)）が残って軸が揃う。副次的に `render.rs` の 400 行への余裕が 40 → 73 行に増える |
| 3 | 共有ヘルパ `raw_type`(`:75-77`、3 行) の置き場所 | **`value_type.rs` に `pub(super)` で置き、`render.rs` と `athena_type.rs` から `super::value_type::raw_type(...)` で呼ぶ** | **Plan の案を覆した**。Plan は「`mod.rs` に private のまま置けば Rust の可視性規則（private は子孫モジュールから見える）で `pub(super)` が不要」とした。可視性は確かに最小になるが、`mod.rs` に実装が入って #19 の形（`operation/mod.rs` は `mod` 宣言と再エクスポートだけの 10 行）を崩す。`raw_type` の中身は `signature?.get("rawType")?.as_str()` で **`value_type.rs`（typeSignature の解析）の責務そのもの**。#19 の設計判断 3（共有関数を責務が合うファイルに置き `pub(super)` にする）と同型で、専用ファイルも要らない |
| 4 | 塊をまたぐテスト #22 `列ごとに自分の型で書く`(`:797-831`、35 行) の置き場所 | **`result_set.rs`** | このテストが直接呼ぶのは `all_rows`(`:825`) だけ（#19 の「対象関数が移った先に追従」がそのまま一意に決める）。配列表記 `[1, 2]` の検証は `all_rows` 内部を通る副次的なもので、同じ検証は #1 `配列は区切りに空白が入る`(`:440-446`) が単独で固定している。`render.rs` に置くと `super::result_set::all_rows` の越境が 1 本増え、`render.rs` が +35 行になる |
| 5 | テストヘルパ `INTEGER`(`:434`、1 行)・`array_of`(`:436-438`、3 行) | **`render.rs` に残し、`result_set.rs` に 4 行だけ複製する**（一致台帳に載せる） | #22 が `array_of(INTEGER)` を使うが `format` は使わないため、`format`(5 行) は複製せず `INTEGER`/`array_of` の 4 行だけ複製する。`#[cfg(test)]` 境界を越えて `pub(super)` で共有する配線はこのリポジトリに先行例 0 件で、4 行の複製に対して過大（新しい抽象を増やさない）。移動だけなので値が変わることはなく、一致の守り方は「移動フェーズでどちらの値も変更しない」ことで足りる |
| 6 | `mod tests` のモジュール doc(`:422-424`) | **`render.rs` の `mod tests` にそのまま移す（複製しない）** | この doc は `format()` ヘルパ（JSON 文字列から `data`/`signature` を読む方式）の使い方とデータの出所（「入力は Trino 482 の応答、期待値は本番 Athena の VarCharValue を、どちらも 2026-09-14 に採取」）を説明したもの。この方式を使うテストは `render.rs` に集まる 22 件だけで、`result_set.rs` の #22/#23・`athena_type.rs` の #14/#15/#16 はいずれも直接値を組み立てている |
| 7 | 可視性 | **`pub(super)` 8 件**（`to_var_char_value`・`raw_type`・`ValueType`・`KeyOrder`・`ValueType::parse`・`number_text`・`varbinary`・`to_column_info`）。`column_infos` は実装ファイル内で素の `pub` にし `mod.rs` の `pub(crate) use` で絞る | #19 の設計判断 4（公開範囲は最小が原則。`pub(super)` は #19 が確立）をそのまま踏襲。`column_infos` を素の `pub` にして `mod.rs` で narrow するのは #19 の各操作関数と同じ形で、crate から見た実効可視性は変わらない（`src/lib.rs:7` は `mod convert;` で `pub` でないため、元の `pub fn all_rows` も実質 `pub(crate)`） |
| 8 | 越境の呼び方 | **関数はフルパス**（`super::render::to_var_char_value(...)` など。`use super::` を書かない）。**例外: `render.rs` の型 import は `use super::value_type::{KeyOrder, ValueType};`** | 関数のフルパスは #19 の慣習（`super::classification::words(query)`）。型だけ例外にするのは、`render` が `ValueType` を**パターンマッチで多用**しており（`:158,:161,:166,:180` の 4 箇所 + `:67`）、`(super::value_type::ValueType::Array(element), Value::Array(items))` と書くと読めなくなるため。関数呼び出しと型の参照は別扱いにする |
| 9 | フェーズ分割 | **2 フェーズ・2 コミット**。フェーズ 1 = 分割そのもの、フェーズ 2 = `CLAUDE.md:43` の書き換え | #19 が分割本体(`18500c9`)と文書の名指し修正(`fe0cefc`)を別コミットにしたのと同じ。分割コミットを「`use`・`mod`・空行を除けば純粋な移動」と機械的に検証できる差分として孤立させるため |
| 10 | 50 行超の関数（`athena_type` 51 行・`java_double` 45 行・`render` 42 行）の内部分割 | **やらない。起票もしない** | #19 の設計判断 6 と `19.md:128` で決着済み:「関数 50 行はこのリポジトリの規則ではない」「分岐の形を書き換えるリスクが 400 行制約にも CI にも寄与しない利得を上回る」 |

## 計画

### フェーズ 1: `src/convert.rs` を `src/convert/` の 6 ファイルに割る

#### ファイル構成と見込み行数

| ファイル | 責務（1 つ） | 本体 | テスト | 全体 |
|---|---|---|---|---|
| `mod.rs` | `mod` 宣言と再エクスポート（実装を持たない） | ~10 | 0 | **~10** |
| `result_set.rs` | `Outcome` → `ResultSet` 変換の入口 | ~58 | #22(35)+#23(5)+`INTEGER`/`array_of` 複製(4)+枠(6)+空行(4) ≒ 54 | **~112** |
| `value_type.rs` | `typeSignature` の解析 | ~84 | 0 | **~84** |
| `render.rs` | 値の入口と複合型の再帰的なレンダリング | ~80 | 22 件(209)+`format`/`INTEGER`/`array_of`(9)+モジュール doc(3)+枠(6)+空行(20) ≒ 247 | **~327** |
| `scalar.rs` | スカラ値を Athena の表記にする | ~96 | 0 | **~96** |
| `athena_type.rs` | Trino の型から Athena の型への変換 | ~97 | #14(94)+#15(22)+#16(23)+`column_with`(14)+枠(5)+空行(5) ≒ 163 | **~260** |
| 合計 | — | ~425 | ~464 | **~889**（旧 869 + 分割の定型オーバーヘッド。#19 は 863→904 で +41） |

最大は `render.rs` の ~327 行で、400 行に対して **73 行の余裕**がある。目安の「新規ファイル 200 行以内」は `render.rs`・`athena_type.rs` が超えるが、#19 の `execution.rs`(362 行) が許容された 3 点（`19.md:203`）と同じ理由で更に割らない: (1) 新規コードを 1 行も書かず既存コードの移動先である (2) 責務は 1 つ (3) `TODO.md` に `convert` 関連の追加項目が無く成長の予定が無い。

#### 移す単位（宣言 + その直前の `///` ブロックと属性を 1 単位。行番号は参考）

**`result_set.rs`**
- `pub fn all_rows`（`:10-39`、doc 2 行）→ `pub` のまま。`to_var_char_value` の呼び出しを `super::render::to_var_char_value(...)` に修飾
- `pub fn result_set`（`:41-50`、doc 無し。**本体 `:43` に 2026-09-14 実測のコメントがある**）→ `pub` のまま
- `pub(crate) fn column_infos`（`:52-56`、doc 2 行）→ 素の `pub` に。`to_column_info` の呼び出しを `super::athena_type::to_column_info` に修飾
- `fn to_row`（`:393-402`）→ private のまま
- `mod tests`: #22（`:797-831`）・#23（`:833-837`）＋ `INTEGER`・`array_of` の複製

**`value_type.rs`**
- `fn raw_type`（`:75-77`）→ **`pub(super)`**
- `enum ValueType`（`:79-89`、doc 外 1 + 内 3 行）→ **`pub(super)`**
- `enum KeyOrder`（`:91-95`、doc 1 行）→ **`pub(super)`**
- `impl ValueType { fn parse }`（`:97-118`、`parse` に doc 1 行）→ `parse` を **`pub(super)`**
- `fn type_argument`（`:120-124`、doc 2 行）→ private のまま
- `fn key_order`（`:126-139`、doc 1 行）→ private のまま
- `fn named_argument`（`:141-152`、doc 2 行）→ private のまま
- `mod tests`: 無し

**`render.rs`**
- `fn to_var_char_value`（`:58-73`、doc 2 行）→ **`pub(super)`**。`raw_type` を `super::value_type::raw_type(...)`、`number_text`/`varbinary` を `super::scalar::...` に修飾
- `fn render`（`:154-195`、doc 1 行。**本体 `:165` に実測のコメントがある**）→ private のまま。`varbinary`/`number_text` を `super::scalar::...` に修飾
- `fn compare_numbers`（`:335-347`、doc 2 行）→ private のまま（`render` から同ファイル内で呼ぶ）
- `fn join`（`:389-391`）→ private のまま
- `mod tests`: モジュール doc（`:422-424`）＋ `format`（`:428-432`）・`INTEGER`（`:434`）・`array_of`（`:436-438`）＋ テスト 22 件（#1-13・#17-21・#24-27）

**`scalar.rs`**
- `fn number_text`（`:197-205`、doc 3 行。**`:198` が実測記録**）→ **`pub(super)`**
- `fn java_double`（`:207-251`、doc 2 行）→ private のまま
- `fn varbinary`（`:349-359`、doc 1 行）→ **`pub(super)`**
- `fn decode_base64`（`:361-387`、doc 1 行）→ private のまま
- `mod tests`: 無し

**`athena_type.rs`**
- `struct AthenaType`（`:253-259`、doc 1 行。**`:253` が 2026-09-14 実測記録**）→ private のまま
- `const UNBOUNDED_VARCHAR`（`:261-262`、doc 1 行）→ private のまま
- `fn athena_type`（`:264-314`、doc 1 行）→ private のまま。`raw_type` を `super::value_type::raw_type(...)` に修飾
- `fn base_type_name`（`:316-333`、doc 2 行）→ private のまま
- `fn to_column_info`（`:404-418`）→ **`pub(super)`**
- `mod tests`: #14（`:592-685`）・#15（`:687-708`）・#16（`:710-732`）＋ `column_with`（`:577-590`）

#### 各ファイルの `//!` モジュール doc（1 行ずつ新規に書く）

#19 は分割後の 5 ファイルすべてに `//!` を新規に書いた（`src/operation/classification.rs:1`・`execution.rs:1`・`query_execution.rs:1`・`work_group.rs:1`・`mod.rs:1`）。`src/catalog.rs:1`・`metadata.rs:1`・`results.rs:1` も `//!` 始まり（`store.rs`・`trino.rs` は `use` 始まり）なので、リポジトリの慣習は「付ける」。

| ファイル | `//!` の案 |
|---|---|
| `mod.rs` | `//! Trino の結果を Athena の ResultSet に写す。`（元の `src/convert.rs:1` をそのまま引き継ぐ） |
| `result_set.rs` | `//! Outcome から Athena の ResultSet を組み立てる入口。行と列情報の両方をここから配る。` |
| `value_type.rs` | `//! Trino の typeSignature を読んで、値をどう書くかを決める型の表現。` |
| `render.rs` | `//! 値を Athena の VarCharValue の表記にする。複合型（array / map / row）は再帰で組み立てる。` |
| `scalar.rs` | `//! スカラ値の表記。double は Java の Double.toString、varbinary は 16 進。` |
| `athena_type.rs` | `//! Trino の型から Athena の型名・Precision・Scale・CaseSensitive を決める。` |

`//!` に「実測」「採取」の語を入れない（検証手順 4 の突き合わせが 8 行のままであることを保つため）。

#### `mod.rs` の全文案（#19 の `operation/mod.rs` を手本）

```rust
//! Trino の結果を Athena の ResultSet に写す。

mod athena_type;
mod render;
mod result_set;
mod scalar;
mod value_type;

pub(crate) use result_set::{all_rows, column_infos, result_set};
```

#### 各ファイルの `use`

| ファイル | `use` |
|---|---|
| `mod.rs` | 無し |
| `result_set.rs` | `use crate::athena::{ColumnInfo, Datum, ResultSet, ResultSetMetadata, Row};` / `use crate::trino::Outcome;`（元の `:7-8` をそのまま） |
| `value_type.rs` | `use serde_json::Value;` |
| `render.rs` | `use serde_json::Value;` / `use super::value_type::{KeyOrder, ValueType};`（設計判断 8 の例外） |
| `scalar.rs` | **無し**。4 関数の署名は `&serde_json::Number`(`:200`)・`f64`(`:209`)・`&str`(`:350`)・`&str`(`:362`) で、本体にも `Value` の参照が 0 件（計画レビューで実測）。書くと `unused_imports` になり、**`cargo build` は警告だけで通るので最後の `clippy -D warnings` まで気づけない** |
| `athena_type.rs` | `use crate::athena::ColumnInfo;`（`to_column_info` の戻り値型。新規）。**`mod tests` には `use serde_json::Value;` も要る**（`column_with` が `Vec<Value>` を組み立てるため。分割前は先頭の `use serde_json::Value;` が `use super::*;` 経由で届いていた。実装時に判明） |

`std::cmp::Ordering` は `compare_numbers` と一緒に `render.rs` へ。`crate::trino::Column` は元から `use` されておらず全箇所フルパスなので**揃えない**（「挙動を変えない移動と抽出だけ」の方針。`use` の新設は範囲外）。

#### 依拠する既存機構の前提表

**コンパイラが守るので表に載せない 4 つ**（計画レビューの削減の指摘で落とした）: `mod.rs` の再エクスポートで呼び出し元が壊れないこと（`src/operation/mod.rs:8-10` の実例。呼び出し元 3 ファイルは `use crate::convert;` のモジュール単位 import）／`pub(super)` 8 件の漏れ（`src/operation/classification.rs:4,13,26` の実例）／`cargo test --lib convert` のフィルタが子孫に届くこと（Plan が `--lib operation::` で 6 passed を実測）／`clippy::module_inception`（6 ファイルとも `convert` と一致しない）。どれも漏れれば `cargo build` か `cargo clippy` が落ちるので、事前の表に書いても「既に固定されている事実の二重化」になる。

**表に残すのは、コンパイラが守らない 2 つだけ。**

| 機構 | 暗黙の前提 | 誰が保証しているか（file:line） | 自分の変更が破らないか |
|---|---|---|---|
| `missing_docs` lint が無効なので doc コメントの消失は fmt/clippy/test が検知しない | doc の消失は機械的に検査しないと気づけない | `Cargo.toml` に `[lints]` 無し・`src/lib.rs` に `#![...]` 無し（Explore B-7 で実測） | **未保証**。だから検証手順 4 に「実測記録の全件突き合わせ」を置く。とくに `:198`（`number_text` の doc）と `:253`（`AthenaType` の doc）は宣言直前の `///` で、行番号で単位を切ると静かに消える |
| `result_set`/`column_infos`/`to_row` は `convert` 内に直接テストが無い | 分割後にこの 3 つを守るのは `tests/` の統合テスト（HTTP 経由） | **計画レビューで裏取り済み**: `tests/metadata.rs` は列 `id integer`／`name varchar` の `ColumnInfo` を protobuf の 16 進バイト列として厳密比較しているので、`column_infos` を固定ダミーにすり替えると確実に落ちる | 破らない。ミューテーション確認 #5 で実測する |

#### 直列化・エスケープ境界／表現の正規化境界／境界の名前／意味論マトリクス／一致台帳

- **直列化・エスケープ境界**: 該当なし。クライアント入力を文字列として組み立てる箇所を新たに作らない（既存の `render`/`varbinary` の組み立ては 1 行も変えずに移すだけ）。
- **表現の正規化境界**: 該当なし。「A の値と B の値を突き合わせる」新しい判定を増やさない（既存の `compare_numbers` は移動のみ）。
- **境界の名前の確認**: 該当なし（テナント・環境・realm 等を扱わない）。
- **意味論マトリクス**: 該当なし（Trino → Athena の変換は 1 実装のまま。ファイルの置き場所だけを変える）。
- **一致台帳**: `INTEGER`・`array_of` が `render.rs` と `result_set.rs` の 2 箇所に同じ値で存在する（設計判断 5）。**守り方: 移動フェーズでどちらの値も変更しない。** 将来値を変える必要が出たら、そのとき共通化を検討する（今回は 4 行の複製で足りる）。

#### 追加・変更するテスト

**新規テストは書かない。** 27 件をすべて「対象の関数が移った先のファイル」に追従させるだけ。固定する内容は「分割前後でテスト本文とアサーションが 1 文字も変わらないこと」で、これは行ソート diff（下記）が検査する。

配分: `result_set.rs` 2 件（#22・#23）／`render.rs` 22 件（#1-13・#17-21・#24-27）／`athena_type.rs` 3 件（#14-16）／`value_type.rs` 0 件／`scalar.rs` 0 件 = **27 件**。

`value_type.rs`（本体 84 行）と `scalar.rs`（本体 96 行）がテスト 0 件になるのは、#19 の `execution.rs`(362 行)・`work_group.rs`(127 行) と同じ状況（どちらも `mod tests` を持たない）。この 2 ファイルの関数は `render.rs` のテストが `format` 経由で守っているので、ミューテーション確認をクロスファイルで行う（下記）。

#### テストが通るための配線

red フェーズは無い（新しい振る舞いを足さない）。green を成立させる配線は 3 つで、**どれも欠けると `cargo build` が落ちる**（doc の消失のように静かに通ることはない）:

1. `mod.rs` の 5 つの `mod` 宣言と `pub(crate) use result_set::{all_rows, column_infos, result_set};`
2. `pub(super)` 8 件（設計判断 7）
3. `render.rs` の `use super::value_type::{KeyOrder, ValueType};`

#### 検証手順（フェーズ 1 のコミット前に実行。そのまま走らせる形）

```sh
cd /home/aoyagikouhei/git/athena-local

# 1. 標準コマンド（分割前: 221 passed / --lib 124 / --lib convert 27 / fmt ok / clippy 警告 0）
cargo test --locked 2>&1 | grep -oE '^test result: ok\. [0-9]+ passed' | grep -oE '[0-9]+' | paste -sd+ | bc
cargo test --locked --lib convert 2>&1 | grep 'test result'
cargo fmt --check
cargo clippy --all-targets --locked -- -D warnings

# 2. 各ファイルの本体行数と全体行数（全体 400 行以内が完了条件）
for f in src/convert/*.rs; do
  awk -v f="$f" '/#\[cfg\(test\)\]/{print f": 本体 "NR-1" 行 / 全体 "; found=1; exit}
                 END{if(!found) print f": 本体 "NR" 行（テスト無し） / 全体 "}' "$f"
  wc -l < "$f"
done

# 3. 分岐の計数（分割前: if 17 / match 12 / => 55 / return 6 / fn 20。**マッチ数で数える**）
body() { for f in src/convert/*.rs; do awk '/#\[cfg\(test\)\]/{exit} {print}' "$f"; done; }
for p in '\bif\b' '\bmatch\b' '=>' '\breturn\b' '\bfn\b'; do
  printf '%-12s %s\n' "$p" "$(body | grep -oE "$p" | wc -l)"
done

# 4. 実測記録の全件突き合わせ（missing_docs lint が無いので doc の消失はこれでしか気づけない）
git show c51cfce:src/convert.rs | grep -nE '実測|採取' | sed 's/^[0-9]*://' | sed -E 's/^[[:space:]]+//' | sort > /tmp/measured-before.txt
cat src/convert/*.rs           | grep -nE '実測|採取' | sed 's/^[0-9]*://' | sed -E 's/^[[:space:]]+//' | sort > /tmp/measured-after.txt
diff /tmp/measured-before.txt /tmp/measured-after.txt && echo "実測記録 8 行すべて一致"

# 5. 行ソート diff（use・mod・空行・モジュール doc を除きインデントを潰して sort。人間が読む）
git show c51cfce:src/convert.rs | grep -Ev '^\s*(use |mod |pub\(crate\) use |//!)' | grep -v '^[[:space:]]*$' \
  | sed -E 's/^[[:space:]]+//' | sort > /tmp/convert-before.txt
cat src/convert/*.rs            | grep -Ev '^\s*(use |mod |pub\(crate\) use |//!)' | grep -v '^[[:space:]]*$' \
  | sed -E 's/^[[:space:]]+//' | sort > /tmp/convert-after.txt
diff /tmp/convert-before.txt /tmp/convert-after.txt

# 6. テスト件数のファイルごとの内訳（result_set 2 / render 22 / athena_type 3 = 27）
cargo test --locked --lib convert 2>&1 | grep -oE 'convert::[a-z_]+::tests' | sort | uniq -c
```

**手順 5 の diff に出てよい差分**（これ以外が出たら要調査）:

1. `super::render::` / `super::scalar::` / `super::value_type::` / `super::athena_type::` の修飾が付いた行
2. `pub` / `pub(super)` が増えた宣言行（設計判断 7 の 8 件 + `column_infos` の `pub(crate)` → `pub`）
3. `#[cfg(test)]`・`mod tests {`・`use super::*;`・裸の閉じ `}` が 1 組 → 3 組に増える（`mod tests` が 3 ファイルに分かれる）
4. `INTEGER`・`array_of` の定義が 2 回ずつ出る（一致台帳）
5. `fmt` の折り返しの違い

**手順 4 の「実測記録」9 行**: 本体 4 行（`:43`・`:165`・`:198`・`:253`）＋ テスト 5 行（`:422-424` の `//!` のうち「採取」を含む 1 行・`:515`・`:538`・`:787`・**テスト関数名 `列情報の型は_athena_の実測と同じになる`(`:593`)**）。`//!` を手順 5 で除外しているので、この手順が唯一の検査になる。（計画時に 8 行と書いていたのはテスト関数名の 1 行の数え漏れ。実装時に 9 行と判明し、分割前後で diff は空。）

#### ミューテーション確認で壊す候補（クロスファイルの配線を狙う）

新規テストを書かないので、「既存テストが移動後も本体を守っているか」と「越境の配線が正しいか」を確かめる形にする。

| # | 壊す箇所 | 壊し方 | 落ちるべきテスト | 確かめる配線 |
|---|---|---|---|---|
| 1 | `scalar.rs` の `varbinary` | 常に固定文字列を返す | #9・#10（`render.rs` の `mod tests`） | `render.rs` → `scalar.rs`（`to_var_char_value` と `render` の 2 つの呼び口） |
| 2 | `scalar.rs` の `java_double` | 指数表記の分岐を潰す | #11・#13（`render.rs`） | `render.rs` → `scalar.rs` の `number_text` 経由 |
| 3 | `value_type.rs` の `ValueType::parse` の `"array"` の腕 | `None` を返す | #1〜#4（`render.rs`） | `render.rs` → `value_type.rs` |
| 4 | `athena_type.rs` の `to_column_info` の `precision` | 常に 0 | #14（`athena_type.rs` 自身） | ファイル内の配線と `pub(super)` の健全性 |
| 5 | `result_set.rs` の `column_infos` の `super::athena_type::to_column_info` 呼び出し | 固定のダミー `ColumnInfo` を返す | `tests/metadata.rs`（`convert` 内に直接テストが無い唯一の辺） | `result_set.rs` → `athena_type.rs`。**前提表の「未保証」行を実測する** |

いずれも壊して単独フィルタで red を見てから、スクラッチパッドのコピーで戻し、`git status` で差分が分割分だけであることを確認する。

#### 実装の手順

`git mv src/convert.rs src/convert/render.rs` から始め、元ファイルを削りながら他の 5 ファイルへ切り出す（**内容の取りこぼしを防ぐため**。リネーム検出は期待しない — #19 でも 41% 類似度で `--find-renames=30%` の明示が要り、今回 `render.rs` は 869 行のうち約 332 行しか残らないので既定のしきい値では検出されない。検証手順 1〜6 はどれも `git` のリネーム検出を参照していない）。

抽出は依存の少ない順（`value_type.rs` → `scalar.rs` → `athena_type.rs` → `result_set.rs`、残りが `render.rs`）。各抽出のあと `cargo build` で可視性と import の漏れを捕まえるが、**`unused_imports` は警告でビルドが通るので、`cargo clippy --all-targets --locked -- -D warnings` も各段で走らせる**（計画レビューの指摘 2）。

### フェーズ 2: `CLAUDE.md:43` を新構成に合わせる

`CLAUDE.md:29` の `operation/` の書き方（ディレクトリ名 → 中のファイルを列挙 → 横断的要素の役割 → `mod.rs` は再エクスポートだけで外からの見え方は不変）を手本にする。`:43` はもとから複数の変換規則を並べる長文行なので、1 ファイル 1 行の体裁は `:45-49` の他の行が保つ。

書き換え案:

> - `convert/` — Trino の値と型から Athena の `ResultSet` を作る。`result_set.rs`（`Outcome` → `ResultSet` の入口。`all_rows`／`result_set`／`column_infos`）、`value_type.rs`（`typeSignature` の解析）、`render.rs`（値の入口と複合型の再帰的なレンダリング）、`scalar.rs`（スカラ値の表記。double は Java の `Double.toString`、varbinary は 16 進）、`athena_type.rs`（Trino の型から Athena の型へ。`ColumnInfo` の Precision／Scale を埋める）に分かれる。1 ページ目の先頭行に列名を入れる、値はすべて文字列にする、複合型は `typeSignature` を見て Athena の表記（`[1, 2]`、`{k=1}`）にする、など。`mod.rs` は `mod` 宣言と 3 関数の再エクスポートだけで、呼び出し元からの見え方は分割前と変わらない。`GetQueryResults` と結果 CSV（`results::to_csv`）の両方がここを通る。

`:44` の行は `convert` に触れていないので変更不要。README・CHANGELOGS は変えない（挙動が変わらないため。issue の完了条件）。

### 人間検証リスト

**0 件。** 挙動を一切変えない機械的なファイル分割で、外部環境への依存も増えない（`CLAUDE.md` の「`cargo test` は Trino も AWS も要らない」がそのまま維持される）。`cargo test --locked`／`cargo fmt --check`／`cargo clippy` と上記の検証手順 2〜6 で完全に検証できる。

### 残っている前提（未検証）

1. `render.rs` の見込み ~327 行は空行と `mod tests` の枠の推定を含む。実装時に `wc -l` で確定させる。400 行を超えたら `compare_numbers`（13 行）と `join`（3 行）を `scalar.rs` に移して調整する（テストの #8 も一緒に動く）。
2. `pub(super)` 8 件の追加で `clippy -D warnings` が新しい警告を出さないか（`unreachable_pub` 等は無効なので可能性は低いが、#19 と同一の保証はない）。検証手順 1 で確認する。
3. `tests/metadata.rs`・`tests/results.rs`・`tests/select.rs` が `result_set`/`column_infos`/`to_row` を意味のある形で検証しているか。ミューテーション確認の #5 で実測する。
4. `scalar.rs` に `use serde_json::Value;` が要るか（`number_text` の引数は `&serde_json::Number`）。実装時にコンパイラが決める。

## 計画レビューの結果

Explore 2 本（(a) 契約・呼び出し元／再利用・一貫性 (b) 構造／削減、どちらも sonnet）。**high 0 件**、medium 2 件、low 4 件（重複を寄せて 6 件）。**採用 5 件・棄却 1 件**。

| # | 重大度 | 指摘 | 採否 |
|---|---|---|---|
| 1 | medium | テスト件数のラベルが誤り。`#1-13`(13)+`#17-21`(5)+`#24-27`(4) は **22 件**で 21 件ではなく、検証の期待値の合計が `2+21+3=26 ≠ 27` で自己矛盾していた（行数 209 は正しい） | **採用**。4 箇所を直した。期待値が誤っていると、実装者が正しい結果（render 22 件）を「1 件多い」と誤認してテストを動かす・削る方向に修正しうる |
| 2 | medium | `scalar.rs` の `use serde_json::Value;` は不要。移す 4 関数の署名は `&serde_json::Number`(`:200`)・`f64`(`:209`)・`&str`(`:350`)・`&str`(`:362`) で本体にも `Value` が 0 件。**`cargo build` は `unused_imports` を警告としてしか出さないので、抽出ごとの `cargo build` では気づけず最後の `clippy -D warnings` まで通る**（2 本が独立に指摘） | **採用**。`use` 表を「無し」に直し、実装手順に「各段で `clippy` も走らせる」を足した |
| 3 | low | #19 は分割後の 5 ファイルすべてに `//!` を新規に書いている（`src/operation/*.rs:1`）。計画は `mod.rs` にしか `//!` を置いていなかった | **採用**。6 ファイルの `//!` の文案を計画に足した。`//!` に「実測」「採取」の語を入れない（検証手順 4 の 8 行を保つため） |
| 4 | low（削減） | 前提表 6 行のうち 4 行（`mod.rs` の再エクスポート・`pub(super)` 8 件・`--lib` フィルタ・`module_inception`）は表自身が「漏れたら `cargo build` が落ちる」と書いており、既に固定されている事実の二重化 | **採用**。表を 2 行（`missing_docs` が無い／`result_set` 系の直接テストが無い）に絞り、4 つは「コンパイラが守るので載せない」の 1 段落にまとめた |
| 5 | low（削減） | `git mv src/convert.rs src/convert/render.rs` で「リネーム検出を効かせる」目的は達成の保証がなく、検証手順 1〜6 はどれもリネーム検出を参照していない | **採用**。目的を「内容の取りこぼしを防ぐため。リネーム検出は期待しない」に書き換えた（`render.rs` は 869 行のうち約 332 行しか残らず既定のしきい値では検出されない） |
| 6 | low（削減） | `INTEGER`/`array_of` の複製 4 行と一致台帳・diff 例外項目は、テスト #22 を `render.rs` に残して `super::result_set::all_rows(&outcome)` の越境 1 本に変えれば丸ごと不要になる | **棄却**（dismissed に理由） |

### レビューが裏取りして「問題なし」とした点（再検討しない）

- **呼び出し元 5 箇所と `src/lib.rs:7` は 0 行変更で足りる**。5 箇所すべて `use crate::convert;`（モジュール単位 import）＋ `convert::関数名(...)` の形で、`mod.rs` が 3 関数を再エクスポートすれば変更不要（grep で裏取り済み）
- **`pub(super)` 8 件は過不足なし**（`src/convert.rs` の全呼び出し箇所と突き合わせ済み）
- `mod result_set;` と `pub(crate) use result_set::{..., result_set, ...};` の**名前衝突は起きない**（Rust の型名前空間と値名前空間の分離）
- **テストの識別子可視性はすべて同一ファイル内で完結**: #8 → `compare_numbers`（`render.rs`）、#16 → `base_type_name`（`athena_type.rs`）、#22 → `INTEGER`/`array_of` の複製（`result_set.rs`）、#24 → `to_var_char_value`（`render.rs`）
- **行数の独立検算**: `render.rs` 約 332 行・`athena_type.rs` 約 261 行・`result_set.rs` 約 114 行。**400 行を超えるファイルは無い**
- **ミューテーション確認 5 件すべて「壊すと落ちる」**ことをテスト本文で確認。とくに #5 は `tests/metadata.rs` が `ColumnInfo` を protobuf の 16 進バイト列で厳密比較しているので、`column_infos` を固定ダミーにすると確実に落ちる
- **design-checklist 5 項目**: 1〜3（早期 return への新規出力・前処理の切り出し・2 語目以降の分岐）は新しい振る舞いを足さないため該当なし。4（宣言＋直前の doc ブロックを 1 単位で運ぶ）は移す単位の表が実測と一致し違反なし。5（テスト件数の突き合わせ）は検証手順 6 に組み込み済み
- `value_type.rs`・`scalar.rs` がテスト 0 件になるのは #19 の `execution.rs`(362 行)・`work_group.rs`(127 行) と同じだが、**保護の性質は #19 より強い**（#19 は `tests/` の統合テストだけ、今回は `render.rs` の `format()` 経由のユニットテストが `ValueType::parse`・`number_text`・`varbinary` を実際に通る）

## フェーズの記録

### フェーズ 1: `src/convert.rs` を `src/convert/` の 6 ファイルに割る

**7-0 の答え**

- **消す・変えるガードや条件**: **無し**。分岐・エラー経路・早期 return を 1 行も変えない。変えるのは (a) 宣言の置き場所 (b) 可視性 8 件 (c) 越境呼び出しの `super::` 修飾 (d) 各ファイルの `use` と `//!` だけ。
- **変える関数・契約の呼び出し元**: `src/results.rs:180,199`・`src/operation/execution.rs:307`・`src/operation/query_execution.rs:47,60` の 5 箇所と `src/lib.rs:7`。**すべて 0 行変更**（5 箇所とも `use crate::convert;` のモジュール単位 import ＋ `convert::関数名(...)`。`mod.rs` の `pub(crate) use` が吸収する。計画レビューが grep で裏取り済み）。
- **このフェーズで増える分岐の全列挙**: **0 件**。新しい `match` の腕・`if`・早期 `return`・エラー経路・出力の有無を分ける条件をどれも足さない。分割前の分岐計数（`if` 17 / `match` 12 / `=>` 55 / `return` 6）が分割後も同数であることを検証手順 3 で機械的に確かめる（#19 が確立した手法）。
- **通る境界入力**: 該当なし。新しい入力経路を作らないため、既存の 27 件のテストがそのまま境界入力（`"`・非 ASCII・空配列・入れ子）を持ち込む。
- **design-checklist の該当項目**（7 項目のうち 4 項目）: 項目 4（宣言＋直前の `///` ブロックと属性を 1 単位で運ぶ）→ 移す単位の表で対応。項目 5（移動前後のテスト件数の突き合わせ）→ 検証手順 6。#30 で足した 2 項目（期待値を内訳から数え直す／`use` を配る先で実際に使うか grep する）→ 計画レビューで既に反映済み。項目 1〜3 は新しい振る舞い・前処理を足さないため該当なし。

**結果**（実装は general-purpose 1 本に委譲。統括役が 7-5b で差分を確認して受け入れた）

| ファイル | 本体 | 全体 | 計画の見込み |
|---|---|---|---|
| `mod.rs` | 9 | **9** | ~10 |
| `result_set.rs` | 68 | **122** | ~114 |
| `value_type.rs` | 82 | **82** | ~84 |
| `render.rs` | 88 | **339**（最大） | ~332 |
| `scalar.rs` | 97 | **97** | ~96 |
| `athena_type.rs` | 102 | **265** | ~261 |
| 合計 | — | **914**（旧 869 から +45。#19 は 863→904 で +41） | ~889 |

**全ファイル 400 行以内**（最大の `render.rs` で 61 行の余裕）。

| 検証 | 分割前 | 分割後 |
|---|---|---|
| `cargo test --locked` | 221 passed | **221 passed** |
| `cargo test --locked --lib convert` | 27 passed | **27 passed**（内訳 `result_set` 2 / `render` 22 / `athena_type` 3） |
| `cargo fmt --check` | ok | **ok** |
| `cargo clippy --all-targets --locked -- -D warnings` | 警告 0 | **警告 0** |
| 本体の分岐（`grep -oE` のマッチ数） | `if` 17 / `match` 12 / `=>` 55 / `return` 6 / `fn` 20 | **完全一致** |
| 「実測」「採取」を含む行 | 9 行 | **diff 空（9 行すべて一致）** |
| 行ソート diff | — | **差分はすべて「出てよい 5 種類」に収まる**（`super::` の修飾／`pub`・`pub(super)` の 9 件／`#[cfg(test)]` が 1→3 組／`INTEGER`・`array_of` の複製／fmt の折り返し 2 箇所） |

**ミューテーション確認 5 件すべて red → 戻して diff 空**

| # | 壊し方 | 落ちたテスト |
|---|---|---|
| 1 | `scalar.rs::varbinary` を固定文字列に | #9・#10（`render.rs`）＝ 計画どおり |
| 2 | `scalar.rs::java_double` の指数表記の分岐を `if true` に | #11・#13（`render.rs`）＝ 計画どおり |
| 3 | `value_type.rs::ValueType::parse` の `"array"` の腕を `None` に | **8 件**（計画は 4 件。`array_of()` を使う varbinary 配列・decimal 配列・row の配列・map の値が配列のケースも同じ経路を通る） |
| 4 | `athena_type.rs::to_column_info` の `precision` を常に 0 に | **2 件**（計画は 1 件。#16 も precision を検証している。#15 は precision 0 が期待値なので通過） |
| 5 | `result_set.rs::column_infos` を固定ダミーに | `tests/metadata.rs` が **8/10 failed**（`convert` 内に直接テストが無い唯一の辺を実測で裏取り） |

**計画から外れた点 3 件**（いずれも判断の変更ではなく計画の補足。上の計画本文に反映済み）

1. `athena_type.rs` の `mod tests` に `use serde_json::Value;` を追加した。`column_with` が `Vec<Value>` を組み立てるため。分割前は先頭の `use serde_json::Value;` が `use super::*;` 経由で届いていた。**テスト本文を 1 文字も変えないための最小の追加**（代替は `Vec<serde_json::Value>` への書き換えで、そちらはテスト本文を変えてしまう）。
2. ミューテーション 3・4 で計画より多くのテストが落ちた（4→8 件、1→2 件）。「壊すと落ちる」という結論は計画と同じ。
3. 「実測」「採取」を含む行が計画の 8 行ではなく 9 行だった（テスト関数名 `列情報の型は_athena_の実測と同じになる` の数え漏れ）。分割前後で diff は空。

**7-5b の自己レビュー**

- **分岐の突き合わせ**: 7-0 の「増える分岐 0 件」と一致。分岐計数が完全一致で、`to_var_char_value` の match も腕の数・順序・条件が分割前と同一（`=> { ... }` へのブロック化は fmt の折り返しのみ）。`column_infos` が 1 行 → 5 行に展開されたのも `super::athena_type::to_column_info` で行が長くなった fmt の結果で、ロジックは同一。**テスト名の無い分岐は 0 件**
- **エラーの出口**: 新しいエラー戻り値・例外は 0 件
- **初めて使う引数・パターン**: `pub(super)` は #19（`src/operation/classification.rs:4,13,26`）に先行例あり
- **昇格した既存コード**: 該当なし（呼び出し元は 1 行も変わらず、新しい入力経路は増えていない）
- **docs**: `convert.rs` をパスと basename で grep した結果、残骸は **`CLAUDE.md:43` の 1 箇所だけ**（フェーズ 2 で直す）。`.claude/issue-notes/*.md` の 8 件は日付付きの過去ログなので対象外
- **計画との差**: 上の 3 件。前提表の 2 行（`missing_docs` が無い／`result_set` 系の直接テストが無い）はどちらも検証で裏取り済み

**7-6**: 省略（フェーズ 2 以下の小規模 issue の経路。SKILL.md Step 7）。

### フェーズ 2: `CLAUDE.md:43` を新構成に合わせる

**7-0 の答え**: 消すガード無し／呼び出し元は無し（文書のみ）／増える分岐 0 件／境界入力なし／design-checklist は自己レビューの docs 項目（削除・移動したファイルをパスと basename で grep）が該当。

**結果**: 統括役が自分で書いた（1 行の書き換えで、指示を書く方が長い）。`CLAUDE.md:29` の `operation/` の書き方（ディレクトリ名 → 中のファイルを列挙 → 横断的要素の役割 → `mod.rs` は再エクスポートだけで外からの見え方は不変）を手本にし、元の行の情報（列名行・値を文字列に・複合型の表記・double の Java 表記・varbinary の 16 進・Precision／Scale・`GetQueryResults` と結果 CSV の両方が通る）を 1 つも落とさずに 5 ファイルへ振り分けた。`raw_type` を `value_type.rs` に置いて両方から呼ぶことも 1 文で書いた（#19 が `classification.rs` を「実行系と参照系の両方から呼ぶ」と書いたのと同じ形）。

記述と実態の照合: `src/convert/` は 6 ファイル、`mod.rs` は 9 行で再エクスポートは 3 関数 — 記述どおり。**`convert.rs` をパスと basename で再 grep して残骸 0 件**（`.claude/issue-notes/*.md` の過去ログは対象外）。

**7-7**: 分割不要。`src/convert/` の最大は `render.rs` の 339 行で完了条件（全体 400 行以内）を満たす。目安の「新規ファイル 200 行以内」は `render.rs`(339)・`athena_type.rs`(265) が超えるが、#19 の `execution.rs`(362 行) が許容された 3 点（`19.md:203`）と同じ理由で更に割らない: (1) 新規コードを 1 行も書いておらず既存コードの移動先である (2) 各ファイルの責務は 1 つ (3) `TODO.md` に `convert` 関連の追加項目が無く成長の予定が無い。更に割ると Step 4 でユーザーが選んだ 5 ファイル構成を覆すことにもなる。

## レビュー実施記録

（回数と、最終パス・独立レビューが返した「本番影響のある妥当指摘」の件数）

## dismissed

| # | 指摘 | 出典 | 棄却の理由 |
|---|---|---|---|
| 1 | テスト #22 を `render.rs` に残せば `INTEGER`/`array_of` の複製 4 行・一致台帳・行ソート diff の例外項目 4 が丸ごと不要になる（計画が小さくなる） | Step 6 の計画レビュー(b) 指摘 3（low・削減） | #19 が確立した「**テスト対象の関数が移った先のファイルに `mod tests` を追従させる**」（`src/operation/classification.rs:75`・`query_execution.rs:181`）を守る方が、4 行の複製を避けることより価値が高い。#22 が直接呼ぶのは `all_rows`(`:825`) だけで、原則がそのまま一意に決める。`render.rs` に置くと (1) `result_set.rs` のテストが #23（5 行）だけになり `all_rows` の中心的な振る舞い（列ごとに型を対応付ける）のテストが本体と別ファイルに行く (2) `render.rs` が 332 → 367 行になり 400 行への余裕が 68 → 33 行に減る。テストヘルパの複製は `#[cfg(test)]` では一般的で、一致の守り方（移動フェーズでどちらの値も変えない）を台帳に書けば足りる |

## deferred

| 指摘 | severity | file | 再トリアージ | 起票 |
|---|---|---|---|---|

## 覆した事前判断

（元の判断 / 新しい判断 / 理由 / 外部契約・プロダクト挙動・運用への影響 / ユーザーに聞いたか）

## 実機検証

（Step 9 のケース表と結果）

## 範囲外の発見

| 内容 | 出どころ | 起票した issue |
|---|---|---|

## 出荷

（PR 番号・issue コメント・起票した issue・出荷判定）
