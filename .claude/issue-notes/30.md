# quick #30 src/convert.rs を分割する

- 入力: issue #30。モード: standalone。ブランチ `main-30`、着手前 SHA `15accc2`
- ゲート: サイズ **419 行・本番ファイル 1→4〜5（≤50・3）→ 外れた**／呼び出し元 5 箇所（≤5 だが `src/operation/execution.rs`・`src/operation/query_execution.rs`・`src/results.rs` にまたがり同一モジュールに収まらない）→ 境界／契約 通過（挙動不変・`pub use` 再エクスポートでシグネチャ不変）／**新規性 外れた（`src/convert/` に新ファイル。先行例は #19 の `src/operation/` 1 件）**／事実 通過（挙動不変なので既存テスト green で確認できる）／判断 通過（issue と `.claude/design-checklist.md` が方針を固定済み）
- 時系列: 着手 16:08 ／ red — ／ green — ／ コミット 16:15
- 検証: 実装に入っていないため標準コマンド・ミューテーションとも未実施。足した層なし（引き継ぎ用に依存グラフだけ実測した）
- 自己レビュー: 実装差分なし（このノートのみ）
- 打ち切り・escalate: 提案あり（変更サイズ 419 行・本番ファイル 4〜5／新規性 新ファイル作成）→ 選択「**implement-issue で進める**」。stash なし（コードには触っていない）。issue コメント: https://github.com/aoyagikouhei/athena-local/issues/30#issuecomment-5748340478
- 予算の実績: Bash 約 9 本、所要 7 分（16:08→16:15）、コミット 1、サブエージェント 0 本
- 出荷: ブランチ `main-30`（PR は作らず、implement-issue に引き継ぐ）

## 引き継ぎ

### 判定箇所（file:line）

`src/convert.rs` は 869 行、`#[cfg(test)]` は 420 行目。本体 419 行・テスト 449 行（テストが 52%）、`#[test]` は **27 件**。本体の宣言 24 個を issue 本文の 3 つの塊に割り当てたものが次のとおり（2026-09-20 実測）。

1. **`Outcome` → `ResultSet` 変換の入口**（外から呼ばれる 3 つと、その直下のヘルパ 1 つ）
   - `src/convert.rs:12` `pub fn all_rows`、`:41` `pub fn result_set`、`:54` `pub(crate) fn column_infos`、`:393` `fn to_row`
2. **値のレンダリング**
   - `src/convert.rs:60` `fn to_var_char_value`、`:80` `enum ValueType`、`:92` `enum KeyOrder`、`:97` `impl ValueType`（`:99` `fn parse`）、`:122` `fn type_argument`、`:127` `fn key_order`、`:143` `fn named_argument`、`:155` `fn render`、`:200` `fn number_text`、`:209` `fn java_double`、`:337` `fn compare_numbers`、`:350` `fn varbinary`、`:362` `fn decode_base64`、`:389` `fn join`
3. **Trino 型 → Athena 型のマッピング**
   - `src/convert.rs:254` `struct AthenaType`、`:262` `const UNBOUNDED_VARCHAR`、`:265` `fn athena_type`、`:318` `fn base_type_name`、`:404` `fn to_column_info`
4. **塊 2・塊 3 が共有するヘルパ（1 つだけ）**
   - `src/convert.rs:75` `fn raw_type(signature: Option<&Value>) -> Option<&str>`（4 行）

塊の行番号は入り混じっている（塊 2 の `compare_numbers`〜`join` が塊 3 の `athena_type` の後ろ、塊 1 の `to_row` が塊 2 の後ろ、塊 3 の `to_column_info` が最後）ので、**行番号の範囲では切れない**。`.claude/design-checklist.md` の #19 由来の 1 行（関数・定数とその直前の `///` ブロックと属性を 1 単位として運ぶ）がそのまま効く。

### 依存グラフ（2026-09-20 実測。全 24 宣言の参照元を本体 1-419 で grep した）

塊をまたぐ辺は **4 本だけ**で、向きは一方向。**塊 2 と塊 3 の間に直接の辺は無い**。

| 辺 | 呼び出し位置 |
|---|---|
| 塊 1 → 塊 2 | `all_rows` が `to_var_char_value` を呼ぶ（`:33`） |
| 塊 1 → 塊 3 | `column_infos` が `to_column_info` を呼ぶ（`:55`） |
| 塊 2 → 共有 | `to_var_char_value` が `raw_type` を呼ぶ（`:63`） |
| 塊 3 → 共有 | `athena_type` が `raw_type` を呼ぶ（`:267`） |

塊の内部で閉じている辺（移動しても可視性を変えなくてよい）:

- 塊 1: `result_set` → `to_row`(`:45`) / `column_infos`(`:47`)
- 塊 2: `to_var_char_value` → `number_text`(`:66`) / `varbinary`(`:63`) / `ValueType::parse`(`:67`) / `render`(`:68`)、`ValueType::parse` → `type_argument`(`:104,:107`) / `key_order`(`:106`) / `named_argument`(`:112`)、`render` → `varbinary`(`:158`) / `number_text`(`:160`) / `join`(`:162,:173,:182`) / `compare_numbers`(`:169`) / 再帰(`:68,:162,:176,:187,:188`)、`number_text` → `java_double`(`:202`)、`varbinary` → `decode_base64`(`:351`)
- 塊 3: `athena_type` → `base_type_name`(`:269`) / `UNBOUNDED_VARCHAR`(`:295`) / `AthenaType`(`:308`)、`to_column_info` → `athena_type`(`:405`)

つまり **3 ファイル + `mod.rs` にきれいに割れる**（`raw_type` を `mod.rs` に置いて `pub(super)` にするか、塊 3 に置いて塊 2 が `use super::` で借りるかの二択。後者を選ぶと塊 2 → 塊 3 の辺が 1 本増える）。

### 共有できる既存機構

- **#19 で入れたディレクトリ形式モジュールの形**が `src/operation/mod.rs`（10 行）にある。`mod` 宣言 + `pub(crate) use` の再エクスポートだけで、`handler` からの見え方を分割前と変えていない。`src/convert/mod.rs` を同じ形にすれば `src/lib.rs:7` の `mod convert;` と 5 箇所の呼び出し元を 1 行も変えずに済む。
- `src/operation/` の分割後の行数は 164 / 362 / 10 / 241 / 127。400 行の上限に対する実績値の目安になる。

### 呼び出し元（5 箇所、すべて `convert::` 経由）

- `src/operation/execution.rs:307` — `&convert::column_infos(outcome)`
- `src/operation/query_execution.rs:47` — `convert::all_rows(&outcome)`
- `src/operation/query_execution.rs:60` — `convert::result_set(&outcome, &rows[offset..end])`
- `src/results.rs:180` — `convert::all_rows(outcome)`
- `src/results.rs:199` — `convert::all_rows(outcome)`

`pub` / `pub(crate)` で外に出ているのは `all_rows` / `result_set` / `column_infos` の 3 つだけ。残り 21 個はモジュール内の私的関数なので、分割すると**塊をまたぐ 4 本の辺の先で可視性の引き上げ（`pub(super)` か `pub(crate)`）が必要**になる。どこまで上げるかが設計論点（`clippy -D warnings` は広すぎる可視性を叱らないので、上げすぎても静かに通る）。

### 本体の行数の見込み（400 行の上限に対して）

分割前の本体 419 行の内訳（宣言の位置から概算）: 塊 1 ≒ 58 行、塊 2 ≒ 250 行、塊 3 ≒ 99 行、共有 4 行 + `use` 数行。
**塊 2 が最大で 250 行**なので、テスト 449 行の配分次第で塊 2 のファイルが 400 行を超えうる。テストの割り方が完了条件に直結する。

### 未実測・未確認の一覧

- **テスト 449 行（27 件）をどう割るか**。本体だけ割ってテストを `src/convert/mod.rs` に残す案と、塊ごとに `#[cfg(test)] mod tests` を分ける案がある。後者なら `.claude/design-checklist.md` の #19 由来の 1 行（移動前後のテスト件数の突き合わせ）が必須。分割後に `convert` のテストが 27 件のままであることを確認する手順を計画に入れる。どのテストがどの塊を検証しているかは未調査。
- **リポジトリ全体のテスト件数**。issue は「#19 時点で 218」と書くが、その後 #26 / #27 が入っているため今の総数は未実測。`cargo test --locked` の出力で分割前の数を取ってから始める。
- **`clippy -D warnings` が分割で新しく出す警告**。`module_inception`（`src/convert/convert.rs` という名前を避ければ回避できる）や、可視性を上げたときの警告は未確認。
- **`crate::trino::Column` をフルパスで書いている 2 箇所**（`:265` `athena_type`、`:404` `to_column_info`。どちらも塊 3）。分割後に `use crate::trino::Column;` に揃えるかどうかは、「挙動を変えない移動と抽出だけ」の範囲に入るかの判断が要る（#19 の方針に照らすと、整形は別コミットか見送り）。

### 再現手順

挙動の欠陥ではないので再現テストは無い。完了条件の確認は次の 4 つ。

```sh
cargo test --locked          # 分割前の件数を記録し、分割後に同じであること
cargo fmt --check
cargo clippy --all-targets --locked -- -D warnings
awk '/#\[cfg\(test\)\]/{print FILENAME": 本体 "NR-1" 行"; nextfile} END{}' src/convert/*.rs   # 本体が 400 行以内
```

### stash 名

なし（コードには触っていない。`git status` は clean）。
