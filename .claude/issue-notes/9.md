# issue #9 ListWorkGroups に対応する

## 基本情報

- issue: #9
- ベースブランチ: main
- 作業ブランチ: main-9
- 着手前 SHA: 3bc70c1
- 着手日: 2026-09-18

## 設計判断

- 2026-09-18 この環境から本物の Athena は叩けない（`aws sts get-caller-identity` が `NoCredentials`、`AWS_` 環境変数 0 個）。issue 本文の「実装の前に本物の Athena で測ること」は #2 と同じく測定スクリプトをユーザーに実行してもらう。
- 2026-09-18 **判断 0（実測）: 実測してから実装する。** `MaxResults` の上限と超過時のエラー、不正な `NextToken` のエラー、`NextToken` の形、一覧の順序は測るまで埋めない（CLAUDE.md の約束）。`Description` の食い違いは issue 2 の生ログで決着済み（下の「事前に確認した事実」）。
- 2026-09-18 **判断 1（一覧の中身）: 環境変数で名前を列挙する。** 未設定なら `primary` の 1 件。理由: 本物は登録したワークグループが並ぶので、athena-local では環境変数が登録の代わりになる。`primary` 1 件だけだと、Grafana や dbt に別名を設定している人の名前が設定画面の候補に出ない。`GetWorkGroup` は引き続き任意の名前を受け付け（#2 の判断 2）、一覧に無い名前でも成功する。**ユーザーに聞いて決めた**（最初の推奨は `primary` 1 件だったが、「本物では登録したものが並ぶのに primary だけで十分な理由は何か」との指摘を受けて変えた）。
- 2026-09-18 **判断 2（ページング）: 実測どおりに検証する。** `MaxResults` の範囲外と不正な `NextToken` は実測したエラーの型・文言・`AthenaErrorCode` で返す。一覧が `MaxResults` を超えるときだけ `NextToken` を返す。既存の `GetQueryResults` の寛容実装（上限チェック無し・不正トークンは先頭へ）には揃えない。`GetQueryResults` 側の差分は別 issue。**ユーザーに聞いて決めた**（「次の対応は実測で」）。

## 事前に確認した事実（Step 3）

Explore 3 本 + クライアント裏取り 1 本 + 実測スクリプト作成 1 本（2026-09-18）。行番号は main（3bc70c1）時点。

**オペレーションを 1 つ足す形（#2 と同じ）。**

- `src/handler.rs:34-41` の `dispatch` は `match` のアーム 5 本。`"GetWorkGroup"` が `:39`、受け皿 `other => invalid_request("未対応のオペレーションです: {other}")` が `:40`。`ListWorkGroups` は Trino にも Store にも触らないので同期 `fn` でよい。`router`／`App`（`src/lib.rs:29-44`、`src/handler.rs:20-27`）は変更不要。
- `src/operation.rs:388-421` の `get_work_group`。実測値は**関数内リテラル**で、`state: "ENABLED"`（`:408`）、`selected_engine_version: "AUTO"`（`:415`）、`effective_engine_version: "Athena engine version 3"`（`:416`）。共有できる定数・ヘルパはまだ無い。`WorkGroupSummary` に `Configuration` は無いので、共有が要るのは `State` と `EngineVersion` の 2 つだけ。
- `DEFAULT_WORK_GROUP`（`src/operation.rs:28-30`）の doc は「GetWorkGroup では使わない」。使用は `:83` の 1 箇所。一覧の要素名に使うなら doc を書き換える。
- `src/athena.rs:191-228` の `GetWorkGroupRequest`／`WorkGroup`／`WorkGroupConfiguration`／`EngineVersion`。`EngineVersion` は `Serialize` のみで `Clone` 無し（`:223`）。慣習: リクエスト任意項目は `#[serde(default)] Option<T>`、レスポンス省略項目は `skip_serializing_if = "Option::is_none"`、省く判断はコメントに書く（`:215` の例）。
- `src/response.rs`: `invalid_request`（`:65`、`__type`+`Message` の 2 キー）、`invalid_request_with_code`（`:37`、4 キー、2026-09-17 実測）、`parse`（`:70`）。エラーは常に HTTP 400。`InternalServerException` 専用ヘルパは無い。
- **ページングの前例 `get_query_results`（`src/operation.rs:423-453`）**: `NextToken` は次オフセットの 10 進文字列（`end.to_string()`、`:451`）、不正トークンは `parse().ok().unwrap_or(0)` で黙って先頭へ（`:437-441`）、`MaxResults` は `max(1)` で下限だけ丸め上限チェック無し、既定 `DEFAULT_MAX_RESULTS = 1000`（`:26`）。**ページング部分に実測コメントは無い**（TODO.md:79-80 に既知の差分として記録済み）。テストは `tests/select.rs:92-117` の 1 本だけで、`NextToken` の具体値は固定していない。
- `tests/dml.rs:165-188` `未対応のオペレーションはエラーになる` が `"ListWorkGroups"` を使う（`:168`、`:176`）。差し替え先は `CreateWorkGroup` が安全（TODO.md:98 で「当面やらない」、issue #9 でも範囲外。`ListDataCatalogs`／`ListDatabases` は TODO.md で実装予定なので再差し替えになる）。
- `tests/work_group.rs:1-139`: 期待値ヘルパ `configuration()`（`:20-32`）、`trino_requests().is_empty()`（`:116`）。`Harness::call`（`tests/common/mod.rs:307`）は `(u16, Value)`。
- 環境変数を 1 つ足すコスト: `Config` フィールド、`from_env`、`parse_X`（`parse_retention` `src/config.rs:155-171` が雛形）、**構造体リテラル 2 箇所（`src/config.rs:228`、`tests/common/mod.rs:228`）**、ユニットテスト、README 表、CHANGELOG の計 6 箇所。順序を保つなら `Vec<String>`（`catalog_map` の `HashMap` は真似しない）。
- 文書: README Supported API 表 `:112-118`（`GetWorkGroup` 行の直後に足す）、Not implemented `:331-333`（"listing" を外す）、Caveats `:443-449`／`:456-459`（workgroup の 2 項目）、compose タグ `:19` は `0.4.0`（`[Unreleased]` に足すだけなら不変）。CHANGELOGS `[Unreleased]` `### Added` の `GetWorkGroup` 項目 `:53-60` が文面の手本。TODO.md `:25-30` の節を削除、`:35` の「#9 とは別に扱う」は言い換え。
- 生成物なし（`build.rs` 無し、4 ファイルとも手書きコミット）。CI（`ci.yml`）は直近 5 件 success。`.claude/design-checklist.md` は 1 項目（早期 return の下流の出力。今回は直接当たらない）。

**クライアントが応答から読む項目（ソースで裏取り済み）。**

| クライアント | 読む項目 | 壊れ方 |
| --- | --- | --- |
| Grafana athena-datasource 3.3.4（`pkg/athena/api/api.go:285-305`） | `WorkGroups[].Name` と `NextToken` だけ。`MaxResults` は送らない | `Name` が無いと `*cat.Name` で panic（SDK に recover 無し）。**`NextToken` を空文字で返すと無限ループ**（`nextToken == nil` しか見ない）。省略か `null` なら 1 回で終わる |
| awswrangler／dbt-athena／PyAthena | 本体は `ListWorkGroups` を呼ばない（awswrangler はテストで `WorkGroups[].Name` を読むだけ） | 影響なし |

- Grafana の設定画面（`src/ConfigEditor.tsx:54-59`、`ResourceSelector.tsx:38-98`）: 一覧は `/workgroups` リソース経由で**設定画面でだけ**呼ばれる（クエリ実行時は `GetWorkGroup`）。保存済みの名前が一覧に無いと、メニューを開いた瞬間に候補から消えて入力欄が空表示になるが `jsonData.workgroup` は変わらずクエリは動く。一覧が空なら「No options found」。`primary` の直書きはプラグインに無い。
- botocore（`service-2.json`）: `ListWorkGroupsInput`／`Output`／`WorkGroupSummary` に必須項目なし。`MaxWorkGroupsCount` は min 1／max 50、`Token` は min 1／max 1024 で pattern 無し、`WorkGroupsList` は max 50、`CreationTime` は timestamp（awsJson1.1 なら epoch 秒の数値）、`WorkGroupState` は `ENABLED`／`DISABLED`。エラーは `InternalServerException` と `InvalidRequestException` のみ。**`paginators-1.json` に `ListWorkGroups` は無い**（boto3 の paginator は使えない）。

**issue 2 の生ログ（`$HOME/athena-workgroup-measurements/run-20260917-064427`）から追加で確かめたこと（2026-09-18。実名は出していない）。**

- `Description` の食い違いは**同じワークグループ**で確認できた。`GetWorkGroup` のキーは `Configuration`／`CreationTime`／`Name`／`State` で `Description` 無し。同じ名前の要素が `ListWorkGroups` では `Description: ""`（空文字）。つまり**説明の無いワークグループは、`GetWorkGroup` ではキーが無く、`ListWorkGroups` では空文字**。説明が非空のワークグループの `GetWorkGroup` は未実測。
- 一覧の並びは 3 件とも名前の辞書順と一致し、`CreationTime` 昇順とは一致しなかった（3 件なので弱い根拠。実測スクリプトでも測る）。
- 3 件とも `State: ENABLED`、`EngineVersion` は `AUTO`／`Athena engine version 3`（`GetWorkGroup` と同じ）。

## 実測の手順

- スクリプト: `.claude/issue-notes/9-measure-list-work-groups.sh`（`2-measure-workgroup.sh` と同じ安全策。クエリは流さない）
- 実行例: `REGION=ap-northeast-1 bash .claude/issue-notes/9-measure-list-work-groups.sh`
- 出力は `$HOME/athena-list-work-groups-measurements/run-<日時>/`。`summary.txt` だけ実名を伏せてあるのでそのまま貼れる。
- 測る項目: 素の応答、`--max-results 1` での `NextToken` の形と辿った結果、`--max-results` 50／51／0、不正な `NextToken`、同じワークグループでの `GetWorkGroup` と `ListWorkGroups` の `Description`、一覧の順序。

## 実測の結果（2026-09-18）

生の出力は `$HOME/athena-list-work-groups-measurements/run-20260918-051509`（実名が入るのでリポジトリには入れない）。ワークグループは 3 件。

| 項目 | 実測値 |
| --- | --- |
| 応答直下のキー | `WorkGroups` のみ（3 件では `NextToken` 無し） |
| 各要素のキー | `CreationTime`／`Description`／`EngineVersion`／`Name`／`State`（`IdentityCenterApplicationArn` 無し） |
| `State` | 3 件とも `ENABLED` |
| `EngineVersion` | `SelectedEngineVersion: AUTO`、`EffectiveEngineVersion: Athena engine version 3`。**ワイヤ上はさらに `Category: "Presto"` があり、CLI（botocore のモデルに無い）が落としている** |
| `Description` | 3 件ともキーあり。2 件が非空、1 件が空文字 |
| `CreationTime` | ワイヤ上は epoch 秒の浮動小数点数（`1.<12 桁>E9`。`GetWorkGroup` と同じ） |
| 順序 | 名前の辞書順と一致。`CreationTime` 昇順とは不一致（昇順に並べると逆順になった） |
| `MaxResults: 1` | 1 件ずつ 3 ページ。`NextToken` は 120 文字の標準 base64（`+`／`/`／`=` を含む）。辿った並びは素の応答と一致し、最終ページに `NextToken` は無い |
| `MaxResults: 50` | 成功。3 件、`NextToken` 無し |
| `MaxResults: 51` | HTTP 400／`__type: InvalidRequestException`／`AthenaErrorCode: INVALID_INPUT`（`ErrorCode` も同値）／`Message: 1 validation error detected: Value at 'maxResults' failed to satisfy constraint: Member must have value less than or equal to 50` |
| `MaxResults: 0` | **未実測。** CLI 側の検証（`valid min value: 1`）で止まり HTTP が飛ばなかった。生 HTTP で測り直す（下） |
| 不正な `NextToken`（`athena-local-invalid-next-token-probe`） | HTTP 400／`InvalidRequestException`／`INVALID_INPUT`／`Message: The nextPageToken is malformed: athena-local-invalid-next-token-probe`（**受け取ったトークンをそのまま末尾に付ける**） |
| 正しいトークンの末尾 1 文字を変えたもの | 同じ形（`The nextPageToken is malformed: <トークン>`） |
| `Description` の食い違い | **食い違わない。** 説明が非空のワークグループでは `GetWorkGroup` にも `Description` キーがあり値も一致（キーは `Configuration`／`CreationTime`／`Description`／`Name`／`State`）。issue 2 の生ログと合わせると、**説明が空のときは `GetWorkGroup` はキーを省き、`ListWorkGroups` は空文字を返す** |
| `x-amzn-errortype` ヘッダ | 採取できず（#2 と同じ。生 HTTP でも無かった） |

**生 HTTP（SigV4 自前署名、`9-measure-raw-list-work-groups.py`、`raw-20260918-052722`）で測った分。**

| 送った本文 | 実測値 |
| --- | --- |
| `{"MaxResults": 0}`、`{"MaxResults": -1}` | 400／`InvalidRequestException`／`INVALID_INPUT`／`Message: 1 validation error detected: Value at 'maxResults' failed to satisfy constraint: Member must have value greater than or equal to 1` |
| `{"NextToken": ""}`、`{"MaxResults": 1, "NextToken": ""}` | 400／`InvalidRequestException`／`INVALID_INPUT`／`Message: 1 validation error detected: Value at 'nextToken' failed to satisfy constraint: Member must have length greater than or equal to 1` |
| `{"MaxResults": "1"}`（型違い） | 400／`__type: SerializationException`／`AthenaErrorCode` 無し／`Message: STRING_VALUE can not be converted to an Integer`。athena-local は `parse` が `InvalidRequestException` にする（全オペレーション共通の経路、`src/response.rs:26-28` で未実測と断っている）。**今回は変えない**（別 issue の材料） |
| `{}` | 200、3 件、`NextToken` 無し |

**判断への影響。**

- athena-local には説明が無いので、`ListWorkGroups` は `Description: ""`、`GetWorkGroup` はキー無し（現状のまま）で、どちらも本物の「説明の無いワークグループ」と一致する。
- `EngineVersion.Category` は SDK のモデルに無く、どのクライアントも読めない（botocore も aws-sdk-go-v2 も落とす）。athena-local は返さず、README の Caveats に実測の事実として 1 行書く。
- 一覧は名前順で返す（環境変数の記述順ではなく、ソートする）。
- `NextToken` の中身は不透明で合わせようがないので、既存 `GetQueryResults` と同じオフセットの 10 進文字列にする。不正なトークンの文言は実測どおり受け取った値をそのまま末尾に付ける。

## 計画（Step 5）

### ListWorkGroups の契約

`WorkGroups` は `Config.work_groups`（環境変数 `ATHENA_LOCAL_WORK_GROUPS`、未設定なら `primary` の 1 件）を**名前の辞書順**に並べたもの（実測: 本物は名前順。設定順ではない）。要素の `State`／`EngineVersion` は `GetWorkGroup` と同じ実測値、`Description` は常に空文字。Trino にも Store にも問い合わせない。`MaxResults`／`NextToken` は実測どおりに検証する（判断 2）。一覧が 1 ページに収まるときは `NextToken` を**キーごと省く**（Grafana は `nextToken == nil` しか見ないので空文字だと無限ループ）。**一覧は `GetWorkGroup` の入力を制限しない**。一覧に無い名前でも `GetWorkGroup` は 200 を返す（#2 判断 2、README:443-449）。

### フェーズ分割: 1 フェーズ

本番コードの見積もりは 110〜130 行（`config.rs` 約 30、`athena.rs` 約 30、`operation.rs` 約 60、`handler.rs` 1）。#2（145 行）と同規模で 1 フェーズだった。設定の追加だけを先に出荷しても意味が無く、分けるとコミットと検証の回数だけ増える。**1 フェーズ 1 コミット。小規模 issue の経路**（独立レビュー 1 回のみ）。

### 対象ファイルと責務

新規モジュールは作らない（#2 の Step 6 採用 1: このリポジトリの分割軸は「`App`/`Response` を扱うディスパッチ本体を `operation.rs` に集約」）。`list_work_groups` は `operation.rs` の 6 つ目の関数。

| ファイル | 変更 | 責務 | 見込み |
| --- | --- | --- | --- |
| `src/config.rs` | `Config.work_groups: Vec<String>`（辞書順にソート済み）。`parse_work_groups` を新設。`DEFAULT_WORK_GROUP` を `operation.rs:28-30` から `pub const` として移設 | 設定の読み取り | 本番 +30／テスト +45 |
| `src/athena.rs` | `ListWorkGroupsRequest`／`ListWorkGroupsResponse`／`WorkGroupSummary`。`:18` のコメント「operation.rs の DEFAULT_WORK_GROUP」を `config.rs` に直す（計画レビュー 1-2） | 型定義 | +30 |
| `src/operation.rs` | `list_work_groups`。`WORK_GROUP_STATE` 定数と `fn engine_version()` を新設して `get_work_group` と共有。実測文言の定数。`DEFAULT_WORK_GROUP` は `use crate::config::DEFAULT_WORK_GROUP` | ディスパッチ本体 | +60（777 → 約 840。7-7 で分割を再評価。分割するなら 6 オペレーション全体なので別 issue 候補） |
| `src/handler.rs` | `:39` の直後に `"ListWorkGroups" => operation::list_work_groups(&app, &body)` | ディスパッチ表 | +1 |
| `tests/common/mod.rs` | `HarnessBuilder` に `work_groups` フィールド・メソッド、`Config` リテラル（`:228-237`）と `builder()` の初期化に 1 行ずつ | 足場 | +12 |
| `tests/list_work_groups.rs`（新規） | 結合テスト 8 件 | 応答・ページング・エラーの固定 | 170 行前後 |
| `tests/dml.rs` | `:168`、`:176` の `"ListWorkGroups"` を `"CreateWorkGroup"` に | 既存テストの差し替え | 2 行 |
| `README.md`／`CHANGELOGS.md`／`TODO.md` | 下の「ドキュメント更新」 | 文書 | 短い |

### 型に入れないもの

- **`CreationTime`**: #2 と同じ理由（実体が無く、どんな値も偽になる。CLAUDE.md:61）。本物が epoch 秒の数値を返すことは README の差分に書く。
- **`IdentityCenterApplicationArn`**: 実測で本物の応答に無かった。
- **`EngineVersion.Category`（ワイヤ上の `"Presto"`）**: SDK のモデルに無く、botocore も aws-sdk-go-v2 も落とすのでどのクライアントにも届かない。返さず、README の Caveats に実測の事実として書く。

**`Description` は入れる（`String`、常に空文字）。** 同じワークグループで測って、`GetWorkGroup` は説明が空ならキー無し、`ListWorkGroups` は `""`（2026-09-17／09-18 実測）。athena-local の全件が「説明の無いワークグループ」に相当するので、`ListWorkGroups` で `""` を返し `GetWorkGroup` で省く現状が、どちらも本物と一致する。`Option` にはしない。

### 依拠する既存機構の前提表

| 前提 | 自分の変更が破らないか | 保証元（file:line） |
| --- | --- | --- |
| `optional_env` は空文字を未設定として落とす | 破らない。`ATHENA_LOCAL_WORK_GROUPS=""` は未設定＝`primary` 1 件 | `src/config.rs:177-179` |
| `parse_retention` は「未設定なら既定、読めなければ起動時エラー」を関数内に持ち、テストはクロージャで差し替える | 破らない。`parse_work_groups` を同じ形にする | `src/config.rs:155-171`、`:243-247` |
| `parse_catalog_map` はカンマ区切り・要素 `trim`・空要素と重複を起動時エラー | 破らない。同じ規則を適用 | `src/config.rs:84-103`、テスト `:200-224` |
| `GetWorkGroup` は任意の名前を受け付け同じ `Configuration` を返す。`StartQueryExecution` の `WorkGroup` 省略時は `primary` | 破らない。`get_work_group` に分岐を足さない（リテラルを定数・関数に置き換えるだけ）。`DEFAULT_WORK_GROUP` は置き場所を移すだけ | `src/operation.rs:387-421`、`:30`、`:83` |
| `parse()` は Deserialize 失敗を `InvalidRequestException` にする | 破らない。全項目 `#[serde(default)] Option<T>` なので Grafana の `{}` が通る | `src/response.rs:70-73` |
| `skip_serializing_if = "Option::is_none"` でキーごと省ける | 破らない。`GetQueryResultsResponse.next_token` と同じ | `src/athena.rs:134-142` |
| `get_query_results` の `NextToken` はオフセットの 10 進文字列、不正は先頭へ、上限チェック無し | **表現だけ引き継ぎ、寛容さは引き継がない**（判断 2）。`get_query_results` は 1 行も変えない | `src/operation.rs:437-451`、TODO.md:79-80 |
| `Config` は構造体リテラルで 2 箇所から作られる | 破る（コンパイルエラー）。両方に足す | `src/config.rs:228-237`、`tests/common/mod.rs:228-237` |
| `tests/dml.rs` は `ListWorkGroups` が 400 を返すことに依存 | 破る。同じコミットで `CreateWorkGroup` に差し替え | `tests/dml.rs:164-187`、TODO.md:98 |

### この計画で決める細部

**環境変数 `ATHENA_LOCAL_WORK_GROUPS`。**

- 名前: athena-local 自身の設定は `ATHENA_LOCAL_*`（`src/config.rs:65-72`）。綴りは API の `WorkGroup` に合わせて `WORK_GROUPS`。
- 区切りはカンマ、要素ごとに `trim`（`parse_catalog_map` `src/config.rs:89-90` と同じ）。
- **空要素は起動時エラー**（末尾カンマは compose で現実に起こり、黙って通すと Grafana に空行の候補が出る。`:94-96` と同じ）。**重複は検査しない**（計画レビューの削減 1。名前の一覧に写像のような曖昧さは無く、起きるのはドロップダウンに同じ名前が 2 回出るだけ。起動を止める方が害が大きい）。
- **`parse_work_groups` で名前の辞書順にソートする**（実測で本物が名前順。`Vec<String>` を `sort()` して持つ。`HashMap` は使わない）。`Config.work_groups` は整列済みが不変条件。結合テストの足場（`tests/common/mod.rs:228` の構造体リテラル）は `parse_work_groups` を通らないので、**足場には整列済みの名前を渡し**、順序の責務は `config.rs` のユニットテストだけが持つ（計画レビュー 3-1）。
- `primary` を含めない設定を許す（自動で足さない）。一覧に無くても `GetWorkGroup` は成功し、Grafana も保存済みの名前が一覧に無くてもクエリは動く。副作用: `WorkGroup` を省いた `StartQueryExecution` は一覧に無くても `primary` になる。README の Caveats に書く。
- 名前の形式（botocore の `[a-zA-Z0-9._-]{1,128}`）は検証しない。`GetWorkGroup` でも検証していないので揃える。
- 件数の上限は検査しない（50 件超の設定は起きず、起きてもページングされるだけ）。

**`Config` の持ち方。** `pub work_groups: Vec<String>`。`parse_work_groups(env: impl Fn(&str) -> Option<String>) -> Result<Vec<String>, String>`。前置き `ATHENA_LOCAL_WORK_GROUPS: ` は関数内で付ける（`parse_retention` と同じ）。

**`DEFAULT_WORK_GROUP` の移設。** `src/config.rs` へ `pub const`（`DEFAULT_RETENTION` `:9-10` の前例）。doc:「StartQueryExecution で WorkGroup が省略されたときの既定名で、ATHENA_LOCAL_WORK_GROUPS が未設定のときに ListWorkGroups が返す唯一の名前。GetWorkGroup では使わない（WorkGroup は必須項目で、無ければ parse が弾く）。」

**`State`／`EngineVersion` の共有: 案 A。** `operation.rs` に `const WORK_GROUP_STATE: &str = "ENABLED";`（実測日コメント）と `fn engine_version() -> EngineVersion` を置き、両関数から呼ぶ。`EngineVersion` に `Clone` は足さない（案 B）。直書き + テストの突き合わせ（案 C）は crate 内 2 箇所なので共通化で足りる。issue 本文の「食い違わせない」をコンパイラで保証する。

**型（`src/athena.rs`）。** `ListWorkGroupsRequest { max_results: Option<i32>, next_token: Option<String> }`（`#[serde(default)]`、`GetQueryResultsRequest:124-131` と同じ）。`ListWorkGroupsResponse { work_groups: Vec<WorkGroupSummary>, next_token: Option<String> }`（`skip_serializing_if`。コメントに空文字だと Grafana が無限ループする旨）。`WorkGroupSummary { name, state, description: String, engine_version: EngineVersion }`（既存の `EngineVersion` を再利用）。

**`list_work_groups` の本体（50 行以内）。**

1. `parse::<ListWorkGroupsRequest>`。
2. `MaxResults` の範囲検査を **`usize` へのキャストより前に**行う（計画レビュー 1-1。キャスト後だと `-1` が巨大値になり `offset + limit` がオーバーフロー、`0` だと `end == offset` で同じ `NextToken` を返し続け Grafana が無限ループする）。`i32` のまま比較し、`< 1` は `invalid_request_with_code("1 validation error detected: Value at 'maxResults' failed to satisfy constraint: Member must have value greater than or equal to 1", "INVALID_INPUT")`、`> 50` は同じ形で `less than or equal to 50`（どちらも 2026-09-18 実測）。未指定は既定 50（**本物の既定ページサイズは未実測**。3 件しか無く測れない。botocore の `MaxWorkGroupsCount` の max に合わせ、README に未実測と書く）。
3. `NextToken` の検査。空文字は `invalid_request_with_code("1 validation error detected: Value at 'nextToken' failed to satisfy constraint: Member must have length greater than or equal to 1", "INVALID_INPUT")`（2026-09-18 実測）。それ以外で「`usize` として読めて `< names.len()`」でないものは `invalid_request_with_code(format!("The nextPageToken is malformed: {token}"), "INVALID_INPUT")`（2026-09-18 実測。受け取った値をそのまま末尾に付ける）。この規則は発行したトークン（`1 <= end < len` の 10 進）を全部受け、発行していない値のうち `"0"`・先頭ゼロ・`+` 付きも通るが、結果は正当なページと同じなので厳密化しない（計画レビュー 3-3）。`get_query_results` のような丸め（`.min(len)`）は入れない。
4. `end = (offset + limit).min(names.len())`、`next_token: (end < names.len()).then(|| end.to_string())`（`src/operation.rs:451` と同じ表現）。
5. 検査の順序は `MaxResults` → `NextToken`。両方が同時に不正なときの本物の順序は実測しない（クライアントからは起こらない）。

**直列化・エスケープ境界。** 環境変数の分割（`split(',')` + `trim`）と `NextToken` の `usize` ⇄ 文字列だけ。JSON は serde。手書きの連結は無い。厄介な入力は `config.rs` の表駆動テスト（前後空白・空要素・末尾カンマ・重複）。

**表現の正規化境界。** 該当なし。一覧と `GetWorkGroup` の名前を突き合わせない。

### 追加・変更するテスト

**`tests/list_work_groups.rs`（新規、8 件）。** 足場（`HarnessBuilder::work_groups`）には整列済みの名前を渡す。順序は `config.rs` のユニットテストが担う。

| テスト名 | 固定する内容 |
| --- | --- |
| `設定が無ければ_primary_の_1_件を返す` | 200、1 件、`Name == "primary"`、**`NextToken` キーが無い** |
| `設定した名前をその順に返す` | `work_groups(&["ad-hoc", "etl", "primary"])` → 3 件が同じ順 |
| `summary_の項目は_name_state_description_engine_version_の_4_つ` | キー集合を `sort()` して完全一致（`tests/dml.rs:181-188` の手口）。`Description == ""`、`State == "ENABLED"`、`EngineVersion` の 2 値 |
| `state_と_engine_version_は_get_work_group_と同じ値を返す` | 同じ Harness で両方を呼び、応答どうしを突き合わせる |
| `max_results_でページングされ_next_token_を辿ると続きが取れる` | 5 件を `MaxResults: 2` で 3 ページ。1・2 ページ目に `NextToken`（具体値 `"2"`、`"4"` も固定）、最終ページに無い。連結すると辞書順の全件 |
| `max_results_が範囲外ならエラーにする` | `51`／`0`／`-1` → 400、`InvalidRequestException`、`INVALID_INPUT`、実測文言（上限と下限で違う）、4 キー |
| `不正な_next_token_はエラーにする` | `"abc"`、`"999"`（範囲外）、`"5"`（`== len`）→ `The nextPageToken is malformed: <値>`。`""` → 長さの制約の実測文言 |
| `trino_には一切問い合わせない` | `trino_requests().is_empty()`（計画レビューの削減 3 は採らない。`ListWorkGroups` 自身がこの性質を持つことを固定する機械検査はこれだけで、`tests/work_group.rs` の同名テストは `GetWorkGroup` の話） |

**`src/config.rs` のユニットテスト（3 件）。** `ワークグループの既定は_primary_の_1_件`、`カンマ区切りで読み前後の空白を捨てて辞書順に並べる`（`" etl , ad-hoc ,primary"` → `["ad-hoc", "etl", "primary"]`）、`空の要素があれば起動時エラーにする`（表駆動: `","`／`"a,"`／`"a,,b"`／`" "`。コメントに「空文字は `optional_env` が先に落とすのでこの関数には届かない」）。

**`tests/dml.rs`** の 2 箇所を `CreateWorkGroup` に。

### テストが通るための配線と red の予測

1. 配線: `Config.work_groups`、`parse_work_groups`、`DEFAULT_WORK_GROUP` 移設、構造体リテラル 2 箇所、`HarnessBuilder::work_groups`。ここでコンパイルが通り、`config::tests` 3 件は red → green。
2. `tests/list_work_groups.rs` を書いて red: 8 件中 7 件が 400（`未対応のオペレーションです`）で落ちる。`trino_には一切問い合わせない` だけは実装前でも green（#2 と同じ。固定力はミューテーションで裏取り）。
3. `athena.rs` の型 → `operation.rs` → `handler.rs` で green。
4. green の直後に `tests/dml.rs` が落ちる（予測済み）。`CreateWorkGroup` に差し替え。
5. README／CHANGELOGS／TODO は同じコミット。

### ミューテーション確認で壊す候補

| 壊す内容 | 落ちるはずのテスト |
| --- | --- |
| `next_token` の `skip_serializing_if` を外す | `設定が無ければ_primary_の_1_件を返す` |
| `(end < names.len())` を `true` に | `max_results_でページングされ...` |
| `parse_work_groups` の既定を `vec![]` に | `ワークグループの既定は...` だけ（結合テストの足場は `parse_work_groups` を通らない。計画レビュー 3-2） |
| `DEFAULT_WORK_GROUP` を `"default"` に | `ワークグループの既定は...`、`設定が無ければ_primary_の_1_件を返す`（足場の初期化が定数を参照する）、`tests/work_group.rs:130-139` |
| `trim` を外す／`sort()` を外す | `カンマ区切りで読み...` だけ |
| 空要素のエラーを外す | `空の要素があれば...` |
| `MaxResults` の検査をキャストの後に動かす（`-1 as usize`） | `max_results_が範囲外ならエラーにする` |
| `Description` を `Option` + `skip_serializing_if` に | `summary_の項目は_..._4_つ` |
| `WORK_GROUP_STATE` を `"DISABLED"`／`EffectiveEngineVersion` を `version 2` に | `summary...`、`state_と_engine_version...`、`tests/work_group.rs:44` |
| `MaxResults` の範囲検査を外す | `max_results_が範囲外ならエラーにする` |
| `NextToken` の検査を `parse().ok().unwrap_or(0)` + `.min(len)` に戻す | `不正な_next_token_はエラーにする` |
| `list_work_groups` の中で `app.trino` を呼ぶ | `trino_には一切問い合わせない` |
| `app.store` を呼ぶ | 検知できない（#2 と同じ）。差分を目で確認 |

### 人間検証リスト（Step 9）

- Grafana athena-datasource 3.3.4 の設定画面でワークグループのドロップダウンに一覧が出る（issue の「できれば」）。docker で Grafana を立てられるかは Step 9 の前に確認する。
- 3 件設定して辞書順に候補が並ぶこと。一覧に無い名前を保存した状態でクエリが動くこと。
- **`NextToken` を返す構成（51 件以上）で Grafana が無限ループしないこと。**
- awswrangler／dbt-athena／PyAthena は `ListWorkGroups` を呼ばないので検証不要。

### 実測で確定した値と未実測（判断 0）

| 項目 | 状態 |
| --- | --- |
| `MaxResults` の上限超過（51） | **確定**（「実測の結果」の表） |
| `MaxResults` の下限超過（0、負） | **確定**（生 HTTP、`raw-20260918-052722`） |
| `MaxResults` 未指定の既定ページサイズ | **未実測**（3 件では測れない）。50 にし README に未実測と書く |
| 不正な `NextToken`（不正な文字列、空文字） | **確定** |
| `NextToken` の形 | 本物は 120 文字の base64（不透明）。athena-local はオフセットの 10 進文字列。README の差分に書く |
| 一覧の順序 | **確定**: 名前順（3 件） |
| `MaxResults` の型違い（文字列） | 確定だが `SerializationException` は全オペレーション共通の `parse` 経路なので今回は変えない |
| `State`／`EngineVersion`／`Description`／`CreationTime` | **確定** |
| 両方同時に不正なときの順序 | 実測しない |

### 未検証の前提

1. Grafana は一覧に無い名前が保存されていてもクエリを実行できる（ソースを読んだ主張。実機未確認 → 人間検証リスト）。
2. Grafana は `MaxResults` を送らない（`api.go:290-292`。実機未確認）。
3. 本物の既定ページサイズ（測れない。README に未実測）。
4. 一覧の順序が名前順なのは 3 件だけの根拠。
5. `CreateWorkGroup` が今後も未実装のまま（破れても差し替えるだけ）。

### ドキュメント更新

- README `:118` の直後に Supported API の行: `ListWorkGroups` は `ATHENA_LOCAL_WORK_GROUPS` の名前を名前順に返す（未設定なら `primary`）。`MaxResults`／`NextToken` でページング。
- README `:331-333`: `listing` を外し `GetWorkGroup and ListWorkGroups are supported` に。
- README 設定表（`:64-75`）に `ATHENA_LOCAL_WORK_GROUPS`（既定 `primary`。カンマ区切り。空要素・重複は起動時エラー。`GetWorkGroup` は一覧に無い名前も受け付ける）。
- README Caveats に 3 項目: (1) 一覧は環境変数から作る固定リストで、一覧に無い名前でも `GetWorkGroup` は成功し、`WorkGroup` を省いた `StartQueryExecution` は一覧に無くても `primary` になる。順序は本物と同じ名前順。(2) `Description` は常に空文字（本物も説明の無いワークグループは空文字）、`CreationTime`／`IdentityCenterApplicationArn`／ワイヤ上の `EngineVersion.Category` は返さない。(3) ページング: `MaxResults` の範囲外と不正な `NextToken` は実測どおり弾くが、既定ページサイズは未実測で 50、`NextToken` の形は本物（不透明な base64）と違う。`GetQueryResults` の差分は TODO.md にだけ書く（計画レビューの削減 6）。
- CHANGELOGS `[Unreleased]` `### Added` に 1 エントリ（`GetWorkGroup` の `:53-60` が手本。実測日 2026-09-18）。
- TODO.md `:25-30` の節を削除、`:35` を「ワークグループの一覧とは別に扱う」に、`:79-80` に `GetQueryResults` の検証が `ListWorkGroups` と揃っていない旨を 1 行。

## 一致台帳

| 値・型 | 出現箇所 | 守り方 |
| --- | --- | --- |
| 既定名 `"primary"` | (a) `src/config.rs` の `DEFAULT_WORK_GROUP`（移設）(b) `src/operation.rs:83` (c) `parse_work_groups` の既定 (d) `tests/list_work_groups.rs` (e) `tests/work_group.rs:138` (f) README | 共通化。(a) を唯一の定義にし (b)(c) が参照。結合テスト (d)(e) は同一入力表テストで固定。(f) は人のレビュー |
| `State` の実測値 `"ENABLED"`、`EngineVersion` の 2 値 | `get_work_group`／`list_work_groups`／`tests/work_group.rs`／`tests/list_work_groups.rs` | 共通化（`WORK_GROUP_STATE`、`fn engine_version()`）+ 機械検査（`state_と_engine_version_は_get_work_group_と同じ値を返す` が応答どうしを突き合わせる） |
| `EngineVersion` の Rust 型 | `WorkGroupConfiguration` と `WorkGroupSummary` | 既存の型を再利用。別型を作らない |
| 環境変数名 `ATHENA_LOCAL_WORK_GROUPS` | `src/config.rs`、README、CHANGELOGS | コードは 1 箇所。README との一致は独立レビューに明示して渡す |
| `NextToken` のオフセット表現 | `get_query_results`（`:437-451`）と `list_work_groups` | **意図的なフォーク。共通化しない。** 判断 2 で検証の寛容さを揃えないと決めた。`get_query_results` は変えない。差分は README と TODO.md に記録し、揃えるかは別 issue。`humanDecisions` に載せる |
| `WorkGroupSummary` に入れない項目 | `src/athena.rs`、README、CHANGELOGS | 機械検査（`summary_の項目は_..._4_つ` がキー集合を完全一致で固定） |
| `tests/dml.rs` の未対応オペレーション名 | `:168`、`:176` | 同じ関数内の 2 箇所。片方だけだと落ちる |

## 計画レビューの結果（Step 6、2026-09-18）

Explore 1 本（小規模 issue の経路。契約・再利用・構造・削減の 4 観点）。4 ゲートで判定した。

**採用**

- **3-1（high）**: ソートを `parse_work_groups` に置く一方、結合テストの足場は `Config` を構造体リテラルで組む（`tests/common/mod.rs:228`）ので `parse_work_groups` を通らず、「辞書順に返す」結合テストは実装しても緑にならない。→ 結合テストを落とし、順序の責務は `config.rs` のユニットテストに一本化。足場には整列済みの名前を渡す。
- **1-1（medium）**: `Option<i32>` → `usize` の変換をキャストの前に検査する。無いと `-1` でオーバーフロー、`0` で `NextToken` が進まず Grafana が無限ループする。→ 下限の実測が揃ったので、`i32` のまま範囲検査してからキャスト。ミューテーション候補にも足した。
- **3-2（medium）**: ミューテーション表が足場の迂回する経路の固定力を誤って主張していた。→ 結合テストの欄を外した。
- **1-2（low）**: `src/athena.rs:18` のコメントが移設で嘘になる。→ 変更ファイル表に足した。
- **3-3（low）**: `NextToken` の規則の文言が不正確（`"0"`・先頭ゼロ・`+` 付きも通る）。→ 文言だけ直し、厳密化の分岐は足さない。

**採用（削減）**

- 重複名の起動時エラーとそのテスト 2 ケース（削減 1）。名前の一覧に曖昧さは無く、起動を止める方が害が大きい。
- 結合テスト `一覧に無い名前でも_get_work_group_は成功する`（削減 4）。`get_work_group` は `work_groups` を読まず、既存の `tests/work_group.rs:48-59` が既定設定（`primary` のみ）で別名の 200 を固定しているので、一覧に無い名前の成功は既に固定済み。
- 結合テスト `環境変数の名前を辞書順に返す`（削減 5、3-1 の帰結）。
- README Caveats から `GetQueryResults` の差分の言及（削減 6）。TODO.md だけに書く。
- 前提表の重複行（削減 7）。

**採用しない**

- **`MaxResults` の下限検査を外す（削減 2）**: レビュー中に生 HTTP の実測が揃い、下限超過の文言が確定した。判断 2（実測どおりに検証。ユーザー決定）に従い実装する。分岐は上限と同じ 1 つの範囲検査で、`.max(1)` の丸めより短い。
- **`trino_には一切問い合わせない` を外す（削減 3）**: `ListWorkGroups` がこの性質を持つことの機械検査はこれだけ（`tests/work_group.rs` の同名テストは `GetWorkGroup`）。1 テスト 8 行で、`app.trino` を呼ぶミューテーションを検知する。

## フェーズの記録

## review-fix-loop の最新 nextArgs

（小規模 issue の経路なら無し）

## レビュー実施記録

| 工程 | 回数 | 本番影響のある妥当指摘の件数 |
| --- | --- | --- |
| 計画の敵対的レビュー（Step 6） | 1（Explore 1 本） | 2。3-1（結合テストが緑にならない）、1-1（`MaxResults` のキャスト前検査） |

## Step 8 の結果

## dismissed

- `ListDataCatalogs`／`GetDataCatalog`、ワークグループの作成・更新・削除、設定の強制。issue 本文が範囲外としている。
- `MaxResults` の型違いで本物が返す `SerializationException`（生 HTTP で実測）。`parse` の失敗経路は全オペレーション共通（`src/response.rs:26-28`）で、`ListWorkGroups` だけ変えると食い違う。別 issue の材料として実測値をノートに残した。
- `GetQueryResults` のページング検証を `ListWorkGroups` に揃える。判断 2 で別 issue とした。TODO.md に 1 行残す。
- `NextToken` の規則の厳密化（先頭ゼロ・`+` 付き・`"0"` を弾く）。結果が正当なページと同じで、トークンを手打ちするクライアントは無い（計画レビュー 3-3）。

## deferred

| 指摘 | severity | file | 再トリアージ |
| --- | --- | --- | --- |

## 覆した事前判断

（元の判断 / 新しい判断 / 理由 / 外部契約・プロダクト挙動・運用への影響 / ユーザーに聞いたか）

## 実機検証

## マージ後の周知事項

## 委譲の記録

| 工程 | 委譲先 | 自分でやった場合の理由 |
| --- | --- | --- |
| issue ノートの骨格 | 自分 | 判断そのもの。委譲先に渡す材料が書く本文より長い |
| Step 3 調査 | Explore 3 本 | |
| クライアントの裏取り | general-purpose 1 本 | |
| 実測スクリプトの作成 | general-purpose 1 本 | |
| issue 2 の生ログの再確認 | 自分 | python 1 本で済み、実名を扱うので委譲しない |
| 調査結果のノートへの集約 | 自分 | 5 本の報告から設計に効く事実を選ぶのは判断 |
| Step 5 計画の下書き | Plan 1 本 | 読み取り専用で書けなかったので、実測結果と判断（順序・範囲外トークン）を反映しつつ自分でノートに転記した |
| 生 HTTP の実測スクリプト | general-purpose 1 本 | |
| Step 6 計画レビュー | Explore 1 本 | |
| 指摘のトリアージと計画の修正 | 自分 | 判断そのもの |

## 出荷

## 時系列

- 2026-09-18 04:52 `main-9` を作成し、着手。着手前 SHA は 3bc70c1。
- 2026-09-18 05:03 調査 5 本を集約。判断 1（環境変数で列挙）と判断 2（実測どおりに検証）をユーザーと決めた。
- 2026-09-18 05:15 ユーザーが実測スクリプトを実行（run-20260918-051509）。`MaxResults: 0` だけ CLI 側で止まり未実測。
- 2026-09-18 05:27 ユーザーが生 HTTP の実測を実行（raw-20260918-052722）。実測待ちが無くなった。
- 2026-09-18 05:30 計画レビュー（Explore 1 本）の指摘 5 件を採用、削減 5 件を採用、2 件を採用せず。Step 6b の設計確認へ。
