# issue #145: エラー応答の x-amzn-errortype ヘッダを本物に合わせて付けないようにするか決める

## 基本情報

- ベース: main（9ca5754、#155 のマージ）。ブランチ: main-145。着手前 SHA（レビューの base）: 535d5f8（過去ノートの削除だけ）
- 経路: 小規模 issue（フェーズ 1）。判断（外すか docs に書くか）はユーザー

## 事前に確認した事実

Explore 1 本（opus、8 項目）から転記。落とした番号: 8 の `invalid_request_with_code` の 16 か所の内訳（外すのは `error_body` の 1 か所で、呼び出し元は変えない）。

1. ヘッダを付けているのは `src/response.rs` の非公開関数 `error_body`（:80-93）の :88-89 だけ。`error`（:27）・`bare_error`（:33）・`invalid_request_with_code`（:67）・`serialization_error`（:39 経由）はすべてここを通るので、エラー応答の全部に付く。値は `__type` と同じ `code`（`AthenaErrorCode` ではない）。理由のコメントは :22「SDK は __type（または x-amzn-errortype）でエラーの種類を判別する。」だけ
2. 参照: `src/response.rs:22,89` と `CLAUDE.md:52`（「`__type` と `x-amzn-errortype` ヘッダ」）。`tests/` には 0 件。docs（caveats・api・clients・README・CHANGELOG）にも 0 件。`docs/dev/decisions.md:154`「本物に無かったが、消す理由も無い（SDK は `__type` を先に見る）ので残す。（#3、2026-09-17）」
3. 本物で「無い」と記録した実測: #3（client-request-token.md:19,79）、#9（work-groups.md:123 の生 HTTP）、#83（query-results.md:22）、#84（errors.md:25,47。46 項目）、#87（errors.md:54）、#147（errors.md:62、client-request-token.md:105,162。2026-09-24 の 200/400 とも 5 ヘッダだけ）。6 issue・2026-09-17〜24。#2 は CLI で採取できず（work-groups.md:84）。issue 本文の行番号はずれている
4. 導入: 68a129c（2026-09-12 の初回）から。残す判断は 71e74f7（#3、2026-09-17）「SDK は __type を先に見るので残す」。「SDK がヘッダを必要とする」根拠はどこにも無い
5. クライアント: botocore の awsJson パーサ（`BaseJSONParser._do_error_parse`）は本文の `__type` だけを見る（`x-amzn-errortype` を読むのは restJson 用の `RestJSONParser._inject_error_code` だけ）。Rust SDK（aws-sdk-athena 1.97.0 の `json_errors.rs:64,71`）は逆にヘッダを先に見て、無ければ本文の `code` → `__type`。値は常に `__type` と同じなので、外しても決まる型は同じ（未検証。JDBC も未検証）。decisions.md:154 の「SDK は `__type` を先に見る」は Rust SDK には当てはまらない
6. テストの足場: `tests/common/mod.rs` の `call`/`call_raw`/`post`（:331-370）は `(u16, Value)` を返し、応答ヘッダを捨てる。ヘッダを外して落ちるテストは 0 件の見込み。付かないことを固定するには Harness にヘッダを返すヘルパが要る
7. `src/response.rs` は 93 行。外すなら :88-90 の 3 行と `HeaderValue` の import（:3）。`error_body` の引数 `code` は使われなくなる

## 設計判断

- Step 4（2026-09-24、ユーザー）: **外す**。推奨どおり: 本物に合わせるのがこの製品の目的で、値は常に `__type` と同じなので SDK が決める例外の型は変わらない（botocore は読まない、Rust SDK は本文にフォールバック）

## 計画

小規模経路（フェーズ 1）。Plan は省略し統括役が書いた。

### フェーズ 1: `x-amzn-errortype` を付けない（本番コード −5 行、テスト +約 25 行）
- 変更ファイル: `src/response.rs`（:88-90 を消し、`HeaderValue` の import を消し、`error_body` の引数 `code` を消して呼び出し 3 か所を直す。:22 のコメントを「本物は `__type` だけで、`x-amzn-errortype` ヘッダは付けない（2026-09-17〜24 の 6 issue で実測。#145）」に）。`tests/common/mod.rs`（`post` の隣に応答ヘッダも返す `post_with_headers` を足す。約 15 行）。`tests/request_errors.rs` か既存のエラー系のテストファイルに「エラー応答に x-amzn-errortype ヘッダを付けない」を 1 本（`UnknownOperationException` の 400 で確かめる。約 10 行）。`docs/dev/decisions.md:154` の判断を書き換え、`CLAUDE.md:52` から「と `x-amzn-errortype` ヘッダ」を消す。`docs/caveats.md` の Errors and request bodies に「本物と同じくヘッダは付けない」を 1 文（利用者向けの事実）。CHANGELOG の Unreleased / Changed に 1 行
- 依拠する前提表: すべてのエラー応答が `error_body` を通る（事実 1。response.rs の 3 呼び出し）／Harness の `post` はヘッダを捨てる（事実 6）→ 新ヘルパはヘッダも返す／SDK は本文から型を決める（事実 5。Rust SDK はヘッダ優先だが値が同じなので同じ結果）
- テスト: 新規 1 本（red: ヘッダが付いているので落ちる → green）。既存の全テストは影響なし（ヘッダを見ていない）
- ミューテーション: `error_body` に insert を戻す → 新規テストが落ちる
- 先行できる作業: 無し。人間検証リスト: 無し（SDK の実挙動は #111 の足場で測れるが、値が `__type` と同じで型が変わらないことは論理で足りる。実機は `tools/e2e/minio/verify.sh` の request-errors が 400 の本文を見ている）

## 計画レビューの結果

Explore 1 本（opus、契約・削減・一貫性）。採用 3、棄却 0。
1. 採用（更新漏れ）: `tools/e2e/request-errors/verify.sh:14`「athena-local は x-amzn-errortype ヘッダを送る」、`docs/dev/measurements/errors.md:47,70`「差分は #145」がマージ後に事実と逆になる → 変更ファイルに足す
2. 記録のみ（契約）: Rust SDK（`json_errors.rs:49-71`）はヘッダ → 本文 `code` → `__type` の順で、本文の `ErrorCode` は大文字なので `__type` にフォールバック。Java SDK v2（JDBC 3.3.0 同梱の `JsonErrorCodeParser`）も `parseErrorCodeFromHeader` → `parseErrorCodeFromContents`（クラスの文字列から推定）。botocore は本文だけ。外しても型は変わらない
3. 採用（削減）: (a) `post_with_headers` は不要（`athena_url` は pub なのでテスト内で直接 POST）(b) caveats.md の 1 文は不要（差が無くなる）
4. 記録のみ: `error_body` の引数 `code` を消すのは妥当（重複が消える。残すと unused）。:22 のコメントには「Rust / Java SDK はヘッダを先に見るが、無ければ `__type`」と書く

## フェーズの記録

### フェーズ 1（2026-09-24、自分）
- 7-0: 変えるのは `error_body` の 3 行（ヘッダの insert）と引数 `code`。依存する呼び出し元: response.rs 内の 3 か所だけ（引数を消して追随）。増える分岐: 無し（消すだけ）。固定するテスト: 新規 `エラー応答に_x_amzn_errortype_ヘッダを付けない`（tests/request.rs）
- red: 新規テストが `tests/request.rs:304` の assert で落ちた → 実装 → green（`cargo test --test request` 10 passed）
- 7-3: `cargo fmt --check` ok、`cargo clippy --all-targets --locked -D warnings` 警告 0、`cargo test --locked` 330 passed / 0 failed
- 7-4: `error_body` に insert を戻す → 新規テストが落ちる（1 failed）。戻して git status で確認
- 計画から外れた点: 無し。7-6: 省略（小規模経路）。7-7: 分割不要（response.rs 86 行）

## レビュー実施記録

## dismissed

## deferred

| 指摘 | severity | 再トリアージ |
|---|---|---|

## 覆した事前判断

## 実機検証

- `tools/dev.sh tools/e2e/request-errors/verify.sh`（compose の trino / minio 不要。athena-local 単体）: PASS=36 FAIL=0 SKIP=0（2026-09-24 13:35）。本文の形は変えていないので従来どおり。ヘッダの不在は結合テスト `エラー応答に_x_amzn_errortype_ヘッダを付けない` が固定する

## 範囲外の発見

| 内容 | 出どころ | issue |
|---|---|---|

## 委譲の記録

| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| 調査 | Explore 1 本（opus） | 小規模経路 |
| 計画 | 自分 | 小規模経路（Plan 省略） |
| 計画レビュー | Explore 1 本（opus） | 規則どおり |
| フェーズ 1 の実装 | 自分（小規模経路・本番 −5 行・テスト +15 行） | 委譲の固定費の方が長い |

## 出荷

## 時系列
- 2026-09-24 13:11 535d5f8 過去のノートを消して着手。Explore 1 本を起動
- 2026-09-24 13:14 Explore が返った。事実と計画をノートに書き、Step 4 でユーザーに確認 → 「外す」。計画レビュー（Explore 1 本）を起動
- 2026-09-24 13:30 5452eb3 計画レビューが返った（採用 3、棄却 0）。6b: 判断は Step 4 で済み、残る論点 0 件のため提示のみ
- 2026-09-24 13:32 bee4f1c フェーズ 1 を実装（red → green、clippy、全テスト 330、ミューテーション）
