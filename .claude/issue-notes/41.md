# quick #41 MERGE の `.metadata` は実測済みなので README の未実測リストから外す
- 入力: issue #41（出どころは `/tmp/athena-local-merge-metadata-instructions.md` の指示書）。モード: standalone。ブランチ `main-41`、着手前 SHA 2cf7a9d
- ゲート: サイズ 本番コードの変更 0 行（README 9 行・CHANGELOG 6 行・テスト 39 行。≤50・3）／呼び出し元 0（挙動を変えない）／契約は README の記述を実測に合わせる修正なので同じコミットで README・CHANGELOG を直す条件で通過／新規性なし（既存の `update_は本体を置かず_csv_metadata_だけを置く` と同じ形）／事実は #35 の生データを `od` で読めば手元で再現できる／判断は issue が方針を固定 → **6 観点すべて通過**（層を足さない素の quick）
- 時系列: 着手 19:11 ／ red 19:13（MERGE のバイト列を固定するテストが無い状態）／ green 19:13 ／ コミット 19:17
- 検証: `cargo fmt --check` ok、`cargo clippy --all-targets --locked -- -D warnings` ok、`cargo test --locked` ok（223 passed / 0 failed）／ミューテーション: `write_metadata` に渡す `updateType` で `MERGE` だけを `UPDATE` に取り違える変異 → 新テストだけが落ちた（10 passed / 1 failed）。`updateType` を丸ごと `"UPDATE"` に固定する粗い変異では 8 件が巻き添えで落ちたので、このテストでしか捕まらない壊し方に絞り直した
- 自己レビュー: 新しい分岐は無い（本番コードを 1 行も変えていない）。テストが踏むのは `write_metadata` が `outcome.update_type` をそのまま field 2 に渡す経路だけで、ミューテーションで効くことを確かめた／新しいエラー戻り値なし／呼び出し元は変えていない
- 打ち切り・escalate: 提案なし。stash なし
- 予算の実績: Bash 約 14 本、所要 6 分、コミット 1、サブエージェント 0 本（本番コードを変えていないので止めない独立レビューは流さない）
- 出荷: ブランチ `main-41`（PR 番号は `gh pr list --head main-41` で引ける）

## 実測の裏取り（#35 の生データから。2026-09-20）

`~/athena-insert-measurements/run-20260920-182735/` の `e`・`f`・`g` の `.metadata` を読んだ。
本物の Athena への再実測はしていない（生データで足りた）。

| ラベル | 文 | field 2 | 全体 |
|---|---|---|---|
| e | `UPDATE` | `12 06 "UPDATE"` | 75B |
| f | `DELETE` | `12 06 "DELETE"` | 75B |
| g | `MERGE` | `12 05 "MERGE"` | 74B |

**`35.md` の実測表が MERGE だけ 74B と記録していた 1 バイトの差は、field 2 の文字列長の違いだった。**
field 2 より後ろは 3 本ともバイト単位で同じ（`18 01` 更新件数 1、`22 22` で列 1 件の `rows bigint`、
`38 13` precision 19、`40 00` scale 0、`48 03` nullable UNKNOWN、`50 00` caseSensitive 0）。
field 1 は 27 バイトのエンジンのクエリ ID で 3 本とも同じ長さ。`5.md` の「top-level のフィールド」の
記録とも整合する。

新しいテストの期待値は手計算ではなく、この生データの g の field 2 以降とバイト単位で一致する
（`12054d45524745 1801` + `COLUMN_ROWS_BIGINT`）。

## 未検証の前提

- **Trino が `MERGE` に `updateType: "MERGE"` を返すか**は確かめていない。テストは偽 Trino に
  `"MERGE"` と書いているだけで、本物の Trino の裏取りにはならない。手元に Trino が無く、MERGE は
  Iceberg のテーブルでしか動かないので、確かめるにはメタストアとオブジェクトストアを含むカタログを
  立てる必要があり、quick の規模を超えると判断した。athena-local は `outcome.update_type` を
  そのまま field 2 に渡すので、Trino が別の文字列を返すなら field 2 がその文字列になる。

## 範囲外の修正

- `35.md` の「予算の実績」が「コミット 2」のままだったのを 3 に直した。独立レビューの修正
  （`1a280d1`）がノートを書いた後に入ったため。独立レビューの結果をノートに書き足していないのは
  quick-implement の規則どおり（結果は PR のコメントに残す）で、誤りではない。
