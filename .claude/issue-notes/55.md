# quick #55 起動時の設定エラーが Debug 形（`Error: "…"`）で表示される
- 入力: issue #55。モード: standalone。ブランチ main-55、着手前 SHA c2a561d
- ゲート: サイズ 9 行・1 ファイル（≤50・3）／呼び出し元 1（`Config::from_env` は `main` だけ。≤5・同一モジュール）／契約: 標準エラーの見た目だけで README の「stops the server at startup」は変わらない → 通過／新規性: `process::exit` は先行例 0 件だが標準ライブラリの慣用で issue の対応案どおり → 通過／事実: `TRINO_CATALOG_MAP=foo cargo run` で再現 → 通過／判断: issue が対応案を決めている → 通過
- 時系列: 着手 18:48 ／ red 18:49 ／ green 18:49 ／ コミット 18:50
- 検証: `cargo fmt --check`、`cargo clippy --all-targets --locked -- -D warnings`、`cargo test --locked` ok（lib 173 passed、新規 startup 1 passed）／ミューテーション: `main.rs` の修正を stash で戻す → `不正な環境変数で止まるときは理由を平文で標準エラーに出す` が Debug 形のまま落ちた
- 自己レビュー: 分岐 2 件（Err: テスト有り／Ok: 正常起動は cargo test では踏めないのでバイナリを起動して "listening" の行を確認）／エラーの出口: Err は `eprintln!` で平文 1 行、終了コード 1（既定のハンドラと同じ）／呼び出し元 1 箇所（main）のみ
- 打ち切り・escalate: 提案なし
- 予算の実績: Bash 約 9 本、所要 約 2 分（着手 18:48 → コミット 18:50）、サブエージェント 1 本（止めない独立レビュー）
- 出荷: main-55
