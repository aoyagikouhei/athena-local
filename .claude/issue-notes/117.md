# quick #117 tools/e2e の古い記述と重複ポートの整理
- 入力: issue #117。モード: standalone。ブランチ main-117、着手前 SHA 83d9464
- ゲート: 本番コード 0 行（足場 1 ファイルと README）／呼び出し元 1（versions.sh）／契約・新規性なし／事実: 手元の compose の trino に 2 回流して再現／判断: 4 項目のうち 3 項目は #128〜#130 で解消済み（下の検証）なので、残る probe.sh の再実行だけを直す
- 時系列: 着手 03:53 ／ red 03:54 ／ green 03:55 ／ コミット 03:57
- 検証: 修正前は 2 回目に c2・c3・setup_*_t_alter が error、修正後は 2 回とも既知の error（a7・b 節）だけ。versions.sh TRINO_TAGS=482 は PASS（FAIL 0）。cargo fmt --check・cargo clippy --all-targets --locked -- -D warnings・cargo test --locked ok（329 passed）／ミューテーション: ループから t_plain を外す → 2 回目の c3 が already exists で落ちた。解消済みの 3 項目: trino-probe の docker-compose.yml（8090）は #128 で消えてルートの compose.yml（ports 無し）に統合、sdk-retry/check.py のコメントは `$PYTHON`（既定 python3）に直っている、tools 配下に jq へファイルを引数で渡す箇所は無い（grep）うえ jq は toolbox の Debian 版
- 自己レビュー: 分岐の追加なし／DROP の失敗は setup_ ラベルなので versions.sh の setup_errors が「準備の失敗」として拾う／呼び出し元 versions.sh は版ごとに作り直すので影響なし
- 打ち切り・escalate: 提案なし
- 予算の実績: Bash 約 12 本（うち 1 本は run.sh の mkdir 漏れの再実行）、所要 約 4 分（待ち時間を除く）、サブエージェント 0 本（足場だけの変更なので独立レビューは流さない）
- 出荷: main-117
