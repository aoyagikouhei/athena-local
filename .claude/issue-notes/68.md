# quick #68 EXPLAIN の Query Plan 列の Precision が本物は 371 で athena-local は 2147483647 になる
- 入力: issue #68。モード: standalone。ブランチ `main-68`、着手前 SHA 7a4c061
- ゲート: サイズ 0 行・0 ファイル（≤50・3。本番コードは変えていない。テスト 2 ファイル + README + CHANGELOG）／呼び出し元 0（変えた本番コード無し）／契約: 文書化された値を本物に合わせる説明で、README・CHANGELOG を同じコミットで直す／新規性: 新ファイル・新パターン無し／事実: **外れた**（371 の由来が未実測。過去 2 ラウンドとも同じ `EXPLAIN SELECT 1`）／判断: 新しい既定値無し → 「事実」だけ外れ、はしごで実測工程を足して続行
- 時系列: 着手 21:36 ／ 実測工程 21:39〜21:44（新規ラウンド 0 回。過去データの読み直しと手元の Trino の確認で決着）／ red 21:45 ／ green 21:46 ／ コミット 21:48
- 検証: `cargo fmt --check` ok／`cargo clippy --all-targets --locked -- -D warnings` ok／`cargo test --locked` ok（lib 174 passed、select 9 passed、results 13 passed、全バイナリ 0 failed）／ミューテーション: `src/convert/athena_type.rs` の varchar の腕を `argument(0).unwrap_or(UNBOUNDED_VARCHAR)` から `UNBOUNDED_VARCHAR` に変える → `explain_の結果には_select_と同じく列名行が入る`（GetQueryResults の Precision）と `explain_の結果は_txt_の先頭に列名行を入れて書く`（`.txt.metadata` の field 7 の varint `f3 02`）の両方が落ちた／足した層（実測）: 過去 2 ラウンドの `explain.results.json` を読み直すと、非空 11 行の文字数 359 + 行間の改行 10 + 末尾の `\n\n` 2 = 371 で、Trino のプラン全文の形（末尾 `\n\n`）と一致。手元の Trino 482 は `EXPLAIN` の列を `varchar(400)`（typeSignature の arguments に 400、非 ASCII を含む文では文字数）と型付けし、athena-local 経由の GetQueryResults も Precision 400 を返した（上限無し varchar の `CAST('abc' AS varchar)` は 2147483647 のまま）。つまり本物の 371 はエンジンが `varchar(<プランの文字数>)` と型付けした結果で、athena-local は既に同じ仕組みで通している。issue の「2147483647 になる」はテストの偽 Trino（typeSignature 無し）の応答から出た誤り。新規の実測依頼は出さず、委譲していたスクリプト作成は止めた
- 自己レビュー: 分岐 0 件（本番コードの変更無し。テストの fixture に本物と同じ `varchar(371)` の typeSignature を入れ、GetQueryResults と `.txt.metadata` の両経路で Precision を固定）／エラーの出口: 新規なし／呼び出し元: 無し
- 打ち切り・escalate: はしご（事実 → 実測工程）。実測は過去データの読み直し + 手元の Trino の確認で 0 ラウンド。範囲外の発見: 本物は EXPLAIN のプランを行ごとに `Rows` に分ける（15 行）が athena-local は Trino の 1 行を改行入りのまま返す件を #73 に起票
- 予算の実績: Bash 約 26 本（うち実測 8 本）、所要 約 14 分（待ち時間を除く）、サブエージェント 1 本（実測スクリプト作成。過去データと手元の Trino で決着したため途中で停止。本番コードを変えていないので止めない独立レビューは流さない）
- 出荷: standalone、ブランチ `main-68`
