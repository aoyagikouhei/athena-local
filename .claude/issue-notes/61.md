# quick #61 tests/retention.rs が高負荷時に「保持期限を過ぎても消えない」で落ちる
- 入力: issue #61。モード: standalone。ブランチ main-61、着手前 SHA 5c67347
- ゲート: サイズ 0 行・0 ファイル（本番コードは触らない見込み。テストは tests/retention.rs 1 ファイル）（≤50・3）／呼び出し元 2（`wait_until_gone`、同一ファイル）（≤5・同一モジュール）／契約なし・新規性なし（新規ファイル無し）・判断は上限 10 秒の根拠を issue の対処案に置く → 外れた観点: 事実（手元で再現できない）
- 時系列: 着手 19:50 ／ red なし ／ green なし ／ コミット 19:56（ノートのみ）。再現の試み 19:51〜19:55
- 検証: 標準コマンドは未実行（コードに触っていない）／ミューテーション: なし／再現の試み: CPU 飽和（12 コアに `yes` 24 本）で retention バイナリ 8 回 0 失敗、`yes` 96 本で 4 本同時 × 6 回（24 回）0 失敗、別 target への `cargo build --release` と同時に `cargo test --locked` 3 回 0 失敗
- 自己レビュー: なし（コードに触っていない）
- 打ち切り・escalate: 提案あり（事実 → 再現 0/35）→ 選択: implement-issue。stash なし。issue コメント: https://github.com/aoyagikouhei/athena-local/issues/61#issuecomment-5775241023
- 予算の実績: Bash 約 12 本、所要 8 分（待ち時間を除く）、サブエージェント 0 本
- 出荷: main-61（このコミットはノートだけ。実装は implement-issue が積む）

## 引き継ぎ
- 判定箇所: `tests/retention.rs:17-28` の `wait_until_gone`（20 ms × 100 回 = 約 2 秒で諦める）。呼び出し元は同ファイルの 45 行目と 118 行目。同じ形の `tests/common/mod.rs:404` の `wait_for` も回数（10 ms × 200 回）で諦める
- 共有できる既存機構: `wait_for`（`tests/common/mod.rs:404`）。経過時間で諦める形に直すなら両方を揃えられる。`std::time::Instant` の先行例は tests 配下に無い
- 掃除の駆動: `src/store.rs:251` の `lock()` が毎回 `sweep(now())` を呼ぶ。`now()` は `SystemTime`（`src/store.rs:275`）。`completed_at` は `finish`（210 行）と `cancel`（245 行）で入る。QUEUED / RUNNING は捨てない
- 原因の仮説: issue のとおり待ちの上限が短い。ただし 100 回の待ちは実時間で 2 秒以上を保証するので、失敗するには保持期限 200 ms を差し引いた約 1.8 秒の間にバックグラウンドのクエリ（PREPARE → /v1/statement → finish）が終わっていない必要がある。`#[tokio::test]` は current_thread で、偽 Trino と本物の router も同じランタイムに同居する（`tests/common/mod.rs:458,588`）ため、CPU の逼迫よりも I/O やスケジューリングの停滞（WSL2 のメモリ圧など）が疑わしいが未確認
- 未実測・未確認: 落ちたときにクエリがどの状態（QUEUED / RUNNING / SUCCEEDED）で止まっていたか（panic の文言に最後の応答が無い）。issue の観測（docker compose + release ビルド + Maven 同時）でのメモリ圧の有無
- 再現手順: 手元では再現せず。試したのは、`yes` で CPU を飽和させて retention のテストバイナリを繰り返す方法と、`cargo build --release --target-dir <別ディレクトリ>` を裏で走らせながらの `cargo test --locked` 3 回
- stash: なし
