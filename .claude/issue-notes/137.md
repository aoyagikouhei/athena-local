# quick #137 GetWorkGroup の Configuration に EnableMinimumEncryptionConfiguration と QuerySchedulingType を返すか決める
- 入力: issue #137。モード: standalone。ブランチ main-137、着手前 SHA ed80feb
- ゲート: サイズ 5 行・2 ファイル（≤50・3）／呼び出し元 1（WorkGroupConfiguration の組み立ては work_group.rs だけ）／契約: 応答にキーを 1 つ足す（本物の実測値に合わせる修正。SDK のモデルにあるキーで、クライアントは値を読むだけ）→ docs・CHANGELOG を同じコミットで直す条件で通過／新規性・事実（2026-09-23 実測済み）通過／判断: QuerySchedulingType は Category の先例（SDK のモデルに無いキーは返さない。decisions.md）に揃えて返さない
- 時系列: 着手 03:47 ／ red 03:49 ／ green 03:50 ／ コミット 03:49
- 検証: cargo fmt --check・cargo clippy --all-targets --locked -- -D warnings・cargo test --locked ok（329 passed）／ミューテーション: 値を true にする → primary_の設定を返す が落ちた
- 自己レビュー: 分岐の追加なし／エラーの出口なし／呼び出し元 1 箇所（get_work_group）。ListWorkGroups は Configuration を持たないので影響なし
- 打ち切り・escalate: 提案なし
- 予算の実績: Bash 約 9 本、所要 約 2 分、サブエージェント 1 本（止めない独立レビュー）
- 出荷: main-137
