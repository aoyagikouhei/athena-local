# quick #181 SHOW CREATE TABLE / SHOW CREATE VIEW の GetQueryResults が改行入りの 1 行で返り、本物の行分けと違う
- 入力: issue #181（#163 の足場で見つけた範囲外の差分）。モード: standalone。ブランチ main-181、着手前 SHA 2241b02（過去ノートの削除コミット c05b4b5 の後から作業）
- ゲート: サイズ 26 行・2 ファイル（≤50・3）／呼び出し元 1（execution.rs の run。split_explain_rows と同じ置き場）／契約: GetQueryResults の行の分け方が変わるが、本物の実測に合わせる修正で `.txt` のバイト列は不変、docs/api.md と CHANGELOG を同じコミットで直す → 通過／新規性: split_explain_rows と同じ形・新ファイル無し／事実: 本物の生データ 6 本（Hive 12・19・22 行、Iceberg 9・7 行、ビュー 2 行）で「GetQueryResults の行数 = 本体の行数、末尾の空行なし」を確認済み／判断: decisions.md の #173 の項（fixed_column は列だけ）とは両立し、行分けの 1 文を足した → 通過
- 時系列: 着手 05:45 ／ red 05:47 ／ green 05:48 ／ コミット 05:50
- 検証: `cargo fmt --check`・`cargo clippy --all-targets --locked -- -D warnings`・`cargo test --locked` ok（398 passed）／ミューテーション: execution.rs の呼び出しを消す → 新テスト 2 本 FAIL、腕から SHOW_CREATE_VIEW を外す → VIEW のテストだけ FAIL／実機: `tools/dev.sh bash tools/measure/jdbc-show-metadata.sh`（Trino 482）で 9 ケースすべて auto = S3 = GetQueryResults（#163 の既知の差分の INFO を外した）
- 自己レビュー: 分岐 3 件（対象外の文は素通し: 既存テストで踏む／String は分ける: 新テスト／String 以外は素通し: 未踏。Trino の SHOW CREATE は varchar なので到達しない、PR に記載）／エラーの出口: 新しいエラー無し／呼び出し元 1 箇所: Iceberg の DESCRIBE のパーティション行は別の問い合わせ（completion::iceberg_partition_specs）の 1 値を読むので影響なし、`.txt` は to_text が `\n` でつなぐので不変（テストで固定）
- 打ち切り・escalate: 提案なし
- 予算の実績: Bash 約 17 本、所要 約 5 分（足場の実行待ちを含む）、サブエージェント 1 本（止めない独立レビュー）
- 出荷: main-181
