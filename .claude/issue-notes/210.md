# quick #210 リテラルだけの SELECT の .metadata の先頭を QueryExecutionId にする
- 入力: issue #210。モード: standalone。ブランチ main-210、着手前 SHA 8cc92c6
- ゲート: サイズ 本番 23+/5- 行（テストの追加行を含む）・2 ファイル（≤50・3）／呼び出し元 1（`metadata_query_id` は `write_result` の 1 箇所、同一モジュール）／契約: 文書化された `.metadata` の値を本物に合わせる修正（docs 同梱）で通過・新規性: 無し・事実: 3 回の実測（#70/#76、#200、#205）あり・判断: 無し → 通過
- 時系列: 着手 12:18 ／ red 12:18 ／ green 12:19 ／ コミット 12:21
- 検証: `tools/dev.sh cargo fmt --check`・`tools/dev.sh cargo clippy --all-targets --locked -- -D warnings`・`tools/dev.sh cargo test` ok（503 passed）／ミューテーション: `|| is_literal_only_select(query)` を `|| false` → ユニット・結合の両方が落ちた
- 自己レビュー: 分岐 1 件（`||` の両側をユニットテストが踏む）／エラーの出口: 新設なし／呼び出し元 1 箇所: `format_override` の `_` 腕だけで、リテラルだけの SELECT に形式の上書きは掛からない
- 打ち切り・escalate: 提案なし
- 予算の実績: Bash 約 17 本、所要 3 分（date の値による）、サブエージェント 1 本（止めない独立レビュー）
- 出荷: main-210
