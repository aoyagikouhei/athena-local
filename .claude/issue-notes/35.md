# quick #35 INSERT の OutputLocation `<id>` を再実測する
- 入力: issue #35。モード: standalone。ブランチ `main-35`、着手前 SHA 0399dab
- ゲート: サイズ 1 ファイル・本番 27 行追加 8 行削除（ロジックの変更は 0。≤50・3）／呼び出し元 3（`ResultFile::Manifest` は `results.rs` 内の `of`・`path`・`content_type` だけ）／契約は README に書いた値を実測に合わせる修正なので同じコミットで README・CHANGELOG を直す条件で通過、新規性なし、判断は issue が「測ってから決める」と固定済み → **事実が外れた**（本物の挙動が未実測で、手元のテストでは再現できない）
- 時系列: 着手 18:12 ／ 実測スクリプト 18:13-18:26（偽 aws でのドライラン 3 回を含む）／ 実測（ユーザー実行）18:27-18:35 ／ red 18:37 ／ green 18:37 ／ コミット 18:43
- 検証: `cargo fmt --check` ok、`cargo clippy --all-targets --locked -- -D warnings` ok、`cargo test --locked` ok（222 passed / 0 failed）／ミューテーション: `ResultFile::of` の `"INSERT" => Self::Manifest` を `Self::Csv` にする → `insert_はテーブルの形式でも更新件数でも_拡張子なしの_id_のまま` が落ちた（left: Csv, right: Manifest）／足した層: 実測 1 ラウンド（11 項目すべて測れた）
- 自己レビュー: 実測が現状のコードを追認したので新しい分岐は無く、差分はコメント・テスト・文書だけ。固定したのは `ResultFile::of` の INSERT の腕 1 つで、ミューテーションで効くことを確かめた／新しいエラー戻り値なし／呼び出し元 3 箇所はいずれも変えていない（`Manifest` の値も Content-Type も据え置き）
- 打ち切り・escalate: はしご（事実 → 実測 1 ラウンド）。escalate なし。stash なし。Bash は上限の 30 本に達したが、残っていたのが出荷作業だけだったので止めずに完走した
- 予算の実績: Bash 約 32 本、所要 20 分（実測の待ち時間を除く）、コミット 3（実測スクリプト 1 + 実装 1 + 独立レビューの修正 1。実測のコミットは docs 単独に数えない）、サブエージェント 0 本（止めない独立レビューを PR の後に 1 本流す）
- 出荷: ブランチ `main-35`（PR 番号は `gh pr list --head main-35` で引ける）

## 実測の結果（2026-09-20）

`35-measure-insert-location.sh` を本物の Athena（engine version 3）で 1 ラウンド。11 項目すべて測れた。
`table_format` は `SHOW CREATE TABLE` による裏取り。**争点だった INSERT の `<id>` は再現した。**

| | 文 | table_format | 本物の OutputLocation | 本体 | `.metadata` | マニフェスト |
|---|---|---|---|---|---|---|
| a | `INSERT INTO <hive> VALUES` | hive | **`<id>`** | 無し | 75B | **`<id>-manifest.csv` 155B** |
| b | `INSERT INTO <iceberg> VALUES` | iceberg | **`<id>`** | 無し | 75B | 無し |
| c | 0 行の `INSERT`（更新件数 0） | hive | **`<id>`** | 無し | 75B | 無し |
| d | 型が合わない `INSERT`（FAILED） | hive | `<id>` | 無し | 無し | 無し |
| e | `UPDATE <iceberg>` | iceberg | `<id>.csv` | 無し | 75B | 無し |
| f | `DELETE FROM <iceberg>` | iceberg | `<id>.csv` | 無し | 75B | 無し |
| g | `MERGE INTO <iceberg>` | iceberg | `<id>.csv` | 無し | 74B | 無し |
| h | `SELECT 1 AS n`（対照） | - | `<id>.csv` | 8B | 73B | 無し |
| i | CTAS（対照） | hive | `tables/<id>` | 無し | 81B | `tables/<id>-manifest.csv` |
| prep-i | Iceberg の CTAS（対照） | iceberg | `tables/<id>` | 無し | 81B | 無し |
| probe | `SHOW TABLES`（対照） | - | `<id>.txt` | 228B | 312B | 無し |

**INSERT はテーブルの形式でも更新件数でも `<id>` のまま。** 同じラウンドの対照（SELECT・CTAS・SHOW）も
すべて README に書いた値どおりで、#26 のときのような食い違いは出なかった。2026-09-17 の 1 ラウンドで
採られた値のうち、CTAS は #26 で覆ったが INSERT は覆らなかったことになる。

副次的に分かったこと（README の Caveats に反映済み）:

- **マニフェストが置かれるのは Hive のテーブルに行を書いた文だけ**。Iceberg の INSERT・
  UPDATE / DELETE / MERGE・0 行の INSERT・失敗した INSERT には置かれない。失敗した INSERT の
  `StateChangeReason` はマニフェストのパスに言及するが、そのキーは実在しなかった。
- **MERGE の `<id>.csv` を初めて実測した**（それまでは UPDATE / DELETE からの類推だった）。
- 失敗した INSERT が本体も `.metadata` も置かないことを、2026-09-17 に続いて再確認した。
- `SELECT 1 AS n` の本体と `.metadata` は `binary/octet-stream` で、5.md の「`SELECT 1` だけ
  binary 系」という観測に `AS` 付きも含まれることが分かった。

## 範囲外の発見

- **`DROP TABLE` の結果ファイルがテーブルの形式で違う。** 後始末の 3 本で、Hive のテーブルは
  本体 0 バイト・`.metadata` 無し、Iceberg のテーブルは本体 1 バイト・`.metadata` 41 バイトだった。
  5.md が 2026-09-17 に記録した「1B / 41B」は Iceberg のときの値だったことになる。
  → #39 に起票（https://github.com/aoyagikouhei/athena-local/issues/39）

## 未実測のまま残るもの

- `CREATE OR REPLACE TABLE ... AS`（Trino だけの構文）のファイル名は引き続き未実測
- カタログの既定など、`WITH` 句以外からテーブルの形式が決まる場合（Athena では作れなかった）
- Iceberg のテーブルへの `INSERT` が 0 行だったとき（c は Hive でだけ測った）
