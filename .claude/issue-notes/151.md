# issue #151: SHOW CREATE VIEW と Iceberg への SHOW CREATE TABLE の Content-Type と .metadata を本物に合わせる

## 基本情報

- issue: #151
- ベースブランチ: main
- 作業ブランチ: main-151
- 着手前 SHA: c1548ee63dc11a8bac93332f9a1eefe11c799fb4（レビューの `base`）

## 事前に確認した事実

Explore 1（コード経路。opus）の番号付き返答 1〜10 から転記。落とした番号: 8（git 履歴。判断に使わない）。

- **Content-Type**: `src/content_type.rs:45-52` `text_content_type` は `(word(0), word(1))` の `("SHOW","CREATE")` で APPLICATION にするので、`SHOW CREATE VIEW v` も今は application。`catalog::words`（`src/catalog.rs:131-153`）は空白・コメント区切りで大文字化し、`SHOW CREATE VIEW v` → `["SHOW","CREATE","VIEW","V"]`。`(` は取り除かない（`catalog.rs:493` のテスト）。
- **`.metadata` の Content-Type**: `ResultLocation::metadata()`（`src/results.rs:132-139`）が本体の `content_type` を引き継ぐ。`write_result`（`src/operation/result_output.rs:44`）の上書きは DROP TABLE × Iceberg と ALTER ADD COLUMNS × Hive だけ。→ `content_type::of` を変えるだけで本体と `.metadata` の両方が binary になる。
- **`.metadata` の先頭 ID**: `metadata_query_id`（`result_output.rs:162-177`）も `(word(0), word(1))` の `("SHOW","CREATE")` で QueryExecutionId。語の分割は `classification::words`（`(` を取り除いて空語を落とす。`catalog::words` と別物）。`SHOW TABLES` などは Trino のエンジン ID（`tests/metadata.rs:132-170` で固定）。
- **`.metadata` を書く条件**: `result_output.rs:79` `!outcome.columns.is_empty() || engine_ddl.is_some()`。SHOW CREATE VIEW は Trino が列を返すので書く。
- **SubstatementType**: `src/operation/classification.rs:48-56` に `("CREATE","TABLE") => "SHOW_CREATE_TABLE"` はあるが `SHOW_CREATE_VIEW` は無く None（項目ごと省く）。本物は `SHOW_CREATE_VIEW`（2026-09-24 実測。measurements/result-files.md:466）。**issue 本文に無い差分。**
- **UpdateCount**: `src/operation/query_execution.rs:218-225` が DDL 以外を `Some(0)` にするので、UTILITY の SHOW CREATE VIEW は今でも 0。変更不要。
- **既存テスト**（SHOW CREATE VIEW を直接扱うものは無い）:
  - `src/content_type.rs:223-250` `txt_は_show_と_ddl_が_binary_で_describe_explain_show_create_が_application`（245-246 行が `SHOW CREATE TABLE t` / 小文字を APPLICATION に固定）
  - `tests/results.rs:669-710` `describe_と_explain_は_txt_と_metadata_を_application_で書く`（678 行に `SHOW CREATE TABLE users`。本体と `.metadata` の Content-Type を固定）
  - `tests/metadata.rs:226-266` `show_create_table_の_metadata_も先頭が実行_id_になる`（`execution_id_field(&id)` で先頭を固定）。`tests/metadata.rs:132-170` `show_は_txt_の隣に_txt_metadata_を置く`（SHOW TABLES: binary と `engine_id_field()`）
  - `src/operation/classification.rs:207,265` SHOW_CREATE_TABLE の分類（265 行のコメントが「`metadata_query_id` の `("SHOW","CREATE")` も同じ `words()` を使うのでここで一緒に守る」）
  - `tests/syntax.rs:126-147` ブロックコメント付き SHOW CREATE TABLE はそのまま通る
- **Harness**: `S3Put { key, body: Vec<u8>, content_type: Option<String>, .. }`（`tests/common/mod.rs:64-73`）、`s3_puts()`（324 行）。`tests/metadata.rs:39-57` に `hex_of`／`hex`／`engine_id_field()`／`execution_id_field(id)`。
- **対称性の制約**: `src/content_type.rs:35-44` の doc「`("DESCRIBE"|"DESC", _) | ("SHOW","CREATE")` の組は `metadata_query_id` と同じで、片方を変えたら両方を変える」と `docs/dev/decisions.md:95`。`metadata_query_id` 側の doc には無い。`("SHOW","CREATE")` を両方とも 3 語目 `TABLE` まで見る形に絞れば組は同じまま保てる。
- **docs の直す箇所**: `docs/result-files.md:82-83`（表）、`:102-103`（not measured の文）、`:201-206`（先頭 ID の説明）; `docs/caveats.md:190-220`（不透明な `.metadata` の SHOW の列挙。312 文字の 4 文に加える）、`:202-203`（`SHOW CREATE TABLE` is not affected either）; `docs/dev/decisions.md:95,101,102`; `docs/dev/unmeasured.md:9,15`; `docs/api.md:21`（UTILITY の一覧）、`:101`; `CHANGELOGS.md` `[Unreleased]` の `### Changed`。
- **前提を述べているコメント**: `src/content_type.rs:40-41`「`SHOW CREATE VIEW` は未測定で…揃えている」、`docs/result-files.md:102-103`。
- **design-checklist で効く行**: 10 行（2 語目以降を見る分岐の grep）、17 行（位置固定の手前のトリビア。`SHOW CREATE /* c */ VIEW v` は本物で成功。statements.md:92）。

Explore 2（過去の実測の生データと足場。opus）の番号付き返答 1〜7 から転記。落とした番号: 1（run の一覧。判断に使わない）、5（他の実測スクリプトの一覧。判断に使わない）。

- **Hive のテーブルへの `SHOW CREATE TABLE` は、本体 Content-Type と `.metadata` の中身が両方保存された run が 4 つあり、全部 application ／ 88B の素の protobuf（先頭 `0a 24 <QueryExecutionId>`）／ `UpdateCount` null**: `~/athena-comment-measurements/run-20260918-090622/a-line-show-create.*`（#17）、`~/athena-keyword-comment-measurements/run-20260922-180816/d-show-create-plain.*`（#52）、`~/athena-content-type-measurements/run-20260923-043027/c10-*` と `run-20260923-065415/c10-*`（#70・#76）。対象は本体の DDL（`CREATE EXTERNAL TABLE ... ROW FORMAT SERDE ...ParquetHiveSerDe`）から Hive と裏付けられる。#1（`~/athena-txt-measurements/run-20260916-090202`）も本体 692B・`.metadata` 88B の素の protobuf・`UpdateCount` null（Content-Type は保存されていない）。
- **#146 の Iceberg（`~/athena-unmeasured-batch-measurements/run-20260924-004554/r4/`）**: Iceberg の CTAS で作ったテーブル。本体 268B binary、`.metadata` 332B binary の不透明な形式（base64）、`UpdateCount` 0。WorkGroup primary、engine 3、Catalog awsdatacatalog で #70 と同条件。
- **日付で変わったのではない根拠**: 同じ 2026-09-24 の real run の x1（Hive 外部テーブル）で `DESCRIBE` は application／152B／`UpdateCount` null、`SHOW COLUMNS` は binary／312B／0 で、#76（2026-09-23）と同じ。→ Hive の経路は 09-23 → 09-24 で変わっていない。テーブルの形式で割れている読みが最も有力。ただし**同じ run に Hive と Iceberg の `SHOW CREATE TABLE` の Content-Type を並べたデータは無い**（本体 DDL だけ保存した run は複数ある: insert 20260920、iceberg-zero-insert 20260923 など）。
- **既存の差分（issue 本文に無い）**: 本物の Hive `SHOW CREATE TABLE` と `DESCRIBE` は `UpdateCount` null だが、athena-local の `update_count`（`src/operation/query_execution.rs:218-225`）は DDL 以外を 0 にするので 0 を返す。
- **足場**: `tools/measure/unmeasured-batch/run.sh`（`ALL_ITEMS` は 222 行、項目は `items-result-files.sh` の `item_<id>()`）。`run_stmt`（`lib-aws.sh:300-358`）が `.sql`／`.execution.json`／`results-N.json`／本体と `.metadata` の head-object（`[ContentLength,ContentType]`）と中身（`.body.bytes`／`.metadata.bytes`／`.od.txt`）を保存し summary に 1 行書く。後始末は `record_created` → `best_effort_drop`（`DROP <kind> IF EXISTS`）→ `finish_cleanup`。Hive の外部テーブルの LOCATION は `record_cleanup_hint`。r4 の全文は `items-result-files.sh:84-112`（Iceberg CTAS を対照にしている。Hive の CTAS は DROP で S3 にデータが残るので避けた）。
- **実測の動かし方**: `docs/dev/development.md:44-49`。`tools/dev.sh env TARGET=real OUTPUT=... DB=... WORKGROUP2=... bash tools/measure/unmeasured-batch/run.sh`。資格情報はホストのシェルの `AWS_*`。出力は `~/athena-unmeasured-batch-measurements/run-<日時>/`。

## 実測の手順

- スクリプト: `tools/measure/unmeasured-batch/`（既存の足場）に項目 `r5` を足した（`items-result-files.sh` の `item_r5`、`run.sh` の `ALL_ITEMS`）。r4（Iceberg の CTAS + ビュー。#146 と同じ）と一緒に流し、同じラウンドに Hive 外部テーブル・Hive CTAS・Iceberg の素の CREATE TABLE・Iceberg CTAS の 4 組の `SHOW CREATE TABLE` を並べる。
- 実行例: `tools/dev.sh env TARGET=real REGION=ap-northeast-1 OUTPUT=s3://<bucket>/<prefix>/ DB=<db> WORKGROUP2=<wg> ONLY=r4,r5 bash tools/measure/unmeasured-batch/run.sh`
- 必須の環境変数: OUTPUT・DB・WORKGROUP2（run.sh が real で要求する。r4・r5 は WORKGROUP2 を使わない）。資格情報はホストのシェルの `AWS_*`。
- 出力先: `~/athena-unmeasured-batch-measurements/run-<日時>/{r4,r5}/` と `summary.txt`。
- 課金と DDL: 本物への呼び出しは約 16 文（CREATE 5・SHOW CREATE 5・DROP 5・SHOW TABLES 2）。スキャンは `SELECT 1` の CTAS 2 本だけ。DDL あり（作ったものは同じ run で DROP）。Hive の外部テーブルの LOCATION と Hive CTAS の `tables/<id>/` は S3 に空ディレクトリ／小さな parquet が残る（cleanup-hints.txt に記録）。
- 受け入れ判定: TARGET=local のドライラン（ONLY=r4,r5）で、CREATE EXTERNAL TABLE の 1 本だけが期待どおり失敗し、残りが SUCCEEDED になること。

## 実測の結果

run: `~/athena-unmeasured-batch-measurements/run-20260924-062232/`（TARGET=real、ONLY=r4,r5。2026-09-24 06:22 UTC。18 文、全部 SUCCEEDED、後始末の DROP 5 本も SUCCEEDED）。

| 文 | 対象 | 作り方 | SubstatementType | 本体 | `.metadata` | Content-Type（本体／`.metadata`） | `UpdateCount` |
|---|---|---|---|---|---|---|---|
| SHOW CREATE VIEW | ビュー | `CREATE VIEW ... AS SELECT 1 AS n` | SHOW_CREATE_VIEW | 69B | 312B 不透明（base64） | binary／binary | 0 |
| SHOW CREATE TABLE | Hive 外部テーブル | `CREATE EXTERNAL TABLE (n int) LOCATION` | SHOW_CREATE_TABLE | 447B（`ROW FORMAT SERDE`／`STORED AS`） | 88B 素の protobuf（先頭 `0a 24 <QueryExecutionId>`） | application／application | null |
| SHOW CREATE TABLE | Hive CTAS | `CREATE TABLE ... AS SELECT 1 AS n` | SHOW_CREATE_TABLE | 710B（`ROW FORMAT SERDE`／`STORED AS`） | 88B 素の protobuf | application／application | null |
| SHOW CREATE TABLE | Iceberg 素の CREATE | `CREATE TABLE (n int) LOCATION ... TBLPROPERTIES ('table_type'='ICEBERG')` | SHOW_CREATE_TABLE | 233B（`'table_type'='iceberg'`） | 332B 不透明 | binary／binary | 0 |
| SHOW CREATE TABLE | Iceberg CTAS | `CREATE TABLE ... WITH (table_type='ICEBERG', ...) AS SELECT 1 AS n` | SHOW_CREATE_TABLE | 268B | 332B 不透明 | binary／binary | 0 |

決め手: 同じラウンドで、作り方（素の CREATE／CTAS）を変えても値は変わらず、形式（Hive／Iceberg）で割れた。#1・#17・#52・#70・#76 の Hive の記録（application／88B／null）と #146 の Iceberg の記録（binary／332B／0）はどちらも正しく、食い違いの原因はテーブルの形式。

## 実測の経緯

- 2026-09-24 15:14: 過去の生データの読み直し（Explore 2）。Hive の SHOW CREATE TABLE は 4 run（#17・#52・#70・#76）で application／素の protobuf 88B／UpdateCount null。同一ラウンドの Hive/Iceberg の対照は無し。
- 2026-09-24 15:19: r5 の TARGET=local ドライランが期待どおり。本物での実測（ONLY=r4,r5）をユーザーに依頼。
- 2026-09-24 15:22〜15:26（JST）: ユーザーが本物で実行（run-20260924-062232）。全項目測れた。1 ラウンドで決着。

## 設計判断

| # | 判断 | 理由 | ユーザーに聞くか |
|---|---|---|---|
| D1 | `.metadata` の先頭のクエリ ID は、本物が不透明な形式を置く文（`SHOW CREATE VIEW`、Iceberg の `SHOW CREATE TABLE`）ではエンジン（Trino）ID にする | 既存方針 `docs/result-files.md:204-206`（不透明な SHOW は EXPLAIN に倣ってエンジン ID）。本物の先頭 ID は観測できない（不透明） | 確認だけ |
| D2 | `text_content_type` と `metadata_query_id` の対を 1 つの述語（`content_type::carries_execution_id`: `DESCRIBE`／`DESC`、または `SHOW CREATE TABLE` の 3 語完全一致）に集約する | 語の分割関数が 2 つで違い、非公開なので同じ表のテストで一緒に守れない。`decisions.md:95` の「片方を変えたら両方」を構造で守る | 確認だけ |
| D3 | 3 語目は `TABLE` の完全一致。`SHOW CREATE VIEW` を含むそれ以外の `SHOW CREATE ...` は `.txt` の既定（binary／エンジン ID）に落ちる | `.txt` の既定は SHOW 系＝binary（`SHOW SESSION`／`STATS` の前例 `result-files.md:98-101`）。`SHOW CREATE SCHEMA` 等は Athena の構文に無く未実測なので、既定に倒す。`unmeasured.md` に載せる | 確認だけ |
| D4 | `SubstatementType` に `SHOW_CREATE_VIEW` を足す（実測済み。2026-09-24） | issue 本文に無い差分だが同じ文で 1 腕。実測済みの値を省き続ける理由が無い | **聞く**（範囲の拡張） |
| D5 | フェーズ 2（Iceberg の `SHOW CREATE TABLE`）は既存の形式判定機構（`target_statement` → `parse_target_table` → `probe_format` → 組み合わせ）を再利用し、`EngineDdl` に腕 `ShowCreateTableIceberg` を足す。上書きの Content-Type を enum のメソッドにし、`write_result` の先頭 ID を 3 腕の match にする | Plan 案 A。別型（案 B）は排他な Option が 2 つになる。SQL 層に形式を渡す案 C は `decisions.md:90,92` を崩す | 確認だけ |
| D6 | `EngineDdl` の改名は**しない**。doc を「テーブルの形式で本体・`.metadata` の書き方を上書きする文」に直す | 28 か所の改名は挙動を変えない差分としては大きく、レビュー密度を下げる。型名の意味のずれは doc で補う（分割・改名は別 issue で起票） | 確認だけ |
| D7 | フェーズ 2 に進む（実測で「テーブルの形式で割れる」と決着。作り方では割れない） | 実測の結果（2026-09-24 run-062232） | 決着済み |
| D8 | 実装の分担: フェーズ 1 は自分（本番コード約 30 行）、フェーズ 2 は委譲（opus。本番コード約 60〜80 行） | 小規模経路の規則と、大きい実作業は Opus に任せる分担 | 確認だけ |

## 計画

Plan（opus）の番号付き下書き 0〜9 から転記。落とした番号: 3 の案 A の「改名コミット」（D6 で不採用）、8-3 の「VIEW だけを例外にする」案（D3 で不採用）。

### フェーズ 1: `SHOW CREATE VIEW`（実測に依存しない）

**ファイル構成**

| ファイル | 責務 | 現在 → 見込み |
|---|---|---|
| `src/content_type.rs` | Content-Type の判定と、述語 `carries_execution_id`（新設。`pub(crate)`） | 273 → 約 295 |
| `src/operation/result_output.rs` | `metadata_query_id` は述語を呼ぶだけにする。インラインのユニットテストを新設 | 177 → 約 205 |
| `src/operation/classification.rs` | `("CREATE","VIEW") => "SHOW_CREATE_VIEW"`（D4） | 502 → 約 508（400 行超は既存。分割は範囲外で起票） |
| `tests/show_create.rs`（新規） | `SHOW CREATE VIEW`／`SHOW CREATE TABLE` × 形式の結合テスト | 約 90（フェーズ 2 後に約 190） |

**変更内容**
- P1-1: `text_content_type`（`content_type.rs:45-52`）の `("SHOW","CREATE")` を述語に置き換える。`SHOW CREATE VIEW` は `_ => BINARY`。`.metadata` は `ResultLocation::metadata()` が本体の値を引き継ぐので両方 binary になる。定数の doc（:11-16）と関数の doc（:35-44）を直す。
- P1-2: `metadata_query_id`（`result_output.rs:162-177`）も述語を呼ぶ。`SHOW CREATE VIEW` は `engine_id.unwrap_or(execution_id)`。
- P1-3: 述語 `carries_execution_id(query) -> bool` を `content_type.rs` に置き、`catalog::words` で `("DESCRIBE"|"DESC", _, _) | ("SHOW","CREATE","TABLE")`。語の分割関数の違い（`classification::words` は先頭の `(` を落とす）は到達しない（`(DESCRIBE t)`／`(SHOW CREATE TABLE t)` は Trino の文法で括弧に入れられるのは query だけなので PREPARE の構文チェックで弾かれる。計画レビュー 1-3）。`src/operation/classification.rs:263-265` のコメント（「`metadata_query_id` も同じ `words()` を使うのでここで一緒に守る」）は P1 の後に誤りになるので書き換える（計画レビュー 1-3）。
- P1-4: `classification.rs:48-56` に `("CREATE","VIEW") => "SHOW_CREATE_VIEW"`。呼ぶ側（`table_format.rs:44`、`query_execution.rs:85,164`、`execution.rs:317`、`result_output.rs:124`）に副作用なし。
- P1-5 docs: `docs/result-files.md:82`（binary の行に `SHOW CREATE VIEW`）、`:102-103`（not measured の文を実測の記述に）、`docs/caveats.md:190-196`（不透明な形式の列挙に `SHOW CREATE VIEW`、312 文字）、`docs/dev/decisions.md:95,102`、`CHANGELOGS.md` `[Unreleased]` `### Changed` に 1 項目。（`docs/api.md` の UTILITY の列挙・分類の例、`unmeasured.md:15`、`verify.sh:10` は計画レビュー 2-4・2-5 で外した。unmeasured と verify.sh は作業ツリーで済んでいる）

**分岐の全列挙（7-0）と固定するテスト**

| # | 分岐 | テスト |
|---|---|---|
| B1 | 述語: `DESCRIBE`／`DESC` → 真 | 既存 `content_type.rs:241-243`（述語の表テストは新設しない。計画レビュー 2-6） |
| B2 | 述語: `SHOW CREATE TABLE`（小文字・コメント入り含む）→ 真 | 既存 :245-246、`tests/metadata.rs:226-266` |
| B3 | `SHOW CREATE VIEW v`／`show create view v`／`SHOW CREATE /* c */ VIEW v`／`-- c\nSHOW CREATE VIEW v` → 偽 → binary | `content_type.rs` の `txt_は_…` の BINARY 側に 4 入力 |
| B4 | `EXPLAIN` は述語の外で application | 既存 :244 |
| B5 | `metadata_query_id` の偽側で `engine_id` が None なら実行 ID | 新設（`result_output.rs` インライン）`metadata_query_id_は_show_create_view_でエンジン_id_を選び無ければ実行_id` |
| B6 | `("CREATE","VIEW")` → `SHOW_CREATE_VIEW` | `classification.rs:187-` と `:401-` の表に 4 入力 |
| B7 | 結合: 本体・`.metadata` とも binary、`.metadata` の先頭 `0a1b`+`TRINO_QUERY_ID`、`SubstatementType` が `SHOW_CREATE_VIEW` | 新設 `tests/show_create.rs` `show_create_view_は本体も_metadata_も_binary_で先頭がエンジン_id` |

**red の予測**: B3 の 4 入力（今は application）、B5（今は実行 ID）、B6（今は None）、B7 が red。既存テストは 1 本も落ちない（`SHOW CREATE VIEW` を扱う既存テストは無い）。

**壊す候補**: 述語を 2 語に戻す → B3・B7 が落ちる ／ 述語から `DESC` を消す → :243 が落ちる ／ `metadata_query_id` の真偽を反転 → `tests/metadata.rs:132-170,226-266` が落ちる ／ `("CREATE","VIEW")` の腕を消す → B6 ／ `EXPLAIN` を落とす → :244。

### フェーズ 2: Iceberg の `SHOW CREATE TABLE`（実測で「形式で割れる」と出た場合）

**ファイル構成**

| ファイル | 責務 | 現在 → 見込み |
|---|---|---|
| `src/operation/table_format.rs` | `TargetStatement::ShowCreateTable`、`EngineDdl::ShowCreateTableIceberg`、`EngineDdl::content_type()`、組み合わせ | 320 → 約 350 |
| `src/operation/target_table.rs` | 動詞をキーワードの並び（`["SHOW","CREATE"]`）にする | 510 → 約 535（400 行超は既存。分割は範囲外で起票） |
| `src/operation/result_output.rs` | 上書きの適用（Content-Type は enum のメソッド、先頭 ID は 3 腕の match） | 約 205 → 約 215 |
| `src/operation/execution.rs` | コメントだけ（:232-234, :249） | 392 → 400 以内 |
| `tests/show_create.rs` | Iceberg／Hive の対照、問い合わせの有無 | 約 90 → 約 190 |

**変更内容**
- P2-1: `target_statement`（`table_format.rs:43-50`）に `Some("SHOW_CREATE_TABLE") => Some(TargetStatement::ShowCreateTable)`。
- P2-2: `verb()`（`target_table.rs:19-26`）を `keywords(statement) -> &[&str]` に変え、`table_name_start`（:70-78）はその並びを `catalog::skip_keyword` で順に読み飛ばしてから `TABLE` を読む（`skip_keyword` が手前のトリビアを読むのでコメント入りに耐える）。`IF EXISTS` の読み飛ばしは共通のまま。
- P2-3: `engine_ddl()` に `(ShowCreateTable, Iceberg) => Some(ShowCreateTableIceberg)`、`(ShowCreateTable, Hive) => None`。
- P2-4: `result_output.rs:44` を `match engine_ddl { Some(ShowCreateTableIceberg) => Some(BINARY), Some(_) => Some(ENGINE_DDL_CONTENT_TYPE), None => None }` にする（`EngineDdl` には `impl` が無く、腕ごとの振る舞いは使う場所で分岐する既存の書き方に揃える。計画レビュー 2-3。enum のメソッドは作らない）。先頭 ID は :83-92 を `match`（`AlterColumnsHive` → `(id, None, None)`、`ShowCreateTableIceberg` → `(outcome.id.as_deref().unwrap_or(id), update_type, update_count)`、それ以外 → `metadata_query_id`）。本体のガード :57 は `DropTableIceberg` だけのまま。`.metadata` を書く条件 :79 は不変。enum の doc（:25-26）を「テーブルの形式で本体・`.metadata` の書き方を上書きする文」に直す（D6）。
- P2-5 docs: `docs/ddl.md:1-3,10-17,45-47,59-80`（冒頭の対象文の列挙、形式の表に `SHOW CREATE TABLE` の行、「3 文だけ」→ 4 文、名前の出どころの文（:59-61）、**判定できないときの節（:68-79）に「`SHOW CREATE TABLE` は Hive の行（application・先頭は QueryExecutionId）に倒れ、Iceberg のテーブルでは本物とずれる」を 1 文足す**（計画レビュー 1-1・2-1 が独立に指摘）。見出しのアンカーは変えない）、`docs/result-files.md:83`（application の行を `SHOW CREATE TABLE` (Hive; see ddl.md)）、`docs/caveats.md:190-203`（列挙に Iceberg の `SHOW CREATE TABLE`（332 文字）、「not affected either」→ Hive だけ）、`docs/dev/decisions.md` に 1 行、`docs/dev/unmeasured.md:9` を消し `measurements/result-files.md` に同一ラウンドの記録（:473 の「未決」は残して決着を追記）、`CHANGELOGS.md` に 1 項目。

**分岐の全列挙（7-0）と固定するテスト**

| # | 分岐 | テスト |
|---|---|---|
| C1 | `SHOW_CREATE_TABLE` → `ShowCreateTable`。`SHOW CREATE VIEW`／`SHOW TABLES` は None | `table_format.rs` ユニット `target_statement_は_show_create_table_も対象にし_show_create_view_は対象外にする` |
| C2 | `keywords(ShowCreateTable)` = `SHOW CREATE`。`t`／`ns.t`／`cat.ns.t`／`"s3tablescatalog/my-bucket".ns.t`／`SHOW /* c */ CREATE TABLE t`／`SHOW CREATE -- c\nTABLE t`／`show create table T` | `target_table.rs` ユニット `parse_target_table_は_show_create_table_の名前も読む` |
| C3 | `(ShowCreateTable, Iceberg)` → Some、`(…, Hive)` → None | `table_format.rs` ユニットの表 |
| C4 | Content-Type の `match` の 3 腕 | C5（binary）と既存 `tests/table_format.rs:64-107,392-437`（application）。ユニットは新設しない（計画レビュー 2-3） |
| C5 | 先頭 ID の 3 腕（Iceberg → エンジン ID） | 結合 `show_create_table_は_iceberg_なら本体も_metadata_も_binary_で先頭がエンジン_id`（`.route(probe_sql(..), probe_response("iceberg"))`。書き方は `tests/table_format.rs:21-37,64-107`） |
| C6 | 本体は `to_text` のまま | 同じテストで Iceberg の本体が Hive の対照（C7）の本体と一致 |
| C7 | Hive → 今までどおり（application、`0a24`） | C3 のユニット（`(ShowCreateTable, Hive) → None`）と既存 `tests/metadata.rs:226-266`・`tests/results.rs:669-710`（問い合わせが None に倒れる経路）。問い合わせが本体より先なのは文の種類によらない共通コードで `tests/table_format.rs:84-90` が固定済み。結合テストは新設しない（計画レビュー 2-7） |
| C8 | 問い合わせ失敗・件数 0・知らない形式 → 今までどおり | 既存 `table_format.rs:233-242,297-318`、`tests/table_format.rs:228`（共通コード。新設しない） |
| C9 | `ResultsMode::None` なら問い合わせない | 既存 `tests/syntax.rs:126-147`（既定のカタログ・スキーマで名前の解析が成功するので、条件を外せば落ちる。計画レビュー 1 が確認）と `tests/table_format.rs:467` |
| C10 | `.metadata` を書く条件 | `SHOW CREATE` は列が必ずある。コメントで明示 |

**red の予測**: C1・C2・C3・C5 が red。C2・C3 は腕を足す前はコンパイルエラー（red の一種）。既存の `tests/metadata.rs:226-266`・`tests/results.rs:669-710` は問い合わせが 1 本増えるが、偽 Trino の既定応答（`select_response`）を `parse_probe_result` が解釈できず None に倒れるので green のまま。`tests/table_format.rs` の 17 本は green。

**壊す候補**: `target_statement` の腕を消す → C5 ／ 並びを `["SHOW"]` に → C2 ／ `(ShowCreateTable, Hive)` を Some に → C3 ／ `ShowCreateTableIceberg` を APPLICATION に → C5 ／ 先頭 ID を実行 ID に → C5 ／ :57 のガードを広げる → C6 ／ `ResultsMode::None` の条件を外す → C9。

### 依拠する既存機構の前提表

| 機構 | 暗黙の前提 | 破らないか | 保証元 |
|---|---|---|---|
| `ResultLocation::new` | Content-Type は StartQueryExecution 時点で SQL だけから決まる | P1 は SQL だけ。P2 は `put` の上書きで渡す | `src/results.rs:119-128` |
| `ResultLocation::metadata()` | `.metadata` は本体の Content-Type を引き継ぐ | P1 はこれに乗る | `results.rs:130-139` |
| `ResultWriter::put` | `Some` は location の値より優先 | P2 は BINARY の `Some` | `results.rs:250-273` |
| `write_result` の上書き | 同じ値を本体と `.metadata` に渡す | 案 A でも 1 値を 2 か所に | `result_output.rs:44,65,100,157` |
| 本体の改行 1 つ | `DropTableIceberg` だけ | ガード不変 | `result_output.rs:57` |
| `ResultLocation::failed` | 失敗の `.txt` は application 固定 | 触らない | `results.rs:141-152` |
| 語の分割 | `catalog::words` は `(` を残し `classification::words` は落とす | 述語が見る語では差が出ない（未保証だが現実には成立） | `catalog.rs:131-153`、`classification.rs:8-14` |
| `target_statement` | 分類が確定した文だけ対象 | `SHOW_CREATE_TABLE` は 3 語完全一致（checklist:15） | `table_format.rs:43-50` |
| `parse_target_table` | 動詞 + `TABLE` + `IF EXISTS`? + 修飾名 | 並びにしても各語は `skip_keyword` | `target_table.rs:64-78` |
| `probe_format` | 失敗・非対応・件数 0 は None（今までどおりに倒す） | SHOW CREATE TABLE でも application／実行 ID に倒れる | `table_format.rs:52-70,86-98` |
| 問い合わせの条件 | `ResultsMode::None` では送らない | 不変 | `execution.rs:254-258` |
| `UpdateCount` | DDL 以外は 0 | UTILITY のまま 0。本物の VIEW・Iceberg と一致 | `query_execution.rs:218-225` |
| 偽 Trino の ID | `id` 無しなら `TRINO_QUERY_ID` | 結合テストの `0a1b` の根拠 | `tests/common/mod.rs:39` |

### 直列化・正規化の境界
- 形式の問い合わせの SQL は既存の `probe_sql` が `quote_literal` で組む（`table_format.rs:75-82`）。新しい文字列の組み立ては無い。
- 正規化: `read_name_part`（`target_table.rs:98-114`）は無引用を小文字にする。無引用の大文字 `show create table T` は `t` として問い合わせ、Trino も小文字で保存するので件数 1 → Iceberg なら binary（本物の大文字小文字を区別しない挙動と一致）。C2 のユニットで `T` → `t` を固定する（計画レビュー 1-2 で期待値を訂正。結合テストは作らない）。引用符付きで大文字小文字が混ざった名前は件数 0 に倒れる（DROP TABLE と共通の既存の制約）。

### 一致台帳
1. `text_content_type` と `metadata_query_id` の対 → D2 で述語に集約し、対を無くす。
2. （外した。計画レビュー 2-6: `target_statement` は `substatement_type` を通すので分類と `keywords` は直列で、`keywords` が読めなければ C2・C5 が落ちる。C2 の入力表だけ残す）
3. `probe_sql` の文字列の写し（`table_format.rs:75-82`、`tests/table_format.rs:22-27`、`tests/metadata.rs:351,394`、`tools/e2e/trino-probe/probe.sh:122`、新設の `tests/show_create.rs` で 5 か所目）→ ずれればルートに当たらず C5 が落ちる（機械的に検出）。
4. docs: 正は Content-Type = `result-files.md` の表、形式による違い = `ddl.md` の表、不透明な形式 = `caveats.md` の 1 項目。ほかはリンク。

### 先行できる作業
- **実機検証の足場**（起動: 計画確定時 = 今）: `tools/e2e/result-content-type/verify.sh` を拡張。準備に `CREATE SCHEMA IF NOT EXISTS iceberg.default`、`CREATE TABLE iceberg.default.t_ct_ice_<RUN_ID> AS SELECT 1 AS n`、`CREATE VIEW hive.default.v_ct_<RUN_ID> AS SELECT 1 AS n`。検査に `.metadata` の先頭 2 バイト（`0a1b` = エンジン ID、`0a24` = UUID）。受け入れ判定: 変更前のビルドで流し、8a・8b・10・11 が FAIL、9 が PASS。
- ケース表（期待は変更後）:

| No | SQL | catalog | 本体／`.metadata` | 先頭 | 変更前 |
|---|---|---|---|---|---|
| 8a | `SHOW CREATE VIEW v_ct_…` | hive | binary／binary | `0a1b` | FAIL |
| 8b | `SHOW CREATE /* c */ VIEW v_ct_…` | hive | binary／binary | `0a1b` | FAIL |
| 9 | `SHOW CREATE TABLE t_ct_desc_…`（Hive、対照） | hive | application／application | `0a24` | PASS |
| 10 | `SHOW CREATE TABLE iceberg.default.t_ct_ice_…` | hive | binary／binary | `0a1b` | FAIL（フェーズ 2 後 PASS） |
| 11 | `SHOW CREATE TABLE t_ct_ice_…` | iceberg | binary／binary | `0a1b` | FAIL（同上） |

### 人間検証リスト
1. （済）同一ラウンドの Hive／Iceberg の `SHOW CREATE TABLE`（実測。run-062232）。
2. 実機（compose の trino/minio）でケース表 8a〜11（Step 9 で自分たちが流す）。
3. （任意）JDBC ドライバが binary の素の protobuf の `.metadata` を読めるか（前例 `caveats.md:204-211`。Content-Type は読み方に影響しない推測）。

### 未検証の前提
1. （検証済み）`SHOW CREATE TABLE` は形式で割れる。同一ラウンドの実測で決着。
2. 本物の不透明な `.metadata` の先頭 ID は観測できない。エンジン ID は類推（D1）。
3. `SHOW CREATE SCHEMA`／`MATERIALIZED VIEW`／`FUNCTION`（Trino にはある。Athena では未実測）は binary／エンジン ID に変わる（D3）。
4. `SHOW CREATE TABLE"t"`（空白なし）は `catalog::words` で 1 語 → 3 語目まで見ると binary。本物は未実測（`SubstatementType` は今も None）。
5. hive コネクタ経由の Iceberg テーブル（リダイレクト）は `hive` と判定される（DROP TABLE と共通の既存の制約）。
6. compose の hive／iceberg カタログで `CREATE VIEW` ができる（足場で確かめる）。
7. 偽 Trino の既定応答で `probe` が None に倒れるのは応答の形に依存（`select_response` の 1 列目が整数で `as_str()` が None。計画レビュー 1・2 が確認）。Hive の結合テストは C3 のユニットで代替（計画レビュー 2-7）。

## 一致台帳

（計画の「一致台帳」の節を正とする）

## 計画レビューの結果

Explore 2 本（契約・構造 ／ 再利用・削減。opus）。high 0、medium 2（同じ原因）、low 8。

| # | 指摘 | severity | 採否 | 反映先 |
|---|---|---|---|---|
| 1-1／2-1 | `docs/ddl.md:59-80`（名前の出どころ、判定できないときの「空ファイル・`.metadata` なし」）が SHOW CREATE TABLE では誤りになるのに P2-5 に無い | medium | 採用 | P2-5 |
| 1-2 | `show create table T` は小文字化されて件数 1 → Iceberg なら binary。計画の「件数 0 に倒れる」は逆 | low | 採用 | 正規化の境界、C2 |
| 1-3 | `classification.rs:263-265` のコメントが P1 後に誤りになる。`(` の差は到達しない | low | 採用 | P1-3、前提表 |
| 2-2 | `tests/show_create.rs` を新設せず `tests/table_format.rs`／`tests/metadata.rs` に振り分ける | low | **棄却** | 両ファイルは 517／651 行で 400 行超。SHOW CREATE のケースはフィクスチャ（ビュー・Iceberg の問い合わせ応答）ごと 1 ファイルに置く方が 1 画面でレビューできる。分担のコメント（`tests/table_format.rs:5-6`）には新ファイルへの参照を足す。一致台帳 3 の「3 か所目」は誤りで 5 か所目に訂正 |
| 2-3 | `EngineDdl::content_type()` より `write_result` の `match` 直書きが既存の書き方 | low | 採用 | P2-4、C4 |
| 2-4 | `unmeasured.md:9,15`・`verify.sh:10` は作業ツリーで済み／不要 | low | 採用 | P1-5 |
| 2-5 | `docs/api.md:18-22,86-91` は足さなくても誤りにならない | low | 採用（削減） | P1-5 |
| 2-6 | 一致台帳 2 と述語の表テストは不要 | low | 採用（削減） | 一致台帳、B1 |
| 2-7 | C7 の結合テスト・C8 の結合・C9 のコメント追記は既存で固定済み | low | 採用（削減） | C7〜C9 |

design-checklist: 1-1／2-1 は「形」（形式に依存する分岐を新しい文に広げるとき、docs の表だけでなく「判定できないときの振る舞い」の節も対象に入れたか）なので 1 行足す。

## フェーズの記録

### フェーズ 1: SHOW CREATE VIEW（自分で実装。小規模経路・本番コード約 30 行）

- 7-0: 消すガード無し。変える契約は `text_content_type`（呼び出し元 `content_type::of` だけ）、`metadata_query_id`（`write_result` だけ）、`substatement_type` の SHOW の腕（呼び出し元は計画のとおり副作用なし）。分岐は計画の B1〜B7。境界入力: コメント入り・小文字・先頭の行コメント。
- red（予測どおり）: B3（`content_type.rs` の 4 入力: application）、B5（`metadata_query_id`: 実行 ID）、B6（分類: None）、B7（結合: SubstatementType が Null で最初に落ちる）。既存テストは落ちず。
- green: `content_type::tests` 5、`classification::tests` 8、`result_output::tests` 1、`tests/show_create.rs` 1。全テスト green（`cargo test --locked`。lib 188 ほか）。fmt・clippy ok。
- mutation（5 件、すべて検出）: M1 述語を 2 語に → content_type・show_create ／ M2 DESC を消す → content_type ／ M3 `metadata_query_id` 反転 → tests/metadata ／ M4 `SHOW_CREATE_VIEW` の腕を消す → classification・show_create ／ M5 EXPLAIN を落とす → content_type。
- 7-5b: 分岐の突き合わせ 0 抜け。直した点: CHANGELOG のアンカー（`#content-type` は無い → `#result-files`）、フェーズ 2 未実装の上書きを実装済みのように書いたコメント 3 か所を外した、docs のインラインコードの途中の折り返し。
- 計画から外れた点: 無し。
- 7-6: 省略（本番コード約 30 行・条件非該当）。

### フェーズ 2: Iceberg の SHOW CREATE TABLE（委譲。opus）

- 7-0: 消すガード無し。変える契約は `target_statement`（呼び出し元 `execution.rs` の `run` だけ）、`table_name_start` の引数（動詞 → キーワードの並び。呼び出し元は `parse_target_table` だけ）、`write_result` の Content-Type と先頭 ID（`EngineDdl` の腕ごとの `match`）。分岐は計画の C1〜C10。境界入力: キーワードの間のコメント（`/* */`・`--`）、無引用の大文字、引用符付きのカタログ。
- red（予測どおり）: C1・C2・C3 はコンパイルエラー（`ShowCreateTable`／`ShowCreateTableIceberg` が無い）。C5 は `trino_sqls()` が `["SHOW CREATE TABLE t"]` だけで落ちた（問い合わせが無い）。既存テストは落ちず。
- green: ユニット 3 本（C1・C2・C3）、結合 2 本（`tests/show_create.rs` の Iceberg と Hive の対照）。`cargo test --locked` 合計 337 passed。fmt・clippy ok。
- mutation（7 件、すべて検出）: M1 `target_statement` の腕を消す → show_create の 2 本 ／ M2 並びを `["SHOW"]` → C2 ／ M3 `(ShowCreateTable, Hive)` を Some → C3 と Hive の結合 ／ M4 binary を application → Iceberg の結合 ／ M5 先頭 ID を `metadata_query_id` → Iceberg の結合 ／ M6 :57 のガードを `Some(_)` → Iceberg の結合 ／ M7 `ResultsMode::None` の条件を外す → `tests/syntax.rs` の `ブロックコメント付きの_show_create_table_も本物と違いそのまま通る`。
- 計画から外れた点: (1) Hive の対照の結合テストを 1 本足した（計画レビュー 2-7 では C3 で代替だったが、固定費が小さく M3 を結合でも拾える）。(2) docs の SHOW CREATE TABLE への言及で、変えないと誤りになる箇所を最小限で直した（`result-files.md` の VIEW の段落の「unlike」、先頭 ID の段落の 2 か所、`caveats.md` の「Table format is detected per Trino catalog」の fallback の文）。(3) `measurements/result-files.md` と `unmeasured.md` は HEAD で済んでいた（unmeasured は「済み」:79 に決着を記載済み）ので触っていない。

## review-fix-loop の最新 nextArgs

該当なし（小規模 issue の経路）

## レビュー実施記録

（未着手）

## Step 8 の結果

（未着手）

## dismissed

- 計画レビュー 2-2（新規テストファイルをやめて既存 2 ファイルへ振り分ける）: 両ファイルが 400 行超で、SHOW CREATE のケースはフィクスチャごと 1 ファイルにまとめる方がレビューしやすい。分担のコメントに参照を足すことで一貫性は保つ。

## deferred

| 指摘 | severity | file | 再トリアージ | 起票 |
|---|---|---|---|---|

## 覆した事前判断

なし

## 実機検証

足場: `tools/e2e/result-content-type/verify.sh`（#70 の足場にケース 8a〜11 と、`run_case` の省略可の第 10 引数 `expect_meta_head`（`.metadata` の先頭 2 バイトの 16 進）を足した）。動かし方: `tools/dev.sh tools/e2e/result-content-type/verify.sh`。先頭は `0a <ID の長さ>` の長さだけを見て、ID の中身は比べない（`0a 1b` = Trino の ID 27 文字、`0a 24` = QueryExecutionId 36 文字）。

準備（Trino に直接）: `CREATE SCHEMA IF NOT EXISTS iceberg.default`（compose の iceberg カタログは TESTING_FILE_METASTORE でスキーマを持たない）、`CREATE TABLE iceberg.default.t_ct_ice_<RUN_ID> AS SELECT 1 AS n`、`CREATE VIEW hive.default.v_ct_<RUN_ID> AS SELECT 1 AS n`（hive カタログで通った）。対照は既存の Hive テーブル `t_ct_desc_<RUN_ID>`。

| No | SQL | Catalog | 期待（本体／`.metadata`） | 期待（先頭） |
|---|---|---|---|---|
| 8a | `SHOW CREATE VIEW v_ct_<RUN_ID>` | hive | binary／binary | `0a 1b` |
| 8b | `SHOW CREATE /* c */ VIEW v_ct_<RUN_ID>` | hive | binary／binary | `0a 1b` |
| 9 | `SHOW CREATE TABLE t_ct_desc_<RUN_ID>`（Hive。対照） | hive | application／application | `0a 24` |
| 10 | `SHOW CREATE TABLE iceberg.default.t_ct_ice_<RUN_ID>` | hive | binary／binary | `0a 1b` |
| 11 | `SHOW CREATE TABLE t_ct_ice_<RUN_ID>` | iceberg | binary／binary | `0a 1b` |

### 変更前の結果（2026-09-24、main-151 の src。#151 の変更なし）

PASS=16 FAIL=4。1〜7b は全部 PASS（今までどおり）。予測（8a・8b・10・11 が FAIL、9 が PASS）と一致。証跡 `/tmp/athena-local-issue70-e2e.rQitL4`。

| No | 結果 | 本体 | `.metadata` | 先頭 |
|---|---|---|---|---|
| 8a | FAIL | application（71B） | application | `0a 24` |
| 8b | FAIL | application（71B） | application | `0a 24` |
| 9 | PASS | application（107B） | application | `0a 24` |
| 10 | FAIL | application（218B） | application | `0a 24` |
| 11 | FAIL | application（218B） | application | `0a 24` |

- エンジン ID の長さの裏付け: 同じ走行のケース 4（SHOW TABLES）の `.metadata` の先頭が `0a 1b 20260924_062943_00015_bztr3`（27 文字）。

## マージ後の周知事項

（未着手）

## 範囲外の発見

| 内容 | 出どころ | 起票した issue 番号（起票しないなら理由） |
|---|---|---|
| Hive の `SHOW CREATE TABLE` と `DESCRIBE` の `UpdateCount` は本物 null、athena-local 0（`query_execution.rs:218-225`） | Explore 2・Plan 9-1 | #160 |
| `SHOW CREATE TABLE`／`VIEW` の列名と型: 本物 `createtab_stmt string`／`create view varchar`、Trino `Create Table`／`Create View` varchar。docs に記載なし | Plan 9-2 | #161 |
| 400 行超のファイル: `src/operation/classification.rs`（502）、`src/operation/target_table.rs`（510）、`tests/metadata.rs`（651）、`tests/results.rs`（710）、`tests/table_format.rs`（517）、`tests/common/mod.rs`（622） | Plan 9-3 | #162 |
| `EngineDdl` の型名が SHOW の腕を含むと意味とずれる（D6 で doc だけ直す） | Plan 3 | #162（分割と一緒に改名を検討） |

## 委譲の記録

| 工程 | 委譲 / 自分 | 自分でやった理由 |
|---|---|---|
| Step 3 調査 | 委譲（Explore 2 本・opus） | |
| Step 3 計画の下書き | 委譲（Plan・opus） | |
| Step 3b 実測スクリプト（r5） | 自分 | 既存の r4 の写しで 1 ファイル・約 60 行。指示を書く方が長い |
| Step 5 計画の転記 | 自分 | 規則どおり（Plan は読み取り専用） |
| 実機検証の足場 | 委譲（general-purpose・opus） | |
| Step 6 計画レビュー | 委譲（Explore 2 本・opus） | |
| Step 6 指摘の反映 | 自分 | 規則どおり |
| フェーズ 1 実装（7-1〜7-4） | 自分（小規模経路・本番 約 30 行） | 委譲の固定費より短い |
| フェーズ 2 実装（7-1〜7-4） | 委譲（general-purpose・opus） | |
| 範囲外の起票（#160〜#162） | 自分 | コマンド 3 本 |

## 出荷

（未着手）

## 時系列

| 時刻 | 出来事 |
|---|---|
| 2026-09-24 15:06 | main-151 を作成（着手前 SHA c1548ee）。過去の issue ノート（149.md）を `git rm`。Explore 2 本を起動 |
| 2026-09-24 15:10 | Explore 1（コード経路）が返る。SubstatementType の SHOW_CREATE_VIEW 欠落を発見 |
| 2026-09-24 15:14 | Explore 2（生データ）が返る。Hive の SHOW CREATE TABLE は 4 run で application／素の protobuf／UpdateCount null。同一ラウンドの対照は無し → 実測 1 ラウンドを依頼する判断。Plan を起動 |
| 2026-09-24 15:17 | 実測項目 r5 を足し、TARGET=local のドライラン（ONLY=r4,r5）をバックグラウンドで開始。先行作業: Plan（起動済み） |
| 2026-09-24 15:19 | ドライラン完了（期待どおり）。r5 を 4548091 でコミット。ユーザーに実測（ONLY=r4,r5）を依頼して停止 |
| 2026-09-24 15:22 | ユーザーが実測を実行。監視開始 |
| 2026-09-24 15:26 | Plan が返り計画を転記。実測完了: 形式で割れると決着。計画レビュー（Explore 2 本）を起動。先行作業: 実機検証の足場の委譲（固定文を読んでから起動） |
| 2026-09-24 15:28 | 足場の委譲（general-purpose・opus）を起動。実測の記録を measurements/unmeasured に書く（未コミット。フェーズ 2 のコミットに同梱） |
| 2026-09-24 15:31 | 足場が返る。変更前のビルドで 8a・8b・10・11 FAIL、9 と 1〜7b PASS（予測どおり）。差分を目視して受け入れ |
| 2026-09-24 15:34 | 計画レビュー 2 本が返り反映（採用 9・棄却 1）。計画確定を 8603523 でコミット。6b で D4 をユーザーに確認 |
| 2026-09-24 15:35 | ユーザーが D4（SHOW_CREATE_VIEW を足す）と設計を承認。フェーズ 1 に着手 |
| 2026-09-24 15:41 | フェーズ 1 green・mutation 5/5・自己レビュー済み。コミット |
| 2026-09-24 15:42 | フェーズ 2 を委譲（opus）。範囲外の発見を #160・#161・#162 に起票 |
| 2026-09-24 15:53 | フェーズ 2 が返る（red 予測どおり、337 passed、mutation 7/7）。差分を目視して受け入れ、コミット |
