# quick #119 本物の Athena + PyAthena の PandasCursor で Iceberg の DROP TABLE を読むと EmptyDataError になるかの確認
- 入力: issue #119（#111 の人間検証）。モード: standalone。ブランチ main-119、着手前 SHA 7d36f27（過去ノートの削除コミット 6d64c17 の後から作業）
- ゲート: 本番コード 0 行（実測スクリプト 1 本、docs 3 ファイル）／契約・新規性なし／事実が外れた（本物の Athena + PyAthena は未実測。既存の実測と PyAthena 3.36.0 の `_read_csv` からは同じ例外の見込み）→ 1 問聞いた: 「実測する」を選択／判断: 本物と同じなら athena-local は変えない
- 時系列: 着手 06:38 ／ 実測スクリプトのコミット（d62984b）と依頼 06:42 前後 ／ 実測（run-20260924-214303、ホストの時計で 06:43）／ コミット 06:44
- 検証: 実測 1 ラウンド: Iceberg × PandasCursor は `OperationalError: No columns to parse from file`（原因 `EmptyDataError`、本体 1 バイト `0a`、application/octet-stream、ColumnInfo 0 個）、対照の Iceberg × Cursor と Hive × PandasCursor（0 バイト、binary）は例外なし。形式は SHOW CREATE TABLE で裏取り。空回し（資格情報なし）で preflight の FAIL の経路と summary の出力を確認／`cargo fmt --check`・`cargo clippy --all-targets --locked -- -D warnings`・`cargo test --locked` ok（398 passed）／追加したファイルを生データの実名で grep して 0 件
- 自己レビュー: 実測スクリプトは本番コードではない。本物に対する操作（CTAS 3・DROP 3・後始末の IF EXISTS 3・S3 の接頭辞 1 つの削除）を冒頭のコメントに明記し、同名のテーブルがあれば作らずに止まる
- 打ち切り・escalate: はしご（事実 → 実測工程 1 ラウンド）
- 予算の実績: Bash 約 22 本（うち実測 約 8 本）、所要 約 6 分（実測の待ちを除く）、サブエージェント 0 本（本番コードを変えていないので止めない独立レビューは流さない）
- 出荷: main-119
