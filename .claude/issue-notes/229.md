# quick #229 S3 Tables の Context で LOCATION 付きの CREATE TABLE を本物の文言で弾く
- 入力: issue #229。モード: standalone。ブランチ main-229、着手前 SHA 1d1ebb4
- ゲート: 着手時は事実（1〜2 部・NV との順などが未実測）と判断（構文チェックの前に判定を置く）が外れ → 実測 1 ラウンド（ユーザーの選択）。実装後にサイズが外れた: 本番の追加 109 行・3 ファイル（≤50・3）。呼び出し元は execution.rs の 1 箇所。契約: 文言は本物に合わせる。新規性: 構文チェックの前の判定は #228 の single_statement が先例
- 時系列: 着手 16:35 ／ 実測スクリプト委譲 16:37 ／ スクリプトのコミット 16:48 ／ 実測の完了 16:53 頃 ／ red 16:55 ／ green 16:55 ／ サイズ超過で退避 16:58
- 検証: ユニットテスト（unquoted_ddl の 33 件）ok。標準コマンド・結合テスト・ミューテーション確認は未実施（escalate）
- 自己レビュー: 未実施（escalate）
- 打ち切り・escalate: 提案あり（変更サイズ 109 行 > 50）→ implement-issue を選択。stash「quick-implement: #229 変更サイズ超過（本番 約115行）」
- 予算の実績: Bash 約 30 本（うち実測 約 8 本）、所要 約 23 分（実測待ちを除く）、サブエージェント 1 本（実測スクリプト）
- 出荷: main-229（implement-issue に引き継ぎ）

## 引き継ぎ

### 実測の結果（ROUND=9、2026-09-26、`tools/measure/unquoted-ddl.sh`、生データ `~/athena-unquoted-ddl-measurements/run-20260926-075143/`）
Context は特記なければ `Catalog=s3tablescatalog/<b>,Database=<ns>`。`<L>` は `'s3://.../'`。StartQueryExecution 38 回（35 + 後始末 3）。作られた n26・n27・n31 は DROP 済み。

| 項目 | 文 | 結果 |
|---|---|---|
| n0 | SELECT 1 | SUCCEEDED |
| n1〜n5 | 3 部 awsdatacatalog・1 部・2 部（名前空間）・2 部（Glue の DB）・3 部 AwsDataCatalog、`(n int) LOCATION <L>` | `Table location can not be specified for tables hosted in S3 table buckets`（MALFORMED_QUERY） |
| n6 | `nosuchcatalog229.<db>.t (n int) LOCATION <L>` | `Catalog 'nosuchcatalog229' does not exist`（DATACATALOG_NOT_FOUND） |
| n7〜n9 | 引用符付きの 3 部・引用符付きの 1 部・4 部 + LOCATION | Trino の `line 1:N: mismatched input 'LOCATION'. Expecting: 'COMMENT', 'WITH', <EOF>` |
| n10・n28 | `CREATE EXTERNAL TABLE t`／`<db>.t (n int) LOCATION <L>` | Table location can not be specified |
| n11・n12 | `CREATE EXTERNAL TABLE t (n int)`／`awsdatacatalog.<db>.t (n int)`（LOCATION 無し） | `External keyword not supported for table type ICEBERG`（MALFORMED_QUERY。n12 は 2 catalogs より先） |
| n13〜n15 | `(n int NOT NULL)`・`("n" int)`・3 部 + NOT NULL、+ LOCATION | Trino の mismatched input 'LOCATION'（NV ではない） |
| n16〜n18 | LOCATION の後ろにごみ・引用符無しの値・値無し | Trino の mismatched input 'LOCATION' |
| n19・n20 | PARTITIONED BY (p string)・ROW FORMAT DELIMITED FIELDS TERMINATED BY ',' STORED AS TEXTFILE LOCATION <L> TBLPROPERTIES ('a'='b') | Table location can not be specified |
| n21 | `(n int) STORED AS PARQUET`（LOCATION 無し） | 開始して FAILED、`Iceberg create table statement does not allow STORED AS/BY`（DDL/CREATE_TABLE） |
| n22 | `TBLPROPERTIES (...) LOCATION <L>`（順番が逆） | Trino の `mismatched input 'TBLPROPERTIES'` |
| n23〜n25 | IF NOT EXISTS・小文字・間に /* c */ | Table location can not be specified |
| n26・n27 | `(location string)`・`COMMENT 'LOCATION'`（LOCATION 句無し） | SUCCEEDED |
| n29 | `... LOCATION <L>; -- c` | `Only one sql statement is allowed. Got: ...`（LOCATION より先） |
| n30 | `CREATE TABLE t LOCATION <L>`（列の並び無し） | Table location can not be specified |
| n31 | 既定の Context で `CREATE EXTERNAL TABLE t (n int) LOCATION <L>` | SUCCEEDED（対照） |
| n32 | Context に Database 無しで `t (n int) LOCATION <L>` | Table location can not be specified |

解釈: 本物は Hive の DDL の構文で読めた文にだけ S3 Tables の LOCATION・EXTERNAL の判定を当て、読めなければ Trino の構文エラーを返す。手元の Trino（compose）も `LOCATION` と `EXTERNAL` を構文エラーにすることを確かめた（`mismatched input 'LOCATION'. Expecting: 'COMMENT', 'WITH', <EOF>`、列名の `location` は通る）。

### 実装の状態（stash に退避済み。`git stash pop` で戻る）
- `src/operation/unquoted_ddl/create_table.rs` に `s3_tables_rejection(query) -> Option<&'static str>`（上の形だけを読む。3 部は 1 部目が大文字小文字によらず awsdatacatalog のときだけ。列の並びは既存の `column` を再利用する `column_list`、`string_literal`・`properties`）
- `src/operation/execution.rs`: `single_statement` の直後・`Trino::syntax_error` の前に `s3_tables` を 1 回だけ計算して呼ぶ（後段の `unquoted_ddl::rejection` も同じ変数を使う）。
- `src/operation/unquoted_ddl.rs`: 再エクスポートに足した。
- ユニットテスト 3 本（create_table/tests.rs）は green。未着手: tests/syntax.rs の結合テスト（構文チェックを送らないこと）、ミューテーション確認、docs（caveats・api・CHANGELOG・measurements/statements.md・unmeasured.md・architecture.md）

### 計画レビューの結果（計画攻撃 Explore 1 本、指摘 3 件: 直す 3・dismissed 0・起票 0。本番影響のある妥当指摘 1＝#1）
| # | severity | 指摘 | 判定 |
|---|---|---|---|
| 1 | medium | `CREATE EXTERNAL TABLE AwsDataCatalog.db.t (n int)` を未実測のまま External の文言で弾く（n12 は小文字だけ） | 直す（D5）。unmeasured.md に 2 部と大文字混じりを足す |
| 2 | low | TBLPROPERTIES を何組でも読むのは実測（1 組）を超える | 直す（D6。削減） |
| 3 | low | `TABLE` の確かめを壊しても落ちるテストが無い | 直す（None のテストに `CREATE VIEW v AS SELECT 1`） |
- 実測表 n1〜n32 と計画のテスト 36 件の突き合わせで、偽陽性・実測済みの取りこぼしは無かった（レビュアーの手のトレース）

## 範囲外の発見（出口は未決）
- n6: 実在しないカタログの 3 部 + LOCATION は本物では DATACATALOG_NOT_FOUND。athena-local は Trino の構文エラーのまま
- n21: S3 Tables の Context の `STORED AS` 付き（LOCATION 無し）は本物では開始して FAILED。athena-local は Trino の構文エラー
- EXTERNAL で LOCATION が無く、ほかの句が付く形・`TRINO_CATALOG_MAP` の別名の 3 部は未実測

---
# implement-issue #229（quick から escalate）

## 基本情報
- ベース main、着手前 SHA 1d1ebb4（レビューの base）。小規模経路（1 フェーズ、本番の見込み約 120 行）

## 事前に確認した事実（Explore 1 本。番号は返答の項目）
1. stash の `column_list` は既存の `column`（create_table.rs:109）を `(` の直後から呼ぶだけで、既存の `columns`（:97）と同じ前提。`statement_start` は NV の文言を作るだけで、`column_list` は文言を捨てる。空の `()` と CTAS は `Unmeasured` で None に落ちる
2. `catalog` は execution.rs:63 で決まり、`single_statement`（:69）・`query`（:83）・`syntax_error`（:89）より前。`s3tablescatalog/` の判定は execution.rs:144-145 と create_table_catalog.rs:56（別用途）だけ
3. 偽 Trino の構文チェックは登録した SQL だけをエラーにする表（tests/common/mod.rs:507-520）。`syntax_checks()`（:319）で「構文チェックを送らない」を固定できる
4. docs: caveats.md:326（Plain CREATE TABLE）・:358-376（S3 Tables の節）、api.md:126-129・:146-179（開始時の順）、measurements/statements.md:538-562（#224 の節の書式）・:551（#229 の予約行）、architecture.md:13-14、CHANGELOGS.md:38-49 の形
5. 既定の Context は変えない（s3_tables のガード）。caveats.md:9-11 の一般則のまま
6. CLAUDE.md の不変条件（書き換えない・PREPARE の前置き）には触れない
7. statements.md:490 の `External keyword required for table type HIVE`（#221）は別の文言。混同しない
- 落とした番号: 無し

## 設計判断
- D1: 判定は構文チェックの前（`single_statement` の後）。S3 Tables の Context のときだけ。順は n29（複数の文が先）と Trino が Hive の句を持たないことから
- D2: 読むのは実測で本物が文言を返した形だけ（表の n1〜n5・n10・n19・n20・n23〜n25・n28・n30・n32）。本物も Trino の構文エラーを返した形（n7〜n9・n13〜n18・n22）は None にして今までどおり Trino に任せる（位置・文言は同じ Trino の文法）。未実測（別名の 3 部・表の COMMENT・ほかの ROW FORMAT）も None
- D3: LOCATION の無い `CREATE EXTERNAL TABLE <名前> (列)` も同じ判定で `External keyword not supported for table type ICEBERG`（n11・n12。issue 本文に無い追加。ユーザーが「含める（測った形だけ）」を選択）
- D5（計画攻撃 #1 で追加）: LOCATION の無い EXTERNAL の文言は、測った名前（1 部＝n11、1 部目がちょうど小文字の `awsdatacatalog` の 3 部＝n12）だけにする。2 部・大文字混じりの 3 部は未実測で None（#224 では大文字混じりが別の経路だった）。LOCATION 付きは n1〜n5 で 1〜3 部・大文字小文字によらず測れている
- D6（計画攻撃 #2 で追加）: TBLPROPERTIES は測った 1 組 `('k'='v')` だけを読む
- D4: ファイル構成は子モジュール `create_table/hive.rs` に出す（create_table.rs に足すと 405 行。ユーザーの確認済み）

## 計画（フェーズ 1 本）
- ファイル構成:
  - 新規 `src/operation/unquoted_ddl/create_table/hive.rs`（約 110 行 + インラインのテスト約 90 行）: `s3_tables_rejection`・`column_list`・`string_literal`・`properties`・2 定数
  - `create_table.rs`（301 → 約 305 行）: `mod hive;` と `pub(in crate::operation) use hive::s3_tables_rejection;`。`column`・`ColumnOutcome` は子から使える（非公開のまま）
  - `unquoted_ddl.rs`: 再エクスポートに 1 語
  - `execution.rs`: `s3_tables` を `query` の確定後に 1 回計算し、`syntax_error` の前で呼ぶ。後段の `unquoted_ddl::rejection` も同じ変数
- 前提表: `column` は `(` の直後から呼ぶ（呼び出し元 2 つとも `punct(b'(')` の直後。保証: create_table.rs:97・hive.rs）／`Cursor::literal` は数も読む → `string_literal` で `'` を先に確かめる（cursor.rs:57）／`qualified_name` の引用符付きの部分は `"` で始まる（create_table.rs:36 と同じ判定）
- テスト: hive.rs のユニット 3 本（LOCATION を弾く 14 形・EXTERNAL の 2 形・None の 20 形 + `CREATE VIEW`・EXTERNAL の 2 部と `AwsDataCatalog`・TBLPROPERTIES 2 組）、tests/syntax.rs の結合 1 本（S3 Tables の Context で 400・MALFORMED_QUERY・文言、`syntax_checks()` と `trino_requests()` が空。既定の Context では同じ文を構文チェックに送る対照）
- 壊す候補: (0) EXTERNAL の名前の絞り込みを外す → `CREATE EXTERNAL TABLE AwsDataCatalog.db.t (n int)` の None が落ちる (1) execution.rs の `s3_tables &&` を外す → 既定の Context の対照が落ちる (2) 3 部の `eq_ignore_ascii_case` を `==` にする → AwsDataCatalog の形が落ちる (3) 末尾の `at_end()` を外す → ごみ付きの形が落ちる
- docs: caveats.md の S3 Tables の節、api.md の開始時の順、CHANGELOG、measurements/statements.md に #229 の節、unmeasured.md、architecture.md
- 人間検証リスト: 無し（実測済みの形だけを実装する）
- 先行できる作業: 実機（compose の trino）で同じ文を流す足場は要らない（構文チェックの前で弾き、Trino に送らない）。結合テストで足りる

## 計画レビューの結果（計画攻撃 Explore 1 本、指摘 3 件: 直す 3・dismissed 0・起票 0。本番影響のある妥当指摘 1＝#1）
| # | severity | 指摘 | 判定 |
|---|---|---|---|
| 1 | medium | `CREATE EXTERNAL TABLE AwsDataCatalog.db.t (n int)` を未実測のまま External の文言で弾く（n12 は小文字だけ） | 直す（D5）。unmeasured.md に 2 部と大文字混じりを足す |
| 2 | low | TBLPROPERTIES を何組でも読むのは実測（1 組）を超える | 直す（D6。削減） |
| 3 | low | `TABLE` の確かめを壊しても落ちるテストが無い | 直す（None のテストに `CREATE VIEW v AS SELECT 1`） |
- 実測表 n1〜n32 と計画のテスト 36 件の突き合わせで、偽陽性・実測済みの取りこぼしは無かった（レビュアーの手のトレース）

## 範囲外の発見
| 内容 | 出どころ | 出口 |
|---|---|---|
| n6: 実在しないカタログの 3 部 + LOCATION は本物では DATACATALOG_NOT_FOUND | ROUND=9 | （未決） |
| n21: S3 Tables の Context の `STORED AS`（LOCATION 無し）は本物では開始して FAILED | ROUND=9 | （未決） |
| EXTERNAL で LOCATION が無くほかの句が付く形・別名の 3 部・表の COMMENT・ほかの ROW FORMAT は未実測 | ROUND=9 | （未決） |

## 委譲の記録
| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| 実測スクリプト | 委譲（general-purpose） | — |
| 調査 | 委譲（Explore 1 本） | — |
| 実装 | 自分 | 小規模経路・本番の見込み約 120 行、stash に green の実装がある |

## 時系列（implement-issue）
- 17:00 頃 escalate、quick ノートをコミット（12ed320）
- 調査の Explore が返る。先行作業: 無し（実機の足場は不要）
- 17:07 計画攻撃（Explore 1 本）を起動
- 17:16 先行作業: 待ち時間に D3 をユーザーに確認（含める）、docs の下書き（CHANGELOG・caveats・api・measurements・unmeasured・architecture）、同じ族の open issue の検索（該当なし。出荷時に 1 件に束ねて起票）
