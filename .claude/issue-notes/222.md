# quick #222 jdbc-drivers の FailedDdlScenario の判定が #207 の Entity Not Found に追随しておらず FAIL する
- 入力: issue #222。モード: standalone。ブランチ main-222、着手前 SHA 4306589
- ゲート: サイズ 本番 0 行（足場 3 ファイル・約 14 行、docs 1 ファイル）／呼び出し元: judge_111 の FAILED_DDL_LABELS 1 箇所、check_pyathena.py の failed_ddl 4 呼び出し（同一ファイル）／契約・新規性: 該当なし／事実: 足場で再現できる／判断: 外れた → 1 問聞く（「文を替える」か「開始時の拒否を判定する」か。本物の実測に近いのはどちらかを答えたうえで、実行時に FAILED の文に替える案 A を選ばれた。PyAthena 側も同じ PR で直す）
- 時系列: 着手 09:02 ／ 質問 09:05〜09:09（ユーザー待ち） ／ red 09:11（PyAthena は SHOW COLUMNS 2 件が SKIP。JDBC は issue の証跡） ／ green 09:15 ／ コミット 09:20
- 検証: `tools/dev.sh cargo fmt --check`・`tools/dev.sh cargo clippy --all-targets --locked -- -D warnings`・`tools/dev.sh cargo test` ok（489 passed）。`tools/dev.sh SKIP_BUILD=1 tools/e2e/python-clients/verify.sh`（PASS=29 FAIL=0 SKIP=0）、`tools/dev.sh DRIVER_VERSIONS="3.8.1 3.4.0" SKIP_BUILD=1 tools/e2e/jdbc-drivers/verify.sh`（FAIL 0、111 は 4/4・.txt 4 件）／ミューテーション: 差し替えた RENAME TO を DESCRIBE（JDBC）・SHOW COLUMNS（PyAthena）に戻す → JDBC は 3.8.1 の 111 が 3 回とも FAIL、PyAthena は FAIL 1 件（新しい query_id 無しの分岐）
- 自己レビュー: 分岐 1 件（check_pyathena.py の query_id 無し）はミューテーションで FAIL・INFO の両方を踏んだ。green の回はこの分岐を足す前に流したが、分岐は query_id がある経路を変えない／エラーの出口: 足場の report だけ／呼び出し元 4 箇所確認
- 打ち切り・escalate: はしご（判断 → 1 問聞く）
- 予算の実績: Bash 約 22 本、所要 約 14 分（着手 09:02〜コミット 09:20 から質問待ち 4 分を除く。足場の待ち時間を含む）、サブエージェント 0 本
- 出荷: main-222
