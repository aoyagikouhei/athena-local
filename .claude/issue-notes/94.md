# quick #94 実 SDK でリトライを誘発して ClientRequestToken の冪等性を確かめる（同時リトライの多重度を含む）
- 入力: issue #94。モード: standalone。ブランチ `main-94`、着手前 SHA 0675971
- ゲート: サイズ 本番 0 行・0 ファイル（テスト 1 本・tools/e2e 5 ファイル・docs 2 ファイル。≤50・3）／呼び出し元 0（≤5・同一モジュール）／契約・新規性・判断は通過。事実は「再現」ではなく「検証」で、項目 2 は偽 Trino の結合テスト、項目 1 は手元の docker + Trino 482 + 応答を落とす代理 + boto3 で再現できるので通過（本物の AWS は不要）
- 時系列: 着手 15:10 ／ red（検証が無い状態） 15:10 ／ green（結合テスト 15:13、実機検証 15:16） ／ コミット 15:19
- 検証: `cargo fmt --check` ok、`cargo clippy --all-targets --locked -- -D warnings` ok、`cargo test --locked` ok（329 passed）／ミューテーション: `Store::submit` のトークン検索を `.filter(|_| false)` で常に空にする → 結合テストは ID 50 個で落ち、実機検証（`tools/e2e/sdk-retry/verify.sh`）は INSERT 3 回・行 3・ID 50 個で 4 項目 FAIL。復元後に release を再ビルド／実機検証の本番: 11 項目 PASS（boto3 が接続を 2 回切られても同じトークンで再送、PREPARE 3 回・INSERT 1 回・行 1、50 並列 0.15 秒で ID 1 つ）
- 自己レビュー: 本番コードの分岐の変更なし／エラーの出口なし／呼び出し元なし。足場は uv がこの環境では別のツールだったので boto3 の前提を python3 と venv に直した
- 打ち切り・escalate: 提案なし。stash なし
- 予算の実績: Bash 約 22 本（うち実機検証 4 本）、所要 約 8 分（実機検証と release ビルドの待ち約 4 分を除く）、サブエージェント 0 本（本番コードを変えていないので止めない独立レビューは流さない）
- 出荷: `main-94`（PR 番号は `gh pr list --head main-94` で引ける）
