# issue #147: unmeasured-batch の生 HTTP と保持期限の項目を本物の Athena で測り、SQL / API の項目から切り離す

## 基本情報

- ベース: main（6afe7f8、#152 のマージ）。ブランチ: main-147。着手前 SHA（レビューの base）: b9a1638（過去ノートの削除だけ）
- 親: #114。前: #113（バッチ本体、PR #148）、#146（SQL / API の 19 項目、PR #152）
- 経路: 小規模 issue（フェーズ 2 以下）。フェーズ 1 = run.sh の保持期限の段階 A/B を ONLY で別々に流せるようにする（コード）、実測（ユーザー、Step 3b。生 HTTP 1 回 + 段階 A + 65 分後の段階 B）、フェーズ 2 = 転記（docs）

## 事前に確認した事実

Explore 1 本（opus、9 項目）から転記。落とした番号: 9（行数。分割の判断に使わない）、8 の行番号のずれの表（下の行番号は現行のもの）。

1. 保持期限の差し込み口: `RETENTION_SELECTED`（run.sh:81-82 `want_item t6 || want_item t7`）で段階 A（run.sh:227-240）と段階 B（run.sh:253-255 `RETENTION_SELECTED=1 && WAIT_RETENTION != 0`）の両方が決まる。**`ONLY=t6` だけでも段階 B が呼ばれて締切まで sleep する**（items-retention.sh:82-86。real は 3900 秒）。`ONLY=t7` + `RUN_DIR` は段階 A が `state.env` があれば投げ直さず（items-retention.sh:29-33。skip 行を 1 行足す）、段階 B は締切を過ぎていれば待たない（:80-86）。`state.env` が無い `ONLY=t7` は段階 A を新しく投げてから 65 分待つ（危険な形）。`reset_item_rows t6` は state.env が無いときだけ、`t7` は毎回（run.sh:230-233）
2. items-retention.sh: 段階 A は `t6-first`（同じトークンで `t6-immediate-reuse` の対照つき）→ `state.env` に TOKEN/ID/DEADLINE（= 完了時刻 + real 3900 秒、local `LOCAL_RETENTION_SECONDS`+5）。段階 B は reuse（`t7-reuse-after-wait`、種別 same_id/new_id/error）→ `GetQueryExecution`（`retention/t7-get.json`）→ `StopQueryExecution`（`t7-stop.json`）。`declare_expectation` は 3 つとも local の中（:105-106,125,141）
3. raw.py: 署名は botocore の `SigV4Auth`（:41-43,56-57）、送信は http.client（例外は 2 秒 × 3 回）。資格情報は botocore の既定の探索（:344-348。無ければ終了コード 2）。`QueryExecutionContext` は `$TCAT_GENERIC`/`$TDB`、OutputLocation は `$OUTPUT`、WorkGroup は付けない（primary）。本文は ensure_ascii（非 ASCII は `\uXXXX`）。ケース: t2 = 40 文字の基準 + 4 変種（前後空白・20 文字目 `"`・`\`・大文字）+ 対照の再送（:218-243）、t3 = `あ`×20（60B）・`あ`×50（150B）・ASCII 31/32/128/129（:250-267）、t4 = 短い(10)×`s3://`、短い×`SELEC 1`、短いだけ、40×`s3://`、40×`SELEC 1`（:275-304。分類は real でも走るが判定ではない）、e2 = 40 文字 × `SELEC 1` で `__type`/`AthenaErrorCode`/`ErrorCode`/`x-amzn-errortype` を detail に（:309-319）。`judge()` は 5 か所すべて `target == local` の中。保存は `<item>/<case>.json`（sent_headers は Authorization/X-Amz-Security-Token 除外、status、headers、body、outcome、request_body）
4. lib-raw.sh の `run_raw_item` は TSV 各行を `write_summary_row item case stmt <status> - ... <detail>` に（:26-29）。report.py は raw の行も `- label: state=… note=…` で出し（:149-156）、「期待と違う」は real では付かない。raw の HTTP 行も「StartQueryExecution を呼んだ回数」に数える（:109）。「開始+90分」は `started_at`（最初の実行のまま）
5. real 分岐: 必須チェック（`WORKGROUP2:?` を含む）→ `RAW_SELECTED` なら raw_preflight → GetWorkGroup(WORKGROUP2) → SELECT 1 → trap。GetWorkGroup と SELECT 1 は ONLY によらず毎回。t2〜t7 は WORKGROUP2 を使わない（使うのは t8・w1）。preflight の SELECT 1 は `CURRENT_ITEM` が空のまま走るので item_id 空の行が summary.tsv に増える（run-20260924-004554 の 2 行目と 88 行目で確認）
6. 転記先: (a) unmeasured.md t2 :17、t3 :18、t4 :19、t6 :20、t7 :21、e2 :31（「済み」:41〜、「測れないもの」:93〜）(b) caveats.md:297-304（正規化・chars().count() は ASCII だけ）、:308-310（60 秒まで）、:314-330（保持期限・期限切れ ID・期限切れトークン）、:436-439（Error body key casing。`MALFORMED_QUERY` を足す候補）、api.md:125-132（32〜128 characters）(c) src/config.rs:9-10（`DEFAULT_RETENTION` 3600。「未実測なので athena-local の都合」）、execution.rs:116-118（`chars().count()`。ASCII でしか測っていない）、:46-73（検証順 token → result_location → syntax）、response.rs:62-73（`ErrorCode` を必ず付ける）(d) client-request-token.md の既存節 :7/:35/:55（生 HTTP の境界。ASCII だけ、正規化・マルチバイト・組は未）/:82/:92/:102/:109、errors.md:49-55（ケース 55 の `ErrorCode`）
7. 既に書いてあること: client-request-token.md:10,12「終了後 60 秒たっても同じ ID」。60 秒より長い記述は無い
8. #146 のノートの事実は行番号がずれただけで内容は成り立つ（raw_preflight は local :147-148、real :162-167、`run_raw_item` は preflight に依存しない、items-retention.sh は raw に依存しない）
9. #147 の範囲外の古い文言: caveats.md:304-308（Database/OutputLocation「has not been measured」。t8 の差分は #150 で扱う）、api.md:124（`Catalog` has not been measured。#150）

## 実測の手順

## 実測の結果

1 回目（run-20260924-024654、2026-09-24）。記録は docs/dev/measurements/client-request-token.md と errors.md。

| 項目 | 結論 | athena-local との関係 |
|---|---|---|
| t2 正規化 | 正規化しない。前後空白・`"`・`\`・大文字化の 4 変種とも別の新しい ID、そのままの再送だけ同じ ID | 同じ（caveats を measured に） |
| t3 長さの単位 | `あ`×20（60B）は「greater than or equal to 32」で拒否、`あ`×50（150B）は別文言「clientRequestToken exceeds maximum allowed length 128」で拒否。ASCII 31/129 拒否・32/128 受理。下限は文字数、上限はバイト数と読める | 上限が違う（athena-local は 50 文字を受理）。差分 |
| t4 検証の優先順位 | 長さの足りないトークンが `OutputLocation` の不正・構文エラーより先（INVALID_INPUT のトークンの文言）。#146 と合わせてトークン → OutputLocation → 構文 | 同じ |
| e2 構文エラーの本文 | `__type`・`AthenaErrorCode`・`ErrorCode`（= `MALFORMED_QUERY`）・`Message` の 4 キー。`x-amzn-errortype` 無し | `ErrorCode` は同じ。ヘッダは既知の差分（#145） |
| t6 段階 A | 完了直後の同じトークンの再送は同じ ID（新しい実行は作られない） | 同じ。段階 B（t7）は 2 回目 |

## 実測の経緯

- 2026-09-24 11:46〜11:48 1 回目（run-20260924-024654、`ONLY=t2,t3,t4,e2,t6`）: 完走。raw_preflight 通過（署名 OK）。t2 = 4 変種とも別 ID・再送は同じ ID（正規化なし）、t3 = mb20（60B）は「greater than or equal to 32」で拒否・mb50（150B）は「clientRequestToken exceeds maximum allowed length 128」で拒否・ASCII 31/129 拒否・32/128 受理（下限は文字数、上限はバイト数。文言も別）、t4 = 短いトークンが OutputLocation・構文より先（INVALID_INPUT）、e2 = `MALFORMED_QUERY` に `ErrorCode` あり・`x-amzn-errortype` 無し。段階 A 完了、締切 12:52 JST（state.env の DEADLINE）

## 設計判断

- 段階 A は `ONLY` に t6 があるとき、段階 B は t7 があるときだけ動かす（今は片方でも両方）。`ONLY=t6` → A だけで終了（sleep 無し）、`RUN_DIR` + `ONLY=t7` → B だけ（締切を過ぎていれば待たない）。`ONLY=t6,t7` は従来どおり A → 待ち → B。`state.env` の無い `ONLY=t7` は skip 行だけ（65 分待ちに入らない）
- WORKGROUP2 の必須チェックと preflight（GetWorkGroup・SELECT 1）は変えない（削減。ユーザーは持っているし、SELECT 1 は疎通の証拠）。item_id 空の行と「開始+90分」の古さは見た目だけなので直さない（範囲外の発見に記録）
- 本物での実行は 2 回: 1 回目 `ONLY=t2,t3,t4,e2,t6`（生 HTTP 4 項目 + 段階 A。約 2 分）、65 分以上あとに 2 回目 `RUN_DIR=<同じ run> ONLY=t7`

## 計画

小規模経路（フェーズ 2）。Plan は省略し統括役が書いた。

### フェーズ 1: run.sh の段階 A/B を ONLY で別々に流せるようにする（本番コード約 10 行）
- 変更ファイル: `tools/measure/unmeasured-batch/run.sh`（270 → 約 275 行）。`RETENTION_SELECTED`（run.sh:81-82）は local の保持期限を短くする判定に残し、段階 A の if（:227）を `want_item t6`、段階 B の if（:253）を `want_item t7` にする。`reset_item_rows t6` は段階 A を新しく流すとき（want t6 かつ state.env 無し）、`reset_item_rows t7` は want t7 のとき。`WAIT_RETENTION=0` の skip 行は want している側だけ出す。ヘッダの #147 の流し方（run.sh:8）はそのまま（コードが追いつく）
- items-retention.sh:4-5 のコメント「片方でもあれば両方が動く」を直す
- 依拠する前提表: `want_item` は ONLY だけで決まる（run.sh:73-80）／段階 B は state.env が無ければ skip 行を書いて return 0（items-retention.sh:73-76）／段階 A は state.env があれば投げ直さない（:29-33。B だけを流すときは A を呼ばないのでこの経路は使わなくなる）／report.py は t6/t7 の行の有無で落ちない（項目を summary.tsv から動的に集める）
- テスト（テストランナー無し。実行で確かめる）: local `ONLY=t6`（LOCAL_RETENTION_SECONDS=30）→ 段階 A の行（t6-first・immediate-reuse-compare）が出て sleep せず完了。続けて `RUN_DIR=<同じ> ONLY=t7` → 合格条件は (a) t6 の段階 A の行が 1 本目のまま残る (b) t6 に「既存の state.env を使う（再開）」の行が増えない（= ONLY=t7 で段階 A が呼ばれていない） (c) t7 の reuse・get・stop の行がある。local の期待 3 件（new_id・NOT_FOUND）は 2 本目が別プロセスの空のストアに投げるので必ず match=yes になり、証拠にならない（計画レビュー 1）
- ミューテーション: 無し（分岐は 2 つの if の条件。上の 3 本の実行がそのまま検出する）

### 実測（Step 3b、ユーザー。2 回）
- 1 回目: `ONLY=t2,t3,t4,e2,t6`。raw_preflight（署名の確認）→ 生 HTTP 約 20 リクエスト → 段階 A（`SELECT 1` 2 本）。約 2 分
- 2 回目（65 分以上あと）: `RUN_DIR=<1 回目の run> ONLY=t7`。3 本（再送・GetQueryExecution・StopQueryExecution）。約 1 分
- 落ちた項目は直して `RUN_DIR` + `ONLY` で再実行（予算 3 ラウンド）

### フェーズ 2: 転記（docs。委譲: general-purpose opus）
- 事実 6 の対応表どおりに client-request-token.md・errors.md へ README の形式で追記、unmeasured.md の 6 行を「済み」へ（測れなかったものは理由付きで残す）、caveats.md の Query lifecycle と Error body key casing、api.md の characters/bytes、src/config.rs:9-10 と execution.rs:116-118 のコメントを「同じなら実測日、違うなら起票」で直す。athena-local の挙動は変えない。CHANGELOG 1 行

### 先行できる作業
- 無し（フェーズ 2 は実測の結果に依存）

### 人間検証リスト
- 本物での 2 回の実行（65 分空ける。資格情報は回ごとに有効なら足りる）

## 計画レビューの結果

Explore 1 本（opus、契約・削減・実測の設計）。採用 2、棄却 0。
1. 採用（テストの証拠、high）: local の 2 本目（`RUN_DIR` + `ONLY=t7`）は別プロセスの athena-local（メモリの HashMap は空）に投げるので、保持期限の処理が壊れていても期待 3 件は必ず match=yes。合格条件を「t6 の行が残る・『再開』行が増えない・t7 の 3 行がある」に差し替え。design-checklist の #128 の行（壊した機構を実際に通る足場か）に #147 を足す
2. 採用（削減）: 3 本目の local テスト（state.env 無しの `ONLY=t7`）は 1 の (b) で同じ分岐を検出できるので削る
3. 記録のみ: 段階 B の再送は段階 A と同じ引数（`$TCAT_GENERIC`・`$TDB`・`$OUTPUT`）。`declare_expectation` は 3 か所とも local の中。1 回目の順序は raw_preflight → 段階 A → 本編で、締切の起点は段階 A の完了時刻。t3（20 文字/60B・50 文字/150B）と t2（大文字化・`"`・`\` は JSON で復号されて 1 文字）は判別できる形。raw_preflight の失敗時は `.preflight/preflight.json` と `raw-preflight.err` を見てもらえば足りる

## フェーズの記録

### フェーズ 2a（2026-09-24、general-purpose opus。1 回目の 5 項目の転記）
- client-request-token.md に t2・t3・t4・t6（段階 A）、errors.md に e2 を README の形式で追記（+90 行）。unmeasured.md は 4 行を「済み」へ、t6/t7 は段階 B 待ちで残す。caveats.md の正規化と Error body key casing、src のコメント 2 か所（execution.rs:46 の検証順、response.rs:64-66）。CHANGELOG 1 行
- 統括役の受け入れ: t3 の 3 ケースの生の本文と応答ヘッダを自分で読んで一致。CHANGELOG のアンカー 2 つの実在を確認。t3 の上限の差分は起票（範囲外の発見）

### フェーズ 1（2026-09-24、自分）
- 7-0: 変えるのは run.sh の段階 A/B の 2 つの if（`RETENTION_SELECTED` → `want_item t6` / `want_item t7`）と reset/skip の振り分け。`RETENTION_SELECTED` は local の保持期限（run.sh:122）にだけ残す。依存する呼び出し元: items-retention.sh の関数（変えない）。増える分岐: 段階 A の `want_item t6` と段階 B の `want_item t7`（それぞれ local の 2 本の実行で検出）
- 検証（テストランナー無し）: local `ONLY=t6`（LOCAL_RETENTION_SECONDS=30）→ 27 秒で完了、sleep 無し、t6 の 3 行。続けて同じ RUN_DIR で `ONLY=t7` → 「再開」行は増えず t6 の 3 行はそのまま、t7 の reuse・get・stop の行あり（41 秒。締切まで 27 秒待った）
- 計画から外れた点: 無し。7-6: 省略（小規模経路）。7-7: 分割不要（run.sh 275 行）

## レビュー実施記録

- 計画レビュー: Explore 1 本（採用 2、棄却 0）
- 独立レビュー（Step 8、フェーズ 1 の差分 b9a1638...fe556a1）: 1 回。本番影響のある妥当指摘 0 件。low の参考 1 件（state.env が無い `ONLY=t7` の skip の理由が「段階Aが失敗した」で、RUN_DIR の渡し忘れと見分けにくい）→ 対応しない（見た目だけ。skip 行は出るし待たない）

## dismissed

## deferred

| 指摘 | severity | 再トリアージ |
|---|---|---|

## 覆した事前判断

## 実機検証

## 範囲外の発見

| 内容 | 出どころ | issue |
|---|---|---|
| ClientRequestToken の長さの上限を本物はバイト数で判定し、文言も別（`clientRequestToken exceeds maximum allowed length 128`）。athena-local は文字数 | 実測 t3 | #153 |
| `x-amzn-errortype` ヘッダは本物の 200/400 のどれにも無い | 実測 t2〜e2 | #145（既存） |
| real の preflight の `SELECT 1` が item_id 空の行を summary.tsv に足す。「開始+90分」は最初の実行の時刻のまま | 調査 | 起票しない（見た目だけ。判定に使わない） |

## 委譲の記録

| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| 調査 | Explore 1 本（opus） | 小規模経路。#146 の run.sh・lib-raw.sh・items-retention.sh を継承 |
| 計画 | 自分 | 小規模経路（Plan 省略） |
| 計画レビュー | Explore 1 本（opus） | 規則どおり |
| フェーズ 1 の実装 | 自分（小規模経路・本番 約 10 行） | 委譲の固定費の方が長い |

## 出荷

## 時系列

- 2026-09-24 10:42 b9a1638 過去のノートを消して着手。Explore 1 本を起動
- 2026-09-24 10:47 Explore が返った。事実と計画をノートに書き、計画レビュー（Explore 1 本）を起動
- 2026-09-24 10:51 b053e0b 計画レビューが返った（採用 2、棄却 0）。6b: 論点 0 件のため提示のみ
- 2026-09-24 10:53 fe556a1 フェーズ 1 を実装し local の 2 本で確認。独立レビューを起動し、ユーザーに 1 回目の実測を依頼
- 2026-09-24 10:55 独立レビューが返った（本番影響 0 件）
- 2026-09-24 11:48 1 回目が完走（5 項目すべて取れた）。フェーズ 2 の転記（t2/t3/t4/e2/t6）を先行して委譲。段階 B の締切 12:52
- 2026-09-24 11:58 フェーズ 2a が返った。生データで t3 を確認、#153 を起票してコミット
