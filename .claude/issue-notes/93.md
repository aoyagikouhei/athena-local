# quick #93 CREATE OR REPLACE TABLE ... AS を本物が受け付けるかと、受け付けるときの結果ファイル名を測る
- 入力: issue #93。モード: standalone。ブランチ `main-93`、着手前 SHA 130b157
- ゲート: サイズ 0 行・0 ファイル（≤50・3。本体は変えない）／呼び出し元 0（≤5）／契約・新規性・判断は通過。**事実が外れた**（本物が受け付けるかもファイル名も測らないと決まらず、過去の実測に `OR REPLACE` を含む SQL が無く、手元に認証情報が無い）→ 実測工程を足して続行
- 時系列: 着手 13:41 ／ 実測スクリプトのコミット 13:45 ／ 実測 1 回目 13:47（preflight で止まった。スクリプトの read の変数不足）／ 修正コミット 13:56 ／ 実測 2 回目 13:56〜13:59（ユーザーが実行） ／ 文書 13:58 ／ コミット 14:00
- 検証: `cargo fmt --check`・`cargo clippy --all-targets --locked -- -D warnings`・`cargo test --locked` ok（328 passed）／ミューテーション: 本体の変更なしのため対象なし（文書だけ）／実測: 2 ラウンド（`93-measure-create-or-replace.sh`。1 回目 `run-20260923-134733` は preflight で 404 になって止まり、原因は SubstatementType の列を足したときに preflight の `read` を 3 変数に直し忘れたこと。2 回目 `run-20260923-135634`、StartQueryExecution 8 回）。Hive・Iceberg とも `CREATE OR REPLACE TABLE ... AS SELECT` は StartQueryExecution で `InvalidRequestException`（`line 1:19: mismatched input 'TABLE'. Expecting: 'MATERIALIZED', 'MULTI', 'PROTECTED', 'VIEW'`、`AthenaErrorCode: MALFORMED_QUERY`）。対照の素の CTAS は `tables/<id>`・`.metadata` 81 バイト・マニフェストあり・`CREATE_TABLE_AS_SELECT` で #26／#35 の再現。待ち時間に手元の Trino 482 で同じ文を流すと Iceberg は新規・置換とも通り、Hive は `This connector does not support replacing tables` で失敗した
- 自己レビュー: 本体の分岐の変更なし／エラーの出口なし／呼び出し元の変更なし。`is_create_table_as` の `OR REPLACE` の扱い（`tables/<id>`）は Trino が実行する文に対する athena-local の規則として残し、README にその旨を書いた
- 打ち切り・escalate: はしご（事実 → 実測工程 2 ラウンド）。提案なし。2 ラウンド目は自分のスクリプトのバグによる再実行で、本物の挙動の追加項目ではない。監視の `pgrep` が監視ループ自身に一致して停止を見逃したので、コマンド行の先頭が `bash` のものだけ見るように改めた
- 予算の実績: Bash 約 16 本（うち実測 8 本）、所要 約 14 分（実測待ち 2 回 約 6 分を除く）、サブエージェント 0 本（本体を変えないので止めない独立レビューは流さない）
- 出荷: `main-93`
