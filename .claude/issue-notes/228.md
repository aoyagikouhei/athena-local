# quick #228 複数の文（末尾の ; の後ろにコメント）を本物の Only one sql statement is allowed で弾く
- 入力: issue #228。モード: standalone。ブランチ main-228、着手前 SHA c835018
- ゲート: サイズ 本番 約 44 行・3 ファイル（≤50・3）／呼び出し元 1（`multiple_statements` は `start_query_execution` の 1 箇所。`athena_sql::statements` は新設で呼び出し元 1）／契約: 本物の文言に合わせる修正で通過・新規性: 無し（athena-sql の skip_quoted・comment_end の再利用）・事実: 外れた（実測 1 点だけ）・判断: 無し → はしご（事実 → 実測工程）
- 時系列: 着手 12:31 ／ 実測スクリプトのコミット 12:35 ／ 実測の結果 12:38 頃（ユーザーの実行）／ red 12:39 ／ green 12:40 ／ コミット 12:43
- 検証: `tools/dev.sh cargo fmt --check`・`tools/dev.sh cargo clippy --all-targets --locked -- -D warnings`・`tools/dev.sh cargo test` ok（506 passed）／ミューテーション: `count > 1` を `count > 2` → 弾くテストが落ちた、空白だけの片の除外を外す → 数えないテストが落ちた／実測: 1 ラウンド（ROUND=6、31 項目。生データ run-20260926-033720）。待ちの間に同じ文を手元の athena-local に通し、`;` の形はすべて Trino の構文チェックで弾かれる（新しい判定がどの形にも届く）ことを確かめた
- 自己レビュー: 分岐 2 件（2 片以上で弾く／それ以外は構文チェックへ）とも結合テストあり、`statements` の引用符・コメント・未閉じの分岐はユニットテストあり／エラーの出口: 構文エラーと同じ `invalid_request_with_code`（400・MALFORMED_QUERY）／呼び出し元 1 箇所: 受け取った SQL（パラメータを当てる前）で数える。トップレベルの `;` の後ろに中身がある SQL は以前も Trino の構文エラーだったので、変わるのは文言だけ
- 打ち切り・escalate: はしご（事実 → 実測工程 1 ラウンド）。末尾の `;` だけの文は本物では通るが athena-local は構文エラーのまま → #240 に起票。予算は上限内
- 予算の実績: Bash 約 45 本（うち実測 約 15 本）、所要 約 12 分（待ち時間を除く）、サブエージェント 1 本（止めない独立レビュー）
- 出荷: ブランチ main-228
