# quick #160 Hive のテーブルへの SHOW CREATE TABLE と DESCRIBE の UpdateCount を本物と同じく省く
- 入力: issue #160。モード: standalone。ブランチ main-160、着手前 SHA 2e8d08b
- ゲート: サイズ 見込み 30〜40 行・3 ファイル（≤50・3。上限内）／呼び出し元: `update_count` 1 箇所、`Store::finish` 1 箇所／契約: 通過（本物に合わせる修正）／**事実: 外れた**（DESCRIBE × Iceberg の UpdateCount 未実測。Hive の外部テーブルだけ #76 c5・#146 x1 で null）／**新規性: 外れた**（SHOW CREATE TABLE の Hive／Iceberg の判定は `run` の中だけにあり、Store の実行情報に保存する変更が要る＝共有状態）／判断: 通過
- 時系列: 着手 18:33 ／ ゲート判定 18:40 ／ 質問 18:41 → 回答「implement-issue で進める」 ／ ノートのコミット 18:38
- 検証: 未着手（コードに触っていない）
- 自己レビュー: 該当なし
- 打ち切り・escalate: 提案あり（事実・新規性が同時に外れた）→ 選択: implement-issue。stash 無し（差分無し）
- 予算の実績: Bash 約 7 本、所要 約 359 分、サブエージェント 0 本
- 出荷: implement-issue に引き継ぐ（このコミットは implement-issue の docs 予算の別枠）

## 引き継ぎ

- 判定箇所:
  - `src/operation/query_execution.rs:223-230` `update_count`: `outcome.update_count` が無ければ「DDL 以外は 0」。UTILITY の DESCRIBE と SHOW CREATE TABLE はここで 0 になる。呼び出し元は同ファイル :112 の 1 箇所（GetQueryResults の応答）
  - `src/operation/execution.rs:262-292`: `table_format::target_statement` → `target_table::parse_target_table` → `table_format::probe_format` → `engine_ddl` で Hive／Iceberg を判定。**`run` のローカル変数で、`write_result`（`result_output.rs:92`）に渡すだけ。Store には残らない。** `ResultsMode::None` のときは問い合わせ自体を省く（:268。レビュー指摘で入れた最適化）
  - `src/store.rs:45` `Execution`、`:212` `finish(id, Result<Outcome, Failure>)`。`src/trino.rs:16` `Outcome`（columns / rows / update_count / id / update_type）
- 共有できる既存機構: `table_format::{TargetStatement::ShowCreateTable, TableFormat, probe_format, engine_ddl}`（#151）。`classification::substatement_type` の `SHOW_CREATE_TABLE`／`DESCRIBE_TABLE`。テストの偽 Trino は `tests/show_create.rs:106-113` のように形式の問い合わせに `connector_name` を返せる
- 原因の仮説: 本物は「UpdateCount が無い（null）文 ＝ 本体が application/octet-stream ＝ `.metadata` が素の protobuf」で一致している（result-files.md:419、r5 の表）。Hive の SHOW CREATE TABLE と DESCRIBE はこの群、Iceberg の SHOW CREATE TABLE とほかの SHOW は 0 の群。DESCRIBE × Iceberg がどちらの群かで DESCRIBE に形式の問い合わせが要るかが決まる
- 設計の選択肢（未決）: (a) `Store::finish` で保存する実行情報に判定結果（`EngineDdl` か「UpdateCount を省く」フラグ）を足し、`update_count` で参照する。(b) `Outcome` に載せる。どちらも `ResultsMode::None` でも SHOW CREATE TABLE の問い合わせが要るようになる（:268 の条件を変える）
- 未実測: DESCRIBE × Iceberg の UpdateCount（対照 DESCRIBE × Hive と同じラウンドで。r5 のフィクスチャ（Hive 外部・Hive CTAS・Iceberg 素の CREATE）に DESCRIBE を足せば 1 ラウンド。DDL あり）
- 再現手順（red の候補）: `tests/show_create.rs` の Hive のケース（`connector_name` = hive）で `GetQueryResults` の `UpdateCount` が無いことを assert する → 今は 0 が返って落ちる。DESCRIBE は `tests/select.rs:546` の `DESCRIBE t` の route を使う
- stash: 無し
