# quick #161 SHOW CREATE TABLE / SHOW CREATE VIEW の GetQueryResults の列名と型が本物と違う
- 入力: issue #161。モード: standalone。ブランチ main-161、着手前 SHA 1f1b239
- ゲート: サイズ 約 50 行・4 ファイル（≤50・3。`classification.rs` に表、`convert/result_set.rs` に適用、呼び出し元 2 ファイルは引数を 1 つ足しただけ）／呼び出し元 2（≤5・operation 内）／契約: `ColumnInfo` の Name／Type を本物の実測値に合わせる修正で docs・CHANGELOG 同梱、新規性: 新ファイル無し、事実: 偽 Trino で再現、判断: issue の「他の SHOW も確かめてから決める」は既存の生データで確認し、列数まで違う `DESCRIBE`／`SHOW COLUMNS` があるので SHOW CREATE の 2 文に限り残りは #173 に起票 → ファイル数だけ外れ（下）
- 時系列: 着手 20:05 ／ red 20:10 ／ green 20:11 ／ コミット 20:15
- 検証: `tools/dev.sh cargo fmt --check`・`cargo clippy --all-targets --locked -- -D warnings`・`cargo test --locked` ok（全テストバイナリ ok。tests/metadata.rs の SHOW CREATE TABLE の固定バイト列を本物の 88 バイトの形に直した）／ミューテーション: `fixed_column` の SHOW_CREATE_TABLE の腕を None にする → tests/metadata.rs の `show_create_table_の_metadata_も先頭が実行_id_になる` が落ちた（show_create のテストも同じ列を固定している）
- 自己レビュー: 分岐 4 件（SHOW_CREATE_TABLE: Hive／Iceberg の結合テスト、SHOW_CREATE_VIEW: 結合テスト、それ以外: 単体テストの `plain` と既存の全テスト、`fixed_column` の Some／None: 単体テスト）／エラーの出口: 新しいエラー無し／呼び出し元 2 箇所（GetQueryResults と `.metadata`）は同じ `fixed_column` を渡し、Hive の `.metadata` が本物の 88 バイトと一致することで両方を確認
- 打ち切り・escalate: 外れた観点はファイル数 4（閾値 3）だけ。分類の表を `classification.rs` に置くと呼び出し元 2 ファイルが必ず増えるため。自律モードで質問できず、1 行の引数追加なので層を足さず続行した（要確認）。予算内（Bash 約 20 本、20 分）
- 予算の実績: Bash 約 20 本、所要 10 分、サブエージェント 1 本（止めない独立レビュー）
- 出荷: standalone、ブランチ main-161
