# #194 catalog.rs の SQL の字句処理を内部 crate にそのまま移す（#192 の段階 2）

## 基本情報

- issue: #194（親 #192。#193 = PR #197 の後）
- ベースブランチ: main
- 着手前 SHA: `0137b93b22a5afe5bcb12a820202d62269b777d6`（レビューの `base` の正）
- 過去ノート削除コミット: `bafc892`
- 経路: 挙動を変えない移動（move-refactor）。小規模 issue の経路（2 フェーズ: 1 = 移動、2 = 文書の追随）。独立レビュー 1 回、止めない
- 前 issue の申し送り（#194 のコメント）: decisions.md の「字句走査の道具は `src/catalog.rs` に集める」（#17・#49）、CLAUDE.md の地図の `catalog.rs` の行、
  architecture.md の `catalog.rs` の箇条と「crate の構成」の「#193 の時点では中身が無い」を「#194 で変更」と添えて直す。
  足場は #193 の `193-verify/check.sh`（PR #197 の履歴）を再利用。cargo は `Running unittests` のパスを package 相対で出す（crate の行も `src/lib.rs`）

## 事前に確認した事実

### 着手前の基準値（ホストの `cargo test --locked`、0137b93、2026-09-25 10:25 採取）

- `test result:` 25 ブロック、合計 **398 passed**（athena_local lib 228 / main 0 / 結合テスト 20 ファイル 170 / athena_sql lib 0 / Doc-tests 0+0）
- `catalog::tests` は 27 本。残す側（`alias_qualified_names` の 12 本）と移す側（`skip_leading_trivia` 6 本・`words` 3 本・`skip_qualified_name` 6 本 = 15 本）
- `src/catalog.rs` 582 行。`///`+`//!` は 74 行（`//!` 15 行、`///` 59 行）。「実測」を含む行は 5 行（:101・:104・:124・:158・:482）。
  分岐の語（if/match/return/else/while/loop/for）を含む行はファイル全体で 39
- `crates/athena-sql/src/lib.rs` は `//!` 5 行のみ

（着手時に統括役が catalog.rs の全文と `catalog::` の grep を自分で読んだ）

### 移動単位と依存（Explore A の転記。落とした番号: 1 の各宣言の行範囲の詳細（実装が終われば実物が答え）、7 の `use` とフルパスの例（設計判断 2 に要る結論だけ残す））

- 宣言（doc を含む行範囲）: `alias_qualified_names` :20-55 `pub` / `skip_quoted` :57-74 / `comment_end` :76-93 / `skip_leading_trivia` :95-119 / `words` :121-153 /
  `skip_keyword` :155-177 / `skip_trivia` :179-192 private / `next_is_dot` :194-197 private / `skip_qualified_name` :199-222 / `skip_name_part` :224-236 private /
  `unquote` :238-247 / `replacement` :249-259 private / tests :261-582。`pub(crate)` は 7 つ（skip_quoted, comment_end, skip_leading_trivia, words, skip_keyword, skip_qualified_name, unquote）。属性は無し
- 依存: `alias_qualified_names` → skip_quoted(:30,:32)・unquote(:34)・next_is_dot(:35)・replacement(:39)・comment_end(:44)。`next_is_dot` → skip_trivia。`words` → skip_trivia・skip_quoted・comment_end。
  `skip_keyword` → skip_leading_trivia。`skip_qualified_name` → skip_name_part・skip_trivia。`skip_name_part` → skip_quoted。`skip_leading_trivia`/`skip_trivia` → comment_end。
  `next_is_dot`・`replacement` を呼ぶのは `alias_qualified_names` だけ。private の `skip_trivia` は words・next_is_dot・skip_qualified_name が呼ぶ
- 呼び出し元（src/、コメント除く）: フルパス `crate::catalog::foo(` が classification.rs（words :9、skip_keyword 16 か所 :115-166、skip_leading_trivia :117、skip_qualified_name :118）、
  results.rs:48 words、type_spelling.rs（skip_quoted :53,:76、unquote :54）、target_table.rs（skip_keyword :81,:83,:84、skip_leading_trivia :87,:97,:99、skip_quoted :110、unquote :112）、
  completion.rs（skip_keyword :21,:22、skip_leading_trivia :26、skip_qualified_name :27）。`use crate::catalog::{...}` が iceberg_partitions.rs:6（skip_quoted, unquote）、
  content_type.rs:8（skip_keyword, skip_leading_trivia, skip_quoted, words）。`alias_qualified_names` の `use` は execution.rs:11・completion.rs:3（残留なので変えない）。
  外部から呼ばれない `pub(crate)`: `comment_end`。テストの参照は src/operation/target_table/tests.rs:348 `crate::catalog::skip_qualified_name(input, 0)` の 1 件だけ。`tests/` は 0 件（コメントの言及のみ）。`mod catalog;`（src/lib.rs:5）は private
- テスト: 27 本。残す 12 本はヘルパ `S3_TABLES`(:265)・`aliases`(:267-272、HashMap)・`alias`(:274-276) と `Cow`(:415,:419)・`HashMap::new`(:414) を使う。移す 15 本（skip_leading_trivia :435-477、words :479-533、skip_qualified_name :535-581）はヘルパも `HashMap`・`Cow` も使わない
- 消えても気づけないもの: `[lints]`・`#![warn(missing_docs)]`・clippy.toml・rustfmt.toml・`.cargo/config.toml` はどれも無い。CI は fmt / clippy `-D warnings` / test（ci.yml:21-24）。
  記録の行は `//!` :4・:12（Trino 482 で確認。残留側）、`///` :59・:78・:240（issue #39 Phase 2）、:101・:104・:124・:158（実測）、:129（計画攻撃）、:163（#49）、:204（#52）、:208（#44）、テスト内 :482・:504
- 慣習: src/ で `athena_sql::` の参照は 0 件。他モジュールの呼び方は `use` が主流だが、字句関数は 5 ファイルがフルパス（decisions.md:77 の規則）
- 基準値（本体 1-260 行）: 分岐の語を含む行 35、`///` 59 行、`//!` 15 行、通常コメント 1 行（:22）

### 文書・コメントの参照と慣習（Explore B の転記。落とした番号: 3 の decisions.md の全文引用（フェーズ 2 で該当行を直接読む）、6 の #193 ノートの要点（上の申し送りと重複）、7 のコミット一覧（decisions.md:247-253 に慣習として写されている））

- 移動側を指す参照（フェーズ 2 で直す）: CLAUDE.md:33（地図。「SQL の字句処理」が誤りになる）、architecture.md:8（「#193 の時点では中身が無い」）・:28（`catalog::words`・`skip_trivia`・`catalog::skip_leading_trivia`）、
  decisions.md:75（道具は `src/catalog.rs` に集める・`lexer.rs` を作らない。#17・#49）・:76（`skip_keyword` は `catalog.rs` に 1 つ）・:77（`catalog.rs` の可視性と `crate::catalog::foo()` のフルパス。#49）・:147（`catalog::skip_qualified_name`）、
  unmeasured.md:11（`catalog::words`）、crate の lib.rs:5（未来形）、catalog.rs の `//!` :14-15。コード内の doc: content_type.rs:69、target_table.rs:39・:75・:106、iceberg_partitions.rs:10、classification.rs:3・:76・:105・:111、target_table/tests.rs:334
- 残留側を指す参照（直さない）: CLAUDE.md:75、architecture.md:15・:29、decisions.md:66（`catalog.rs` の `replacement`）・:74、tests/describe.rs:312、tests/table_format.rs:185-186、execution.rs:11・completion.rs:3 の `use`
- 歴史的（直さない）: classification/tests.rs:218・target_table/tests.rs:385（#49 の経緯）、design-checklist.md:17（#39 の経緯）、measurements/statements.md:64（実測記録）、decisions.md の出典番号
- decisions.md の書き方: :6「変わったものは新しいほうを載せ、『#N で変更』と添える」。前例 :76「（#49、2026-09-22。#44 の「2 版を統合しない」を変更）」、:137「（#151 では改名を見送った。#162、2026-09-25 で変更）」。
  パスだけを直した前例 #175（6237a86）は「#N で変更」を添えずその場で置換。:242「分割後の各ファイルに 1 行の `//!`、「実測」「採取」を入れない」、:243「存在しなくなるファイル名を指す記述は直す。出典の issue 番号は書き換えない」、:252「分割と文書の追随は別コミット」
- CHANGELOG・README・docs/*.md は変えない（decisions.md:24・:69）。`[Unreleased]` に関連項目なし
- 400 行の規則（CLAUDE.md:50・decisions.md:237）: 全体（本体 + テスト）で測り、超えたら `<name>/tests.rs`（`mod.rs` 無し）
- 先行の分割の許容リスト: #162 は `mod tests { }` の閉じ括弧 1 行だけ、fmt が折り返しを結合したときは許容を広げず正規化側を足した。#175 は許容を行ごとに名指し

## 設計判断

1. **移す単位**: `skip_quoted`・`comment_end`・`skip_leading_trivia`・`words`・`skip_keyword`・`skip_trivia`・`skip_qualified_name`・`skip_name_part`・`unquote` の 9 関数と、それを直接呼ぶテスト 15 本。
   残す: `alias_qualified_names` と、それ専用の private 補助 `next_is_dot`・`replacement`（decisions.md:239「呼び出し元が 1 つの補助は呼ぶ側と一緒に」、:81「`next_is_dot` は書き直さない」）、テスト 12 本とヘルパ。
   issue 本文の「`alias_qualified_names` だけを残す」はこの読みで、覆していない。
2. **可視性**: 移した `pub(crate)` 7 つは crate 境界を越えるので `pub`。`skip_trivia` は残留側の `next_is_dot` が呼ぶので `pub`。`skip_name_part` は private のまま。計 8 件。それ以外の可視性の変更は事故。
3. **呼び出し元の書き方**: フルパスで呼んでいた 5 ファイル（classification / results / type_spelling / target_table / completion）と target_table/tests.rs:348 は `crate::catalog::foo(` → `athena_sql::foo(`。
   `use` で取り込んでいた content_type.rs・iceberg_partitions.rs は `use athena_sql::{...}`（std／外部／crate を空行で分ける慣習に従い、content_type.rs では組を分ける空行が 1 行増える）。
   catalog.rs の残留側は `use athena_sql::{comment_end, skip_quoted, skip_trivia, unquote};`（呼び出し行を変えず fmt の折り返しを起こさない）。再エクスポート（`pub use`）やラッパーは作らない（decisions.md:76 の「前例になる」）。
4. **crate 側の置き場**: `crates/athena-sql/src/lib.rs` に直接置く（本体 + テスト 約 350 行で 400 行以下。#195 で分けるときも `pub use` でパスは変わらない）。関数の並びは元の順のまま。
5. **モジュール doc はフェーズ 1 に含める**（move-refactor「モジュール doc は新ファイルに書き起こす」）: catalog.rs の `//!` :13-15 を 1 行に書き換え、lib.rs の `//!` :5 を「#194 で catalog.rs から中身を変えずに移した字句処理の道具。athena-local は `athena_sql::foo()` で呼ぶ」に書き直す（関数名の一覧は書かない。コードが正で、#195 で古くなる。計画レビュー 4-5）。
   新しい `//!` に「実測」「採取」「計画攻撃」「確認」と分岐の語（if/match/return/else/while/loop/for）を入れない（足場 4・8 が `//!` も数える）。
6. **文書（フェーズ 2）**: decisions.md:75・:77 は「#194 で変更」を添えて書き換え、:76・:147・architecture.md:28・unmeasured.md:11 とコード内の doc はパスの接頭辞だけ置換（#175 の前例）。
   CLAUDE.md は :33 だけ（:34 の crate の役割は誤りにならない）。README・CHANGELOG・docs/*.md は変えない（decisions.md:24）。歴史的な記述と移した関数の `///` は直さない。
7. **実機検証**: `194-verify/e2e.sh`（別名の置換 → crate の 4 関数、SELECT の分類、ALTER TABLE RENAME の分類、別名無しの対照。CTAS と DROP TABLE は準備と後始末で、`RESULTS=none` では target_table を通らない）と
   `tools/e2e/result-content-type/verify.sh`（content_type・`ResultFile::of`・classification・SHOW CREATE TABLE の target_table）。
8. **委譲**: フェーズ 1 の移動は general-purpose（opus）に委譲（移動 約 340 行。move-refactor の実績どおり）。フェーズ 2 は統括役が転記。

## 計画

（Plan の下書き A〜K を転記。落とした番号: A の切り出しブロックの行番号の詳細（実装の指示にはそのまま渡す）、B の前提表のうち言語規則の 2 行（`use super::*` の挙動。今のコードが依存済み）、
J-1・J-2 の fmt の推測（実装後に `git diff` で見る）、I の 9-3（任意。時間があれば）、K-6（害なしと判定、足場はそのまま））

### フェーズ 1: 移動（1 コミット）

| ファイル | 責務 | 見込み |
|---|---|---|
| `crates/athena-sql/src/lib.rs` | 字句処理の道具 9 関数とテスト 15 本 | 5 → 約 350 行（`//!` 5〜8 + 本体 186 + テスト 152） |
| `src/catalog.rs` | `TRINO_CATALOG_MAP` の別名の置換（3 関数）とテスト 12 本 | 582 → 約 246 行 |
| 呼び出し元 8 ファイル（classification / results / type_spelling / target_table / completion / content_type / iceberg_partitions / target_table/tests） | パスの付け替えだけ（38 行 + 空行 1） | ±0 |

Cargo.toml・Cargo.lock・crates/athena-sql/Cargo.toml・src/lib.rs は変えない。

手順（コピーせず削りながら切り出す）: (1) catalog.rs の 57-193（6 関数）と 199-248（3 関数）を切り取り lib.rs の `//!` の後ろに貼り、可視性 8 件を直す (2) 435-581（テスト 15 本）と 434 の空行を切り取り、lib.rs に `#[cfg(test)] mod tests { use super::*; ... }` で貼る
(3) catalog.rs に `use athena_sql::{comment_end, skip_quoted, skip_trivia, unquote};` を足し、`cargo build -p athena-sql`・`cargo clippy -p athena-sql --all-targets -- -D warnings`・`cargo test -p athena-sql`（15 件）で crate 単体を緑にする
(4) 呼び出し元の `crate::catalog::(words|skip_keyword|skip_leading_trivia|skip_qualified_name|skip_quoted|unquote)` → `athena_sql::\1`、content_type.rs:8・iceberg_partitions.rs:6 の `use` を付け替え、build と clippy
(5) `//!` を直す（設計判断 5） (6) `cargo fmt` を実際に当てて `git diff` で fmt が変えた行を目で見る → `AFTER=1 check.sh` 全 ok。

`use` の配分: lib.rs 本体は `use` 無し（prelude だけ）。lib.rs のテストは `use super::*` だけ（`HashMap`・`Cow` は使わない）。catalog.rs のテストは変えない。content_type.rs のテスト（:172）・iceberg_partitions.rs のテスト（:92）は親の `use` から届くので変えない。

### フェーズ 2: 文書とコメントの追随（1 コミット）

| 場所 | 直し方 |
|---|---|
| decisions.md:75 | 「道具は内部 crate `athena-sql`（`crates/athena-sql/src/lib.rs`）に集める。athena-local の中に字句処理のモジュール（`lexer.rs` など）を作らない」。末尾に「#194、2026-09-25 で「`src/catalog.rs` に集める」を変更」 |
| decisions.md:76 | 「`catalog.rs` に 1 つだけ置く」→「`athena-sql` に 1 つだけ置く」（パスだけ） |
| decisions.md:77 | 「`athena-sql` の字句関数は athena-local から呼ぶものを `pub`、crate の中だけの補助（`skip_name_part`）を private。athena-local からは `athena_sql::foo()` のフルパス（#194 の前から `use` だった content_type.rs・iceberg_partitions.rs と、別名の置換の catalog.rs は `use athena_sql::{...}`）」。末尾に「#194、2026-09-25 で「`catalog.rs` の `pub(crate)`、`crate::catalog::foo()`」を変更」 |
| decisions.md:147 | `catalog::skip_qualified_name` → `athena_sql::skip_qualified_name` |
| CLAUDE.md:33 | 「`catalog.rs`（`TRINO_CATALOG_MAP` の別名を修飾名に当てる置換）」 |
| architecture.md:8 | 「字句処理の道具は #194 で `catalog.rs` から中身を変えずに移し、athena-local は `athena_sql::foo()` で呼ぶ。文の頭・句の認識はまだ無い（#195。段階は親 #192）」（関数名の一覧は書かない） |
| architecture.md:28 | `catalog::words` → `athena_sql::words`、`catalog::skip_leading_trivia` → `athena_sql::skip_leading_trivia` |
| unmeasured.md:11 | `catalog::words` → `athena_sql::words` |
| コード内の doc・コメント | content_type.rs:69、target_table.rs:39（「`catalog.rs` の」→「`athena_sql` の」）・:75・:106、iceberg_partitions.rs:10、classification.rs:3・:76・:105・:111、target_table/tests.rs:334（接頭辞だけ） |

直さない: 歴史的な記述（classification/tests.rs:218、target_table/tests.rs:385、design-checklist.md:17・:47、measurements/statements.md:64、decisions.md の出典番号）、残留側（CLAUDE.md:75、architecture.md:15・:29、decisions.md:66・:74、tests/describe.rs:312、tests/table_format.rs:185-186）、
移した関数の `///`（今もある athena-local のファイルを指している。直すと足場 5 が NG）、テスト名 target_table/tests.rs:332、CLAUDE.md:34、README・CHANGELOG・docs/*.md。
残骸の grep（フェーズ 2 の後に 0 件）: `git grep -nE 'catalog::(\{|words|skip_|comment_end|unquote)|`catalog\.rs` の `skip|SQL の字句処理）|字句処理は #194' -- src docs CLAUDE.md crates`（実装前の今のツリーでは移動側の参照だけに当たることを確認する）。

### 依拠する既存機構の前提表

| 前提 | 破らないか | 保証元 |
|---|---|---|
| `athena_sql::` のパスで呼べる | 破らない | Cargo.toml:16（path 依存）、crates/athena-sql/Cargo.toml:2。コードからの参照はまだ 0 件なので最初のビルドで確かめる |
| 引数なしの fmt / clippy / test が crate にも効く | 破らない | Cargo.toml:13 `default-members`、decisions.md:63、ci.yml:21-24 |
| edition と lint の設定が両 crate で同じ | 破らない | Cargo.toml:4・crates/athena-sql/Cargo.toml:5（2024）。`[lints]`・`#![...]`・rustfmt.toml・clippy.toml 無し |
| `skip_name_part` は crate 内だけ | 守る | decisions.md:239。`skip_qualified_name` と一緒に移る |
| crate は外部依存を持たない・Cargo.lock は変わらない | 守る | decisions.md:68。移す関数は prelude だけ。足場 7b |
| athena_local の公開 API は変わらず `tests/` から字句関数は見えない | 破らない | src/lib.rs:5 `mod catalog;` private、`pub use` 無し。足場 9 |
| doctest は 0 件のまま | 破らない | 移す `///` にコードブロック無し。足場 1（Doc-tests athena_sql 0） |
| README・CHANGELOG・docs を変えない | 守る | decisions.md:24・:69 |

### ミューテーション確認で壊す候補（7-4。lib.rs を壊して別 crate のテストが落ちる = 配線の証拠）

| # | 壊し方 | 落ちるべきテスト |
|---|---|---|
| M1 | `skip_trivia` の空白の腕に `\| b'.'` を足す | `catalog::tests::引用符付きのカタログ名を別名にして桁を空白で揃える`、`operation::target_table::tests::parse_qualified_name_と_catalog_skip_qualified_name_は同じ書き方を受け付ける`、athena_sql `tests::skip_qualified_name_はドットで繋いだ修飾名を読み飛ばす`、tests/catalog.rs `修飾名のカタログは別名にして送り_構文チェックと実行情報は受け取った_sql_のまま` |
| M2 | `unquote` が `identifier.to_string()` をそのまま返す | `catalog::tests::識別子の二重の引用符は中身として比べて別名では二重にする`、`operation::target_table::tests::parse_target_table_は引用符付きの第_1_パートを中身のまま使う`、`operation::type_spelling::tests::hive_は_row_のフィールド名の引用符を外す`、`operation::iceberg_partitions::tests::partition_row_は引用符付きの列名の引用符を外す` |
| M3 | `skip_keyword` の「続きが識別子の文字なら不一致」の分岐（:173-175）を消す | `operation::target_table::tests::parse_target_table_はキーワードの直後が識別子の文字かどうかで一致を決める` |
M4（`comment_end`）・M5（`words`）は外した（M5 は crate 内のテストでも落ちて越境の証拠にならず、M4 の配線は M1 と同じ。計画レビュー 4-4）。
注意: 読み飛ばしを弱める向き（改行を外す等）に壊すと `words` の外側のループが進まず無限ループになるので使わない。対象テストは単独フィルタで走らせる。

### 先行できる作業

- 検証の足場 `194-verify/check.sh`: 作成済み、着手前で全 ok、壊し試験済み、Plan の指摘（K-1・K-2）を反映済み（`+` の空行 1 行以内、`P1_SHA` と 7c）。計画レビューに渡す
- 実機検証の足場 `194-verify/e2e.sh`: 作成済み、着手前で全 PASS（対照ケース込み）。Step 9 で移動後に流す
- フェーズ 2 の文案: 上の表のとおり。フェーズ 1 の委譲の待ち時間に用意する

### 該当なしとして省く項目

直列化境界・正規化境界・境界の名前・意味論マトリクス・人間検証リスト・追加するテスト・一致台帳（ヘルパの複製は無い）。

### 独立レビューに渡すもの

`git diff 0137b93...HEAD -- . ':(exclude).claude/issue-notes'` と、検証コマンド: `AFTER=1 P1_SHA=<フェーズ 1 の SHA> .claude/issue-notes/194-verify/check.sh`、`tools/dev.sh cargo test --locked`。

### 範囲外（Plan K-7）

移動後、crate の中には `skip_keyword`・`unquote`・`skip_quoted`・`comment_end` を直接呼ぶテストが無く、守るのは athena_local 側のテストだけ。移動前も同じ状態（catalog.rs にも直接のテストは無かった）なので本 issue では扱わない → 範囲外の発見に載せる。

## 計画レビューの結果（Explore 1 本・opus、2026-09-25 11:00）

| # | 観点 | severity | 採否 | 内容 |
|---|---|---|---|---|
| 4-1 | 足場 | high | **採用** | check.sh 7a が `P1_SHA` 無しだと BASE..HEAD のコミット同士を比べ、コミット前の正しい実装（委譲先の verify）で必ず NG。`${P1_SHA:+"$P1_SHA"}` にして作業ツリーと比べる形に直した。design-checklist の #175 の行に出典を追加 |
| 2-1 | 一貫性 | low | 採用 | 残骸 grep が decisions.md:75-77 の形に当たらない → パターンを足した |
| 4-2 | 足場 | low | 採用 | e2e.sh のケース 4（DROP TABLE）は `RESULTS=none` では target_table を通らない → 注記を「後始末」に直し、target_table は result-content-type/verify.sh の SHOW CREATE TABLE で見る |
| 4-4 | 削減 | low | 採用 | M4・M5 を外し M1〜M3 にする（越境の証拠にならない・M1 と重複） |
| 4-5 | 削減 | low | 採用 | architecture.md:8 と lib.rs の `//!` から関数名の一覧を外す（コードが正、#195 で古くなる） |
| 1・2-2・3・4-3 | 契約 / 規則 / 構造 / 許容リスト | — | 0 件 | fmt の折り返し（chain_width 60 超で縦のまま）、`use super::*` の解決、clippy の lint、doctest 0、参照の仕分け、`next_is_dot` を移す案との比較、項目 5 の正規化を確認 |

## フェーズの記録

（7-0 の答えと結果）

## レビュー実施記録

| 工程 | 回数 | 本番影響のある妥当指摘 |
|---|---|---|

## Step 8 の結果

## dismissed

## deferred

| 指摘 | severity | file | 再トリアージ | 起票 |
|---|---|---|---|---|

## 覆した事前判断

| 元の判断 | 新しい判断 | 理由 | 影響 | ユーザーに聞いたか |
|---|---|---|---|---|

## 実機検証

## マージ後の周知事項

## 範囲外の発見

| 内容 | 出どころ | 起票 / 対応しない理由 |
|---|---|---|

## 委譲の記録

| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| Step 3 調査 | 委譲（Explore 2 本・opus） | — |
| 着手前の基準値の採取 | 自分 | Bash 1 本（指示を書く方が長い） |
| 検証の足場（check.sh・e2e.sh） | 自分 | 2 ファイル、#193 の足場の再利用。Explore・Plan の待ち時間に作成（指示を書く方が長い） |
| Plan | 委譲（Plan・opus） | — |
| 計画レビュー | 委譲（Explore 1 本・opus） | — |

## 出荷

## 時系列

| 時刻 | 出来事 |
|---|---|
| 2026-09-25 10:23 | ブランチ main-194 を作成、過去ノート削除 `bafc892` |
| 2026-09-25 10:25 | 着手前の基準値をホストで採取（背景）。Explore 2 本（A: 移動単位と依存、B: 慣習と文書の参照）を起動。先行作業: 検証の足場 `194-verify/check.sh` の作成 |
| 2026-09-25 10:28 | Explore A が返る。足場を着手前のツリーで流して全 ok（9 項目）、壊し試験 4 パターンで期待どおり NG（`SKIP_CARGO=1` を追加） |
| 2026-09-25 10:32 | Explore B が返る。Plan（opus）を起動。先行作業: 実機検証の足場 `194-verify/e2e.sh`（compose の trino + memory カタログ、4 ケース）を作成し、着手前のツリーで toolbox から流す（背景） |
| 2026-09-25 10:38 | e2e の足場が着手前のツリーで全 PASS（1 回目は Trino の起動待ちと ClientRequestToken の欠落で失敗、足場側を直した。`tools/dev.sh` は `SKIP_BUILD` を渡さない） |
| 2026-09-25 10:50 | Plan が返る（暫定案どおり。足場との食い違い 2 点 K-1・K-2 を足場に反映、e2e に対照ケースを追加して全 PASS）。計画をノートに転記、計画レビュー（Explore 1 本・opus）を起動 |
| 2026-09-25 11:00 | 計画レビューが返る（high 1: 足場 7a がコミット前の作業ツリーを見ない。low 4）。全件採用して足場・計画・design-checklist に反映、計画確定コミット |
