# quick #157 GetQueryExecution の QueryExecutionContext.Catalog を本物と同じ小文字で返すか決める
- 入力: issue #157。モード: standalone。ブランチ main-157、着手前 SHA 5d6cb51
- ゲート: サイズ 4 行・1 ファイル（≤50・3）／呼び出し元 1（`query_execution.rs` の QueryExecution 組み立てのみ）／契約・新規性: 通過（応答の表示を本物に合わせる修正。docs・CHANGELOG 同コミット）／事実: 実測で確定（c1）／判断: **外れた**（小文字化するか issue が決めていない）→ 1 問聞き「先に実測」。実測で実在しない名前まで無条件に小文字化と分かり、本物に合わせる方針（CLAUDE.md）から小文字化を採用。Database は送ったまま
- 時系列: 着手 16:30 ／ 質問 16:33 → 回答「先に実測」 ／ 実測スクリプト c1 作成・ドライラン・コミット 16:37（5de7107） ／ 実測依頼 16:38 → 本物の run-20260924-074911 完了 16:51 ／ red 16:52 ／ green 16:52 ／ コミット 16:55
- 検証: `tools/dev.sh cargo fmt --check`・`cargo clippy --all-targets --locked -- -D warnings`・`cargo test --locked` ok（338 passed）／ミューテーション: 小文字化を `to_string()` に戻す → 落ちた／実測: 1 ラウンド（c1、本物への StartQueryExecution 10 本 + ListDataCatalogs 1 本、DDL 無し）。コミット前の実名 grep がテストのコメントの DB 名を 1 件拾い、直した
- 自己レビュー: 分岐 0 件（Option の map のみ。None は既存経路）／エラーの出口 該当なし／呼び出し元 1 箇所。冪等性の照合（store.rs の Fingerprint）と Trino への送信（execution.rs の trino_catalog）は生の値のままでテストで固定
- 打ち切り・escalate: はしご（判断 → 1 問聞く → 実測 1 ラウンド）。提案なし。範囲外の発見: 省略した Catalog／Database を athena-local は既定値で返すが本物はキー無し → #167 に起票
- 予算の実績: Bash 約 17 本（うち実測 6 本）、所要 約 1487 分（実測待ち約 13 分を除く）、サブエージェント 1 本（止めない独立レビュー）
- 出荷: ブランチ main-157（PR 番号は `gh pr list --head main-157`）
