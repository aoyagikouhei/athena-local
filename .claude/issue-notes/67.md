# quick #67 tests の wait_for と run_query も回数ではなく経過時間で諦める
- 入力: issue #67。モード: standalone。ブランチ main-67、着手前 SHA 1568ae3
- ゲート: サイズ 0 行・0 ファイル（本番コード無し。tests は 3 ファイル変更 + `tests/waiting.rs` 新規）（≤50・3）／呼び出し元: `wait_for` 15・`run_query` 82 だがシグネチャ不変で触らない（≤5・同一モジュール）／契約なし・新規性なし（`wait_until_gone` の `Instant` + `loop` が先行例）・事実（`statement_delay(3 秒)` と 2.5 秒後に成り立つ閉包で旧実装が落ちる red を作れた）・判断（上限 30 秒は #61 でユーザーが決めた `GONE_DEADLINE` と同じ値。新しい既定値ではない） → 通過
- 時系列: 着手 21:19 ／ red 21:21 ／ green 21:21 ／ コミット 21:22
- 検証: `cargo fmt --check` ok／`cargo clippy --all-targets --locked -- -D warnings` ok／`cargo test --locked` 全バイナリ 0 failed（lib 174 passed、waiting 2 passed）／ミューテーション: `WAIT_DEADLINE` を 2 秒に縮める → `waiting` の 2 本が落ちた
- 自己レビュー: 分岐 4 件（`run_query`・`wait_for` の返る側は既存 97 箇所の呼び出しが踏む。諦める側 2 件はミューテーションで踏んだ）／エラーの出口: panic の文言に待った時間（`run_query` は最後の状態も）を足しただけで、テストの失敗表示にしか出ない／呼び出し元 97 箇所: シグネチャ・成功時の挙動不変（`cargo test --locked` 全通過）。`results.rs`・`failed_results.rs` の同型ループ 2 件は上限に達しても直後の assert で落ちるので専用の red は作らない
- 打ち切り・escalate: 提案なし
- 予算の実績: Bash 約 9 本、所要 3 分、サブエージェント 1 本（止めない独立レビュー）
- 出荷: standalone、ブランチ main-67
