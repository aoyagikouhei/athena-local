# issue #116: tools/e2e/minio/verify.sh の分割と、tls-proxy を証明書なしで上げてしまう問題

## 基本情報

- ベース: main（30eb4eb）。ブランチ: main-116。着手前 SHA（レビューの base）: 30eb4eb（34122e5 は過去ノートの削除だけ）
- 経路: 小規模 issue の経路 + 挙動を変えない移動・分割の経路（references/move-refactor.md）。2 フェーズ

## 事前に確認した事実

1. issue の 2 点目（`docker compose up -d` がサービスを指定せず tls-proxy まで上げる）は、#126 の compose 統合（3da9706）で
   `SERVICES=(trino minio minio-init)` を down / up に渡す形になり、解消済み（verify.sh:28・main の down/up）。今回は何もしない
2. 着手前の verify.sh は 679 行・関数 23 個・「実測」を含む行 4 行。shellcheck は toolbox にも CI にも無いので構文は `bash -n` で見る
3. 先行例: tools/e2e/jdbc-drivers/lib.sh（#115）。`# shellcheck shell=bash` + 先頭コメントで呼び出し元の約束を書く形
4. 移すヘルパが読む verify.sh の変数: REPO_ROOT・BINARY・TRINO_BASE・MINIO_ENDPOINT・ATHENA_BASE・BUCKET・OUTPUT_LOCATION・
   EVIDENCE_DIR・ATHENA_LOG・BUILD_LOG（呼び出し時に読むので、source の位置は代入の後ならよい）。ATHENA_PID は start_athena_local が書く
5. 参照: tools/e2e/minio/README.md の構成、cases-dml-retention.sh の先頭コメント（ヘルパの出どころ）。docs/・CLAUDE.md は verify.sh の
   呼び方だけで、ヘルパの位置には触れない（書き換え不要）

## 計画

- フェーズ 1（移動）: `tools/e2e/minio/lib.sh` を新設し、verify.sh の汎用ヘルパを移す。挙動・順序・文言は 1 行も変えない
  - lib.sh（316 行）: 結果の配列・log・record・print_table・trino_exec・wait_for_trino・wait_for_bucket・build_athena_local・
    start_athena_local・athena_call・athena_start_query・athena_start_query_raw・athena_wait・mc_stat・mc_exists・mc_get・
    check_body・check_metadata・read_metadata_head（18 関数）
  - verify.sh（372 行）: 冒頭の設定・cleanup と trap・ケース（run_case・run_merge_case・probe_add_columns_spelling）・main（5 関数）
  - lib.sh の source は cases-dml-retention.sh より前（配列の宣言を持つため）かつ trap より前
- フェーズ 2（文書）: README の構成に lib.sh を足し、cases-dml-retention.sh の先頭コメントのヘルパの出どころを直す
- 検証の足場: `.claude/issue-notes/116-verify/check.sh`（構文・関数の集合と二重定義・lib.sh の関数の割り当て・実測の行・
  正規化 diff（許すのは lib.sh の先頭コメントと source の 2 行だけ）・source の順序）。わざと壊して NG が出ることを確認済み
  （lib.sh のコメント 1 行を消す → 正規化 diff が NG、source を cases の後ろへ → 順序が NG）
- 実機: 移動の前後で `tools/dev.sh tools/e2e/minio/verify.sh` を流し、結果表（24 項目）が同じであること

## 設計判断

- 移す範囲は issue の例（record・trino_exec・mc_*・athena_*）に、同じ性質の汎用ヘルパ（log・print_table・wait_*・
  build/start_athena_local・check_body・check_metadata・read_metadata_head）を足したもの。ケース固有の判定（run_case 等）と
  後始末（cleanup。KEEP_UP・SERVICES を読む）は verify.sh に残す
- 設定変数は verify.sh に残す（jdbc-drivers/lib.sh は変数も持つが、あちらは 3 本が共有するため。こちらの呼び出し元は 1 本で、
  変数を移すと「移動だけ」の検証が効かなくなる）
- 6b: 判断が要る論点 0 件（2 点目は解消済みの事実で、issue 本文を覆す判断ではない）のため提示のみで進める

## 覆した事前判断

なし（issue の 2 点目は #126 で解消済みだったので手を入れていない）

## 実機検証

| | 結果 |
|---|---|
| 移動前（30eb4eb 相当のツリー） | 24 項目すべて PASS（exit 0、31 秒） |
| 移動後 | 24 項目すべて PASS（exit 0）。移動前と項目・状態が一致。スクリプトのエラー（command not found・line N:）0 件 |

## レビュー実施記録

- 計画レビュー: 省略（移動の範囲と検証の足場が機械的に閉じているため。正規化 diff・関数集合・実機の 24 項目で覆う）
- 独立レビュー: 1 回（sonnet、止めない）。本番影響のある妥当指摘 0 件（指摘なし）。報告対象外の cosmetic として verify.sh の空行 2 連続（run_case の後。切り出しの境目の取り方の誤り）を挙げたので、出荷記録のコミットで 1 行消した
- CI: check・e2e とも pass

## 出荷

- PR #144（Closes #116）。独立レビューの結果は PR にコメントした。deferred 0 件なので issue へのコメントは無し
- 出荷判定: 検証の green・実機 24 PASS 一致・独立レビューの本番影響 0 件

## 範囲外の発見

なし

## 委譲の記録

| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| 調査・計画 | 自分 | 対象が 1 ファイルで、関数一覧と参照の grep 数本で閉じた |
| 移動の実装 | 自分（小規模経路） | 行範囲の sed 1 本。指示を書く方が長い |
| 独立レビュー | 委譲（sonnet） | 規則どおり |

## 時系列

- 2026-09-24 05:28 34122e5 過去のノートを消して着手
- 2026-09-24 05:29 移動前の e2e（24 PASS）
- 2026-09-24 05:30 lib.sh へ移動、check.sh 全 ok、壊して NG を確認
- 2026-09-24 05:31 移動後の e2e（24 PASS、移動前と一致）
- 2026-09-24 05:31 ae930e9 文書の追随
- 2026-09-24 05:34 PR #144、独立レビュー指摘なし、CI pass
