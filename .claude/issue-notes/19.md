# issue #19 src/operation.rs を分割する

## 基本情報

- issue: #19「src/operation.rs を分割する」
- ベースブランチ: `main` / 作業ブランチ: `main-19`
- **着手前 SHA: `e4c1826`**（レビューの `base` の正）
- 完了条件（issue 本文）: 1 ファイルが 400 行を超えない構成／`cargo test --locked` が無変更で green／README と CHANGELOGS は変えない／CLAUDE.md のアーキテクチャの節を新しい構成に合わせる
- 制約（issue 本文）: 挙動を変えない移動と抽出だけで割る（分岐・エラー経路・共有状態の扱いは 1 行も変えない）

## quick-implement からの引き継ぎ

quick のゲート判定で範囲外（変更サイズ 863 行／新規性 `src` 配下に新ファイル／判断 分割軸の選択）。コミット `32b7cb7` で引き継ぎを残し、implement-issue に escalate した。issue #19 にも同じ内容をコメント済み（<https://github.com/aoyagikouhei/athena-local/issues/19#issuecomment-5723767032>）。

## 事前に確認した事実

Explore 3 本の返答の全文はセッション限りのスクラッチパッドにあった。以下は設計判断・計画・7-7 が使った事実だけ。実装で確定したものは「フェーズの記録」にある。

### 分割の材料（Explore A）

- `src/operation.rs` 863 行。use 3-25、定数 11 本 27-67、実行系 69-394、参照系 395-716、`#[cfg(test)]` 717-863（147 行）。
- pub 関数は 6 本のみ。`start_query_execution`(54 行) と `list_work_groups`(51 行) だけが 50 行超。
- **`words` が実行系と参照系の境界をまたぐ唯一の関数**。`metadata_query_id`・`statement_type`・`substatement_type` から呼ばれ、`crate::catalog::skip_leading_trivia` を使う。
- 定数 11 本はすべて単一関数からのみ参照され（`WORK_GROUP_STATE` のみ参照系 2 関数で共有）、**テストからの参照は 0 件**。実行系専用 5 本・参照系専用 6 本。
- `#[cfg(test)]` の 6 テストは**全て参照系の 4 関数**（`statistics`・`update_count`・`statement_type`・`substatement_type`）が対象。実行系のユニットテストは 0 件。→ これが案 (b) を不成立にした主因（参照系に 147 行が集中する）。
- 型の越境（両ファイルが同じ `use` を書けば済み、可視性の問題にはならない）: `App`・`Execution`・`Outcome`・`ResultConfiguration`・`ResultLocation`・`QueryExecutionContext`・`ResultsMode`・`convert`・`invalid_request_with_code`/`ok`/`parse`・`Bytes`/`Response`。

### リポジトリの慣習（Explore B）— #30 でも同じ材料が要る

- **`src/` にディレクトリ + `mod.rs` の先行例は 0 件**だった（`find src -type d` は `src` 自身のみ）。本 issue が最初。
- **`pub(super)` の使用例も 0 件**だった。モジュール間共有は `pub(crate)` が慣習（`catalog.rs:102`・`results.rs:91`・`convert.rs:54`・`handler.rs:29`・`metadata.rs:15` の 5 件、いずれも**別モジュール間**）。同一モジュール内のサブモジュール間という状況が初だったため、慣習は決め手にならず、公開範囲最小の原則で `pub(super)` を選んだ。
- `lib.rs:4-16` の `mod` 宣言はモジュール名のアルファベット順。`pub mod` は `athena`・`config`・`trino` の 3 つだけ。
- `//!` は 14 ファイル中 10 ファイルにあり、1〜2 文の「〜する。」形式。
- `Cargo.toml` に `[lints]` 無し、`clippy.toml` 無し、クレート属性 `#![...]` 無し。**`missing_docs`・`unreachable_pub` はデフォルト無効**（→ doc コメントが消えても検出されない、という R1 の前提）。踏みやすいのは `dead_code` と `unused_imports`。
- `convert.rs`(869) / `results.rs`(610) / `store.rs`(614) / `config.rs`(441) も 400 行超だが、本体（テスト前）はそれぞれ 419 / 281 / 281 / 223 行。**本体が 400 行を超えるのは `convert.rs` だけ** → #30 に起票。

### 外部への波及（Explore C）

- 呼び出し元は `src/handler.rs:12` と `35-40`、`src/lib.rs:11` のみ。**`tests/` からの参照は 0 件**（14 ファイル 3287 行が全て HTTP 越し）。
- CLAUDE.md の書き換えが要る行: 29, 30, 31, 32, 33, 44, 46。README.md・CHANGELOGS.md は 0 件。他ファイルのコメント 5 箇所が `operation.rs` をファイル名で名指し。
- CI: `cargo fmt --check` / `cargo clippy --all-targets --locked -- -D warnings` / `cargo test --locked`。`Dockerfile:6` は `COPY src ./src` なので分割で修正不要。
- 既存の肥大化ファイルを分割し直した単独リファクタコミットの先例は直近 30 件には無かった。

### 転記で落としたもの

- Explore A の `use` ごとの全使用箇所 → 越境する型の一覧だけ残した（過不足は `clippy` の `unused_imports` が機械的に検出するため）。
- Explore B の convert/results/store の責務の塊 → 本 issue の範囲外。#30 の本文に書いた。
- Explore C の各 src ファイルの追加コミット SHA → コミットメッセージの文体の確認だけ残した。
- Plan の案 (a)（7 ファイル）の内訳行数 → Step 4 の選択肢の材料として使い、採らなかったので落とした。
- Plan のフェーズ分割案（6 フェーズ）→ Step 4 でユーザーが選んだ 2 フェーズに置き換えた。
- Plan は `words` を `pub(crate)` にするとしていた → 公開範囲最小の原則で `pub(super)` に変えた（設計判断 4）。

## 設計判断

| # | 論点 | 決定 | 理由・根拠 |
|---|---|---|---|
| 1 | 分割軸 | **`src/operation/` に `mod.rs` + 4 ファイル**（`execution.rs` / `query_execution.rs` / `work_group.rs` / `classification.rs`） | Step 4 でユーザーが選択。issue の案 (b)「実行系／参照系の 2 分割」は参照系が 516 行（関数 322 + テスト 147 + 定数 26）で完了条件の 400 行を超えて成立しない。案 (c) の定数切り出しを足しても 26 行しか逸せない。案 (a) はオペレーションごと 7 ファイルになるが、複数オペレーションから呼ばれる共有ヘルパ 5 本（`words`・`statement_type`・`substatement_type`・`engine_version`・`unknown_execution`）を `mod.rs` に集めることになり `mod.rs` が 195 行に育って軸が濁る |
| 2 | `src/` 初のディレクトリ導入 | **受け入れる** | `src` はフラット構造で先行例 0 件（Explore B）。だが #2 の決定「`App`／`Response` を扱うディスパッチ本体は `operation` に集約」を守るには `operation/` 配下に置くしかない。トップレベルの兄弟モジュール（案 d）は `lib.rs:11` が 4 行に増え、`handler.rs:35-40` の 6 呼び出しがモジュールごとの書き分けを要求され、集約の軸に反する |
| 3 | `words`(639-644) の置き場所 | **`classification.rs` に置く** | 実行系と参照系の境界をまたぐ唯一の関数。呼び出し元 3 本のうち `statement_type`(649)・`substatement_type`(662) が同じファイルに集まり、`execution.rs` の `metadata_query_id`(353) だけが越境になって最小 |
| 4 | `words`・`statement_type`・`substatement_type` の可視性 | **`pub(super)`**（計画レビューに投げる論点） | 公開範囲は最小が原則で、`operation` モジュールの外から呼ぶ必要が無い。既存の `pub(crate)` 5 件（`catalog.rs:102`・`results.rs:91`・`convert.rs:54`・`handler.rs:29`・`metadata.rs:15`）はすべて**別モジュール間**の共有で、同一モジュール内のサブモジュール間という状況自体がこのリポジトリで初。`pub(super)` の先行例 0 件は `pub(crate)` をこの用途に使う先行例 0 件と等価なので、慣習は決め手にならない |
| 5 | `#[cfg(test)]` 6 本の移し先 | **対象関数の定義先に追従**（統計 3 本と `update_count` 1 本 → `query_execution.rs`、`statement_type` 1 本と `substatement_type` 1 本 → `classification.rs`） | src 全 10 ファイルが「テスト対象と同じファイルに `mod tests`」の形（Explore B）。テスト名は変えないので `cargo test <部分一致>` の絞り込みは影響を受けない |
| 6 | 50 行超の 2 関数（`start_query_execution` 54 行・`list_work_groups` 51 行）の内部抽出 | **やらない。起票もしない** | issue の完了条件は「1 ファイル 400 行以内」だけで関数長に触れていない。1 関数の内部を小関数に割るのは分岐の形（早期 return の位置・入れ子）を書き換える操作で、「分岐・エラー経路を 1 行も変えない」という issue の制約の保証が弱くなる。関数 50 行は統括役スキルの目安であってこのリポジトリの規則ではない（`.claude/design-checklist.md` にも無い） |
| 7 | 他ファイルのコメント 5 箇所（`catalog.rs:14`・`store.rs:53`・`store.rs:125`・`athena.rs:22`・`athena.rs:211`）の `operation.rs` 表記 | **直す**（フェーズ2） | 分割後は `operation.rs` というファイルが存在しなくなり、コメントが事実として誤りになる。文言だけの変更で挙動に影響しない |
| 8 | CLAUDE.md:29 の「4 つのオペレーション」 | **「6 つ」に直す** | #9 で `GetWorkGroup`／`ListWorkGroups` が増えて実際は 6 つ（`pub` 関数 6 本）。「新しい構成に合わせる」の一環 |
| 9 | フェーズ数 | **2 フェーズ** | Step 4 でユーザーが選択。移動だけなので検証は結合テスト 3287 行が丸ごと担保する。フェーズ2 以下なので小規模 issue の経路（独立レビュー 1 回のみ）に乗る |
| 10 | TODO.md のオペレーション追加との順序 | **分割を先にやる** | `ListDatabases` ほかは Trino に別クエリを投げる新しい性質で、実行系にも参照系にも完全には一致しない。`operation/` 配下に新ファイルを足す形で収まるので手戻りが少ない。逆順にすると `operation.rs` が 900 行超のまま複数コミット肥大化し続ける |

## 計画

実装済み。行番号ベースの割り当て表と前提表の詳細は、実装（`18500c9`）とフェーズの記録で置き換わったので畳んだ。残すのは方針と、次の分割 issue（#30）が使う部分だけ。

### フェーズ1: `operation.rs` を `operation/` の 5 ファイルに割る

| ファイル | 責務（1 つ） | 見込み → 実測 | 入れたもの |
|---|---|---|---|
| `mod.rs` | `mod` 宣言と 6 公開関数の再エクスポート | 18 → **10** | 旧 `//!` の引き継ぎ、`mod` 4 本、`pub(crate) use` 3 行 |
| `execution.rs` | StartQueryExecution 一式 | 362 → **362** | 定数 5 本、`start_query_execution`・`submit_response`・`client_request_token`・`context_defaults`・`result_location`・`spawn_query`・`write_result`・`write_failure`・`write_metadata`・`metadata_query_id`・`run` |
| `query_execution.rs` | GetQueryExecution／GetQueryResults／StopQueryExecution | 237 → **241** | `DEFAULT_MAX_RESULTS`、`get_query_execution`・`get_query_results`・`stop_query_execution`・`not_succeeded`・`to_query_execution`・`statistics`・`update_count`・`unknown_execution`、テスト 4 本 |
| `work_group.rs` | GetWorkGroup／ListWorkGroups | 129 → **127** | 定数 5 本、`engine_version`・`get_work_group`・`list_work_groups` |
| `classification.rs` | 文の種類の判定 | 168 → **164** | `words`・`statement_type`・`substatement_type`、テスト 2 本 |

**切り出しの単位**（計画レビューの R1 で決め直した。行番号の範囲で切り出さない）:

> 関数・定数を、その直前の `///` ブロックと属性を 1 単位として運ぶ。

行番号表は `fn` / `const` の行から数えるため、直前の doc コメントがどの範囲にも入らない。実際に 7 箇所・計 14 行が漏れていた（`engine_version`・`get_work_group`（5 行）・`not_succeeded`・`update_count`（3 行）・`words`・`statement_type`（2 行）・`substatement_type`（3 行）の doc）。`missing_docs` lint が無いので消えても 3 コマンドは沈黙する。教訓は `.claude/design-checklist.md` に 1 行落とした。

`src/operation.rs:1` の `//!` は `mod.rs` に引き継ぐ。`src/lib.rs:11` と `src/handler.rs:12,35-40` は無変更。

**越境する呼び出し（4 箇所だけ）**: `execution.rs::metadata_query_id` → `super::classification::words`、`query_execution.rs::to_query_execution` → `super::classification::statement_type` / `substatement_type`、`query_execution.rs::update_count` → `super::classification::statement_type`。

**依拠する既存機構の前提表**（実装で全行が検証された）

| 機構 | 暗黙の前提 | 破らないか | 誰が保証しているか |
|---|---|---|---|
| `mod operation;` の 1 行宣言 | `operation.rs` か `operation/mod.rs` のどちらかがあれば解決する | 破らない | Rust のモジュール解決規則。リポジトリ側の保証は無く `cargo build` が唯一の検証者（**未保証**だった → 実装で確定） |
| `handler.rs` の `operation::xxx` 呼び出し | `operation` 直下に 6 公開関数が見える | 破らない（`pub(crate) use` で再エクスポート） | `src/handler.rs:12,35-40`。未解決 import を `cargo build` が即検出 |
| `cargo test <部分一致>` の絞り込み | モジュールパスが変わってもテスト関数名の文字列一致で拾える | 破らない（テスト名は無変更） | `CLAUDE.md:14-16` のコマンド例 |
| CI の `-D warnings` | 未使用 import があれば即失敗 | 分割後に不要になった `use` を検出する | `.github/workflows/ci.yml` |
| 結合テストが `operation` に直接依存しない | `tests/` は `router()` を HTTP 越しに叩くだけ | 破らない | `tests/` に `operation::` の参照 0 件 |

**テスト: 新しいテストは足さない。** 挙動を変えない移動なので「既存テストが無変更で green」を根拠にする。ただし 3 コマンドをすり抜ける失敗が 2 種類ある（doc コメントの消失／`mod tests` の宣言漏れでテストが実行されなくなる。後者は件数が減るだけで exit code 0）。どちらも**計数の突き合わせ**で検出する。移動前の基準値（`e4c1826` で実測）:

| 計数 | 基準値 | 移動後の期待 |
|---|---|---|
| `///` 行 | 65 | 65 |
| `実測` を含む行 | 32 | 32 |
| `#[test]` | 6 | 6 |
| `//!` 行 | 1 | 5 |
| `cargo test --locked` のテスト総数 | **218**（うち `--lib` 122） | 218（うち 122） |

**ミューテーション確認**: `classification.rs` の `statement_type` で `"OPTIMIZE"` を DDL の腕から DML の腕に移す → `statement_type_は先頭のキーワードで決まる` が落ちる。

### フェーズ2: 文書を新しい構成に合わせる

`CLAUDE.md` の 29（`operation/` の内訳と「6 つ」）・30-33（`execution.rs` の所属）・44（`StatementType`／`SubstatementType` は `classification.rs`、`UpdateCount` は `query_execution.rs`）・46（`operation/execution.rs`）。他ファイルのコメント 5 箇所（`catalog.rs`・`store.rs` 2・`athena.rs` 2）。README.md・CHANGELOGS.md は変えない（挙動が変わらないため）。

### 一致台帳

**無し。** ロジックの複製を作らない（既存関数をそのまま 1 箇所ずつ移す）。

### 人間検証リスト

**無し。** 内部のファイル配置だけを変える挙動不変のリファクタで、新しい経路は生まれない。

### 計画から外したもの

1. `start_query_execution`(54 行)・`list_work_groups`(51 行) の内部抽出。分岐の形を変えるリスクが、400 行制約にも CI にも寄与しない利得を上回る。起票もしない（関数 50 行はこのリポジトリの規則ではない）。
2. 定数を `messages.rs` に集める案 (c) の単独採用。11 定数はすべて単一関数からしか参照されず、集めると「なぜこの文言か」を読むのに 2 ファイル往復が要る。
3. `git mv` を単独フェーズにすること。中間状態の `mod.rs` は次のコミットでほぼ空になる。
4. 前提表の 2 行（計画レビューの削減指摘で採用）。「`pub(crate)` の慣習」は設計判断 4 の言い換え、「`result_large_err` の `Box<Response>`」は issue の制約から自動的に真になる自明事項。
5. ミューテーション候補 5（`words` の `pub(super)` を外す）。`cargo build` が自動で検出する種類。

## 計画レビューの結果

Explore 3 本（契約・呼び出し元／構造・一貫性／削減・テスト）。

### 採用

| # | 指摘 | 出どころ | 対応 |
|---|---|---|---|
| R1 | **「移すもと」の行範囲が関数直前の `///` ブロックを体系的に取りこぼす。**行 46・410-414・548・629-631・638・646-647・658-660 の計 14 行がどのファイルにも割り当てられておらず、字面どおり切り出すと実測の記録が静かに消える。`missing_docs` lint が無いので 3 コマンドは沈黙する | 契約・構造の**2 本が独立に指摘**（統括役が `sed` で全箇所を裏取り済み） | 行番号を広げるのではなく、切り出しの正を「関数・定数とその直前の `///` ブロックを 1 単位で運ぶ」と明記。行番号表は目安と明示。取りこぼす 14 行を表で列挙。加えて `///` 65 行・`実測` 32 行の計数で機械的に守る |
| R2 | `src/operation.rs:1` の `//!` は行き先を決めないと消える | 削減 | `mod.rs` に引き継ぐと明記。`//!` の計数も検証に入れる |
| R3 | **テストが移動先で実行されなくなっても `cargo test` は green**（件数が減るだけで exit code 0） | 削減 | 移動前後のテスト総数を突き合わせる。統括役が実測してベースラインを確定（**218**、うち `--lib` 122。レビュアーの自己申告のままにしない） |
| R4 | 前提表 7 行のうち 2 行が前提表の体を成していない／自明 | 削減 | 2 行を落とした |
| R5 | ミューテーション候補 5（`pub(super)` 剥がし）は `cargo build` が自動で検出する種類 | 削減 | 候補から落とした |

### 棄却

| 指摘 | 出どころ | 棄却の理由 |
|---|---|---|
| フェーズ2 の「他ファイルのコメント 5 箇所の修正」を外すべき（issue の完了条件は CLAUDE.md だけを要求しており、直さなくても本番で起きることは 0） | 削減 | 分割後 `operation.rs` というファイルは存在しなくなるので、5 箇所は**事実として誤った記述**になる。「来月の本番で何が起きるか」という削減の基準はコメントの正確性には元々当てはまらず、同じ基準を当てれば CLAUDE.md 7 行の修正も外れてしまう（それは issue の完了条件そのもの）。1 行ずつの文字列置換 5 箇所でレビューコストはほぼゼロ、フェーズ2 の文書コミットに同梱できる。issue の完了条件が CLAUDE.md だけを挙げているのは、起票時に他ファイルのコメントを把握していなかったためと読むのが自然 |
| 新 4 ファイルに `//!` を書くのは慣習上の義務ではない（src 14 中 4 ファイルに `//!` が無い） | 削減 | 義務でないことは事実として受け入れる。ただし分割の目的が「人間が 1 画面でレビューできる」ことなので、ファイル冒頭で責務を 1 文で示す価値が 1 行のコストを上回る。`mod.rs` への引き継ぎ（R2）は必須として分離した |

### 確認が取れたこと（指摘ではないが計画の土台）

- `lib.rs:11 mod operation;` が `src/operation/mod.rs` を解決すること、`pub(crate) use execution::start_query_execution;` で `handler.rs` が無変更で通ること、`async fn` の再エクスポートが `.await` に影響しないこと（契約レビューが Rust の可視性規則で検証）
- `pub(super)` で兄弟モジュールから到達できること。`words`・`statement_type`・`substatement_type` の **3 本とも `pub(super)` が必要**で、余分なものは無い（両レビューが呼び出し元から独立に検証）
- 「`OPTIMIZE` を DDL の腕から DML の腕に移すと `statement_type_は先頭のキーワードで決まる` が落ちる」ことを、削減レビューが 648-656 と 790-793 の実物で確認
- `use super::*;`(719) をテスト 2 ファイルに分けても解決できなくなる名前は無い。861 行の `statement_type("")` も `classification.rs` に同居する
- 定数の文言の取り違えは `tests/idempotency.rs`・`list_work_groups.rs`・`metadata.rs` が厳密比較しているので green で検出できる
- `#[allow(...)]` は `src/operation.rs` に 0 件（消失のリスクなし）

### 再レビューの判定

**しない。** 構造の観点で指摘（R1）が出たが、書き直したのは「切り出しの単位の指定」と「検証手順」で、ファイル構成・責務配置・フェーズ分割という構造そのものは変えていない。

## フェーズの記録

### フェーズ1: 7-0 の答え（着手時）

- **消す・変えるガードや条件と、それに実際に依存している呼び出し元**: **無し。** ガード・条件を 1 つも消さず変えない。移動のみ。
- **変える関数・契約の呼び出し元**: 6 公開関数の呼び出し元は `src/handler.rs:35-40` の 6 行だけ（`grep -rn 'operation::' src/ tests/` で確認済み。`tests/` は 0 件）。`mod.rs` の再エクスポートで**無変更**にする。
- **このフェーズで増える分岐の全列挙**: **0 件。** match の腕・`if`・早期 `return`・エラー経路・出力の有無を分ける条件を 1 つも増やさない。したがって「固定するテスト名の無い分岐」も 0 件。増えたら計画違反なので、7-5b で `git diff` を機械的に確認する。
- **通る境界入力**: 該当なし（入力を受け取る経路を変えない）。
- **`.claude/design-checklist.md` のうちこのフェーズに当たる項目**:
  - 「ファイルを分割・移動する計画で、移す単位を行番号の範囲で指定していないか」（#19 で追加）→ **正面から該当**。切り出しは関数・定数と直前の `///` ブロックを 1 単位で運ぶ。
  - 「テストを別ファイルへ移す計画に、移動前後のテスト件数を突き合わせる手順があるか」（#19 で追加）→ **正面から該当**。218 件を突き合わせる。
  - 残り 3 項目（#5・#17 由来）は、新しい出力も前処理の切り出しも判定の差し込みも無いので該当なし。
- **可視性を変える関数**: `words`・`statement_type`・`substatement_type` の 3 本のみ（素の `fn` → `pub(super) fn`）。3 本とも兄弟モジュールから呼ばれることをレビュー 2 本が呼び出し元から検証済み。

### フェーズ1: 結果

1. 計数の突き合わせ: `///` 65/65、`実測` 32/32、`#[test]` 6/6（すべて移動前と一致）、`//!` 1→5（想定どおり）、総行数 863→904（想定「900 前後」の範囲内）。取りこぼし無し。
2. `wc -l`: `execution.rs` 362（見込み一致）、`query_execution.rs` 241（見込み 237、+4）、`work_group.rs` 127（見込み一致）、`classification.rs` 164（見込み 168、-4）、`mod.rs` 10（見込み 18、-8。再エクスポートを `pub(crate) use` 3 行にまとめたため）。いずれも小さな差で取りこぼしは無い。
3. `cargo fmt --check` 差分無し。`cargo clippy --all-targets --locked -- -D warnings` 警告 0。`cargo test --locked` テスト総数 218（`--lib` 122）で全緑、移動前と一致。
4. ミューテーション確認: `classification.rs` の `statement_type` で `OPTIMIZE` を DDL の腕から DML の腕へ移す → `statement_type_は先頭のキーワードで決まる` が失敗（`left: "DML", right: "DDL"`）することを確認 → バックアップから復元 → `git diff -- src/operation/classification.rs` が空であることを確認。
5. `git diff --stat`: `src/operation.rs` 863 削除のみ。新 5 ファイルは合計 904 行（untracked のため diff --stat には出ないが `wc -l` で確認）。差は use の重複・`mod tests` の開閉・`//!` 4 本増加で説明できる範囲。`src/lib.rs`・`src/handler.rs` は差分に無し（`git diff --stat -- src/lib.rs src/handler.rs` が空）。
6. 計画から外れた点: 無し。

**統括役の受け入れ判定（7-5b）**: 委譲先の報告を鵜呑みにせず、移動前後の本文を機械的に突き合わせた。`use` / `mod` / `//!` / 空行を除いて行をソートして `diff` を取ると、実質行数は 771 → 778 の **+7 行**で、その内訳が `#[cfg(test)]` +1・`mod tests {` +1・`use super::*;` +1・閉じ `}` +1・複数行 `use` の `};` +2・`cargo fmt` の折り返し +1 に**過不足なく一致**した。本文の書き換えは 0。加えて分岐の計数（`if` 18・`else` 14・`match` 27・`=>` 80・`return` 34・`None` 21）が**すべて移動前と完全一致**し、7-0 で宣言した「増える分岐 0 件」が機械的に確認できた。テスト名 6 本もソート順が違うだけで 1 文字も変わっていない。

### フェーズ2: 結果

- `CLAUDE.md` 7 行を書き換え（29 行を `operation/` の 4 ファイルの内訳と「6 つ」に、30-33 行を `execution.rs` の所属が分かる形に、44 行を「`StatementType`／`SubstatementType` は `classification.rs`、`UpdateCount` は `query_execution.rs` がその `statement_type` を使って決める」に、46 行を `operation/execution.rs` に）。
- 他ファイルのコメント 5 箇所（`catalog.rs`・`store.rs` 2 箇所・`athena.rs` 2 箇所）を新しいファイル名に。
- **計画を 1 点覆した**: TODO.md:145 は計画で「変えない」としていたが、他ファイルのコメントを直す理由（分割後に存在しないファイル名を指すのは誤り）が同じく当てはまるので直した（「呼び出しは `operation.rs` だけ」→「呼び出しは `operation` モジュールだけ」）。
- 検証: `grep -rn 'operation\.rs' src/ tests/ CLAUDE.md README.md TODO.md CHANGELOGS.md` が **0 件**。`cargo fmt --check` 差分無し、`clippy --all-targets --locked -D warnings` 警告 0、`cargo test --locked` テスト総数 **218** で全緑（FAILED 0）。
- README.md・CHANGELOGS.md は計画どおり無変更（挙動が変わらないため）。

### 7-7: 分割リファクタの判断

**分割不要。** `src/operation/` の最大は `execution.rs` の 362 行で、issue の完了条件（400 行以内）を満たす。目安の「新規ファイル 200 行以内」は超えるが、(1) 今回は新規コードを 1 行も書いておらず既存コードの移動先であること、(2) `execution.rs` の責務は「StartQueryExecution の受付・実行・書き込み」の 1 つであること、(3) TODO.md の新オペレーション（`ListDatabases` ほか）は `operation/` に新ファイルを足す形で入るので `execution.rs` は成長しないこと、から更に割らない。更に割るとユーザーが Step 4 で選んだ 4 ファイル構成を覆すことにもなる。

最終形: `mod.rs` 10 / `work_group.rs` 127 / `classification.rs` 164 / `query_execution.rs` 241 / `execution.rs` 362。

## レビュー実施記録

| 工程 | 回数 | 本番影響のある妥当指摘 |
|---|---|---|
| 計画の敵対的レビュー（Step 6） | 1 回（Explore 3 本） | 採用 5 件（うち high 相当 2 件: R1・R3）／棄却 2 件 |
| フェーズのレビュー（7-6） | 0 回（小規模 issue の経路のため行わない） | — |
| 最終パス（Step 8） | 0 回（小規模 issue の経路のため行わない） | — |
| 独立レビュー（Step 8） | 1 回 | **0 件**（差分と検証コマンドだけを渡し、観点・ノート・dismissed は渡していない） |

レビュー起動の予算 4 に対する消費: **1**（独立レビュー 1 回のみ。計画レビューは別枠）。

**出荷判定**: 独立レビューが「本番影響のある妥当指摘 0 件」を返したので、停止規則によりレビュー工程を終了した。

## dismissed

（棄却した指摘と反証）

## deferred

| 指摘 | severity | file | 再トリアージ | 起票 |
|---|---|---|---|---|
| （なし） | | | | |

## 覆した事前判断

| # | 元の判断 | 新しい判断 | 理由 | 外部契約・プロダクト挙動・運用への影響 | ユーザーに聞いたか |
|---|---|---|---|---|---|
| 2 | 計画では TODO.md:145 の `operation.rs` 表記は「前提が崩れないので変えない」 | **直す**（「呼び出しは `operation` モジュールだけ」） | 他ファイルのコメント 5 箇所を直す理由（分割後に存在しないファイル名を指すのは事実として誤り）が TODO.md にも同じく当てはまり、計画の中で一貫していなかった | 無し（文書の文言のみ） | 聞いていない（リポジトリ内の前例から導出できる工学判断のため） |
| 1 | issue 本文の方針案 (b)「クエリの実行」と「状態と設定の参照」の 2 つに分ける | **4 ファイルに分ける**（`execution.rs` / `query_execution.rs` / `work_group.rs` / `classification.rs`） | (b) の参照系は関数 322 行 + ユニットテスト 147 行 + 定数 26 行 = 516 行で、完了条件の「1 ファイル 400 行以内」を満たせない。案 (c) の定数切り出しを足しても 26 行しか逸せない | 無し（内部のファイル配置のみ。`handler.rs`・`lib.rs`・`tests/` はすべて無変更） | **聞いた**（Step 4。ユーザーが 4 ファイル構成を選択） |

## 範囲外の発見

| 内容 | 出どころ | 起票した issue（起票しないなら理由） |
|---|---|---|
| `src/convert.rs` が 869 行（本体 419 行）で `src` 最大。本体が 400 行を超えるのはこのファイルだけ | Step 3 の調査（Explore B）と 7-7 の実測。`convert.rs` 420 行目から `#[cfg(test)]` | **#30 を起票**（<https://github.com/aoyagikouhei/athena-local/issues/30>） |
| `store.rs` 614 行・`results.rs` 610 行・`config.rs` 441 行 | 同上 | 起票しない。本体はそれぞれ 281・281・223 行で、400 行を超えているのはテストを含めた全体だけ。#19 の基準（人間が 1 画面でレビューできるか）では分割の必要が無い |

## 委譲の記録

| 工程 | 委譲 / 自分 | 自分でやった理由 |
|---|---|---|
| Step 1 issue 読み | 自分 | コマンド 1 本 |
| Step 2 ブランチ・ノート | 自分 | 指示を書く方が長い |
| Step 3 調査 | 委譲（Explore 3 本） | — |
| Step 3 計画の下書き | 委譲（Plan 1 本） | — |
| Step 3 Explore の転記 | 自分 | 読み取り専用の委譲先の結果は統括役が転記する規則 |
| Step 5 計画の転記 | 自分 | 読み取り専用の委譲先（Plan）の結果は統括役が転記する規則 |
| Step 6 計画レビュー | 委譲（Explore 3 本） | — |
| フェーズ1 実装 | 委譲（general-purpose 1 本） | — |
| フェーズ1 受け入れ判定（7-5b） | 自分 | 差分を自分の目で読むのは規則 |
| フェーズ2 文書 | 自分 | CLAUDE.md の書き換えは文面の判断そのもので、文面案を指示に書く方が自分で書くより長い |
| Step 8 独立レビュー | 委譲（general-purpose 1 本） | — |
| Step 8 独立レビューの転記 | 自分 | ノートを渡さない委譲先の結果は統括役が転記する規則 |
| Step 9 実機検証 | 自分 | サーバ起動と curl 8 本で、指示を書く方が長い |

## 実機検証

CLAUDE.md にブラウザ検証の手順は無い（Web UI が無く API サーバのため）。**実機相当の代替として、実際にサーバを起動して HTTP で叩いた**（checklists.md「内部ロジックのみの変更」の代替。ロジックの変更は 0 行なので UI・API 契約に届かない）。

`ATHENA_LOCAL_BIND=127.0.0.1:18084 TRINO_URL=http://127.0.0.1:1 ./target/debug/athena-local` で起動（**Trino も S3 も無い最小構成**。`ATHENA_LOCAL_RESULTS=none`）。

| # | 叩いたもの | 通る新ファイル | 結果 |
|---|---|---|---|
| 1 | `GetWorkGroup {"WorkGroup":"primary"}` | `work_group.rs` | 200。`State: ENABLED`、`EngineVersion: Athena engine version 3` |
| 2 | `ListWorkGroups {}` | `work_group.rs` | 200。`primary` 1 件 |
| 3 | `ListWorkGroups {"MaxResults":0}` | `work_group.rs` の `MAX_RESULTS_TOO_SMALL` | 400。実測文言そのまま |
| 4 | `StartQueryExecution`（トークン無し） | `execution.rs` の `TOKEN_MISSING` | 400。`clientRequestToken is null or empty` |
| 5 | `StartQueryExecution`（トークン 5 文字） | `execution.rs` の `TOKEN_TOO_SHORT` | 400。実測文言そのまま |
| 6 | `GetQueryExecution`（存在しない ID） | `query_execution.rs` の `unknown_execution` | 400。`QUERY_EXECUTION_NOT_FOUND` |
| 7 | `StopQueryExecution`（存在しない ID） | `query_execution.rs` | 400。同上 |
| 8 | `StartQueryExecution "CREATE TABLE t (a int)"` → `GetQueryExecution` | **`classification.rs`（越境呼び出し）** | 200 → `StatementType: DDL`、`SubstatementType: CREATE_TABLE`、`State: FAILED`（Trino が無いためで想定どおり）、`WorkGroup: primary` |

**8 が最も重要**: `query_execution.rs` の `to_query_execution` から `super::classification::statement_type` / `substatement_type` を呼ぶ**越境呼び出しが実機で動いている**証拠で、これは移動で新たに生じた唯一のファイル間依存にあたる。`mod.rs` の再エクスポートが効いていることも、1〜8 すべてが `handler::dispatch` 経由で振り分けられていることで確認できる。

最小構成での検証: 上記がすべて Trino も S3 も無い状態。加えて `cargo test --locked` を絞り込み無しで 1 回走らせ 218 件 green（外部サービス不要）。

終了後にサーバを停止し、ポートが応答しないことを確認した。

## マージ後の周知事項

**なし。** 環境変数・依存・起動方法・API のいずれも変わらない。`src/operation.rs` が `src/operation/` の 5 ファイルになったので、このファイルを開いていた人はパスが変わる点だけ。

## 出荷

- **PR: #31**（<https://github.com/aoyagikouhei/athena-local/pull/31>）「src/operation.rs を責務ごとの 4 ファイルに割る」
- **起票した issue: #30**（`src/convert.rs` を分割する）
- **issue コメント**: deferred が 0 件なので投稿しない（規則どおり）。#19 への引き継ぎコメントは escalate 時に投稿済み
- **出荷判定**: 独立レビューが「本番影響のある妥当指摘 0 件」で返り、停止規則を満たした。issue の完了条件 4 つをすべて満たしている（1 ファイル 400 行以内＝最大 `execution.rs` 362 行／`cargo test --locked` が無変更で green＝218 件／README・CHANGELOGS は無変更／CLAUDE.md のアーキテクチャの節を新構成に更新）
- **予算**: 実装 2（フェーズ数 2 + 2 = 4 以内）、レビュー対応 0（5 以内）、docs 単独 2 + 別枠 1（規定どおり）、レビュー起動 1（4 以内。計画レビューは別枠で 1 回、再レビューなし）

## 時系列

| 時刻 | 出来事 | SHA |
|---|---|---|
| 10:32 | quick-implement で着手、ゲート判定で範囲外 | — |
| 10:43 | quick ノートをコミットし implement-issue へ escalate | `32b7cb7` |
| 10:43 | issue #19 に引き継ぎをコメント | — |
| 10:44 | Step 3 調査: Explore 3 本を並列起動 | — |
| 10:50 | Explore 3 本が返る | — |
| 10:51 | Plan 1 本を起動、ノートを implement-issue の節構成に広げる | — |
| 11:02 | Plan が返る。案 (b) は参照系 516 行で 400 行を超え不成立と判明 | — |
| 11:05 | Step 4: 分割軸（4 ファイル構成）とフェーズ数（2）をユーザーが決定 | — |
| 11:06 | 計画をノートに転記（218 行） | — |
| 11:07 | Step 6: 計画レビュー Explore 3 本を並列起動 | — |
| 11:11 | 計画レビュー 3 本が返る。契約と構造が独立に doc コメントの取りこぼしを指摘 | — |
| 11:12 | ベースラインを実測（テスト 218・`///` 65・`実測` 32・`#[test]` 6） | — |
| 11:13 | 計画を確定してコミット。design-checklist に 2 行追記 | `50c99f7` |
| 11:16 | Step 6b: 設計をユーザーに提示 → 承認 | — |
| 11:17 | フェーズ1 の 7-0 を記録し、実装を委譲 | — |
| 11:23 | フェーズ1 の実装が返る（計数一致・テスト 218・mutation 落ちた） | — |
| 11:24 | 7-5b: 本文の同一性と分岐の計数を機械的に検証して受け入れ | — |
| 11:25 | フェーズ1 をコミット | `18500c9` |
| 11:29 | フェーズ2: CLAUDE.md 7 行・他ファイル 5 箇所・TODO.md 1 箇所を書き換え、3 コマンド green（テスト 218） | — |
| 11:27 | フェーズ2 をコミット | `fe0cefc` |
| 11:28 | Step 8: 独立レビューを起動 | — |
| 11:29 | 7-7 は分割不要と判断。範囲外の発見（`convert.rs` 869 行）を #30 に起票 | — |
| 11:29 | 独立レビューが **0 件**で返る → 出荷判定を満たす | — |
| 11:30 | Step 9: サーバを起動し 8 ケースを HTTP で検証（最小構成） | — |
| 11:32 | ブランチを push。PR 本文のドラフトを委譲 | — |
| 11:33 | ノートを 382 → 320 行に圧縮（計画と事実の節を実装済みの内容に畳む） | — |
| 11:35 | PR #31 を作成（予算の区分の誤りを 1 点直して） | — |
