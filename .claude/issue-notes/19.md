# quick #19 src/operation.rs を分割する

- 入力: issue #19。モード: standalone。ブランチ `main-19`、着手前 SHA e4c1826
- ゲート: サイズ 863 行・新ファイル 2〜3 個（≤50 行・3 ファイル）→ 外れ／呼び出し元 6 箇所（`src/handler.rs` のみ、≤5）→ 外れ／契約: 変えない（挙動不変の移動）／新規性: `src` 配下に新ファイルを作る → 外れ／事実: 再現不要（リファクタ）／判断: 分割軸 (a)(b)(c) の選択を含む → 外れ → **提案あり**
- 時系列: 着手 10:32 ／ ゲート判定 10:35 ／ 3 択の回答 10:40 ／ ノートのコミット 10:42
- 検証: 実装に入っていないため未実施（quick としての red→green は無し）
- 自己レビュー: 差分は本ノートのみ。本番コードは 1 行も触っていない
- 打ち切り・escalate: 提案あり（変更サイズ 863 行・新規性 新ファイル作成・判断 分割軸の選択）→ 選択: **implement-issue で進める**。stash 無し（未コミットの変更を作っていない）
- 予算の実績: Bash 約 7 本、所要 10 分、コミット 1
- 出荷: ブランチ `main-19`（implement-issue がこのブランチを引き継ぐ）

## 引き継ぎ（implement-issue の出発点）

### 事前に確認した事実

- `src/operation.rs` は 863 行。`src/convert.rs` 869 行に次ぐ 2 番目で、`src` 全体は 5324 行。
- 外から呼ばれるのは `pub fn` 6 本だけで、呼び出し元は `src/handler.rs:35-40` の 6 行のみ。`tests/` からの直接参照は 0 件（結合テストは HTTP 経由）。
- ファイル内の並びは、おおむね方針案 (b) の境界で既に分かれている:
  - 1-68: `use` 群、実測文言の定数 11 本（`DEFAULT_MAX_RESULTS`, `LIST_WORK_GROUPS_MAX_RESULTS`, `WORK_GROUP_STATE`, `MAX_RESULTS_TOO_SMALL`, `MAX_RESULTS_TOO_LARGE`, `NEXT_TOKEN_EMPTY`, `NO_OUTPUT_LOCATION`, `IDEMPOTENT_MISMATCH`, `TOKEN_MISSING`, `TOKEN_TOO_SHORT`, `TOKEN_TOO_LONG`）、`engine_version`
  - 69-394（約 326 行）: クエリ実行系 — `start_query_execution` / `submit_response` / `client_request_token` / `context_defaults` / `result_location` / `spawn_query` / `write_result` / `write_failure` / `write_metadata` / `metadata_query_id` / `run`
  - 395-716（約 322 行）: 状態と設定の参照系 — `get_query_execution` / `get_work_group` / `list_work_groups` / `get_query_results` / `stop_query_execution` / `not_succeeded` / `to_query_execution` / `statistics` / `update_count` / `words` / `statement_type` / `substatement_type` / `unknown_execution`
  - 717-863（147 行）: `#[cfg(test)]` のユニットテスト（分割後、どちらのファイルのテストかを振り分ける必要がある）
- この並びのまま (b) で割ると、実行系・参照系ともに 400 行を下回り、完了条件を満たす見込み。(c) の定数切り出しを重ねるかは選択の余地がある。
- 文の種類の判定（`statement_type` / `substatement_type` / `update_count` / `words`）は参照系の末尾にあるが、`ResultFile::of`（`src/results.rs`）と判定が 2 か所に分かれているという CLAUDE.md 記載の事情があり、移動先の選び方に影響する。

### 未実測・未確認

- 分割軸 (a) オペレーション別 / (b) 実行系と参照系 / (c) 定数の切り出し のどれを採るか（issue 本文も「着手時に決める」としている）。
- 定数 11 本と `words` / `statement_type` / `substatement_type` を、実行系・参照系のどちらから参照しているか（`pub(crate)` や `super::` の可否）。
- `#[cfg(test)]` 147 行の各テストがどちらの側を対象にしているか。
- TODO.md の `ListDatabases` / `ListTableMetadata` / `ListDataCatalogs` を先に足すか、先に割るか（issue 本文が手戻りの少なさに言及している）。

### 変更が要る文書

- `CLAUDE.md` の「アーキテクチャ」節。`operation.rs` を名指ししている箇所（リクエストの流れ 2、取り消し、Athena の見え方への変換の「文の種類は…判定が 2 か所にある」）を新しい構成に合わせる。
- README・CHANGELOGS は変えない（挙動が変わらないため。issue の完了条件どおり）。

### 再現手順

リファクタのため再現は不要。回帰の基準は `cargo test --locked` が無変更で green であること。
