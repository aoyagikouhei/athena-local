# quick #175 src/operation/execution.rs（553 行）を分割する
- 入力: issue #175。モード: standalone。ブランチ main-175、着手前 SHA a34621c
- ゲート: サイズ 50 行超・3 ファイル（新ファイル・execution.rs・mod.rs）の見込み（≤50・3）／呼び出し元 1（`run` の中だけ、同一モジュール）／契約 通過・新規性 外れ（src 配下に新ファイル）・事実 外れ（挙動を変えない移動で red が成立しない）・判断 通過 → 変更サイズ・新規性・事実が外れ
- 時系列: 着手 22:09 ／ red なし ／ green なし ／ コミット 22:11
- 検証: 未実施（コードに触らず escalate）
- 自己レビュー: 未実施（コードに触らず escalate）
- 打ち切り・escalate: 提案あり（変更サイズ 50 行超・新規性 新ファイル・事実 red 不成立。挙動を変えない移動・分割）→ 選択: implement-issue（move-refactor.md の経路）
- 予算の実績: Bash 約 7 本、所要 2 分、サブエージェント 0 本
- 出荷: main-175（implement-issue に引き継ぐ）

## 引き継ぎ

- 判定箇所: `src/operation/execution.rs` は 553 行（本体 437 行、`#[cfg(test)] mod tests` は 438 行目から、`#[test]` 5 件）。移動前のテスト件数は execution.rs 内で 5 件（`update_count` 2 件、`split_explain_rows` 3 件）。全体の件数は着手時に `tools/dev.sh cargo test` で実測して突き合わせる
- 関数の並び（行は a34621c 時点）: `start_query_execution` 44、`submit_response` 102、`client_request_token` 124、`context_defaults` 156、`result_location` 177、`spawn_query` 210、`run` 247〜349、`iceberg_partition_specs` 351〜379、`split_explain_rows` 381〜409、`update_count` 411〜436（いずれも doc コメント込み）
- issue の案 1: `run` の前半（`execution.rs:266-318` 付近。`table_format::target_statement` → `target_table::parse_target_table` → `table_format::probe_format` → `table_format::engine_ddl`／`DESC_VIEW` の `substatement_type` の決定）を `operation/probe.rs` などへ。戻り値は `(statement, format, engine_ddl, substatement_type)` 相当。後半の `iceberg_partition_specs` の呼び出し条件が `statement` と `format` を使うので、両方を返す必要がある
- issue の案 2: `split_explain_rows` と `iceberg_partition_specs` を `utility_rows` に寄せる案（#173 の計画レビュー B-(d) で「EXPLAIN は DML なので名前が合わない」と見送り）。`utility_rows.rs` は既に 615 行なので寄せると 400 行の目安をさらに超える点に注意
- 呼び出し元: `split_explain_rows`・`iceberg_partition_specs` は `run` の中だけ、`update_count` は `spawn_query`（execution.rs:224）だけ。いずれも非公開で、`mod.rs` の再エクスポートには関わらない
- CLAUDE.md の「リクエストの流れ」2. の `operation/` の段落に execution.rs の担当が書いてあるので、分けたら新ファイルの名前と担当を足す
- 未実測・未確認: なし（挙動を変えない移動）
- stash: なし

---

# implement-issue #175（quick からの引き継ぎ）

## 基本情報
- issue #175、ベース main、ブランチ main-175。着手前 SHA: a34621c（src の基準）。レビューの base は 9a22501（quick ノートのコミット。src は a34621c と同一）
- 経路: 小規模 issue の経路 ＋ move-refactor.md（2 フェーズ）

## 事前に確認した事実
（Explore A = 移動単位と依存、B = 慣習と文書参照。落とした番号: A-7 の doc の例（慣習として 1 行で足りる）、B-3 の lint 以外の詳細、B-4 の #44 の D3・D6・D7（今回に効かない））
- F1 移動単位（a34621c）: `iceberg_partition_specs` 351-379（doc 351-354）、`split_explain_rows` 381-409（doc 381-388）、`update_count` 411-436（doc 411-416）、`#[cfg(test)] mod tests` 438-553（`use super::*;` だけ、テスト 5 件: update_count 系 2 件 442-501、ヘルパ `plan` 503-508、explain 系 3 件 510-552）。属性は無し
- F2 依存: 3 関数が `use` 経由で使うのは `Trino`・`Config`・`Cancel`・`Outcome`・`EngineDdl`・`alias_qualified_names`。それ以外（`crate::catalog::skip_*`、`super::iceberg_partitions::parse_partitioning`、`super::classification::{statement_type,substatement_type}`、`crate::content_type::plain_text_statement`、`std::mem::take`、`serde_json::Value`）はフルパス。移動後に残留側で不要になる `use` は `Cancel` だけ（`run` は `&execution.cancel` を渡すだけで型名を書かない。足場の委譲先が移動のシミュレーションで clippy の unused_imports として実測）。`Config`・`Trino`・`Outcome`・`EngineDdl`・`alias_qualified_names` は `run` も使う
- F3 呼び出し元: `iceberg_partition_specs` は execution.rs:343、`split_explain_rows` は :338（いずれも `run`）、`update_count` は :224（`spawn_query`）。src/tests の他からの呼び出し 0、`mod.rs` の再エクスポート無関係（mod.rs は `mod` 11 行＋`pub(crate) use` 3 行）
- F4 `run` の前半 266-317: 入力 `trino`・`config`・`execution`・`raw_catalog`・`database`・`cancel`、出力 `statement`・`format`・`engine_ddl`・`substatement_type`（4 つとも後ろで使う: 340・347・348 行）。早期 return と `?` は無い。`TargetStatement`・`TableFormat`・`EngineDdl` は Copy（table_format.rs:12,36,45）
- F5 基準値（a34621c）: execution.rs の `///` 44（本体 43）、`//!` 1、「実測」23 行（本体 19、移動する 3 関数とテストに 9）。本体の `if` 12・`match` 13・`return` 19。src 全体の `///` 723
- F6 慣習: operation/ のファイルどうしは `pub(super)`。越境は関数はフルパス（decisions.md:224）、execution.rs は `table_format`・`target_table`・`result_output` をモジュールごと `use` している。新ファイルは operation/ 直下（decisions.md:223）、各ファイルに `//!`、そこに「実測」「採取」を入れない（:226）。テストは直接呼ぶ関数の定義先へ追随。行数はファイル全体（本体＋テスト）で測る（:221）
- F7 文書の参照（移動側を指すので直す）: CLAUDE.md:32（`split_explain_rows` の直後）、CLAUDE.md:47（`operation/execution.rs` の `update_count`）、src/content_type.rs:56（同）、src/operation/utility_rows.rs:4（`execution::split_explain_rows`）、docs/dev/decisions.md:119（`operation::execution::update_count`）、src/operation/table_format.rs:33（`run` の `iceberg_partition_specs` の条件）。直さない: decisions.md:106（`operation::update_count` は当時のパスの記録）・:127・:137、measurements/trino.md:120（実測記録）。残留側を指す参照（CLAUDE.md:32-34、athena.rs:22、store.rs:54,136、tools/measure の 4 件）は直さない
- F8 lint: `missing_docs` 無し、`[lints]`・clippy.toml 無し → doc の消失は足場でしか拾えない

## 設計判断
- D1 分け方（Step 4、ユーザー回答 22:14）: 両方を 2 フェーズで。フェーズ 1 = 完了時の後処理 3 関数（`split_explain_rows`・`update_count`・`iceberg_partition_specs`）とテスト 5 件を新ファイルへ純粋に移動。フェーズ 2 = `run` の前半（形式の問い合わせ）を新ファイルの関数に抽出し、文書を追随させる。フェーズ 2 は純粋な移動ではないので正規化 diff は当てず、既存テスト・分岐の計数・自己レビューで守る
- D2 decisions.md:227（挙動を変えない分割では 1 関数の内部を小関数に割らない・起票もしない）を削除する（ユーザー回答 22:20）。元は #19 で 54・51 行の関数を割らなかった場面の判断で、理由（分岐の形が変わり純粋な移動を証明できない）は「移動と抽出を混ぜない」までしか支えない。`run` の前半は早期 return も `?` も無い let の並びで、分岐の形を変えずに抽出できる

## 覆した事前判断
| 元の判断 | 新しい判断 | 理由 | 影響 | 聞いたか |
|---|---|---|---|---|
| decisions.md:227「挙動を変えない分割では 1 関数の内部を小関数に割らない（起票もしない）」 | 決まりを削除し、`run` の前半（形式の問い合わせ）を関数に抽出する | 理由が「移動と抽出を混ぜない」までしか支えず、対象のブロックは分岐の形を変えずに抽出できる | 無し（内部構成のみ） | 聞いた（22:20） |
| issue 本文「`split_explain_rows`・`iceberg_partition_specs` を `utility_rows` に寄せる案もある」 | 寄せず、`update_count` と合わせて新ファイルへ | utility_rows.rs は既に 615 行 | 無し | 聞いた（Step 4、22:14） |

## 計画
（Plan の下書き 1〜11 を転記。落とした番号: 9 の行数見込み（553→185 は算術の誤り。自分で数え直した値に差し替え）、2 の `//`→`///` 変換（下の P2-3 で覆した）、2 のファイル名 `probe.rs`（P2-1 で覆した）、8 の table_format.rs:33 の「任意」（直すことにした））

### フェーズ 1: 完了時の後処理 3 関数とテストを `completion.rs` へ純粋に移動（1 コミット）
- P1-1 新ファイル `src/operation/completion.rs`、`//! run の完了後の後処理（Iceberg のパーティション取得・EXPLAIN の行分割・UpdateCount の決定）。`（「実測」「採取」を含まない。decisions.md:226）。命名は classification.rs・validation.rs と同じ「機構名」の軸
- P1-2 移動単位（a34621c）: `iceberg_partition_specs` 351-379 → `pub(super) async fn`、`split_explain_rows` 381-409 → `pub(super) fn`、`update_count` 411-436 → `pub(super) fn`、`#[cfg(test)] mod tests` 438-553 はそのまま。付与する `pub(super)` は 3 件。本文は 1 行も変えない
- P1-3 `use`（本文の grep で決める）: completion.rs に `crate::catalog::alias_qualified_names`・`crate::config::Config`・`crate::trino::{Cancel, Outcome, Trino}`・`super::table_format::EngineDdl`。フルパスで書かれている名前（`crate::catalog::skip_*`・`super::classification::*`・`super::iceberg_partitions::parse_partitioning`・`crate::content_type::plain_text_statement`・`std::mem::take`・`serde_json::Value`）はフルパスのまま。execution.rs は `use super::completion;` を足し（execution.rs:22-24 と同じくモジュールを use してモジュール修飾で呼ぶ）、`Cancel` を `use crate::trino::{...}` から外す（F2）
- P1-4 呼び出し 3 か所: execution.rs:224 `completion::update_count(...)`、:338 `completion::split_explain_rows(...)`、:343 `completion::iceberg_partition_specs(...)`
- P1-5 mod.rs に `mod completion;`（アルファベット順で classification の次）
- 見込み: execution.rs 約 350 行、completion.rs 約 210 行（本体 86＋テスト 116＋use・//!）。decisions.md:221 の 200 行超は「新規のコードでなく責務 1 つ」なので割らない

### フェーズ 2: `run` の前半を `format_probe.rs` に抽出（1 コミット）＋文書の追随（1 コミット）
- P2-1 新ファイル `src/operation/format_probe.rs`（Plan 案は `probe.rs`。`run` のパラメータ分類のループの局所変数 `probe`（execution.rs:323）と紛れるので覆す。6b で確認）。`//!` は 1 行、「実測」「採取」を含まない
- P2-2 関数 `pub(super) async fn probe_target_format(trino: &Trino, config: &Config, execution: &Execution, raw_catalog: Option<&str>, database: Option<&str>, cancel: &Cancel) -> (Option<table_format::TargetStatement>, Option<table_format::TableFormat>, Option<EngineDdl>, Option<&'static str>)`。戻り値はタプル（execution.rs:160 `context_defaults`・:251 `run` と同じ前例。6 引数は clippy の too_many_arguments（7）未満）。`execution: &Execution` を渡すのはブロックが `execution.query` を書き換えずに使うため
- P2-3 ブロック（execution.rs:266-317）は `//` コメントも含めて本体へそのまま運び、末尾に `(statement, format, engine_ddl, substatement_type)` を足すだけ。関数の `///` は新しく 1〜2 行の要約だけ書く（足場の `EXTRA_DOC_PHASE2` にその行数を入れる）。コメントの「パラメータ分類のループより前に置く」は呼び出し位置の説明として真のまま（呼び出しはループの前）
- P2-4 `run` 側: `let (statement, format, engine_ddl, substatement_type) = format_probe::probe_target_format(trino, config, execution, raw_catalog, database, cancel).await;`。`use super::format_probe;` を足し、`use super::target_table;` を外す（:282 だけが使う）。`table_format` と `EngineDdl` は run の残り（:340-341・戻り値型）が使うので残す
- P2-5 テストは足さない（結合テストが覆う: tests/table_format.rs:110 対象外の文では問い合わせない、:468 S3 無効なら DROP TABLE でも問い合わせない、tests/describe.rs:367・:486 none でも DESCRIBE／SHOW COLUMNS は問い合わせる、tests/describe.rs:689-693 DESC_VIEW、tests/show_create.rs:248）
- P2-6 文書の追随（別コミット）: CLAUDE.md:32（operation/ の列挙に completion.rs と format_probe.rs を足し、`split_explain_rows` の直後 → `completion::split_explain_rows` の直後）、CLAUDE.md:47・src/content_type.rs:56（`operation/execution.rs` の `update_count` → `operation/completion.rs` の）、src/operation/utility_rows.rs:4（`execution::split_explain_rows` → `completion::split_explain_rows`）、src/operation/table_format.rs:33（`run` の `iceberg_partition_specs` の条件 → 呼び出しの条件は run に残るので文意を確認して最小の書き換え）、docs/dev/decisions.md:119（`operation::execution::update_count` → `operation::completion::update_count`）、docs/dev/decisions.md:227 の行を削除（D2）。直さない: decisions.md:106・127・137、measurements/trino.md:120。README・CHANGELOG は変えない
- 見込み: execution.rs 約 305 行、format_probe.rs 約 75 行、mod.rs 19 行

### 検証
- 足場 `.claude/issue-notes/175-verify/check.sh`（before/phase1/phase2）。フェーズ 2 にも正規化 diff を当てる（比較元 = フェーズ 1 のコミットの execution.rs＋completion.rs、比較先 = 現在の execution.rs＋completion.rs＋format_probe.rs。許容行: 関数の宣言と引数・戻り値型の行、末尾のタプル式、`fn` の閉じ括弧、`run` 側の分解束縛と呼び出しの行、`use` は元々除外）
- 標準コマンド: `tools/dev.sh cargo fmt --check`、`tools/dev.sh cargo clippy --all-targets --locked -- -D warnings`、`tools/dev.sh cargo test --locked`（396 件）

### ミューテーション候補
- M1（フェーズ 1）completion.rs の `update_count` の `EngineDdl::DescribeIceberg` の腕を外す → `operation::completion::tests` が落ちる（移動先のテストが移動先を守る）
- M2（フェーズ 1）execution.rs の `completion::split_explain_rows` の呼び出しを外す（`let outcome = outcome;`）→ EXPLAIN の結合テスト（tests/select.rs の EXPLAIN のケース）が落ちる（越境の配線）
- M3（フェーズ 1）execution.rs の `completion::iceberg_partition_specs` の呼び出し条件を偽にする → tests/describe.rs の Iceberg の `# Partition spec:` のケースが落ちる
- M4（フェーズ 2）format_probe.rs の `DESC_VIEW` の条件を偽にする → tests/describe.rs:689 付近のビューのケースが落ちる
- M5（フェーズ 2）format_probe.rs の `ResultsMode::None` の条件から `&& !statement.is_some_and(...)` を外す → tests/describe.rs:367 か tests/show_create.rs:248 が落ちる
- M6（フェーズ 2）format_probe.rs の戻り値のタプルの `statement` と…（型が違うので入れ替え不可）→ 代わりに `engine_ddl` を `None` に固定 → tests/table_format.rs の DROP TABLE Iceberg のケースが落ちる

### 先行できる作業
- 足場の許容リストの修正（宣言 3・呼び出し 3 を名指し、名前を completion／format_probe に、フェーズ 2 の正規化 diff）: 計画レビューと並行して今から（自分）
- 足場の修正は 22:34 に済み（before で全 ok: テスト 396・/// 723・実測記録 300・分岐 45・fmt・clippy）
- 実機検証（Step 9）: `tools/dev.sh tools/e2e/utility-rows/verify.sh`（DESCRIBE の Iceberg のパーティション＝iceberg_partition_specs、DESC_VIEW＝形式の問い合わせ、UpdateCount＝update_count）と `tools/dev.sh tools/e2e/result-content-type/verify.sh`（EXPLAIN＝split_explain_rows、DROP TABLE の形式判定）。同じ compose の trino／minio を使うので順番に流す

## 計画レビューの結果
Explore 1 本（sonnet、小規模経路）。指摘 1 件＋削減 1 件。
- 採用 R1（medium）: フェーズ 2 の許容リストは rustfmt の折り返しを実測せずに書いた仮定だった。サンプルを `tools/dev.sh rustfmt --edition 2024 --emit stdout` に通して実測 → 宣言は引数ごと・戻り値のタプル型も要素ごとに折り返し、呼び出しは `let (...) =` の後で改行して `.await` が別行、`completion::iceberg_partition_specs(` は引数ごとに折り返し、`completion::update_count` の呼び出しは 1 行に収まる。この実測で、足場の perl の置換パターンにエスケープしていない `[^{]` があり `s{}{}` の区切りが崩れて perl が構文エラーになる（phase1・phase2 の項目 5 が必ず NG になる）欠陥も見つけて直した。直した後は折り返しが 1 行に戻り、許容リストの形と一致することを確かめた
- 棄却（削減の提案）: 「実機検証（Step 9）を省く」。結合テストは偽 Trino が相手で、本物の Trino の `SHOW CREATE TABLE`（iceberg_partition_specs）と形式の問い合わせを通るのは実機だけ。move-refactor.md も移動した関数を通る最小のケースを残すとしている
- 確認だけ（問題なし）: `Cancel` だけが未使用になる、フェーズ 2 は `target_table` だけ（`ResultsMode` は result_location が使う）、基準値 723・45、phase1 の件数 6・6、M1・M4〜M6 の落ちるべきテストが実在して分岐を踏む、decisions.md の直す・直さないの切り分け

## 委譲の記録
| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| Step 3 調査 | Explore 2 本（sonnet） | |
| 検証の足場 | general-purpose 1 本（sonnet） | |
| Plan | Plan 1 本（sonnet） | |
| 計画レビュー | Explore 1 本（sonnet） | |
| 足場の許容リストの修正 | 自分 | 受け入れ判定で見つけた欠陥の直しで、許容する行を決めるのは計画の判断そのもの（Bash 10 本を超えたら委譲） |

## 時系列
- 22:09 quick-implement 着手、22:11 ゲートで escalate（9a22501）
- 22:13 implement-issue 起動、Explore 2 本（移動単位と依存／慣習と文書参照）
- 22:14 Step 4: 分け方を「両方（2 フェーズ）」と回答
- 22:15 先行作業: 検証の足場（175-verify/check.sh）を general-purpose に委譲
- 22:18 Explore 2 本が返る。decisions.md:227 と D1 の衝突を検出
- 22:20 decisions.md:227 を消して両方やると回答（D2）
- 22:21 Plan に計画の下書きを依頼
- 22:30 足場（175-verify/check.sh）が返る。基準値: テスト 396、`///` 723、実測記録 300 行、execution.rs 本体の分岐 45。受け入れ判定: 正規化 diff の許容リスト `^< .*\b(update_count|…)\(` がテスト内の呼び出し行（update_count 17 行・split_explain_rows 4 行）の消失も通すので、計画の確定後に宣言 3・呼び出し 3 を名指しする形に絞る
- 22:33 Plan が返る。計画を転記（ファイル名 probe.rs → format_probe.rs、コメントは `//` のまま運ぶ、の 2 点を覆す）
- 22:34 足場の許容リストを修正し before で全 ok。計画レビュー（Explore 1 本）を起動
- 22:50 計画レビューが返る（R1 採用・削減は棄却）。rustfmt の実測で足場の perl の欠陥を見つけて直す
