# quick #199 CTAS の `AS (VALUES 1)` でファイル名は tables/<id> なのに SubstatementType は CREATE_TABLE になる
- 入力: issue #199。モード: standalone。ブランチ main-199、着手前 SHA b8359ed
- ゲート: サイズ 17 行・1 ファイル（≤50・3。テストの固定値で content_type.rs も 1 行）／呼び出し元 2（≤5・ResultFile::of と substatement_type）／契約: 本物に合わせる修正で通過・新規性: 通過・事実: 外れ（本物の実測 0 件）・判断: 通過（decisions.md が「#199 で測って揃える」と固定）→ はしご
- 時系列: 着手 13:32 ／ 実測スクリプト依頼 13:36・コミット 13:47 ／ ユーザー実行 13:51〜13:59 ／ red 14:00 ／ green 14:01 ／ コミット 14:04
- 検証: `tools/dev.sh cargo fmt --check`・`tools/dev.sh cargo clippy --all-targets --locked -- -D warnings`・`tools/dev.sh cargo test` ok（全 passed、failed 0）／ミューテーション: `AS(` の分割を外す → 新テスト 2 本が落ちた、`VALUES` を先頭語から外す → 同じく落ちた／実測: 1 ラウンド（9 項目すべて CTAS・tables/<id>。v1〜v4 は MISSING_COLUMN_NAME で FAILED）
- 自己レビュー: 分岐 5 件（`AS(` の分割・先頭 `(` の除去・空語の除去・先頭語 4 種・`AS` 以外）のうち、`AS` の次が問いの先頭語でない分岐だけ直接のテスト無し（`CREATE TABLE t (i int)` など既存の非 CTAS の行が `AS` を含まない）／エラーの出口: 新規なし。FAILED の CTAS は `failed()` が None で何も置かず、本物の key 0 件と一致／呼び出し元 2 箇所とも新テストで同じ 13 形を確認
- 打ち切り・escalate: はしご（事実 → 実測工程）。計画攻撃は足さず（本番 1 ファイル）
- 予算の実績: Bash 約 40 本（うち実測 約 12 本）、所要 約 20 分（着手からコミットまで 32 分から、実測スクリプトの作成待ちと実測の実行待ちを除く）、サブエージェント 2 本（実測スクリプト 1、止めない独立レビュー 1）
- 出荷: main-199
