# quick #184 toolbox（tools/dev.sh）の人間検証の残り: amd64 の cargo test、Docker Desktop / rootless、~/.aws/credentials
- 入力: issue #184（#131 から移した 3 項目）。モード: standalone。ブランチ main-184、着手前 SHA afaae7b（過去ノートの削除コミット 4763f9f の後から作業）
- ゲート: 本番コード 0 行（CI 1 ファイル +3、docs 2 ファイル）／事実が外れた（開発機は arm64・エミュレーション無し、rootless は uidmap が無い、Docker Desktop と ~/.aws は試せない）→ 1 問聞いた: amd64 は「CI の e2e に常設」、rootless は「このマシンで試す」（uidmap はユーザーが別シェルで入れた）
- 時系列: 着手 06:14 ／ コミット 06:22（途中の時刻は `date` で控えていない。rootless の導入・失敗の確認・uninstall はこの間）
- 検証: rootless: `dockerd-rootless-setuptool.sh install --force` → toolbox のイメージはビルドできるが `tools/dev.sh cargo test --locked` が `could not find Cargo.toml`（rc=101）。dev の中は uid 1000（ホストの subuid）で `/home/aoyagikouhei` に Permission denied、`/var/run/docker.sock` は nobody:nogroup で docker につながらない。minio の足場はリポジトリを読めない段階で止まるので流していない／amd64: e2e ジョブに `tools/dev.sh cargo test --locked` を足し、この PR の CI で確かめる
- 自己レビュー: ci.yml の手順は既存の run と同じ形。check ジョブの cargo test（ホスト直）とは別物（toolbox のイメージ・uid・マウントの上で通るか）なので重複ではない。debug の target はキャッシュしていないので e2e が数分延びる
- 打ち切り・escalate: はしご（事実 → 1 問聞く）。rootless は issue の指示どおり development.md に前提として書き、対応はしない（未着手と明記）。Docker Desktop と ~/.aws は #184 に残す
- 予算の実績: Bash 約 20 本、所要 約 8 分（イメージのビルド待ちを含む）、サブエージェント 0 本（本番コードを変えていないので止めない独立レビューは流さない）
- 出荷: main-184
- 後片付け: rootless の daemon は uninstall、CLI のコンテキストは default に戻し rootless を削除。rootless のデータ（~/.local/share/docker、3.2G）は残した（消すなら `rootlesskit rm -rf ~/.local/share/docker`）
