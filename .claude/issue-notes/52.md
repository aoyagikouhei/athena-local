# quick #52 動詞と TABLE の間にコメントを挟むと SubstatementType が None になり、CTAS は結果ファイルの置き場所も変わる
- 入力: issue #52。モード: standalone。ブランチ `main-52`、着手前 SHA 24d847f
- ゲート: サイズ 本番 43 行・3 ファイル（≤50・3。`catalog.rs` +34 うち doc 10、`classification.rs` +5/-4、`results.rs` +4/-7）／呼び出し元 3（`statement_type`・`substatement_type`・`metadata_query_id`。≤5・`operation/` 内、シグネチャ不変。`ResultFile::of` は同じ判定の複製を差し替えた）／契約: 通過（CTAS の置き場所が変わるのは本物に合わせる修正。README・CHANGELOG 同梱）・新規性: 新ファイルなし・判断: 新しい既定値なし → **事実が外れた**（本物のキーワード間コメントの扱いが未実測）
- 時系列: 着手 17:41 ／ 実測スクリプト作成・コミット 17:47 ／ 実測 18:08〜18:16（ユーザー実行、1 ラウンド、29 項目）／ 計画攻撃 18:18〜18:25 ／ red 18:19 ／ green 18:26 ／ コミット 18:29
- 検証: `cargo fmt --check` ok、`cargo clippy --all-targets --locked -- -D warnings` ok、`cargo test --locked` ok（lib 173 passed、結合テスト全 pass）／ミューテーション: `catalog::words` のコメントでの `break` を `i += 1` にすると 4 本落ちた。引用符の読み飛ばし（`skip_quoted`）を消すと 3 本落ちた／実測: 1 ラウンド（本物はキーワード間の `/* c */`・`-- c` を空白として分類。CTAS は成功・失敗とも `tables/<id>`。`SHOW /* c */ CREATE TABLE`・`SHOW CREATE /* c */ TABLE`・存在しないテーブルへの `ALTER /* c */ TABLE` は分類は付くが実行は ParseException。Iceberg への `ALTER /* c */ TABLE` は成功）／計画攻撃: 指摘 1 件・採用 1 件（リテラル中の `--`／`/*` をコメントと読む退行 → `skip_quoted` で飛ばす。design-checklist に 1 行）
- 自己レビュー: 分岐 5 件（空白で切る・引用符を飛ばす・コメントで切る・それ以外を進める・空の語を積まない）すべて `catalog.rs` のユニットテストが踏む／エラーの出口: 新しいエラーなし／呼び出し元 3 箇所 + `ResultFile::of`: 引用符付きリテラルが空白を含んでも 1 語になる点だけ語列が変わるが、位置固定の分岐はリテラルより前の語しか見ず、`is_create_table_as` はリテラル中の `AS SELECT` を拾わなくなる方向（既存テスト全 pass）
- 打ち切り・escalate: はしご（事実 → 実測工程 1 ラウンド。本番 2 ファイル以上のため計画攻撃 1 本も追加）。選択: 層を足して続行（ユーザー）。実測待ちの間にローカル Trino 482 で同じ 11 通りの SQL が受け付けられることを確認した。手元の Trino も本物も通す形なので README の Caveat に本物だけが弾く形（SHOW CREATE TABLE・Hive 側の ALTER）を追記した
- 予算の実績: Bash 約 27 本（うち実測 6 本）、所要 20 分（着手からコミットまで 48 分のうち実測待ち 28 分を除く。上限 20 分 + 実測 10 分 = 30 分の内）、サブエージェント 2 本（計画攻撃 1、止めない独立レビュー 1）
- 出荷: ブランチ `main-52`（PR 番号は `gh pr list --head main-52` で引ける）
