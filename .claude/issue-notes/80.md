# quick #80 SHOW FUNCTIONS の結果ファイルを本物と同じ <id>.csv（見出し行つきの CSV、SubstatementType SHOW_FUNCTIONS）にする

- 入力: issue #80。モード: standalone。ブランチ `main-80`、着手前 SHA 702936a
- ゲート: サイズ 本番の論理行 約 8 行・4 ファイル（≤50・**3 を超過**。`results.rs` の腕 1 つ、`classification.rs` の腕 1 つ、`query_execution.rs` の条件 1 つ、`content_type.rs` は到達しなくなる腕の語 1 つとコメント。`result_output.rs` はコメントだけ）／呼び出し元: `ResultFile::of` 1（`ResultLocation::new`）・`substatement_type` 2（`to_query_execution`・`get_query_results`）・`text_content_type` 1（≤5・同一モジュール）／契約: 文書化済みの値（ファイル名・`SubstatementType`・列名行）を本物に合わせる修正で README・CHANGELOG を同じコミットで直す条件で通す／新規性: 通過（新ファイル無し。SHOW の 2 語目を見るのは `SHOW CREATE` と同じ）／事実: 通過（#76 の生データ `~/athena-content-type-measurements/run-20260923-065415/h1-show-functions.*` で CSV の書式（引用・空文字 `""`・末尾改行）と `.metadata` の先頭 ID（Trino の ID）・GetQueryResults の列名行を確認）／判断: 通過（失敗した SHOW FUNCTIONS が結果ファイルを置くかは未測定なので、他の `.csv` の文と同じく置かない扱いにして README に未測定と書いた）
- 時系列: 着手 08:29 ／ 計画攻撃 08:32〜08:34 ／ red 08:33 ／ green 08:34 ／ コミット 08:37
- 検証: 標準コマンド ok（`cargo fmt --check`、`cargo clippy --all-targets --locked -- -D warnings`、`cargo test --locked`。lib 182 passed、結合テスト込みで 306 passed）／ミューテーション: `results.rs` の SHOW FUNCTIONS の腕を消す → `tests/results.rs` とユニットが落ちた、`classification.rs` の SHOW_FUNCTIONS の腕を消す → `tests/select.rs` の SubstatementType で落ちた、`query_execution.rs` の例外を消す → 列名行の件数（2 ≠ 3）で落ちた／計画攻撃: 指摘 3 件（既存テスト 2 件の落ち・文書の古い記述）・採用 3 件（前 2 件は red で先に対処済み、文書は Step 7 で直した）。high 0 件
- 自己レビュー: 分岐 3 件（ファイル名の腕・SubstatementType の腕・GetQueryResults の例外）すべてユニットか結合テストに有り（ページングも）／エラーの出口: 新しいエラー無し。`.csv` になったので本体の PUT 失敗が FAILED になる（SELECT と同じ扱い。`.txt` のときは SUCCEEDED のままだった）／呼び出し元 4 箇所: `ResultLocation::new` は Csv → `content_type::of` が SELECT と同じ判定で application、`.metadata` も同じ値。`to_query_execution` は `SHOW_FUNCTIONS` を載せる。`get_query_results` は列名行を残す。`text_content_type` は届かなくなった腕を消しただけ
- 打ち切り・escalate: はしご（変更サイズのファイル数 4 > 3 → 計画攻撃 ＋ 止めない独立レビュー）。ユーザーが quick を指定していたので implement-issue は提案せず、層を足して続けた。stash 無し
- 予算の実績: Bash 約 16 本、所要 約 8 分（08:29〜08:37）、サブエージェント 2 本（計画攻撃 1・止めない独立レビュー 1）
- 出荷: standalone、ブランチ `main-80`
