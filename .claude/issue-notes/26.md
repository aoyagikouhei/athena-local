# quick #26 is_iceberg_table がコメントと文字列リテラルの中の table_type='ICEBERG' も数える
- 入力: issue #26。モード: standalone。ブランチ `main-26`、着手前 SHA 505d592
- ゲート: サイズ 1 ファイル・本番 12 行追加 18 行削除（≤50・3）／呼び出し元 1（`operation/execution.rs:188`。`is_iceberg_table` は `results.rs` 内の 1 箇所）／契約は README に書いた値を実測に合わせる修正なので同じコミットで README・CHANGELOG を直す条件で通過、新規性なし → **判断と事実が外れた**（issue が直し方の範囲を未決／本物の挙動が未実測）
- 時系列: 着手 21:34 ／ 実測スクリプト 21:35-21:43 ／ 実測（ユーザー実行）21:45-21:50 ／ 判断 21:54 ／ red 21:56 ／ green 21:57 ／ コミット 22:00
- 検証: `cargo fmt --check` ok、`cargo clippy --all-targets --locked -- -D warnings` ok、`cargo test --locked` ok（220 passed / 0 failed）／ミューテーション: `ResultFile::of` に旧判定（`is_create_table_as` かつ空白を潰した全文に `TABLE_TYPE='ICEBERG'`）を戻す → `ctas_は_iceberg_でも_tables_を付ける` が落ちた（left: Manifest, right: Table）／足した層: 実測 1 ラウンド（6 項目すべて測れた）、1 問（今日の実測と 2026-09-17 の実測のどちらを採るか）
- 自己レビュー: 分岐は 1 本減るだけ（CTAS の Iceberg 腕を削除）で、残る 5 分岐はユニットの表と新テスト 11 件が踏む／新しいエラー戻り値なし／呼び出し元 1 箇所では `Manifest` → `Table` でキーが `<id>` から `tables/<id>` に変わるだけ（本体を書かない・`failed()` は None・Content-Type は同じ `binary/octet-stream` という点は両者同じ）
- 打ち切り・escalate: はしご（判断 → 1 問聞く、事実 → 実測 1 ラウンド）。escalate なし。stash なし
- 予算の実績: Bash 約 24 本、所要 26 分（実測の待ち時間を除く）、コミット 2（実測スクリプト 1 + 実装 1。実測のコミットは docs 単独に数えない）、サブエージェント 1 本（止めない独立レビュー）
- 出荷: ブランチ `main-26`（PR 番号は `gh pr list --head main-26` で引ける）

## 実測の結果（2026-09-19）

`26-measure-iceberg-detection.sh` を本物の Athena（engine version 3、workgroup `primary`）で 1 ラウンド。
6 項目すべて測れた。`table_format` は `SHOW CREATE TABLE` による裏取り。

| | SQL | table_format | 本物の OutputLocation | 当時の athena-local |
|---|---|---|---|---|
| A | 素の CTAS | hive | `tables/<id>` | `tables/<id>` |
| B | `AS SELECT 1 AS i -- table_type = 'ICEBERG'` | hive | `tables/<id>` | `<id>` |
| C | `AS SELECT 'table_type=''ICEBERG''' AS s` | hive | `tables/<id>` | `<id>` |
| D | `WITH (table_type = 'ICEBERG')` | **iceberg** | **`tables/<id>`** | `<id>` |
| E | `WITH (table_type /* c */ = 'ICEBERG')` | **iceberg** | **`tables/<id>`** | `tables/<id>` |
| F | `WHERE t.table_type = 'ICEBERG'`（WITH 句の外） | hive | `tables/<id>` | `<id>` |

付随ファイルはどれも `tables/<id>.metadata`（81 バイト）で、本体は置かれない。

**D が 2026-09-17 の実測（`.claude/issue-notes/5.md:76`、`12.md`。「Iceberg の CTAS は `<id>`」）と食い違う。**
投げた SQL の形は `5-measure-metadata.sh:252` と同一（`location` を結果プレフィックス配下に指定、`is_external = false`）で、
ワークグループもエンジン版も同じ。原因は特定できていない。2026-09-19 ユーザーの判断: **今日の実測を採る**
（`is_iceberg_table` ごと消して CTAS は常に `tables/<id>`）。

## 未実測のまま残るもの

- `INSERT` の `<id>`（2026-09-17 実測）は今回測っていない。同じ 1 ラウンドで採られた D が覆ったので、こちらも再実測の価値がある → #35 に起票
- カタログの既定など、`WITH` 句以外からテーブルの形式が決まる場合（Athena では作れなかったので項目にしていない）
- ~~`CREATE OR REPLACE TABLE ... AS`（Trino だけの構文）のファイル名は引き続き未実測~~ → #93 で 2026-09-23 に実測。本物は構文エラー（`mismatched input 'TABLE'`）で受け付けないので、ファイル名は存在しない
