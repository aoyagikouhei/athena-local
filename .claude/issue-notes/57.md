# quick #57 SHOW TABLES 以外の SHOW 4 文の .txt.metadata を Athena JDBC 3.8.1 が読めるか確かめる

- 入力: issue #57（#24 の「未実測のまま残るもの」から起票された実測課題）。モード: standalone。ブランチ `main-57`、着手前 SHA `b8b67eb`
- ゲート: サイズ 本番 0 行・0 ファイル（≤50・3）／呼び出し元 0（≤5・同一モジュール）／契約 通過（`src/` に触らない）・新規性 通過（`src` に新ファイル無し）・判断 通過（新しい既定値なし）／**事実が外れた**（正解が Athena JDBC ドライバの実挙動でしか決まらない）→ はしごで実測工程だけ足した（ユーザーが「実測工程を足して続ける」を選択）
- 時系列: 着手 19:11 ／ ゲート判定とブランチ作成 19:12-19:14 ／ 実測ラウンド 1 19:16-19:18（スクリプトの判定 3 箇所が実態と合わず、生ログで差の正体を確認）／ README 更新 19:19 ／ ラウンド 2 19:21-19:22 ／ green（全判定 PASS）19:22 ／ コミット 19:24
- 検証: 標準コマンド ok（`cargo fmt --check` ok、`cargo clippy --all-targets --locked -- -D warnings` ok、`cargo test --locked` 287 passed / 0 failed。実測と同時に走らせた 1 回目は `tests/retention.rs` の 2 件が負荷で落ちた → #61 に起票、単独では通る）／ミューテーション: 突き合わせ判定の「GetQueryResults は SHOW で +1 行」を外すとラウンド 2 の保存ログで SHOW の 3 ケースが FAIL になる（判定が効いている）／実測: 2 ラウンド
- 自己レビュー: 分岐 0 件（本番コードの変更なし）／エラーの出口 なし／呼び出し元 0 箇所。変更は検証の足場（`39-e2e/jdbc-client/Main.java` に 2 つ目の引数でシナリオ切り替え。#46 の 4 ケースは引数なしのまま）と docs（`README.md`・`39-e2e/README.md`）と新しい実測スクリプトだけ
- 打ち切り・escalate: はしご（事実 → 実測工程）。提案なし。予算の超過なし（Bash 約 25 本 / 上限 30 + 2 × 15 = 60、所要 約 16 分 / 上限 20 + 2 × 10 = 40。実測の実行待ちを除く）
- 予算の実績: Bash 約 25 本（うち実測の実行 2 本とその調査 5 本）、所要 約 16 分、サブエージェント 0 本（止めない独立レビューを除く）
- 出荷: standalone。ブランチ `main-57`

## 実測の結果（2026-09-22）

**結論: JDBC 経由で athena-local に届く SHOW は 3 文（`SHOW TABLES` / `SHOW SCHEMAS` / `SHOW COLUMNS`）で、その `.txt.metadata`（素の protobuf）を Athena JDBC 3.8.1 は既定の `ResultFetcher=auto` で読み込み、例外を出さない。残る `SHOW DATABASES` / `SHOW PARTITIONS` / `SHOW TBLPROPERTIES` は Trino の文法に無く、構文チェックで弾かれるのでファイルに到達しない。athena-local の実装を見直す必要は無い。**

| 文 | `.txt.metadata` | auto | S3 明示 | GetQueryResults |
|---|---|---|---|---|
| `SHOW TABLES ... LIKE`（対照） | 72 バイト（`Table` 1 列） | `loaded query result metadata`、1 行 | 取りに行かない、1 行 | S3 を読まない、2 行（先頭が `Table`） |
| `SHOW SCHEMAS` | 74 バイト（`Schema` 1 列） | 同上、2 行 | 同上、2 行 | 3 行（先頭が `Schema`） |
| `SHOW COLUMNS FROM t` | 205 バイト（Trino の 4 列） | 同上、2 行 | 同上、2 行 | 3 行（先頭が `Column`） |
| `SHOW DATABASES`（原文） | 無し | `mismatched input 'DATABASES'`（400。`QueryExecutionId` なし） | 同左 | 同左 |
| `SHOW PARTITIONS t`（原文） | 無し | `mismatched input 'PARTITIONS'` | 同左 | 同左 |
| `SHOW TBLPROPERTIES t`（原文） | 無し | `mismatched input 'TBLPROPERTIES'` | 同左 | 同左 |
| `SELECT * FROM "t$partitions"`（対照） | `.csv.metadata` 64 バイト | 2 行 | 2 行 | 2 行 |

3 モードとも `failures=0`。nginx のアクセスログで `.metadata` への GET は 10 件（auto 7 件、S3 明示 3 件、GetQueryResults 0 件）。

観察（README の Caveats に書いた）:

- auto はドライバのログに `loaded query result metadata from ".../<id>.txt.metadata"` を 3 文ぶん残すが、`ResultSetMetaData` は `_col0:varchar` の 1 列で、`.txt` の 1 行を 1 値として返す（`SHOW COLUMNS` の 4 列はタブ区切りの 1 文字列になる）。metadata の列情報は列の見え方には使っていない
- S3 を明示すると `.txt.metadata` を取りに行かない（`.csv.metadata` と INSERT の `.metadata` は取る）。差の理由はドライバのソースが非公開なので分からない
- GetQueryResults は SHOW（UTILITY）で見出し行をデータとして返す（`SELECT` では読み飛ばす）。#5 の「気づいたこと」4 と同じ観察で、未起票だったので #60 に起票した
- 余分な `.csv.metadata` 3 個は、ドライバが接続時に流す接続テストの `SELECT`（#5 で判明、#52 で `.csv` になった）

## 実測の経緯（2 ラウンド）

| ラウンド | 変えた条件 | 結果 |
|---|---|---|
| 1 | 初回 | 3 モードとも例外なし。スクリプトの判定 3 箇所が実態と合わず FAIL: `SHOW TABLES` がラウンドごとに 1 行増える（テーブル名にラウンド ID を含む）、GetQueryResults が SHOW で +1 行、S3 明示が `.txt.metadata` を取らない |
| 2 | `SHOW TABLES ... LIKE` で自分のテーブルに絞る。判定を観察に合わせる（+1 を既知として期待、S3 明示は観察のみ） | 全判定 PASS |

ラウンド 1 の FAIL はいずれもドライバ側の事実ではなく判定の設計だったので、生ログ（RESULT 行、`od`、nginx）で差の正体を突き止めてから直した（measurement.md「保存した生データは中身も同じラウンドで読む」）。

## 判断

- **CHANGELOG は更新しない。** 本番コードの挙動が 1 行も変わらないため（#46 と同じ）
- README の Caveats「SHOW metadata is not the opaque form Athena writes」に、読める 3 文・到達しない 3 文とその回避策・ドライバの見え方を追記した
