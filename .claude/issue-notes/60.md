# quick #60 GetQueryResults が SHOW / DESCRIBE でも列名の見出し行を返すが、本物の Athena の挙動は未実測
- 入力: issue #60。モード: standalone。ブランチ main-60、着手前 SHA 78e6de7
- ゲート: サイズ 7 行・1 ファイル（≤50・3）／呼び出し元 3（all_rows。変更せず、get_query_results 側で外す）／契約: GetQueryResults の行数が変わるが本物に合わせる修正で README・CHANGELOG 同梱のため通過／新規性: 通過／事実: **外れ**（本物の挙動が未実測）／判断: 通過
- 時系列: 着手 19:28 ／ 実測（既存の生データの読み直し）19:31〜19:33 ／ red 19:34 ／ green 19:34 ／ コミット 19:37
- 検証: `cargo fmt --check` ok、`cargo clippy --all-targets --locked -- -D warnings` ok、`cargo test --locked` ok（全 21 バイナリ通過。select は 9 passed）／ミューテーション: 見出し行を外すガードを `false` に → `show_と_describe_の結果には列名行が入らない` が落ちた／足した層の結果（実測: 新規ラウンド 0。過去 5 ラウンド（2026-09-15〜09-22、`~/athena-*-measurements`）の GetQueryResults 応答 130 件を読み直し、DML（SELECT / EXPLAIN）は先頭行＝列名、UTILITY（SHOW TABLES / DATABASES / COLUMNS / CREATE TABLE / PARTITIONS / TBLPROPERTIES、DESCRIBE）は先頭行＝データで一貫。`.txt` の行数（見出し無し）とも一致。PyAthena は先頭行の値が列名と一致したときだけ読み飛ばす値ベースの判定で、文の種類は見ない（result_set.py `_is_first_row_column_labels`）。awswrangler は S3 の結果ファイルを読む経路が主で、GetQueryResults の経路は未確認）
- 自己レビュー: 分岐 3 件（UTILITY で行あり→外す: SHOW / DESCRIBE のテスト、DML→残す: EXPLAIN と既存 SELECT のテスト、行なし→何もしない: 既存の DDL テスト dml.rs:34）／エラーの出口: 新規なし／呼び出し元: statement_type の既存呼び出し（update_count）はシグネチャ不変で影響なし。all_rows の 3 呼び出し元は変更なし（to_csv は列名行あり、to_text は自前で外す）
- 打ち切り・escalate: はしご（事実 → 実測工程）。ユーザーは「実測工程を足して続ける」を選択。新規の実測依頼は不要だった（過去ラウンドの生データに答えがあった）。範囲外の発見を #63（EXPLAIN の .txt に本物は Query Plan の見出し行を入れる）に起票
- 予算の実績: Bash 約 20 本（うち実測の読み直し 4 本）、所要 約 9 分、サブエージェント 1 本（止めない独立レビュー）
- 出荷: main-60
