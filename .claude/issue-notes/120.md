# quick #120 dbt-athena の dbt run を本物の Glue・STS 相手に通す（人間検証）
- 入力: issue #120（#111 の人間検証）。モード: standalone。ブランチ main-120、着手前 SHA 9053c40（過去ノートの削除コミット fd8244f の後から作業）
- ゲート: 本番コード 0 行（docs 2 ファイル）／契約・新規性なし／事実が外れた（本物の Glue・STS が要る）→ 1 問聞いた: 「手元で確かめて閉じる」を選択（dbt-athena のマクロから CTAS が必ず table_type・is_external を入れることを先に確認）／判断: athena-local は SQL を書き換えない（CLAUDE.md）ので変えない
- 時系列: 着手 06:49 ／ 回答 06:51 前後 ／ 手元の確認 06:52 ／ コミット 06:53
- 検証: toolbox で compose の trino・minio を立て、athena-local の release ビルドに dbt と同じ形の CTAS を送った。Hive・Iceberg とも FAILED（`INVALID_TABLE_PROPERTY: ... table property 'table_type' does not exist`）、対照の `with (format='parquet')` の CTAS と `create or replace view` は SUCCEEDED。1 回目は ClientRequestToken を付け忘れて StartQueryExecution で 4 本とも弾かれた（athena-local の必須の検証どおり）ので付けて流し直した／`cargo fmt --check`・`cargo clippy --all-targets --locked -- -D warnings`・`cargo test --locked` ok（398 passed）
- 自己レビュー: docs の主張（table・incremental・snapshot・seed が create_table_as を使う）はマクロの grep で裏取り。is_external が弾かれることは直接は見ていない（Trino は最初の不正なプロパティで止まる）と記録に明記
- 打ち切り・escalate: はしご（事実 → 1 問聞く）。本物の Glue・STS との組み合わせは流さずに閉じる
- 予算の実績: Bash 約 14 本、所要 約 4 分（回答の待ちを除く）、サブエージェント 0 本（docs だけなので止めない独立レビューは流さない）
- 出荷: main-120
