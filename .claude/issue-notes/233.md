# quick #233 compose の Trino で CREATE SCHEMA した名前空間が system.jdbc.schemas・SHOW SCHEMAS に出ない
- 入力: issue #233。モード: standalone。ブランチ main-233、着手前 SHA e368f0b
- ゲート: 本番コード 0 行（設定 3 ファイル: tools/compose/catalog の 2 つと compose.yml、ほかに足場のコメントと development.md）／呼び出し元: カタログ設定を読む足場はすべて compose の trino 経由／契約なし（開発環境だけ）・新規性なし・事実は生の Trino CLI で再現・判断: 使われていない volume を外す（足場はどれも down -v で作り直すので永続化に頼っていない）→ 通過
- 時系列: 着手 19:14 ／ red 19:16（CREATE SCHEMA 後の SHOW SCHEMAS に出ない。書き込みは /tmp/data/<catalog>、volume の /data/<catalog> は root 所有で空）／ green 19:19 ／ 足場 8 本 19:19〜19:23 ／ コミット 19:24
- 検証: 原因は local fs の根が local.location（既定 /tmp）で、場所を file:// で書くと書き込みは通るのに一覧が空になること。local:///hive・local:///iceberg で SHOW SCHEMAS・system.jdbc.schemas に出て、CTAS と SELECT も通る（482）。ミューテーション: hive だけ file:///data/hive に戻す → hive の一覧からだけ消えた。標準コマンド fmt・clippy ok、cargo test 532 passed。足場: context-catalog 15/15、utility-rows 21/21、create-table-catalog 10/11（M3 は #237 の後の古い期待値。#254）、ctas-catalog 8/8、s3-tables-location 12/12、quoted-names 27/27、result-content-type 20/20、minio は終了コード 0（FAIL の件数が終了コード。出力は末尾だけ残し、見えた 15 行はすべて PASS）。jdbc-drivers・python-clients・trino-probe は流していない（ドライバ取得が重い。trino-probe は自前の catalog/ を使い本変更の影響外）
- 自己レビュー: 分岐なし（設定）／エラーの出口なし／local.location=/ で file:///data を使う案は volume が root 所有で CREATE SCHEMA が Could not write database schema になり不採用。trino-probe の catalog*/ は旧版の起動を測る足場で、版によって local:// を扱えるか未確認のため触らない
- 打ち切り・escalate: 提案なし
- 予算の実績: Bash 約 25 本、所要 約 10 分（着手 19:14〜コミット 19:24。足場の走行中は docs とノートを進めた）、サブエージェント 0 本
- 出荷: main-233。範囲外の出口: create-table-catalog の M3 の古い期待値を #254 に起票
