# issue #49 skip_keyword が classification.rs と target_table.rs に同名で別定義されている

## 基本情報

| | |
|---|---|
| issue | #49 |
| ベースブランチ | `main` |
| ブランチ | `main-49` |
| 着手前 SHA | `d4aa0f9` |
| 入口 | quick-implement のゲートで「事実」（red 不成立）と「影響範囲」が外れ、ユーザーの選択で implement-issue に escalate（コミット `25ec0bf`、issue コメント https://github.com/aoyagikouhei/athena-local/issues/49#issuecomment-5772264335） |
| 経路 | 小規模 issue の経路（フェーズ 2 以下）。**move-refactor の経路は使わない**（→ 設計判断 D1） |

## 事前に確認した事実

Explore 2 本（調査）と実装での実測をまとめたもの。**実装が終わったので、次の issue が読む価値のあるものだけ残した**
（quick のゲートの 6 項目と、置き場・可視性の判断に使って D2・計画 3 に反映済みの項目は落とした）。

### 統合の根拠（Explore A）

1. **本体の差分は 3 行**（issue 本文の「違いは 1 行だけ」は不正確）。意味のある差分は
   `classification.rs` 版の `let trimmed = crate::catalog::skip_leading_trivia(input);` の 1 行で、
   残り 2 行は `input` → `trimmed` の字面のみ相違。それ以外は一字一句一致。

2. **`target_table.rs` 側の呼び出し 4 箇所（`table_name_start:67, 68, 70, 71`）はすべて
   「トリビア読み飛ばし済み」の文字列を受け取っていた**。74 行目だけは性質が違い、
   `skip_keyword` の前処理ではなく `parse_qualified_name` へ渡す直前の最終トリム。

3. **事前トリム 4 箇所は消せる。74 行目だけは消せない**。消すと `"DROP TABLE t"` のような普通の入力でも
   `rest` が `" t"` のまま返り、`read_name_part`（`target_table.rs` の `end == 0` 分岐）が `None` を返す。
   → issue 本文の「素朴に寄せると読み飛ばす位置が変わる」という警告は、**5 箇所のうち 1 点だけ**が該当した。
   消せる根拠は当初「冪等性」に置いていたが、計画レビューがより弱い前提で足りることを示した（→ D2-(b)・前提表）。

4. **`classification.rs` 側の呼び出しは 16 式**（15 行。131 行目だけ 2 式）。
   **issue 本文と quick のゲートの「12 箇所」は誤り**。全呼び出しが一貫して**未読み飛ばし**の文字列を受け取る。
   とくに `classification.rs:89` の `rest` は `catalog::skip_qualified_name` が末尾のトリビアを
   読み飛ばさずに返すため、テーブル名直後の空白・コメントを含む。**これが内部トリムを持つ理由そのもの**。

5. **既存テストのギャップ**: `target_table.rs` の 14 件には「キーワード直後に空白が無い入力」を含むテストが
   1 件も無かった。その軸を固定していたのは `classification.rs` の
   `substatement_type_は_alter_table_のキーワードの直後に空白が無くても分類する` だけ。

6. **`missing_docs` 相当の lint は無効**（`src/lib.rs`・`src/main.rs` にクレートレベル属性 0 件、
   `Cargo.toml` に `[lints]` 無し、`clippy.toml` 無し）。**doc コメントの消失は機械検査でしか拾えない**。
   日付付きの実測記録は `catalog.rs` 2 行・`classification.rs` 17 行・`target_table.rs` 0 行だった。

7. **基準値**（着手前。`d4aa0f9`）: `cargo test --locked` **275 passed / 18 スイート**。
   `catalog.rs` test 24 件・doc 54 行・467 行／`classification.rs` 4 件・23 行・346 行／
   `target_table.rs` 14 件・21 行・392 行。`src/` 全体の `実測|採取` を含む行は **128 行**。

### リポジトリの慣習（Explore B。次の issue でも使える）

8. **`catalog.rs` の可視性の基準**: 「`operation/` から呼ばれるから `pub(crate)`」でおおむね説明できるが、
   `comment_end` が反例（`pub(crate)` だが `operation/` からの呼び出しは 0 件）。より正確なパターンは
   **「doc に『どのファイルが再利用するか』と issue 番号を明記したものが `pub(crate)`、
   私的な実装補助でそれが無いものが private」**。

9. **`catalog.rs` のモジュール doc が第二の責務を明記している**（`catalog.rs:14-15`）:
   「文の種類の判定（`operation/classification.rs`／`results.rs`）が …読み飛ばすのにも使う字句処理
   （`skip_leading_trivia`）をここに置く」。特定の関数名の名指しで一般憲章ではないが、
   `skip_keyword` も収まる読み方ができる（#49 でこの行に `skip_keyword` を書き足した）。

10. **呼び出しの書き方の多数派は `crate::catalog::foo()` のフルパス**（`src/operation/` で 15 件）。
    `use crate::catalog;` は `src/` 全体で 0 件。

11. **テストの慣習**: `mod tests` は `src/` 全体で 15 ファイル、すべて `use super::*;`。
    `catalog.rs` の 24 件のうち `skip_leading_trivia` 系 9 件・`skip_qualified_name` 系 6 件は関数名で直接呼ぶが、
    `skip_quoted`／`comment_end`／`unquote` は `pub(crate)` でも直接テスト 0 件（間接検証のみ）。
    → 計画レビューの削減指摘（`skip_keyword` の直接テストを足さない）の根拠になった。

12. **標準コマンド**（CLAUDE.md、CI と同じ）: `cargo fmt --check` ／
    `cargo clippy --all-targets --locked -- -D warnings` ／ `cargo test --locked`。

13. **README・CHANGELOG の条件**: 内部構造への言及が両方とも 0 件。CLAUDE.md の約束は「挙動を変えたら」が条件で、
    先例は `44.md` の D7。コミットと PR の本文は日本語（動詞終止形の 1 行）。

14. **`catalog.rs:141-151` の「用途が違うので無理に共通化しない。issue #44」は `skip_qualified_name` と
    `parse_qualified_name` の対についての記述**で、`skip_keyword` への言及は無い。
    `44.md` の D3 は「#44 の中ではやらない」で、44.md 自身が #49 を起票している。

## 設計判断

### D1. move-refactor の経路は使わない（統括役が判断、2026-09-22 15:43）

`~/.claude/skills/implement-issue/references/move-refactor.md` の「この経路を使う条件」が、
**「似た関数の統合」を挙動の変更として明示的に除外している**:

> 次は**挙動の変更なので同じ issue に混ぜない**（見つけたら起票する）: 長い関数の内部分割、似た関数の統合（コピー由来でも
> 関数単体の挙動が違うことがある。実例: 同名の 2 関数の片方だけが内部で読み飛ばしを呼んでいた）、名前の付け直し、デッドコードの削除

「実例」として挙げられているのが **この issue そのもの**（#44 の D3 で残した判断が教訓化されている）。
したがって移動・分割の定型（正規化 diff を中心にした検証）はそのまま当てはめず、**小規模 issue の経路**で進める。

ただし危険の種類は移動・分割と同じ（red が成立せず、消えても green のままになるものがある）ので、
move-refactor.md の検証の考え方（基準値の着手前実測・機械的な合否判定・わざと壊して足場が NG を出すことの確認）は借りる。

quick のゲート判定でユーザーに提示したプレビューには「正規化 diff 検証スクリプト」と書いたが、定型が除外している以上、
検証の形は計画で決め直す（→ 覆した事前判断）。

### D2. 案 B（`classification.rs` 版に完全統合）を採る。案 A（ラッパー維持）は採らない

Plan の評価を受け入れた（統括役が確認）。**Step 6b でユーザーに提示して承認を取る**
（quick のゲートでプレビューに案 A を出しており、それを覆すため）。

| | 案 A（ラッパー維持） | 案 B（採用） |
|---|---|---|
| `catalog.rs` | 素の（トリムしない）`skip_keyword` | **内部でトリムする** `skip_keyword` |
| `classification.rs` | 1 行ラッパーを残す。呼び出し 16 式は無傷 | 定義を消す。16 式を `crate::catalog::skip_keyword` へ |
| `target_table.rs` | 定義を消し、4 式を修飾。構造はそのまま | 定義を消し、4 式を修飾。**事前トリム 4 箇所（66・68・69・71）を削除。74 行は残す** |

案 B を採る根拠:
- **(a) issue タイトルを文字通り解消する**。案 A では `classification.rs` に `fn skip_keyword` の**定義**が
  1 行ラッパーとして残り、「同名で別定義」という字面は消えない。
- **(d) `table_name_start` が 11 行 → 7 行**になり、明示トリムが 5 箇所 → 1 箇所に減る。
  ただし「なぜ最後の 1 箇所だけ残るのか」の非対称性が自明でなくなるので、doc での説明を必須にする。
- **(e)** 案 A は「ラッパーを書けば呼び出し元を変えずに済む」前例を作り、次の字句ヘルパで同じパターンが再生産されうる。

案 A が優位な点と、それを採らない理由:
- **(b) 挙動不変の立証しやすさ**: 案 A は呼び出し元を一文字も変えないので自明。案 B は
  **「trivia の無い文字列に trim をかけても no-op」**という実装から自明な事実に依拠する
  （当初は「`skip_leading_trivia` の冪等性（doc 未保証）」に依拠すると書いていたが、計画レビューが
  より弱い前提で足りることを示した → 前提表）。フェーズ 1 のテストとミューテーション候補 4・5 で
  機械的に検証可能な形にも転化できる。
- **(c) 変更の狭さ**: 案 B は 20 式を書き換えるが、すべて「関数名の修飾」という機械的で浅い変更。
  対して案 A は「なぜラッパーが残るのか」を読者が毎回確認するコストを永続的に残す。

issue 本文自身が「`classification.rs` 版に寄せて…明示呼びを外す形が考えられる」と案 B の方向を示唆し、
唯一の危険点（`table_name_start` の最終トリム）を名指しで警告している。その警告への対処は
ミューテーション候補 5 として実装前に具体化済み。**優先度が低いことは「中途半端な解決で済ませてよい」ことを意味しない。**

### D3. 2 フェーズ構成にする（フェーズ 1 = 挙動固定テスト、フェーズ 2 = 統合）

フェーズ 1 のテストは統合前も後も通る（red にならない）が、**分ける価値がある**:
(i) 着手前のツリーで全 green の状態でコミットを切ることが「期待値が思い込みではなく着手前の実測値である」ことの証跡になる
(ii) フェーズ 2 の差分を本体変更のみに絞れ、レビュアーが「テストが変わっていないか」を気にせず済む
(iii) テスト総数の増分を記録でき、フェーズ 2 で減っていないことの基準になる。

**明記する限界**: red が成立しないため、フェーズ 1 の期待値に書き間違いがあれば両フェーズとも間違った値で green のまま。
埋める手段は「期待値を機械的に実測から転記する」手順（計画の 4）と、**フェーズ 1 でのミューテーション確認**（→ D4）。

### D4. Plan の判断を 3 点直す（統括役の判断）

1. **フェーズ 1 でもミューテーション確認を行う**（Plan は フェーズ 2 のみを想定）。
   フェーズ 1 の時点で `classification.rs:139` の `skip_leading_trivia` を消すと **B 側のテストだけが落ち、A 側は落ちない**
   ことを確かめる。これが「フェーズ 1 のテストが実際に `skip_keyword` を通っている」ことの実行による裏取りで、
   D3 の限界（期待値の書き間違いに気づけない）を部分的に埋める。
2. **検証の足場に「実測記録の全件突き合わせ」の 1 コマンドを足す**。Plan は「`44-verify/check.sh` は移動用なので
   転用できない」として専用スクリプトを作らないと判断したが、**その check.sh のチェック 4（`実測|採取` を含む行を
   着手前と現在で `src/` 全体の集合として突き合わせる）は移動でも統合でも同じように効く**。
   ファイルをまたぐ転記（`classification.rs` → `catalog.rs`）でも集合は保たれるので、そのまま使える。
   専用スクリプトは作らず、コミット前チェックの 1 コマンドとして回す（→ 計画の 6）。
   目視（Plan の提案 3）も併せて行う。
3. **実機検証の「着手前の対照取り」は docker の疎通確認だけ先に行い、完全な実行は Step 9 に置く**。
   `verify.sh` は `cargo build --release` と docker compose を使うので、フェーズの `cargo test` と直列にしかできない。
   着手前の全 PASS は #39 の出荷時に確認済みで、CI も回っている。Step 9 で FAIL したら着手前 SHA でも走らせて切り分ける。

## 計画

**実施済み。結果はフェーズの記録にあるので、判断の根拠だけ残した**
（ファイル構成・手順・ミューテーション候補の表・検証手順・先行できる作業は実績に置き換わったので落とした）。

### 1. 一致台帳

この issue は「触るコピーペア」そのもの。対は `classification.rs:138-151` と `target_table.rs:79-91` の 2 箇所。
**守り方は「共通化する」を選んだ**（案 B）。統合後は対が物理的に 1 つになるので、
以後「一致確認」という運用上の負担自体が消える。

### 2. `catalog.rs` への追加で決めたこと

- **置き場所**: `skip_leading_trivia` の直後、`skip_trivia` の手前。`catalog.rs` は
  「使う側の直後に依存先を置く」並びで（`next_is_dot` が依存する `skip_trivia` の直後にある）、
  `skip_keyword` は `skip_leading_trivia` に直接依存するため。
- **可視性**: `pub(crate)`（事実 8 の基準に合致）。
- **doc**: `classification.rs` 版の 4 行を**文言のまま**転記した。`実測` を含む行の文言を保つことで、
  検証 2（実測記録の全件突き合わせ）が完全一致で通り、消失と変質の両方を機械的に検出できる。
- **`mod tests` への直接テストは 0 件**（計画レビューの削減指摘を採用）。ミューテーション候補 1-4 の
  検出責任はすべてフェーズ 1 のテストにあり、直接テストに検出責任を負わせている箇所が 1 つも無い。
  事実 11 の先例（`skip_quoted`・`comment_end`・`unquote` は `pub(crate)` でも直接テスト 0 件）にも合う。

### 3. 挙動固定テストの入力集合（6 軸）

(1) トリビアの位置（先頭・キーワード間・`IF`/`EXISTS` 間・名前の直前・**2 つ重ねる**）
(2) 識別子境界 (3) キーワード直後の空白なし（**A 側の `DROP TABLE"my table"` は既存テストに無かった**）
(4) 大文字小文字 (5) マルチバイト境界＋短い入力 (6) `IF EXISTS` の亜種。

**期待値は着手前のコードに一時テストを足して実際に流した出力をそのまま焼き込む**（推測で書かない）。
焼き込み後に一時テストを消し、`git diff --stat` で本体が 0 行差分であることを確認してからコミットする。

### 4. 前提表（依拠する既存機構）

| 機構 | 暗黙の前提 | 破るか | 誰が保証しているか |
|---|---|---|---|
| `skip_leading_trivia` | **trivia の無い文字列に trim をかけても no-op**（冪等性の弱い形で足りる。計画レビューの指摘） | 破らない | `catalog.rs:112-115`（先頭が非 trivia なら 1 回目の判定で即 `break` する実装から自明）。より強い「任意の入力での冪等性」を明示する doc は無いが、**この計画はそこまで要求しない** |
| `skip_qualified_name` | 末尾のトリビアを読み飛ばさずに返す | 破らない | `catalog.rs:152-163`（158-161 行） |
| `parse_qualified_name` | **入力の先頭に空白・コメントが無いこと** | **破る唯一の危険点**（`table_name_start` の最終トリムを誤って消すと破れる） | `target_table.rs:94-107`。doc に明記されていない（**未保証**。最終トリムが事実上の保証者）。→ #49 でその doc を書き足した |
| `read_name_part` | 同上 | 同上 | `target_table.rs:111-127`（`end == 0` 分岐） |

**前処理を共有関数に差し込むときの列挙**（design-checklist の #17 由来の項目）:
`alter_table_action` の 16 式は**一貫して未読み飛ばしの文字列を受け取る**（前段の `tail` を渡す連鎖）。
これが統合後の `skip_keyword` を「内部トリムする」仕様にできる根拠そのもの。
`table_name_start` の 4 式は現在すべて**明示的にトリム済み**を受け取っており、統合後は冗長になるだけで安全。
最後の 1 箇所だけは `skip_keyword` を経由しない別処理なので、前処理後の値として残す必要がある。

### 5. 未検証の前提（出荷時点でもそのまま）

| | 始末 |
|---|---|
| `skip_leading_trivia` の**任意の入力での**冪等性は doc で保証されていない | **この計画は要求しない**（弱い形で足りることを計画レビューが示した）。フェーズ 1 の軸 1（2 つ重ねる入力）が実行時にも裏付けている。対応不要 |
| `parse_qualified_name`／`read_name_part` の「先頭に空白が無い」前提が、将来 `parse_qualified_name` が新しい呼び出し元を得た場合にも成り立つか | 本 issue の範囲外。現在の唯一の呼び出し元（`parse_target_table`）では `table_name_start` の最終トリムが保証しており、その理由を doc に書き足した。仮定の話なので**起票しない** |
| `classification.rs` の 16 式すべてが未読み飛ばしを受け取るという結論は Explore の目視で、型レベルで保証する機構は無い | フェーズ 1 の B 側テストが実行時に検証している（ミューテーション候補 0・4 で確認済み）。対応不要 |
| 本物の Athena が動詞と `TABLE` の間のコメントをどう扱うか | **#52 に起票**（この issue の範囲外の発見） |

## 計画レビューの結果

Explore 1 本（小規模 issue の経路）。2026-09-22。指摘 3 件（medium 1・削減 2）。

### 採用（3 件）

| # | severity | 指摘 | 対応 |
|---|---|---|---|
| R1 | 削減（必須） | `catalog.rs` の `mod tests` への直接テスト 5-7 件は外せる。ミューテーション候補 1-4 の検出責任はすべてフェーズ 1 のテストにあり、直接テストに検出責任を負わせている箇所が 1 つも無い。軸も決定 4 の 1-5 と字面まで一致 | **採用**。0 件にした（計画 3）。既存の `skip_quoted`・`comment_end`・`unquote` も直接テスト 0 件という先例に合う |
| R2 | low（削減） | ミューテーション候補 6（二重適用の実験）は、実装から結果が変わらないことが定義から自明で実行の価値が無い | **採用**。候補 6 を削った（計画 6） |
| R3 | （回答 1 に含まれる洞察） | 事前トリム削除の安全性は「冪等性」という強い性質ではなく、**「trivia の無い文字列に trim をかけても no-op」**という自明な事実だけで成立する。74 行目の最終トリムがすべての分岐の出口を無条件に再トリムすることも効く | **採用**。前提表と D2-(b) と未検証の前提を、より弱い前提に書き換えた |

### 既に対応済み（1 件）

| # | severity | 指摘 | 対応 |
|---|---|---|---|
| R4 | medium（主指摘） | 決定 6 が `design-checklist.md:11`（#19 由来）と `:19`（#44 由来）に抵触する。`classification.rs:137` の「3 本目のレビューで実測」が転記の過程で落ちても `fmt`・`clippy`・`test` は全て exit 0 で通り、次に `skip_keyword` を「読みやすく」する人が `TBLPROPERTIES(...)` の誤判定を再発させうる。手順 3 を目視でなく grep で機械確認する形に具体化すべき | **D4-2 で既に対応済み**（実測記録の全件突き合わせ。レビューには D4 を反映する前の計画を渡していた）。加えて、**転記時に `実測` を含む行の文言をそのまま保つ**ことを計画 3 に明記した。これで突き合わせが完全一致で通り、消失と変質の両方を検出できる |

### レビューが裏取りした点（指摘ではないが計画の根拠になる）

- `target_table.rs:19-26` の `verb()` は `"DROP"` / `"ALTER"` しか返さず、他の値で `table_name_start` が呼ばれる経路は無い
- **軸 5（マルチバイト境界）は到達可能**。`DROP TABLE "日本語"` のとき `skip_keyword(rest, "IF")` に
  `"`(1B)+`日`(3B) が渡り `is_char_boundary(2)` が false になる。`metadata.rs:322-325`・`catalog.rs:417` でも
  多バイト識別子が実際のテスト対象になっている → **軸 5 とミューテーション候補 3 は削減しない**
- 呼び出しの書き換え漏れは、定義を削除すれば `cargo build` がコンパイルエラーで即検出する（doc の欠落と違い機械的に補足される）
- `skip_columns_keyword` の doc「長い方から試す」は境界判定ロジックが不変なので統合後も成立する

### design-checklist への追記

**なし**。採用した指摘に high が無く、R4 の「形」は既に `design-checklist.md:11`（#19 由来）と `:19`（#44 由来）に
書かれている（レビュー自身がその 2 項目を引用して指摘した）。既存の行で足りる。

### 再レビュー

**しない**。構造の観点で計画を書き直してはおらず、変更は削減 2 件と前提の弱化のみ。

## フェーズの記録

### フェーズ 1: 挙動固定テストの追加（2026-09-22 17:00 完了）

**7-0 の答え**: 消す・変えるガード無し／変える契約の呼び出し元無し／**増える分岐 0**（テストコードのみ）／
境界入力は計画 4 の 6 軸／design-checklist の該当項目はテスト件数の突き合わせ（検証 3 で対応済み）。

| | 結果 |
|---|---|
| red | 成立しない（挙動を変えないため）。代わりに**着手前のツリーで一時テストを流して期待値を実測**し、出力をそのまま焼き込んでから一時テストを消した（計画 4 の手順どおり） |
| green | `cargo test --locked` 275 → **281 passed**（A 側 3 件・B 側 3 件）。`fmt --check` ok / `clippy -D warnings` ok |
| 本体の差分 | **0 行**。`git diff -U0 src/` のハンクが両ファイルとも `mod tests {` の中の 1 つずつ（`@@ -345,0 +346,85 @@ mod tests` / `@@ -391,0 +392,131 @@ mod tests`） |
| mutation 候補 0 | `classification.rs:139` の `skip_leading_trivia(input)` を `input` に置換 → **B 側 FAILED・A 側 ok**。各経路が別の `skip_keyword` を通っていることの実行による裏取り。本体はスクラッチパッドのコピーから戻し、`git diff --stat` が追加 216 行のみに戻ることを確認 |

**計画から外れた点**: 軸 1 の B 側に入れた `ALTER /* c */ TABLE t ADD COLUMNS (c int)` は
実測で **None** だった（`ALTER_TABLE_ADD_COLUMN` を予想していた）。`words()` が `split_whitespace` で
語を数えるため `word(1)` が `/*` になり、`substatement_type` の guard で落ちて `alter_table_action` に
届かないため。**`skip_keyword` の手前の話なので統合の影響を受けない**。「到達しない」ことを固定する
意味があるのでテストには残し、理由をコメントに書いた。これは範囲外の発見にも積んだ。

### フェーズ 2: 統合（2026-09-22 17:09 完了）

**7-0 の答え**: 消すのは `skip_keyword` の 2 定義と `table_name_start` の事前トリム 4 箇所。
依存する呼び出し元は A 4 式・B 16 式で、すべて同じファイル内かつ定義を消せばコンパイラが検出する／
**増える分岐 0**（本体ロジックは一字一句同じものを 1 箇所に移すだけ）／境界入力はフェーズ 1 の 6 件がカバー／
design-checklist の該当項目は「前処理を共有関数に切り出すとき、判定が使う値をすべて列挙して前処理後・前処理前を
1 つずつ決めたか」（計画 8 で列挙済み）。

| | 結果 |
|---|---|
| red | 成立しない（フェーズ 1 のテストが代わり） |
| green | `cargo test --locked` **281 passed**（フェーズ 1 と同数＝**挙動が変わっていない**）。fmt ok / clippy ok |
| 検証 1 | `grep -rn "fn skip_keyword" src/` → `src/catalog.rs:130` の **1 件だけ** |
| 検証 2 | 実測記録の全件突き合わせ（着手前 `d4aa0f9` と現在）→ **128 行すべて一致**。doc の転記が文言レベルで成功した証拠 |
| 検証 3 | テスト総数 275 → 281 →（フェーズ 2 後も）281。減っていない |
| 検証 4 | `git diff d4aa0f9 -- src/` の削除された `///` 8 行を目視。**1 行だけ行き先が無かった**（`target_table.rs` 版 doc の「`TABLES` など」の例）ので、統合先の doc に「`TABLE` と `TABLES` を分ける」として補った。`実測` を含む行は触っていないので検証 2 は一致のまま |

**ミューテーション確認**（すべて生の出力で確認。最初に一括スクリプトで回したときは候補 5 の判定が誤っていた）

| # | 壊し方 | A 側（`target_table::tests`） | B 側（`classification::tests`） |
|---|---|---|---|
| 1 | 識別子境界の判定（`tail.starts_with(...)`）を消す | 軸 2 FAILED | 軸 2 FAILED |
| 2 | `eq_ignore_ascii_case` を `==` にする | 軸 4/5 FAILED | 軸 4/5 FAILED |
| 3 | `is_char_boundary` の判定を消す | 軸 4/5 **FAILED**（`DROP TABLE "日本語"` が踏む） | **7 件すべて ok**（B 経路のテスト入力に境界を割るものが無い。計画レビューの回答 3 と一致） |
| 4 | `catalog::skip_keyword` 本体の `skip_leading_trivia` を消す | 軸 1 FAILED | 軸 1 FAILED → **A・B が同じ 1 つの関数を通っている証拠。統合が成立したことの実行による証明** |
| 5 | `table_name_start` の最終トリムを消す | **17 件中 13 件 FAILED**（残る 4 件は元から `None` を期待するテスト） | 影響なし（単独確認で ok） |

**7-7 の判断**: 分割不要。新規ファイル 0・`catalog::skip_keyword` 14 行・`table_name_start` 9 行で、
1 ファイルに責務が 2 つ入ったわけでもない（むしろ**同じ分岐が 2 箇所に生えていたのを 1 箇所に減らした**）。
ファイル行数は `catalog.rs` 467→491、`classification.rs` 346→416、`target_table.rs` 392→510
（増分はすべてフェーズ 1 のテスト。本番コードは差し引き −29 行）。

**CLAUDE.md を変えない判断**: `skip_keyword` は CLAUDE.md に出てこない private ヘルパで、
29 行目（「文の種類の判定だけ `classification.rs` に置いて」）も 44 行目（「`catalog::skip_leading_trivia` で
読み飛ばしてから判定する」）も統合後に誤りにならない。#44 が CLAUDE.md を直したのは「移動元のファイル名を指す
記述が事実として誤りになった」ケースで、今回はそれに当たらない。

## レビュー実施記録

| 工程 | 回数 | 結果 |
|---|---|---|
| 計画レビュー（Step 6） | 1（Explore 1 本） | 指摘 3 件（medium 1・削減 2）。採用 3・既対応 1。high は 0 件 |
| 独立レビュー（Step 8） | 1（general-purpose 1 本） | **指摘なし**。本番影響のある妥当指摘 **0 件**。計画レビューの R3 と同じ洞察（最終トリムがすべての分岐の出口を再トリムする）に独立に到達していた |

## dismissed

（棄却した指摘と反証）

## deferred

| severity | file | 指摘 | 再トリアージ | 起票 |
|---|---|---|---|---|

## 覆した事前判断

| 元の判断 | 新しい判断 | 理由 | 影響 | ユーザーに聞いたか |
|---|---|---|---|---|
| quick の escalate 時に「implement-issue の移動・分割経路（正規化 diff 検証）で進める」と提示した | 小規模 issue の経路で進め、検証の形は計画で決め直す | move-refactor.md が「似た関数の統合」を経路の対象外として明示している（D1） | 進め方のみ。外部契約・プロダクト挙動・運用への影響なし | 聞いていない（リポジトリ内の前例から導出できる工学判断） |

## 実機検証

`.claude/issue-notes/39-e2e/verify.sh` を**改変せず**実行（2026-09-22 17:2x）。本物の Trino 482 + MinIO。
**全 17 項目 PASS、終了コード 0**。後始末（`docker compose down -v`）も走り、`docker ps` に issue39 の
コンテナは残っていない。作業ツリーもクリーン。

| # | ケース | 結果 | 通る経路 |
|---|---|---|---|
| 1 | DROP TABLE（Iceberg） | 本体 1B（改行）・`.metadata` 41B | A |
| 2 | DROP TABLE（Hive） | 本体 0B・`.metadata` 無し | A |
| 3 | DROP TABLE IF EXISTS（存在しない・Iceberg） | 本体 0B・`.metadata` 無し | A（`IF EXISTS` の読み飛ばし） |
| 4 | CREATE TABLE（Iceberg、回帰確認） | `.metadata` 無し | - |
| 5 | SELECT（回帰確認） | `.csv` 8B | - |
| 6前 | ADD COLUMNS の綴り | Trino の構文エラーで弾かれる（`MALFORMED_QUERY`） | - |
| 6 | ALTER TABLE ADD COLUMN（Hive） | 本体 0B・`.metadata` 38B | A・B |
| 7 | ALTER TABLE ADD COLUMN（Iceberg） | `.metadata` 無し | A・B |
| 8 | ALTER TABLE SET PROPERTIES（Iceberg） | `.metadata` 無し | B |

1・2・3 が `table_name_start`（A 経路）、6・7・8 が `alter_table_action`（B 経路）を、
どちらも本物の Trino の構文チェックまで通している。**統合による退行は無し**。
挙動を変えない issue なので新規ケースは足していない（計画 9 のとおり）。

バックエンドは `verify.sh` の中で `cargo build --release` から起動し直している（再起動の要件を満たす）。

## 範囲外の発見

| 内容 | 出どころ | 起票 |
|---|---|---|
| **動詞と `TABLE` の間にコメントを挟むと、2 か所の判定が食い違う**。`words()` が `split_whitespace` で語を数えるため `word(1)` が `/*` になり、`SubstatementType` が None に落ちる。**CTAS では結果ファイルの置き場所まで変わる**（`CREATE /* c */ TABLE t AS SELECT 1` は `ResultFile::Table`（`tables/<id>`）ではなく `Text`（`<id>.txt`））。一方 `parse_target_table` は `skip_keyword` で 1 語ずつ読むのでキーワード間のコメントを正しく読み飛ばし、同じ文から対象テーブルを解析できる。CLAUDE.md が書いている「判定が 2 か所にある」構造の帰結で、トリビアの扱いだけが揃っていない。**本物の Athena の挙動は未実測**（CLAUDE.md の 2026-09-18 実測は「先頭の」コメントについてのもの） | フェーズ 1 の期待値の実測（B03）＋ 起票前の追加実測（`DROP` / `CREATE` の腕と `ResultFile::of` を測った） | **#52** |

## 委譲の記録

| 工程 | 委譲 / 自分 | 自分でやった理由 |
|---|---|---|
| quick のゲート判定（Step 1-2 相当） | 自分 | quick-implement の規則（原因の当たりを付ける読み取りは Explore に投げない） |
| Step 3 の調査 | 委譲（Explore 2 本、sonnet） | - |
| Step 5 の計画 | 委譲（Plan 1 本、sonnet） | - |
| Step 6 の計画レビュー | 委譲（Explore 1 本、sonnet） | - |
| 着手前の基準の確認（fmt/clippy/test） | 自分 | コマンド 2 本。指示を書く方が長い |
| フェーズ 1・2 の実装 | 自分 | 小規模 issue の経路。本番コードの差し引きが −29 行で、委譲 1 回の固定費の方が長い |
| Step 8 の独立レビュー | 委譲（general-purpose 1 本、sonnet） | - |
| Step 9 の実機検証 | 自分 | 既存の `39-e2e/verify.sh` を改変せず 1 コマンド走らせるだけ |
| Step 10 の PR 本文 | 自分 | 小規模 issue の経路（ノートからの転記） |

## 出荷

| | |
|---|---|
| PR | **#53** https://github.com/aoyagikouhei/athena-local/pull/53 |
| 起票した issue | **#52**（動詞と `TABLE` の間のコメントで 2 か所の判定が食い違う。CTAS は結果ファイルの置き場所も変わる） |
| issue コメント | **投稿なし**（deferred 0 件） |
| 出荷判定 | 小規模 issue の経路の 3 条件がすべて揃った: 検証 green（**281 passed**・fmt・clippy）／7-5b の分岐の突き合わせで抜け 0（統合で増えた分岐が 0）／実機 **全 17 項目 PASS**。加えて独立レビューも指摘なしで返った |
| 予算 | 実装コミット 2（上限 4）／レビュー対応 0（上限 5）／docs 単独 2（上限 2。quick ノートは別枠）／レビュー起動 2（上限 4）／計画の敵対的レビュー 1（上限 1）。**すべて上限内** |

ノートは圧縮済み（549 → 385 行）。落としたのは quick のゲートの 6 項目・置き場と可視性の判断の根拠
（D2 と計画 2 に反映済み）・実施済みの手順とケース表（結果がフェーズの記録にある）。

## 時系列

| 時刻 | 出来事 | SHA |
|---|---|---|
| 09-22 15:29 | quick-implement 着手。issue #49 を読む | - |
| 09-22 15:39 | ゲート判定: 事実（red 不成立）と影響範囲が外れた | - |
| 09-22 15:40 | ユーザーが「implement-issue で進める」を選択。`main-49` を作成 | - |
| 09-22 15:41 | quick ノートを 1 コミットで残し、issue にコメント | `25ec0bf` |
| 09-22 15:43 | implement-issue 起動。move-refactor が対象外と判明（D1）。Step 3 の Explore 2 本を起動 | - |
| 09-22 16:11 | Explore 2 本 → Plan 1 本 → 計画レビュー 1 本が順に完了。着手前の fmt / clippy / test が全 green（275 passed）であることを自分で確認 | - |
| 09-22 16:13 | 計画レビューを反映（削減 2 件・前提の弱化・doc 転記の明文化）。Step 6b の提示へ | - |
| 09-22 17:00 | Step 6b でユーザーが案 B（完全統合）を承認。フェーズ 1（挙動固定テスト）を実装し、281 passed・本体 0 行差分・mutation 候補 0 が期待どおりを確認 | `eb3c3f8` |
| 09-22 17:09 | フェーズ 2（統合）。検証 4 項目と mutation 候補 1-5 をすべて確認。検証 4 が doc の取りこぼし 1 行を拾ったので補った | `d5b6efc` |
| 09-22 17:2x | 独立レビューを起動し、並行して実機検証（39-e2e/verify.sh）を実行 → **全 17 項目 PASS**。範囲外の発見を追加実測して **#52** を起票 | - |
| 09-22 17:14 | 独立レビューが**指摘なし**で返る。出荷判定の 3 条件が揃ったので push して **PR #53** を作成 | - |
| 09-22 17:2x | ノートを圧縮（549 → 385 行）して出荷記録を残す | - |
