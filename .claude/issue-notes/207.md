# quick #207 未実測の引用符付きの名前の形を本物で測り、#204 の一致条件を広げる
- 入力: issue #207。モード: standalone。ブランチ main-207、着手前 SHA 764eefe
- ゲート: サイズ 見込み 50 行超・1〜2 ファイル（≤50・3）／呼び出し元 1（execution.rs:81。≤5・同一モジュール）／契約 通過（本物に合わせる文言）・新規性 外れ（存在の確認に Trino への別の問い合わせ）・事実 外れ（本物の実測が要る）・判断 外れ（#204 の「非 ASCII は弾かない」の前提が実測で崩れた）
- 時系列: 着手 19:19 ／ 実測スクリプト（ROUND=3）コミット 19:31 ／ 実測 19:47〜19:52 ／ escalate 19:55（red・green には入っていない）
- 検証: 未実施（コードに触る前に escalate）。実測: 1 ラウンド（ROUND=3、run-20260925-104713）、スクリプト作成を general-purpose 1 本に委譲。測る形はすべて手元の Trino の PREPARE を通り、quoted_names の判定まで届くことを確認済み
- 自己レビュー: 未実施（コード変更なし）
- 打ち切り・escalate: はしご（事実 → 実測工程）→ 実測の結果で判断が外れ 1 問聞いた（答え: 存在の確認まで今回実装）→ 新規性が外れ提案 → 選択: implement-issue で進める
- 予算の実績: Bash 約 25 本（うち実測 約 10 本）、所要 約 36 分（待ち時間を除くと約 25 分）、サブエージェント 1 本
- 出荷: ブランチ main-207（implement-issue に引き継ぎ）

## 引き継ぎ

### 実測の結果（ROUND=3、2026-09-25、`tools/measure/quoted-names.sh`、生データ `~/athena-quoted-names-measurements/run-20260925-104713`）

表記: NV(from…to @ at) = `line L:C: no viable alternative at input '<from から to までの原文>'`（位置 at）、MM(p) = `line L:C: mismatched input '<p>' expecting {…#204 と同じ一覧…}`。コードはどれも MALFORMED_QUERY（Entity Not Found だけ INVALID_INPUT）。

- V1 3 部の ALTER の途中が引用符付き（`ALTER TABLE awsdatacatalog."db".nope RENAME TO …`／`DROP COLUMN`、`awsdatacatalog."db"."nope"` も）: NV(文の最初の語 … 引用符付きの部分 @ 引用符付きの部分) = `line 1:28: … 'ALTER TABLE awsdatacatalog."db"'`。ALTER の一般の規則どおり → `quoted_names.rs` の `(AlterTable, 3, 1) => None` の腕を消せばよい
- V2 4 部の修飾名（`awsdatacatalog.db.t.n`、引用符を 1〜4 部目のどれか 1 つに、無引用の対照）:
  - DESCRIBE・SHOW COLUMNS FROM: 5 形とも（無引用も）`Invalid table name awsdatacatalog.db.t.n`（引用符を外した名前。MALFORMED_QUERY）。名前の検査が存在の確認より先（t は実在、4 部の名前は実在しない）。無引用の部分の大文字小文字・`""` を含む引用符付きの部分・5 部以上・DESC・SHOW COLUMNS IN は未実測
  - DROP TABLE: 引用符が 1〜3 部目にあれば 3 部の規則どおり（p1 MM(p1)、p2 NV(名前の始まり … p2)、p3 MM(p3)）。無引用・4 部目だけ引用符付きは `line 1:67: mismatched input '.' expecting {<EOF>, 'PURGE'}`（3 つ目の `.` の位置）
  - ALTER TABLE（RENAME TO）: 引用符が 1〜3 部目にあれば一般の規則（NV(文の最初の語 … 引用符付きの部分)）。無引用・4 部目だけは `line 1:68: no viable alternative at input 'ALTER TABLE awsdatacatalog.db.nope.'`（3 つ目の `.` の位置、input は文の最初の語からその `.` まで）
- V3 SHOW TABLES IN の 2 部: 無引用 `awsdatacatalog.db` は SUCCEEDED（SHOW_TABLES）。`"awsdatacatalog"."db"`・`"awsdatacatalog".db` は MM(p1) @1:16、`awsdatacatalog."db"` は MM(p2) @1:31 → 最初の引用符付きの部分で MM
- V3 CTAS でない CREATE TABLE: 無引用 `db.nope3`・`IF NOT EXISTS nope3` は `No location was specified for table. An S3 location must be specified`（範囲外、caveats 既載）。引用符付きは 2 部・3 部とも NV(文の最初の語 … 最初の引用符付きの部分 @ その部分): `"db"."t"`→`'CREATE TABLE "db"'` @1:14、`db."t"`→`'CREATE TABLE db."t"'` @1:27、`awsdatacatalog."db".t`→@1:29、`awsdatacatalog.db."t"`→@1:42。`CREATE TABLE IF NOT EXISTS "t" (n int)` は `line 1:28: no viable alternative at input 'CREATE TABLE IF NOT EXISTS "t"'`（IF NOT EXISTS を含めて文の最初の語から）。いまは `target_table::table_name_start` が `IF NOT` で None を返すので判定まで来ない
- V4 実在しない ASCII の名前: `DESCRIBE "nope"`・`DESCRIBE db."nope"`・`DESCRIBE "db".nope`・**無引用の `DESCRIBE nope`**・`SHOW COLUMNS FROM "nope"` はすべて `Entity Not Found (Service: AmazonDataCatalog; Status Code: 400; Error Code: EntityNotFoundException; Request ID: <毎回違う UUID>; Proxy: null)`、AthenaErrorCode INVALID_INPUT（開始時に弾く）
- V5 実在しない非 ASCII の名前 "日本": DESCRIBE・DESC・`DESCRIBE db."日本"`・SHOW COLUMNS FROM は Entity Not Found（INVALID_INPUT）。DROP TABLE は MM @1:12、ALTER は NV `'ALTER TABLE "日本"'` @1:13、SHOW CREATE TABLE は `Queries of this type are not supported`、SHOW TABLES IN は MM @1:16、CREATE TABLE は NV `'CREATE TABLE "日本"'` @1:14 → DESCRIBE・SHOW COLUMNS 以外は一般の規則どおり（非 ASCII でも）
- V6 実在する非 ASCII の名前（CTAS で `db."t_日本"` を作れた）: `DESCRIBE "t_日本"` は NV @1:10、`DESCRIBE db."t_日本"` は NV `'db."t_日本"'` @1:23、`SHOW COLUMNS FROM "t_日本"` は MM @1:19、バッククォートは SUCCEEDED（DESCRIBE_TABLE）→ 実在すれば非 ASCII でも一般の規則
- V7 S3 Tables（対照 SELECT が通った状態で再確認）: DESCRIBE・DESC・SHOW COLUMNS・実在しない表への DESCRIBE・全部引用符付きの DESCRIBE は `Unsupported DDL with 2 catalogs`、SHOW CREATE TABLE は `Queries of this type are not supported`、DROP（実在しない）は MM(p1)、ALTER（実在しない）は NV → ラウンド 1・2 と同じ。別名の検査が存在の確認より先
- 生データの読み直し（ラウンド 2 `u2-ifq`・`u2-ifu`）: `ALTER TABLE IF EXISTS "t"` は無引用と同じ `line 1:16: no viable alternative at input 'ALTER TABLE IF EXISTS'`。無引用と同じく範囲外の差（caveats 既載）なので unmeasured.md から消すだけ

### 判定の順序（本物、DESCRIBE・SHOW COLUMNS）
S3 Tables の別名（2 catalogs）→ 4 部の名前（Invalid table name）→ 存在の確認（Entity Not Found、無引用でも）→ 構文（NV／MM）。SHOW CREATE TABLE・DROP・ALTER・SHOW TABLES IN・CREATE TABLE は存在の確認をせず構文だけ（実在しない "日本" でも構文の文言）

### 決まったこと（ユーザーの判断、2026-09-25）
- DESCRIBE・SHOW COLUMNS の存在の確認まで今回実装する（Trino に別のクエリを投げてテーブルの有無を確かめ、無ければ Entity Not Found を INVALID_INPUT で返す。Request ID は毎回違う UUID）。#204 の「非 ASCII は弾かない」の除外は外す

### 判定箇所・既存機構
- 一致判定: `src/operation/quoted_names.rs` の `rejection`（呼び出し元は `src/operation/execution.rs:81` だけ。構文チェックの後、`invalid_request_with_code(message, "MALFORMED_QUERY")`）
- 名前の始まり: `src/operation/target_table.rs:77` の `table_name_start`（`IF EXISTS` を読み飛ばす。`IF NOT EXISTS` は None）
- 修飾名の字句: `athena_sql::Cursor::qualified_name`（parts と各部の start/end、`value()`）
- 別名: `app.config.catalog_map`（`TRINO_CATALOG_MAP`）。存在の確認でカタログ・スキーマを決めるときは QueryExecutionContext の既定（`context_defaults`）と別名の置換（`catalog.rs`）を考える
- ユニットテスト: `src/operation/quoted_names/tests.rs`（`rejected`・`nv`・`mm` のヘルパ）

### 未実測・未確認
- 4 部の `Invalid table name` で、無引用の部分の大文字小文字をそのまま出すか小文字にするか。`""` を含む引用符付きの部分の出し方。5 部以上。DESC・SHOW COLUMNS IN の 4 部（ラウンド 1・2 ではどの形も DESCRIBE・FROM と同じだった）
- 4 部の DROP TABLE IF EXISTS（無引用・4 部目だけ引用符付き）の expecting の一覧
- 存在の確認で、スキーマ（DB）が実在しないとき・ビューのとき・大文字の名前のときの文言
- `SHOW TABLES IN` の 3 部

### 再現手順
- 実測: `tools/dev.sh OUTPUT=s3://<bucket>/<prefix>/ DB=<db> ROUND=3 S3TABLES_CATALOG=… S3TABLES_NS=… S3TABLES_TABLE=… bash tools/measure/quoted-names.sh`
- 手元: 形はどれも Trino の `PREPARE athena_local_syntax_check FROM\n<sql>` を通る（`docker compose -f compose.yml up -d trino` の後、toolbox から curl で確認した）
