# #115 JDBC の e2e 足場 3 本の環境関数の重複と、古い前提の整理

## 基本情報
- ベース: main。ブランチ: main-115。着手前 SHA: fc3ffa9（ノート削除コミット。main は 6c0dbdf）
- 経路: 小規模 issue の経路（似た関数の統合。move-refactor.md「似た関数を統合するとき」）

## 事前に確認した事実
Explore 2 本の返答から、計画と 7-0 が参照する事実だけを写す（来歴の詳細・参照文書の全件・出力形の説明は落とした。git log と grep で引ける）。
1. decisions.md:47（#128）「足場の共通の lib は作らない」。根拠は lib.sh:2-4 と #128 ノートの計画レビュー 8。#115（#111 で起票）はこれより前に起票された
2. 3 本とも CI に入っていない（decisions.md:50）。3 本は jdbc-client/target を共有するので同時に 1 本だけ（development.md:71）
3. 環境まわりの変更（#98・#127・#128・#129）は毎回 3 本を並べて同じように直していた（b476a73・3da9706・4413a40・2c0ef1a）
4. lib.sh は record・OUT_ROOT を呼び出し元に任せ、成功時は record しない。measure の 2 本は各段で PASS を record する
5. 意味のある差（lib.sh を正とする側）: prepare_driver は版を引数に取り、失敗は DRIVER_SKIP_REASON と return 1 で返す。3.1.0 以前向けに AthenaDriver.class でも判定する。run_jvm は 6 引数で timeout（JVM_TIMEOUT 既定 300）に包み、ハング時に使い捨てコンテナを消す。判定はしない。compose run は -T と </dev/null。compose_up は retry と 1 record。起動確認は ListWorkGroups の応答に WorkGroups があるか
6. measure 側だけにあるもの: trino_exec が PARAMETRIC_DATETIME を送り、失敗時に error を stderr に出す。run_jdbc_case は proxy_ip が空なら FAIL。build の失敗時に tail -n 30。PASS の record
7. 名前空間の差: lib.sh は PREFIX_ROOT=e2e-jdbc111 で、起動時の OUTPUT_LOCATION に /default/ を付ける。measure は PREFIX=e2e-jdbc。ログの接頭辞・TRINO_USER・X-Trino-User は issue 番号入り。OUT_ROOT は lib 系が /tmp の mktemp、measure はホストの ~/athena-local-issue{46,57}-measurements
8. 古い前提（issue の 3 項目目）: jdbc-show-metadata.sh:386-388 の INFO は実測 9 個（基準値の実行で確認）。check_s3_access（show:392-406、metadata:385-399 と同一コード）は .metadata の部分一致で、PREFLIGHT の .csv.metadata だけでも満たされる。基準値の実行での GET の内訳は txt 4・csv 6・拡張子なし 2（計 12）。judge.sh:96 は .txt.metadata に絞っている
9. source の先例: minio/verify.sh:46（cases-dml-retention.sh）、python-clients/verify.sh:182（env.sh）、jdbc-drivers/verify.sh:23-25（lib.sh と judge.sh）。いずれも同じディレクトリの中
10. 参照している文書: docs/ddl.md:27、docs/dev/development.md:65-71、docs/dev/measurements/clients.md（記録なので書き換えない）、decisions.md:33・41・47・50、tools/e2e/minio/README.md:45-51、tools/e2e/jdbc-drivers/README.md、tools/compose/jdbc-client/pom.xml:18
11. 移動前の基準値（2026-09-24 04:01-04:07、SKIP_BUILD は 2 本目以降）: jdbc-metadata PASS 15（rc 0）、jdbc-show-metadata PASS 17・INFO 2・SKIP 1（rc 0）、jdbc-drivers FAIL 0（rc 0）。ログは scratchpad の b115/

## 設計判断
- 共通 lib に寄せる（ユーザーの回答 2026-09-24「lib に寄せる（推奨）」）。decisions.md:47 は「jdbc 系 3 本は例外」と書き換える。理由: 3 本は同じ環境（trino・minio・tls-proxy・jdbc-client）を使い、環境の変更のたびに 3 本を並べて直してきた（事実 3）
- 差がある関数は lib.sh の版を正にする（新しく、版の引数・timeout・-T を持つ）。measure の 2 本の出力行と細かい挙動は変わってよい。判定の行（JDBC の各ケース、.metadata、S3、行数の突き合わせ）の結果は前後で同じでなければならない

## 計画
Plan の下書き（項目 1〜12）から転記。落とした番号: なし。判断で直した箇所: 3 の URL（`jdbc:athena://` に揃える案をやめ、空で渡して Main.java の既定 `jdbc:awsathena://` のままにする。判定に関係しない出力も変えない方が前後比較が素直）、9 の check_s3_access の絞り方（下記）。

### 置き場所と呼び出し規約（1・2）
- lib は `tools/e2e/jdbc-drivers/lib.sh` のまま。measure の 2 本は `. "$REPO_ROOT/tools/e2e/jdbc-drivers/lib.sh"` でディレクトリをまたいで source する（先例は同じディレクトリだけ。新しい形）。tools/compose/jdbc-client は maven の /work に bind されるので置かない
- 呼び出し元ごとに変わる値は `${VAR:-既定}` にし、呼び出し元が source の前に代入する: PREFIX_ROOT（既定 e2e-jdbc111）、TRINO_SETUP_USER（既定 issue111-setup）、ATHENA_TRINO_USER（既定 athena-local-issue111）。verify.sh は何も代入しないので出力は 1 文字も変わらない。log は lib の `[jdbc-drivers]` 固定のままにし、measure は自前の log を source の後に定義して上書きする（計画レビュー 2）
- record と OUT_ROOT は今どおり呼び出し元が用意する。lib は成功時に record しない

### measure 2 本（3・4）
- preflight → preflight_tools、prepare_cert・build_athena_local・start_athena_local・build_jdbc_client は lib の同名、trino_exec・wait_for_trino・mc_run・wait_for_bucket と compose のブロック → compose_up（段ごとの PASS 4 行が 1 行になる）
- prepare_driver "3.8.1"。失敗は呼び出し元で `record "ドライバ取得" FAIL "$DRIVER_SKIP_REASON"; exit 1`（measure は版が 1 つなので SKIP にしない）
- run_jdbc_case は JVM の実行を `run_jvm 3.8.1 "$fetcher" <46|57> "$OUTPUT_LOCATION" "" "$out"` に置き換え、rc と failures= の判定と record は残す。URL は空で渡して既定のまま
- measure 側だけの良いものは lib に入れる: trino_exec の PARAMETRIC_DATETIME とエラーの stderr 出力（retry が出力を捨てるので verify.sh の出力は変わらない）、run_jvm の proxy_ip が空のときの return（record しない）、build 失敗時の tail -n 30
- 証跡の置き場（~/athena-local-issue{46,57}-measurements）と PREFIX=e2e-jdbc は変えない（PREFIX_ROOT=e2e-jdbc を代入）

### 古い前提 2 点
- show の INFO を「$partitions の SELECT × 3 + 接続テストの SELECT × 3 + PREFLIGHT の SELECT 1 × 3 = 9 のはず」にする
- check_s3_access: show は `.txt.metadata` への GET に絞る（基準値で 4 件）。metadata は DROP・ALTER の置く拡張子なしの `<id>.metadata`（UUID の直後が .metadata）への GET に絞る（基準値で 6 件）。文言に「.txt.metadata」「拡張子なし」と何を数えたかを書く（compare.py がその文言を見る）

### ファイル構成（5）
| ファイル | 責務 | 見込み |
|---|---|---|
| tools/e2e/jdbc-drivers/lib.sh | jdbc 系 3 本の環境関数 | 214 → 約 250 行 |
| tools/measure/jdbc-metadata.sh | #46 の判定と流れ | 446 → 約 200 行 |
| tools/measure/jdbc-show-metadata.sh | #57 の判定と流れ | 515 → 約 300 行 |
| verify.sh・judge.sh | 変えない | |
判定の関数（collect_metadata・check_s3_access）の 2 本間の重複は範囲外（issue は環境関数の重複）。

### フェーズ（6）
1. lib.sh に呼び出し規約と measure 側の良いものを入れる。検証: jdbc-drivers を流して compare.py の drivers 行が全一致
2. measure 2 本を lib に寄せ、古い前提 2 点を直し、文書を追随させる。検証: 3 本を流して compare.py が NG 0

### 前提表（7）
| 前提 | 破らないか | 保証元 |
|---|---|---|
| verify.sh は lib → judge の順で source | 変えない | verify.sh:22-25 |
| judge.sh は lib の dc・mc_run・athena_call を使う | 名前を変えない | judge.sh:3、47・50・61・117 |
| cleanup は ATHENA_PID を見る（set -u） | lib が `ATHENA_PID=""` を代入し続ける | lib.sh:23 |
| jdbc-client/target を 3 本で共有 | 排他は足さない | development.md:71 |
| retry は出力を捨てる | trino_exec を足しても verify.sh の出力は変わらない | lib.sh:30-38 |
| run_jvm は判定しない | proxy_ip のガードも record しない | lib.sh:192-214 |

### 一致台帳（8）
SERVICES・BUCKET・DRIVER_MOUNT・DRIVER_CACHE_DIR・TRINO_BASE・ATHENA_BASE・BINARY・/etc/hosts の書式は lib の 1 か所になる。呼び出し元ごとに違ってよいのは PREFIX_ROOT・log の接頭辞（measure は自前の log で上書き）・TRINO_SETUP_USER・ATHENA_TRINO_USER・OUT_ROOT。

### 検証（9）とミューテーション（10）
- 足場: `.claude/issue-notes/115-verify/compare.py <前> <後>`。前は scratchpad の b115/
- ミューテーション: (a) measure の PREFIX_ROOT の代入を外す → .metadata 回収が落ちる (b) show の check_s3_access を .metadata の部分一致に戻す → compare.py の「S3 直読みの裏取り」の期待（.txt.metadata と書く）で NG (c) prepare_driver に取れない版を渡す → measure が FAIL で止まる (d) metadata の check_s3_access を部分一致に戻す → compare.py の「拡張子なし」の期待で NG（計画レビュー 1）

### 文書（11）
decisions.md:47 に「例外: jdbc 系 3 本は lib.sh を共有する（#115）」を足す。lib.sh:2-4 の冒頭コメント、jdbc-drivers/README.md:19、tools/e2e/minio/README.md の該当行、development.md の該当行を事実に合わせる。measurements と docs/ddl.md は変えない。挙動は変わらないので CHANGELOG・README は変えない（decisions.md:24）

### 未検証の前提（12）
- ディレクトリをまたぐ source は先例が無い（CLAUDE.md に禁止は無い）
- .metadata 回収のバイト数の並びが安定か → compare.py で並べ替えて比べるので不問
- pom.xml:18 のコメントが今の実装（ドライバを bind する）と合っていない → 範囲外。実装時に見て、事実と違えば起票する
- preflight_tools の「同じプロジェクトに別の dev」チェックが measure で誤検知しないか → フェーズ 2 の実行で確かめる

## 計画レビューの結果
Explore 1 本（小規模経路）。2 件とも採用。
1. [medium] compare.py の metadata 側「S3 直読みの裏取り」の期待が件数だけで、絞り込み漏れ（部分一致のまま）でも ok になる → 詳細に「拡張子なし」を求める形に締め、ミューテーション (d) を足した
2. [削減] LOG_PREFIX は measure が自前の log で上書きするので使われない → 計画から外した
design-checklist への追記: なし（1 は足場の期待の非対称で、既存項目「全項目が失敗フラグを立てるかを、わざと壊して確かめる」の範囲）

## フェーズの記録
（未着手）

## レビュー実施記録
（未着手）

## dismissed
（未着手）

## 覆した事前判断
（未着手）

## 実機検証
（未着手）

## 範囲外の発見
（未着手）

## 委譲の記録
| 工程 | 委譲 / 自分 | 理由 |
|---|---|---|
| Step 3 調査 | Explore 2 本（sonnet） | |
| 基準値の実行 | 自分 | コマンド 3 本を流すだけ |
| 前後比較の足場 | 自分 | Plan の待ち時間。基準値のログを読みながら書く方が指示より短い（80 行） |
| Plan | Plan 1 本（sonnet） | |
| 計画レビュー | Explore 1 本（sonnet） | |

## 時系列
- 04:00 着手（main-115 を切り、過去のノートを消した。fc3ffa9）
- 04:01 Explore 2 本を起動。先行作業: 移動前の基準値として jdbc-metadata・jdbc-show-metadata・jdbc-drivers を順に実行
- 04:07 基準値の実行が終わる（3 本とも rc 0）
- 04:12 Explore 2 本が返る。decisions.md:47 と衝突するのでユーザーに聞き、「lib に寄せる」の回答
- 04:14 Plan を起動。先行作業: 前後比較の足場 `.claude/issue-notes/115-verify/compare.py` を作る
- 04:18 足場の受け入れ: 基準値どうしで NG 2（意図して変える 2 行が変更前の形のため。想定どおり）。rc・判定の行 2 つ・drivers の行の消失をわざと壊すと 4 件とも NG
- 04:19 計画を転記し、計画レビュー（Explore 1 本）を起動。先行作業: 無し（足場は作り終えた）
- 04:30 計画レビューが返る（2 件とも採用）。6b: 人間の判断が要る論点（共通 lib の可否）は 04:12 に回答済みで、残りは 0 件のため提示のみで進む
