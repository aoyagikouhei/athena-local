# quick #20 ATHENA_LOCAL_OUTPUT_LOCATION が無いとき起動時に警告を出す
- 入力: issue #20。モード: standalone。ブランチ main-20、着手前 SHA 5188739
- ゲート: サイズ 21 行・本番 2 ファイル（≤50・3、テストと docs は別枠）／呼び出し元 1（≤5・新規関数なので main.rs だけ）／契約 通過（API の形は変えず stderr に 1 行足すだけ。環境変数も足さない）／新規性 通過（新ファイル無し・共有状態に触らない）／事実 通過（危険は #2 のケース 4-2 で実測済み、README の Caveats に記載済み）／判断 **外れた**（issue が「none のときも出すかは着手時に決める」と保留）→ AskUserQuestion で 1 問聞き「両方で出す（共通文言）」を選択。根拠: 危険の条件は「GetWorkGroup が OutputLocation を返さないこと」で ResultsMode::None でも同じ
- 時系列: 着手 21:02 ／ red 21:04 ／ green 21:05 ／ コミット 21:08（1 問聞く層: 21:03〜21:04）
- 検証: `cargo fmt --check` ok・`cargo clippy --all-targets --locked -- -D warnings` ok・`cargo test --locked` ok（220 passed / 0 failed）／ミューテーション: `location.is_none()` → `is_some()` に反転 → 追加した 2 本とも落ちた／実機: `./target/debug/athena-local` を 3 通りの設定で起動し、none・s3 かつ OUTPUT_LOCATION 無しでは警告が出て、OUTPUT_LOCATION 有りでは出ないことを確認
- 自己レビュー: 分岐 3 件（None／S3(None)／S3(Some)）すべてユニットテストが踏む。main.rs の `if let Some` は実機起動で確認／エラーの出口: 新しいエラー戻り値は無く、起動も止めない（stderr に 1 行）／呼び出し元 1 箇所（main.rs:28）を確認、既存の起動ログの直後に並ぶ
- 打ち切り・escalate: はしご（判断 → 1 問聞く）。escalate 無し
- 予算の実績: Bash 約 12 本、所要 6 分（待ち時間を除く）、コミット 1、サブエージェント 0 本（止めない独立レビューは別途）
- 出荷: standalone、ブランチ main-20
