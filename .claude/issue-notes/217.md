# quick #217 実在しない Context の Catalog で INSERT・ALTER TABLE・CREATE TABLE などが本物でどうなるかを測る（#214 の残り）
- 入力: issue #217（最初に指定された #226 は存在せず、ユーザーが #217 を選んだ）。モード: standalone。ブランチ main-217、着手前 SHA d538ae8
- ゲート: サイズ 本番 20 行・1 ファイル（≤50・3）／呼び出し元 2（execution.rs の開始時と run。同一モジュール）／契約: 無し・新規性: 無し・判断: 実測で決まる → 事実が外れた（本物の実測が要る）
- 時系列: 着手 03:40 ／ はしご（実測）開始 03:42・スクリプトのコミット 04:05・ユーザーの実行 04:07〜04:12 ／ red 04:13 ／ green 04:13 ／ コミット 04:16
- 検証: `cargo fmt --check`・`cargo clippy --all-targets --locked -- -D warnings`・`cargo test` ok（455 passed）／ミューテーション: `RESOLVED_STATEMENTS` から `DROP_VIEW` を消す → `DROP VIEW IF EXISTS v` で落ちた／実測: 1 ラウンド（75 本、run-20260925-190731）。手元の Trino で到達性を 2 回確かめた（Hive の書き方の DDL は構文エラー、DROP VIEW / DROP SCHEMA IF EXISTS は黙って何も消さない）
- 自己レビュー: 分岐の追加なし（定数の追加だけ。足した 7 種のうちテストで踏むのは 7 種すべて、差し替えない INSERT・DELETE も固定）／エラーの出口: 変化なし／呼び出し元 2 箇所: 開始時は差し替え後の名前を entity_check に、run は format_probe と本体に渡す。どちらも Trino 側の名前で期待どおり
- 打ち切り・escalate: はしご（事実 → 実測工程。計画攻撃は本番 1 ファイルなので足さない）。実測スクリプトの INFO 行が実名を伏せていなかったのでこのコミットで直した（生データはリポジトリの外）
- 予算の実績: Bash 約 30 本（うち実測 約 8 本）、所要 36 分（待ち時間を除く）、サブエージェント 1 本（実測スクリプト）＋止めない独立レビュー 1 本
- 出荷: main-217
