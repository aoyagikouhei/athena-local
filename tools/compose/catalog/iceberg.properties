connector.name=iceberg
iceberg.catalog.type=TESTING_FILE_METASTORE
# local.location（既定 /tmp）が根。file:// で書くと CREATE SCHEMA は通るのに SHOW SCHEMAS・system.jdbc.schemas に出ない（#233、482 で確認）。
hive.metastore.catalog.dir=local:///iceberg
fs.local.enabled=true
